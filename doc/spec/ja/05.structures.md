# 5. 構造物

> Canonical language: English. English canonical version: [../en/05.structures.md](../en/05.structures.md).

この章では、複合型 (構造体) がどのように宣言、継承、インスタンス化されるかを定義します。

- [5. 構造](#5-構造物)
  - [5.1 概念と目的](#51-概念と目的)
  - [5.2 構文](#52-構文)
  - [5.3 継承](#53-継承)
    - [5.3.1 構文 (EBNF)](#531-構文-ebnf)
    - [5.3.2 コヒーレンス (型の特殊化)](#532-コヒーレンス-型の特殊化)
  - [5.4 多重継承とダイヤモンド継承](#54-多重継承とダイヤモンド継承)
  - [5.5 コンストラクター](#55-コンストラクター)
    - [5.5.1 デフォルトのコンストラクター](#551-デフォルトのコンストラクター)
    - [5.5.2 コンストラクターの変換](#552-コンストラクターの変換)
  - [5.6 依存構造](#56-依存構造)
  - [5.7 フィールドアクセス (セレクター)](#57-フィールドアクセス-セレクター)
  - [5.8 論理エンコーディングの詳細](#58-論理エンコーディングの詳細)
    - [5.8.1 述語としての構造](#581-述語としての構造)
    - [5.8.2 関数およびタイプガードとしてのフィールド](#582-関数およびタイプガードとしてのフィールド)
    - [5.8.3 含意とレダクト関数としての継承](#583-含意とレダクト関数としての継承)
    - [5.8.4 コンストラクターの公理](#584-コンストラクターの公理)
    - [5.8.5 拡張性](#585-拡張性)
  - [5.9 完全な構文 (EBNF)](#59-完全な構文-ebnf)

## 5.1 概念と目的

構造体は、名前付きフィールドとプロパティを持つ複合型を定義するために使用される基数型です。これらは次の 3 つの目的を果たします。

1. **データのカプセル化**: 論理的に関連するコンポーネントをグループ化する
2. **型形成**: 構造定義による新しい型の作成
3. **数学モデリング**: 代数的および解析的構造の表現

## 5.2 構文

**構文 (EBNF)**:

```ebnf
struct_def    ::= "struct" struct_def_name [ type_params ] "where"
                  struct_member { struct_member } "end" ";" ;
struct_member ::= field_decl | property_decl ;
field_decl    ::= "field" identifier "->" type_expression
                  [ ":=" term_expression ] ";" ;
property_decl ::= "property" identifier "->" type_expression ";" ;
type_params   ::= ( "of" | "over" ) type_parameter_list
                | "[" type_parameter_list "]" ;
type_parameter_list ::= identifier { "," identifier } ;
struct_def_name ::= constructor_name ;
```

* `field`: 組み込みコンポーネント (保存されたデータ) を表します。
* `property`: 一意に決定された派生値を表します。それらの定義条件は、モード宣言を介して外部で確立されます (第 7 章を​​参照)
* `struct_def_name` には、通常の識別子、または `1-sorted`、`2-sorted`、
  `R-module`、`C-star-algebra` のような読みやすいハイフン区切りのコンストラクタ名を
  使用できます。演算子風の句読点は構造体名には使えません。フィールド名、
  プロパティ名、変数、ラベル、セレクタは通常の識別子のままです。
* bracket 形式 `Name[T]` は canonical なtemplateパラメータ表記です。宣言が提供する場合、`of` および `over` 形式は短縮形として受け入れられます。

**例**：

```mizar
definition
  struct UnitalMagmaStr where
    field carrier -> set;
    field binop -> BinOp of carrier;
    property unit -> Element of carrier;
  end;
end;
```

## 5.3 継承

構造は、`inherit` を介して他の構造から継承できます。継承する場合、フィールドとプロパティをより特定の型に特化 (絞り込む) できます。

### 5.3.1 構文 (EBNF)

```ebnf
inherit_def     ::= "inherit" inherit_child "extends" parent_type
                    [ "where" inherit_member { inherit_member }
                      [ coherence_block ]
                      "end" ] ";" ;

inherit_child   ::= struct_name [ type_args ] ;
parent_type     ::= struct_name [ type_args ] | "set" ;

inherit_member  ::= field_redef | property_redef ;

(* name may differ from parent (renaming); type is optional if unchanged from parent *)
field_redef     ::= "field" identifier [ "->" type_expression ] "from" (identifier | "it") ";" ;
property_redef  ::= "property" identifier [ "->" type_expression ] "from" identifier ";" ;

coherence_block ::= "coherence" justification ";" ;
```

* **短縮形式** (`inherit X extends Y;`): `Y` のすべてのフィールドとプロパティは、同じ型の名前によって自動的にマップされます。 `where` ブロックは必要ありません。
* **明示的な形式** (`inherit X extends Y where ... end;`): フィールドまたはプロパティが **名前変更** (子の名前が親の名前と異なる)、**タイプ特化** (絞り込み)、または **一貫性** 証明が必要な場合に必要です。
* すべての基本フィールド/プロパティはいずれかの形式でカバーされる必要があります。

> [！注記]
> 各 `inherit` ステートメントは、**1 つの親** を正確に指定します。多重継承の場合は、複数の `inherit` 定義を使用します。これにより、単一ステートメント内でのフィールド/プロパティ名の競合が回避されます。

**例** — 名前の変更:

```mizar
definition
  inherit Derived extends Base where
    field derived_field from base_field;      :: renamed
    property derived_prop from base_prop;     :: renamed
  end;
end;
```

**セットの継承** が許可されます:

```mizar
definition
  inherit OneSorted extends set where
    field carrier from it;
  end;
end;
```

### 5.3.2 コヒーレンス (型の特殊化)

**フィールドまたはプロパティ**がより狭い型で継承される場合、値が実際に新しい型に属していることを検証するために `coherence` 証明が必要です。

**例**：
`PolyStr` は、`Nat` を返すプロパティ `degree` を定義します。 `NonConstPolyStr` はそれを `non zero Nat` に特化し、一貫性の証明を必要とします。

```mizar
definition
  struct PolyStr where
    field coeffs -> FinSequence of Int;
    property degree -> Nat;
  end;
end;

definition
  struct NonConstPolyStr where
    property degree -> non zero Nat;
  end;

  inherit NonConstPolyStr extends PolyStr where
    field coeffs from coeffs;

    :: Property narrowing: proof required
    property degree -> non zero Nat from degree;
    coherence
    proof
      :: non zero Nat ⊆ Nat (upcast is valid)
      thus for x be non zero Nat holds x is Nat;
    end;
  end;
end;
```

## 5.4 多重継承とダイヤモンド継承

構造名が異なる場合、多重継承が許可されます。ダイヤモンド継承がサポートされています。複数の親が **同じ名前と型**のフィールドを共有する場合、検証器は `from` チェーンをトレースすることによって自動的に一貫性を確認します。明示的な証明は必要ありません。継承された型が異なる場合、(安全なアップキャストを保証するために) 型の包含を検証するために `coherence` 証明が必要です。

**例** — 同じタイプ、証明は必要ありません:

```mizar
definition
  struct ZeroStr where
    field carrier -> set;
    property zero -> Element of carrier;
  end;

  struct OneStr where
    field carrier -> set;
    property one -> Element of carrier;
  end;

  struct ZeroOneStr where
    field carrier -> set;
    property zero -> Element of carrier;
    property one -> Element of carrier;
  end;

  :: shorthand: all fields/properties map by name with identical types
  inherit ZeroOneStr extends ZeroStr;
  inherit ZeroOneStr extends OneStr;
end;
```

**例** — さまざまなタイプ、`coherence` が必要です:

リングは、加法構造 (`AddStr`、`carrier -> non empty set`、ゼロ要素は空でないことを保証します) と乗法構造 (`MulStr`、`carrier -> set`) を組み合わせています。 `RingStr` が両方から継承する場合、キャリア タイプが異なるため、コヒーレンス証明が必要になります。

```mizar
definition
  struct AddStr where
    field carrier -> non empty set;
    field add -> BinOp of carrier;
    property zero -> Element of carrier;
  end;

  struct MulStr where
    field carrier -> set;
    field mul -> BinOp of carrier;
  end;

  struct RingStr where
    field carrier -> non empty set;
    field add -> BinOp of carrier;
    field mul -> BinOp of carrier;
    property zero -> Element of carrier;
  end;

  :: shorthand: all fields/properties (carrier, add, zero) map by name
  inherit RingStr extends AddStr;

  :: RingStr.carrier : non empty set, MulStr.carrier : set
  :: non empty set ⊆ set: coherence required
  inherit RingStr extends MulStr where
    field carrier from carrier;
    field mul from mul;
    coherence
    proof
      :: non empty set is a subtype of set (upcast is valid)
      thus for x being non empty set holds x is set;
    end;
  end;
end;
```

## 5.5 コンストラクター

構造体は、デフォルトのコンストラクターまたは変換コンストラクターを使用してインスタンス化できます。

### 5.5.1 デフォルトのコンストラクター

**構文 (EBNF)**:

```ebnf
constructor_call ::= struct_name [ type_args ] "(" [ named_arg { "," named_arg } ] ")" ;
named_arg        ::= identifier ":" term_expression ;
```

名前付き引数を任意の順序で指定します。

```mizar
set x = OneStr(carrier: A, one: b);
```

### 5.5.2 コンストラクターの変換

互換性のある構造体型間の暗黙的な型強制を許可します。

```mizar
let G be Group;
reconsider x = G as OneStr;  :: view G as OneStr (carrier and one come from G)
```

## 5.6 依存構造

構造は canonical な bracket 形式、または宣言された場合の `over` / `of` 短縮形を使用してパラメータ化できます。

**例**：

```mizar
definition
  let R be Ring;
  struct Polynomial[R] where
    field coeffs -> FinSequence of R.carrier;
    property degree -> Nat;
  end;
end;
```

* 複数のパラメータ: `struct Name[T, U]`
* 使用法: `let p be Polynomial[REAL];`
* 短縮形が宣言されている場合の使用法: `let p be Polynomial over REAL;`

## 5.7 フィールドアクセス (セレクター)

フィールドとプロパティにはドット表記を使用してアクセスします。

**構文 (EBNF)**:

```ebnf
field_access ::= term_expression "." identifier [ "(" [ term_list ] ")" ] ;
term_list    ::= term_expression { "," term_expression } ;
```

**例**:

```mizar
let G be Group;
set c = G.carrier;        :: Access field
set u = G.unit;           :: Access property
set f = G.binop;          :: Access function-valued field
set xy = G.binop(x, y);   :: Apply function-valued field

:: Chained access (nested structures)
let S be Module over R;
set base_carrier = S.ring.carrier;  :: Access parent structure's field
```

## 5.8 論理エンコーディングの詳細

このセクションでは、構造が一階論理にどのようにマップされるかについて説明します。エンコードは、構造コンポーネントをグローバル述語と関数に平坦化することに依存します。

### 5.8.1 述語としての構造

各構造型 `S` は、単項述語 `is_S(x)` としてエンコードされます。

|Mizar source | FOL エンコーディング |
| --- | --- |
| `x is UnitalMagmaStr` | `is_UnitalMagmaStr(x)` |

### 5.8.2 関数およびタイプガードとしてのフィールド

各 `field` は、グローバル関数シンボルとしてエンコードされます。さらに、**型ガード公理** により、有効な構造体に適用された場合にセレクターが正しい型の値を返すことが保証されます。

* **記号**: `carrier(x)`
* **公理**：
```
forall x (is_UnitalMagmaStr(x) implies is_set(carrier(x)))
```

> [！注記]
> プロパティは、同じタイプ ガード パターンを使用して関数シンボルとしてもエンコードされます。

### 5.8.3 含意とレダクト関数としての継承

`inherit` 辺のエンコードは、そのフィールド写像と、派生構造体から祖先へ
到達する inherit 経路の数に依存します。

**恒等写像・経路一意の辺** — すべてのフィールド/プロパティが**同名**の
親メンバーに写り、祖先への経路がちょうど 1 つの場合、辺は全称含意(包含)
としてエンコードされます。セレクタが一致するため、セレクタ等式は自明です:

```
inherit Derived extends Base        (全メンバーが同名で写る・経路一意)
⇔  forall x (is_Derived(x) implies is_Base(x))
```

**改名または複数経路の辺** — 辺がメンバーを改名する場合
(`field d_field from b_field` で `d_field ≠ b_field`)、または派生構造体
から祖先へ複数の inherit 経路が到達可能な場合、辺は包含ではなく明示的な
**レダクト関数** `view_{D→B}` でエンコードされます:

```
inherit Derived extends Base where field d_field from b_field
⇔  forall x (is_Derived(x) implies is_Base(view_D_B(x)))
   forall x (is_Derived(x) implies b_field(view_D_B(x)) = d_field(x))
```

この種の辺では `is_Derived(x) → is_Base(x)` 含意は放出されません。連鎖
するアップキャストは辺ごとのレダクト関数を合成します。共有祖先への異なる
経路は異なるレダクト項を生成します。

> [!IMPORTANT]
> 改名された辺に対して包含 + 大域セレクタ等式を放出することは**不健全**
> です: Ring → AddGroup → Magma(`field binop from add`)と
> Ring → MulMonoid → Magma(`field binop from mul`)のようなダイヤモンド
> では、等式がすべての Ring について `add(x) = mul(x)` へ崩壊します。
> レダクトエンコードは Ring の 2 つの Magma ビューを異なる項
> (`view_add(R)` と `view_mul(R)`)として保ち、これが属性事実をビュー
> 固有にするものでもあります(
> [第13章 §13.8.7](./13.term_expression.md#1387-qua-演算子のエンコーディング)
> および
> [第18章 §18.10.2](./18.templates.md#18102-型パラメータのエンコーディング)
> を参照)。

### 5.8.4 コンストラクターの公理

デフォルトのコンストラクターは、**Projection** (フィールド値) と **Typing** (結果の型) の 2 種類の公理を生成する集計関数としてエンコードされます。

`set x = OneStr(carrier: A, one: b)` が用語 `Agg_OneStr(A, b)` としてエンコードされた場合:

> アグリゲータの引数は、構造体定義のフィールド宣言の順序に従います。

1. **射影公理**:
```
carrier(Agg_OneStr(A, b)) = A
one(Agg_OneStr(A, b)) = b
```

2. **入力公理**:
```
is_OneStr(Agg_OneStr(A, b))
exact_OneStr(Agg_OneStr(A, b))
```

補助述語 `exact_S` は、`S` のアグリゲータで構築された値(§5.8.3 の
`view` 関数が `S` を対象として生成するレダクト値を含む)に対して正確に
成り立ち、`forall x (exact_S(x) implies is_S(x))` を満たします。

### 5.8.5 拡張性

構造の「多数のソートされた集合」の性質を捉えるために、拡張性公理は、同一性がフィールドによってのみ決定されることを指示します。この公理は**厳密**インスタンスに対してのみ述べられます。

フィールド `f1`、...、`fn` を持つ構造体 `S` の場合:

```
forall x, y (
  (exact_S(x) & exact_S(y))
  implies
  ((f1(x) = f1(y) & ... & fn(x) = fn(y)) iff x = y)
)
```

> [!IMPORTANT]
> 拡張性を `is_S` 全域で述べることは、継承の存在下では**不健全**です:
> 包含により `is_S` を満たす子孫は `S` より多くのメンバーを持つため、
> `S` のフィールドで一致し他所で異なる 2 つの子孫(例: `carrier` と
> `zero` を共有し `one` だけ異なる 2 つの `ZeroOneStr` 値)が等しいと
> 強制されてしまいます。`exact_S` ガードは公理を真の `S` アグリゲート
> (§5.8.3 のレダクトビューを含む)に制限し、そこではフィールドで決まる
> 同一性が意図された意味論です。

## 5.9 完全な構文 (EBNF)

構造関連の構文の概要。

```ebnf
(* Structure Definition *)
struct_def     ::= "struct" struct_def_name [ type_params ] "where"
                   struct_member { struct_member } "end" ";" ;
struct_member  ::= field_decl | property_decl ;
field_decl     ::= "field" identifier "->" type_expression
                   [ ":=" term_expression ] ";" ;
property_decl  ::= "property" identifier "->" type_expression ";" ;
type_params    ::= ( "of" | "over" ) type_parameter_list
                 | "[" type_parameter_list "]" ;
type_parameter_list ::= identifier { "," identifier } ;

(* Inheritance *)
inherit_def    ::= "inherit" inherit_child "extends" parent_type
                   [ "where" inherit_member { inherit_member }
                     [ coherence_block ]
                     "end" ] ";" ;
inherit_child  ::= struct_name [ type_args ] ;
parent_type    ::= struct_name [ type_args ] | "set" ;

inherit_member ::= field_redef | property_redef ;
field_redef    ::= "field" identifier [ "->" type_expression ] "from" (identifier | "it") ";" ;
property_redef ::= "property" identifier [ "->" type_expression ] "from" identifier ";" ;

(* Coherence for Type Specialization *)
coherence_block ::= "coherence" justification ";" ;

(* Constructor Call *)
constructor_call ::= struct_name [ type_args ] "(" [ named_arg { "," named_arg } ] ")" ;
named_arg        ::= identifier ":" term_expression ;

(* Field Access *)
field_access   ::= term_expression "." identifier [ "(" [ term_list ] ")" ] ;
term_list      ::= term_expression { "," term_expression } ;

(* Common *)
struct_def_name ::= constructor_name ;
struct_name     ::= qualified_constructor_name ;
```
