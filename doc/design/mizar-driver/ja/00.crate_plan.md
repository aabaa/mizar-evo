# Crate Plan: mizar-driver

> 正本は英語です。英語版:
> [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## 責務

所有する挙動:

- batch、watch、LSP 由来 build の build request lifecycle;
- 1 つの current source/dependency snapshot に対する `BuildSession` 作成;
- 置き換えられた watch/LSP 結果を拒否する current-snapshot publication guard;
- 決定的な phase-service 登録と lookup;
- pure phase service を包み、phase crate に query-engine 依存を漏らさない
  driver-owned query boundary;
- `mizar-build` による phase-0 bootstrap、`mizar-build` scheduler への task graph
  submission、対応済み supersession を `mizar-build` cancellation policy で表現する
  cancellation request;
- CLI progress、diagnostics readiness、phase completion、stale-result
  suppression、artifact-boundary handoff の protocol-agnostic build event;
- 同じ driver API 上の CLI batch entry point と watch-mode orchestration。

範囲外:

- phase semantics、type checking、name resolution、VC generation、proof
  acceptance、trusted status、kernel acceptance、proof-policy selection;
- cache compatibility decision、cache-key validation authority、proof-reuse
  validation、cluster-db ownership;
- artifact serialization、manifest transaction authority、publication-token
  issuance;
- LSP protocol conversion、source range conversion、document-version protocol
  handling、editor command / code-action shaping;
- fake phase adapter、provisional artifact publication token、placeholder LSP
  bridge、stub producer output。

## 開始前確認

| 確認 | 結果 | 根拠 |
|---|---|---|
| 開始前 worktree clean | Pass | この plan 前の `git status --porcelain=v1` は path を返さなかった。 |
| `mizar-diagnostics` closeout commit が履歴にある | Pass | `084e9c988d1e2cd72d68206175270cc359b35ace` は `HEAD` の祖先。 |
| `mizar-ir` closeout commit が履歴にある | Pass | `9bd11960f1c8f8890f0571a5ed28d99056511b42` は `HEAD` の祖先。 |
| closeout report を参照できる | conflict 付き Pass | `mizar-build`、`mizar-ir`、`mizar-cache`、`mizar-diagnostics` report は読める。 |
| `mizar-artifact` closeout report | `repo_metadata_conflict` | `doc/design/mizar-artifact/en/crate_exit_report.md` がない。報告のみ行い、この stream では artifact metadata を修復しない。 |
| backup branch / stash | 未使用 | inventory は current worktree と `HEAD` 履歴のみを使った。 |

## 仕様項目

| Spec ref | 要求 | 状態 |
|---|---|---|
| [internal 01](../../internal/en/01.compiler_driver_and_pipeline_scheduler.md) | Driver は build request、session、registry wiring、scheduler submission、event、cancellation、artifact-boundary handoff の front door。 | この plan と task D-001 から D-016 で扱う。downstream seam は下で分類する。 |
| [internal 07](../../internal/en/07.crate_module_layout.md) | `mizar-driver` は request lifecycle、phase registry、CLI/watch/LSP entry point を所有し、internal 01 を精緻化する。 | `todo.md` の module split と下の task decomposition で扱う。 |
| [architecture 20](../../architecture/en/20.test_strategy.md) | 決定性、fail-heavy testing、実装済み seam の clean/parallel/incremental property。 | crate-local Rust determinism test を要求する。real equivalence は real producer/adapter が揃うまで deferred。 |
| [architecture 22](../../architecture/en/22.incremental_verification_contract.md) | Snapshot-scoped result、current publication のみ、clean-build equivalence、cache/proof reuse validation boundary。 | driver freshness/session/event は scope 内。cache/proof authority は scope 外。 |
| [spec 23.4](../../../spec/en/23.package_management_and_build_system.md#234-build-lifecycle-and-reproducibility) | Build lifecycle は reproducible phase work を実行し、acceptance 後に artifact を書く。 | Driver は CLI/watch/LSP request を build lifecycle orchestration へ写すが、phase semantics や artifact serialization は所有しない。 |
| [spec 23.5](../../../spec/en/23.package_management_and_build_system.md#235-verifier-artifacts-and-build-output) | Verified artifact は downstream interface。 | Driver は real owner seam がある場合だけ artifact handoff event を記録する。 |
| [spec 23.6](../../../spec/en/23.package_management_and_build_system.md#236-lsp-integration-and-ide-support) | IDE support は LSP 経由で diagnostics/artifact を消費する。 | Driver は protocol-agnostic build event を emit し、`mizar-lsp` が protocol owner のまま。 |

## 関連テスト

| Test path | 意図 | Spec refs |
|---|---|---|
| `crates/mizar-driver/tests/lint_policy.rs` | workspace lint inheritance と public enum policy を guard する。 | internal 07、`todo.md` task 1/17。 |
| `crates/mizar-driver/tests/request.rs` | request/session construction、snapshot capture、current-snapshot boundary、obsolete publication rejection。 | `request.md`、architecture 22。 |
| `crates/mizar-driver/tests/registry.rs` | phase registry registration、duplicate rejection、deterministic lookup、query-boundary owner check、cache-key purity harness。 | `registry.md`、internal 01。 |
| `crates/mizar-driver/tests/driver.rs` | phase-0 bootstrap、task graph submission、scheduler authority consumption、cancellation、deterministic phase order。 | `driver.md`、internal 01。 |
| `crates/mizar-driver/tests/events.rs` | deterministic event ordering、valid session reference、diagnostics readiness、stale-output suppression、protocol-agnostic consumer。 | `events.md`、architecture 22。 |
| `crates/mizar-driver/tests/cli.rs` | batch request parsing、stable exit-code mapping、`BuildEventStream` からの progress rendering、records/indexes 経由の diagnostics rendering、LSP conversion 不在。 | `cli.md`、spec 23。 |
| `crates/mizar-driver/tests/watch.rs` | watch snapshot replacement、superseded-session cancellation、stale result suppression。 | `request.md`、`driver.md`、architecture 22。 |
| `crates/mizar-driver/tests/determinism.rs` | 実装済み driver seam について repeated-run、worker-count、completion-order、CLI output、diagnostics owner-gap、unavailable-owner、multi-task deferral、superseded-watch replay の決定性を検証する。 | architecture 20、architecture 22、spec 23。 |

Test-first 追加予定:

- この task stream は言語 syntax、static semantics、proof semantics、
  diagnostics intent、expectation file を変えないため `.miz` test は不要。
- 各 implementation task で Rust test を追加する。owner seam がなければ fake
  adapter や placeholder test ではなく `external_dependency_gap` または
  `deferred` として記録する。

## Design / Source Inventory

Design files:

- `doc/design/mizar-driver/en/todo.md`
- `doc/design/mizar-driver/ja/todo.md`
- この crate plan と日本語 companion
- module specs: `request.md`、`registry.md`、`driver.md`、`events.md`、`cli.md`
- `frontend_adapter.md` などの adapter readiness record
- completed audit docs: `source_spec_correspondence.md`、
  `bilingual_documentation_sync.md`、`architecture_22_follow_up_audit.md`、
  `module_boundary_refactor_gate.md`
- closeout docs: `crate_exit_report.md`

Source files:

- `crates/mizar-driver/Cargo.toml`
- `crates/mizar-driver/src/lib.rs`
- `crates/mizar-driver/src/request.rs`
- `crates/mizar-driver/src/registry.rs`
- `crates/mizar-driver/src/registry/catalog.rs`
- `crates/mizar-driver/src/driver.rs`
- `crates/mizar-driver/src/driver/event_log.rs`
- `crates/mizar-driver/src/driver/scheduler.rs`
- `crates/mizar-driver/src/driver/watch.rs`
- `crates/mizar-driver/src/driver/tests.rs`
- `crates/mizar-driver/src/events.rs`
- `crates/mizar-driver/src/cli.rs`
- `crates/mizar-driver/src/cli/output.rs`
- `crates/mizar-driver/tests/*.rs`

観測された挙動:

- この task stream 前には `mizar-driver` は workspace に存在しない。
- `mizar-session` は `BuildRequestId`、`BuildSessionId`、`BuildSnapshotId`、
  `SnapshotRegistry::create_snapshot`、`SnapshotRegistry::is_current_for_request`
  を公開している。
- `mizar-build` は manifest planning、module indexing、task graph
  construction、scheduler run、scheduler event/result、cache scheduling
  decision、artifact-commit freshness guard、cancellation policy/state を公開している。
- `mizar-ir` は sealed phase-output storage、`PhaseOutputPublisher`、snapshot
  replacement、artifact projection draft を公開するが、real driver、
  diagnostics、producer payload、artifact-publication-token integration を
  external gap としている。
- `mizar-diagnostics` は stable diagnostic record/index と CLI rendering を公開する。
  message text は diagnostic identity ではない。
- `mizar-frontend` は real source-to-syntax orchestration を公開するが、
  driver-owned phase adapter は fake payload なしで real `mizar-ir` producer
  contract を通じて output を seal できる場合だけ消費できる。
- `mizar-artifact` は manifest transaction と opaque freshness check を公開しているが、
  full phase-15 producer emission と publication authority は driver が mint する API
  ではない。
- `mizar-lsp` は現状 source-range conversion utility のみを公開しており、build
  bridge は external owner seam のまま。

## 既知の gap / drift

| ID | Class | Disposition | 根拠 | 対応 |
|---|---|---|---|---|
| DRIVER-G-001 | `repo_metadata_conflict` | report-only | 要求された `mizar-artifact` closeout report がない一方、artifact design/source は存在する。 | 報告のみ。この stream では artifact metadata を修復しない。 |
| DRIVER-G-002 | `source_drift` | implement | internal/todo の ownership map には `mizar-driver` があるが workspace crate が存在しない。 | D-001 で real crate scaffold を追加する。 |
| DRIVER-G-003 | `design_drift` | `external_dependency_gap` | build/cache/ir/diagnostics closeout report は real driver request/session/registry/event integration をこの crate に残している。 | driver から real seam を消費し、upstream owner boundary は変えない。 |
| DRIVER-G-004 | `design_drift` | `external_dependency_gap` | resolver/checker/core/VC/ATP/kernel/proof/artifact の phase-service adapter がすべて service-facing producer output として公開済みではない。 | 利用不可の semantic / proof / artifact adapter は external gap として記録し、owner surface がある real adapter だけ実装する。 |
| DRIVER-G-005 | `design_drift` | `external_dependency_gap` | Artifact publication token と full phase-15 producer emission はまだ利用できない。 | real artifact owner seam が committed result を返すまで committed/published artifact event は emit しない。利用不可 handoff は分類済み gap として記録する。 |
| DRIVER-G-006 | `design_drift` | `external_dependency_gap` | `mizar-lsp` は現状 range conversion を所有するだけで、real driver build bridge はない。 | driver event は protocol-agnostic のままにし、LSP bridge work を owner-seam follow-up として記録する。 |
| DRIVER-G-007 | `test_gap` | locally implemented / full system は `deferred` | real cache hit、artifact commit、proof reuse、worker race を含む full clean/incremental/parallel equivalence には real downstream producer が必要。D-016 は実装済み driver seam の crate-local determinism を追加する。 | full system property は real cache / producer / artifact / proof seam が着地するまで deferred のままにする。 |
| DRIVER-G-008 | `boundary_violation` | guard | phase semantics、proof acceptance、cache compatibility、artifact serialization、LSP conversion をこの crate に移すと internal 01/07 と closeout report に反する。 | specs、lint-policy test、source/spec audit、implementation review で guard する。 |
| DRIVER-G-009 | `repo_metadata_conflict` | report-only | `mizar-artifact` design text はまだ `mizar-proof` が workspace crate ではないと述べるが、workspace にはすでに `crates/mizar-proof` がある。 | 報告のみ。この stream では artifact metadata を修復しない。 |
| DRIVER-G-010 | `design_drift` | `external_dependency_gap` | `mizar-frontend` は real in-memory `FrontendOutput` を公開するが、`mizar-ir` 向け owner-defined canonical producer payload、frontend-to-`DiagnosticDraft` bridge、driver build-plan-to-source-request mapping はまだ存在しない。 | D-006 readiness gap を `frontend_adapter.md` に記録し、fake `SourceFrontend` adapter は追加しない。 |
| DRIVER-G-011 | `design_drift` | `external_dependency_gap` | 現在の `mizar-build` scheduler は precomputed modeled outcome を消費し、scheduler-selected execution point で driver-owned registry dispatch を呼ぶ public callback を公開していない。 | D-008 では scheduler submission / result consumption の validation に留め、scheduler より前に phase service を実行したり synthetic output を sealed phase output として扱ったりしない。 |
| DRIVER-G-012 | `design_drift` | `external_dependency_gap` / `deferred` | watch change と protocol conversion を driver へ供給する real external file-watcher / coalescing owner と LSP build bridge はまだない。`mizar-ir` は `PhaseOutputPublisher` による real snapshot replacement を公開している。 | D-014 は owner-provided changed path、snapshot input、任意の real `PhaseOutputPublisher` replacement seam だけを消費する。missing watcher / LSP / publisher seam は分類済み gap として記録し、fake watcher API、source-loading rule、LSP payload、provisional artifact / output token は追加しない。 |
| DRIVER-G-013 | `design_drift` | `external_dependency_gap` | D-015 は `ModuleResolver`、`SemanticChecker`、`Elaborator`、`VcService`、`AtpService`、`KernelService`、`ArtifactService` を再監査する。production adapter に必要な driver-callable input、canonical `mizar-ir` producer output、`mizar-diagnostics` bridge、driver が proof / cache / artifact / LSP authority を持たない契約をすべて公開している owner は現時点で存在しない。 | semantic / proof / artifact phase は driver では未登録のままにする。将来の real adapter work は owner-provided seam と fixture coverage を持つ adapter ごと 1 task で行う。 |
| DRIVER-G-014 | `design_drift` | `deferred` | D-015 は `mizar-doc` には design TODO があるが、workspace crate も実装済みの service-facing extraction owner surface も存在しないことを確認する。 | documentation / extraction owner crate が着地するまで `DocExtractionService` は deferred のままにする。 |

## Task Decomposition

### Task D-000: Crate plan

Purpose: 実装前に責務、gap classification、順序付き task scope、exit criteria を確立する。

Spec refs: autonomous crate protocol、internal 01、internal 07、architecture 22。

Tests: documentation review と diff check。

Affected design files: このファイルと `../en/00.crate_plan.md`。

Affected source files: なし。

Completion condition: paired crate plan が blocking/high finding なしで review
され、独立 commit される。

Forbidden behavior: source implementation、artifact metadata repair、TODO status
churn。

### Task D-001: Crate scaffold and lint-policy guard

Purpose: real `mizar-driver` workspace member と baseline lint guard を追加する。

Spec refs: internal 01、internal 07。

Tests: `crates/mizar-driver/tests/lint_policy.rs`、`cargo test -p mizar-driver`、
crate clippy、fmt。

Affected design files: inventory が変わった場合はこの plan。

Affected source files: workspace `Cargo.toml`、
`crates/mizar-driver/Cargo.toml`、`src/lib.rs`、
`tests/lint_policy.rs`。

Completion condition: new crate が build でき、workspace lint を継承する。

Forbidden behavior: request/session semantics、phase adapter、scheduler
semantics、placeholder integration API。

### Task D-002: Spec `request.md`

Purpose: `BuildRequest`、`BuildSession`、snapshot、lifecycle state、
current-snapshot publication boundary を定義する。

Spec refs: internal 01 "Build Session"、architecture 22。

Tests: documentation review のみ。

Affected design files: 両言語の `request.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期され review される。

Forbidden behavior: source implementation。

### Task D-003: `BuildRequest` and `BuildSession`

Purpose: `mizar-session` id と snapshot registry currentness 上で
request/session record を実装する。

Spec refs: `request.md`。

Tests: request/session Rust tests。

Affected source files: `src/request.rs`、`src/lib.rs`、`tests/request.rs`。

Completion condition: 同一 snapshot input が同一 snapshot id を生み、置き換えられた
watch/LSP request snapshot の obsolete publication が拒否される。

Forbidden behavior: LSP protocol conversion、source range conversion、artifact
publication。

### Task D-004: Spec `registry.md`

Purpose: `PhaseService` contract、phase service table、cache-key purity rule、
driver-owned query boundary を定義する。

Spec refs: internal 01 "Phase Services" / "Phase Service API"。

Tests: documentation review のみ。

Affected design files: 両言語の `registry.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期され review される。

Forbidden behavior: source implementation、phase crate への query-engine 依存の移動。

### Task D-005: Phase service registry and query boundary

Purpose: deterministic service registration、duplicate rejection、lookup、
cache-key purity harness、driver-owned query-compatible execution boundary を実装する。

Spec refs: `registry.md`。

Tests: registry Rust tests。

Affected source files: `src/registry.rs`、`src/lib.rs`、`tests/registry.rs`。

Completion condition: service は registry/query boundary 経由で実行され、duplicate
phase は拒否され、同一 input の cache key は決定的で、phase crate は query-engine
free のまま。

Forbidden behavior: cache compatibility decision ownership、phase semantics、
stub phase output。

### Task D-006: `SourceFrontend` service adapter

Purpose: real `mizar-frontend` phase 1-3 を、output が real `mizar-ir`
producer contract 経由で seal できる場合だけ包む。

Spec refs: `registry.md`、`mizar-frontend` todo。

Tests: real seam がある場合だけ frontend adapter fixture。

Affected design files: 両言語の `frontend_adapter.md` と、この plan / `todo.md` の
task / gap record。

Affected source files: readiness check が `external_dependency_gap` を記録する場合は
なし。それ以外の場合は `src/registry.rs` または private adapter module、
`tests/registry.rs`。

Completion condition: diagnostics が `mizar-diagnostics` に流れ sealed output
handle を返す real adapter、または不足 producer/sealing seam を説明する
`external_dependency_gap` record。

Forbidden behavior: fake frontend adapter、real output のふりをする synthetic IR
payload、message text keyed diagnostics。

### Task D-007: Spec `driver.md`

Purpose: `CompilerDriver`、`submit`、`cancel`、event subscription、phase-0
bootstrap、scheduler submission、artifact-boundary handoff を定義する。

Spec refs: internal 01 "Driver API" / "Control Flow"。

Tests: documentation review のみ。

Affected design files: 両言語の `driver.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期され review される。

Forbidden behavior: source implementation。

### Task D-008: Driver core

Purpose: real build planner / task graph seam と、現在の modeled `mizar-build`
scheduler submission seam 上で `submit` を実装する。registered phase service は
実装済み real dispatch boundary 経由の場合だけ駆動してよい。missing
scheduler-to-registry dispatch seam は external gap のまま。

Spec refs: `driver.md`。

Tests: driver Rust tests、および seam に触れる場合の adjacent build/ir/
diagnostics/frontend tests。

Affected source files: `src/driver.rs`、`src/lib.rs`、`tests/driver.rs`。

Completion condition: phase 0 は `mizar-build` 経由で bootstrap され、phase-0
scheduler submission は output-free な driver summary を通じて scheduler authority を消費し、
non-phase-0 work は real dispatch seam で block され、missing real producer adapter は stub では
なく記録される。

Forbidden behavior: scheduler semantics、cache validation、artifact serialization、
proof acceptance の複製。

### Task D-009: Spec `events.md`

Purpose: protocol-agnostic build event と deterministic event ordering を定義する。

Spec refs: internal 01 "Build Events"、architecture 22。

Tests: documentation review のみ。

Affected design files: 両言語の `events.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期され review される。

Forbidden behavior: LSP protocol payload や range conversion。

### Task D-010: Build event stream

Purpose: worker completion order に依存しない deterministic build event
publication を実装する。

Spec refs: `events.md`。

Tests: event Rust tests。

Affected source files: `src/events.rs`、`src/driver.rs`、`src/lib.rs`、
`tests/events.rs`、`tests/driver.rs`、`tests/lint_policy.rs`。

Completion condition: shuffled scheduler completion が同一 driver event sequence を生み、
全 event が valid session/snapshot を参照する。stale session は suppress され、dispatch、
phase-service、diagnostics、artifact の gap / non-authority guard が強制される。implementation は
protocol-agnostic event payload だけを公開し、CLI / LSP / diagnostics / artifact authority は
owner crate に残す。

Forbidden behavior: artifact ordering authority、text による diagnostic identity。

### Task D-011: Cancellation flow

Purpose: session cancellation を terminal session state/event と、対応済みの
`mizar-build` cancellation policy seam へ変換して `cancel` を実装する。

Spec refs: `driver.md`、internal 01 "Cancellation"。

Tests: driver/request/event cancellation tests。

Affected source files: `src/driver.rs` と `tests/driver.rs`。既存の request / event
cancellation tests は coverage baseline の一部として残る。

Completion condition: build 中 cancellation が current partial publication なしで
terminal state に達し、double cancel が冪等。

Forbidden behavior: worker thread の直接 kill、cancellation を proof failure として扱うこと。

Result: D-011 は `CompilerDriver::cancel` を terminal かつ idempotent にし、terminal replay
event を追加する。supersession は `mizar-build::CancellationPolicy::supersede_snapshot`
で伝播し、snapshot-wide explicit / shutdown policy propagation は fake cancellation token を
発明せず `external_dependency_gap` として記録する。

### Task D-012: Spec `cli.md`

Purpose: spec 23 に照らして CLI surface を決め、binary/subcommand、exit code、
progress rendering、diagnostics rendering を定義する。

Spec refs: spec 23.4 から 23.6。

Tests: documentation review のみ。

Affected design files: 両言語の `cli.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期され review される。

Forbidden behavior: source implementation、LSP protocol conversion。

Result: D-012 は同期された `cli.md` spec を追加し、`mizar build`、batch request
mapping、protocol-agnostic event からの progress rendering、`mizar-diagnostics` 経由の
diagnostics rendering、stable exit code、unavailable owner seam の明示的
`external_dependency_gap` handling を定義する。

### Task D-013: CLI batch entry point

Purpose: batch request parsing、driver execution、diagnostics/progress rendering、
exit-code mapping を実装済み seam 上で実装する。

Spec refs: `cli.md`。

Tests: inline golden output assertion または checked-in golden file を含む CLI Rust tests。

Affected source files: `src/cli.rs`、`src/lib.rs`、optional binary target、
`tests/cli.rs`。

Completion condition: fixture batch run が stable exit code/output を生み、missing
artifact emission は分類済みのまま。

Forbidden behavior: artifact serialization、text による diagnostics identity、
LSP conversion。

Result: D-013 は `src/cli.rs` と CLI tests を追加し、argument parsing、
owner-provided snapshot input mapping、driver submission、event stream からの human /
JSON progress rendering、success / unavailable / usage / internal の stable exit code、
non-owner authority を防ぐ source guard を cover した。planning / lockfile error の
diagnostics rendering は real `mizar-diagnostics` record が利用可能になるまで明示的な
`external_dependency_gap` のまま。artifact publication と LSP protocol conversion は
driver の外に残る。manifest-path selection、target filtering、source-layout / snapshot
mismatch は、現在の single-package batch input で満たせない限り owner gap として拒否する。

### Task D-014: Watch mode

Purpose: owner-provided changed path と snapshot input、superseded-session
cancellation、stale replay suppression、real owner seam がある範囲の
`mizar-ir::PhaseOutputPublisher` snapshot replacement による watch orchestration を実装する。

Spec refs: `request.md`、`driver.md`、architecture 22、
[mizar-ir crate exit report](../../mizar-ir/ja/crate_exit_report.md) の snapshot
replacement。

Tests: 実装済み seam 上の watch Rust tests。owner-provided snapshot input による
change-to-rebuild、previous-session replay の suppressed `Superseded` への mutation、
real publisher replacement、same-snapshot replacement no-op、missing publisher gap、
non-watch request rejection、source authority guard を cover する。

Affected source files: `src/driver.rs`、`src/request.rs`、`src/events.rs` と tests。

Completion condition: change が replacement snapshot を作成し、stale session はすでに
terminal state に達していた場合でも current result を publish せず、real publisher
replacement は owner seam が retain する old snapshot output を生存させ、missing
external file-watcher / LSP bridge / publisher seam は分類される。

Forbidden behavior: protocol-specific LSP handling、fake file watcher API、
source-loading / discovery rule、provisional artifact publication token、fake producer
output handle。

Result: D-014 は `CompilerDriver::submit_watch_change` と watch tests を追加し、
owner-provided change resubmission、lane-current previous-session validation、stale
replay の suppressed `Superseded` への mutation、real `PhaseOutputPublisher`
snapshot replacement、same-snapshot no-op behavior、owner-seam replacement error、
missing watcher / LSP / publisher gap の分類を cover する。この実装は OS watcher
API、source loading / discovery、LSP protocol conversion、phase semantics、cache /
proof authority、artifact publication token、fake producer output を追加しない。

### Task D-015: Semantic phase adapters

Purpose: owner crate が service-facing output を公開した semantic/proof/artifact/doc
phase の real adapter を登録する。

Spec refs: `registry.md`。

Tests: real adapter ごとに 1 integration fixture。

Affected source files: real seam がある場合だけ adapter module と registry tests。

Completion condition: 利用可能な owner-provided semantic / proof / artifact
adapter はすべて real かつ tested。利用不可の semantic / proof / artifact
adapter は `external_dependency_gap` として持ち越す。利用不可の doc extraction は
owner crate が存在するまで `deferred` のまま。

Forbidden behavior: fake adapter、placeholder producer output、cache/proof authority
移動、provisional artifact token。

Result: D-015 は adapter readiness を `registry.md` に記録し、production semantic /
proof / artifact / doc phase registration は変更しない。現在利用可能な owner crate は
いずれも必要な adapter seam の少なくとも 1 つを欠いているため、Rust source や fixture
adapter は追加しない。`ModuleResolver`、`SemanticChecker`、`Elaborator`、
`VcService`、`AtpService`、`KernelService`、`ArtifactService` は
`external_dependency_gap` のまま。`DocExtractionService` は `deferred` のまま。
欠落している `mizar-artifact` closeout report は report-only
`repo_metadata_conflict` のまま。

### Task D-016: End-to-end determinism suite

Purpose: 実装済み seam について、worker count / run をまたぐ event、diagnostics、
session、exit code、stale-output rejection の決定性を test する。

Spec refs: architecture 20、architecture 22。

Tests: determinism Rust tests。

Affected source files: tests と必要なら小さな test helper。

Completion condition: crate-local determinism が通る。full clean/incremental/
parallel real-system equivalence は producer/cache/artifact owner seam が揃うまで deferred。

Forbidden behavior: test を満たすために proof/cache/artifact behavior を発明すること。

Result: D-016 は実装済みの driver-owned seam に対して `tests/determinism.rs` を
追加する。この suite は phase-zero driver submission について repeated-run、
worker-count、scheduler completion order の安定性を検証し、success、manifest
diagnostics owner gap、利用不可な phase-service owner gap の CLI human / JSON output と
exit code が byte-stable であることを確認する。real phase dispatch が owner gap である間、
multi-task source / module work が scheduler submission 前に決定的に deferred されることも
検証する。watch supersession replay は、obsolete publication が current diagnostics または
artifact-boundary event として emit される前に suppressed になることを決定的に検証する。
real cache hit、producer output、artifact commit、proof reuse、multi-task driver phase
dispatch を含む full clean / incremental / parallel equivalence は、それらの owner seam が
存在するまで意図的に deferred のままにする。

### Task D-017: Public enum forward-compatibility policy

Purpose: 全 public enum を分類し、明示的に文書化された exhaustive exception がない限り
`#[non_exhaustive]` を適用する。

Spec refs: 全 module specs。

Tests: lint-policy guard。

Affected design files: enum policy section を追加する場合の module specs。

Affected source files: audit 対象としての public enum declaration、および guard
文言または coverage を変える場合の lint-policy tests。

Completion condition: 全 public enum が guard で covered。

Forbidden behavior: enum compatibility 以外の behavior change。

Result: D-017 は各 owning module spec に public enum policy を記録する。`request`、
`registry`、`driver`、`events`、`cli` の現在の public enum はすべて downstream-facing な
`#[non_exhaustive]` boundary type であり、exhaustive exception はない。既存の enum
declaration はすでに policy を満たしていたため、変更ではなく監査した。lint-policy guard は
完了済みの policy を表す文言になり、文書化済み例外が先に追加されない限り、新しい public
enum が `#[non_exhaustive]` を欠くと失敗し続ける。frontend task-25 procedure の
source/spec correspondence table 部分は D-018 に残す。runtime behavior は変更していない。

### Task D-018: Source/spec correspondence audit

Purpose: public API と promised behavior を implementation / tests に trace する。

Spec refs: 全 module specs とこの plan。

Tests: review-only audit。

Affected design files: 両言語の `source_spec_correspondence.md`。

Affected source files: audit が blocking/high drift、または source で修正すべき
medium finding を見つけ、理由付き deferred にしない場合のみ。

Completion condition: 未解決の blocking/high source/spec drift がなく、medium
source/spec finding は修正済みまたは理由付きで明示的に deferred されている。

Forbidden behavior: 分類なしで undocumented behavior を受け入れること。

Result: D-018 は paired `source_spec_correspondence.md` audit document を追加する。
この audit は `request`、`registry`、`driver`、`events`、`cli` specs の public API と
promised behavior を `crates/mizar-driver/src` と crate tests へ trace する。新しい未解決の
blocking、high、medium source/spec drift は見つからなかった。既存の
`external_dependency_gap`、`deferred`、report-only `repo_metadata_conflict` は分類済み
follow-up のまま残す。source behavior は変更していない。

### Task D-019: Bilingual documentation sync audit

Purpose: English canonical docs と Japanese companion を比較する。

Spec refs: repository documentation policy。

Tests: review-only audit と diff checks。

Affected design files: 両言語の `bilingual_documentation_sync.md` と同期が必要な docs。

Affected source files: なし。

Completion condition: 未解決の blocking/high EN/JA drift がない。

Forbidden behavior: Japanese sync なしで English behavior を変えること。

Result: D-019 は paired `bilingual_documentation_sync.md` audit document を追加する。
この audit は crate plan、TODO、module spec、`frontend_adapter.md`、
`source_spec_correspondence.md`、新規 sync audit 自体を含む driver design corpus の
1 対 1 の EN/JA coverage を記録する。未解決の blocking/high EN/JA documentation
drift は見つからなかった。既存の `external_dependency_gap`、`deferred`、
report-only `repo_metadata_conflict` record は同期済みのまま変更していない。

### Task D-020: Architecture-22 follow-up audit

Purpose: query boundary、stale-output rejection、diagnostics、cache、artifact-publication
contract を architecture 22 に照らして再監査する。

Spec refs: architecture 22、全 module specs。

Tests: review-only audit と implemented-seam tests。

Affected design files: 両言語の `architecture_22_follow_up_audit.md`。

Affected source files: blocking/high drift が見つかった場合のみ。

Completion condition: implemented seam が architecture-22 check を通り、残る broader
integration risk が分類される。

Forbidden behavior: deferred cache/proof/artifact authority を driver ownership に変えること。

Result: D-020 は paired `architecture_22_follow_up_audit.md` document を追加し、新しい
file pair について bilingual sync audit を更新する。この audit は、実装済み driver seam に
未解決の blocking/high architecture-22 drift がないことを確認した。registry/query
boundary は cache/proof authority を持たない driver-owned boundary のままであり、obsolete
watch/LSP session は current diagnostics や artifact-boundary event を publish できない。
diagnostics identity は `mizar-diagnostics` が所有し、real owner record がない限り artifact
publication は driver の外に残る。real producer/cache/artifact/proof seam を伴う full
clean / incremental / parallel equivalence は `deferred` のまま、scheduler-selected real
phase dispatch、LSP bridge、semantic/proof/artifact adapter は `external_dependency_gap` の
ままである。

### Task D-021: Module-boundary refactor gate

Purpose: oversized file と mixed responsibility について source layout を監査する。

Spec refs: internal 07 と全 module specs。

Tests: module-boundary audit。

Affected design files: 両言語の `module_boundary_refactor_gate.md`、必要なら
TODO/source-path table。

Affected source files: 必要な場合だけ private module split。

Completion condition: completed surface に review-bottleneck source layout が残らない。

Forbidden behavior: file move に behavior cleanup や API exposure を混ぜること。

Result: D-021 は paired `module_boundary_refactor_gate.md` audit document を追加し、
review bottleneck になっていた private helper を public API 変更なしで分割した。`cli`
rendering helper は `src/cli/output.rs`、driver event / scheduler / watch / unit-test
helper は `src/driver/` 配下、registry phase catalog と query-fingerprint helper は
`src/registry/catalog.rs` に置く。`src/lib.rs` は引き続き完了済みの public module だけを
公開する。deterministic rendering、diagnostics/artifact-facing schema、owner-gap
classification、driver non-ownership boundary は変更していない。

### Task D-022: Crate exit report and quality review

Purpose: hard-gate evidence、task commit hash、quality score、deferred gap、
verification、next-phase handoff で autonomous task stream を close する。

Spec refs: autonomous crate protocol。

Tests: read-only quality review と final verification。

Affected design files: 両言語の `crate_exit_report.md`。

Affected source files: closeout が blocking/high drift を見つけた場合のみ。

Completion condition: 全 hard gate が通り、quality score が 90/100 以上で、final
verification が通るか未実行 command が正当化され、report が独立 commit される。

Forbidden behavior: owner seam で実装されていない real adapter、artifact publication、
LSP bridge、cache/proof authority を claim すること。

Result: D-022 は paired `crate_exit_report.md` closeout report を追加する。この report は
全 task commit、milestone scope、included/excluded item、最終 ownership shape、
hard-gate status、score cap、score breakdown、verification evidence、94/100 quality
score、human review surface、test expectation summary、残る分類済み
`external_dependency_gap` / `deferred` / report-only `repo_metadata_conflict` item、
next-phase handoff を記録する。source behavior は変更しない。

## Exit Criteria

Hard gates:

- blocking/high specification inconsistency が残らない;
- すべての public driver behavior が paired EN/JA design file に文書化され、Rust test
  または分類済み deferred/external gap に trace される;
- design/spec/test intent にない source behavior が残らない;
- implementation behavior に合わせるためだけに `.miz` test、language spec、
  traceability metadata、expectation sidecar を変更しない;
- driver boundary を守る: phase semantics、proof authority、cache compatibility
  decision、artifact serialization/token issuance、LSP protocol conversion を
  `mizar-driver` に移さない;
- required verification が通るか、未実行 command を residual risk とともに正当化する;
- 残る risk は `external_dependency_gap`、`deferred`、out-of-scope、human-owned
  として分類される。

Verification commands:

```text
cargo test -p mizar-driver
cargo clippy -p mizar-driver --all-targets -- -D warnings
cargo fmt --check
cargo test -p mizar-build
cargo test -p mizar-ir
cargo test -p mizar-diagnostics
cargo test -p mizar-frontend
cargo clippy --all-targets --all-features -- -D warnings
cargo test
git diff --check
git diff --cached --check
```

Review expectations:

- 利用可能な場合、implementation specification / documentation review、
  test sufficiency review、full implementation review、source/docs consistency
  review、final quality review に review-only sub-agent を使う;
- blocking/high finding は修正または明示 reclassification まで actionable とする;
- review と verification 後、完了した task を 1 commit ずつ commit する;
- task-related path だけを明示的な `git add` で stage する。
