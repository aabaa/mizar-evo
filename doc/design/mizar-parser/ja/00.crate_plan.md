# Crate Plan: mizar-parser

> canonical languageはEnglish。English canonical:
> [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## Current Post-Task-46 Status

fresh inventoryにより、完了済みfrontend Task 20がTask 46のnamed
position-sensitive string lexingとlocal operator-metadata triggerをすでに満たして
いたことを確認した。したがってTask 46は実行可能であり、同一gapを表す
P-043-01/P-046をboundedなparser `source_drift` / `test_gap` sliceとしてcloseする。
exact Appendix-A operator-declaration parsing、append-only syntax kind 193、local
recovery、active pass/fail pair 1組、exact trace row 1件を追加する。language
specification、existing corpus source/expectation、operator activation、fixity
semantics、resolution、precedence validation、Task 49、Steps 6/7は変更しない。

本file後半のpre-Task-46 parser closeout recordはhistoricalである。
`PARSER-CRATE-POST-TASK46-CLOSEOUT`は9 hard gateを再実行し、live status surfaceを
同期してfresh independent scoreを得るseparate docs-only logical taskである。
specification/source/test/expectation/trace row/coverage mapping/oracleは変更しない。

Task-46実測oracle: parser productionは12 paths / 38,940 lines、path hash
`192f9d0b5e6534c4daab010ec51a9356e9e0fd6fb86876bd2600a75844e7566a`、
ordered content-manifest hash
`6f27be7c5689cc12b6cf684736bc44b1f92acebf6ce313ce581b22a46451cb5b`。
parser testはunit 225 / determinism 3 / lint-policy 14、syntaxはunit 55 /
lint-policy 8。plan 409/370、parse coverage 44/44、active
parse/declaration/type/proof admission 101/5/188/1、pass/fail 223/186、
warnings/errors 23/0。plan/parse/declaration/type/proof stdout hashは
`9b1e3058bde355163b1153339250647633beef9920456615cf6661c4140a93cf`、
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`、
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`、
`1dadbeabb219f5853c713ad53aa1cc7cd720a0e80abd7f882e9e0a5ea7802625`、
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`。

## Responsibility

`mizar-parser`はfrontend tokenを消費し、parser-owned syntax event、recovery
diagnostic、`mizar-syntax` surface treeを生成する。concrete grammar recognition、
source-order preservation、grammar boundary recoveryを所有する。

name resolution、type checking、proof acceptance、semantic evidence、CoreIr、
ControlFlowIr、VC generation、既存parser vocabulary外のpublic diagnostic code
allocationは所有しない。

## Specification Items

| Spec ref | Requirement | Task 47選定時のfresh status |
|---|---|---|
| Chapter 4 §4.4.2 | `reconsider_tail ::= [ simple_justification ] ";" \| proof ";"`。 | `source_drift`: parserは`by`を必須にし、既存mixed recovery fixtureも同じ不一致を記録する。 |
| Chapter 8 §8.2.2 | justification省略はsyntax-admissibleであり、proof-free evidenceがsemantic obligationをdischargeできない場合だけ後段でrejectする。proof blockもvalid tailである。 | parser Task 47は実行可能。checker Task 44がsemantic authorityを確定し、external gateは残らない。 |
| Chapter 15 §§15.5.1, 15.8.2, 15.12 | omitted、simple-justification、proof-block `reconsider` tailは別々のvalid syntax formである。 | omitted/proof-block tailのexact parse-only rowはTask 47だけを理由にdeferred。 |
| Appendix A §§A.4, A.15 | type-changing statement summaryも同じ`reconsider_tail`を使う。 | parser/design alignmentはTask 47待ち。 |

English specificationはTask 47に十分で矛盾しない。selected sliceをblockする
`spec_gap`、`repo_metadata_conflict`はない。

## Relevant Tests

| Test path | Intent | Spec refs |
|---|---|---|
| `tests/miz/pass/parser/pass_parser_consider_reconsider_001.miz` | bare/equated itemの既存explicit `by` control。 | Chapter 15 statement syntax |
| `tests/miz/fail/parser/fail_parser_consider_reconsider_recovery_001.miz` | 既存mixed recovery source。`reconsider x as set;`だけがvalidとなり、他のmalformed statementはfailureのまま。 | Chapter 15 statement syntax; Task 47 drift classification |
| `tests/miz/fail/types/fail_types_reconsider_omitted_justification_001.miz` | inactiveな後段semantic rejection。Task 47はactivateもreinterpretもしない。 | Chapter 8 omitted-justification semantic gate |
| `tests/miz/pass/parser/pass_parser_reconsider_tails_001.miz` | omitted/proof-block tail、bare/equated item、concrete proof bodyをcoverするplanned test-first active parse-only case。 | Chapters 4/8/15; Appendix A |

Task 47はexact `test_gap`から導いたplanned `.miz`とexpectationを追加できる。既存
`.miz`は変更しない。existing mixed recovery expectationはobsoleteなomitted-tail
parser diagnosticだけを削除する。これは記録済み`test_expectation_drift`のauthorized
repairであり、implementation由来rebaselineではない。

## Design And Source Inventory

- `grammar.md`はTask-18 mandatory/simple-only behaviorを記述し、その修正ownerを
  Task 47に割り当てる。
- `recovery.md`、`source_spec_audit.md`、`todo.md`、paired bilingual auditも同じ
  owner/driftを記録する。
- traceにはomitted/proof-block tailのexact deferred parse-only rowが2件ある。
- private `parse_reconsider_statement_at`は現在
  `parse_mandatory_simple_justification`を呼ぶ。
- 既存private `parse_justification_clause_at`と`parse_proof_block_at`がexplicit
  tailを所有する。
- `mizar-syntax`には必要な`ReconsiderStatement`、`ReconsiderItem`、
  `JustificationClause`、`ProofBlock`が既にあり、新public node/accessorは不要。

selection HEAD `d3cc4e1e391b6f3800b35168ec8089e545d69f2d`のbaselineはparser source
12 Rust path / 36,482 line、parser test 208 unit / 3 determinism / 14 lint-policy。
corpus planは404 case / 369 requirement、warning/error 23/0、parse-only 96 case。
parse-only coverageは43 requirement中covered 40 / deferred 3。

## Known Gaps And Drift

| ID | Class | Evidence | Task 47 action |
|---|---|---|---|
| P-265-47A | `source_drift` | parserが4 canonical grammar locationに反して`by`を必須にする。 | `;`前のno tail、既存`by`、既存`ProofBlock` childをacceptする。 |
| P-265-47B | `test_expectation_drift` | active mixed recovery sidecarがvalid omitted formに`malformed_justification`を期待する。 | obsolete diagnosticだけを削除し、他のrecovery intentを維持する。 |
| P-265-47C | `test_gap` | exact parse-only requirement 2件にactive testがない。 | 両tail formをexerciseするspec-derived active pass fixture/sidecarを1件追加する。 |
| P-265-47D | `spec_gap`（nonblocking / out of scope） | Chapter 8のcompact EBNFは`reconsider_item` 1件と書く一方、Chapters 4/15とAppendix Aは`type_change_list`を一貫して使う。selected tail grammarは4文書で一致する。 | Task 47で`doc/spec`を編集せず、list grammar changeを推測しない。既存source-order item listを維持し、このwording inconsistencyをhuman-ownedとして報告する。 |
| P-265-48 | `source_drift` / `test_gap` | canonical Chapter 7 property-implementation grammarは未実装。 | 独立Task 48へ残す。 |
| P-046 | deferred | concrete operator declarationはnamed frontend context待ち。 | Task 47でpromoteしない。 |

selected sliceに`source_undocumented_behavior`、`boundary_violation`、
`repo_metadata_conflict`はない。Task-18 mandatory-tail design textとmissing crate
planはTask 47が同期する`design_drift`である。

## Specification Coverage Audit Impact

Task 47では`doc/design/spec_coverage_audit.md`更新が必要。Chapters 4/8/15と
Appendix Aのparser部分をTask-47 drift/deferred wordingからexact active parse-only
coverageへ変更する。semantic E0102 gate、checker Tasks 251/271-272、`MT10-AS`、
Task 48、Steps 6/7にはcreditしない。

## Task Decomposition

### Task 47: `reconsider` justification parser alignment

purposeはP-265-47A/B/Cだけをcloseすること。

authority sliceはEnglish Chapters 4 §4.4.2、8 §8.2.2、15 §§15.5.1/15.8.2/
15.12、Appendix A §§A.4/A.15、exact deferred trace row、既存`.miz` control。

consumer/visibilityは既存private `parse_reconsider_statement_at` producerと既存
parse-only runner。public syntax node kind/accessorは変更しない。

required behavior:

- `reconsider ... as T;`はjustification childもparser diagnosticもない
  `ReconsiderStatement`を生成する。
- `reconsider ... as T by refs;`は`JustificationClause`を1件維持する。
- `reconsider ... as T proof ... end;`は既存missing-`end` recoveryを含む
  `ProofBlock`を1件維持する。`ProofBlock`は`proof`、reasoning body、`end`を
  ownershipし、`ReconsiderStatement`はfinal semicolonをownershipする。
- bare/equated item、target type ownership、source order/range、`then` wrapping、
  explicit-simple behavior、malformed item/type recoveryは不変。
- `consider`はmandatory-simple-justification hostのまま。

tests:

- planned test-first pass fixture/sidecarを追加する。
- 3 tail form、AST child ownership、recoveryなしのomission、proof-block
  missing-`end` recovery、unchanged `consider`/malformed item behaviorをunit testする。
- proof blockをrejectするhostの既存unit-test listから`reconsider`だけを外し、真に
  simple-onlyな全hostはguardし続ける。
- existing mixed recovery expectation/unit countはobsolete omitted-tail errorだけを更新。
- Task-47 trace row exactly 2件だけをactivateし、general Chapter-15 statement rowへ
  new pass caseを追加する。

source edit前にaffected design pathを次へ固定する:

- `doc/design/mizar-parser/README.md`;
- parser paired `00.crate_plan.md`、`README.md`、`grammar.md`、`recovery.md`、
  `source_spec_audit.md`、`todo.md`、
  `bilingual_documentation_synchronization.md`;
- mizar-test paired `00.crate_plan.md`、`todo.md`、`harness.md`、
  `traceability.md`、`bilingual_sync_audit.md`、`module_boundary_audit.md`;
- `doc/design/todo.md`と`doc/design/spec_coverage_audit.md`。

parser `pratt.md`、他component design、historical current-state snapshotはout of scope。
source/test pathは正確に`crates/mizar-parser/src/module.rs`、
`crates/mizar-parser/src/module/tests.rs`、`crates/mizar-test/tests/metadata.rs`、
`tests/miz/pass/parser/pass_parser_reconsider_tails_001.miz`とmatching sidecar、
`tests/miz/fail/parser/fail_parser_consider_reconsider_recovery_001.expect.toml`、
`tests/coverage/spec_trace.toml`。必要なnode/accessorは既存なので`mizar-syntax`
source changeはauthorizedしない。

expected corpus deltaはactive pass +1: plan 405/369、pass/fail 221/184、parse-only
97、parse coverage 43 requirement中covered 42 / deferred 1、warning/error 23/0。
plan/parse-only CLI hashだけが変わり、他runner admission/semantic outputは不変。

completion conditionはexact behavior/recovery test、real parse-only execution、exact
row 2件covered、old mixed failureのnon-Task-47 diagnostic維持、paired docsと全oracle
一致、focused/full verification、全独立review findingなし、1 commit、clean tree。

forbiddenは`doc/spec` edit、existing `.miz` edit、semantic acceptance/E0102 production、
proof search、resolver/checker payload、theorem acceptance、public parser API/node/
diagnostic追加、`reconsider` computation justification、`consider`緩和、Task 48、
checker Task 271、Steps 6/7、unrelated parser cleanup。

### Task 48: property-implementation grammar and parse-only corpus

`todo.md`でauthorized済みの独立later task。purposeはChapter 7 §§7.4.1/7.8.2の
`property_impl` means/equals grammar、meansのmandatory existence/uniqueness、optional
coherence、case/otherwise definiens、owner/property dot recovery、source order、typed
syntax accessorだけを実装すること。

testは列挙済みform/recovery boundaryのspec-derived pass/fail parser `.miz`と
parser/syntax unit coverage。fresh task freezeでexisting fixtureを変更せずexact filename
を選ぶ。affected design/sourceはpaired parser plan/grammar/recovery/audit/TODO、
`module.rs`/test、必要な`mizar-syntax` AST/accessor、parse-only expectation/trace、
mizar-test count assertion、coverage audit。coverageは
`spec.en.07.modes.property_implementation.parser`だけをactivateし、Task-39 semantic
coverageはgatedのまま。

completionはpositive formのordered typed node、exact recovery、real parse-only execution、
paired docs/trace/oracle一致、review/verification pass、clean 1 commit。forbiddenは
mode/property resolution、overlap/coherence semantics、proof acceptance、checker payload、
advanced-semantics activation、existing expectation rebaseline、broader definition cleanup、
Tasks 46/47、Steps 6/7。edit前にfresh inventory/reviewed contractを改めて要求する。

### Task 46: concrete operator declarations

fresh inventoryでfrontend Task 20がnamed string-required contextをすでに満たしていたと
確認した後にcompleteした。current contractとmeasured oracleは本plan冒頭に記録する。

## Exit Criteria

Task 47 hard gateは上記completion condition。推奨commandはEnglish canonical planに
列挙し、focused expectation/metadata test、5 CLIとstdout hash、raw/normalized
mizar-test test-list hash、production path/content hash、HEAD/clean treeも含む。
Task-47 checkpointではparser crate全体をcloseしていなかった。Task 48はauthorized
nonempty Step-5 taskとして残り、Task 46はexplicit deferredのままだった。

eventual parser-crate closeoutはTasks 47/48 complete、Task 46がnamed external context上
legitimately deferred、protocol hard gate全pass、paired docs/trace/oracle一致、read-only
quality review 90/100以上を要求する。Task 47はincrementでありcloseoutではない。

## Task 47 completion record

Task 47は上記frozen slice exactlyを実装した。parser source inventoryは12 Rust path /
36,702 line、unit 211、determinism 3、lint-policy 14 testである。corpusは405 case /
369 requirement、parse-only admissionは97/97、
parse coverageは43 requirement中42 covered / 1 deferred、pass/failは221/184、
warnings/errorsは23/0である。plan/parse-only stdout hashは順に
`b23ef45284bbdde020431ec1f8a2b6a92c27940b4f5c0ca5117a6b5331d653ab`、
`b824094f5bb4b57fb0cd976d669034aa5a3b294eaa8dc40264d8fb5bf3e48959`である。
declaration/type/proof admission/hashは5/188/1および
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8` /
`1dadbeabb219f5853c713ad53aa1cc7cd720a0e80abd7f882e9e0a5ea7802625` /
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`のままである。

P-265-47A/B/Cとpaired `design_drift`はcloseした。P-265-47Dはnonblockingでhuman-ownedの
wording `spec_gap`として残る。このcheckpointではTask 48が次のauthorized nonempty
parser task、Task 46はdeferredのままで、Steps 6/7はpromoteしなかった。

## Task 48 fresh freeze

selection baselineはHEAD
`7dffcd21066ef5992bebdccc748379b1c7200ac2`、clean worktree。Task IDは
`P-265-48` / parser Task 48。`SPEC-07-PI-PLACEMENT`はこのtaskより先に
canonical placement authorityを修正した。complete `property_impl`は通常の
`definition_block`の内部contentではなくtop-level `declaration`である。旧Task-48
TODOの「definition-local」はderived `design_drift`なので、本タスクでEnglish
specification側へ同期し、旧nested shapeのauthorityとはしない。

authority sliceはEnglish Chapter 7 §§7.4.1/7.8.2/7.10、Chapter 12 §12.7、
Appendix A §§A.7/A.12、exact deferred trace requirement
`spec.en.07.modes.property_implementation.parser`、existing parser corpus controlである。
authorityはsufficientかつnon-contradictory。exact declaration producer/typed nodeがない
current sourceを`source_drift`、active caseがないexact trace rowを`test_gap`、
implementationをdeferするparser/syntax/mizar-test designを`design_drift`と分類する。
selected sliceに`spec_gap`、`source_undocumented_behavior`、
`boundary_violation`、`repo_metadata_conflict`はない。incomplete direct
`definition property ... end;` placeholderをdiagnostic-freeとするinternal parser unit test
2件は`test_expectation_drift`である。Task 48はこの2 expectationだけをdedicated
recoveryで`MissingEnd`なしの期待へ修正する。

purpose/consumerはparser、snapshot、typed syntax、real parse-only runner向けの、
syntax-onlyでsource-orderを保存するtop-level property implementation declarationを一つ
追加すること。append-only public `SyntaxKind::PropertyImplementation = 192`、
corresponding `SurfaceNodeKind`、stable snapshot name、raw-kind mapping、node-kind
classification、`as_property_implementation` accessorを追加する。existing
`DefinitionParameter`、`TypeHead`、`QualifiedSymbol`、`TypeArguments`、
`FormulaDefiniens`、`TermDefiniens`、`CorrectnessCondition`、justification/proof nodeを
reuseし、semantic fieldやsecond mode-application schemaは追加しない。

required parser behavior:

- property-shaped `definition`をgeneric `DefinitionBlockItem`より先に専用top-level
  producerへdispatchする。bounded discriminatorはdirect `property` startまたは、initial
  `let` regionの中でouter `end`/next top-level boundaryより前にあるtop-level
  `property`を選ぶ。delimiter/nested-block depthをtrackするので、parameter semicolonが
  missingでも専用recoveryを選び、nested proof内の`property`はmatchしない。
  `property` keywordがない`definition`はordinary definition blockと識別できないため
  generic producerに保ち、Task 48はspecialized missing-`property` recoveryを約束しない。
  missing parameter semicolon、bounded negative discriminator、following-declaration preservationを
  testでpinする。canonical top-level annotation prefixは、他declaration producerと同じ
  ownershipでsource orderを保つ。
- `let identifier be mode_application;`に専用の`DefinitionParameter` childを1つ
  produceする。ordinary identifier 1つ、exact `be`、qualified constructor symbolとoptional
  existing `TypeArguments`を持つ`TypeHead`で表した`mode_ref_name`だけを受理する。
  multiple binder、`being`、`such`/`by` constraint、missing piece、builtin
  `object`/`set`、attribute chain、trailing parameter materialは拒否する。generic
  definition-parameter parserは変更しない。
- ownerの`PropertyImplementation`に`property`、owner identifier、dot、property
  identifier、exact `means`/`equals`を保存する。`means`は一つの
  `FormulaDefiniens`、mandatoryでorderedな`existence`/`uniqueness`
  `CorrectnessCondition`、optional `coherence`を持つ。`equals`は一つの
  `TermDefiniens`とoptional `coherence`だけを許し、`existence`/`uniqueness`は
  invalidとdiagnoseする。両definiens branchはsimple/case/otherwise parserをreuseする。
- correctness conditionはomitted simple justification、`by` reference、
  `by computation(...)`、`proof ... end`を含むexisting general justification grammarを
  reuseする。Task 48はsyntax shapeだけを保存し、proofをaccept/dischargeしない。
- outer `end;`は`PropertyImplementation`が所有する。bounded discriminatorが
  `property`を検出した場合、malformed/missingなparameter/owner/property-name/dot/
  body/correctness/terminatorをexisting diagnostic/recovery vocabularyで回復し、invalid tokenの
  source orderを保存し、correctness keyword、outer `end`、next top-level item、EOFで
  synchronizeする。following declarationをconsumeしない。

test-first fileは
`tests/miz/pass/parser/pass_parser_property_implementations_001.miz`とsidecar、
`tests/miz/fail/parser/fail_parser_property_implementations_recovery_001.miz`とsidecarに固定する。
pass sourceはmeans/equals、simple/case/otherwise definiens、mandatory
existence/uniqueness、optional coherence、omitted/reference/computation/proof
justification shapeをcoverする。fail sourceはmalformed owner/property dot、missing/reordered
means correctness condition、forbidden equals correctness condition、following declarationへのrecoveryを
coverする。parser unit testはexact single-binder/`be`/mode-application restrictionと
unchanged ordinary definition blockもpinする。stale direct-property placeholder assertion 2件は
block-depth/no-`MissingEnd` intentを保ちつつTask-48 diagnostic/node ownershipを期待する。
syntax testはraw-kind、node/accessor、snapshot
name、green-tree、append-only numberingをpinする。existing `.miz`/expectationは変更しない。

exact source/test scopeは`crates/mizar-parser/src/lib.rs`、`src/module.rs`、
`src/module/tests.rs`、
`crates/mizar-syntax/src/ast.rs`、`src/ast/snapshot.rs`、`src/ast/tests.rs`、
`crates/mizar-test/tests/metadata.rs`、新規corpus 4 file、
`tests/coverage/spec_trace.toml`である。exact trace rowのsection/notesには、すでにcanonicalな
Chapter-12/A.12 top-level placement authorityも追加し、変更はこの1 rowと2 test linkに
限定する。expected corpus deltaはactive parse-only 2 case:
407 case / 369 requirement、parse-only 99/99、parse coverage 43/43、pass/fail
222/185、warnings/errors 23/0。declaration/type/proof admissionは5/188/1のまま。
exact rowだけを新規2 sidecarでdeferredからcoveredへ変更する。inactive Task-39
advanced-semantic fixture/sidecarはbyte-identical/inactiveに保つ。

documentation scopeはroot parser README、paired parser plan/README/grammar/recovery/
source-spec audit/TODO/bilingual audit、root syntax READMEとpaired syntax plan/README/AST/
grammar audit/source-spec correspondence/TODO/crate exit report/bilingual audit、paired mizar-test plan/harness/traceability/
TODO/bilingual audit/module-boundary audit、top-level TODO、
`doc/design/spec_coverage_audit.md`である。coverage auditは新規active Chapter-7/
Appendix-A/Chapter-12 parser/syntax surfaceだけをcreditし、semantic/payload/coherence/
Steps-6/7 deferralを全て保持する。

exit criteria: test-first focused AST assertionがmissing producerにより当初failした証跡を
記録すること（parse-only pass runner単体はAST shapeを比較しない）。required
positive/negative/recovery/typed-node assertionがすべてpassすること。exact trace rowだけが
activateされ、paired docs/count/hash oracleが一致すること。
focused parser/syntax/mizar-test、fmt、workspace Clippy、full workspace tests、5 CLI、
test-listとproduction path/content hash、diff checkがpassすること。independent
spec/test/implementation/source-doc reviewがno findingsで終了し、変更をexactly one
Task-48 commitとし、直後のclean worktreeを確認する。

forbidden: 追加`doc/spec` edit、existing `.miz`/expectation edit、generic
definition-parameter/broader definition cleanup、mode/property resolution、property payload
extraction、overlap/coherence decision、proof acceptance/discharge、checker/Core/CFG/VC、
advanced-semantic runner activation、public diagnostic追加、Tasks 46/49、Steps 6/7、
unrelated refactor。

## Task 48 completion record

Task 48はfrozen sliceだけを実装した。test-first AST assertionはproducer実装前の
generic/placeholder shapeでfailした。final parser source inventoryは12 Rust path /
38,256 line、unit 221、determinism 3、lint-policy 14 test、syntaxはunit 54、lint-policy
8 testである。corpusは407 case / 369
requirement、parse-only 99/99、parse coverage 43/43、pass/fail 222/185、
warnings/errors 23/0である。declaration/type/proof admissionは5/188/1のままである。

5 CLI stdout hashは
`2957a40b91a4cf64206301b4bf91d1c42ecdac2a564b70af370d2e52333ab57b`、
`c9dcbcef79e727f31720d46532febe5a20e02a7710cf691e49d89fcfb69bccfa`、
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`、
`1dadbeabb219f5853c713ad53aa1cc7cd720a0e80abd7f882e9e0a5ea7802625`、
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`である。
unchanged raw/normalized 276-test-list hashは
`967495e78e1068f592e64834ea3ffb9eac9c25692ea5cbd4f11006a679c66590` /
`1be4ae09188b27a40814adc6597de4806dabb13bcac019b294154e1455072adf`、
unchanged 18-path production hashは
`63e4e770b0d10872415548410d417071c1901f3ffa5aea964a81d2dbbc572ed0` /
`7e5adca22db2b73f94f04c406f10788f2cd49ba48109bb105a3fd076c339d560`である。

P-265-48 `source_drift` / `test_gap`、paired documentation `design_drift`、internal unit
`test_expectation_drift` 2件をcloseした。existing `.miz`/expectationとinactive Task-39
semantic seedは変更しない。specification、test sufficiency、implementation、
source/document consistency reviewはno findingsで終了した。focused test、format、
denied-warning workspace Clippy、full workspace test、5 CLI、test-list/production
manifest、diff checkはpassした。Task 46とSteps 6/7はdeferredのままで、Task 49を
promoteしない。

## Superseded Pre-Task-46 Parser Crate Closeout Record

`PARSER-CRATE-CLOSEOUT`は当時のparser crate milestoneを閉じたが、global Step 5は
閉じなかった。当時のexplicit closeout conditionは満たされた。Tasks 47/48はcomplete、
Task 46はnamed frontend contextにより正当にdeferredと扱われ、paired
exit report と stale index の同期後は全 protocol hard gate が pass、独立
read-only quality score は 94/100 である。

milestoneはTasks 1-45/47-48を含んだ。P-043-01/P-046は同一の
operator-declaration `source_drift` / `test_gap`、P-265-47D は別の nonblocking
human-owned `spec_gap`だった。そのcheckpointではauthorized nonempty successor
parser taskはなかった。本recordはhistoryとして保持し、下記post-Task-46 recordにより
supersededされる。

## Post-Task-46 Parser Crate Closeout Record

`PARSER-CRATE-POST-TASK46-CLOSEOUT`はTasks 1-48までのparser milestoneを閉じるが、
global Step 5は閉じない。9 protocol hard gateはすべてpassし、fresh independent
read-only scoreは99/100である。P-043-01/P-046はTask 46によりclosed。
P-265-47Dはdistinct nonblocking human-owned `spec_gap`のままで、overbroad frontend
string-position heuristicはexternal frontend `source_drift` /
`source_undocumented_behavior` + unit `test_expectation_drift`としてparser credit外に残る。

authorized nonempty successor parser taskはない。Task 49を推定せず、Steps 6/7を
promoteしない。[crate_exit_report.md](./crate_exit_report.md)をcanonical current
closeout evidenceとする。本docs-only taskはspecification/source/test/expectation/
trace row/coverage mapping/owner/deferred rationaleを変えないため、
`doc/design/spec_coverage_audit.md`の更新は不要。
