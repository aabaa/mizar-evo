# Crate Plan: mizar-syntax

> Canonical language: English. Japanese companion:
> [../ja/00.crate_plan.md](../ja/00.crate_plan.md).

Status: autonomous crate-development plan; S-018 incremental syntax reuse audit
is complete after parser task 35.

This plan is the crate-scope entry point required by
[the autonomous crate-development protocol](../../autonomous_crate_development.md)
before further autonomous `mizar-syntax` implementation work. It records the
current boundary, the specification and test inputs, known gaps and drift, the
remaining task split, and the exit gates for completing crate-wide syntax work.

## Responsibility

Owned behavior:

- define the source-shaped `SurfaceAst` boundary produced by `mizar-parser`;
- own rowan-backed syntax storage, deterministic syntax snapshots, typed
  compatibility views, and syntax-kind vocabulary;
- own syntax-level diagnostics and recovery node vocabulary;
- own syntax-facing trivia side tables for comment ranges, doc-comment
  attachment hints, skipped-token ranges, and whitespace hints;
- provide parser-facing builder/accessor APIs without exposing raw storage as a
  semantic identity contract.

Out of scope:

- lexing, token disambiguation, parser-assisted lexing plans, and raw comment
  extraction, which belong to `mizar-frontend` / `mizar-lexer`;
- grammar parsing and synchronization producers, which belong to `mizar-parser`;
- name resolution, type checking, overload resolution, cluster facts, proof
  obligations, theorem validity, algorithm VC generation, and package/build
  semantics;
- `salsa` query integration and cache orchestration;
- stable external artifact schemas.

Boundary rule: `mizar-syntax` may represent surface syntax and recovery facts,
but it must not encode resolved symbol ids, inferred types, semantic categories
that require the active environment, or proof/VC obligations.

## Specification Items

| Spec ref | Requirement | Status |
|---|---|---|
| [ast.md](./ast.md) | `SurfaceAst` uses rowan-backed storage behind builder/accessor APIs, with compatibility views during migration. | Implemented foundation; vocabulary still growing. |
| [trivia.md](./trivia.md) | `SurfaceTrivia` stores syntax-facing trivia hints while raw comment bodies stay frontend-owned. | Implemented foundation; first syntax-level item attachment fixture landed. |
| [recovery.md](./recovery.md) | Recovery nodes cover missing constructs, skipped tokens, unmatched delimiters, and malformed annotations. | Vocabulary implemented; task-5 skipped-token producer landed; many parser producers remain future work. |
| [appendix A](../../../spec/en/appendix_a.grammar_summary.md) | Parser-facing grammar summary defines the surface constructs that future node vocabulary must represent. | Audited by [grammar_audit.md](./grammar_audit.md). |
| [12.modules_and_namespaces.md](../../../spec/en/12.modules_and_namespaces.md) | Module file shape, imports, exports, and item visibility. | Module skeleton, import items, export items, and top-level visibility wrappers landed. |
| [03.type_system.md](../../../spec/en/03.type_system.md), [05.structures.md](../../../spec/en/05.structures.md), [06.attributes.md](../../../spec/en/06.attributes.md), [07.modes.md](../../../spec/en/07.modes.md) | Type, structure, attribute, and mode surface forms. | Planned node vocabulary. |
| [09.predicates.md](../../../spec/en/09.predicates.md), [10.functors.md](../../../spec/en/10.functors.md), [13.term_expression.md](../../../spec/en/13.term_expression.md), [14.formulas.md](../../../spec/en/14.formulas.md) | Predicate, functor, term, and formula surface forms. | Planned node vocabulary with ambiguity-preserving shapes. |
| [15.statements.md](../../../spec/en/15.statements.md), [16.theorems_and_proofs.md](../../../spec/en/16.theorems_and_proofs.md) | Proof statements, justifications, theorem items, and proof nesting. | S-013 statement nodes complete; S-014 theorem/proof nodes complete. |
| [17.clusters_and_registrations.md](../../../spec/en/17.clusters_and_registrations.md), [18.templates.md](../../../spec/en/18.templates.md), [20.algorithm_and_verification.md](../../../spec/en/20.algorithm_and_verification.md), [21.source_code_annotation_and_atp.md](../../../spec/en/21.source_code_annotation_and_atp.md) | Registrations, templates, algorithms, claims, verification clauses, and annotations. | Registration node vocabulary implemented for parser task 30; template parameter/loci/argument vocabulary implemented for parser task 31; basic non-contract algorithm/claim vocabulary implemented for parser task 32; algorithm control-flow vocabulary implemented for parser task 33; algorithm verification clause vocabulary implemented for parser task 34; annotation vocabulary implemented for parser task 35. |
| [appendix B](../../../spec/en/appendix_b.operator_precedence.md) | Operator precedence and associativity must be represented without semantic resolution. | Task 12 expands the term vocabulary from minimal infix to active prefix/postfix/infix operator surfaces. |

## Relevant Tests

| Test path | Intent | Spec refs |
|---|---|---|
| `crates/mizar-syntax/src/ast.rs` unit tests | Builder/accessor round trips, range invariants, deterministic snapshots, recovery rendering, and trivia validation. | [ast.md](./ast.md), [recovery.md](./recovery.md), [trivia.md](./trivia.md) |
| `crates/mizar-syntax/src/trivia.rs` unit tests | Deterministic trivia sorting and attachment data contracts. | [trivia.md](./trivia.md) |
| `crates/mizar-syntax/src/recovery.rs` unit tests | Recovery kind constructibility and diagnostic contracts. | [recovery.md](./recovery.md) |
| `crates/mizar-syntax/tests/lint_policy.rs` | Workspace lint opt-in, documented allow exceptions, and public enum compatibility policy. | [todo.md](./todo.md), [ast.md](./ast.md), [trivia.md](./trivia.md), [recovery.md](./recovery.md) |
| `tests/snapshots/mizar_syntax_surface_ast_current_vocabulary.snap` | Baseline snapshot for the currently implemented syntax vocabulary. | [ast.md](./ast.md) |
| `tests/miz/pass/parser/pass_parser_minimal_token_stream_001.*` | Active parse-only pass case for frontend-reachable parser/syntax surface. | [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md) |
| `tests/miz/fail/parser/fail_parser_missing_definition_end_001.*`, `tests/miz/fail/parser/fail_parser_stray_end_001.*` | Active parse-only recovery cases for current parser producers. | [doc/design/mizar-parser/en/recovery.md](../../mizar-parser/en/recovery.md), [recovery.md](./recovery.md) |
| `tests/miz/pass/parser/pass_parser_module_skeleton_001.*`, `tests/miz/fail/parser/fail_parser_missing_block_semicolon_001.*`, `tests/miz/fail/parser/fail_parser_missing_definition_end_001.*`, `tests/miz/fail/parser/fail_parser_missing_semicolon_001.*`, `tests/miz/fail/parser/fail_parser_unexpected_top_level_token_001.*` | Active parse-only module skeleton and top-level recovery cases for parser task 5. | [12.modules_and_namespaces.md](../../../spec/en/12.modules_and_namespaces.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md), [recovery.md](./recovery.md) |
| `tests/miz/pass/parser/pass_parser_inline_definitions_001.*`, `tests/miz/fail/parser/fail_parser_inline_definitions_recovery_001.*` | Active parse-only local inline-definition pass/recovery cases for parser task 21. | [15.statements.md](../../../spec/en/15.statements.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md), [ast.md](./ast.md), [recovery.md](./recovery.md) |
| `tests/miz/pass/parser/pass_parser_registrations_001.*`, `tests/miz/fail/parser/fail_parser_registrations_recovery_001.*` | Active parse-only registration pass/recovery cases for parser task 30, including registration-local parameters, parameterized registered types, cluster branches, compound reductions, definition-local visibility, malformed registration content, and missing-end recovery. | [17.clusters_and_registrations.md](../../../spec/en/17.clusters_and_registrations.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md), [ast.md](./ast.md), [recovery.md](./recovery.md) |
| `tests/miz/pass/parser/pass_parser_template_arguments_001.*`, `tests/miz/pass/parser/pass_parser_template_references_001.*`, `tests/miz/fail/parser/fail_parser_template_arguments_chained_iff_001.*` | Active parser task-31 parse-only coverage for template parameters, pattern loci, call-site/reference arguments, citation reference arguments, and chained-`iff` rejection after template predicate arguments become concrete syntax. | [18.templates.md](../../../spec/en/18.templates.md), [appendix A](../../../spec/en/appendix_a.grammar_summary.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md) |
| `tests/miz/pass/parser/pass_parser_algorithms_claims_001.*`, `tests/miz/fail/parser/fail_parser_algorithms_claims_recovery_001.*` | Active parser task-32 parse-only coverage for basic algorithm definitions, identifier-only algorithm schema loci, parameters, optional return types, visibility wrappers, declarations, assignments, snapshots, returns, top-level claims, and recovery for malformed algorithm/claim surfaces. | [20.algorithm_and_verification.md](../../../spec/en/20.algorithm_and_verification.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md) |
| `tests/miz/pass/parser/pass_parser_algorithm_control_flow_001.*`, `tests/miz/fail/parser/fail_parser_algorithm_control_flow_recovery_001.*` | Active parser task-33 parse-only coverage for algorithm if/else, while, `to`/`downto` range loops with optional `step`, collection loops with and without `processed`, multiple match cases, `otherwise` / `exhaustive` endings with and without justification, `break`, `continue`, and malformed control-flow surfaces. | [20.algorithm_and_verification.md](../../../spec/en/20.algorithm_and_verification.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md) |
| `tests/miz/pass/parser/pass_parser_algorithm_verification_001.*`, `tests/miz/fail/parser/fail_parser_algorithm_verification_recovery_001.*` | Active parser task-34 parse-only coverage for terminating algorithms, header `requires` / `ensures` / `decreasing`, while `invariant` / `decreasing`, range/collection `invariant`, `assert`, duplicate/out-of-order header recovery, forbidden `for decreasing`, misplaced loop clauses, and malformed `TermList` recovery. | [20.algorithm_and_verification.md](../../../spec/en/20.algorithm_and_verification.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md) |
| `tests/miz/pass/parser/pass_parser_annotations_001.*`, `tests/miz/fail/parser/fail_parser_annotations_recovery_001.*` | Active parser task-35 parse-only coverage for library annotations, generic annotations, fixed `@latex` / `@proof_hint` / `@suppress` / debugging annotations, standalone `@show_type` / `@eval`, annotation attachment at top-level declaration, definition, registration, proof, algorithm, and claim sites, and malformed annotation recovery. | [21.source_code_annotation_and_atp.md](../../../spec/en/21.source_code_annotation_and_atp.md), [doc/design/mizar-parser/en/grammar.md](../../mizar-parser/en/grammar.md) |
| `tests/coverage/spec_trace.toml` | Traceability metadata for active and deferred parser-facing syntax cases. | [doc/design/mizar-test/en/traceability.md](../../mizar-test/en/traceability.md) |

Planned test-first additions:

- Activate rows from [parse_only_fixture_seed.md](./parse_only_fixture_seed.md)
  only when the owning parser task can satisfy the expectation through the
  frontend parser seam.
- Each node-vocabulary increment must add or activate `.miz` parse-only corpus
  cases, `.expect.toml` sidecars, and `tests/coverage/spec_trace.toml` entries
  when the corresponding grammar position is executable.
- AST snapshots are added only after the increment documents node kinds, child
  roles, range rules, accessors, recovery/trivia interaction, and snapshot text.

## Design And Source Inventory

Design files:

- [README.md](./README.md)
- [todo.md](./todo.md)
- [ast.md](./ast.md)
- [trivia.md](./trivia.md)
- [recovery.md](./recovery.md)
- [grammar_audit.md](./grammar_audit.md)
- [parse_only_acceptance_matrix.md](./parse_only_acceptance_matrix.md)
- [parse_only_fixture_seed.md](./parse_only_fixture_seed.md)

Source files:

- `crates/mizar-syntax/src/lib.rs`
- `crates/mizar-syntax/src/ast.rs`
- `crates/mizar-syntax/src/trivia.rs`
- `crates/mizar-syntax/src/recovery.rs`
- `crates/mizar-syntax/tests/lint_policy.rs`
- `crates/mizar-syntax/Cargo.toml`

Observed behavior:

- `SurfaceAst` is rowan-backed and exposes compatibility node views for the
  task-12 migration surface.
- The current syntax vocabulary contains root, compatibility token, recovery,
  shared path, task-5 module skeleton shapes, task-6 through task-8
  import/export/type shapes, task-9 through task-12 term shapes, task-13
  atomic formula plus task-14 connective/quantifier formula shapes, and
  S-013 statement shapes through parser task 21, S-014 theorem/proof/
  justification shapes, and S-015 definition, property, structure, and
  registration shapes through parser task 30, plus S-016 template,
  algorithm header/body/claim, algorithm control-flow, and algorithm
  verification and annotation shapes through parser task 35.
- Snapshot rendering is deterministic for the current vocabulary.
- Trivia side tables validate source ownership and node/token attachment
  targets before they are attached to an AST.
- Recovery vocabulary is broader than current parser production: missing-end,
  missing-string, task-5 skipped-token/semicolon producers, task-13
  missing-term recovery for built-in predicate RHS, task-13 missing-type
  recovery for malformed `is` assertion bodies, task-14 missing-formula
  recovery, task-14 unmatched parenthesized-formula recovery, and task-14
  missing-type recovery for explicit quantifier binders are active today.
  Statement and definition-family producers now also exercise missing formula,
  term, type, statement, semicolon, end, unmatched-delimiter, skipped-token,
  malformed correctness-condition, structure, inheritance, registration,
  reduction, template, algorithm, claim, ghost-assignment, and algorithm
  control-flow recovery through parser tasks 16 and 18-33.

Observed behavior is inventory only. It is not normative unless supported by
the specification, tests, traceability metadata, or explicit human approval.

## Known Gaps And Drift

| ID | Class | Evidence | Action |
|---|---|---|---|
| MSYN-GAP-001 | `test_gap` | [parse_only_fixture_seed.md](./parse_only_fixture_seed.md) records many stable seed cases that are not active runner cases yet. | Activate each seed only with its owning parser grammar task; add `.miz`, `.expect.toml`, and traceability metadata without changing the selected intent. |
| MSYN-GAP-002 | `source_drift` | [todo.md](./todo.md) tasks 9-16 list node vocabulary that the current source cannot represent yet. | Add vocabulary incrementally with paired parser tasks; do not claim crate completion until all milestone-owned constructs are represented or explicitly deferred. |
| MSYN-GAP-003 | `source_drift` | [recovery.md](./recovery.md) marks several recovery kinds as vocabulary-only because parser producers do not emit them yet. | Keep syntax kinds constructible; add producer-specific diagnostics/trivia rules when each parser task starts emitting them. |
| MSYN-GAP-004 | `test_gap` | Historical gap for doc-comment-to-item attachment before item nodes existed. | Closed by the task-5 `PlaceholderItem` attachment fixture; keep future concrete-item attachment tests with their owning item-node increments. |
| MSYN-GAP-005 | `spec_gap` | Historical [grammar_audit.md](./grammar_audit.md) definition/template dispatch ambiguity before definition/template AST nodes. | Closed by parser task 31 / S-016: template-shaped definition blocks pre-scan leading template parameters and template-only content, while ordinary definition blocks keep ordinary `DefinitionParameter` dispatch. |
| MSYN-GAP-006 | `spec_gap` | [grammar_audit.md](./grammar_audit.md) records `type_head` radix/mode ambiguity. | Closed for task S-010 by preserving a generic syntactic `TypeHead`; semantic radix/mode classification remains outside `mizar-syntax` until a future human-approved boundary supplies lookup facts. |
| MSYN-GAP-007 | `spec_gap` | Dot roles remained unresolved across namespace paths, selector access/update, and active user symbols before parser task 10. | Closed for the parser/syntax surface by preserving qualified-name heads as qualified surfaces and parsing `.` after an already parsed term as selector/update postfix; scope-dependent selector-versus-namespace classification remains resolver-owned. |
| MSYN-GAP-008 | `spec_gap` | Compact justified equality overlaps with zero-continuation iterative equality. | Closed for parser task 19 / S-013 by dispatching to `CompactStatement` unless a top-level `.=` continuation follows the first equality; continuation cases produce `IterativeEqualityStatement`. |
| MSYN-GAP-009 | `design_drift` | Existing task numbers in `mizar-parser` and `mizar-syntax` do not line up one-to-one after grammar growth begins, and older parser TODO dependency lines may name a syntax task number without the corresponding S-task. | Use the pairing map below as the crate-plan authority for autonomous syntax work; in every task, cite both the `mizar-syntax` S-task bucket and the concrete `mizar-parser` task number. |
| MSYN-GAP-010 | `repo_metadata_conflict` | None observed during kickoff inventory. | Report only if future metadata conflicts are found; do not auto-repair metadata conflicts under this protocol. |
| MSYN-GAP-011 | `spec_gap` | Historical [grammar_audit.md](./grammar_audit.md) helper follow-ups for template/parser fixture planning. | Closed for task 31 template helpers: `functor_loci` is represented by `TemplateLoci` / `TemplateLocus`, call/reference-side `type_arg_list` / `type_arg` by `TemplateArguments` / `TemplateArgument`, and radix-only `qua_arg` by `QuaExpression` inside `TemplateArgument`. `scheme_app` / `scheme_name` are not task-31 template AST aliases and remain outside S-016 unless a later scheme grammar task is scheduled. |
| MSYN-GAP-012 | `spec_gap` | [grammar_audit.md](./grammar_audit.md) records that Chapter 15 examples/prose allow `per cases;`, while the complete EBNF summary shows an unbracketed `simple_justification`. Existing task-19 active coverage also kept `then per cases;` as a fragment. | Closed for the parser task-20 surface: the parser accepts both `per cases;` and `per cases by A;`, records a `JustificationClause` only for the explicit `by` form, and covers both forms in fixtures. Any stricter proof/exhaustiveness rule remains later proof semantics or human-owned spec cleanup. |
| MSYN-GAP-013 | `test_gap` | Dotted algorithm `Lvalue` is represented and unit-tested in parser task 32, but active `.miz` parse-only coverage cannot yet carry the dotted assignment surface through frontend dot-role disambiguation. | Keep task-32 parser-unit coverage for `Lvalue`; add active `.miz` coverage when the owning frontend/parser dot-role increment can preserve dotted algorithm assignment input without unrelated diagnostics. |

Kickoff conclusion: no blocking crate-wide contradiction is known for starting
the next module/item milestone. Later gaps above are local gates for their
owning increments.

## Parser Task Pairing Map

Use this table when a parser TODO dependency line and the syntax task numbering
appear to disagree. The `mizar-parser` task number controls grammar-change
granularity; the `mizar-syntax` S-task controls the vocabulary bucket and
boundary review.

| Syntax task | Parser tasks | Notes |
|---|---|---|
| S-009 Module, item, and shared path nodes | 4-7 | Parser task 4 is a shared qualified-symbol/module-path helper needed before import/export items; corpus coverage lands with consuming parser tasks 6, 7, and 8. |
| S-010 Type expression nodes | 8 | Implemented with generic type-head boundary for the radix/mode ambiguity. |
| S-011 Term nodes | 9-12, 15 | Consumes the shared path vocabulary from S-009 and adds term-primary, dot/update, `qua`, operator, and Fraenkel shapes. |
| S-012 Formula nodes | 13-14 | Keeps `is_assertion` generic until resolution. |
| S-013 Statement nodes | 16, 18-21 | Parser task 17 justifications are owned by S-014 even when statement forms consume them. |
| S-014 Theorem, proof, and justification nodes | 17, 22 | Covers citation/justification clauses and theorem/proof item nesting. |
| S-015 Definition, structure, and registration nodes | 23-30 | Includes notation aliasing and symbol-management syntax. |
| S-016 Template, algorithm, and annotation nodes | 31-35 | Includes template helper cleanup, algorithm/claim shapes, verification clauses, and annotations. |

## Task Decomposition

### Task S-009: Module, item, and shared path nodes

Purpose: represent shared qualified-symbol/module-path surface nodes, module
file shape, item lists, import/export items, and visibility syntax.

Spec refs: [12.modules_and_namespaces.md](../../../spec/en/12.modules_and_namespaces.md),
[appendix A](../../../spec/en/appendix_a.grammar_summary.md).

Tests: parser task 4 path-helper unit coverage, parser task 5 module skeleton
coverage, parser task 6 import item active parse-only coverage, and parser task
7 export/visibility active parse-only coverage are present. Syntax snapshots
cover every S-009 node role.

Affected design files: [ast.md](./ast.md), [trivia.md](./trivia.md) for
doc-comment item attachment, [recovery.md](./recovery.md) for item-level
skipped or missing nodes.

Affected source files: `src/ast.rs`, `src/trivia.rs`, `src/recovery.rs` as
needed for item recovery.

Completion condition: shared path, module/item node kinds, child roles,
accessors, ranges, snapshots, corpus cases, and traceability entries are present
for parser tasks 4-7.

Forbidden behavior: import resolution, visibility semantics, symbol identity,
or item validity checking.

### Task S-010: Type expression nodes

Purpose: represent attribute chains, generic syntactic type heads, type
arguments, and `of`/`over` argument lists.

Spec refs: [03.type_system.md](../../../spec/en/03.type_system.md),
[appendix A](../../../spec/en/appendix_a.grammar_summary.md).

Tests: activate type-head ambiguity, malformed type arguments, positive
attribute-chain cases, `of`/`over` term argument lists, bracket nested
type-expression arguments, bracket `qua_arg` placeholders, and local
parameter-prefix preservation where tokenization exposes the split.

Affected design files: [ast.md](./ast.md), [recovery.md](./recovery.md).

Affected source files: `src/ast.rs`, `src/recovery.rs` as needed.

Completion condition: type expressions preserve radix/mode ambiguity without
semantic lookup for parser task 8 unless a specification change explicitly
permits otherwise.

Forbidden behavior: deciding whether a type head is a mode, radix, structure,
or attribute-bearing semantic category.

Result: completed by paired parser task 8. `ReserveItem` and `ReserveSegment`
host syntax-only `TypeExpression` nodes, `TypeHead` remains generic,
`TypeArguments` preserves `of`/`over` term argument lists, which task 9 now
fills with `TermExpression` children, plus bracket nested types and `qua_arg`
placeholders. Local `ParameterPrefix` is covered in parser unit tests where
tokens expose the split, and active parse-only pass/fail coverage lands
through `parser.type_fixtures`.

### Task S-011: Term nodes

Purpose: consume shared qualified-symbol/path vocabulary from S-009 and
represent primary terms, choice expressions, applications, syntax-only dot-role
and selector/update surfaces, `qua`, operator expressions, and
Fraenkel/set-builder forms.

Spec refs: [13.term_expression.md](../../../spec/en/13.term_expression.md),
[appendix B](../../../spec/en/appendix_b.operator_precedence.md).

Tests: activate dot-chain, choice/update, `qua`, application delimiter, and
Fraenkel rows as their grammar positions land.

Affected design files: [ast.md](./ast.md), [recovery.md](./recovery.md).

Affected source files: `src/ast.rs`, `src/recovery.rs` as needed.

Completion condition: term nodes preserve source shape and operator facts
without name resolution for parser tasks 9-12 and 15; parser/syntax dot-role
surface shape is documented before selector/update nodes freeze, while
scope-dependent selector-versus-namespace classification remains resolver-owned.

Forbidden behavior: selector-versus-namespace semantic resolution, overload
selection, inferred result types, or sethood evidence.

Task 9 local classification: task-8 `TermPlaceholder` children in `of`/`over`
and attribute argument lists are `source_drift` now that primary terms are the
owning parser task; replace them with syntax-only `TermExpression` nodes.
Primary-term parse-only fixtures are a `test_gap` derived from
[13.term_expression.md](../../../spec/en/13.term_expression.md). Active
user-symbol bracket delimiter pairs remain `source_drift` for parser task 12
because current `ParserInputs` carry fixity entries but no bracket-pair
metadata; task 9 covers the reserved `[` / `]` bracket functor form only.
`template_functor_application` remains deferred `source_drift` for parser task
31 / S-016 because task 9 does not own template argument surfaces.
Ambiguous zero-field `S()` constructors remain generic application syntax until
a future semantic boundary supplies structure facts.

Task 9 result: the primary-term increment is implemented as S-011 progress.
`TermExpression` and its current primary-term children are available through
reserve-hosted term lists, `of` / `over` and attribute argument positions no
longer use task-8 term placeholders, active parse-only primary-term pass/fail
fixtures close the local `test_gap`, and the documented `source_drift` items
remain explicitly assigned to parser tasks 11, 12, and 31 / S-016.

Task 10 local classification: the dot-role surface shape is a nonblocking
`spec_gap` now resolved for parser/syntax by the appendix-A handoff text and
the S-011 `SelectorAccess` surface. Existing `line.end.y` examples were
`design_drift` / nonblocking spec example drift because `end` is reserved while
`field_name` is an identifier; task 10 uses `line.finish.y` in docs and tests.
Selector/update parse-only fixtures are a `test_gap` derived from
[13.term_expression.md](../../../spec/en/13.term_expression.md) and
[02.lexical_structure.md](../../../spec/en/02.lexical_structure.md).

Task 10 result: the selector/update increment is implemented as S-011
progress. `SelectorAccess`, `StructureUpdate`, and `FieldUpdate` are available
through syntax-only term postfix chains, parser unit tests and active
parse-only pass/fail corpus cases close the local `test_gap`, and malformed
selector/update syntax emits the documented `MalformedTermExpression`,
`MissingTerm`, and `UnmatchedOpeningDelimiter` recovery shapes. Standalone
`p.x := t` remains assigned to later statement/algorithm hosts, not this term
increment.

Task 11 local classification: the task-8 bracket `qua_arg` placeholder handoff
is `source_drift` once `qua` becomes the owning parser task; replace bracket
`TermPlaceholder` output with a `TermExpression` / `QuaExpression` surface
while preserving the narrower Appendix-A `qua_arg ::= identifier { "qua"
radix_type }` shape. `QuaExpression` raw syntax vocabulary and typed accessor
coverage are required node-vocabulary work for S-011. Missing or malformed
`qua` target coverage is a `test_gap` derived from
[13.term_expression.md](../../../spec/en/13.term_expression.md). No blocking
`spec_gap` was found for selector/application precedence, left-associative
chains, or `x qua Element of S qua Magma`; these are implemented toward the
canonical term grammar rather than current source behavior.

Task 11 result: the `qua` increment is implemented as S-011 progress.
`QuaExpression` is available through syntax-only term parsing, bracket
`qua_arg` entries no longer use parser-produced `TermPlaceholder` nodes,
parser unit tests and active parse-only pass/fail corpus cases close the local
`test_gap`, and missing or malformed `qua` target syntax emits the documented
`MalformedTypeExpression`, `MissingTypeExpression`, and skipped-tail recovery
shapes.

Task 12 result: the operator increment is implemented as S-011 progress.
`OperatorFixity` now carries prefix/infix/postfix parser-facing metadata,
frontend `ParserInputs` derives deterministic entries from active lexical
summaries, and the module term parser uses active-lexicon Pratt grouping for
prefix, postfix, and infix operators before the fixed lowest-precedence `qua`
chain. `PrefixExpression` and `PostfixExpression` extend the syntax vocabulary
alongside the existing `InfixExpression`; parser tests and active parse-only
pass/fail corpus cases cover precedence, associativity, selector/application
binding, parentheses, `qua`, non-associative chains, and dangling operators.
The local task-12 `source_drift` and `test_gap` are closed. No blocking
`spec_gap` was found.

Task 15 local classification: the missing `SetComprehension` and
`ComprehensionVariableSegment` syntax vocabulary is `source_drift` for the
S-011 term bucket because task-9 `SetEnumeration` intentionally deferred
Fraenkel/set-builder forms. Active parse-only comprehension pass/fail rows are
a `test_gap` derived from
[13.term_expression.md](../../../spec/en/13.term_expression.md). No blocking
`spec_gap` is known for the parse-only surface: sethood, binder identity,
capture, mapper result typing, and Fraenkel symbol elaboration are semantic
work outside `mizar-syntax`.

Task 15 result: the Fraenkel/set-builder increment is implemented as the final
S-011 parse-only term-node increment. `SetComprehension` and
`ComprehensionVariableSegment` are available through the parser and syntax
surface, set enumeration remains distinct, missing generator/type/condition
and closing-brace recovery use the documented recovery vocabulary, active
parse-only pass/fail corpus rows and parser unit tests close the local
`test_gap`, and rowan/snapshot/typed-accessor coverage closes the local
`source_drift`. The paired scope-skeleton guard prevents expression-level
`is set` type words in comprehension fixtures from being misclassified as
malformed `set name =` binder statements. No blocking `spec_gap` was found.

### Task S-012: Formula nodes

Purpose: represent predicate applications, built-in predicate applications,
generic `is_assertion`, connectives, and quantifiers.

Spec refs: [14.formulas.md](../../../spec/en/14.formulas.md),
[appendix B](../../../spec/en/appendix_b.operator_precedence.md).

Tests: activate built-in predicate, non-associative `iff`, generic `is`, and
missing formula recovery rows.

Affected design files: [ast.md](./ast.md), [recovery.md](./recovery.md).

Affected source files: `src/ast.rs`, `src/recovery.rs` as needed.

Completion condition: formula nodes keep `is_assertion` generic until resolver
classification and preserve fixed connective precedence for parser tasks 13-14.

Forbidden behavior: type/attribute classification, predicate overload
resolution, proof checking, or truth evaluation.

Task 13 local classification: missing formula vocabulary for atomic formulas is
`source_drift` until `FormulaExpression`, built-in predicate, generic
`IsAssertion`, attribute-test, and user predicate nodes land. The theorem/lemma
`label: formula;` placeholder host was deliberate `design_drift` while task 22
was pending; represented theorem/proof item nodes now own those formula hosts.
Attribute-only `is` bodies are a local
`source_drift` because task-8 `TypeExpression` cannot represent `non empty`
without a trailing type head. Template predicate arguments were deferred
`source_drift` until task 31 / S-016, which now represents them. Active built-in/user predicate and generic
`is` parse-only fixtures are a `test_gap` derived from
[14.formulas.md](../../../spec/en/14.formulas.md) and the
[parse-only acceptance matrix](./parse_only_acceptance_matrix.md). No blocking
`spec_gap` is known: the grammar audit already resolved the type-vs-attribute
assertion boundary by requiring generic `is_assertion`.

Task 13 result: the atomic-formula increment is implemented as S-012 progress.
`FormulaExpression`, `BuiltinPredicateApplication`, generic `IsAssertion`,
`AttributeTestChain`, `PredicateApplication`, `PredicateSegment`,
`PredicateHead`, and `InlinePredicateApplication` now cover the parser task-13
surface. The theorem/lemma `label: formula;` placeholder host is implemented
without changing other theorem/proof placeholder tails in task 13; task 22 later
replaced represented theorem/proof tails with concrete theorem/proof nodes.
Built-in predicates remain single atoms, mixed user/built-in predicate chains
are rejected, and template predicate arguments were deferred to task 31 /
S-016 before task 31 represented them. The local task-13 `source_drift` and `test_gap` are closed, and the
deliberate theorem placeholder `design_drift` is closed for represented
theorem/proof forms. Task 14 completed the remaining S-012 connective,
quantifier, parenthesized formula, `thesis`, and `contradiction` nodes.

Task 14 local classification: missing connective, quantifier, parenthesized
formula, formula constant, and missing-formula recovery vocabulary was
`source_drift` before parser task 14 landed. The absence of active `.miz`
coverage for fixed precedence, non-associative `iff`, repetition tokens,
quantifiers, `thesis` / `contradiction`, and `MissingFormula` recovery was
`test_gap` derived from [14.formulas.md](../../../spec/en/14.formulas.md) and
Appendix B. Repetition expansion and alpha-equivalence validation remain
semantic/checker-owned and are not parser/syntax gaps. Fraenkel and
set-builder term forms that embed formulas remain task-15 `source_drift`.
Template predicate arguments were deferred `source_drift` until task 31 /
S-016, which now represents them. No blocking `spec_gap` is known for task 14.

Task 14 result: S-012 is complete for parser-owned formula syntax.
`PrefixFormula`, `BinaryFormula`, `ParenthesizedFormula`,
`QuantifiedFormula`, `QuantifierVariableSegment`, and `FormulaConstant` now
cover the fixed connective, quantifier, grouping, and formula-constant surface.
`MissingFormula` and `MalformedFormulaExpression` are produced for task-14
formula recovery, and frontend diagnostic key mapping includes
`malformed_formula_expression`. Parser and syntax unit tests plus active
parse-only pass/fail cases cover precedence, non-associative `iff`, repetition
tokens, quantifiers, `thesis` / `contradiction`, and missing formula recovery.
The task-14 `source_drift` and `test_gap` are closed. Template predicate
arguments were deferred until task 31 / S-016 and are now represented;
formula-embedding Fraenkel/set-builder terms are implemented by task 15 /
S-011.

### Task S-013: Statement nodes

Purpose: represent proof statements, local declarations, type-changing
statements, conclusions, iterative equality, compact statements, block
statements, and local definitions.

Spec refs: [15.statements.md](../../../spec/en/15.statements.md).

Tests: `consider` / `reconsider`, compact equality, iterative equality, block
statements, local inline definitions, and `then` rejection rows are active.
The later proof/justification task owns theorem/proof nesting recovery rows.

Affected design files: [ast.md](./ast.md), [recovery.md](./recovery.md).

Affected source files: `src/ast.rs`, `src/recovery.rs` as needed.

Completion condition: statement node shapes cover parser tasks 16 and 18-21,
including a documented compact-equality versus iterative-equality boundary.

Forbidden behavior: proof obligation generation, witness leakage checks, type
well-formedness, or justification validation.

Progress: parser tasks 16, 18, 19, 20, and 21 have landed for S-013. Task 18 adds
`ConsiderStatement`, `ReconsiderStatement`, and `ReconsiderItem` syntax,
shared-type `consider` variable segments, condition lists, mandatory simple
justifications, reconsider item lists, target-type recovery, syntax typed
accessors, lexer scope-skeleton `type_change_list` support, and active
parse-only pass/fail corpus coverage. Task 19 adds `ConclusionStatement`,
`ThenStatement`, `IterativeEqualityStatement`, and `IterativeEqualityStep`,
including the G-AUD-010 compact-equality versus iterative-equality dispatch,
label and `then` variants, recovery fixtures, and active parse-only pass/fail
corpus coverage. Task 20 adds `NowStatement`, `HerebyStatement`,
`CaseReasoningStatement`, `CaseItem`, and `SupposeItem`, including nested
reasoning bodies, homogeneous `case`/`suppose` branch lists, optional explicit
`per cases by` justification, `then per cases`, end synchronization, recovery
fixtures, and active parse-only pass/fail corpus coverage. Task 21 adds
`InlineFunctorDefinition`, `InlinePredicateDefinition`, and `TypedParameter`,
including standalone-only `deffunc` / `defpred` dispatch, typed and
zero-argument parameter lists, `->` / `equals` / `means` delimiter recovery,
missing name/type/body/formula recovery, parser unit tests, syntax typed
accessors, and active parse-only pass/fail corpus coverage. S-013 is complete.

### Task S-014: Theorem, proof, and justification nodes

Purpose: represent theorem/lemma items, labels, proof blocks, justification
clauses, grouped/bulk citations, computation justification options, and the
minimal statement hosts needed to exercise those justifications before the
remaining S-013 statement tasks land.

Spec refs: [15.statements.md](../../../spec/en/15.statements.md),
[16.theorems_and_proofs.md](../../../spec/en/16.theorems_and_proofs.md),
[20.algorithm_and_verification.md](../../../spec/en/20.algorithm_and_verification.md).

Tests: activate `let ... by` and explicit compact-statement justification
rows, theorem/proof nesting, citation, grouped/bulk reference, and missing
proof-end rows.

Progress: complete. Parser task 17 landed the citation/computation subset:
`JustificationClause`, `ReferenceList`, local/qualified/grouped/bulk reference
nodes, `ComputationJustification`, `ComputationOption`, the minimal
explicit-justification `CompactStatement` host, and active parse-only
justification corpus coverage. Parser task 22 landed `TheoremItem`,
`LemmaItem`, `ProofBlock`, status-token preservation, visibility-wrapped
theorem targets, proof-body statement wiring, statement-level proof
justifications on conclusion and compact statement hosts, theorem/proof
recovery, typed accessors, and active parse-only theorem/proof corpus coverage.

Affected design files: [ast.md](./ast.md), [recovery.md](./recovery.md).

Affected source files: `src/ast.rs`, `src/recovery.rs` as needed.

Completion condition: proof and justification nodes preserve labels and
references syntactically with deterministic snapshots for parser tasks 17 and
22.

Forbidden behavior: citation resolution, theorem validity, ATP/prover
selection, or computation proof replay.

### Task S-015: Definition, structure, and registration nodes

Purpose: represent definition blocks, correctness clauses, attribute/predicate/
functor/mode definitions, redefinitions, notation aliases, property clauses,
structures, registrations, clusters, and reductions.

Spec refs: [05.structures.md](../../../spec/en/05.structures.md),
[06.attributes.md](../../../spec/en/06.attributes.md),
[07.modes.md](../../../spec/en/07.modes.md),
[09.predicates.md](../../../spec/en/09.predicates.md),
[10.functors.md](../../../spec/en/10.functors.md),
[11.symbol_management.md](../../../spec/en/11.symbol_management.md),
[16.theorems_and_proofs.md](../../../spec/en/16.theorems_and_proofs.md),
[17.clusters_and_registrations.md](../../../spec/en/17.clusters_and_registrations.md).

Tests: activate definition/template ambiguity, built-in predicate definition
rejections, registration rows, and property/correctness clause cases. Parser
task 23 has active pass/fail corpus coverage for definition block skeletons,
ordinary definition parameters, attribute definitions, correctness conditions,
template-ambiguous definition content placeholders, and malformed definition
block recovery. Parser task 24 has active pass/fail corpus coverage for
predicate definitions, definition-local predicate visibility, malformed
predicate patterns, and built-in predicate-token rejection. Parser tasks 25-30
add active pass/fail corpus coverage for functor definitions, mode
definitions, redefinitions, notation aliases, property clauses, structures, and
registrations, including visibility-wrapped definition content, recovery cases,
and traceability metadata.

Affected design files: [ast.md](./ast.md), [recovery.md](./recovery.md),
[trivia.md](./trivia.md) for item attachment.

Affected source files: `src/ast.rs`, `src/recovery.rs`, `src/trivia.rs` as
needed.

Completion condition: definition-family nodes preserve syntactic category and
ambiguity boundaries required by the grammar audit for parser tasks 23-30.

Forbidden behavior: definitional correctness checking, cluster closure,
reduction validation, notation resolution, or type/proof semantics.

Task 23 local classification: `source_drift` because definition blocks were
still placeholder-only in source, `design_drift` because parser/syntax grammar
docs stopped at task 22, `test_gap` because no active definition/attribute/
correctness corpus covered the new surface, and existing `spec_gap`
MSYN-GAP-005 / G-AUD-006 for definition/template dispatch ambiguity remains
open. The increment resolves the source/design/test drift for the documented
task-23 subset while preserving template-ambiguous and later definition-family
forms as `PlaceholderItem`.

Task 23 result: the first S-015 increment is implemented. `DefinitionBlockItem`
now owns `definition ... end;`, source-ordered concrete content, placeholders,
and `MissingEnd` recovery. `DefinitionParameter`, `AttributeDefinition`,
`AttributePattern`, `FormulaDefiniens`, `FormulaCase`, and
`CorrectnessCondition` cover ordinary parameters, `attr ... means ...;`
definitions with single-formula bodies or formula cases with optional
`otherwise`, assumptions, theorem/lemma content, and empty/reference/
computation/proof correctness-condition justifications. Active parse-only
pass/fail fixtures and traceability metadata cover attribute definitions and
correctness conditions. At task 23 completion, S-015 remained open for parser
tasks 24-30.

Task 24 local classification: predicate definitions were `source_drift` because
`pred ... means ...;` still parsed as placeholder content, active predicate
definition pass/fail fixtures were a `test_gap` derived from
[09.predicates.md](../../../spec/en/09.predicates.md), and parser/syntax design
docs were `design_drift` after task 23. No blocking `spec_gap` was found for
ordinary predicate definitions. The identifier-only `pred_pattern` split stays
resolver-owned by preserving a raw `PredicatePattern` and validating only that
the token span can match the grammar under at least one syntactic split.
Template-definition classification remains the existing G-AUD-006 `spec_gap`
for S-016 and is not closed by task 24.

Task 24 result: predicate definitions are implemented as S-015 progress.
`PredicateDefinition` and `PredicatePattern` are available through syntax typed
accessors and parser output, `public pred` / `private pred` inside definition
blocks use the existing `VisibleItem` wrapper, formula bodies reuse
`FormulaDefiniens`, malformed predicate labels/patterns/bodies recover with
the documented recovery vocabulary, and active parse-only pass/fail fixtures
plus traceability metadata cover Chapter 9 predicate definitions. S-015 remains
open for parser tasks 25-30.

Task 25 local classification: functor definitions are `source_drift` because
`func ... -> ... means|equals ...;` still parses as placeholder content,
active functor-definition pass/fail fixtures are a `test_gap` derived from
[10.functors.md](../../../spec/en/10.functors.md), and parser/syntax design
docs are `design_drift` after task 24. Ordinary functor definitions have no
blocking `spec_gap`. Chapter 10's examples and lexical rules document
circumfix functor surfaces such as `[: X, Y :]` and `|. x .|`, while the
complete `func_pattern` EBNF is still single-symbol; task 25 treats that as a
nonblocking `spec_gap` by preserving a raw `FunctorPattern` and accepting both
canonical single-symbol patterns and the documented two-symbol circumfix
surface without recording semantic roles. Template-definition classification
remains the existing G-AUD-006 `spec_gap` for S-016 and is not closed by task
25.

Task 25 result: functor definitions are implemented as S-015 progress.
`FunctorDefinition`, raw `FunctorPattern`, `TermDefiniens`, and `TermCase` are
available through syntax typed accessors and parser output. `public func` /
`private func` inside definition blocks reuse the existing `VisibleItem`
wrapper. `means` bodies reuse `FormulaDefiniens`; `equals` bodies preserve term
case lists and optional `otherwise` terms. Malformed functor labels, patterns,
return types, body keywords, term bodies, and term-case conditions recover with
the documented recovery vocabulary, including `MissingTypeExpression`,
`MissingTerm`, and `MissingFormula` at the documented insertion points. Active
parse-only pass/fail fixtures plus traceability metadata cover Chapter 10
functor definitions, including source-level circumfix patterns and the
canonical `let F be func(T) -> T;` template-functor placeholder preservation.
S-015 remains open for parser tasks 26-30.

Task 26 local classification: canonical mode definitions are `source_drift`
because `mode ... is ...;` still parses as placeholder content, active
mode-definition pass/fail fixtures are a `test_gap` derived from
[07.modes.md](../../../spec/en/07.modes.md), and parser/syntax design docs are
`design_drift` after task 25. Ordinary `mode` definitions using the canonical
`is` body have no blocking `spec_gap`. Chapter 7 assigns existence checks,
sethood proof obligations, and dependent-mode parameter validity to semantic
or proof phases; task 26 therefore preserves a raw `ModePattern`, a syntactic
`TypeExpression` body, and a `ModeProperty` justification without creating
semantic mode declarations. The legacy `means` mode-definition form is
explicitly outside the accepted grammar and is covered only by recovery tests.
Standalone property content and template-definition classification remain
open for later S-015/S-016 tasks.

Task 26 result: mode definitions are implemented as S-015 progress.
`ModeDefinition`, raw `ModePattern`, and `ModeProperty` are available through
syntax typed accessors and parser output. `public mode` / `private mode` inside
definition blocks reuse the existing `VisibleItem` wrapper. Mode bodies reuse
task-8 `TypeExpression` for syntactic attribute-chain plus type-head
preservation, while `ModePattern` preserves `of`, `over`, and bracketed type
parameters in source order. Immediately following `sethood` clauses are owned
as `ModeProperty` nodes with citation, computation, or proof justifications;
malformed patterns, missing bodies, missing semicolons, missing property
justifications, and rejected legacy `means` bodies recover with the documented
recovery vocabulary. Active parse-only pass/fail fixtures plus traceability
metadata cover Chapter 7 mode definitions, including imported symbolic mode
names and source-level `sethood` coverage. S-015 remains open for parser tasks
27-30.

Task 27 local classification: redefinition and notation-alias forms are
`source_drift` because `redefine attr`, `redefine pred`, `redefine func`,
`synonym`, and `antonym` still parse as placeholder content, active pass/fail
fixtures for those forms are a `test_gap` derived from Chapters 6, 9, 10, and
11, and parser/syntax design docs are `design_drift` after task 26. The
canonical grammar defines `redefine_attr`, `redefine_pred`, and
`redefine_func`, but it does not define `redefine_mode`; task 27 treats that as
a nonblocking `spec_gap` boundary by not inventing a mode-redefinition node.
Mode-like syntax is still preserved in `synonym` / `antonym` raw notation
patterns. Alias branch classification for predicate, functor, mode, and
attribute patterns remains resolver-owned because it depends on the active
symbol environment.

Task 27 result: redefinitions and notation aliases are implemented as S-015
progress. `AttributeRedefinition`, `PredicateRedefinition`,
`FunctorRedefinition`, nested `CoherenceCondition`, `NotationAlias`, and raw
`NotationPattern` are available through syntax typed accessors and parser
output. Definition-local `public` / `private` redefinitions and aliases reuse
the existing `VisibleItem` wrapper. The parser requires each concrete
redefinition body to be followed by a coherence citation or proof while keeping
coherence as part of the redefinition subtree. `synonym` and `antonym` preserve
their alternate and original sides as raw notation patterns; semantic branch
classification and notation resolution remain resolver-owned. `redefine mode`
stays a placeholder/recovery boundary because the canonical grammar defines no
mode redefinition. Active parse-only pass/fail fixtures plus traceability
metadata cover the task-27 surface. At task 27 completion, S-015 remained open
for parser tasks 28-30.

Task 28 local classification: property clauses were `source_drift` because the
canonical predicate and functor property clauses, plus standalone `sethood`
property items, still parsed as placeholder content; active pass/fail fixtures
for those clauses were a `test_gap` derived from Chapters 7, 9, 10, and
Appendix A; and parser/syntax design docs were `design_drift` after task 27.
The parser TODO's `transitivity` entry is a local `design_drift` against the
canonical property productions, which list no `transitivity` property clause.
The Chapter 7 / Appendix A `property_impl` surface remains a nonblocking
`spec_gap` because the block-level production and examples/definition-content
placement are not yet consistent enough to implement without inventing
behavior. Task 28 therefore implements only justification-bearing
`property_item` clauses and leaves the `property` keyword placeholder for a
future clarified task. Predicate/functor arity checks, proof obligations, and
association between a property and the definition it annotates remain
semantic/proof-phase responsibilities.

Task 28 result: property clauses are implemented as S-015 progress.
`PropertyClause` is available through syntax typed accessors and parser output
for canonical predicate properties (`symmetry`, `asymmetry`, `connectedness`,
`reflexivity`, `irreflexivity`), functor properties (`commutativity`,
`idempotence`, `involutiveness`, `projectivity`), and standalone `sethood`.
Immediately following mode `sethood` clauses still use task-26 `ModeProperty`.
Malformed or missing property justifications and missing semicolons recover
with the documented recovery vocabulary while preserving following definition
items. Active parse-only pass/fail fixtures plus traceability metadata cover
the task-28 surface. S-015 remains open for parser tasks 29-30.

Task 29 local classification: structures and inheritance were `source_drift`
because `struct ... where ... end;` and `inherit ... extends ...` still parsed
as placeholder content, active pass/fail fixtures for Chapter 5 structure
forms were a `test_gap`, and parser/syntax design docs were `design_drift`
after task 28. No blocking `spec_gap` was found for the parser-visible
structure definition and inheritance surface. The task records semantic
boundaries explicitly: structure identity, selector declarations as semantic
facts, inherited-member coverage, diamond consistency, type-specialization
validity, constructor behavior, and coherence proof obligations remain outside
the parse/syntax crates. The ambiguous Chapter 7 / Appendix A `property_impl`
surface remains the task-28 nonblocking `spec_gap` and is not closed by
structure `property` member parsing.

Task 29 result: structure definitions and inheritance definitions are
implemented as S-015 progress. `StructureDefinition`, raw `StructurePattern`,
`StructureField`, `StructureProperty`, `InheritanceDefinition`,
`InheritanceTarget`, `FieldRedefinition`, and `PropertyRedefinition` are
available through syntax typed accessors and parser output. Definition-local
`public struct` / `private struct` and `public inherit` / `private inherit`
reuse `VisibleItem`. Structure patterns preserve `of`, `over`, and bracketed
parameters in source order; fields preserve optional `:=` initializers;
inheritance preserves shorthand `extends`, explicit `where ... end` mappings,
`extends set`, optional narrowed member types, and optional coherence
justifications. Malformed patterns, members, targets, explicit empty `where`
blocks, missing types, missing semicolons, and malformed coherence clauses
recover with the documented recovery vocabulary. The frontend scope skeleton
recognizes nested `struct` and explicit `inherit ... where` blocks so active
parse-only fixtures do not emit spurious unmatched-end diagnostics. Active
parse-only pass/fail fixtures plus traceability metadata cover the task-29
surface. At task 29 completion, S-015 remained open for parser task 30.

Task 30 local classification: registrations and clusters were `source_drift`
because `registration ... end`, `cluster`, and `reduce` surfaces still parsed
as placeholder or unexpected content; active registration pass/fail fixtures
were a `test_gap` derived from
[17.clusters_and_registrations.md](../../../spec/en/17.clusters_and_registrations.md);
and parser/syntax design docs were `design_drift` after task 29. No blocking
`spec_gap` was found for the parser-visible Chapter 17 surface. A
nonblocking Chapter 17 / Appendix A placement tension remains because
Appendix A and Chapter 12 admit definition-local registration items while some
Chapter 17 prose emphasizes top-level registration blocks; task 30 implements
both syntax positions without assigning semantics. Nullary functorial
registration ambiguity remains deferred because syntax alone cannot distinguish
a bare active attribute/functor spelling from a single-adjective conditional
cluster.

Task 30 result: registration blocks and registration items are implemented,
completing S-015. `RegistrationBlockItem`, `RegistrationParameter`,
`ExistentialRegistration`, `ConditionalRegistration`,
`FunctorialRegistration`, and `ReductionRegistration` are available through
syntax typed accessors and parser output. Definition-local `public` /
`private` cluster and reduction registrations reuse `VisibleItem`.
Registration adjectives preserve optional `non`, optional parameter prefixes,
and argument-free attribute names; parameterized registered and target types
reuse ordinary `TypeExpression` children; functorial registration payloads are
restricted to syntactically unambiguous application/operator/bracket-functor
term surfaces; reductions preserve left and right `TermExpression` children.
Malformed parameters, missing labels, missing antecedents or consequents,
unsupported functorial payloads, argument-bearing registration adjectives,
missing correctness justifications, missing semicolons, and missing
registration block ends recover with the documented recovery vocabulary. The
frontend scope skeleton recognizes `registration ... end` and avoids treating
registration target-type `for set` / `for T` forms as binders. Active
parse-only pass/fail fixtures plus traceability metadata cover the task-30
surface. Cluster
closure, existence/coherence/reducibility proof obligations, reduced normal
forms, and nullary functorial disambiguation remain outside `mizar-syntax`.

### Task S-016: Template, algorithm, and annotation nodes

Purpose: represent template parameters, template arguments, algorithm blocks,
control flow, verification clauses, top-level claims, and annotations.

Spec refs: [18.templates.md](../../../spec/en/18.templates.md),
[20.algorithm_and_verification.md](../../../spec/en/20.algorithm_and_verification.md),
[21.source_code_annotation_and_atp.md](../../../spec/en/21.source_code_annotation_and_atp.md).

Tests: parser task 31 has promoted the template seed cases and added AST/parser
coverage for `TemplateParameter`, `TemplateLoci`, `TemplateArguments`, radix-only
`qua` recovery, reference/template-functor arguments, template-local identifier
predicate heads, active reference-citation template-argument corpus coverage,
and `nest` traceability. Parser task 32 adds AST/parser coverage for
`AlgorithmDefinition`, `AlgorithmParameters`, `AlgorithmBody`,
`AlgorithmStatementList`, `VariableDeclaration`, `VariableBinding`,
`AssignmentStatement`, `Lvalue`, `SnapshotStatement`, `ReturnStatement`, and
`ClaimBlockItem`, plus active algorithm/claim parse-only pass/fail corpus
coverage and frontend scope-skeleton coverage for algorithm bodies and ghost
assignments. Dotted `Lvalue` preservation is pinned by parser-unit coverage;
active `.miz` coverage for that dot surface is tracked by MSYN-GAP-013.
Parser task 33 adds AST/parser coverage for `IfStatement`, `WhileStatement`,
`ForRangeStatement`, `ForCollectionStatement`, `MatchStatement`, `MatchCase`,
`MatchEnding`, `BreakStatement`, and `ContinueStatement`, plus active
algorithm-control parse-only pass/fail corpus coverage and frontend
scope-skeleton coverage for match `otherwise` blocks. Parser task 34 adds
AST/parser coverage for `AlgorithmTerminationClause`,
`AlgorithmRequiresClause`, `AlgorithmEnsuresClause`,
`AlgorithmDecreasingClause`, `LoopInvariantClause`, `LoopDecreasingClause`,
`AssertStatement`, and `TermList`, plus active algorithm-verification
parse-only pass/fail corpus coverage. Parser task 35 adds AST/parser coverage
for `Annotation`, `LibraryAnnotation`, `AnnotationLabelList`,
`AnnotationLabel`, `AnnotationArgumentList`, `AnnotationArgument`,
`ProofHintOptionList`, `ProofHintOption`, `StandaloneDiagnosticAnnotation`,
`AnnotatedStatement`, `AnnotatedAlgorithmStatement`,
`AnnotatedDefinitionContent`, and `AnnotatedRegistrationContent`, plus active
annotation pass/fail corpus coverage. Parser-unit registration coverage also
pins annotation wrappers around concrete registration items.

Task 31 closes the earlier task-local deferred notes for predicate template
arguments, reference template arguments, and template-ambiguous definition
content. Task 32 closes the basic algorithm/claim `source_drift` and active
parse-only `test_gap` for declarations, assignments, snapshots, returns, and
top-level claims. Task 33 closes the control-flow `source_drift` and active
parse-only `test_gap`. Task 34 closes the algorithm-verification
`source_drift`, `test_gap`, and task33 expectation drift for loop verification
clauses. Task 35 closes the annotation `source_drift` and active parse-only
`test_gap` for attachable annotations, standalone diagnostic annotations, and
malformed annotation recovery.

Affected design files: [ast.md](./ast.md), [recovery.md](./recovery.md),
[trivia.md](./trivia.md).

Affected source files: `src/ast.rs`, `src/recovery.rs`, `src/trivia.rs` as
needed.

Completion condition: template, algorithm, claim, verification, and annotation
nodes are represented syntactically for parser tasks 31-35, helper aliases from
the grammar audit are removed/repurposed/preserved deliberately, and
parser-owned recovery/trivia rules are documented.

Forbidden behavior: template instantiation checking, VC generation, algorithm
termination checking, annotation registry validation, or ATP semantics.

### Task S-017: Public enum forward-compatibility policy

Purpose: revisit all public enums added by node-vocabulary increments and
record `#[non_exhaustive]` versus deliberate exhaustiveness decisions.

Spec refs: [ast.md](./ast.md), [trivia.md](./trivia.md), [recovery.md](./recovery.md).

Tests: extend `crates/mizar-syntax/tests/lint_policy.rs` coverage for new
public enums.

Result: complete. The final audit found no new public enum type added after the
initial gate, and `crates/mizar-syntax/tests/lint_policy.rs` already classifies
every current public enum as either `#[non_exhaustive]` or a documented
exhaustive exception. The owning module specs now describe these as current API
decisions and require future public enums to enter exactly one lint-policy
classification before landing.

Affected design files: owning module specs.

Affected source files: `src/ast.rs`, `src/trivia.rs`, `src/recovery.rs`,
`tests/lint_policy.rs`.

Completion condition: lint policy and docs agree for every public enum.

Forbidden behavior: making downstream exhaustive matching promises without a
documented compatibility decision.

### Task S-018: Incremental syntax reuse audit

Purpose: audit rowan kind numbering, node identity rules, range attachment,
trivia/recovery placement, typed accessors, and localized-edit reuse readiness.

Spec refs: [ast.md](./ast.md), [trivia.md](./trivia.md), [recovery.md](./recovery.md).

Tests: add determinism or reuse-focused tests only if the audit finds a concrete
gap.

Result: complete. The audit found no need for a query engine or build-system
state in `mizar-syntax`: `SurfaceAst` remains an immutable rowan-backed output
that later layers can cache behind frontend-owned content keys. The module
specs now document append-only raw `SyntaxKind` numbering for this phase,
`SurfaceNodeId` as deterministic-but-not-persistent identity, range-attached
trivia and recovery reuse, and localized-edit validation by range/snapshot or
green-node equality. A concrete source/test gap was closed by adding typed
`SurfaceNodeView` helpers for task-35 annotation nodes and rowan/raw-kind test
coverage for annotation nodes and `TokenAnnotationMarker`.

Affected design files: [ast.md](./ast.md), [trivia.md](./trivia.md),
[recovery.md](./recovery.md).

Affected source files: syntax source files as needed by findings.

Completion condition: documented identity/reuse rules match implementation and
future query layers can cache syntax outputs without exposing unstable ids.

Forbidden behavior: introducing `salsa` or build-system state into
`mizar-syntax`.

### Task S-019: Source/spec correspondence audit

Purpose: trace every public API and promised behavior in syntax design docs to
source and tests.

Spec refs: all `mizar-syntax` module specs and this crate plan.

Tests: add targeted tests for any uncovered promised behavior.

Affected design files: all `mizar-syntax` design docs.

Affected source files: all `mizar-syntax` source files as needed.

Completion condition: no undocumented public behavior or unimplemented promised
behavior remains inside the target scope.

Forbidden behavior: changing source, expectations, or docs merely to hide
drift without classifying it.

### Task S-020: Bilingual documentation sync audit

Purpose: synchronize English canonical docs and Japanese companions for all
syntax design docs.

Spec refs: repository documentation policy.

Tests: documentation review; no runtime tests expected unless docs expose a
source/test mismatch.

Affected design files: `doc/design/mizar-syntax/en/*` and
`doc/design/mizar-syntax/ja/*`.

Affected source files: none unless the audit finds source/documentation drift.

Completion condition: English and Japanese docs agree on statuses, APIs,
terminology, links, and behavior promises.

Forbidden behavior: changing canonical behavior only in Japanese companion docs.

### Task S-021: Rustdoc summaries

Purpose: add Rustdoc summaries once a long-lived consumer or workspace rustdoc
policy requires them.

Spec refs: repository documentation policy.

Tests: rustdoc/lint checks only after a workspace policy exists.

Affected design files: [todo.md](./todo.md) if the deferral status changes.

Affected source files: syntax source files.

Completion condition: deferred until the trigger in [todo.md](./todo.md) is
met.

Forbidden behavior: treating Rustdoc as the canonical syntax specification.

## Exit Criteria

Hard gates:

- no blocking/high specification inconsistency remains for milestone-owned
  syntax behavior;
- no language-facing source behavior remains that is absent from
  `doc/spec/en/`, `.miz` tests, or traceability metadata;
- no implementation-facing API behavior remains that is absent from
  `doc/design/mizar-syntax`, Rust tests, or explicit deferred notes;
- milestone-owned specification items have existing or test-first `.miz`
  coverage, or an explicit deferred reason;
- existing `.miz` tests and `.expect.toml` files are not rebaselined merely to
  match current implementation behavior;
- `doc/design/mizar-syntax` and `crates/mizar-syntax` agree within the target
  scope;
- parser, lexer, resolver, type, proof, and VC responsibility boundaries are
  respected;
- required verification commands pass, or every unrun command is explicitly
  justified;
- remaining risks are classified as deferred, out of scope, or human-owned;
- a read-only review assigns a valid crate quality score of at least 90/100
  after hard gates pass.

Verification commands:

```text
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo run -p mizar-test -- plan --tests-root tests --manifest tests/coverage/spec_trace.toml
```

Narrower commands may run first for individual increments:

```text
cargo test -p mizar-syntax
cargo clippy -p mizar-syntax --all-targets --all-features -- -D warnings
cargo test -p mizar-parser
cargo test -p mizar-frontend
```

Review expectations:

- Review the implementation specification before each increment.
- Review test sufficiency against this crate plan and the owning spec refs.
- Review the full implementation for bugs, boundary violations, missing edge
  cases, and drift.
- Review source/documentation consistency before committing.
- Produce a Crate Exit Report when crate-wide autonomous work is complete.
