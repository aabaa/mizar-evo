# Crate Plan: mizar-test

> Canonical language: English. Japanese companion: [../ja/00.crate_plan.md](../ja/00.crate_plan.md).

## Responsibility

Owned behavior:

- deterministic discovery of corpus payloads and `.expect.toml` sidecars;
- parsing and validation of expectation metadata and traceability metadata;
- staged-model admission and coverage accounting;
- active runner gates for stage-owned compiler seams;
- snapshot comparison/update policy and deterministic report surfaces;
- fail/soundness bookkeeping for tests that must not silently pass, disappear,
  or weaken.

Out of scope:

- compiler language semantics;
- name resolution, type checking, proof search, ATP, certificate replay, or
  kernel proof authority outside explicit consumer-runner seams;
- changing `doc/spec`, existing `.miz` tests, or existing expectation files to
  match current implementation behavior.

## Specification Items

| Spec ref | Requirement | Status |
|---|---|---|
| [layout.md](./layout.md) | Known roots, payload/sidecar pairing, deterministic order, naming and sidecar rules. | partial |
| [expectation_schema.md](./expectation_schema.md) | Sidecar schema, outcome compatibility, fail identity, active runner metadata, transitional parse-only snapshots. | partial |
| [staged_model.md](./staged_model.md) | Canonical stage ids and prerequisite-gated coverage credit. | partial |
| [traceability.md](./traceability.md) | Manifest schema, sidecar backrefs, validation modes, coverage status, reporting. | partial |
| [harness.md](./harness.md) | Metadata plan, active runner modes, deterministic diagnostics/reports. | partial |
| [miz_corpus.md](./miz_corpus.md) | Corpus classes, generated/fuzz/stress metadata, review rules, pass/fail mix. | partial |
| [snapshot.md](./snapshot.md) | Snapshot kinds, public API, canonicalization, update policy, determinism checks. | partial |
| [fail_soundness.md](./fail_soundness.md) | Required soundness cases, expected-failure identity, weakening/deletion rules. | partial |
| [minimal_crate.md](./minimal_crate.md) | Metadata-only plan scope, CLI, validation, determinism, exit codes. | partial |

## Relevant Tests

| Test path | Intent | Spec refs |
|---|---|---|
| `crates/mizar-test/tests/lint_policy.rs` | Workspace lint opt-in and inline `allow` rationale guard. | repository conventions |
| `crates/mizar-test/tests/metadata.rs` | Synthetic metadata/traceability fixtures, active runner gates, repository plan checks, transitional parse-only snapshots. | mizar-test module specs |
| `crates/mizar-test/src/runner.rs` unit tests | Parse-only fixture import-summary behavior. | harness.md |
| `tests/coverage/spec_trace.toml` | Requirement manifest consumed by the metadata plan. | traceability.md |
| `tests/**/*.expect.toml` | Sidecar metadata consumed by discovery, schema validation, traceability, and active runner gates. | expectation_schema.md |

Planned test-first additions:

- task 3 CLI fixture tests for validation modes and exit codes;
- task 4/5 snapshot canonicalization, update, and determinism fixtures;
- task 6 traceability coverage/status/report fixtures;
- task 7 stage prerequisite violation fixtures;
- task 8 fail/soundness contract fixtures (implemented);
- task 9 corpus policy fixtures;
- task 16 source-derived builtin type-expression pass fixture and runner
  regression tests;
- task 17 source-derived builtin `ResolvedTypedAst` handoff regression tests;
- task 18 source-derived reserve declaration semantic bridge regression tests.
- task 19 reserve bridge `ResolvedTypedAstSummary::from_ast` readiness
  regression tests and builtin declaration inventory.
- checker task 56 source-derived one-edge local-mode expansion chain pass/gap
  fixtures and runner regression tests.
- checker task 57 source-derived local-mode structure-RHS evidence-gap fixture
  and runner regression tests.
- checker task 58 source-derived local-mode attributed-builtin-RHS evidence-gap
  fixture and runner regression tests.
- checker task 59 source-derived attributed local-mode reserve evidence-gap
  fixture and runner regression tests.
- checker task 60 source-derived attributed local-mode structure-RHS
  evidence-gap fixture and runner regression tests.
- checker task 61 source-derived attributed local-mode attributed-builtin-RHS
  evidence-gap fixture and runner regression tests.
- checker task 62 source-derived local-mode structure-RHS chain evidence-gap
  fixture and runner regression tests.
- checker task 63 source-derived local-mode attributed-builtin-RHS chain
  evidence-gap fixture and runner regression tests.
- checker task 64 source-derived attributed local-mode bare-builtin chain
  evidence-gap fixture and runner regression tests.
- checker task 65 source-derived attributed local-mode structure-RHS chain
  evidence-gap fixture and runner regression tests.
- checker task 66 source-derived attributed local-mode attributed-builtin-RHS
  chain evidence-gap fixture and runner regression tests.
- checker task 67 source-derived structure-qualified attribute extraction-gap
  boundary fixture and runner regression tests.
- checker task 68 source-derived argument-bearing mode reserve extraction-gap
  boundary fixture and runner regression tests.
- checker task 69 source-derived argument-bearing structure reserve
  extraction-gap boundary fixture and runner regression tests.
- checker task 70 source-derived bracket-form local mode reserve extraction-gap
  boundary fixture and runner regression tests.
- checker task 71 source-derived bracket-form local structure reserve
  extraction-gap boundary fixture and runner regression tests.
- checker task 72 source-derived two-edge bare local-mode chain pass fixtures, checker task 73 source-derived three-edge bare local-mode chain pass fixtures, checker task 74 structural bare local-mode chain pass fixtures, and runner regression tests.
- checker task 75 source-derived local-mode forward-reference active-range
  boundary fixture and runner regression tests.
- checker task 76 source-derived local-structure forward-reference active-range
  boundary fixture and runner regression tests.
- checker task 77 source-derived local-attribute forward-reference active-range
  boundary fixture and runner regression tests.
- checker task 78 source-derived imported structure reserve extraction-gap
  boundary fixture and runner regression tests.
- checker task 79 source-derived imported mode reserve extraction-gap
  boundary fixture and runner regression tests.
- checker task 80 source-derived imported attribute reserve extraction-gap
  boundary fixture and runner regression tests.
- checker task 82 source-derived imported mode reserve provenance bridge
  fixture and runner regression tests.
- checker task 83 source-derived imported structure reserve provenance bridge
  fixture and runner regression tests.
- checker task 84 source-derived imported attribute reserve provenance bridge
  fixture and runner regression tests.

## Design And Source Inventory

Design files:

- `doc/design/mizar-test/en/*.md`
- `doc/design/mizar-test/ja/*.md`

Source files:

- `crates/mizar-test/src/layout.rs`
- `crates/mizar-test/src/path_rules.rs`
- `crates/mizar-test/src/expectation.rs`
- `crates/mizar-test/src/staged_model.rs`
- `crates/mizar-test/src/traceability.rs`
- `crates/mizar-test/src/harness.rs`
- `crates/mizar-test/src/runner.rs`
- `crates/mizar-test/src/snapshot.rs`
- `crates/mizar-test/src/main.rs`
- `crates/mizar-test/src/diagnostic.rs`
- `crates/mizar-test/src/toml_lite.rs`

Observed behavior:

- metadata `plan` discovery, sidecar parsing, trace manifest parsing, duplicate
  id checks, missing source/sidecar checks, and deterministic plan ordering are
  implemented and tested;
- active `parse-only`, `declaration-symbol`, and `type-elaboration` runners
  have explicit active-tag gates and repository coverage;
- the active `type-elaboration` runner reaches checker-owned `BindingEnv`,
  reserve `DeclarationInput`, binding-specific builtin `set`/`object`
  `TypeExpressionInput`, and `DeclarationChecker` output through the
  checker-owned syntax-free task-48 seam, then continues to `TypedAst` and
  `ResolvedTypedAst` assembly for the supported source-derived reserve
  declaration slice, reads that real payload through `mizar-core`'s
  `ResolvedTypedAstSummary::from_ast`, prepares a binder-only `CoreContext`
  from the same real reserve bindings, extracts same-module attributed builtin
  reserve payloads far enough to report the checker `MissingEvidenceQuery`
  diagnostic, extracts same-module local mode reserve heads far enough to
  report the checker missing mode-expansion diagnostic, extracts same-module
  local structure reserve heads far enough to report the checker
  `MissingEvidenceQuery` diagnostic, extracts attributed same-module local
  structure reserve heads far enough to report the checker
  `MissingEvidenceQuery` diagnostic, extracts attributed same-module local mode
  reserve heads without supported real expansions or with mixed bare reserve
  uses far enough to report the checker missing mode-expansion diagnostic,
  extracts bare same-module no-argument local mode expansions from
  preceding bare-builtin RHS mode definitions far enough to add an active pass
  reserve fixture, extracts one-edge same-module local-mode chains when the
  dependency mode has an accepted bare-builtin RHS expansion, extracts real
  same-module local-mode expansions whose RHS is a same-module local structure
  head far enough to report the checker `MissingEvidenceQuery` diagnostic for
  missing base-shape/constructor-witness evidence, extracts real same-module
  local-mode expansions whose RHS is an attributed builtin head far enough to
  report the checker `MissingEvidenceQuery` diagnostic for missing
  attributed-type existential evidence, extracts attributed same-module
  local-mode reserve heads with real direct bare-builtin expansions far enough
  to report the same checker `MissingEvidenceQuery` diagnostic for missing
  attributed-type existential evidence, extracts attributed same-module
  local-mode reserve heads with real direct local-structure RHS expansions far
  enough to report the same checker `MissingEvidenceQuery` diagnostic for
  missing base-shape/constructor-witness and full attributed-type evidence,
  extracts attributed same-module local-mode reserve heads with real direct
  attributed-builtin RHS expansions far enough to report the same checker
  `MissingEvidenceQuery` diagnostic for missing full attributed-type evidence,
  extracts one-edge same-module local-mode chains ending in local-structure,
  attributed-builtin, bare-builtin attributed-root, structure-RHS
  attributed-root, and attributed-builtin-RHS attributed-root terminal payloads
  far enough to report the corresponding missing evidence-query diagnostics,
  reports forward same-module local-mode reserve heads that name later
  declarations as lower-stage active-range failures before checker handoff,
  and keeps mixed imports, imported attributes/structures, imported mode
  expansions beyond task 82's provenance bridge, and unsupported
  semantic/CoreIr/ControlFlowIr/VC/proof payload families on the external-gap
  key;
- architecture-22 matrix sidecar metadata is validated and reported as
  planned-only scenario coverage until owning consumers provide active
  clean/incremental/parallel/cache-race execution;
- transitional active parse-only `SurfaceAst` snapshot text baselines are
  validated and compared;
- general snapshot records, update mode, determinism mode, coverage reporting,
  pass/fail mix reporting, manifest ordering, and obsolete-reference checks
  are implemented;
- stage prerequisite validation is implemented;
- fail/soundness metadata bookkeeping validates certificate/kernel
  `rejection_reason`, recognized required soundness case shape/profile/phase,
  and mode-aware missing-case diagnostics; active proof/certificate/kernel
  execution remains paced by future consumer runners.

## Known Gaps And Drift

| ID | Class | Evidence | Action |
|---|---|---|---|
| MT-AUDIT-001 | design_drift | `layout.md` public API names `TestCase`/`TestMetadata`, while source exposes `DiscoveredLayout` and harness-owned `TestCase`. | Sync design API wording under task 3 or the next layout-focused doc change. |
| MT-AUDIT-002 | resolved | Task 9 parses and retains generated/fuzz/property `[origin]` metadata, including reproducibility fields and schema fixtures. | Keep origin schema docs synchronized as new provenance fields are added. |
| MT-AUDIT-003 | resolved / watch | Task 3 retains `profiles`, `notes`, `ast_profile`, and `snapshot_profiles`, and filters returned plan cases by `TestProfile`; task 9 adds generated/fuzz/stress corpus profile gates. | Keep profile policy synchronized when new profiles land. |
| MT-AUDIT-004 | resolved / runner watch | Task 8 requires certificate/kernel `rejection_reason`, preserves `stable_detail_key` fallback identity for runners without public diagnostic codes, and adds case-level fail/soundness metadata gates. | Keep active runner matching synchronized when proof/certificate/kernel runners land. |
| MT-AUDIT-005 | partial source_drift | Tasks 4 and 5 implement the general in-memory snapshot record API, canonicalization, hashing, validation, comparison, explicit baseline verify/update IO, and repeat-render determinism helper; general `[[snapshots]]` sidecar parsing and runner integration remain pending. | Implement remaining snapshot registry/runner integration in the expectation/harness follow-up tasks. |
| MT-AUDIT-006 | resolved / design_drift watch | Task 7 implements declared prerequisite gates over stage ids, `depends_on`, and `built_in` requirements. Broader corpus admission policy remains task 9/follow-up work. | Keep staged-model docs synchronized as new stages become executable. |
| MT-AUDIT-007 | resolved / watch | Task 6 implements mode-aware coverage completeness, computed/stored status comparison, and obsolete-reference checks. Task 7 implements `depends_on` and prerequisite credit gates. | Keep coverage/prerequisite gates synchronized as new evidence kinds land. |
| MT-AUDIT-008 | resolved | Trace manifest deterministic ordering is documented; the metadata ordering conflict was repaired by `897d549`, and task 6 adds validator coverage. | No further action unless manifest ordering rules change. |
| MT-AUDIT-009 | resolved / design_drift watch | Task 3 updates `harness.md` to show the exported metadata plan and runner-specific reports, with generic outcome/snapshot reporting marked as future API. | Keep docs synchronized as later generic reporting surfaces land. |
| MT-AUDIT-010 | resolved | Task 3 documents the narrow `parser.type_fixtures` symbol-injection exception. | No further action unless the fixture provider changes. |
| MT-AUDIT-011 | partial source_drift / test_gap | Tasks 5 and 11 implement general snapshot records, deterministic hashing, repeat-render determinism checks, snapshot-level parallel equivalence, and plan/report byte-stability tests. Fuzz-regression and update runner modes remain future active-runner work. | Keep remaining runner modes deferred until owning consumer/update flows exist. |
| MT-AUDIT-012 | resolved / watch | Task 6 reports the corpus-wide pass/fail mix. Task 9 validates generated reproducibility metadata, fuzz category preservation, stress exclusion, provenance metadata, and corpus size/naming/placement review rules. | Keep corpus policy validation synchronized as new corpus classes land. |
| MT-AUDIT-013 | resolved / watch | Task 3 adds focused expectation-schema fixtures, and task 6 adds manifest/source/link/deferred/planned-warning traceability fixtures. | Keep fixture coverage in step with future schema fields. |
| MT-AUDIT-014 | test_gap | Task 14 records all 18 architecture-22 regression rows as planned metadata and rejects active gates. No prepared consumer runner increment was confirmed for source-derived clean/incremental/parallel/cache-race execution, so active rows cannot run without fabricating execution. | Keep the matrix planned/open as a consumer-paced external dependency until consumer-specific source-derived increments exist; the next source-derived semantic bridge should inventory prepared seams before promoting any row to active. |
| MT-AUDIT-015 | partial source_drift / external_dependency_gap | Task 16 adds the first narrow `.miz` source bridge for builtin type-expression normalization, task 17 projects that supported slice through checker-owned `ResolvedTypedAst`, task 18 widens the active pass slice to reserve-only builtin declarations, checker task 48 moves the reserve binding/declaration handoff producer into a checker-owned syntax-free seam, checker task 50 extracts same-module attributed builtin reserve payloads to the checker evidence-query diagnostic, checker task 51 extracts same-module local mode reserve heads to the checker missing mode-expansion diagnostic, checker task 52 extracts same-module local structure reserve heads to the checker evidence-query diagnostic, checker task 53 extracts attributed same-module local structure reserve heads to the checker evidence-query diagnostic without positive existential/base-shape evidence, checker task 54 extracts attributed same-module local mode reserve heads to the checker missing mode-expansion diagnostic when no supported real expansion is available or the same mode is mixed with a bare reserve use, checker task 55 extracts real AST-derived bare builtin RHS expansions for same-module no-argument local mode reserve heads, checker task 56 extracts one-edge same-module local-mode expansion chains when the dependency mode already has that accepted task-55 builtin RHS expansion, checker task 57 extracts a real same-module local-mode expansion whose RHS is a same-module local structure head to the checker evidence-query diagnostic for missing base-shape/constructor-witness evidence, checker task 58 extracts a real same-module local-mode expansion whose RHS is an attributed builtin head to the checker evidence-query diagnostic for missing attributed-type existential evidence, checker task 59 extracts an attributed same-module local mode reserve head with a real direct bare-builtin RHS expansion to the same evidence-query diagnostic when the same mode is not mixed with a bare reserve use, checker task 60 extracts an attributed same-module local mode reserve head with a real direct local-structure RHS expansion to the missing full attributed structure-type evidence query when the same mode is not mixed with a bare reserve use, checker task 61 extracts an attributed same-module local mode reserve head with a real direct attributed-builtin RHS expansion to the missing full attributed-type evidence query when the same mode is not mixed with a bare reserve use, checker task 62 extracts a one-edge bare local-mode chain ending in a same-module local structure RHS to the missing base-shape/constructor-witness evidence query, checker task 63 extracts a one-edge bare local-mode chain ending in an attributed builtin RHS to the missing attributed-type evidence query, checker task 64 extracts an attributed local-mode reserve head whose one-edge dependency chain ends in a bare builtin RHS to the missing attributed-type evidence query, checker task 65 extracts an attributed local-mode reserve head whose one-edge dependency chain ends in a same-module local structure RHS to the missing structure and attributed-type evidence query, checker task 66 extracts an attributed local-mode reserve head whose one-edge dependency chain ends in an attributed builtin RHS to the missing full attributed-type evidence query, checker task 67 keeps same-module structure-qualified attribute references on the payload extraction gap until real qualifier and attribute-owner provenance exist, checker task 68 keeps argument-bearing same-module local mode reserve heads on the payload extraction gap until real type-argument and term-argument provenance exist, checker task 69 keeps argument-bearing same-module local structure reserve heads on the payload extraction gap until real type-argument and term-argument provenance exist, checker task 70 keeps bracket-form same-module local mode reserve heads on the payload extraction gap before mode-head resolution until real bracket type-argument and `qua`-argument provenance exist, checker task 71 keeps bracket-form same-module local structure reserve heads on the payload extraction gap before structure-head resolution until real bracket type-argument and `qua`-argument provenance exist, checker task 72 extracts two-edge bare local-mode chains ending in builtin `set` / `object`, checker task 73 extracts three-edge bare local-mode chains ending in builtin `set` / `object`, checker task 74 extracts AST-bounded structural bare local-mode chains ending in builtin `set` / `object`, checker task 75 records a lower-stage active-range rejection for forward same-module local-mode reserve heads before checker handoff, checker task 76 records the matching lower-stage active-range rejection for forward same-module local-structure reserve heads before checker handoff, checker task 77 records the matching lower-stage active-range rejection for forward same-module local-attribute reserve type expressions before checker handoff, checker task 78 historically recorded the documented imported structure `R` reserve head on the source-to-checker extraction gap before task 83 superseded that `R` portion, checker task 79 records imported mode reserve heads from that same import summary on the same extraction gap, checker task 80 historically records imported attribute reserve types from that same import summary on the same extraction gap before task 84 supersedes the documented `TypeCaseAttr` portion, task 85 supersedes the negative `empty`/builtin-`set` portion, and task 116 supersedes the positive `empty`/builtin-`set` portion, checker task 82 promotes only the imported mode symbol provenance/type-head handoff from that import summary to the checker missing mode-expansion diagnostic, checker task 83 promotes only the imported structure `R` provenance/type-head handoff from that import summary to the checker missing structure-evidence diagnostic, checker task 97 promotes only the imported structure `TypeCaseStruct` provenance/type-head handoff to the same checker missing structure-evidence diagnostic, checker task 84 promotes only the imported attribute `TypeCaseAttr` provenance/`AttributeInput` handoff from that import summary to the checker missing attributed-type evidence diagnostic, checker task 85 promotes only the imported negative `empty`/builtin-`set` provenance/`AttributeInput` handoff from that import summary to the same checker missing attributed-type evidence diagnostic, checker task 116 promotes only the imported positive `empty`/builtin-`set` provenance/`AttributeInput` handoff from that import summary to the same checker missing attributed-type evidence diagnostic, checker task 86 records a formula-only theorem source as an active parser/resolver-to-checker extraction-gap boundary, checker task 117 supersedes checker task 115 for the exact `FormulaPayloadBoundary: thesis` source by passing the source-derived `thesis` formula constant as a real `FormulaKind::Thesis` checker payload before failing closed on missing formula payload, checker task 106 supersedes checker task 87 for the exact builtin equality theorem source by passing real checker term/formula payloads before failing on missing numeric type payloads and partial formula checking, checker task 110 supersedes checker task 98 for the exact imported predicate/functor theorem source by passing real checker numeral, imported functor-application, and predicate-application payloads before failing on missing numeric/signature payloads and partial formula checking, checker task 108 supersedes checker task 100 for the exact builtin membership theorem source by passing real checker term/formula payloads before failing on missing numeric type payloads and partial formula checking, checker task 107 supersedes checker task 101 for the exact builtin inequality theorem source by passing real checker term/formula payloads before failing on missing numeric type payloads and partial formula checking, checker task 109 supersedes checker task 102 for the exact builtin type-assertion theorem source by passing real checker term/formula/asserted-type payloads before failing on missing numeric type payloads and partial formula checking, checker task 113 supersedes task 103 for the exact imported attribute assertion theorem formula by validating imported `empty` provenance and passing checker term/formula payloads before failing on missing numeric type payload, missing formula/attribute semantic payload, and partial formula checking, checker task 114 supersedes checker task 104 for the exact attribute-level non-empty imported attribute assertion theorem formula by passing real checker term/formula payloads before failing on missing numeric type payload, missing formula/attribute semantic payload, and partial formula checking, checker task 111 supersedes checker task 105 for the exact set-enumeration theorem source by passing real checker term/formula payloads before failing on missing numeric/result-type payloads and partial formula checking, checker task 112 supersedes checker task 99 for the exact connective/quantifier theorem formula source by passing real checker formula shell payloads before failing on missing formula/quantifier payloads, and checker task 117 extends that exact source with real `FormulaKind::Contradiction` constant payloads, checker task 180 checks the exact standalone `SourceDerivedContradictionConstantBoundary: contradiction` leaf as one `Checked` `FormulaKind::Contradiction` for type/well-formedness only, and checker task 88 records a proof-block theorem source as the same active extraction-gap boundary, checker task 89 records statement-level proof-justification theorem sources as the same active extraction-gap boundary, and checker task 90 records predicate/functor definition sources as the same active parser/resolver-to-checker extraction-gap boundary, checker task 91 records attribute definition sources as the same active parser/resolver-to-checker extraction-gap boundary, and checker task 92 records mode/structure definition sources as the same active parser/resolver-to-checker extraction-gap boundary, and checker task 93 records proof-local declaration statement sources as the same active parser/resolver-to-checker extraction-gap boundary, and checker task 94 records proof-local inline definition sources as the same active parser/resolver-to-checker extraction-gap boundary, checker task 95 records registration block sources as the same active parser/resolver-to-checker extraction-gap boundary, and checker task 96 records redefinition/notation sources as the same active parser/resolver-to-checker extraction-gap boundary. AST-wide non-builtin declarations beyond task 96's redefinition/notation extraction-gap boundary, imported attributes beyond the task-84 `TypeCaseAttr` bridge, task-85 negative `empty`/builtin-`set` bridge, task-116 positive `empty`/builtin-`set` bridge, and task-80 boundary, imported structures beyond the task-83 `R` bridge and task-97 `TypeCaseStruct` bridge, imported mode expansion payloads, attribute arguments, qualified attribute provenance, mode/structure arguments, bracket `type_arg_list` and `qua`-argument provenance, term-argument provenance, structure base-shape and full attributed-type existential evidence, broader/imported/attributed/argument-bearing/parameterized/contextual/ambiguous/cyclic mode expansion and chains that violate task 74 structural guards, imported predicate/functor semantic payloads, numeric/signature/result-type payloads beyond tasks 106, 107, 108, 109, 110, 111, 113, 114, and 117 plus equality/inequality/membership/type-assertion/imported predicate-functor/set-enumeration semantic checking beyond those exact bridges, set-enumeration result-type/sethood payload extraction beyond task 111, formula-constant semantics beyond task 180 exact standalone contradiction type/well-formedness slice, formula child/binder semantics beyond task 112, membership operand expected-type construction/checking, inequality desugaring or equality semantic checking, broader type-assertion type payload extraction, type-assertion semantic checking, imported attribute assertion attribute-chain/provenance payload extraction, imported attribute-level non-empty assertion attribute-chain/provenance payload extraction, negated attribute admissibility/semantic checking, attribute admissibility/semantic checking, quantifier binder/context payloads, formula/theorem payloads, proof-local declaration payloads beyond the task-93 extraction-gap boundary, inline definition payloads beyond the task-94 extraction-gap boundary, registration-item/correctness/activation payloads beyond the task-95 extraction-gap boundary, redefinition/notation payloads beyond the task-96 extraction-gap boundary, and proof skeleton and statement proof payloads beyond the task-110, task-111, task-112, task-113, task-114, and task-117 checker bridges and task-105/task-88/task-89 extraction-gap boundaries, coercions, overload payloads, facts, proof evidence, CoreIr, ControlFlowIr, and VC extraction are still unavailable. | Keep unsupported semantic/CoreIr/ControlFlowIr/VC/proof payload families deferred. Promote traceability rows only after prepared consumer execution is confirmed, and do not fabricate active fixtures for unimplemented runners, imported/ambiguous symbols beyond the task-84 `TypeCaseAttr` bridge, task-85 negative `empty`/builtin-`set` bridge, and task-116 positive `empty`/builtin-`set` bridge, task-82 imported-mode provenance slice, task-83 imported-structure `R` provenance slice, task-97 imported-structure `TypeCaseStruct` provenance slice, task-84 imported-attribute `TypeCaseAttr` provenance slice, task-85 imported negative `empty`/builtin-`set` provenance slice, task-116 imported positive `empty`/builtin-`set` provenance slice, task-106 builtin equality theorem checker bridge, task-107 builtin inequality theorem checker bridge, task-108 builtin membership theorem checker bridge, task-109 builtin type assertion theorem checker bridge, task-110 imported predicate/functor theorem checker bridge, task-111 set-enumeration theorem checker bridge, task-112 formula connective/quantifier shell checker bridge, task-86 parser/resolver formula-only reachability boundary, task-105 theorem/formula extraction-gap boundary, and the task-113/task-114/task-117 checker bridges, task-88 proof-skeleton extraction-gap boundary, task-89 statement-proof extraction-gap boundary, task-92 mode/structure definition extraction-gap boundary, task-93 proof-local declaration extraction-gap boundary, task-94 proof-local inline definition extraction-gap boundary, task-95 registration block extraction-gap boundary, task-96 redefinition/notation extraction-gap boundary, imported mode expansions, mixed attributed/bare local-mode uses, broader mode-expansion payloads outside task 74 structural guards, qualified attribute payloads, type/term-argument payloads, bracket/`qua`-argument payloads, existential evidence, structure evidence beyond the task-83/task-97 missing-evidence diagnostic, attributed-type evidence beyond the task-84/task-85/task-116 missing-evidence diagnostics, or proof-local declaration payloads, inline definition payloads, registration payloads, or future-declaration payloads. |
| MT-AUDIT-016 | partial source_drift / external_dependency_gap | Task 19 confirms that the real reserve-only builtin `ResolvedTypedAst` payload can be read by `mizar-core`'s `ResolvedTypedAstSummary::from_ast` with source/module identity preserved and no checker sites. The next builtin declaration inventory found no safe promotion: `let`/`given`/`consider`/quantified declarations need local scope, assumption, formula, or constraint-discharge payloads; `set` needs RHS term inference; `reconsider` needs coercion/obligation evidence; `deffunc`/`defpred` need body/formal payloads. | Keep these declaration families on the source-to-checker extraction gap until a prepared active runner seam can execute real payloads without raw reconstruction or fake evidence. Do not treat summary-readiness as `CoreIr`, `ControlFlowIr`, VC, or proof execution. |
| MT-AUDIT-017 | partial source_drift / external_dependency_gap | Task 20 confirms that the same real reserve-only builtin checker payload can seed `mizar-core`'s binder-only `CoreContext` preparation with one `CoreVariableSeed` and `CoreBinderSeed` per extracted reserve binding and no `CoreItemSeed`. The runner checks source/module identity, binder source ranges, provenance, empty item registry, empty diagnostics, and an empty core worklist. Reserve declarations still do not provide owner items, terms, formulas, proof skeletons, algorithm shells, or obligation seeds. | Keep `CoreIr`, `ControlFlowIr`, VC, proof rows, publication/orchestration, and broader semantic payload families deferred until real source-derived payloads for those consumers exist. |
| MT-AUDIT-018 | resolved design_drift | Task 22 reconciles architecture 20's retired `tests/kernel_evidence/` directory name with the implemented `tests/certificates/` corpus root, updates the corpus README, and records kernel audit F8 as resolved. | Keep `tests/certificates/` as the canonical certificate/kernel-evidence corpus root. No `doc/design/spec_coverage_audit.md` update is required because no spec chapter coverage, follow-up owner, traceability status, or deferred rationale changed. |
| MT-AUDIT-019 | resolved test_gap / design_drift | Task 21 extends the required soundness-case registry and paired docs with the corrected kernel rejection vocabulary from audit F7, and re-keys existing corrected-path certificate sidecars onto `soundness.certificate.*` stable keys. | Keep the registry synchronized with the certificate corpus as rejection vocabulary evolves. No `doc/design/spec_coverage_audit.md` update is required because traceability status, owner crate, and deferred rationale are unchanged. |

Task 81 addendum: `mizar-test` also owns the active runner metadata for the
argument-bearing local-attribute bridge boundary. The active declaration-symbol
fixture records the resolver-derived parameterized local attribute suffix
(`ranked`) as the exported primary symbol while preserving the prefixed
surface (`2 - ranked`) as notation provenance. The paired active
type-elaboration fixture records the fail-closed checker extraction gap on
`checker.type_elaboration.payload_extraction.argument_bearing_attribute_gap`.
This addendum does not promote checker `AttributeInput` argument payloads,
attributed evidence, CoreIr, ControlFlowIr, VC, or proof payloads; those remain
deferred until real consumer payloads and runner support exist.

Task 82 addendum: `mizar-test` also owns the active runner metadata for the
imported-mode reserve provenance bridge. The existing imported-mode
type-elaboration fixture may now credit only the real imported `SymbolKind::Mode`
symbol handoff from `parser.type_fixtures` into the checker type head and must
still fail closed on
`type_elaboration.checker.checker.type.external.mode_expansion_payload` until
imported mode expansion payloads exist. This addendum does not promote imported
module AST extraction, positive imported mode elaboration, CoreIr,
ControlFlowIr, VC, or proof payloads.

Task 83 addendum: `mizar-test` also owns the active runner metadata for the
imported-structure reserve provenance bridge. The existing imported-structure
type-elaboration fixture may now credit only the real imported
`SymbolKind::Structure` symbol handoff for `parser.type_fixtures` structure `R`
into the checker type head and must still fail closed on
`type_elaboration.checker.checker.declaration.deferred.evidence_query` until
imported module AST extraction plus base-shape/constructor-witness evidence
exist. Imported structures outside that documented `R` fixture remain on the
task-78 extraction-gap boundary. This addendum does not promote positive
imported structure elaboration, CoreIr, ControlFlowIr, VC, or proof payloads.

Task 97 addendum: `mizar-test` also owns the active runner metadata for the
documented `parser.type_fixtures` structure `TypeCaseStruct` imported-structure
reserve provenance bridge. The active fixture may credit only the real imported
`SymbolKind::Structure` symbol handoff into the checker type head and must fail
closed on `type_elaboration.checker.checker.declaration.deferred.evidence_query`
until imported module AST extraction plus base-shape/constructor-witness
evidence exist. Imported structures outside the task-83 `R` and task-97
`TypeCaseStruct` fixtures remain on the task-78 extraction-gap boundary.
This addendum does not promote positive imported structure elaboration, CoreIr,
ControlFlowIr, VC, or proof payloads.

Task 84 addendum: `mizar-test` also owns the active runner metadata for the
imported-attribute reserve provenance bridge. The existing imported-attribute
type-elaboration fixture may now credit only the real imported
`SymbolKind::Attribute` symbol handoff for `parser.type_fixtures` attribute
`TypeCaseAttr` into checker `AttributeInput` over builtin `set` and must still
fail closed on `type_elaboration.checker.checker.declaration.deferred.evidence_query`
until imported module AST extraction plus attributed-type evidence queries
exist. Imported attributes outside that documented `TypeCaseAttr set` fixture
remain on the task-80 extraction-gap boundary. This addendum does not promote
positive imported attribute elaboration, attribute arguments, qualified/owner
attribute provenance, CoreIr, ControlFlowIr, VC, or proof payloads.

Task 85 / task 116 / task 171 addendum: `mizar-test` also owns the active runner metadata
for the imported empty attribute reserve provenance bridges. The existing
`non empty set`, `empty set`, and `non empty object` type-elaboration fixtures
may now credit only
the real imported `SymbolKind::Attribute` symbol handoff for
`parser.type_fixtures` attribute `empty` as negative/positive checker
`AttributeInput` payloads over builtin `set` or the exact negative payload over
builtin `object`, and must still fail closed on
`type_elaboration.checker.checker.declaration.deferred.evidence_query` until
imported module AST extraction plus attributed-type evidence queries exist.
Positive `empty object`, imported `empty` on symbol heads, and imported
attributes outside the documented `TypeCaseAttr set` and task-85/task-116/
task-171 exact fixtures remain on the task-80 extraction-gap boundary.

Task 86 / task 115 / task 117 addendum: `mizar-test` owns the active runner
metadata for the formula-only theorem boundary. Task 115 introduced the exact
`FormulaPayloadBoundary: thesis` recovery marker, and task 117 supersedes that
marker by passing the source-derived `thesis` formula constant to the checker
as a real `FormulaKind::Thesis` payload, then failing closed on missing formula
payload. This addendum does not promote formula constant semantic checking,
child-formula graph payloads, recorded facts, theorem acceptance, proof
skeletons, `formula_statement` runner support, CoreIr, ControlFlowIr, VC, or
proof payloads.

Task 106 addendum: `mizar-test` supersedes the task-87 generic boundary for the
exact term-bearing builtin equality theorem sidecar. The runner may execute
`theorem TermFormulaPayloadBoundary: 1 = 1;` through parser and resolver under
`type_elaboration`, build real source-derived checker `TermInput`/`FormulaInput`
payloads under a module-shell binding context, and fail closed on missing
numeric type payloads plus partial formula checking. This addendum does not
promote numeric type payloads, equality semantic checking, recorded facts,
theorem acceptance, proof skeletons, `formula_statement` runner support, CoreIr,
ControlFlowIr, VC, or proof payloads.

Task 98 addendum: `mizar-test` also owns the historical active runner metadata
for the imported predicate/functor theorem formula extraction-gap boundary.
Task 110 supersedes the exact
`ImportedPredicateFunctorPayloadBoundary: 1 divides (1 ++ 2)` sidecar: it may
import `parser.type_fixtures`, validate imported `divides`/`++` resolver
provenance, build source-derived checker numeral, imported functor-application,
and predicate-application payloads under `type_elaboration`, and fail closed on
missing numeric/signature payloads plus partial formula checking. This addendum
does not promote imported module AST extraction, predicate/functor semantic
signatures, term inference, formula checking, recorded facts, theorem
acceptance, the dedicated `formula_statement` runner, CoreIr, ControlFlowIr,
VC, or proof payloads.

Task 100 addendum: `mizar-test` also owns the active runner metadata for the
builtin membership theorem formula extraction-gap boundary. Task 108 supersedes
the exact sidecar by executing
`theorem BuiltinMembershipPayloadBoundary: 1 in 1;` through parser and resolver
under `type_elaboration`, building real source-derived checker
`TermInput`/`FormulaInput` payloads under a module-shell binding context, and
failing closed on missing numeric type payloads plus partial formula checking.
This addendum does not promote numeric type payloads, membership operand
expected-type construction/checking, recorded facts, theorem acceptance, proof
skeletons, `formula_statement` runner support, CoreIr, ControlFlowIr, VC, or
proof payloads.

Task 107 addendum: `mizar-test` supersedes the task-101 generic boundary for
the exact builtin inequality theorem sidecar. The runner may execute
`theorem BuiltinInequalityPayloadBoundary: 1 <> 2;` through parser and resolver
under `type_elaboration`, build real source-derived checker
`TermInput`/`FormulaInput` payloads under a module-shell binding context, and
fail closed on missing numeric type payloads plus partial formula checking.
This addendum does not promote numeric type payloads, inequality
desugaring/equality semantic checking, recorded facts, theorem acceptance,
proof skeletons, `formula_statement` runner support, CoreIr, ControlFlowIr, VC,
or proof payloads.

Task 118 addendum: `mizar-test` tightens the shared task 106/107/108
builtin-binary theorem producer so equality, membership, and inequality configs
are selected only for exact direct theorem tokens `theorem <label> : ;`.
Status-prefixed or extra-token theorem shapes stay on
`type_elaboration.external_dependency.ast_payload_extraction`. This guard-only
repair does not add active `.miz` sidecar coverage, traceability coverage, spec
coverage credit, or new checker payload support.

Tasks 119-169 current-state override: this paragraph and
the sibling task paragraphs below are the authoritative qualification of the earlier
MT-AUDIT-015 master row. Its generic unavailable term/formula wording now means
payloads beyond the exact promoted Tasks 119-169 reserved-variable slices.
`mizar-test` owns the exact active pass source
`reserve x for set; theorem ReservedVariableEqualityPayloadBoundary: x = x;`.
The runner combines the existing real reserve handoff with identifier-term and
equality extraction, resolves both `x` terms through independent checker-owned
`BindingEnv` lookups at source-order-derived use ordinals, and projects the written builtin `set` type to distinct
source-anchored result/expected role sites. The run produces two `Inferred`
variable terms and one `Checked` equality formula without candidates,
diagnostics, deferred reasons, or facts. Production validation enforces the full
payload invariants, and a real-frontend/resolver unit test observes the same
payload behind the active sidecar.
This credits type/well-formedness only; implicit closure, equality truth/facts,
theorem acceptance, `formula_statement`, proof, CoreIr, ControlFlowIr, and VC
remain deferred.

Task 123 current-state override: `mizar-test` owns the exact active pass source
`reserve x, y for set; theorem DistinctReservedVariableEqualityPayloadBoundary: x = y;`.
The runner reuses the real two-binding reserve handoff, preserves the shared
written builtin `set` type range without collapsing binding identity, derives
source-order use ordinals after both bindings, and resolves `x` and `y` to
distinct checker bindings. Operand-specific result/expected roles produce two
`Inferred` variables and one fact-free `Checked` equality with empty
candidate/deferred/diagnostic output. Production validation, a near-miss
matrix, and a real frontend/resolver sidecar test guard the exact slice. The
generic MT-AUDIT-015 term/formula gap now means coverage beyond task 119's
same-binding equality and task 123's distinct-binding equality, as well as the
other promoted exact siblings. Implicit closure/order nodes, equality
truth/facts, theorem acceptance, proof, CoreIr, ControlFlowIr, and VC remain
deferred.

Task 124 current-state override: `mizar-test` owns the exact active pass source
`reserve x for set; reserve y for set; theorem MultipleReserveDeclarationEqualityPayloadBoundary: x = y;`.
The runner accepts exactly two ordered reserve items, preserves two binding
identities and two distinct written builtin `set` ranges, and retains those
ranges in four operand-specific pre-normalization result/expected inputs. The
checker interns the semantically equal inputs to one normalized type with the
earliest deterministic source representative; production validation checks the
original inputs independently and therefore does not confuse semantic identity
with written provenance. An exact near-miss matrix, a task-specific invalid key,
and a real frontend/resolver sidecar guard the bridge. MT-AUDIT-015 still covers
all broader term/formula shapes. Implicit closure/order, equality truth/facts,
theorem acceptance, proof, CoreIr, ControlFlowIr, and VC remain deferred.

Task 125 current-state override: `mizar-test` owns the exact active pass source
`reserve x for object; reserve y for set; theorem HeterogeneousReserveMembershipPayloadBoundary: x in y;`.
The binary bridge now validates binding-specific builtin shapes. It retains a
left source-derived `object` result and right source-derived `set` result plus
expected input, then requires exactly two normalized identities with both right
roles sharing `set`. Production invariants validate the identity partition and
per-type deterministic source representatives; a task invalid key, exact
near-miss matrix, and real frontend/resolver sidecar guard the slice. The generic
MT-AUDIT-015 gap still covers broader term/formula shapes. Membership truth/facts,
object/set coercion evidence, implicit closure/order, theorem acceptance, proof,
CoreIr, ControlFlowIr, and VC remain deferred.

Task 126 current-state override: `mizar-test` owns the exact active pass source
with one direct bare-set `LocalModeFormula` definition, its reserve, and
`LocalModeReservedVariableEqualityPayloadBoundary: x = x;`. Four raw inputs
retain mode provenance while the real AST-derived expansion is passed to
`TermFormulaChecker`, producing one builtin-set identity and a fact-free checked
equality. Exact guards, an invalid key, near misses, and a real sidecar cover the
slice. Mode acceptance/inhabitation, broader modes, closure/order, truth/facts,
theorem acceptance, proof, CoreIr, ControlFlowIr, and VC remain deferred.

Task 127 current-state override: `mizar-test` owns only the exact active pass source
with separate `BaseModeFormula -> set` and
`ChainModeFormula -> BaseModeFormula` definition blocks, one outer-mode reserve,
and `ChainedLocalModeReservedVariableEqualityPayloadBoundary: x = x;`. The
runner retains four raw outer-mode inputs, passes both real AST-derived
expansions to `TermFormulaChecker`, requires one terminal builtin-set identity
anchored at the terminal RHS, and fails closed on every non-exact block/chain
shape. Mode acceptance or
inhabitation, object terminals, longer-chain formulas, closure/order,
truth/facts, theorem acceptance, proof, CoreIr, ControlFlowIr, and VC remain
deferred.

Task 128 current-state override: `mizar-test` owns only the exact active pass
source with task 55's direct `LocalObjectMode -> object` definition, one
reserve, and
`LocalObjectModeReservedVariableEqualityPayloadBoundary: x = x;`. The runner
retains four raw object-mode inputs, passes the real expansion to
`TermFormulaChecker`, requires one terminal builtin-object identity at the real
RHS, and fails closed on non-exact mode/formula shapes. Mode acceptance or
inhabitation, closure/order, truth/facts, theorem acceptance, proof, CoreIr,
ControlFlowIr, and VC remain deferred.

Task 129 current-state override: `mizar-test` owns only the exact active pass source
that extends task 56's `ChainObjectMode -> BaseObjectMode -> object` definitions
with one outer-mode reserve and
`ChainedLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;`. The
runner retains four raw outer-mode inputs, passes both real expansions to
`TermFormulaChecker`, requires one terminal-RHS builtin-object identity, and fails
closed on non-exact block/chain/formula shapes. Mode acceptance/inhabitation,
closure/order, truth/facts, theorem acceptance, proof, CoreIr, ControlFlowIr,
and VC remain deferred.

Task 130 current-state override: `mizar-test` owns only the exact active pass
source `LocalModeInequality -> set`, one reserve, and
`LocalModeReservedVariableInequalityPayloadBoundary: x <> x;`. The runner
retains four raw mode inputs, passes the real expansion to
`TermFormulaChecker`, requires one RHS-anchored builtin-set identity and one
fact-free pre-desugaring `Checked` inequality, and fails closed on non-exact
shapes. Declaration acceptance/inhabitation, desugaring, truth/facts, theorem
acceptance, proof/Core/ControlFlow/VC remain deferred.

Task 131 current-state override: `mizar-test` owns only the exact active pass
source `LocalObjectModeInequality -> object`, one reserve, and
`LocalObjectModeReservedVariableInequalityPayloadBoundary: x <> x;`. The runner
retains four raw object-mode inputs, passes the real expansion to
`TermFormulaChecker`, requires one RHS-anchored builtin-object identity and one
fact-free pre-desugaring `Checked` inequality, and fails closed on non-exact
shapes. Mode declaration acceptance/inhabitation, desugaring, closure/order,
truth/facts, theorem acceptance, proof/Core/ControlFlow/VC remain deferred.

Task 132 current-state override: `mizar-test` owns only the exact active pass
source `ChainModeInequality -> BaseModeInequality -> set`, one outer reserve,
and `ChainedLocalModeReservedVariableInequalityPayloadBoundary: x <> x;`. The
runner retains four raw outer-mode inputs, passes both real expansions to
`TermFormulaChecker`, requires one terminal-RHS builtin-set identity and one
fact-free pre-desugaring `Checked` inequality, and fails closed on non-exact
links/block/formula shapes. Mode declaration acceptance/inhabitation,
object-terminal, direct, or longer shapes, desugaring, closure/order,
truth/facts, theorem acceptance, proof/Core/ControlFlow/VC remain deferred.

Task 133 current-state override: `mizar-test` owns only the exact active pass
source `ChainObjectModeInequality -> BaseObjectModeInequality -> object`, one
outer reserve, and
`ChainedLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;`.
The runner retains four raw outer-mode inputs, passes both real expansions to
`TermFormulaChecker`, requires one terminal-RHS builtin-object identity and one
fact-free pre-desugaring `Checked` inequality, and fails closed on non-exact
links/block/formula shapes. Declaration acceptance/inhabitation, set-terminal,
direct or longer shapes, desugaring, closure/order, truth/facts, theorem acceptance,
proof/Core/ControlFlow/VC remain deferred.

Task 134 current-state override: `mizar-test` owns only the exact active pass
source
`OuterTwoEdgeModeEquality -> MiddleTwoEdgeModeEquality -> BaseTwoEdgeModeEquality -> set`,
one outer reserve, and
`TwoEdgeLocalModeReservedVariableEqualityPayloadBoundary: z = z;`. The runner
retains four raw outer-mode inputs, passes all three real expansions to
`TermFormulaChecker`, requires one terminal-RHS builtin-set identity and one
fact-free `Checked` equality, and fails closed on non-exact links/block/formula
shapes. Declaration acceptance/inhabitation, object-terminal, direct, one-edge,
or longer shapes, implicit closure/order, truth/facts, theorem acceptance,
proof/Core/ControlFlow/VC remain deferred.

Task 135 current-state override: `mizar-test` owns only the exact active pass
source
`OuterTwoEdgeObjectModeEquality -> MiddleTwoEdgeObjectModeEquality -> BaseTwoEdgeObjectModeEquality -> object`,
one outer reserve, and
`TwoEdgeLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;`. The
runner retains four raw outer-mode inputs, passes all three real expansions to
`TermFormulaChecker`, requires one terminal-RHS builtin-object identity and one
fact-free `Checked` equality, and fails closed on non-exact links/block/formula
shapes. Declaration acceptance/inhabitation, set-terminal semantics beyond
task 134, direct, one-edge, or longer shapes, implicit closure/order,
truth/facts, theorem acceptance, proof/Core/ControlFlow/VC remain deferred.

Task 136 current-state override: `mizar-test` owns only the exact active pass
source
`OuterTwoEdgeModeInequality -> MiddleTwoEdgeModeInequality -> BaseTwoEdgeModeInequality -> set`,
one outer reserve, and
`TwoEdgeLocalModeReservedVariableInequalityPayloadBoundary: z <> z;`. The
runner retains four raw outer-mode inputs, passes all three real expansions to
`TermFormulaChecker`, requires one terminal-RHS builtin-set identity and one
fact-free pre-desugaring `Checked` inequality, and fails closed on non-exact
links/block/formula shapes. Mode declaration acceptance/inhabitation,
object-terminal, direct, one-edge, or longer shapes, inequality desugaring,
implicit closure/order, truth/facts, theorem acceptance,
proof/Core/ControlFlow/VC remain deferred.

Task 137 current-state override: `mizar-test` owns only the exact active pass
source
`OuterTwoEdgeObjectModeInequality -> MiddleTwoEdgeObjectModeInequality -> BaseTwoEdgeObjectModeInequality -> object`,
one outer reserve, and
`TwoEdgeLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;`.
The runner retains four raw outer-mode inputs, passes all three real expansions
to `TermFormulaChecker`, requires one terminal-RHS builtin-object identity and
one fact-free pre-desugaring `Checked` inequality, and fails closed on non-exact
links/block/formula shapes. Declaration acceptance/inhabitation, set-terminal,
direct, one-edge, or longer shapes, inequality desugaring, implicit
closure/order, truth/facts, theorem acceptance, proof/Core/ControlFlow/VC remain
deferred.

Task 138 current-state override: `mizar-test` owns only the exact
active source with one unique, unrecovered, source-preceding, no-argument bare
mode definition `LocalModeTypeAssertion -> set`, one reserve of that mode, and
`LocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`. The runner
preserves the raw local-mode subject-result source and the independent
formula-side builtin-set asserted source, passes the one real expansion to the
checker, and requires one RHS-anchored normalized builtin-set identity,
`BindingId(0)`, one `Inferred` term, and one fact-free `Checked` type assertion.
All non-exact mode/definition/reserve/formula shapes fail closed. Formula-side
local-mode asserted-head extraction, mode declaration acceptance/inhabitation,
general reachability/widening/`qua`, facts, theorem/proof, and
Core/ControlFlow/VC remain deferred. The production bridge and real
frontend/resolver sidecar pass, so the active runner count is 89.

Task 139 current-state override: `mizar-test` owns only the exact
active source with one unique, unrecovered, source-preceding, no-argument bare
mode definition `LocalModeMembership -> set`, ordered reserves `x` for that
mode and `y` for explicit `set`, and
`LocalModeReservedVariableMembershipPayloadBoundary: x in y;`. The runner must
preserve the raw local-mode left-result source and the independent right
builtin-set result/expected sources, pass the one real expansion to the
checker, and require one RHS-anchored normalized builtin-set identity,
`BindingId(0/1)`, two `Inferred` terms, one fact-free `Checked` membership,
exactly one expected constraint on the right, and none on the left. All
non-exact mode/definition/reserve/formula shapes fail closed. Unit coverage
independently corrupts the sole real expansion and the independently
reserve-derived right expected-`set` projection/constraint; both must report
the dedicated invalid-payload key, while the exact case proves no left expected
input and exactly one right-owned formula constraint. Mode declaration
acceptance/inhabitation, membership truth/facts, implicit closure/order,
theorem/proof, and Core/ControlFlow/VC remain deferred. The production bridge
and real frontend/resolver sidecar pass, so the active runner count is 90.

Task 140 current-state override: `mizar-test` owns only the exact
active source with one unique, unrecovered, source-preceding, no-argument bare
mode definition `LocalObjectModeMembership -> object`, ordered reserves `x`
for that mode and `y` for explicit `set`, and
`LocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;`. The
runner must preserve the raw local object-mode left-result source and the
independent right builtin-set result/expected sources, pass the one real
expansion to the checker, and require distinct RHS-anchored builtin-object and
explicit-reserve-anchored builtin-set identities, `BindingId(0/1)`, two
`Inferred` terms, one fact-free `Checked` membership, exactly one expected
constraint on the right, and none on the left. All non-exact
mode/definition/reserve/formula shapes fail closed. Unit coverage must
independently corrupt the sole real expansion and the independently
reserve-derived right expected-`set` projection/constraint; both must report
the dedicated invalid-payload key, while the exact case proves the two
semantic identities, no left expected input, and exactly one right-owned
formula constraint. Mode declaration acceptance/inhabitation, membership
truth/facts, object/set coercion, implicit closure/order, theorem/proof, and
Core/ControlFlow/VC remain deferred. The production bridge and real
frontend/resolver sidecar pass, so the active runner count is 91.

Task 141 current-state override: `mizar-test` owns only the exact
active source with two unique, unrecovered, source-preceding, no-argument bare
mode definitions `ChainModeMembership -> BaseModeMembership -> set`, ordered
reserves `x` for the outer mode and `y` for explicit `set`, and
`ChainedLocalModeReservedVariableMembershipPayloadBoundary: x in y;`. The
runner must preserve the raw outer-mode left-result source and independent
right builtin-set result/expected sources, pass both real expansions to the
checker, and require one terminal-RHS-anchored builtin-set identity,
`BindingId(0/1)`, two `Inferred` terms, one fact-free `Checked` membership,
exactly one expected constraint on the right, and none on the left. All
non-exact mode/definition/reserve/formula shapes fail closed. Unit coverage
must independently corrupt either real chain link and the independently
reserve-derived right expected-`set` projection/constraint; all must report
the dedicated invalid-payload key, while the exact case proves one semantic
identity, no left expected input, and exactly one right-owned formula
constraint. Mode declaration acceptance/inhabitation, membership truth/facts,
implicit closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred.
The production bridge and real frontend/resolver sidecar pass, so the active
runner count is 92.

Task 142 current-state override: `mizar-test` owns only the exact active
source with two unique, unrecovered, source-preceding, no-argument bare mode
definitions `ChainObjectModeMembership -> BaseObjectModeMembership -> object`,
ordered reserves `x` for the outer mode and `y` for explicit `set`, and
`ChainedLocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;`.
The runner must preserve the raw outer-mode left-result source and independent
right builtin-set result/expected sources, pass both real expansions to the
checker, and require distinct terminal-RHS-anchored builtin-object and
explicit-reserve-anchored builtin-set identities, `BindingId(0/1)`, two
`Inferred` terms, one fact-free `Checked` membership, exactly one expected
constraint on the right, and none on the left. All non-exact
mode/definition/reserve/formula shapes must fail closed. In particular, unit
coverage must independently reject a wrong label on the base definition, a
wrong label on the chain definition, and the argument-bearing chain radix
`ChainObjectModeMembership is BaseObjectModeMembership(...)`. It must also
independently corrupt either real chain link and the independently
reserve-derived right expected-`set` projection/constraint; all must report
the dedicated invalid-payload key, while the exact case proves two semantic
identities, no left expected input, and exactly one right-owned formula
constraint. Mode declaration acceptance/inhabitation, membership truth/facts,
object/set coercion, implicit closure/order, theorem/proof, and
Core/ControlFlow/VC remain deferred. The production bridge and real
frontend/resolver sidecar pass, so the active runner count is 93.

Task 143 current-state override: `mizar-test` owns only the exact active source
with three unique, unrecovered, source-preceding, no-argument bare mode
definitions
`OuterTwoEdgeModeMembership -> MiddleTwoEdgeModeMembership -> BaseTwoEdgeModeMembership -> set`,
ordered reserves `x` for the outer mode and `y` for explicit `set`, and
`TwoEdgeLocalModeReservedVariableMembershipPayloadBoundary: x in y;`. The
runner must preserve the raw outer-mode left-result source and independent
right builtin-set result/expected sources, pass all three real expansions to
the checker, and require one terminal-RHS-anchored builtin-set identity,
`BindingId(0/1)`, two `Inferred` terms, one fact-free `Checked` membership,
exactly one expected constraint on the right, and none on the left. All
non-exact mode/definition/reserve/formula shapes must fail closed. Unit
coverage must independently reject wrong labels on the base, middle, and outer
definitions and argument-bearing radices on both chain links. It must also
independently corrupt every real expansion and the right expected-`set`
projection/constraint; all must report the dedicated invalid-payload key.
Mode declaration acceptance/inhabitation, membership truth/facts, implicit
closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred.
The production bridge and real frontend/resolver sidecar pass, so the active
runner count is 94.

Task 144 current-state override: `mizar-test` owns only the exact active source
with three unique, unrecovered, source-preceding, no-argument bare mode
definitions
`OuterTwoEdgeObjectModeMembership -> MiddleTwoEdgeObjectModeMembership -> BaseTwoEdgeObjectModeMembership -> object`,
ordered reserves `x` for the outer mode and `y` for explicit `set`, and
`TwoEdgeLocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;`.
The runner must preserve the raw outer-mode left-result source and independent
right builtin-set result/expected sources, pass all three real expansions to
the checker, and require distinct terminal-object-RHS-anchored builtin-object
and explicit-reserve-anchored builtin-set identities, `BindingId(0/1)`, two
`Inferred` terms, one fact-free `Checked` membership, exactly one expected
constraint on the right, and none on the left. All non-exact mode/definition/
reserve/formula shapes must fail closed. Unit coverage must independently
reject wrong labels on the base, middle, and outer definitions and
argument-bearing radices on both chain links. It must also independently
corrupt every real expansion and the right expected-`set` projection/
constraint; all must report the dedicated task-144 invalid-payload key. Mode
declaration acceptance/inhabitation, membership truth/facts, object/set
coercion, implicit closure/order, theorem/proof, and Core/ControlFlow/VC remain
deferred. The production bridge and real frontend/resolver sidecar pass, so the
active runner count is 95.

Task 145 current-state override: `mizar-test` owns only the exact active source
with one unique, unrecovered, source-preceding, no-argument bare-object mode
definition `LocalObjectModeTypeAssertion -> object`, one reserve `x` for that
mode, and
`LocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`.
The runner preserves the raw local-mode subject result and the independent
formula-side builtin-object asserted source, passes the one real expansion to
the checker, and requires one terminal-RHS-anchored builtin-object identity,
`BindingId(0)`, source-order use ordinal 1, one `Inferred` term, and one
fact-free `Checked` type assertion without general reachability or object/set
coercion. All non-exact definition/reserve/formula shapes fail closed. Unit
coverage independently rejects the wrong definition label and corrupts the
real expansion; the latter reports the dedicated task-145 invalid-
payload key. Mode declaration acceptance/inhabitation, formula-side local-mode
asserted-head extraction, general reachability/widening/`qua`, truth/facts,
implicit closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred.
The production bridge and real frontend/resolver sidecar pass, so the active
runner count is 96.

Task 146 current-state override: `mizar-test` owns only the exact active source
with two unique, unrecovered, source-preceding, no-argument bare-set mode
definitions `ChainModeTypeAssertion -> BaseModeTypeAssertion -> set`, one
reserve `x` for the outer mode, and
`ChainedLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`.
The runner preserves the raw outer-mode subject result and the independent
formula-side builtin-set asserted source, passes both real expansions to the
checker, and requires one terminal-RHS-anchored builtin-set identity,
`BindingId(0)`, source-order use ordinal 1, one `Inferred` term, and one
fact-free `Checked` type assertion without general reachability. All non-exact
definition/reserve/formula shapes fail closed. Unit coverage independently
rejects both wrong definition labels and corrupts both real expansion entries;
corruptions report the dedicated task-146 invalid-
payload key. Mode declaration acceptance/inhabitation, formula-side local-mode
asserted-head extraction, general reachability/widening/`qua`, truth/facts,
implicit closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred.
The production bridge and real frontend/resolver sidecar pass, so the active
runner count is 97.

Task 147 current-state override: `mizar-test` owns only the exact active
source with two unique, unrecovered, source-preceding, no-argument bare-object
mode definitions
`ChainObjectModeTypeAssertion -> BaseObjectModeTypeAssertion -> object`, one
reserve `x` for the outer mode, and
`ChainedLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`.
The runner preserves the raw outer-mode subject result and the independent
formula-side builtin-object asserted source, passes both real expansions to the
checker, and requires one terminal-RHS-anchored builtin-object identity,
`BindingId(0)`, source-order use ordinal 1, one `Inferred` term, and one fact-
free `Checked` type assertion without general reachability or object/set
coercion. All non-exact definition/reserve/formula shapes fail closed. Unit
coverage independently rejects both wrong definition labels and corrupts both
real expansion entries; corruptions report the dedicated
task-147 invalid-payload key. Mode declaration acceptance/inhabitation,
formula-side local-mode asserted-head extraction, general reachability/
widening/`qua`, truth/facts, implicit closure/order, theorem/proof, and Core/
ControlFlow/VC remain deferred. The production bridge and real frontend/
resolver sidecar pass, so the active runner count is 98.

Task 148 current-state override: `mizar-test` owns only the exact active
source with three unique, unrecovered, source-preceding, no-argument bare-set
mode definitions
`OuterTwoEdgeModeTypeAssertion -> MiddleTwoEdgeModeTypeAssertion -> BaseTwoEdgeModeTypeAssertion -> set`,
one reserve `x` for the outer mode, and
`TwoEdgeLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`.
The runner preserves the raw outer-mode subject result and the independent
formula-side builtin-set asserted source, passes all three real expansions to
the checker, and requires one terminal-RHS-anchored builtin-set identity,
`BindingId(0)`, source-order use ordinal 1, one `Inferred` term, and one fact-
free `Checked` type assertion without general reachability. All non-exact
definition/reserve/formula shapes fail closed. Unit coverage independently
rejects all three wrong definition labels and corrupts all three
real expansion entries; corruptions report the dedicated task-148 invalid-
payload key. Mode declaration acceptance/inhabitation, formula-side local-mode
asserted-head extraction, general reachability/widening/`qua`, truth/facts,
implicit closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred.
The production bridge and real frontend/resolver sidecar pass, so the active
runner count is 99.

Task 149 current-state override: `mizar-test` owns only the exact
source with three unique, unrecovered, source-preceding, no-argument bare-
object mode definitions
`OuterTwoEdgeObjectModeTypeAssertion -> MiddleTwoEdgeObjectModeTypeAssertion -> BaseTwoEdgeObjectModeTypeAssertion -> object`,
one reserve `x` for the outer mode, and
`TwoEdgeLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`.
The runner preserves the raw outer-mode subject result and independent
formula-side builtin-object asserted source, passes all three real expansions to
the checker, and requires one terminal-RHS-anchored builtin-object identity,
`BindingId(0)`, source-order use ordinal 1, one `Inferred` term, and one fact-
free `Checked` type assertion without general reachability or object/set
coercion. All non-exact definition/reserve/formula shapes fail closed.
Unit coverage independently rejects all three wrong definition labels and
corrupts all three real expansion entries; corruptions report the dedicated
task-149 invalid-payload key. Mode declaration acceptance/inhabitation,
formula-side local-mode asserted-head extraction, general reachability/
widening/`qua`, truth/facts, implicit closure/order, theorem/proof, and Core/
ControlFlow/VC remain deferred. The production route and real frontend/resolver
sidecar pass, so the active runner count is 100.

Task 150 current-state override: `mizar-test` owns only the exact
source with four unique, unrecovered, source-preceding, no-argument bare-set
mode definitions
`OuterThreeEdgeModeTypeAssertion -> MiddleThreeEdgeModeTypeAssertion -> InnerThreeEdgeModeTypeAssertion -> BaseThreeEdgeModeTypeAssertion -> set`,
one reserve `x` for the outer mode, and
`ThreeEdgeLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`.
The runner must preserve the raw outer-mode subject result and independent
formula-side builtin-set asserted source, pass all four real Task-73-compatible
expansions to the checker, and require one terminal-RHS-anchored builtin-set
identity, `BindingId(0)`, source-order use ordinal 1, one `Inferred` term, and
one fact-free `Checked` type assertion without general reachability. All non-
exact definition/reserve/formula shapes must fail closed. Unit coverage must
independently reject all four wrong definition labels, all three wrong chain
radices, and corrupt all four real expansion entries; corruptions report the
dedicated task-150 invalid-payload key. Mode declaration acceptance/
inhabitation, formula-side local-mode asserted-head extraction, general
reachability/widening/`qua`, truth/facts, implicit closure/order, theorem/proof,
and Core/ControlFlow/VC remain deferred. The production route and real
frontend/resolver sidecar pass, so the active runner count is 101.

Task 151 current-state override: `mizar-test` owns only the exact
source with four unique, unrecovered, source-preceding, no-argument bare-object
mode definitions
`OuterThreeEdgeObjectModeTypeAssertion -> MiddleThreeEdgeObjectModeTypeAssertion -> InnerThreeEdgeObjectModeTypeAssertion -> BaseThreeEdgeObjectModeTypeAssertion -> object`,
one reserve `x` for the outer mode, and
`ThreeEdgeLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`.
The runner must preserve the raw outer-mode subject result and independent
formula-side builtin-object asserted source, pass all four real Task-73-
compatible expansions to the checker, and require one terminal-RHS-anchored
builtin-object identity, `BindingId(0)`, source-order use ordinal 1, one
`Inferred` term, and one fact-free `Checked` type assertion without general
reachability or object/set coercion. All non-exact definition/reserve/formula
shapes must fail closed. Unit coverage must independently reject all four wrong
definition labels, all three wrong chain radices, and corrupt all four real
expansion entries; corruptions report the dedicated task-151 invalid-payload
key. Mode declaration acceptance/inhabitation, formula-side local-mode asserted-
head extraction, general reachability/widening/`qua`, object/set coercion,
truth/facts, implicit closure/order, theorem/proof, and Core/ControlFlow/VC
remain deferred. The production route and real frontend/resolver sidecar pass,
so the active runner count is 102.

Task 152 current-state override: `mizar-test` owns only the exact source with five
unique, unrecovered, source-preceding, no-argument bare-set mode definitions
`TooDeepFourEdgeModeTypeAssertion -> OuterFourEdgeModeTypeAssertion -> MiddleFourEdgeModeTypeAssertion -> InnerFourEdgeModeTypeAssertion -> BaseFourEdgeModeTypeAssertion -> set`,
one reserve `x` for the outermost mode, and
`FourEdgeLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`.
The runner must preserve the raw outermost-mode subject result and independent
formula-side builtin-set asserted source, pass all five real Task-74-compatible
expansions to the checker, and require one terminal-RHS-anchored builtin-set
identity, `BindingId(0)`, source-order use ordinal 1, one `Inferred` term, and
one fact-free `Checked` type assertion without general reachability. All non-
exact definition/reserve/formula shapes must fail closed. Unit coverage must
independently reject all five wrong definition labels, all four wrong chain
radices, and corrupt all five real expansion entries; corruptions must report
the dedicated task-152 invalid-payload key. Mode declaration acceptance/
inhabitation, formula-side local-mode asserted-head extraction, general
reachability/widening/`qua`, truth/facts, implicit closure/order, theorem/proof,
and Core/ControlFlow/VC remain deferred. The production route and real
frontend/resolver sidecar pass, so the active runner count is 103.

Task 153 current-state override: `mizar-test` owns the exact active source with five
unique, unrecovered, source-preceding, no-argument bare-object mode definitions
`TooDeepFourEdgeObjectModeTypeAssertion -> OuterFourEdgeObjectModeTypeAssertion -> MiddleFourEdgeObjectModeTypeAssertion -> InnerFourEdgeObjectModeTypeAssertion -> BaseFourEdgeObjectModeTypeAssertion -> object`,
one reserve `x` for the outermost mode, and
`FourEdgeLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`.
The runner must preserve the raw outermost-mode subject result and independent
formula-side builtin-object asserted source, pass all five real Task-74-
compatible expansions to the checker, and require one terminal-RHS-anchored
builtin-object identity, `BindingId(0)`, source-order use ordinal 1, one
`Inferred` term, and one fact-free `Checked` type assertion without general
reachability or object/set coercion. All non-exact definition/reserve/formula
shapes must fail closed. Unit coverage must independently reject all five wrong
definition labels, all four wrong chain radices, and corrupt all five real
expansion entries; corruptions must report the dedicated task-153 invalid-
payload key. Mode declaration acceptance/inhabitation, formula-side local-mode
asserted-head extraction, general reachability/widening/`qua`, object/set
coercion, truth/facts, implicit closure/order, theorem/proof, and Core/
ControlFlow/VC remain deferred. The production route and real frontend/resolver
sidecar pass, so the active runner count is 104.

Task 154 implementation specification: `mizar-test` owns the exact test-first
source with four unique, unrecovered, source-preceding, no-argument bare-set
mode definitions
`OuterThreeEdgeModeEquality -> MiddleThreeEdgeModeEquality -> InnerThreeEdgeModeEquality -> BaseThreeEdgeModeEquality -> set`,
one reserve `z` for the outer mode, and
`ThreeEdgeLocalModeReservedVariableEqualityPayloadBoundary: z = z;`. The runner
must preserve four raw outer-mode result/expected inputs, resolve both operands
to `BindingId(0)` with ordinals 1 and 2, pass all four real Task-73-compatible
expansions to `TermFormulaChecker`, and require one terminal-RHS builtin-set
identity, two `Inferred` variables, and one fact/deferred-free `Checked`
equality. Exact source guards must independently reject every wrong definition
label/radix and corrupt every expansion entry; withheld chain, terminal,
reserve, and formula families remain on the extraction gap. Mode declaration
acceptance/inhabitation, equality truth/facts, closure/order, theorem/proof,
and Core/ControlFlow/VC remain deferred. The fixture, expectation, trace row,
production route, full near-miss/corruption matrix, and real frontend/resolver
sidecar now guard the slice, so the active runner count is 105.

Task 155 implementation specification: `mizar-test` owns the exact test-first
source with four unique, unrecovered, source-preceding, no-argument bare-object
mode definitions
`OuterThreeEdgeObjectModeEquality -> MiddleThreeEdgeObjectModeEquality -> InnerThreeEdgeObjectModeEquality -> BaseThreeEdgeObjectModeEquality -> object`,
one reserve `z` for the outer mode, and
`ThreeEdgeLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;`.
The runner must preserve four raw outer-mode result/expected inputs, resolve
both operands to `BindingId(0)` with ordinals 1 and 2, pass all four real Task-
73-compatible expansions to `TermFormulaChecker`, and require one terminal-RHS
builtin-object identity, two `Inferred` variables, and one fact/deferred-free
`Checked` equality. Exact source guards must independently reject every wrong
definition label/radix and corrupt every expansion entry; withheld chain,
terminal, reserve, and formula families remain on the extraction gap. Mode
declaration acceptance/inhabitation, object/set coercion, equality truth/facts,
closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred. The
fixture, expectation, trace row, production route, full near-miss/corruption
matrix, and real frontend/resolver sidecar now guard the slice, so the active
runner count is 106.

Task 156 implementation specification: `mizar-test` owns the exact test-first
source with four unique, unrecovered, source-preceding, no-argument bare-set
mode definitions
`OuterThreeEdgeModeInequality -> MiddleThreeEdgeModeInequality -> InnerThreeEdgeModeInequality -> BaseThreeEdgeModeInequality -> set`,
one reserve `z` for the outer mode, and
`ThreeEdgeLocalModeReservedVariableInequalityPayloadBoundary: z <> z;`. The
runner must preserve four raw outer-mode result/expected inputs, resolve both
operands to `BindingId(0)` with ordinals 1 and 2, pass all four real Task-73-
compatible expansions to `TermFormulaChecker`, and require one terminal-RHS
builtin-set identity, two `Inferred` variables, and one fact/deferred-free pre-
desugaring `Checked` inequality. Exact source guards must independently reject
every wrong definition label/radix and corrupt every expansion entry; withheld
chain, terminal, reserve, and formula families remain on the extraction gap.
Mode declaration acceptance/inhabitation, inequality desugaring, truth/facts,
closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred. The
fixture, expectation, trace row, production route, full near-miss/corruption
matrix, and real frontend/resolver sidecar now guard the slice, so the active
runner count is 107.

Task 157 implementation specification: `mizar-test` owns the exact test-first
source with four unique, unrecovered, source-preceding, no-argument bare-object
mode definitions
`OuterThreeEdgeObjectModeInequality -> MiddleThreeEdgeObjectModeInequality -> InnerThreeEdgeObjectModeInequality -> BaseThreeEdgeObjectModeInequality -> object`,
one reserve `z` for the outer mode, and
`ThreeEdgeLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;`.
The runner must preserve four raw outer-mode result/expected inputs, resolve
both operands to `BindingId(0)` with ordinals 1 and 2, pass all four real Task-
73-compatible expansions to `TermFormulaChecker`, and require one terminal-RHS
builtin-object identity, two `Inferred` variables, and one fact/deferred-free
pre-desugaring `Checked` inequality. Exact source guards must independently
reject every wrong definition label/radix and corrupt every expansion entry;
withheld chain, terminal, reserve, and formula families remain on the extraction
gap. Mode declaration acceptance/inhabitation, object/set coercion, inequality
desugaring, truth/facts, closure/order, theorem/proof, and Core/ControlFlow/VC
remain deferred. The fixture, expectation, trace row, production route, full
near-miss/corruption matrix, and real frontend/resolver sidecar now guard the
contract, so the active runner count is 108.

Task 158 implementation specification: `mizar-test` owns the exact active
source with four unique, unrecovered, source-preceding, no-argument bare-set
mode definitions
`OuterThreeEdgeModeMembership -> MiddleThreeEdgeModeMembership -> InnerThreeEdgeModeMembership -> BaseThreeEdgeModeMembership -> set`,
ordered reserves `x` for the outer mode and `y` for explicit `set`, and
`ThreeEdgeLocalModeReservedVariableMembershipPayloadBoundary: x in y;`. The
runner must preserve the raw outer-mode left result and independent explicit-
set right result/sole expected input, keep the left expected input absent,
resolve to `BindingId(0/1)` at ordinals 2/3, pass all four real task-73
expansions to `TermFormulaChecker`, and require one terminal-RHS builtin-set
identity, two `Inferred` variables, one fact/deferred-free `Checked` membership,
and exactly one right-owned expected-type constraint. Exact source guards must
independently reject every wrong definition label/radix and corrupt every
expansion entry; withheld chain, terminal, reserve, formula, recovery, context,
parameter, argument, cycle, depth, and extra-item families remain on the
extraction gap. Mode declaration acceptance/inhabitation, membership truth/
facts, closure/order, theorem/proof, Core/ControlFlow/VC, object-terminal
behavior, and broader semantics remain deferred. The fixture, expectation,
trace row, production route, full near-miss/corruption matrix, and real
frontend/resolver sidecar now guard the active contract, so the active runner
count is 109.
Chapter 3 receives no new coverage credit.

Task 159 current-state override: `mizar-test` composes Task 123's
real one-item/two-binding/shared-range reserve producer with Tasks 120/125's
right-only expected-set membership consumer for exactly
`reserve x, y for set; theorem DistinctReservedVariableMembershipPayloadBoundary: x in y;`.
The runner must preserve `BindingId(0/1)` at source-order ordinals 2/3 and one
written builtin-set range across both bindings plus the left result, right
result, and sole right expected input, with no left expected input. Those three
raw roles must intern to one shared-source-anchored builtin-set identity before
two `Inferred` variables and one fact/deferred-free `Checked` membership with
exactly one right-owned expected-type constraint. Exact source guards, the full
near-miss/corruption matrix, and a real frontend/resolver sidecar now protect
the active route;
separate reserve declarations, non-set types, broader multi-name reserve or
formula shapes, membership truth/facts, closure/order, theorem/proof, and
Core/ControlFlow/VC remain deferred. The active runner count is 110 and Chapter
3 receives no new coverage credit.

Task 160 current-state override: `mizar-test` composes Task 123's real one-
item/two-binding/shared-range reserve producer with Task 121's pre-desugaring
inequality consumer for exactly `reserve x, y for set; theorem
DistinctReservedVariableInequalityPayloadBoundary: x <> y;`. The runner must
preserve `BindingId(0/1)` at source-order ordinals 2/3 and one written builtin-
set range across both bindings and all four left/right result/expected roles.
Those roles must intern to one shared-source-anchored builtin-set identity
before two `Inferred` variables and one fact/deferred-free `Checked` inequality
with two ordered operand-owned constraints. Exact source/route guards, the full
near-miss/corruption matrix, and a real frontend/resolver sidecar now protect
the active route;
separate declarations, non-set types, broader reserve/formula shapes,
desugaring/truth/facts, closure/order, theorem/proof, and Core/ControlFlow/VC
remain deferred. The active runner now contains 111 cases. Chapter 3 receives
no new coverage credit.

Task 161 current-state override: `mizar-test` composes Task 124's real two-
item/two-binding/distinct-written-range reserve producer with Task 160's
pre-desugaring inequality consumer for exactly `reserve x for set; reserve y
for set; theorem MultipleReserveDeclarationInequalityPayloadBoundary: x <> y;`.
The runner must preserve `BindingId(0/1)` at ordinals 2/3, the first reserve
range across left result/expected roles, and the second range across right
roles. All four roles must intern to one canonical builtin-set identity
anchored at the earlier `x` range before two `Inferred` variables and one fact/
deferred-free `Checked` inequality with two ordered constraints. Exact source/
route guards, full near-miss/corruption coverage, and a real frontend/resolver
sidecar now protect the active route. Shared ranges, non-set types, broader shapes,
desugaring/truth/facts, closure/order, theorem/proof, and Core/ControlFlow/VC
remain deferred. The active runner now contains 112 cases. Chapter 3 receives
no new coverage credit.

Task 162 current-state override: `mizar-test` composes Task 124's real
two-item/two-binding/distinct-written-range reserve producer with Tasks 120/159's
right-only expected-set membership consumer for exactly `reserve x for set;
reserve y for set; theorem MultipleReserveDeclarationMembershipPayloadBoundary:
x in y;`. The runner must preserve `BindingId(0/1)` at ordinals 2/3, the first
reserve range on the left result, and the second range on the right result plus
sole right expected input, with no left expected input. All three roles must
intern to one canonical builtin-set identity anchored at the earlier `x` range
before two `Inferred` variables and one fact/deferred-free `Checked` membership
with exactly one right-owned constraint. Exact source/route guards, full near-
miss/corruption coverage, and a real frontend/resolver sidecar now protect the
active route. Shared ranges, non-set types, broader shapes, membership truth/
facts, closure/order, theorem/proof, and Core/ControlFlow/VC remain deferred.
The active runner now contains 113 cases. Chapter 3 receives no new coverage
credit.

Task 163 current-state override: `mizar-test` composes the real four-expansion
object-terminal producer already exercised by Tasks 73/151/155/157 with Task
144's real object-left/set-right membership consumer for only the new test-
first source. The runner must preserve `BindingId(0/1)` at ordinals 2/3, the raw
outer-mode left result, the independent explicit-set right result and sole
right expected input, no left expected input, and distinct terminal-object-RHS
and explicit-set identities before two `Inferred` variables and one fact/
deferred-free `Checked` membership with exactly one right-owned constraint.
Exact route, full near-miss/corruption coverage, and a real frontend/resolver
sidecar protect active runner 114. Truth/facts, object/set coercion, closure/order, theorem/
proof/Core/ControlFlow/VC, other depths, and broader shapes remain deferred.
The fixture/expectation and six trace backlinks are active.

Task 164 current-state override: `mizar-test` composes the real five-
expansion set-terminal producer already exercised by Tasks 74/152 with Task
158's real set-left/set-right membership consumer for only the new test-first
source. The runner must preserve `BindingId(0/1)` at ordinals 2/3, raw
outermost-mode left provenance, independent explicit-set right result and sole
right expected provenance, no left expected input, and one terminal-set-RHS
identity before two `Inferred` variables and one fact/deferred-free `Checked`
membership with exactly one right-owned constraint. The route must require the
exact five-definition chain and exact reserves/formula; full link, order,
recovery, context, parameterization, terminal, depth, reserve, formula, symbol,
matched-output, expansion-corruption, and real frontend/resolver guards are
required. Truth/facts, closure/order, theorem/proof/Core/ControlFlow/VC,
object-terminal behavior, other depths, and broader shapes remain deferred.
The fixture/expectation, six trace backlinks, production route, full near-miss/
corruption coverage, and real sidecar now protect active runner 115.

Task 165 current-state override: `mizar-test` composes the real five-expansion
object-terminal producer already exercised by Tasks 74/153 with Task 163's real
object-left/set-right membership consumer for only the new test-first source.
The runner must preserve `BindingId(0/1)` at ordinals 2/3, raw outermost-mode
left provenance, independent explicit-set right result and sole right expected
provenance, no left expected input, and distinct terminal-object-RHS and
explicit-set identities before two `Inferred` variables and one fact/deferred-
free `Checked` membership with exactly one right-owned constraint. The route
must require the exact five-definition chain and exact reserves/formula; full
link, order, recovery, context, parameterization, terminal, depth, reserve,
formula, symbol, matched-output, expansion-corruption, and real frontend/
resolver guards are required. Truth/facts, object/set coercion, closure/order,
theorem/proof/Core/ControlFlow/VC, other depths, and broader shapes remain
deferred. The fixture/expectation, six trace backlinks, production route, full
guards, and real sidecar now protect active runner 116.

Task 166 current-state override: `mizar-test` composes the real five-
expansion set-terminal producer already exercised by Tasks 74/152 with Task
154's equality consumer for only the new test-first source. The runner must
preserve `BindingId(0)` at ordinals 1/2, all four raw outermost-mode result/
expected inputs, all five expansions, and one terminal-set-RHS identity before
two `Inferred` variables, one fact/deferred-free `Checked` equality, and two
ordered operand-owned expected constraints. The
route must require the exact five-definition chain, one exact reserve, and exact
formula; full link/order/recovery/context/parameterization/terminal/depth/
reserve/formula/symbol/matched-output/expansion-corruption guards plus a real
frontend/resolver sidecar are required. Declaration acceptance/inhabitation,
truth/facts, closure/order, theorem/proof/Core/ControlFlow/VC, object-terminal
behavior, other depths, and broader shapes remain deferred. The fixture,
expectation, six trace backlinks, production route, full guard matrix, and real
frontend/resolver sidecar now protect active runner 117.

Task 167 current-state override: `mizar-test` composes the real five-
expansion object-terminal producer already exercised by Tasks 74/153 with Task
155's equality consumer for only the new test-first source. The runner must
preserve `BindingId(0)` at ordinals 1/2, all four raw outermost-mode result/
expected inputs, all five expansions, and one terminal-object-RHS identity
before two `Inferred` variables, one fact/deferred-free `Checked` equality, and
two ordered operand-owned expected constraints, without object/set coercion.
The route must require the exact five-definition chain, one exact reserve, and
exact formula; full link/order/recovery/context/parameterization/terminal/
depth/reserve/formula/symbol/matched-output/expansion-corruption guards plus a
real frontend/resolver sidecar are required. Declaration acceptance/
inhabitation, truth/facts, closure/order, theorem/proof/Core/ControlFlow/VC,
set-terminal behavior, other depths, and broader shapes remain deferred. The
fixture, expectation, six trace backlinks, production route, full guard
matrix, and real frontend/resolver sidecar now protect active runner 118.

Task 168 current-state override: `mizar-test` composes the real
five-expansion set-terminal producer already exercised by Tasks 74/152 with
Task 156's pre-desugaring inequality consumer for only the new test-first
source. The runner must preserve `BindingId(0)` at ordinals 1/2, all four raw
outermost-mode result/expected inputs, all five expansions, and one terminal-
set-RHS identity before two `Inferred` variables, one fact/deferred-free
pre-desugaring `Checked` inequality, and two ordered operand-owned expected
constraints. The route must require the exact five-definition chain, one exact
reserve, and exact formula; full link/order/recovery/context/parameterization/
terminal/depth/reserve/formula/symbol/matched-output/expansion-corruption guards
plus a real frontend/resolver sidecar are required. Declaration acceptance/
inhabitation, inequality desugaring/truth/facts, closure/order, theorem/proof/
Core/ControlFlow/VC, object-terminal behavior, other depths, and broader shapes
remain deferred. The fixture, expectation, six trace backlinks, production
route, full guard matrix, and real frontend/resolver sidecar now protect active
runner 119.

Task 169 current-state override: `mizar-test` composes the real
five-expansion object-terminal producer already exercised by Tasks 74/153 with
Task 157's pre-desugaring inequality consumer for only the new test-first
source. The runner must preserve `BindingId(0)` at ordinals 1/2, all four raw
outermost-mode result/expected inputs, all five expansions, and one terminal-
object-RHS identity before two `Inferred` variables, one fact/deferred-free
pre-desugaring `Checked` inequality, and two ordered operand-owned expected
constraints without object/set coercion. The route must require the exact
five-definition chain, one exact reserve, and exact formula; full link/order/
recovery/context/parameterization/terminal/depth/reserve/formula/symbol/
matched-output/expansion-corruption guards plus a real frontend/resolver
sidecar are required. Declaration acceptance/inhabitation, inequality
desugaring/truth/facts, closure/order, theorem/proof/Core/ControlFlow/VC, set-
terminal behavior, other depths, and broader shapes remain deferred. The
fixture, expectation, six trace backlinks, production route, full guard matrix,
and real frontend/resolver sidecar now protect active runner 120.

Task 172 current-state override: `mizar-test` composes task 74's real seven-expansion
set-terminal long-chain producer with task 166's equality consumer for only the
new test-first source. The route must retain four raw `ChainMode6` result/
expected inputs, resolve both identifiers to `BindingId(0)` at ordinals 1/2,
consume all seven real AST-derived expansions, and normalize every role to one
terminal-`BaseMode`-RHS builtin-set identity before two `Inferred` variables,
one fact/deferred-free `Checked` equality, and two ordered operand-owned
expected constraints. It must require the exact seven definitions, reserve,
and formula, with full link/order/recovery/context/parameterization/terminal/
depth/reserve/formula/symbol/matched-output/expansion-corruption guards plus a
real frontend/resolver sidecar. Declaration acceptance/inhabitation, truth/
facts, closure/order, theorem/proof/Core/ControlFlow/VC, imported/attributed/
argument-bearing or other chain shapes, and general unbounded semantics remain
deferred. The fixture, expectation, six trace backlinks, production route,
full guard matrix, and real frontend/resolver sidecar now protect active runner
121.

Task 173 current-state override: the harness reuses Task 172's exact seven-expansion
source route with task 168's pre-desugaring inequality consumer for exact
`LongLocalModeReservedVariableInequalityPayloadBoundary: z <> z;`. It must
preserve four raw `ChainMode6` roles, ordinal 1/2 `BindingId(0)`, the terminal-
`BaseMode`-RHS identity, two `Inferred` terms, two ordered constraints, and one
fact/deferred-free checked inequality. Full exact/near-miss/corruption and real
frontend/resolver sidecar guards now protect active runner 122; broader
semantics remain deferred.

Task 174 current-state override: the harness composes task 74's real seven-expansion
source producer with task 164's right-only expected-set membership consumer,
reusing Task 172's exact source-shape and structural guard pattern, for only
`LongLocalModeReservedVariableMembershipPayloadBoundary: x in y;`. It must
preserve the raw `ChainMode6` left result and independent explicit-set right
result/sole expected input, ordinal 2/3 `BindingId(0/1)`, one terminal-
`BaseMode`-RHS identity, no left expected input, two `Inferred` terms, one fact/
deferred-free checked membership, and exactly one right-owned constraint. Full
exact/near-miss/corruption and real frontend/resolver sidecar guards now
protect active runner 123.

Task 175 current-state override: the harness composes task 74's real seven-expansion
source producer with task 152's normalized-reflexive type-assertion consumer,
reusing Task 172's exact source-shape and structural guard pattern, for only
`LongLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`. It
must preserve the raw `ChainMode6` subject and independent formula-side
builtin-set asserted provenance, ordinal 1 `BindingId(0)`, one terminal-
`BaseMode`-RHS identity, one `Inferred` term, and one fact/deferred-free checked
type assertion without general reachability. Full exact/near-miss/corruption
and real frontend/resolver sidecar guards now protect active runner 124.

Task 176 current-state override: the harness composes Task 74's real AST-bounded
builtin-object-terminal chain producer with Task 167's object-normalizing
equality consumer, reusing Task 172's seven-definition structural guard pattern,
for only
`LongLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;`. It must
preserve four raw `ChainObjectMode6` result/expected inputs, ordinal 1/2
`BindingId(0)`, seven real expansions, one terminal-`BaseObjectMode`-RHS
identity, two `Inferred` terms, two ordered constraints, and one fact/deferred-
free checked equality without object/set coercion. Full exact/near-miss/
corruption and real frontend/resolver sidecar guards are required; active
runner 125 is now protected by those guards.

Task 177 current-state override: the harness composes Task 74's real AST-bounded
builtin-object-terminal chain producer with Task 169's object-normalizing pre-
desugaring inequality consumer, reusing Task 172's seven-definition structural
guard pattern, for only
`LongLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;`. It must
preserve four raw `ChainObjectMode6` result/expected inputs, ordinal 1/2
`BindingId(0)`, seven real expansions, one terminal-`BaseObjectMode`-RHS
identity, two `Inferred` terms, two ordered constraints, and one fact/deferred-
free pre-desugaring checked inequality without object/set coercion. Full exact/
near-miss/corruption and real frontend/resolver sidecar guards now protect active
runner 126.

Task 178 current-state override: the harness composes Task 74's real AST-bounded
builtin-object-terminal chain producer with Task 165's object-left/set-right
membership consumer, reusing Task 172's seven-definition structural guard
pattern, for only
`LongLocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;`. It
preserves the raw `ChainObjectMode6` left result and independent explicit-
set right result/sole expected input, ordinal 2/3 `BindingId(0/1)`, seven real
expansions, distinct terminal-object-RHS and explicit-set identities, no left
expected input, two `Inferred` terms, one right-owned constraint, and one fact/
deferred-free checked membership without object/set coercion. Full exact/near-
miss/corruption and real frontend/resolver sidecar guards plus production routing
protect active runner 127.

Task 179 current-state override: the harness composes Task 74's real AST-bounded
builtin-object-terminal chain producer with Task 153's object-normalizing type-
assertion consumer, reusing Task 172's seven-definition structural guards and
Task 175's long-chain sibling pattern, for only
`LongLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`.
It preserves the raw `ChainObjectMode6` subject result and independent
formula-side builtin-object asserted input, ordinal 1 `BindingId(0)`, seven real
expansions, one terminal-object-RHS identity, one `Inferred` term, and one fact/
deferred-free normalized-reflexive checked type assertion without general
reachability or object/set coercion. Full exact/near-miss/corruption and real
frontend/resolver sidecar guards plus production routing protect active runner
128.

Task 180 current-state override: the harness promotes only
`theorem SourceDerivedContradictionConstantBoundary: contradiction;`. It adds a
standalone exact leaf extractor by reusing the existing contradiction-kind
mapping and standalone theorem-shape validation pattern, then passes the exact
leaf site/range in module-root context to the existing
`FormulaKind::Contradiction` checker consumer without a deferred reason.
Expected output is one `Checked` formula with no terms, asserted type, expected
constraints, candidates, facts, deferred reasons, or diagnostics. Exact-source, near-miss,
corruption, and real parser/resolver sidecar tests must keep thesis, connective,
quantified, recovered, wrong-label, duplicate, and extra-item sources on their
existing paths. The runner must not claim truth/fact publication, theorem
acceptance, proof-goal closure, `formula_statement`, proof, CoreIr,
ControlFlowIr, or VC coverage.
These guards and the sidecar protect active runner 129.

Task 181 current-state repair keeps the active corpus and runner count at 129
while narrowing the generic imported attributed-reserve source gate to the
five already traced task-84/85/116/171 fixtures. Source-shaped unit tests
distinguish the credited ordered `reserve x for set; reserve y for non empty
set;` mixed source from missing/wrong/duplicate imports, duplicate/mixed
attributes, arguments, wrong polarity/head, broader binding/item/order shapes,
definitions, and recovery nodes that stay on the extraction gap. Existing
positive `TypeCaseAttr set`, positive/negative `empty set`, negative `empty
object`, and exact mixed sources continue to reach the same checker
evidence-query diagnostic. No `.miz`, expectation, traceability, runner-count,
or semantic-coverage change belongs to this repair.

Task 182 current-state override adds one active pass source for the first
formula-side local-mode asserted head. The exact source owns one `definition`
block containing `mode LocalModeAssertedHeadDef: LocalModeAssertedHead is set;`,
one reserve for that mode, and
`LocalModeAssertedHeadPayloadBoundary: x is LocalModeAssertedHead;`. The runner
preserves independent reserve-subject and formula-side asserted raw mode
provenance, consumes the one real expansion, resolves ordinal 1 to `BindingId(0)`,
interns three known type entries to one terminal-RHS builtin-set identity, and
requires one `Inferred` variable plus one fact/deferred-free normalized-reflexive
`Checked` type assertion. Source-shaped exact/near-miss, matched-output
corruption, production-route, and real frontend/resolver-sidecar tests reject
collapsed provenance and broader asserted-head/definition shapes. The
new case raises the active runner from 129 to 130 without changing existing
expectations. It does not credit declaration acceptance/inhabitation,
widening/`qua`, truth/facts, theorem/proof/CoreIr/ControlFlowIr/VC, chain or
object-terminal asserted heads, or general semantics.

Task 183 current-state override adds the direct object-terminal sibling.
The exact source owns one definition block containing `mode
LocalObjectModeAssertedHeadDef: LocalObjectModeAssertedHead is object;`, one
reserve for that mode, and `LocalObjectModeAssertedHeadPayloadBoundary: x is
LocalObjectModeAssertedHead;`. The runner preserves independent raw
reserve-subject and formula-side asserted mode provenance, consumes one real
object-terminal expansion, resolves ordinal 1 to `BindingId(0)`, interns three
known type entries to one terminal-RHS builtin-object identity, and requires one
`Inferred` variable plus one fact/deferred-free normalized-reflexive `Checked`
type assertion without general reachability or object/set coercion. Source-
shaped exact/near-miss, corruption, production-route, and real frontend/
resolver-sidecar tests reject set terminals, builtin/other asserted heads,
chains, attributes/arguments, recovery, extra items, and collapsed provenance.
The new case raises the active runner from 130 to 131 without changing existing
expectations. It does not credit declaration acceptance/inhabitation, truth/
facts, theorem/proof/CoreIr/ControlFlowIr/VC, other asserted-head families, or
general semantics.

Task 184 current-state override adds one active pass source for the exact
one-edge set-terminal same-outer-mode asserted-head chain. The source contains
two ordered definition blocks with `mode BaseModeAssertedHeadDef:
BaseModeAssertedHead is set;` and `mode ChainModeAssertedHeadDef:
ChainModeAssertedHead is BaseModeAssertedHead;`, one reserve for the outer mode,
and `ChainedLocalModeAssertedHeadPayloadBoundary: x is
ChainModeAssertedHead;`. The runner retains independent reserve-subject and
formula-side asserted raw outer-mode provenance, consumes both real expansions,
resolves ordinal 1 to `BindingId(0)`, interns three known type entries to one
terminal-base-definition-RHS builtin-set identity, and requires one `Inferred`
variable plus one fact/deferred-free normalized-reflexive `Checked` type
assertion without general reachability. Source-shaped exact/near-miss,
corruption, production-route, and real frontend/resolver-sidecar tests
reject wrong links/terminals/order/depth, builtin/base/other asserted heads,
attributes/arguments, recovery, extra items, and collapsed provenance. The new
case raises the active runner from 131 to 132 without changing
existing expectations. It does not credit declaration acceptance/inhabitation,
widening/`qua`, truth/facts, closure/order, theorem/proof/CoreIr/ControlFlowIr/
VC, object/deeper/other asserted-head chains, or general chain semantics.

Task 185 current-state override adds the exact one-edge object-terminal same-
outer-mode asserted-head sibling. The source contains two ordered definition
blocks with `mode BaseObjectModeAssertedHeadDef: BaseObjectModeAssertedHead is
object;` and `mode ChainObjectModeAssertedHeadDef:
ChainObjectModeAssertedHead is BaseObjectModeAssertedHead;`, one reserve for the
outer mode, and `ChainedLocalObjectModeAssertedHeadPayloadBoundary: x is
ChainObjectModeAssertedHead;`. The runner must retain independent reserve-
subject and formula-side asserted raw outer-mode provenance with distinct
ranges, consume both real expansions, resolve ordinal 1 to `BindingId(0)`,
intern three known type entries to one terminal-base-definition-RHS builtin-
object identity, and require one `Inferred` variable plus one fact/deferred-free
normalized-reflexive `Checked` type assertion without general reachability,
widening, `qua`, or object/set coercion. Exact/near-miss/corruption, production-
route, and real frontend/resolver-sidecar tests must reject wrong links/
terminals/order/depth, builtin/base/other asserted heads, attributes/arguments,
imported provenance, recovery, extra items, collapsed provenance, and builtin-
set corruption. Five shared trace backlinks plus one dedicated row protect
active runner 133 without changing existing expectations. Imported/declaration/
attribute, broader term/formula/child-graph,
truth/fact, theorem/proof/CoreIr/ControlFlowIr/VC, deeper/other asserted-head,
and general-chain coverage remain deferred. No module layout update is required.

Task 186 current-state override adds the exact two-edge set-terminal same-outer-mode
asserted-head sibling. The source has three ordered definitions:
`BaseTwoEdgeModeAssertedHeadDef: BaseTwoEdgeModeAssertedHead is set`,
`MiddleTwoEdgeModeAssertedHeadDef: MiddleTwoEdgeModeAssertedHead is
BaseTwoEdgeModeAssertedHead`, and `OuterTwoEdgeModeAssertedHeadDef:
OuterTwoEdgeModeAssertedHead is MiddleTwoEdgeModeAssertedHead`; it reserves `x`
for the outer mode and checks `TwoEdgeLocalModeAssertedHeadPayloadBoundary: x
is OuterTwoEdgeModeAssertedHead`. The runner retains distinct reserve and
asserted sites/ranges for the same outer symbol, consumes three real expansions,
resolves ordinal 1 to `BindingId(0)`, normalizes three known entries to one
terminal-base-RHS builtin-set identity, and requires one `Inferred` variable and
one fact/deferred-free normalized-reflexive `Checked` assertion without
reachability, widening, or `qua`. Exact/near-miss/corruption and real frontend/
resolver-sidecar guards include imported/ambiguous provenance and all structural
chain failures. Five shared backlinks plus one dedicated trace row raise active
runner 133 to 134 without rebaselining. Object/deeper/imported semantics,
declarations/attributes, broader terms/formulas/child graphs, truth/facts,
proof/CoreIr/ControlFlowIr/VC, and general chain semantics remain deferred. No
module layout update is required.

Task 187 current-state override adds the exact two-edge object-terminal same-outer-
mode asserted-head sibling. The source has ordered definitions
`mode BaseTwoEdgeObjectModeAssertedHeadDef: BaseTwoEdgeObjectModeAssertedHead is
object;`, `mode MiddleTwoEdgeObjectModeAssertedHeadDef:
MiddleTwoEdgeObjectModeAssertedHead is BaseTwoEdgeObjectModeAssertedHead;`, and
`mode OuterTwoEdgeObjectModeAssertedHeadDef: OuterTwoEdgeObjectModeAssertedHead
is MiddleTwoEdgeObjectModeAssertedHead;`, reserves `x` for the outer mode, and checks
`TwoEdgeLocalObjectModeAssertedHeadPayloadBoundary: x is
OuterTwoEdgeObjectModeAssertedHead`. The runner retains distinct raw
subject/asserted sites and ranges for the same local outer symbol, consumes
three real expansions, resolves ordinal 1 to `BindingId(0)`, normalizes three
known entries to one base-definition-RHS builtin-object identity, and requires
one `Inferred` variable plus one fact/deferred-free normalized-reflexive
`Checked` assertion with no expected constraints, reachability, widening,
`qua`, or object/set coercion. Exact/near-miss/corruption and real frontend/
resolver-sidecar guards include imported Base/Middle/Outer, imported/ambiguous
asserted provenance, wrong labels, attributed/argument-bearing formula-side
asserted heads, collapsed provenance, and `BuiltinSet` output corruption.
Five shared backlinks plus one dedicated trace row protect active runner 135
without rebaselining. Positive imported semantics, declarations/attributes,
broader terms/formulas/child graphs, truth/facts, implicit closure/order,
theorem acceptance, proof/CoreIr/ControlFlowIr/VC, and general chain semantics
remain deferred. Step 5 stays active; Steps 6/7 stay deferred. No module layout
update is required.

Task 188 adds the exact active pass source
`reserve x for object; theorem ReservedObjectVariableEqualityPayloadBoundary:
x = x;`. It is a `test_gap` plus narrow `source_drift` and `design_drift`, with
no blocking `spec_gap`: Chapters 03, 04, 13, 14, and 16 directly provide the
builtin object, reserve/free-theorem-variable, identifier-term, equality, and
theorem syntax intent. The runner must reuse the real object reserve producer
and equality consumer, preserve ordinal 1/2 `BindingId(0)` lookups and four
distinct result/expected role sites on one written range, and require one
canonical builtin-object identity, two inferred variables, two ordered
constraints, and one fact/deferred-free checked equality. Five shared trace
backlinks plus one dedicated row protect active runner 136 without changing
existing expectations. General object
equality, coercion, truth/facts, acceptance, proof, and downstream payloads
remain deferred.

Task 189 adds the exact active pass source
`reserve x for object; theorem
ReservedObjectVariableTypeAssertionPayloadBoundary: x is object;`. It is a
`test_gap` plus narrow `source_drift` and `design_drift`, with no blocking
`spec_gap`: Chapters 03, 04, 13, 14, and 16 directly provide the builtin
object, reserve/free-theorem-variable, identifier-term, type-assertion, and
theorem syntax intent. The runner must reuse the real object reserve producer
and normalized-reflexive assertion consumer, preserve ordinal 1
`BindingId(0)` lookup plus distinct reserve-subject result and formula-side
asserted sites/ranges, and require one canonical builtin-object identity
anchored at the reserve, one inferred variable, three known type entries, zero
expected constraints, and one fact/deferred-free checked assertion. Five
shared trace backlinks plus one dedicated row protect active runner 137
without changing existing expectations. Reachability/widening/`qua`, object/
set coercion, truth/facts, closure/order, acceptance, proof, and downstream
payloads remain deferred.

Task 190 adds the exact active pass source
`reserve x for object; theorem
ReservedObjectVariableInequalityPayloadBoundary: x <> x;`. It is a `test_gap`
plus narrow `source_drift` and `design_drift`, with no blocking `spec_gap`:
Chapters 03, 04, 13, 14, and 16 directly provide the builtin object,
reserve/free-theorem-variable, identifier-term, inequality, and theorem syntax
intent. The runner must reuse the real object reserve producer and pre-
desugaring inequality consumer, preserve ordinal 1/2 `BindingId(0)` lookups
plus four distinct result/expected role sites on one written range, and
require one canonical builtin-object identity, two inferred variables, six
known type entries, two ordered expected constraints, and one fact/candidate/
diagnostic/deferred-free checked inequality. Five shared trace backlinks plus
one dedicated row protect active runner 138 without changing existing
expectations. Inequality desugaring/equality truth, object/set coercion, facts,
closure/order, acceptance, proof, and downstream payloads remain deferred.

Task 191 adds the exact active pass source
`reserve x, y for object; theorem
DistinctReservedObjectVariableEqualityPayloadBoundary: x = y;`. It is a
`test_gap` plus narrow `source_drift` and `design_drift`, with no blocking
`spec_gap`: Chapters 03, 04, 13, 14, and 16 directly provide the builtin
object, multi-name reserve/free-theorem-variable, identifier-term, equality,
and theorem intent. The runner composes Task 123's real one-item/two-binding
shared-range producer with Task 188's builtin-object equality consumer,
preserves ordinal 2/3 `BindingId(0/1)` lookups plus four distinct result/expected
role sites on one shared written range, and requires one reserve-range-anchored
canonical builtin-object identity, two inferred variables, six known type
entries, two ordered expected constraints, and one fact/candidate/diagnostic/
deferred-free checked equality. Five shared trace backlinks plus one dedicated
row protects active runner 139 without changing existing
expectations. Equality truth, object/set coercion, facts, closure/order,
acceptance, proof, and downstream payloads remain deferred.

Task 192 adds the exact active pass source
`reserve x, y for object; theorem
DistinctReservedObjectVariableInequalityPayloadBoundary: x <> y;`. It is a
`test_gap` plus narrow `source_drift` and `design_drift`, with no blocking
`spec_gap`: Chapters 03, 04, 13, 14, and 16 directly provide the builtin
object, multi-name reserve/free-theorem-variable, identifier-term, inequality,
and theorem intent. The runner composes tasks 123/191's real one-item/two-
binding shared-range producer with tasks 121/160/190's real pre-desugaring
inequality consumer, preserve ordinal 2/3 `BindingId(0/1)` lookups plus four
distinct result/expected role sites on one shared written range, and require
one reserve-range-anchored canonical builtin-object identity, two inferred
variables, six known type entries, two ordered expected constraints, and one
fact/candidate/diagnostic/deferred-free checked inequality. Five shared trace
backlinks plus one dedicated row protect active runner 140 without changing
existing expectations. Inequality desugaring/equality truth,
object/set coercion, facts, closure/order, acceptance, proof, and downstream
payloads remain deferred.

Task 193 adds the exact active pass source `reserve x for object; reserve y
for object; theorem MultipleObjectReserveDeclarationEqualityPayloadBoundary:
x = y;`. It is a `test_gap` plus narrow `source_drift` and `design_drift`, with
no blocking `spec_gap`: Chapters 03, 04, 13, 14, and 16 directly provide the
builtin object, separate reserve/free-theorem-variable, identifier-term,
equality, and theorem intent. The runner composes Task 124's real two-item/
two-binding/distinct-written-range producer with tasks 188/191's builtin-
object equality consumer, preserves ordinal 2/3 `BindingId(0/1)` lookups plus
four distinct result/expected role sites on the two binding-owned written
ranges, and requires one earlier-`x`-range-anchored canonical builtin-object
identity, two inferred variables, six known type entries, two ordered expected
constraints, and one fact/candidate/diagnostic/deferred-free checked equality.
Five shared trace backlinks plus one dedicated row protect active runner 141
within 356 cases and 320 requirements without changing existing expectations.
Equality truth, object/set coercion, facts, closure/order, acceptance, proof,
shared-range shapes, and downstream payloads remain deferred.

Task 194 adds the exact active pass source `reserve x for object; reserve y for
object; theorem MultipleObjectReserveDeclarationInequalityPayloadBoundary: x <>
y;`. It is a `test_gap` plus narrow `source_drift` and `design_drift`, with no
blocking `spec_gap`: Chapters 03, 04, 13, 14, and 16 directly provide the
builtin object, separate reserve/free-theorem-variable, identifier-term,
inequality, and theorem intent. The runner composes Task 193's real ordered two-
item/two-binding/distinct-written-object-range producer with tasks 190/192's
pre-desugaring builtin-object inequality consumer, preserves ordinal 2/3
`BindingId(0/1)` lookups plus four distinct raw result/expected roles on the
two binding-owned written ranges, and requires one earlier-`x`-range-anchored
canonical builtin-object identity, two inferred variables, six known type
entries, two ordered expected constraints, and one fact/candidate/diagnostic/
deferred-free checked inequality. Five shared trace backlinks plus one
dedicated row protect active runner 142 within 357 cases and 321 requirements
without changing existing expectations. Inequality desugaring/equality truth,
object/set coercion, facts, closure/order, acceptance, proof, shared-range
shapes, and downstream payloads remain deferred.

Task 195 adds the exact active pass source with four ordered local mode
definitions `Outer -> Middle -> Inner -> Base -> set`, `reserve x for
OuterThreeEdgeModeAssertedHead`, and theorem
`ThreeEdgeLocalModeAssertedHeadPayloadBoundary: x is
OuterThreeEdgeModeAssertedHead;`. It is a `test_gap` plus narrow `source_drift`
and `design_drift`, with no blocking `spec_gap`: Chapters 03, 04, 07, 13, 14,
and 16 directly provide the local mode expansion, reserve/free variable,
identifier term, asserted type, and theorem intent. The runner composes Task
73's real four-expansion producer with Task 186's same-outer formula-side
asserted-head consumer, preserves distinct raw subject/asserted sites and
ranges, resolves ordinal 1 to `BindingId(0)`, normalizes three known type
entries to one base-definition-RHS-anchored builtin-set identity, and requires
one inferred variable plus one fact/candidate/diagnostic/deferred-free checked
type assertion with no expected constraints. Five shared trace backlinks plus
one dedicated row protect active runner 143 within 358 cases and 322
requirements without changing existing expectations. Object-terminal/deeper/
imported/attributed/argument-bearing/other asserted heads, reachability/
widening/`qua`, acceptance, truth/facts, closure/order, broader semantic
payloads, proof, and downstream IR remain deferred.

Task 196 adds the exact active pass source with four ordered local mode
definitions `Outer -> Middle -> Inner -> Base -> object`, `reserve x for
OuterThreeEdgeObjectModeAssertedHead`, and theorem
`ThreeEdgeLocalObjectModeAssertedHeadPayloadBoundary: x is
OuterThreeEdgeObjectModeAssertedHead;`. It is a `test_gap` plus narrow
`source_drift` and `design_drift`, with no blocking `spec_gap`: Chapters 03,
04, 07, 13, 14, and 16 directly provide the object-terminal local mode
expansion, reserve/free variable, identifier term, asserted type, and theorem
intent. The runner composes Tasks 73/151's real four-expansion object producer
with Task 187's same-outer formula-side asserted-head consumer, preserves
distinct raw subject/asserted sites and ranges, resolves ordinal 1 to
`BindingId(0)`, normalizes three known type entries to one base-definition-RHS-
anchored builtin-object identity, and requires one inferred variable plus one
fact/candidate/diagnostic/deferred-free checked type assertion with no expected
constraints or object/set coercion. Five shared trace backlinks plus one
dedicated row protect active runner 144 within 359 cases and 323 requirements
without changing existing expectations. Deeper/imported/attributed/argument-
bearing/other asserted heads, reachability/widening/`qua`, acceptance, truth/
facts, closure/order, broader semantic payloads, proof, and downstream IR
remain deferred.

Task 197 adds the exact active pass source with five ordered local mode
definitions `TooDeep -> Outer -> Middle -> Inner -> Base -> set`, `reserve x
for TooDeepFourEdgeModeAssertedHead`, and theorem
`FourEdgeLocalModeAssertedHeadPayloadBoundary: x is
TooDeepFourEdgeModeAssertedHead;`. It is a `test_gap` plus narrow `source_drift`
and `design_drift`, with no blocking `spec_gap`: Chapters 03, 04, 07, 13, 14,
and 16 directly provide the local mode expansion, reserve/free variable,
identifier term, asserted type, and theorem intent. The runner composes Tasks
74/152's real five-expansion set producer with Tasks 186/195's same-outermost
formula-side asserted-head consumer, preserves distinct raw subject/asserted
sites and ranges, resolves ordinal 1 to `BindingId(0)`, normalizes three known
type entries to one base-definition-RHS-anchored builtin-set identity, and
requires one inferred variable plus one fact/candidate/diagnostic/deferred-free
checked type assertion with no expected constraints. Five shared trace
backlinks plus one dedicated row protect active runner 145 within 360 cases and
324 requirements without changing existing expectations. Object-terminal/
other-depth/imported/attributed/argument-bearing/other asserted heads,
reachability/widening/`qua`, acceptance, truth/facts, closure/order, broader
semantic payloads, proof, and downstream IR remain deferred.

Task 198 adds the exact active pass source with five ordered local mode
definitions `TooDeep -> Outer -> Middle -> Inner -> Base -> object`, `reserve x
for TooDeepFourEdgeObjectModeAssertedHead`, and theorem
`FourEdgeLocalObjectModeAssertedHeadPayloadBoundary: x is
TooDeepFourEdgeObjectModeAssertedHead;`. It is a `test_gap` plus narrow
`source_drift` and `design_drift`, with no blocking `spec_gap`: Chapters 03,
04, 07, 13, 14, and 16 directly provide the local mode expansion, reserve/free
variable, identifier term, asserted type, and theorem intent. The runner
composes Tasks 74/153's real five-expansion object producer with Tasks 187/196's
same-outermost formula-side asserted-head consumer, preserves distinct raw
subject/asserted sites and ranges, resolves ordinal 1 to `BindingId(0)`,
normalizes three known type entries to one base-definition-RHS-anchored builtin-
object identity, and requires one inferred variable plus one fact/candidate/
diagnostic/deferred-free checked type assertion with no expected constraints or
object/set coercion. Five shared trace backlinks plus one dedicated row protect
active runner 146 within 361 cases and 325 requirements without changing
existing expectations. Set-terminal/other-depth/imported/attributed/argument-
bearing/other asserted heads, reachability/widening/`qua`, acceptance, truth/
facts, closure/order, broader semantic payloads, proof, and downstream IR
remain deferred.

Task 199 adds the exact active pass source with `BaseMode -> set`, six ordered
local links through `ChainMode6 -> ChainMode5`, `reserve x for ChainMode6`, and
theorem `LongLocalModeAssertedHeadPayloadBoundary: x is ChainMode6;`. It is a
`test_gap` plus narrow `source_drift` and `design_drift`, with no blocking
`spec_gap`: Chapters 03, 04, 07, 13, 14, and 16 directly provide the local mode
expansion, reserve/free-variable, identifier term, asserted type, and theorem
intent. The runner composes Tasks 74/175's real seven-expansion set producer
with Tasks 186/195/197's same-symbol formula-side asserted-head consumer,
preserves distinct raw subject/asserted sites and ranges, resolves ordinal 1 to
`BindingId(0)`, normalizes three known type entries to one `BaseModeDef` RHS-
anchored builtin-set identity, and requires one inferred variable plus one
fact/candidate/diagnostic/deferred-free checked type assertion with no expected
constraints. Five shared trace backlinks plus one dedicated row protect active
runner 147 within 362 cases and 326 requirements without changing existing
expectations. Object-terminal/other-depth/imported/attributed/argument-bearing/
other asserted heads, reachability/widening/`qua`, acceptance, truth/facts,
closure/order, broader semantic payloads, proof, and downstream IR remain
deferred.

Task 200 adds the exact active pass source with `BaseObjectMode -> object`, six
ordered local links through `ChainObjectMode6 -> ChainObjectMode5`, `reserve x
for ChainObjectMode6`, and theorem
`LongLocalObjectModeAssertedHeadPayloadBoundary: x is ChainObjectMode6;`. It is
a `test_gap` plus narrow `source_drift` and `design_drift`, with no blocking
`spec_gap`: Chapters 03, 04, 07, 13, 14, and 16 directly provide the local mode
expansion, reserve/free-variable, identifier term, asserted type, and theorem
intent. The runner composes Tasks 74/179's real seven-expansion object producer
with Tasks 187/196/198's same-symbol formula-side asserted-head consumer,
preserves distinct raw subject/asserted sites and ranges, resolves ordinal 1 to
`BindingId(0)`, normalizes three known type entries to one `BaseObjectModeDef`
RHS-anchored builtin-object identity, and requires one inferred variable plus
one fact/candidate/diagnostic/deferred-free checked type assertion with no
expected constraints, general reachability, or object/set coercion. Five shared
trace backlinks plus one dedicated row protect active runner 148 within 363
cases and 327 requirements without changing existing expectations. Set-
terminal/other-depth/imported/attributed/argument-bearing/other asserted heads,
reachability/widening/`qua`, acceptance, truth/facts, closure/order, broader
semantic payloads, proof, and downstream IR remain deferred.

Task 120 current-state override: `mizar-test` owns the exact active membership
pass source
`reserve x for set; theorem ReservedVariableMembershipPayloadBoundary: x in x;`.
The runner shares task 119's independent source-order lookup producer, records
two source-derived result roles plus only the right expected-`set` role, and
requires two `Inferred` variables with one no-fact `Checked` membership.
Production invariant validation and a real frontend/resolver payload test guard
the active sidecar. MT-AUDIT-015's generic term/formula gap now means coverage
beyond task 119/123 equality and task 120 membership; truth/facts, implicit closure,
theorem acceptance, proof, CoreIr, ControlFlowIr, and VC remain deferred.

Task 121 current-state override: `mizar-test` owns the exact active inequality
pass source
`reserve x for set; theorem ReservedVariableInequalityPayloadBoundary: x <> x;`.
The runner uses the checker-owned two-expected-type inequality API with task
119's real reserve binding/use producer, records two linked source-derived
result/expected role pairs, and requires two `Inferred` variables with one
fact-free pre-desugaring `Checked` inequality. Task 107's numeral inequality
bridge remains partial and supplies no expected types. Production invariant
validation, a full near-miss matrix, and a real frontend/resolver payload test
guard the active sidecar. MT-AUDIT-015's generic term/formula gap therefore now
means coverage beyond task 119/123 equality, task 120 membership, and task 121
inequality; desugaring/truth/facts, implicit closure, theorem acceptance, proof,
CoreIr, ControlFlowIr, and VC remain deferred.

Task 122 current-state override: `mizar-test` owns the exact active type-
assertion pass source
`reserve x for set; theorem ReservedVariableTypeAssertionPayloadBoundary: x is set;`.
The runner combines task 119's real reserve lookup/result producer with task
109's formula-side asserted-type AST producer and retains both
pre-normalization inputs with independent source anchors. The checker accepts
their normalized identity as the reflexive admissibility case and records one
`Inferred` variable plus one fact-free `Checked` type assertion. Known
non-identical types remain partial on the external reachability payload gap.
Production validation, the full near-miss matrix, and a real frontend/resolver
payload test guard the active sidecar. MT-AUDIT-015's generic term/formula gap
now means coverage beyond promoted tasks 119-169;
general reachability/widening/`qua`,
attributes, truth/facts, implicit closure, theorem acceptance, proof, CoreIr,
ControlFlowIr, and VC remain deferred.

Task 109 addendum: `mizar-test` supersedes the exact builtin type-assertion
theorem source previously owned by task 102. The active sidecar may execute
`theorem BuiltinTypeAssertionPayloadBoundary: 1 is set;` through parser and
resolver under `type_elaboration`, pass source-derived checker `TermInput`,
`FormulaInput`, and asserted builtin `set` `TypeExpressionInput` payloads, and
then fail closed on missing numeric type payloads plus partial formula checking.
This addendum does not promote broader asserted type payload extraction,
type-assertion semantic checking, recorded facts, theorem acceptance, the
dedicated `formula_statement` runner, CoreIr, ControlFlowIr, VC, or proof
payloads.

Task 113 addendum: `mizar-test` supersedes task 103 for the exact imported
attribute assertion theorem formula checker bridge. The runner may recognize
only the source importing `parser.type_fixtures` and proving
`ImportedAttributeAssertionPayloadBoundary: 1 is empty`, validate imported
`empty` attribute provenance, pass source-derived numeral `TermInput` and
attribute-assertion `FormulaInput` payloads through `type_elaboration`, and
then fail closed on missing numeric type payload, missing formula/attribute
semantic payload, and partial formula checking. This addendum does not promote
imported module AST extraction, attribute-chain semantic payloads, checker
`AttributeInput` payload extraction for theorem formulas, term inference,
attribute admissibility/semantic checking, formula checking, recorded facts,
theorem acceptance, the dedicated `formula_statement` runner, CoreIr,
ControlFlowIr, VC, or proof payloads.

Task 114 addendum: `mizar-test` supersedes task 104 for the exact
attribute-level non-empty imported attribute assertion theorem formula sidecar.
The active `type_elaboration` runner imports `parser.type_fixtures`, validates
the direct `non` surface and documented imported `empty` attribute provenance,
passes source-derived numeral and attribute-assertion formula checker payloads,
and fails closed on missing numeric type payload, missing formula/attribute
semantic payload, and partial formula checking. This addendum does not promote
imported module AST extraction, theorem-formula `AttributeInput` payload
extraction, negated attribute-chain semantic payloads, term inference, negated
attribute admissibility/semantic checking, formula checking, recorded facts,
theorem acceptance, the dedicated `formula_statement` runner, CoreIr,
ControlFlowIr, VC, or proof payloads.

Task 111 addendum: `mizar-test` supersedes the task-105 set-enumeration theorem
formula extraction-gap boundary only for the exact
`SetEnumerationPayloadBoundary: {1, 2} = {1, 2}` source. The active
`type_elaboration` runner now passes real checker payloads for the four
numeral item terms, two set-enumeration terms, and builtin equality formula,
then fails closed on checker diagnostics for missing numeric type payloads,
missing set-enumeration result-type/sethood payloads, and partial formula
checking. This addendum does not promote broader set-enumeration payloads, term
inference, equality/formula checking, theorem acceptance, CoreIr, ControlFlowIr,
VC, or proof payloads.

Task 112 / task 117 addendum: `mizar-test` supersedes the task-99
connective/quantifier theorem formula extraction-gap boundary only for the exact
`FormulaConnectiveQuantifierPayloadBoundary: contradiction implies for x being
set holds not contradiction` source. The active `type_elaboration` runner now
passes source-derived checker `FormulaInput` shells for the implication,
quantified formula, and negation, plus exact `FormulaKind::Contradiction`
payloads for the two source constants, then fails closed on checker diagnostics
for missing formula payloads and missing quantifier payloads. This addendum
does not promote formula constant semantic truth values, child-formula graph
payloads, quantifier binder/context payloads, formula checking, theorem
acceptance, CoreIr, ControlFlowIr, VC, or proof payloads.

Task 88 addendum: `mizar-test` also owns the active runner metadata for the
proof-block/proof-skeleton extraction-gap boundary. The new theorem source
sidecar may execute `theorem ProofSkeletonPayloadBoundary: thesis proof thus
thesis; end;` through parser and resolver under `type_elaboration`, but it must
still fail closed on `type_elaboration.external_dependency.ast_payload_extraction`
until checker-owned proof skeleton payload extraction, local proof context,
formula payload extraction, recorded facts, theorem acceptance, and the
dedicated `formula_statement` runner exist. This addendum does not promote
proof skeleton payloads, formula payloads, local facts, theorem acceptance,
CoreIr, ControlFlowIr, VC, or proof payloads. With task 89, the MT-AUDIT-015
formula/theorem/proof gap wording is narrowed only to the
task-106 builtin equality theorem checker bridge, task-107 builtin inequality theorem checker bridge, task-108 builtin membership theorem checker bridge, task-109 builtin type assertion theorem checker bridge, task-110 imported predicate/functor theorem checker bridge, task-111 set-enumeration theorem checker bridge, task-112/task-117 connective/quantifier formula shell checker bridge, task-113 imported attribute assertion checker bridge, task-114 exact attribute-level non-empty imported attribute assertion checker bridge, task-117 exact formula statement checker bridge, and task-105/task-88/task-89 extraction-gap boundaries; semantic
term/formula/proof payload extraction remains unavailable beyond those
boundaries.

Task 89 addendum: `mizar-test` also owns the active runner metadata for the
statement-level proof-justification extraction-gap boundary. The new theorem
source sidecar may execute a labeled `A: thesis proof ... end;` statement and a
final `thus thesis proof ... end;` conclusion through parser and resolver under
`type_elaboration`, but it must still fail closed on
`type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned statement proof payload extraction, nested proof skeleton
payloads, local proof context, formula payload extraction, label-reference
semantic checking, recorded facts, theorem acceptance, and the dedicated
`formula_statement` runner exist. This addendum does not promote statement
proof payloads, proof skeleton payloads, formula payloads, local facts, theorem
acceptance, CoreIr, ControlFlowIr, VC, or proof payloads. It narrows the
MT-AUDIT-015 formula/theorem/proof gap wording only to the
task-106 builtin equality theorem checker bridge, task-107 builtin inequality theorem checker bridge, task-108 builtin membership theorem checker bridge, task-109 builtin type assertion theorem checker bridge, task-110 imported predicate/functor theorem checker bridge, task-111 set-enumeration theorem checker bridge, task-112/task-117 connective/quantifier formula shell checker bridge, task-113 imported attribute assertion checker bridge, task-114 exact attribute-level non-empty imported attribute assertion checker bridge, task-117 exact formula statement checker bridge, and task-105/task-88/task-89 extraction-gap boundaries; semantic
term/formula/proof payload extraction remains unavailable beyond those
boundaries.

Task 90 addendum: `mizar-test` also owns the active runner metadata for the
predicate/functor definition extraction-gap boundary. The new definition source
sidecar may execute predicate and functor definitions through parser and
resolver under `type_elaboration`, but it must still fail closed on
`type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned definition declaration payload extraction, definition-local
contexts, definiens formula/term payloads, overload payloads, recorded facts,
and the dedicated `formula_statement` runner exist. This addendum does not
promote definition payloads, formula/term body payloads, overload payloads,
facts, CoreIr, ControlFlowIr, VC, or proof payloads. It narrows the
MT-AUDIT-015 non-builtin declaration gap wording only to the task-90
extraction-gap boundary; semantic definition payload extraction remains
unavailable beyond that boundary.

Task 91 addendum: `mizar-test` also owns the active runner metadata for the
attribute definition extraction-gap boundary. The new definition source sidecar
may execute an attribute definition through parser and resolver under
`type_elaboration`, but it must still fail closed on
`type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned attribute definition declaration payload extraction,
definition-local contexts, formula-definiens payloads, attributed-type
evidence, recorded facts, and the dedicated `formula_statement` runner exist.
This addendum does not promote definition payloads, formula body payloads,
evidence, facts, CoreIr, ControlFlowIr, VC, or proof payloads. It narrows the
MT-AUDIT-015 non-builtin declaration gap wording only to the task-91
extraction-gap boundary; semantic attribute definition payload extraction
remains unavailable beyond that boundary.

Task 92 addendum: `mizar-test` also owns the active runner metadata for the
mode/structure definition extraction-gap boundary. The new definition source
sidecar may execute mode and structure definitions through parser and resolver
under `type_elaboration`, but it must still fail closed on
`type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned mode/structure definition declaration payload extraction, mode
expansion, structure base-shape/constructor/selector evidence, definition-local
contexts, recorded facts, and the dedicated `formula_statement` runner exist.
This addendum does not promote definition payloads, mode-expansion payloads,
structure evidence, facts, CoreIr, ControlFlowIr, VC, or proof payloads. It
narrows the MT-AUDIT-015 non-builtin declaration gap wording only to the task-92
extraction-gap boundary; semantic mode/structure definition payload extraction
remains unavailable beyond that boundary.

Task 93 addendum: `mizar-test` also owns the active runner metadata for the
proof-local declaration extraction-gap boundary. The new theorem-proof source
sidecar may execute proof-local `let`, `given`, `consider`, `set`, and
`reconsider` statements through parser and resolver under `type_elaboration`,
but it must still fail closed on
`type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned proof-local declaration payload extraction, local proof contexts,
formula/term payload extraction, RHS term inference, reconsider
coercion/obligation evidence, recorded facts, theorem acceptance, and the
dedicated `formula_statement` runner exist. This addendum does not promote
proof-local declaration payloads, formula/term payloads, facts, CoreIr,
ControlFlowIr, VC, or proof payloads. It narrows the MT-AUDIT-015 non-builtin
declaration gap wording only to the task-93 extraction-gap boundary; semantic
proof-local declaration payload extraction remains unavailable beyond that
boundary.

Task 94 addendum: `mizar-test` also owns the active runner metadata for the
proof-local inline definition extraction-gap boundary. The new theorem-proof
source sidecar may execute proof-local `deffunc` and `defpred` statements
through parser and resolver under `type_elaboration`, but it must still fail
closed on `type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned inline definition formal/body payload extraction, local
abbreviation expansion, term/formula body payloads, guard evidence, recorded
facts, theorem acceptance, and the dedicated `formula_statement` runner exist.
This addendum does not promote inline definition payloads, term/formula body
payloads, facts, theorem acceptance, CoreIr, ControlFlowIr, VC, or proof
payloads. It narrows the MT-AUDIT-015
non-builtin declaration gap wording only to the task-94 extraction-gap
boundary; semantic inline definition payload extraction remains unavailable
beyond that boundary.

Task 95 addendum: `mizar-test` also owns the active runner metadata for the
registration-block extraction-gap boundary. The new source sidecar may execute
a top-level registration block with existential and conditional clusters
through parser and resolver under `type_elaboration`, but it must still fail
closed on `type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned registration-item payload extraction, correctness-condition/
proof-obligation payloads, accepted activation/evidence status,
cluster/reduction semantics, and the dedicated `formula_statement` or
`advanced_semantics` runners exist. This addendum does not promote Chapter 17
semantic rows, registration payloads, activation status, facts, CoreIr,
ControlFlowIr, VC, or proof payloads.

Task 96 addendum: `mizar-test` also owns the active runner metadata for the
redefinition/notation extraction-gap boundary. The new source sidecar may
execute top-level and definition-local synonym/antonym aliases plus attribute,
predicate, and functor redefinition declarations through parser and resolver
under `type_elaboration`, but it must still fail closed on
`type_elaboration.external_dependency.ast_payload_extraction` until
checker-owned redefinition payload extraction, notation alias relation payloads,
target inference, coherence proof-obligation payloads, overload candidate
payloads, and the dedicated `formula_statement` or `advanced_semantics` runners
exist. This addendum does not promote Chapter 11 alias semantic resolution,
Chapter 19 overload/redefinition semantic rows, redefinition payloads, overload
facts, CoreIr, ControlFlowIr, VC, or proof payloads.

The task-15 re-run records no blocking `spec_gap`, accepted
`repo_metadata_conflict`, required language behavior change, or need to change
existing expectation semantics.

## Task Decomposition

### Task 1: Lint-policy guard

Purpose: keep `mizar-test` opted into workspace lints and require documented
lint exceptions.

Completion: committed as `0b24d1a`.

### Task 2: Source/spec gap audit and status sync

Purpose: record the audit baseline, classify drift/gaps, and synchronize module
status.

Completion: this crate plan plus `todo.md` status/follow-up updates.

Forbidden behavior: repairing `doc/spec`, existing `.miz`, or existing
expectations in this audit commit.

### Task 3: Runner modes and CLI completion

Purpose: complete validation-mode CLI behavior over corpus and coverage
metadata, including exit-code fixtures and deterministic output.

Spec refs: `minimal_crate.md`, `harness.md`.

Completion: validation-mode CLI parsing, strict/permissive unknown-root policy,
profile metadata retention/filtering, plan CLI fixtures, expectation-schema
fixtures, and harness documentation sync.

### Task 4: Snapshot API and canonicalization

Purpose: introduce the general snapshot module and canonical record API.

Spec refs: `snapshot.md`.

Completion: general in-memory `SnapshotRecord` API, canonical hash input,
line-ending normalization, local absolute path validation, deterministic
profile metadata ordering, and comparison diffs.

### Task 5: Snapshot update policy and determinism checks

Purpose: implement explicit update flow and repeat-render determinism checks.

Spec refs: `snapshot.md`.

Completion: explicit `VerifyOnly` versus `Update { reason }` baseline helper,
safe `.snap` baseline path validation, create/update round-trips, no-write
verify failures, and repeat-render determinism mismatch reports.

### Task 6: Coverage and pass/fail-mix reporting

Purpose: compute/report trace coverage, stored status drift, and corpus
pass/fail mix from discovery and traceability data.

Spec refs: `traceability.md`, architecture test strategy.

Completion: per-requirement and per-stage coverage reports, mode-aware
coverage/status gates, manifest id-order validation, obsolete-reference
diagnostics, link-validator regression fixtures, and deterministic CLI
pass/fail mix output.

### Task 7: Stage-prerequisite validation

Purpose: prevent higher-stage coverage credit before prerequisites are covered,
declared built-in, or accepted through `depends_on`.

Spec refs: `staged_model.md`, `traceability.md`.

Completion: manifest `depends_on` validation, `built_in` declarations,
stage-mismatch diagnostics, unsatisfied-prerequisite diagnostics, and
coverage-credit withholding before mode-aware completeness checks.

### Task 8: Fail/soundness contract support

Purpose: implement fail/soundness required-domain bookkeeping, typed failure
identity checks, false-arithmetic coverage, and weakening/deletion regression
rules.

Spec refs: `fail_soundness.md`.

Completion: certificate/kernel `rejection_reason` validation, recognized
`soundness.*` required-case shape/profile/phase validation, mode-aware
missing-case diagnostics for fail/soundness manifests, false-arithmetic stable
key gate, and regression fixtures. Real proof/certificate/kernel execution is
not fabricated before the owning consumer runners exist.

### Task 9: Corpus size and review-rule validation

Purpose: enforce corpus naming, size, class placement, generation metadata,
corpus-policy profile constraints, stress exclusion, and fuzz provenance rules.

Spec refs: `miz_corpus.md`.

Completion: `[origin]` provenance parsing/retention, generated/fuzz/property
placement gates, stress root discovery, profile constraints, fuzz-category
preservation, upper-bound `.miz` size diagnostics, naming diagnostics, and
metadata fixtures for clean and violating corpora.

### Task 10: Consumer-runner support

Purpose: keep discovery, expectations, stages, snapshots, active/planned gates,
and reporting synchronized with each consumer runner as it lands.

Spec refs: `harness.md`.

Current increment: `parse-only`, `declaration-symbol`, the task-12
`type-elaboration` external-gap runner, the task-16 source-derived builtin
type-expression normalization slice, the task-17 builtin `ResolvedTypedAst`
handoff, the task-18 reserve declaration semantic bridge, and the task-19
`ResolvedTypedAstSummary::from_ast` readiness check plus next builtin
declaration inventory, the task-20 binder-only `CoreContext` readiness check,
plus the post-task-20 resolver R-G007 parser-backed
same-signature/different-return functor conflict active declaration-symbol
seed and exact SymbolEnv-derived declaration-symbol pass payload assertions
for represented kind, visibility, and export status, plus checker task 50's
same-module attributed reserve evidence-query active fail slice, checker task
55's bare local-mode expansion pass slice, and checker task 56's one-edge
same-module local-mode expansion chain pass/gap slice, checker task 57's
same-module local-mode structure-RHS evidence-query active fail slice, and
checker task 58's same-module local-mode attributed-builtin-RHS evidence-query
active fail slice, and checker task 59's same-module attributed local-mode
reserve evidence-query active fail slice, and checker task 60's same-module
attributed local-mode structure-RHS evidence-query active fail slice, and
checker task 61's same-module attributed local-mode attributed-builtin-RHS
evidence-query active fail slice, checker task 62's same-module
local-mode structure-RHS chain evidence-query active fail slice, and checker
task 63's same-module local-mode attributed-RHS chain evidence-query active
fail slice, checker task 64's same-module attributed local-mode bare-builtin
chain evidence-query active fail slice, checker task 65's same-module
attributed local-mode structure-RHS chain evidence-query active fail slice,
and checker task 66's same-module attributed local-mode attributed-builtin-RHS
chain evidence-query active fail slice, checker task 67's
structure-qualified attribute extraction-gap active boundary slice, and checker
task 68's argument-bearing mode reserve extraction-gap active boundary slice,
and checker task 69's argument-bearing structure reserve extraction-gap active
boundary slice, and checker task 70's bracket-form local mode reserve
extraction-gap active boundary slice, checker task 71's bracket-form local
structure reserve extraction-gap active boundary slice, and checker task 72's
two-edge bare local-mode chain active pass slice, and checker task 73's
three-edge bare local-mode chain active pass slice, and checker task 74's
structural bare local-mode chain active pass slice, checker task 75's
lower-stage forward local-mode active-range boundary, checker task 76's
lower-stage forward local-structure active-range boundary, and checker task
77's lower-stage forward local-attribute active-range boundary, are
prepared/implemented and verified as active runner commands. Checker task 48's
syntax-free reserve source declaration producer seam is prepared/implemented
as a checker-owned seam consumed by the active type-elaboration runner. Broader
checker semantic, CoreIr/ControlFlowIr, and VC/proof-verification coverage
remain paced by missing extraction seams.
Checker task 29, VC task 15, ATP task 20, and kernel task 17 remain
`paced/open` with deferred trace rows or metadata-only handoff fixtures; no
placeholder runner or fake active fixture is created before the owning seams
exist.

### Task 11: Determinism suite

Purpose: verify byte-stable discovery, plans, diagnostics, reports, snapshot
comparisons, and future parallel-equivalence surfaces.

Spec refs: `harness.md`.

Completion: metadata plan and active runner report canonical-byte stability
tests, generic snapshot nondeterminism diagnostics outside `SurfaceAst`, and
`verify_snapshot_parallel_equivalence` for snapshot-level sequential/parallel
observable equivalence.

### Task 12: Public-enum forward-compatibility policy

Purpose: decide and document public enum exhaustiveness/non-exhaustiveness for
the crate API.

Spec refs: all module specs.

Completion: every public enum in the public `mizar-test` source tree is
downstream `#[non_exhaustive]`, owning English and Japanese module specs record
the enum inventory and decision, and lint coverage checks both source
attributes and EN/JA documentation inventory. No exhaustive public enum
exceptions are recorded.

### Task 13: Bilingual documentation sync audit

Purpose: synchronize every English canonical document with its Japanese
companion.

Spec refs: repository documentation policy.

Completion: `bilingual_sync_audit.md` records a paired-file audit for every
document under `doc/design/mizar-test/en/` and `doc/design/mizar-test/ja/`;
task 14 completion is recorded below.

### Task 14: Incremental/parallel verification regression matrix

Purpose: add metadata/reporting support for architecture-22 regression matrix
scenarios while keeping execution in consumer crates.

Spec refs: architecture test strategy and incremental verification contract.

Completion: task 14 adds `architecture22_scenarios`,
`architecture22_equivalence_class`, and `architecture22_gate` sidecar metadata,
the scenario registry and deterministic plan matrix report, and
`tests/property/architecture22_matrix_001` as a metadata-only planned anchor for
all 18 required architecture-22 rows. All task-14 scenarios have active
eligibility `none`; active gates are rejected until a future consumer-specific
increment owns real execution.

### Task 15: Architecture-22 follow-up audit

Purpose: re-run source/spec and bilingual documentation audits after task 14
and record any remaining matrix gaps.

Spec refs: architecture test strategy, incremental verification contract, and
repository documentation policy.

Completion: task 15 re-runs the source/spec and EN/JA documentation sync
audits after task 14. `bilingual_sync_audit.md` now records the follow-up
review; MT-AUDIT-014 classifies the remaining architecture-22 active execution
work as a consumer-paced `test_gap`; task 14
scenario ids, equivalence classes, planned-only gating, and traceability anchor
match architecture 20 and 22. Task 10 remains consumer-paced with no newly
prepared runner increment. No `spec_gap`, `repo_metadata_conflict`, language
behavior change, or existing expectation semantic change is required.

### Task 16: Source-derived builtin type-expression bridge

Purpose: retire the first safe part of the checker payload-extraction gap by
building real checker-owned payloads from `.miz` source for builtin type
expressions only.

Spec refs: `harness.md`, `expectation_schema.md`, `traceability.md`, checker
MC-G020.

Completion: task 16 adds `mizar-checker` as a type-elaboration runner
dependency, extracts reserve-only unrecovered builtin `set`/`object`
`TypeExpression` nodes into `TypeExpressionInput`, normalizes them with
`TypeNormalizer`, assembles a minimal `TypedAst` shell, and adds active pass
coverage for that supported slice plus active boundary fail coverage for an
imported non-builtin type head. Unsupported declarations, terms, formulas,
coercions, attributes, modes, structures, overloads, facts, proofs, CoreIr,
ControlFlowIr, and VC seeds stay on explicit external gaps.

### Task 17: Source-derived builtin ResolvedTypedAst bridge

Purpose: promote the supported source-derived builtin type-expression runner
slice from a checker-owned `TypedAst` shell to checker-owned `ResolvedTypedAst`.

Spec refs: `harness.md`, checker `resolved_typed_ast.md`, checker
MC-G020/MC-G027.

Completion: task 17 projects the task-16 normalized builtin `set`/`object`
type-expression payloads through `ResolvedTypedAst::assemble` with real
expression metadata, source-preserved node hints, and empty cluster/overload
predecessor outputs. The runner checks that every supported source type site
has a resolved node, expression metadata, and final type without diagnostics.
Unsupported declarations, non-builtin type heads, attributes,
mode/structure expansion, term/formula payloads beyond the task-specific
theorem bridges, formula child/binder semantics beyond task 112, coercion
sites, overload payloads, recorded facts, proof evidence, CoreIr, ControlFlowIr, VC seeds, and
`proof_verification` rows remain explicit external gaps.

### Task 18: Source-derived reserve declaration semantic bridge

Purpose: widen the supported source-derived type-elaboration pass slice from
builtin type-expression nodes to reserve-only builtin declaration payloads.

Spec refs: `harness.md`, `expectation_schema.md`, `traceability.md`, checker
MC-G011/MC-G016/MC-G020.

Completion: task 18 extracts unrecovered top-level reserve items with bare
builtin `set`/`object` type heads into the supported reserve declaration slice.
Checker task 48 now owns the syntax-free producer seam that builds a
checker-owned module `BindingEnv`, one `DeclarationInput` per binding,
binding-specific `TypeExpressionInput` sites, and `DeclarationChecker` output;
the runner continues that handoff into `TypedAst` and `ResolvedTypedAst`.
Multi-variable reserve segments keep the shared source type range but receive
distinct typed sites. Unsupported non-builtin declarations beyond task 96's
redefinition/notation extraction-gap boundary, task 95's registration block extraction-gap boundary, imported symbols, argument-bearing or
attributed symbol heads, type-argument or term-argument provenance, structure
evidence payloads, proof-local declaration payloads, inline definition
payloads, registration payloads, activation/correctness payloads, redefinition/notation payloads, terms, formulas, coercions, overload
payloads, facts, CoreIr/ControlFlowIr/VC
payloads, and proof evidence stay on explicit external gaps; no downstream
CoreIr/ControlFlowIr/VC/proof trace row is promoted without real payload
execution.

### Task 19: Reserve bridge core summary readiness and builtin declaration inventory

Purpose: confirm `mizar-core` can read the real reserve-only checker payload at
the summary boundary, then inventory the next builtin declaration extraction
slice without promoting unprepared semantics.

Spec refs: `harness.md`, `expectation_schema.md`, `traceability.md`, core
`elaborator.md`.

Completion: task 19 adds `mizar-core` as a type-elaboration runner dependency
for the active runner only, passes the existing reserve-only builtin
`ResolvedTypedAst` payload through `ResolvedTypedAstSummary::from_ast`, and
checks source/module identity plus empty checker-site output for successful
active pass cases. The inventory records that no next builtin declaration
family is safe to promote yet: `let`, `given`, `consider`, and quantified
declarations require local scope, assumption, formula, or constraint-discharge
payloads; `set` requires RHS term inference; `reconsider` requires
coercion/obligation evidence; `deffunc`/`defpred` require body/formal payloads.
No `CoreIr`, `ControlFlowIr`, VC, proof, public diagnostic-code, `.miz`, or
expectation semantic change is introduced.

### Task 20: Reserve bridge core context readiness

Purpose: confirm that the same real reserve-only checker payload can cross the
next safe `mizar-core` boundary by preparing binder-only core context data,
without promoting `CoreIr` or downstream verification rows.

Spec refs: `harness.md`, `expectation_schema.md`, `traceability.md`, core
`elaborator.md`.

Completion: task 20 extends the active `type-elaboration` runner so the
reserve-only builtin `BindingEnv` and `ResolvedTypedAst` handoff feed
`CoreContextInput` with one `CoreVariableSeed` and one `CoreBinderSeed` per
extracted reserve binding and no `CoreItemSeed`. The runner verifies
source/module identity, binding count, binder source ranges, checker
provenance, empty item registry, empty core diagnostics, and an empty core
worklist for successful pass cases. Reserve declarations still do not carry
owner item, term, formula, proof, algorithm, or obligation payloads, so no
`CoreIr`, `ControlFlowIr`, VC, proof, public diagnostic-code, `.miz`, or
expectation semantic change is introduced.

### Task 21: Corrected-path soundness vocabulary

Purpose: extend the fail/soundness required-case registry and documentation
with the corrected kernel rejection vocabulary from kernel audit F7 without
changing rejection behavior.

Spec refs: architecture test strategy and kernel `soundness_argument.md` F7.

Completion: task 21 adds the corrected `soundness.certificate.*` registry
keys for `invalid_sat_refutation`, `context_mismatch`, `missing_provenance`,
and unsupported legacy certificates under normal policy, while keeping the
legacy `invalid_sat_proof` audit key. Existing corrected-path certificate
sidecars are re-keyed to those stable identities with `domain = "certificate"`;
proof, certificate, and kernel runner behavior is unchanged.

Spec coverage audit impact: unchanged. The task does not change traceability
status, follow-up owner, owner crate, or deferred coverage rationale.

### Task 22: Certificate-corpus root naming reconciliation

Purpose: resolve kernel audit F8 by making the architecture test strategy,
kernel audit note, and certificate corpus README agree on the implemented
`tests/certificates/` root.

Spec refs: architecture test strategy and kernel `soundness_argument.md` F8.

Completion: task 22 updates architecture 20 (EN/JA), the certificate corpus
README, this crate plan, and the kernel soundness argument (EN/JA) so
`tests/certificates/` is the canonical certificate/kernel-evidence corpus root
and `tests/kernel_evidence/` is only a retired audit-draft name. No Rust
source, `doc/spec`, `.miz` tests, expectations, runner behavior, traceability
metadata, or semantic intent changes are introduced.

Spec coverage audit impact: no `doc/design/spec_coverage_audit.md` update is
required because this task does not change spec chapter coverage, design
mapping, follow-up ownership, traceability status, or deferred coverage
rationale.

## Exit Criteria

Hard gates:

- no blocking `spec_gap`, accepted `repo_metadata_conflict`, or language
  behavior change remains hidden;
- no source behavior remains undocumented at crate exit;
- all task-owned gaps are fixed or explicitly deferred with a reason;
- `doc/design/mizar-test/en/` and `doc/design/mizar-test/ja/` stay
  synchronized;
- `doc/spec`, existing `.miz`, and existing expectations are untouched unless a
  future user request explicitly changes that intent.

Verification commands:

- `cargo test -p mizar-test`
- `cargo clippy -p mizar-test --all-targets -- -D warnings`
- `cargo fmt --check`

Review expectations:

- task-specific spec/docs review;
- test sufficiency review;
- implementation review;
- source/documentation consistency review.


## Task 201 Immediate-Radix Asserted-Head Active Slice

Task 201 adds one exact active pass source with `BaseModeRadixAssertedHead -> set`, `OuterModeRadixAssertedHead -> BaseModeRadixAssertedHead`, one outer-mode reserve, and theorem `ChainedLocalModeRadixAssertedHeadPayloadBoundary: x is BaseModeRadixAssertedHead;`. This is a `test_gap` plus narrow `source_drift` and `design_drift`, with no `spec_gap`. The runner consumes two real AST/resolver expansions, preserves distinct Outer/Base symbols, sites, and ranges, requires the formula asserted symbol to be the binding expansion's resolved immediate radix, resolves ordinal 1 to `BindingId(0)`, and normalizes three known entries to one Base-definition-RHS builtin-set identity. One inferred variable and one checked type assertion have zero expected constraints/candidates/facts/diagnostics/deferred reasons. Five shared backlinks plus one dedicated requirement protect active runner 149 within 364 cases and 328 requirements without rebaselining existing expectations. Broader asserted heads and semantics remain deferred; Step 5 is active and Steps 6/7 are deferred.


## Task 202 Object Immediate-Radix Asserted-Head Active Slice

Task 202 adds the exact active object-terminal source `BaseObjectModeRadixAssertedHead -> object`, `OuterObjectModeRadixAssertedHead -> BaseObjectModeRadixAssertedHead`, one outer reserve, and a Base asserted type. The unchanged closed immediate-radix relation consumes two real expansions, preserves distinct Outer/Base provenance, resolves ordinal 1 to `BindingId(0)`, and normalizes three known entries to one Base-definition-RHS builtin-object identity. One inferred variable and one checked assertion have zero constraints/candidates/facts/diagnostics/deferred and no object/set coercion. Five shared plus one dedicated backlink protect active runner 150 within 365 cases and 329 requirements. Broader semantics remain deferred; Step 5 is active and Steps 6/7 are deferred.


## Task 203 Two-Edge Immediate-Radix Asserted-Head Active Slice

Task 203 adds one exact active set-terminal source with Base, Middle, and Outer local-mode definitions, one Outer reserve, and an assertion of the immediate Middle radix. Task 72's three real expansions and Task 186's formula consumer feed the unchanged closed immediate-radix relation. The runner preserves distinct Outer/Middle provenance, resolves ordinal 1 to `BindingId(0)`, consumes three expansions, normalizes three known entries to one Base-definition-RHS builtin-set identity, and emits one inferred variable plus one checked assertion with zero constraints/candidates/facts/diagnostics/deferred reasons. Five shared plus one dedicated backlink and exact/corruption/reordering/duplicate/spelling/imported/ambiguous/deeper/real-route isolation/sidecar guards protect active runner 151 within 366 cases and 330 requirements. Two-hop Base assertion, the object sibling, and broader semantics remain deferred; Step 5 is active and Steps 6/7 are deferred.


## Task 204 Two-Edge Object Immediate-Radix Asserted-Head Active Slice

Task 204 adds one exact active object-terminal source with Base, Middle, and Outer local-mode definitions, one Outer reserve, and an assertion of the immediate Middle radix. Task 72's three real object expansions and Task 187's formula consumer feed the unchanged closed immediate-radix relation. The runner preserves distinct Outer/Middle provenance, resolves ordinal 1 to `BindingId(0)`, consumes three expansions, normalizes three known entries to one Base-definition-RHS builtin-object identity, and emits one inferred variable plus one checked assertion with zero constraints/candidates/facts/diagnostics/deferred reasons and no object/set coercion. Five shared plus one dedicated backlink and exact/corruption/order/duplicate/spelling/imported/ambiguous/deeper/real-route isolation/sidecar guards protect active runner 152 within 367 cases and 331 requirements. Two-hop Base assertion and broader semantics remain deferred; Step 5 is active and Steps 6/7 are deferred.

## Task 205 Three-Edge Set Immediate-Radix Asserted-Head Active Slice

Task 205 adds one exact active set-terminal source with Base, Inner, Middle, and Outer local-mode definitions, one Outer reserve, and an assertion of the immediate Middle radix. Task 73's four real set-terminal expansions and Task 195's formula consumer feed the unchanged closed immediate-radix relation. The runner preserves distinct Outer/Middle provenance, resolves ordinal 1 to `BindingId(0)`, consumes four expansions, normalizes three known entries to one Base-definition-RHS builtin-set identity, and emits one inferred variable plus one checked assertion with zero constraints/candidates/facts/diagnostics/deferred reasons. Five shared plus one dedicated backlink and exact/corruption/all-23-orders/missing/duplicate/label/spelling/radix/imported/ambiguous/deeper/multi-hop/bidirectional-16-route/sidecar guards protect active runner 153 within 368 cases and 332 requirements. Multi-hop Inner/Base assertion, the matching object sibling, and broader semantics remain deferred; Step 5 is active and Steps 6/7 are deferred.

## Task 206 Three-Edge Object Immediate-Radix Asserted-Head Active Slice

Task 206 adds one exact active object-terminal source with Base, Inner, Middle, and Outer local-mode definitions, one Outer reserve, and an assertion of the immediate Middle radix. Task 73's four real object-terminal expansions and Task 196's formula consumer feed the unchanged closed immediate-radix relation. The runner preserves distinct Outer/Middle provenance, resolves ordinal 1 to `BindingId(0)`, consumes four expansions, normalizes three known entries to one Base-definition-RHS builtin-object identity, and emits one inferred variable plus one checked assertion with zero constraints/candidates/facts/diagnostics/deferred reasons and no object/set coercion. Five shared plus one dedicated backlink and exact/corruption/all-23-orders/per-definition/imported/ambiguous/deeper/multi-hop/local-other/bidirectional-17-route/sidecar guards protect active runner 154 within 369 cases and 333 requirements. Multi-hop Inner/Base assertion and broader semantics remain deferred; Step 5 is active and Steps 6/7 are deferred.

## Task 207 Four-Edge Set Immediate-Radix Asserted-Head Active Slice

Task 207 adds one exact active set-terminal source with Base, Inner, Middle, Outer, and TooDeep local-mode definitions, one TooDeep reserve, and an assertion of the immediate Outer radix. Task 74's five real set-terminal expansions and Task 197's formula consumer feed the unchanged closed immediate-radix relation. The runner preserves distinct TooDeep/Outer provenance, resolves ordinal 1 to `BindingId(0)`, consumes five expansions, normalizes three known entries to one Base-definition-RHS builtin-set identity, and emits one inferred variable plus one checked assertion with zero constraints/candidates/facts/diagnostics/deferred reasons. Five shared plus one dedicated backlink and exact/corruption/all-119-orders/per-definition/imported/ambiguous/deeper/multi-hop/local-other/bidirectional-20-route/sidecar guards protect active runner 155 within 370 cases and 334 requirements. Multi-hop Middle/Inner/Base assertions, the matching object sibling, and broader semantics remain deferred; Step 5 is active and Steps 6/7 are deferred.
