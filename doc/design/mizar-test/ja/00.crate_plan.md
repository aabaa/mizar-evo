# Crate Plan: mizar-test

> 正本は英語です。英語版: [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## 責務

所有する振る舞い:

- corpus payload と `.expect.toml` sidecar の決定的 discovery
- expectation metadata と traceability metadata の parse / validation
- staged-model admission と coverage accounting
- stage-owned compiler seam の active runner gate
- snapshot comparison/update policy と決定的 report surface
- 黙って pass、削除、弱体化してはならない fail/soundness case の bookkeeping

対象外:

- compiler language semantics
- 明示的な consumer-runner seam 以外での name resolution、type checking、
  proof search、ATP、certificate replay、kernel proof authority
- current implementation に合わせる目的での `doc/spec`、既存 `.miz`、
  既存 expectation の変更

## 仕様項目

| Spec ref | Requirement | Status |
|---|---|---|
| [layout.md](./layout.md) | known roots、payload/sidecar pairing、deterministic order、naming と sidecar rules | partial |
| [expectation_schema.md](./expectation_schema.md) | sidecar schema、outcome compatibility、fail identity、active runner metadata、transitional parse-only snapshots | partial |
| [staged_model.md](./staged_model.md) | canonical stage ids と prerequisite-gated coverage credit | partial |
| [traceability.md](./traceability.md) | manifest schema、sidecar backrefs、validation modes、coverage status、reporting | partial |
| [harness.md](./harness.md) | metadata plan、active runner modes、deterministic diagnostics/reports | partial |
| [miz_corpus.md](./miz_corpus.md) | corpus classes、generated/fuzz/stress metadata、review rules、pass/fail mix | partial |
| [snapshot.md](./snapshot.md) | snapshot kinds、public API、canonicalization、update policy、determinism checks | partial |
| [fail_soundness.md](./fail_soundness.md) | required soundness cases、expected-failure identity、weakening/deletion rules | partial |
| [minimal_crate.md](./minimal_crate.md) | metadata-only plan scope、CLI、validation、determinism、exit codes | partial |

## 関連テスト

| Test path | Intent | Spec refs |
|---|---|---|
| `crates/mizar-test/tests/lint_policy.rs` | workspace lint opt-in と inline `allow` rationale guard | repository conventions |
| `crates/mizar-test/tests/metadata.rs` | synthetic metadata/traceability fixtures、active runner gates、repository plan checks、transitional parse-only snapshots | mizar-test module specs |
| `crates/mizar-test/src/runner.rs` unit tests | parse-only fixture import-summary behavior | harness.md |
| `tests/coverage/spec_trace.toml` | metadata plan が消費する requirement manifest | traceability.md |
| `tests/**/*.expect.toml` | discovery、schema validation、traceability、active runner gate が消費する sidecar metadata | expectation_schema.md |

Planned test-first additions:

- task 3: validation mode と exit code の CLI fixture
- task 4/5: snapshot canonicalization、update、determinism fixture
- task 6: traceability coverage/status/report fixture
- task 7: stage prerequisite violation fixture
- task 8: fail/soundness contract fixture（実装済み）
- task 9: corpus policy fixture
- task 16: source-derived builtin type-expression pass fixture と runner
  regression tests
- task 17: source-derived builtin `ResolvedTypedAst` handoff regression tests
- task 18: source-derived reserve declaration semantic bridge regression tests
- task 19: reserve bridge `ResolvedTypedAstSummary::from_ast` readiness
  regression tests と builtin declaration inventory
- checker task 56: source-derived one-edge local-mode expansion chain pass/gap
  fixtures と runner regression tests
- checker task 57: source-derived local-mode structure-RHS evidence-gap fixture
  と runner regression tests
- checker task 58: source-derived local-mode attributed-builtin-RHS evidence-gap
  fixture と runner regression tests
- checker task 59: source-derived attributed local-mode reserve evidence-gap
  fixture と runner regression tests
- checker task 60: source-derived attributed local-mode structure-RHS
  evidence-gap fixture と runner regression tests
- checker task 61: source-derived attributed local-mode attributed-builtin-RHS
  evidence-gap fixture と runner regression tests
- checker task 62: source-derived local-mode structure-RHS chain evidence-gap
  fixture と runner regression tests
- checker task 63: source-derived local-mode attributed-builtin-RHS chain
  evidence-gap fixture と runner regression tests
- checker task 64: source-derived attributed local-mode bare-builtin chain
  evidence-gap fixture と runner regression tests
- checker task 65: source-derived attributed local-mode structure-RHS chain
  evidence-gap fixture と runner regression tests
- checker task 66: source-derived attributed local-mode attributed-builtin-RHS
  chain evidence-gap fixture と runner regression tests
- checker task 67: source-derived structure-qualified attribute extraction-gap
  boundary fixture と runner regression tests
- checker task 68: source-derived argument-bearing mode reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 69: source-derived argument-bearing structure reserve
  extraction-gap boundary fixture と runner regression tests
- checker task 70: source-derived bracket-form local mode reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 71: source-derived bracket-form local structure reserve
  extraction-gap boundary fixture と runner regression tests
- checker task 72: source-derived two-edge bare local-mode chain pass fixture、checker task 73: source-derived three-edge bare local-mode chain pass fixture、checker task 74: structural bare local-mode chain pass fixture、および runner regression tests
- checker task 75: source-derived local-mode forward-reference active-range
  boundary fixture と runner regression tests
- checker task 76: source-derived local-structure forward-reference active-range
  boundary fixture と runner regression tests
- checker task 77: source-derived local-attribute forward-reference active-range
  boundary fixture と runner regression tests
- checker task 78: source-derived imported structure reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 79: source-derived imported mode reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 80: source-derived imported attribute reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 82: source-derived imported mode reserve provenance bridge
  fixture と runner regression tests
- checker task 83: source-derived imported structure reserve provenance bridge
  fixture と runner regression tests
- checker task 84: source-derived imported attribute reserve provenance bridge
  fixture と runner regression tests

## design と source の inventory

Design files:

- `doc/design/mizar-test/en/*.md`
- `doc/design/mizar-test/ja/*.md`

Source files:

- `crates/mizar-test/src/layout.rs`
- `crates/mizar-test/src/path_rules.rs`
- `crates/mizar-test/src/expectation.rs`
- `crates/mizar-test/src/staged_model.rs`
- `crates/mizar-test/src/traceability.rs`
- `crates/mizar-test/src/harness.rs`
- `crates/mizar-test/src/runner.rs`
- `crates/mizar-test/src/snapshot.rs`
- `crates/mizar-test/src/main.rs`
- `crates/mizar-test/src/diagnostic.rs`
- `crates/mizar-test/src/toml_lite.rs`

Observed behavior:

- metadata `plan` discovery、sidecar parsing、trace manifest parsing、
  duplicate id checks、missing source/sidecar checks、deterministic plan ordering
  は実装・テスト済み
- active `parse-only`、`declaration-symbol`、`type-elaboration` runner は
  explicit active-tag gate と repository coverage を持つ
- active `type-elaboration` runner は対応済み source-derived reserve declaration
  slice について checker-owned `BindingEnv`、reserve `DeclarationInput`、
  binding 固有の builtin `set` / `object` `TypeExpressionInput`、
  `DeclarationChecker` output まで checker-owned syntax-free task-48 seam を通し、
  その後 `TypedAst`、`ResolvedTypedAst` assembly まで継続し、
  その real payload を `mizar-core` の `ResolvedTypedAstSummary::from_ast` で読み、
  同じ real reserve binding から binder-only `CoreContext` を準備し、
  same-module attributed builtin reserve payload は checker `MissingEvidenceQuery`
  diagnostic まで、same-module local mode reserve head は checker missing
  mode-expansion diagnostic まで、same-module local structure reserve head は checker
  `MissingEvidenceQuery` diagnostic まで、attributed same-module local structure reserve head は
  checker `MissingEvidenceQuery` diagnostic まで、supported real expansion がないか mixed bare
  reserve use を持つ attributed same-module local mode reserve head は checker missing
  mode-expansion diagnostic まで、bare same-module no-argument local mode expansion は
  preceding bare-builtin RHS mode definition から active pass reserve fixture まで抽出し、
  dependency mode が accepted bare-builtin RHS expansion を持つ one-edge same-module
  local-mode chain も抽出し、RHS が same-module local structure head である real
  same-module local-mode expansion は missing base-shape / constructor-witness evidence
  の checker `MissingEvidenceQuery` diagnostic まで抽出し、RHS が attributed builtin
  head である real same-module local-mode expansion は missing attributed-type
  existential evidence の checker `MissingEvidenceQuery` diagnostic まで抽出し、
  real direct bare-builtin expansion を持つ attributed same-module local-mode reserve head は
  missing attributed-type existential evidence の同じ checker `MissingEvidenceQuery` diagnostic
  まで抽出し、real direct local-structure RHS expansion を持つ attributed
  same-module local-mode reserve head は missing base-shape / constructor-witness と
  full attributed-type evidence の同じ checker `MissingEvidenceQuery` diagnostic まで抽出し、
  real direct attributed-builtin RHS expansion を持つ attributed
  same-module local-mode reserve head は missing full attributed-type evidence の同じ
  checker `MissingEvidenceQuery` diagnostic まで抽出し、local-structure、
  attributed-builtin、bare-builtin attributed-root、structure-RHS attributed-root、
  attributed-builtin-RHS attributed-root terminal payload で終端する one-edge same-module
  local-mode chain は対応する missing evidence-query diagnostic まで抽出し、
  later declaration を名前参照する forward same-module local-mode reserve head は
  checker handoff 前の lower-stage active-range failure として報告し、
  mixed import、imported attribute / structure、task 82 の provenance bridge を超える
  imported mode expansion と未対応の semantic / CoreIr / ControlFlowIr / VC / proof payload family は
  external-gap key のままにする
- architecture-22 matrix sidecar metadata は validate / report され、owning consumer が
  active clean/incremental/parallel/cache-race execution を提供するまで planned-only
  scenario coverage として扱われる
- transitional active parse-only `SurfaceAst` snapshot text baseline は
  validate / compare される
- general snapshot records、update mode、determinism mode、coverage reporting、
  pass/fail mix reporting、manifest ordering、obsolete-reference checks は実装済み
- stage prerequisite validation は実装済み
- fail/soundness metadata bookkeeping は certificate/kernel `rejection_reason`、
  認識済み required soundness case の shape/profile/phase、mode-aware な missing-case
  diagnostic を検証する。active proof/certificate/kernel execution は将来の
  consumer runner に律速される。

## 既知のギャップと drift

| ID | Class | Evidence | Action |
|---|---|---|---|
| MT-AUDIT-001 | design_drift | `layout.md` public API は `TestCase`/`TestMetadata` を示すが、source は `DiscoveredLayout` と harness-owned `TestCase` を公開する。 | task 3 または次の layout-focused doc change で design API wording を同期する。 |
| MT-AUDIT-002 | resolved | task 9 は generated/fuzz/property `[origin]` metadata を parse / retain し、reproducibility fields と schema fixtures を追加した。 | 新しい provenance fields を追加するとき origin schema docs を同期する。 |
| MT-AUDIT-003 | resolved / watch | task 3 は `profiles`、`notes`、`ast_profile`、`snapshot_profiles` を保持し、返却される plan case を `TestProfile` で filter する。task 9 は generated/fuzz/stress corpus profile gates を追加した。 | 新しい profiles が入るとき profile policy を同期する。 |
| MT-AUDIT-004 | resolved / runner watch | task 8 は certificate/kernel `rejection_reason` を必須化し、public diagnostic code が無い runner の `stable_detail_key` fallback identity を保ち、case-level fail/soundness metadata gate を追加した。 | proof/certificate/kernel runner が着地するとき active runner matching を同期する。 |
| MT-AUDIT-005 | partial source_drift | task 4 と task 5 は general in-memory snapshot record API、canonicalization、hashing、validation、comparison、explicit baseline verify/update IO、repeat-render determinism helper を実装する。general `[[snapshots]]` sidecar parsing と runner integration は pending。 | 残る snapshot registry/runner integration は expectation/harness follow-up tasks で実装する。 |
| MT-AUDIT-006 | resolved / design_drift watch | task 7 は stage id、`depends_on`、`built_in` requirement に対する declared prerequisite gates を実装した。より広い corpus admission policy は task 9 / follow-up work として残る。 | 新しい stage が executable になるたび staged-model docs を同期する。 |
| MT-AUDIT-007 | resolved / watch | task 6 は mode-aware coverage completeness、computed/stored status comparison、obsolete-reference checks を実装した。task 7 は `depends_on` と prerequisite credit gates を実装した。 | 新しい evidence kind が入るたび coverage/prerequisite gates を同期する。 |
| MT-AUDIT-008 | resolved | trace manifest deterministic ordering は文書化済み。metadata ordering conflict は `897d549` で修復済みで、task 6 が validator coverage を追加した。 | manifest ordering rules が変わらない限り追加対応なし。 |
| MT-AUDIT-009 | resolved / design_drift watch | task 3 は `harness.md` を更新し、exported metadata plan と runner-specific reports を示し、generic outcome/snapshot reporting を future API と明記する。 | 後続で generic reporting surface が入るたび docs を同期する。 |
| MT-AUDIT-010 | resolved | task 3 は狭い `parser.type_fixtures` symbol-injection exception を文書化する。 | fixture provider が変わらない限り追加対応なし。 |
| MT-AUDIT-011 | partial source_drift / test_gap | task 5 と task 11 は general snapshot records、deterministic hashing、repeat-render determinism checks、snapshot-level parallel equivalence、plan/report byte-stability tests を実装した。fuzz-regression と update runner modes は future active-runner work として残る。 | 残る runner modes は owning consumer/update flows が存在するまで deferred のままにする。 |
| MT-AUDIT-012 | resolved / watch | task 6 は corpus-wide pass/fail mix を report する。task 9 は generated reproducibility metadata、fuzz category preservation、stress exclusion、provenance metadata、corpus size/naming/placement review rules を validate する。 | 新しい corpus classes が入るとき corpus policy validation を同期する。 |
| MT-AUDIT-013 | resolved / watch | task 3 は focused expectation-schema fixtures を追加し、task 6 は manifest/source/link/deferred/planned-warning traceability fixtures を追加した。 | 将来の schema fields と fixture coverage を同期し続ける。 |
| MT-AUDIT-014 | test_gap | task 14 は 18 個すべての architecture-22 regression row を planned metadata として記録し、active gate を reject する。source-derived clean/incremental/parallel/cache-race execution の prepared consumer runner increment は確認できていないため、execution を捏造せずに active row は実行できない。 | consumer-specific source-derived increment が存在するまで、consumer-paced external dependency として matrix は planned/open のままにする。次の source-derived semantic bridge は、row を active に昇格する前に prepared seam を棚卸しする。 |
| MT-AUDIT-015 | partial source_drift / external_dependency_gap | task 16 から task 81 までは builtin reserve / type-expression source bridge、checker-owned `ResolvedTypedAst` projection、reserve declaration handoff、supported same-module bare local-mode expansion family、および local structure / attribute / argument / bracket / forward-reference / imported-attribute の active diagnostic boundary を段階的に追加した。checker task 82 は documented `parser.type_fixtures` import summary 由来 imported mode symbol の provenance/type-head handoff だけを昇格し、checker missing mode-expansion diagnostic まで到達させる。checker task 83 は documented imported structure `R` の provenance/type-head handoff だけを昇格し、checker missing structure-evidence query まで到達させる。checker task 84 は documented imported attribute `TypeCaseAttr` の provenance / `AttributeInput` handoff だけを昇格し、checker missing attributed-type evidence diagnostic まで到達させる。checker task 85 は documented imported negative `empty`/builtin-`set` provenance / `AttributeInput` handoff だけを昇格し、同じ checker missing attributed-type evidence diagnostic まで到達させる。checker task 86 は formula-only theorem source を active parser/resolver-to-checker extraction-gap boundary として記録する。AST-wide non-builtin declaration、task 84 `TypeCaseAttr` bridge、task 85 negative `empty`/builtin-`set` bridge、task 80 boundary を超える imported attribute、task 83 `R` provenance bridge を超える imported structure、task 82 の provenance bridge を超える imported mode expansion payload、attribute argument、qualified attribute provenance、mode / structure argument、bracket `type_arg_list` と `qua`-argument provenance、term-argument provenance、structure base-shape と full attributed-type existential evidence、broader / attributed / argument-bearing / parameterized / contextual / ambiguous / cyclic mode expansion と task 74 structural guard を満たさない chain、term、task-86 extraction-gap boundary を超える formula/theorem payload、coercion、overload payload、fact、proof evidence、CoreIr、ControlFlowIr、VC extraction はまだ利用できない。 | 未対応 semantic / CoreIr / ControlFlowIr / VC / proof payload family は deferred のままにする。prepared consumer execution が確認できた場合だけ traceability row を昇格し、未実装 runner、task-84 `TypeCaseAttr` bridge と task-85 negative `empty`/builtin-`set` bridge、task-82 imported-mode provenance slice、task-83 imported-structure `R` provenance slice、task-84 imported-attribute `TypeCaseAttr` provenance slice、task-85 imported negative `empty`/builtin-`set` provenance slice、task-86 theorem/formula extraction-gap boundary を超える imported / ambiguous symbol、imported mode expansion、mixed attributed/bare local-mode use、task 74 structural guard 外の broader mode-expansion payload、qualified attribute payload、type/term-argument payload、bracket / `qua`-argument payload、existential evidence、task-83 missing-evidence diagnostic を超える structure evidence、task-84/task-85 missing-evidence diagnostic を超える attributed-type evidence、future declaration payload の active fixture を捏造しない。 |
| MT-AUDIT-016 | partial source_drift / external_dependency_gap | task 19 は real reserve-only builtin `ResolvedTypedAst` payload が `mizar-core` の `ResolvedTypedAstSummary::from_ast` で読め、source/module identity が保たれ、checker site が出ないことを確認する。次の builtin declaration inventory では安全な昇格は見つかっていない。`let` / `given` / `consider` / quantified declaration は local scope、assumption、formula、constraint-discharge payload を必要とし、`set` は RHS term inference、`reconsider` は coercion / obligation evidence、`deffunc` / `defpred` は body / formal payload を必要とする。 | prepared active runner seam が real payload を raw reconstruction や fake evidence なしに実行できるまで、これらの declaration family は source-to-checker extraction gap に残す。summary-readiness を `CoreIr`、`ControlFlowIr`、VC、proof execution として扱わない。 |
| MT-AUDIT-017 | partial source_drift / external_dependency_gap | task 20 は同じ real reserve-only builtin checker payload が、抽出済み reserve binding ごとに 1 個の `CoreVariableSeed` と `CoreBinderSeed` を持ち `CoreItemSeed` を持たない `mizar-core` binder-only `CoreContext` preparation を seed できることを確認する。runner は source/module identity、binder source range、provenance、empty item registry、empty diagnostics、empty core worklist を確認する。reserve declaration は owner item、term、formula、proof skeleton、algorithm shell、obligation seed をまだ提供しない。 | real source-derived payload が各 consumer に存在するまで、`CoreIr`、`ControlFlowIr`、VC、proof row、publication/orchestration、より広い semantic payload family は deferred のままにする。 |
| MT-AUDIT-018 | resolved design_drift | task 22 は architecture 20 の退役済み `tests/kernel_evidence/` directory 名と実装済み `tests/certificates/` corpus root を調停し、corpus README を更新し、kernel audit F8 を解決済みとして記録する。 | `tests/certificates/` を certificate/kernel-evidence corpus の正準 root として維持する。spec chapter coverage、follow-up owner、traceability status、deferred rationale は変わらないため、`doc/design/spec_coverage_audit.md` 更新は不要。 |
| MT-AUDIT-019 | 解決済み test_gap / design_drift | task 21 は audit F7 の訂正後 kernel rejection vocabulary を required soundness-case registry と paired docs に追加し、既存 corrected-path certificate sidecar を `soundness.certificate.*` stable key へ付け替える。 | rejection vocabulary が変化するたび registry と certificate corpus を同期する。traceability status、owner crate、deferred rationale は変わらないため、`doc/design/spec_coverage_audit.md` 更新は不要。 |

Task 81 addendum: `mizar-test` は argument-bearing local-attribute bridge
boundary の active runner metadata も所有する。active declaration-symbol
fixture は resolver 由来の parameterized local attribute suffix (`ranked`) を
exported primary symbol として記録し、prefixed surface (`2 - ranked`) は notation
provenance として保持する。対応する active type-elaboration fixture は
`checker.type_elaboration.payload_extraction.argument_bearing_attribute_gap`
の fail-closed checker extraction gap を記録する。この addendum は checker
`AttributeInput` argument payload、attributed evidence、CoreIr、ControlFlowIr、
VC、proof payload を昇格しない。それらは real consumer payload と runner support
が存在するまで deferred のままとする。

Task 82 addendum: `mizar-test` は imported-mode reserve provenance bridge の
active runner metadata も所有する。既存の imported-mode type-elaboration
fixture は、`parser.type_fixtures` から real imported `SymbolKind::Mode`
symbol を checker type head へ渡すところだけを credit してよく、imported mode
expansion payload が存在するまでは
`type_elaboration.checker.checker.type.external.mode_expansion_payload` で
fail closed しなければならない。
この addendum は imported module AST extraction、positive imported mode
elaboration、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 83 addendum: `mizar-test` は imported-structure reserve provenance bridge
の active runner metadata も所有する。既存の imported-structure
type-elaboration fixture は、`parser.type_fixtures` の structure `R` について
real imported `SymbolKind::Structure` symbol を checker type head へ渡すところ
だけを credit してよく、imported module AST extraction と
base-shape/constructor-witness evidence が存在するまでは
`type_elaboration.checker.checker.declaration.deferred.evidence_query` で
fail closed しなければならない。その documented `R` fixture 外の imported
structure は task-78 extraction-gap boundary に残る。この addendum は positive
imported structure elaboration、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 84 addendum: `mizar-test` は imported-attribute reserve provenance bridge
の active runner metadata も所有する。既存の imported-attribute
type-elaboration fixture は、`parser.type_fixtures` の attribute `TypeCaseAttr`
について real imported `SymbolKind::Attribute` symbol を builtin `set` 上の
checker `AttributeInput` へ渡すところだけを credit してよく、imported module AST
extraction と attributed-type evidence query が存在するまでは
`type_elaboration.checker.checker.declaration.deferred.evidence_query` で
fail closed しなければならない。その documented `TypeCaseAttr set` fixture 外の
imported attribute は task-80 extraction-gap boundary に残る。この addendum は
positive imported attribute elaboration、attribute argument、qualified/owner
attribute provenance、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 85 addendum: `mizar-test` は imported non-empty attribute reserve
provenance bridge の active runner metadata も所有する。既存の `non empty set`
type-elaboration fixture は、`parser.type_fixtures` の attribute `empty` について
real imported `SymbolKind::Attribute` symbol を builtin `set` 上の negative
checker `AttributeInput` として渡すところだけを credit してよく、imported module
AST extraction と attributed-type evidence query が存在するまでは
`type_elaboration.checker.checker.declaration.deferred.evidence_query` で
fail closed しなければならない。positive `empty set`、non-`set` head 上の
imported `empty`、documented `TypeCaseAttr set` と negative
`empty`/builtin-`set` fixture 外の imported attribute は task-80 extraction-gap
boundary に残る。task 85 は positive `empty set` と `non empty object` の
active boundary sidecar も保持し、それらは
`type_elaboration.external_dependency.ast_payload_extraction` を assert して
negative `empty`/builtin-`set` bridge を credit しない。

Task 86 addendum: `mizar-test` は formula-only theorem extraction-gap boundary の
active runner metadata も所有する。新しい theorem source sidecar は
`type_elaboration` で parser / resolver まで実行してよいが、checker-owned
theorem/formula payload extraction、local proof context、recorded fact、theorem
acceptance、dedicated `formula_statement` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は formula payload、proof skeleton、CoreIr、
ControlFlowIr、VC、proof payload を昇格しない。

task 15 の再監査では、blocking な `spec_gap`、採用すべき
`repo_metadata_conflict`、必要な language behavior change、または既存 expectation の
意味変更が必要な箇所は記録していない。

## タスク分解

### Task 1: Lint-policy guard

Purpose: `mizar-test` を workspace lint に opt-in させ、lint exception の
根拠を必須にする。

Completion: `0b24d1a` として commit 済み。

### Task 2: Source/spec gap audit and status sync

Purpose: audit baseline を記録し、drift/gap を分類し、module status を同期する。

Completion: この crate plan と `todo.md` の status/follow-up updates。

Forbidden behavior: この audit commit で `doc/spec`、既存 `.miz`、既存
expectation を修復すること。

### Task 3: Runner modes and CLI completion

Purpose: corpus と coverage metadata に対する validation-mode CLI behavior、
exit-code fixtures、deterministic output を完成させる。

Spec refs: `minimal_crate.md`, `harness.md`.

Completion: validation-mode CLI parsing、strict/permissive unknown-root
policy、profile metadata retention/filtering、plan CLI fixtures、
expectation-schema fixtures、harness documentation sync。

### Task 4: Snapshot API and canonicalization

Purpose: general snapshot module と canonical record API を導入する。

Spec refs: `snapshot.md`.

Completion: general in-memory `SnapshotRecord` API、canonical hash input、
line-ending normalization、local absolute path validation、deterministic
profile metadata ordering、comparison diffs。

### Task 5: Snapshot update policy and determinism checks

Purpose: explicit update flow と repeat-render determinism checks を実装する。

Spec refs: `snapshot.md`.

Completion: explicit `VerifyOnly` versus `Update { reason }` baseline helper、
safe `.snap` baseline path validation、create/update round-trips、no-write
verify failures、repeat-render determinism mismatch reports。

### Task 6: Coverage and pass/fail-mix reporting

Purpose: discovery と traceability data から trace coverage、stored status drift、
corpus pass/fail mix を compute/report する。

Spec refs: `traceability.md`, architecture test strategy.

Completion: per-requirement/per-stage coverage report、mode-aware
coverage/status gate、manifest id-order validation、obsolete-reference
diagnostics、link-validator regression fixtures、deterministic CLI pass/fail
mix output。

### Task 7: Stage-prerequisite validation

Purpose: prerequisites が covered、built-in 宣言済み、または `depends_on` により
accept されるまで higher-stage coverage credit を与えない。

Spec refs: `staged_model.md`, `traceability.md`.

Completion: manifest `depends_on` validation、`built_in` declarations、
stage-mismatch diagnostics、unsatisfied-prerequisite diagnostics、mode-aware
completeness check 前の coverage-credit withholding。

### Task 8: Fail/soundness contract support

Purpose: fail/soundness required-domain bookkeeping、typed failure identity
checks、false-arithmetic coverage、weakening/deletion regression rules を実装する。

Spec refs: `fail_soundness.md`.

Completion: certificate/kernel `rejection_reason` validation、認識済み
`soundness.*` required-case の shape/profile/phase validation、fail/soundness
manifest に対する mode-aware missing-case diagnostics、false-arithmetic stable
key gate、regression fixture。所有する consumer runner が存在する前に real
proof/certificate/kernel execution は捏造しない。

### Task 9: Corpus size and review-rule validation

Purpose: corpus naming、size、class placement、generation metadata、
corpus-policy profile constraints、stress exclusion、fuzz provenance rules を
強制する。

Spec refs: `miz_corpus.md`.

Completion: `[origin]` provenance の parsing/retention、generated/fuzz/property
placement gates、stress root discovery、profile constraints、fuzz-category
preservation、upper-bound `.miz` size diagnostics、naming diagnostics、clean /
violating corpora の metadata fixtures。

### Task 10: Consumer-runner support

Purpose: consumer runner の着地ごとに discovery、expectations、stages、
snapshots、active/planned gates、reporting を同期する。

Spec refs: `harness.md`.

Current increment: `parse-only`、`declaration-symbol`、task-12 の
`type-elaboration` external-gap runner、task-16 の source-derived builtin
type-expression normalization slice、task-17 の builtin `ResolvedTypedAst`
handoff、task-18 の reserve declaration semantic bridge、task-19 の
`ResolvedTypedAstSummary::from_ast` readiness check と次の builtin declaration
inventory は active runner command として prepared/implemented であり検証済み。
task-20 の binder-only `CoreContext` readiness check も active runner command として
prepared/implemented であり検証済み。post-task-20 resolver R-G007 の parser-backed
same-signature/different-return functor conflict active declaration-symbol seed も
active runner command として prepared/implemented であり検証済み。表現済みの
kind、visibility、export status に対する exact SymbolEnv-derived
declaration-symbol pass payload assertion と checker task 50 の same-module
attributed reserve evidence-query active fail slice、checker task 55 の bare
local-mode expansion pass slice、checker task 56 の one-edge same-module
local-mode expansion chain pass/gap slice も prepared/implemented
であり検証済み。checker task 57 の same-module local-mode structure-RHS
evidence-query active fail slice と checker task 58 の same-module local-mode
attributed-builtin-RHS evidence-query active fail slice も prepared/implemented
であり検証済み。checker task 59 の same-module attributed local-mode reserve
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 60 の same-module attributed local-mode structure-RHS
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 61 の same-module attributed local-mode attributed-builtin-RHS
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 62 の same-module local-mode structure-RHS chain evidence-query
active fail slice も prepared/implemented であり検証済み。
checker task 63 の same-module local-mode attributed-RHS chain evidence-query
active fail slice も prepared/implemented であり検証済み。
checker task 64 の same-module attributed local-mode bare-builtin chain
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 65 の same-module attributed local-mode structure-RHS chain
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 66 の same-module attributed local-mode attributed-builtin-RHS chain
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 67 の structure-qualified attribute extraction-gap active boundary
slice も prepared/implemented であり検証済み。
checker task 68 の argument-bearing mode reserve extraction-gap active boundary
slice も prepared/implemented であり検証済み。
checker task 69 の argument-bearing structure reserve extraction-gap active boundary
slice も prepared/implemented であり検証済み。
checker task 70 の bracket-form local mode reserve extraction-gap active boundary
slice、checker task 71 の bracket-form local structure reserve extraction-gap active boundary
slice、checker task 72 の two-edge bare local-mode chain active pass slice、checker task 73 の three-edge bare local-mode chain active pass slice、checker task 74 の structural bare local-mode chain active pass slice、checker task 75 の lower-stage forward local-mode active-range boundary、checker task 76 の lower-stage forward local-structure active-range boundary、checker task 77 の lower-stage forward local-attribute active-range boundary も prepared/implemented であり検証済み。
checker task 48 の syntax-free reserve source declaration producer seam は
active type-elaboration runner が消費する checker-owned seam として
prepared/implemented である。
より広い checker semantic、CoreIr / ControlFlowIr、VC / proof-verification
coverage は不足している extraction seam に律速される。checker task 29、VC task 15、ATP task 20、kernel task 17 は deferred
trace rows または metadata-only handoff fixtures を持つ `paced/open` のままで、所有する
seam が存在する前に placeholder runner や fake active fixture は作らない。

### Task 11: Determinism suite

Purpose: discovery、plans、diagnostics、reports、snapshot comparisons、将来の
parallel-equivalence surface が byte-stable であることを検証する。

Spec refs: `harness.md`.

Completion: metadata plan と active runner report の canonical-byte stability
tests、`SurfaceAst` 外の generic snapshot nondeterminism diagnostics、
snapshot-level sequential/parallel observable equivalence 用の
`verify_snapshot_parallel_equivalence`。

### Task 12: Public-enum forward-compatibility policy

Purpose: crate API の public enum exhaustiveness/non-exhaustiveness を決め、
文書化する。

Spec refs: all module specs.

Completion: public `mizar-test` source tree のすべての public enum は downstream
`#[non_exhaustive]` であり、所有する英語/日本語 module spec は enum inventory と
decision を記録する。lint coverage は source attributes と EN/JA documentation
inventory の両方を検査する。exhaustive public enum exception は記録しない。

### Task 13: Bilingual documentation sync audit

Purpose: すべての English canonical document と Japanese companion を同期する。

Spec refs: repository documentation policy.

Completion: `bilingual_sync_audit.md` は `doc/design/mizar-test/en/` と
`doc/design/mizar-test/ja/` 配下の全 document に対する paired-file audit を記録する。
task 14 の完了は下に記録する。

### Task 14: Incremental/parallel verification regression matrix

Purpose: architecture-22 regression matrix scenarios の metadata/reporting
support を追加し、実行は consumer crate に置いたままにする。

Spec refs: architecture test strategy and incremental verification contract.

Completion: task 14 は `architecture22_scenarios`、
`architecture22_equivalence_class`、`architecture22_gate` sidecar metadata、
scenario registry、deterministic plan matrix report、18 個すべての required
architecture-22 row の metadata-only planned anchor である
`tests/property/architecture22_matrix_001` を追加した。task 14 scenario はすべて
active eligibility `none` であり、将来の consumer-specific increment が real
execution を所有するまで active gate を reject する。

### Task 15: Architecture-22 follow-up audit

Purpose: task 14 後に source/spec と bilingual documentation audit を再実行し、
残る matrix gap を記録する。

Spec refs: architecture test strategy、incremental verification contract、
repository documentation policy.

Completion: task 15 は task 14 後に source/spec と EN/JA documentation sync audit
を再実行した。`bilingual_sync_audit.md` は follow-up review を記録し、
MT-AUDIT-014 は残る architecture-22 active execution work を consumer-paced
`test_gap` と分類する。task 14 の scenario id、
equivalence class、planned-only gating、traceability anchor は architecture 20 と
22 に一致している。task 10 は consumer-paced のままで、新しく準備済みの runner
increment はない。`spec_gap`、`repo_metadata_conflict`、language behavior change、
既存 expectation の semantic change は不要である。

### Task 16: Source-derived builtin type-expression bridge

Purpose: builtin type expression だけについて `.miz` source から real
checker-owned payload を構築し、checker payload-extraction gap の最初の安全な部分を
閉じる。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、checker
MC-G020。

Completion: task 16 は `mizar-checker` を type-elaboration runner dependency として
追加し、reserve-only の unrecovered な builtin `set` / `object` `TypeExpression`
node を `TypeExpressionInput` に抽出し、`TypeNormalizer` で normalize し、最小の
`TypedAst` shell を組み立て、この対応済み slice の active pass coverage と
imported non-builtin type head の active boundary fail coverage を追加した。未対応の
declaration、term、formula、coercion、attribute、mode、structure、overload、
fact、CoreIr、ControlFlowIr、VC、proof seed は explicit external gap のままにする。

### Task 17: Source-derived builtin ResolvedTypedAst bridge

Purpose: 対応済み source-derived builtin type-expression runner slice を
checker-owned `TypedAst` shell から checker-owned `ResolvedTypedAst` へ昇格する。

Spec refs: `harness.md`、checker `resolved_typed_ast.md`、checker
MC-G020/MC-G027。

Completion: task 17 は task 16 の normalized builtin `set` / `object`
type-expression payload を、real expression metadata、source-preserved node hint、
empty cluster/overload predecessor output により `ResolvedTypedAst::assemble` へ
投影する。runner は対応済み source type site がすべて resolved node、
expression metadata、final type に diagnostic なしで到達することを確認する。
未対応の declaration、non-builtin type head、attribute、mode / structure expansion、
term、formula、coercion site、overload payload、recorded fact、proof evidence、
CoreIr、ControlFlowIr、VC seed、`proof_verification` row は explicit external gap のままにする。

### Task 18: Source-derived reserve declaration semantic bridge

Purpose: 対応済み source-derived type-elaboration pass slice を builtin
type-expression node から reserve-only builtin declaration payload へ広げる。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、checker
MC-G011/MC-G016/MC-G020。

Completion: task 18 は bare builtin `set` / `object` type head を持つ
unrecovered top-level reserve item を対応済み reserve declaration slice へ抽出する。
checker task 48 は checker-owned module `BindingEnv`、binding ごとの
`DeclarationInput`、binding 固有の `TypeExpressionInput` site、`DeclarationChecker`
output を構築する syntax-free producer seam を所有し、runner はその handoff を
`TypedAst`、`ResolvedTypedAst` へ継続する。
multi-variable reserve segment は共有 source type range を保つが、distinct typed
site を受け取る。未対応の non-builtin declaration、imported symbol、
argument-bearing / attributed symbol head、type-argument / term-argument provenance、
structure evidence payload、term、formula、coercion、overload payload、fact、
CoreIr / ControlFlowIr / VC payload、proof evidence は
explicit external gap のままである。real payload execution なしに downstream CoreIr /
ControlFlowIr / VC / proof trace row は昇格しない。

### Task 19: Reserve bridge core summary readiness and builtin declaration inventory

Purpose: real reserve-only checker payload を summary boundary で `mizar-core`
が読めることを確認し、未準備の semantics を昇格せずに次の builtin declaration
extraction slice を棚卸しする。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、core
`elaborator.md`。

Completion: task 19 は active runner 専用に `mizar-core` を type-elaboration
runner dependency として追加し、既存の reserve-only builtin `ResolvedTypedAst`
payload を `ResolvedTypedAstSummary::from_ast` に通し、successful active pass case
について source/module identity と empty checker-site output を確認する。inventory
では、まだ次の builtin declaration family は安全に昇格できないと記録する。`let`、
`given`、`consider`、quantified declaration は local scope、assumption、formula、
constraint-discharge payload を必要とし、`set` は RHS term inference、`reconsider`
は coercion / obligation evidence、`deffunc` / `defpred` は body / formal payload を
必要とする。`CoreIr`、`ControlFlowIr`、VC、proof、public diagnostic-code、`.miz`、
expectation semantic change は導入しない。

### Task 20: Reserve bridge core context readiness

Purpose: 同じ real reserve-only checker payload が次の安全な `mizar-core`
boundary を越え、binder-only core context data を準備できることを確認する。
`CoreIr` や downstream verification row は昇格しない。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、core
`elaborator.md`。

Completion: task 20 は active `type-elaboration` runner を拡張し、reserve-only
builtin `BindingEnv` と `ResolvedTypedAst` handoff から、抽出済み reserve binding
ごとに 1 個の `CoreVariableSeed` と `CoreBinderSeed` を持つ `CoreContextInput` を
構築し、`CoreItemSeed` は渡さない。runner は successful pass case について
source/module identity、binding count、binder source range、checker provenance、
empty item registry、empty core diagnostics、empty core worklist を確認する。
reserve declaration は owner item、term、formula、proof、
algorithm、obligation payload をまだ持たないため、`CoreIr`、`ControlFlowIr`、VC、
proof、public diagnostic-code、`.miz`、expectation semantic change は導入しない。

### Task 21: 訂正後経路の soundness 語彙

Purpose: kernel audit F7 の訂正後 kernel rejection vocabulary を
fail/soundness required-case registry と文書へ追加する。拒否挙動は変えない。

Spec refs: architecture test strategy と kernel `soundness_argument.md` F7。

Completion: task 21 は `invalid_sat_refutation`、`context_mismatch`、
`missing_provenance`、normal policy 下の unsupported legacy certificate に対する
訂正後 `soundness.certificate.*` registry key を追加し、legacy
`invalid_sat_proof` audit key を保持する。既存 corrected-path certificate sidecar は
`domain = "certificate"` とそれらの stable identity へ付け替えられた。proof、
certificate、kernel runner behavior は変えない。

Spec coverage audit impact: unchanged。traceability status、follow-up owner、
owner crate、deferred coverage rationale は変えない。

### Task 22: certificate corpus root 命名の調停

Purpose: kernel audit F8 を解決するため、architecture test strategy、kernel
audit note、certificate corpus README を、実装済み `tests/certificates/` root に
一致させる。

Spec refs: architecture test strategy と kernel `soundness_argument.md` F8。

Completion: task 22 は architecture 20(英日)、certificate corpus README、本
crate plan、kernel soundness argument(英日)を更新し、`tests/certificates/` を
certificate/kernel-evidence corpus の正準 root とし、`tests/kernel_evidence/` を
退役済みの監査 draft 名だけにする。Rust source、`doc/spec`、`.miz` tests、
expectations、runner behavior、traceability metadata、semantic intent は変更しない。

Spec coverage audit impact: spec chapter coverage、design mapping、follow-up
ownership、traceability status、deferred coverage rationale は変わらないため、
`doc/design/spec_coverage_audit.md` 更新は不要。

## Exit Criteria

Hard gates:

- blocking な `spec_gap`、採用すべき `repo_metadata_conflict`、language
  behavior change が隠れたまま残らない
- crate exit 時点で source behavior が undocumented のまま残らない
- task-owned gap は修正済み、または理由付きで明示的に deferred
- `doc/design/mizar-test/en/` と `doc/design/mizar-test/ja/` が同期している
- 将来の user request が明示的に意図を変えない限り、`doc/spec`、既存
  `.miz`、既存 expectation は触らない

Verification commands:

- `cargo test -p mizar-test`
- `cargo clippy -p mizar-test --all-targets -- -D warnings`
- `cargo fmt --check`

Review expectations:

- task-specific spec/docs review
- test sufficiency review
- implementation review
- source/documentation consistency review
