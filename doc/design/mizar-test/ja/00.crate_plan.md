# Crate Plan: mizar-test

> 正本は英語です。英語版: [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## 責務

所有する振る舞い:

- corpus payload と `.expect.toml` sidecar の決定的 discovery
- expectation metadata と traceability metadata の parse / validation
- staged-model admission と coverage accounting
- stage-owned compiler seam の active runner gate
- snapshot comparison/update policy と決定的 report surface
- 黙って pass、削除、弱体化してはならない fail/soundness case の bookkeeping

対象外:

- compiler language semantics
- 明示的な consumer-runner seam 以外での name resolution、type checking、
  proof search、ATP、certificate replay、kernel proof authority
- current implementation に合わせる目的での `doc/spec`、既存 `.miz`、
  既存 expectation の変更

## 仕様項目

| Spec ref | Requirement | Status |
|---|---|---|
| [layout.md](./layout.md) | known roots、payload/sidecar pairing、deterministic order、naming と sidecar rules | partial |
| [expectation_schema.md](./expectation_schema.md) | sidecar schema、outcome compatibility、fail identity、active runner metadata、transitional parse-only snapshots | partial |
| [staged_model.md](./staged_model.md) | canonical stage ids と prerequisite-gated coverage credit | partial |
| [traceability.md](./traceability.md) | manifest schema、sidecar backrefs、validation modes、coverage status、reporting | partial |
| [harness.md](./harness.md) | metadata plan、active runner modes、deterministic diagnostics/reports | partial |
| [miz_corpus.md](./miz_corpus.md) | corpus classes、generated/fuzz/stress metadata、review rules、pass/fail mix | partial |
| [snapshot.md](./snapshot.md) | snapshot kinds、public API、canonicalization、update policy、determinism checks | partial |
| [fail_soundness.md](./fail_soundness.md) | required soundness cases、expected-failure identity、weakening/deletion rules | partial |
| [minimal_crate.md](./minimal_crate.md) | metadata-only plan scope、CLI、validation、determinism、exit codes | partial |

## 関連テスト

| Test path | Intent | Spec refs |
|---|---|---|
| `crates/mizar-test/tests/lint_policy.rs` | workspace lint opt-in と inline `allow` rationale guard | repository conventions |
| `crates/mizar-test/tests/metadata.rs` | synthetic metadata/traceability fixtures、active runner gates、repository plan checks、transitional parse-only snapshots | mizar-test module specs |
| `crates/mizar-test/src/runner/tests.rs` と `src/runner/tests/` fragment | parse-only provider behavior、shared test support、および type-elaboration の source extraction、payload validation、fixture/corruption、cross-owner isolation behavior | harness.md |
| `tests/coverage/spec_trace.toml` | metadata plan が消費する requirement manifest | traceability.md |
| `tests/**/*.expect.toml` | discovery、schema validation、traceability、active runner gate が消費する sidecar metadata | expectation_schema.md |

Planned test-first additions:

- task 3: validation mode と exit code の CLI fixture
- task 4/5: snapshot canonicalization、update、determinism fixture
- task 6: traceability coverage/status/report fixture
- task 7: stage prerequisite violation fixture
- task 8: fail/soundness contract fixture（実装済み）
- task 9: corpus policy fixture
- task 16: source-derived builtin type-expression pass fixture と runner
  regression tests
- task 17: source-derived builtin `ResolvedTypedAst` handoff regression tests
- task 18: source-derived reserve declaration semantic bridge regression tests
- task 19: reserve bridge `ResolvedTypedAstSummary::from_ast` readiness
  regression tests と builtin declaration inventory
- checker task 56: source-derived one-edge local-mode expansion chain pass/gap
  fixtures と runner regression tests
- checker task 57: source-derived local-mode structure-RHS evidence-gap fixture
  と runner regression tests
- checker task 58: source-derived local-mode attributed-builtin-RHS evidence-gap
  fixture と runner regression tests
- checker task 59: source-derived attributed local-mode reserve evidence-gap
  fixture と runner regression tests
- checker task 60: source-derived attributed local-mode structure-RHS
  evidence-gap fixture と runner regression tests
- checker task 61: source-derived attributed local-mode attributed-builtin-RHS
  evidence-gap fixture と runner regression tests
- checker task 62: source-derived local-mode structure-RHS chain evidence-gap
  fixture と runner regression tests
- checker task 63: source-derived local-mode attributed-builtin-RHS chain
  evidence-gap fixture と runner regression tests
- checker task 64: source-derived attributed local-mode bare-builtin chain
  evidence-gap fixture と runner regression tests
- checker task 65: source-derived attributed local-mode structure-RHS chain
  evidence-gap fixture と runner regression tests
- checker task 66: source-derived attributed local-mode attributed-builtin-RHS
  chain evidence-gap fixture と runner regression tests
- checker task 67: source-derived structure-qualified attribute extraction-gap
  boundary fixture と runner regression tests
- checker task 68: source-derived argument-bearing mode reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 69: source-derived argument-bearing structure reserve
  extraction-gap boundary fixture と runner regression tests
- checker task 70: source-derived bracket-form local mode reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 71: source-derived bracket-form local structure reserve
  extraction-gap boundary fixture と runner regression tests
- checker task 72: source-derived two-edge bare local-mode chain pass fixture、checker task 73: source-derived three-edge bare local-mode chain pass fixture、checker task 74: structural bare local-mode chain pass fixture、および runner regression tests
- checker task 75: source-derived local-mode forward-reference active-range
  boundary fixture と runner regression tests
- checker task 76: source-derived local-structure forward-reference active-range
  boundary fixture と runner regression tests
- checker task 77: source-derived local-attribute forward-reference active-range
  boundary fixture と runner regression tests
- checker task 78: source-derived imported structure reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 79: source-derived imported mode reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 80: source-derived imported attribute reserve extraction-gap
  boundary fixture と runner regression tests
- checker task 82: source-derived imported mode reserve provenance bridge
  fixture と runner regression tests
- checker task 83: source-derived imported structure reserve provenance bridge
  fixture と runner regression tests
- checker task 84: source-derived imported attribute reserve provenance bridge
  fixture と runner regression tests

## design と source の inventory

Design files:

- `doc/design/mizar-test/en/*.md`
- `doc/design/mizar-test/ja/*.md`

Source files:

- `crates/mizar-test/src/lib.rs`
- `crates/mizar-test/src/layout.rs`
- `crates/mizar-test/src/path_rules.rs`
- `crates/mizar-test/src/expectation.rs`
- `crates/mizar-test/src/staged_model.rs`
- `crates/mizar-test/src/traceability.rs`
- `crates/mizar-test/src/harness.rs`
- `crates/mizar-test/src/runner.rs`
- `crates/mizar-test/src/runner/tests.rs`
- `crates/mizar-test/src/runner/tests/support.rs`
- `crates/mizar-test/src/runner/tests/parse_only.rs`
- `crates/mizar-test/src/runner/tests/type_elaboration/source_extraction.rs`
- `crates/mizar-test/src/runner/tests/type_elaboration/reserved_binary.rs`
- `crates/mizar-test/src/runner/tests/type_elaboration/mode_chain.rs`
- `crates/mizar-test/src/snapshot.rs`
- `crates/mizar-test/src/main.rs`
- `crates/mizar-test/src/diagnostic.rs`
- `crates/mizar-test/src/toml_lite.rs`

Observed behavior:

- metadata `plan` discovery、sidecar parsing、trace manifest parsing、
  duplicate id checks、missing source/sidecar checks、deterministic plan ordering
  は実装・テスト済み
- active `parse-only`、`declaration-symbol`、`type-elaboration` runner は
  explicit active-tag gate と repository coverage を持つ
- active `type-elaboration` runner は対応済み source-derived reserve declaration
  slice について checker-owned `BindingEnv`、reserve `DeclarationInput`、
  binding 固有の builtin `set` / `object` `TypeExpressionInput`、
  `DeclarationChecker` output まで checker-owned syntax-free task-48 seam を通し、
  その後 `TypedAst`、`ResolvedTypedAst` assembly まで継続し、
  その real payload を `mizar-core` の `ResolvedTypedAstSummary::from_ast` で読み、
  同じ real reserve binding から binder-only `CoreContext` を準備し、
  same-module attributed builtin reserve payload は checker `MissingEvidenceQuery`
  diagnostic まで、same-module local mode reserve head は checker missing
  mode-expansion diagnostic まで、same-module local structure reserve head は checker
  `MissingEvidenceQuery` diagnostic まで、attributed same-module local structure reserve head は
  checker `MissingEvidenceQuery` diagnostic まで、supported real expansion がないか mixed bare
  reserve use を持つ attributed same-module local mode reserve head は checker missing
  mode-expansion diagnostic まで、bare same-module no-argument local mode expansion は
  preceding bare-builtin RHS mode definition から active pass reserve fixture まで抽出し、
  dependency mode が accepted bare-builtin RHS expansion を持つ one-edge same-module
  local-mode chain も抽出し、RHS が same-module local structure head である real
  same-module local-mode expansion は missing base-shape / constructor-witness evidence
  の checker `MissingEvidenceQuery` diagnostic まで抽出し、RHS が attributed builtin
  head である real same-module local-mode expansion は missing attributed-type
  existential evidence の checker `MissingEvidenceQuery` diagnostic まで抽出し、
  real direct bare-builtin expansion を持つ attributed same-module local-mode reserve head は
  missing attributed-type existential evidence の同じ checker `MissingEvidenceQuery` diagnostic
  まで抽出し、real direct local-structure RHS expansion を持つ attributed
  same-module local-mode reserve head は missing base-shape / constructor-witness と
  full attributed-type evidence の同じ checker `MissingEvidenceQuery` diagnostic まで抽出し、
  real direct attributed-builtin RHS expansion を持つ attributed
  same-module local-mode reserve head は missing full attributed-type evidence の同じ
  checker `MissingEvidenceQuery` diagnostic まで抽出し、local-structure、
  attributed-builtin、bare-builtin attributed-root、structure-RHS attributed-root、
  attributed-builtin-RHS attributed-root terminal payload で終端する one-edge same-module
  local-mode chain は対応する missing evidence-query diagnostic まで抽出し、
  later declaration を名前参照する forward same-module local-mode reserve head は
  checker handoff 前の lower-stage active-range failure として報告し、
  mixed import、imported attribute / structure、task 82 の provenance bridge を超える
  imported mode expansion と未対応の semantic / CoreIr / ControlFlowIr / VC / proof payload family は
  external-gap key のままにする
- architecture-22 matrix sidecar metadata は validate / report され、owning consumer が
  active clean/incremental/parallel/cache-race execution を提供するまで planned-only
  scenario coverage として扱われる
- transitional active parse-only `SurfaceAst` snapshot text baseline は
  validate / compare される
- general snapshot records、update mode、determinism mode、coverage reporting、
  pass/fail mix reporting、manifest ordering、obsolete-reference checks は実装済み
- stage prerequisite validation は実装済み
- fail/soundness metadata bookkeeping は certificate/kernel `rejection_reason`、
  認識済み required soundness case の shape/profile/phase、mode-aware な missing-case
  diagnostic を検証する。active proof/certificate/kernel execution は将来の
  consumer runner に律速される。

## 既知のギャップと drift

| ID | Class | Evidence | Action |
|---|---|---|---|
| MT-AUDIT-001 | resolved design_drift | task 238 は `layout.md` を public `mizar_test::layout` discovery surface と同期し、parsed case と metadata の harness/expectation ownership を記録する。 | layout surface が変わるとき discovery、missing-sidecar diagnostic、unknown-root inventory、cross-module ownership wording の同期を保つ。 |
| MT-AUDIT-002 | resolved | task 9 は generated/fuzz/property `[origin]` metadata を parse / retain し、reproducibility fields と schema fixtures を追加した。 | 新しい provenance fields を追加するとき origin schema docs を同期する。 |
| MT-AUDIT-003 | resolved / watch | task 3 は `profiles`、`notes`、`ast_profile`、`snapshot_profiles` を保持し、返却される plan case を `TestProfile` で filter する。task 9 は generated/fuzz/stress corpus profile gates を追加した。 | 新しい profiles が入るとき profile policy を同期する。 |
| MT-AUDIT-004 | resolved / runner watch | task 8 は certificate/kernel `rejection_reason` を必須化し、public diagnostic code が無い runner の `stable_detail_key` fallback identity を保ち、case-level fail/soundness metadata gate を追加した。 | proof/certificate/kernel runner が着地するとき active runner matching を同期する。 |
| MT-AUDIT-005 | partial source_drift | task 4 と task 5 は general in-memory snapshot record API、canonicalization、hashing、validation、comparison、explicit baseline verify/update IO、repeat-render determinism helper を実装する。general `[[snapshots]]` sidecar parsing と runner integration は pending。 | 残る snapshot registry/runner integration は expectation/harness follow-up tasks で実装する。 |
| MT-AUDIT-006 | resolved / design_drift watch | task 7 は stage id、`depends_on`、`built_in` requirement に対する declared prerequisite gates を実装した。より広い corpus admission policy は task 9 / follow-up work として残る。 | 新しい stage が executable になるたび staged-model docs を同期する。 |
| MT-AUDIT-007 | resolved / watch | task 6 は mode-aware coverage completeness、computed/stored status comparison、obsolete-reference checks を実装した。task 7 は `depends_on` と prerequisite credit gates を実装した。 | 新しい evidence kind が入るたび coverage/prerequisite gates を同期する。 |
| MT-AUDIT-008 | resolved | trace manifest deterministic ordering は文書化済み。metadata ordering conflict は `897d549` で修復済みで、task 6 が validator coverage を追加した。 | manifest ordering rules が変わらない限り追加対応なし。 |
| MT-AUDIT-009 | resolved / design_drift watch | task 3 は `harness.md` を更新し、exported metadata plan と runner-specific reports を示し、generic outcome/snapshot reporting を future API と明記する。 | 後続で generic reporting surface が入るたび docs を同期する。 |
| MT-AUDIT-010 | resolved | task 3 は狭い `parser.type_fixtures` symbol-injection exception を文書化する。 | fixture provider が変わらない限り追加対応なし。 |
| MT-AUDIT-011 | partial source_drift / test_gap | task 5 と task 11 は general snapshot records、deterministic hashing、repeat-render determinism checks、snapshot-level parallel equivalence、plan/report byte-stability tests を実装した。fuzz-regression と update runner modes は future active-runner work として残る。 | 残る runner modes は owning consumer/update flows が存在するまで deferred のままにする。 |
| MT-AUDIT-012 | resolved / watch | task 6 は corpus-wide pass/fail mix を report する。task 9 は generated reproducibility metadata、fuzz category preservation、stress exclusion、provenance metadata、corpus size/naming/placement review rules を validate する。 | 新しい corpus classes が入るとき corpus policy validation を同期する。 |
| MT-AUDIT-013 | resolved / watch | task 3 は focused expectation-schema fixtures を追加し、task 6 は manifest/source/link/deferred/planned-warning traceability fixtures を追加した。 | 将来の schema fields と fixture coverage を同期し続ける。 |
| MT-AUDIT-014 | test_gap | task 14 は 18 個すべての architecture-22 regression row を planned metadata として記録し、active gate を reject する。source-derived clean/incremental/parallel/cache-race execution の prepared consumer runner increment は確認できていないため、execution を捏造せずに active row は実行できない。 | consumer-specific source-derived increment が存在するまで、consumer-paced external dependency として matrix は planned/open のままにする。次の source-derived semantic bridge は、row を active に昇格する前に prepared seam を棚卸しする。 |
| MT-AUDIT-015 | partial source_drift / external_dependency_gap | task 16 から task 81 までは builtin reserve / type-expression source bridge、checker-owned `ResolvedTypedAst` projection、reserve declaration handoff、supported same-module bare local-mode expansion family、および local structure / attribute / argument / bracket / forward-reference / imported-attribute の active diagnostic boundary を段階的に追加した。checker task 82 は documented `parser.type_fixtures` import summary 由来 imported mode symbol の provenance/type-head handoff だけを昇格し、checker missing mode-expansion diagnostic まで到達させる。checker task 83 は documented imported structure `R` の provenance/type-head handoff だけを昇格し、checker missing structure-evidence query まで到達させる。checker task 97 は documented imported structure `TypeCaseStruct` の provenance/type-head handoff だけを同じ checker missing structure-evidence query まで昇格する。checker task 84 は documented imported attribute `TypeCaseAttr` の provenance / `AttributeInput` handoff だけを昇格し、checker missing attributed-type evidence diagnostic まで到達させる。checker task 85 は documented imported negative `empty`/builtin-`set` provenance / `AttributeInput` handoff だけを昇格し、同じ checker missing attributed-type evidence diagnostic まで到達させる。checker task 116 は documented imported positive `empty`/builtin-`set` provenance / `AttributeInput` handoff だけを昇格し、同じ checker missing attributed-type evidence diagnostic まで到達させる。checker task 86 は formula-only theorem source を active parser/resolver-to-checker extraction-gap boundary として記録し、checker task 117 は checker task 115 を exact `FormulaPayloadBoundary: thesis` source について supersede し、source-derived `thesis` formula constant を real `FormulaKind::Thesis` checker payload として渡して missing formula payload で fail closed し、checker task 106 は exact builtin equality theorem source について real checker term/formula payload を渡し、missing numeric type payload と partial formula checking で fail closed し、checker task 110 は task 98 の exact imported predicate/functor theorem formula source を supersede して real checker numeral、imported functor-application、predicate-application payload を渡し、missing numeric/signature payload と partial formula checking で fail closed し、checker task 108 は exact builtin membership theorem source について real checker term/formula payload を渡し、missing numeric type payload と partial formula checking で fail closed し、checker task 107 は exact builtin inequality theorem source について real checker term/formula payload を渡し、missing numeric type payload と partial formula checking で fail closed し、checker task 109 は exact builtin type-assertion theorem source について real checker term/formula/asserted-type payload を渡し、missing numeric type payload と partial formula checking で fail closed し、checker task 113 は exact imported attribute assertion theorem formula について task 103 を supersede し、imported `empty` provenance と checker term/formula payload handoff を記録して missing numeric type payload、missing formula/attribute semantic payload、partial formula checking で fail closed し、checker task 114 は exact attribute-level non-empty imported attribute assertion theorem formula について task 104 を supersede して checker term/formula payload handoff を記録し、missing numeric type payload、missing formula/attribute semantic payload、partial formula checking で fail closed し、checker task 111 は checker task 105 の exact set-enumeration theorem source を supersede して real checker term/formula payload を渡し、missing numeric/result-type payload と partial formula checking で fail closed し、checker task 112 は checker task 99 の exact connective/quantifier theorem formula source を supersede して real checker formula shell payload を渡し、missing formula/quantifier payload で fail closed し、checker task 117 は同じ exact source の real `FormulaKind::Contradiction` constant payload も渡し、checker task 180 は exact standalone `SourceDerivedContradictionConstantBoundary: contradiction` leaf を type/well-formedness だけの 1 `Checked` `FormulaKind::Contradiction` として check し、checker task 88 は proof-block theorem source を同じ active extraction-gap boundary として記録し、checker task 89 は statement-level proof-justification theorem source を同じ active extraction-gap boundary として記録し、checker task 90 は predicate/functor definition source を同じ active parser/resolver-to-checker extraction-gap boundary として記録する。checker task 91 は attribute definition source を同じ active parser/resolver-to-checker extraction-gap boundary として記録する。checker task 92 は mode/structure definition source を同じ active parser/resolver-to-checker extraction-gap boundary として記録する。checker task 93 は proof-local declaration statement source を同じ active parser/resolver-to-checker extraction-gap boundary として記録する。checker task 94 は proof-local inline definition source を同じ active parser/resolver-to-checker extraction-gap boundary として記録する。checker task 95 は registration block source を同じ active parser/resolver-to-checker extraction-gap boundary として記録する。checker task 96 は redefinition/notation source を同じ active parser/resolver-to-checker extraction-gap boundary として記録する。AST-wide non-builtin declaration（task 96 の redefinition/notation extraction-gap boundary を超えるもの）、task 84 `TypeCaseAttr` bridge、task 85 negative `empty`/builtin-`set` bridge、task 116 positive `empty`/builtin-`set` bridge、task 80 boundary を超える imported attribute、task 83 `R` provenance bridge と task 97 `TypeCaseStruct` provenance bridge を超える imported structure、task 82 の provenance bridge を超える imported mode expansion payload、attribute argument、qualified attribute provenance、mode / structure argument、bracket `type_arg_list` と `qua`-argument provenance、term-argument provenance、structure base-shape と full attributed-type existential evidence、broader / attributed / argument-bearing / parameterized / contextual / ambiguous / cyclic mode expansion と task 74 structural guard を満たさない chain、task-92 extraction-gap boundary を超える definition payload、task-93 extraction-gap boundary を超える proof-local declaration payload、task-94 extraction-gap boundary を超える inline definition payload、task-95 extraction-gap boundary を超える registration payload / correctness-condition / activation payload、task-96 extraction-gap boundary を超える redefinition/notation payload、task-106/task-107/task-108/task-109/task-110/task-111/task-113/task-114 を超える numeric/signature/result-type payload と equality/inequality/membership/type-assertion/imported predicate-functor/set-enumeration semantic checking、task-180 exact standalone contradiction type/well-formedness slice を超える formula-constant semantics、task-112/task-117 を超える formula child/binder semantics、および task-105/task-88/task-89 extraction-gap boundary と task-112/task-113/task-114/task-117 checker bridge を超える imported predicate/functor semantic payload、set-enumeration term payload extraction、membership operand expected-type construction/checking、inequality desugaring または equality semantic checking、broader type-assertion type payload extraction、type-assertion semantic checking、imported attribute assertion attribute-chain/provenance payload extraction、imported attribute-level non-empty assertion attribute-chain/provenance payload extraction、negated attribute admissibility/semantic checking、attribute admissibility/semantic checking、quantifier binder/context payload、term / formula / theorem / proof skeleton / statement proof payload、coercion、overload payload、fact、proof evidence、CoreIr、ControlFlowIr、VC extraction はまだ利用できない。 | 未対応 semantic / CoreIr / ControlFlowIr / VC / proof payload family は deferred のままにする。prepared consumer execution が確認できた場合だけ traceability row を昇格し、未実装 runner、task-84 `TypeCaseAttr` bridge、task-85 negative `empty`/builtin-`set` bridge、task-116 positive `empty`/builtin-`set` bridge、task-82 imported-mode provenance slice、task-83 imported-structure `R` provenance slice、task-97 imported-structure `TypeCaseStruct` provenance slice、task-84 imported-attribute `TypeCaseAttr` provenance slice、task-85 imported negative `empty`/builtin-`set` provenance slice、task-116 imported positive `empty`/builtin-`set` provenance slice、task-106 builtin equality theorem checker bridge、task-107 builtin inequality theorem checker bridge、task-108 builtin membership theorem checker bridge、task-109 builtin type assertion theorem checker bridge、task-110 imported predicate/functor theorem checker bridge、task-111 set-enumeration theorem checker bridge、task-112 formula connective/quantifier shell checker bridge、task-86 parser/resolver formula-only reachability boundary、task-105 theorem/formula extraction-gap boundary、および task-113/task-114/task-117 checker bridge、task-88 proof-skeleton extraction-gap boundary、task-89 statement-proof extraction-gap boundary、task-92 mode/structure definition extraction-gap boundary、task-93 proof-local declaration extraction-gap boundary、task-94 proof-local inline definition extraction-gap boundary、task-95 registration block extraction-gap boundary、task-96 redefinition/notation extraction-gap boundary を超える imported / ambiguous symbol、imported mode expansion、mixed attributed/bare local-mode use、task 74 structural guard 外の broader mode-expansion payload、qualified attribute payload、type/term-argument payload、bracket / `qua`-argument payload、existential evidence、task-83/task-97 missing-evidence diagnostic を超える structure evidence、task-84/task-85/task-116 missing-evidence diagnostic を超える attributed-type evidence、proof-local declaration payload、inline definition payload、registration payload、future declaration payload の active fixture を捏造しない。 |
| MT-AUDIT-016 | partial source_drift / external_dependency_gap | task 19 は real reserve-only builtin `ResolvedTypedAst` payload が `mizar-core` の `ResolvedTypedAstSummary::from_ast` で読め、source/module identity が保たれ、checker site が出ないことを確認する。次の builtin declaration inventory では安全な昇格は見つかっていない。`let` / `given` / `consider` / quantified declaration は local scope、assumption、formula、constraint-discharge payload を必要とし、`set` は RHS term inference、`reconsider` は coercion / obligation evidence、`deffunc` / `defpred` は body / formal payload を必要とする。 | prepared active runner seam が real payload を raw reconstruction や fake evidence なしに実行できるまで、これらの declaration family は source-to-checker extraction gap に残す。summary-readiness を `CoreIr`、`ControlFlowIr`、VC、proof execution として扱わない。 |
| MT-AUDIT-017 | partial source_drift / external_dependency_gap | task 20 は同じ real reserve-only builtin checker payload が、抽出済み reserve binding ごとに 1 個の `CoreVariableSeed` と `CoreBinderSeed` を持ち `CoreItemSeed` を持たない `mizar-core` binder-only `CoreContext` preparation を seed できることを確認する。runner は source/module identity、binder source range、provenance、empty item registry、empty diagnostics、empty core worklist を確認する。reserve declaration は owner item、term、formula、proof skeleton、algorithm shell、obligation seed をまだ提供しない。 | real source-derived payload が各 consumer に存在するまで、`CoreIr`、`ControlFlowIr`、VC、proof row、publication/orchestration、より広い semantic payload family は deferred のままにする。 |
| MT-AUDIT-018 | resolved design_drift | task 22 は architecture 20 の退役済み `tests/kernel_evidence/` directory 名と実装済み `tests/certificates/` corpus root を調停し、corpus README を更新し、kernel audit F8 を解決済みとして記録する。 | `tests/certificates/` を certificate/kernel-evidence corpus の正準 root として維持する。spec chapter coverage、follow-up owner、traceability status、deferred rationale は変わらないため、`doc/design/spec_coverage_audit.md` 更新は不要。 |
| MT-AUDIT-019 | 解決済み test_gap / design_drift | task 21 は audit F7 の訂正後 kernel rejection vocabulary を required soundness-case registry と paired docs に追加し、既存 corrected-path certificate sidecar を `soundness.certificate.*` stable key へ付け替える。 | rejection vocabulary が変化するたび registry と certificate corpus を同期する。traceability status、owner crate、deferred rationale は変わらないため、`doc/design/spec_coverage_audit.md` 更新は不要。 |
| MT-AUDIT-020 | 解決済み test_gap | task 240 は sort 済み raw `DiscoveredLayout` payload/sidecar inventory、単独 missing-known-root warning の identity、既知 root と通常 file を除外した複数 unknown-root inventory の sort を直接検証する。 | payload extension または known root が変わるとき direct public-module test を同期する。language/spec coverage、traceability、runner ownership、deferred rationale は変わらないため `doc/design/spec_coverage_audit.md` 更新は不要。 |
| MT-AUDIT-021 | 解決済み source_drift | task 239 は到達不能な `E-LAYOUT-BAD-SIDECAR-NAME` branch を削除する。discovery は UTF-8 name が `.expect.toml` で終わる場合だけ sidecar として受理し、削除した branch は同じ predicate を再検査していた。 | 文書どおり non-payload file を無視し続ける。authority が candidate namespace を定義しない限り malformed-sidecar-name rejection を捏造しない。task 240 は MT-AUDIT-020 の direct-test gap を独立して解決する。known root が変わるとき coverage を同期する。language/spec coverage、traceability、runner ownership、deferred rationale は変わらないため `doc/design/spec_coverage_audit.md` 更新は不要。 |
| MT-AUDIT-022 | active design_drift | Task 248 は `src/runner.rs` が public facade、phase-specific production helper、pre-module test-only helper 137件、272 tests を持つ約94,120行の private module 1個を混在させていることを記録する。 | [module_boundary_audit.md](./module_boundary_audit.md) の Tasks 249-264 を behavior-preserving/nonempty/move-only increment として実行する。API、test list、count、diagnostic、detail key、payload、ordering、fail-closed behavior を保持する。specification coverage、design mapping、traceability status、owner crate、follow-up ownership、deferred rationale は変わらないため `doc/design/spec_coverage_audit.md` 更新は不要。 |

Task 81 addendum: `mizar-test` は argument-bearing local-attribute bridge
boundary の active runner metadata も所有する。active declaration-symbol
fixture は resolver 由来の parameterized local attribute suffix (`ranked`) を
exported primary symbol として記録し、prefixed surface (`2 - ranked`) は notation
provenance として保持する。対応する active type-elaboration fixture は
`checker.type_elaboration.payload_extraction.argument_bearing_attribute_gap`
の fail-closed checker extraction gap を記録する。この addendum は checker
`AttributeInput` argument payload、attributed evidence、CoreIr、ControlFlowIr、
VC、proof payload を昇格しない。それらは real consumer payload と runner support
が存在するまで deferred のままとする。

Task 82 addendum: `mizar-test` は imported-mode reserve provenance bridge の
active runner metadata も所有する。既存の imported-mode type-elaboration
fixture は、`parser.type_fixtures` から real imported `SymbolKind::Mode`
symbol を checker type head へ渡すところだけを credit してよく、imported mode
expansion payload が存在するまでは
`type_elaboration.checker.checker.type.external.mode_expansion_payload` で
fail closed しなければならない。
この addendum は imported module AST extraction、positive imported mode
elaboration、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 83 addendum: `mizar-test` は imported-structure reserve provenance bridge
の active runner metadata も所有する。既存の imported-structure
type-elaboration fixture は、`parser.type_fixtures` の structure `R` について
real imported `SymbolKind::Structure` symbol を checker type head へ渡すところ
だけを credit してよく、imported module AST extraction と
base-shape/constructor-witness evidence が存在するまでは
`type_elaboration.checker.checker.declaration.deferred.evidence_query` で
fail closed しなければならない。その documented `R` fixture 外の imported
structure は task-78 extraction-gap boundary に残る。この addendum は positive
imported structure elaboration、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 97 addendum: `mizar-test` は documented `parser.type_fixtures` structure
`TypeCaseStruct` の imported-structure reserve provenance bridge についても active
runner metadata を所有する。active fixture は real imported
`SymbolKind::Structure` symbol を checker type head へ渡すところだけを credit してよく、
imported module AST extraction と base-shape / constructor-witness evidence が存在する
までは `type_elaboration.checker.checker.declaration.deferred.evidence_query` で
fail closed しなければならない。task-83 `R` と task-97 `TypeCaseStruct` fixture
外の imported structure は task-78 extraction-gap boundary に残る。この addendum は
positive imported structure elaboration、CoreIr、ControlFlowIr、VC、proof payload を
昇格しない。

Task 84 addendum: `mizar-test` は imported-attribute reserve provenance bridge
の active runner metadata も所有する。既存の imported-attribute
type-elaboration fixture は、`parser.type_fixtures` の attribute `TypeCaseAttr`
について real imported `SymbolKind::Attribute` symbol を builtin `set` 上の
checker `AttributeInput` へ渡すところだけを credit してよく、imported module AST
extraction と attributed-type evidence query が存在するまでは
`type_elaboration.checker.checker.declaration.deferred.evidence_query` で
fail closed しなければならない。その documented `TypeCaseAttr set` fixture 外の
imported attribute は task-80 extraction-gap boundary に残る。この addendum は
positive imported attribute elaboration、attribute argument、qualified/owner
attribute provenance、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 85 / task 116 / task 171 addendum: `mizar-test` は imported empty attribute reserve
provenance bridge の active runner metadata も所有する。既存の `non empty set` と
`empty set`、`non empty object` type-elaboration fixture は、
`parser.type_fixtures` の attribute `empty` について real imported
`SymbolKind::Attribute` symbol を builtin `set` 上の negative / positive checker
`AttributeInput`、または builtin `object` 上の exact negative `AttributeInput` として
渡すところだけを credit
してよく、imported module AST extraction と attributed-type evidence query が存在するまでは
`type_elaboration.checker.checker.declaration.deferred.evidence_query` で
fail closed しなければならない。positive `empty object`、symbol head 上の
imported `empty`、documented `TypeCaseAttr set` と task-85/task-116/task-171 exact
fixture 外の imported attribute は task-80 extraction-gap boundary に残る。

Task 86 / task 115 / task 117 addendum: `mizar-test` は formula-only theorem boundary の
active runner metadata を所有する。task 115 は exact
`FormulaPayloadBoundary: thesis` sidecar を supersede し、source-derived
`thesis` formula constant site/range を checker recovery `FormulaInput` として渡す。
task 117 は同じ source を real `FormulaKind::Thesis` payload に進めてから、
missing formula payload で fail closed する。この
addendum は formula constant semantic checking、child-formula graph payload、
recorded fact、theorem acceptance、proof skeleton、`formula_statement` runner
support、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 106 addendum: `mizar-test` は task-87 generic boundary の exact
term-bearing builtin equality theorem sidecar を supersede する。runner は
`theorem TermFormulaPayloadBoundary: 1 = 1;` を `type_elaboration` で parser /
resolver まで実行し、real source-derived checker `TermInput` / `FormulaInput`
payload を module-shell binding context の下で構築し、missing numeric type payload
と partial formula checking で fail closed してよい。この addendum は numeric type
payload、equality semantic checking、recorded fact、theorem acceptance、proof
skeleton、`formula_statement` runner support、CoreIr、ControlFlowIr、VC、proof
payload を昇格しない。

Task 98 addendum: `mizar-test` は imported predicate/functor theorem formula
extraction-gap boundary の historical active runner metadata も所有する。task 110
は exact `ImportedPredicateFunctorPayloadBoundary: 1 divides (1 ++ 2)` sidecar
だけを supersede し、`parser.type_fixtures` import と imported `divides` / `++`
resolver provenance を検証し、source-derived checker numeral、imported
functor-application、predicate-application payload を `type_elaboration` で構築し、
missing numeric/signature payload と partial formula checking で fail closed
してよい。この addendum は imported module AST extraction、
predicate/functor semantic signature、term inference、formula checking、recorded
fact、theorem acceptance、dedicated `formula_statement` runner、CoreIr、
ControlFlowIr、VC、proof payload を昇格しない。

Task 100 addendum: `mizar-test` は builtin membership theorem formula
extraction-gap boundary の active runner metadata も所有する。task 108 は exact
sidecar を supersede し、`theorem BuiltinMembershipPayloadBoundary: 1 in 1;`
を `type_elaboration` で parser / resolver まで実行し、real source-derived
checker `TermInput` / `FormulaInput` payload を module-shell binding context の
下で構築し、missing numeric type payload と partial formula checking で fail
closed してよい。この addendum は numeric type payload、membership operand
expected-type construction/checking、recorded fact、theorem acceptance、proof
skeleton、`formula_statement` runner support、CoreIr、ControlFlowIr、VC、proof
payload を昇格しない。

Task 107 addendum: `mizar-test` は task-101 generic boundary の exact builtin
inequality theorem sidecar を supersede する。runner は
`theorem BuiltinInequalityPayloadBoundary: 1 <> 2;` を `type_elaboration` で
parser / resolver まで実行し、real source-derived checker `TermInput` /
`FormulaInput` payload を module-shell binding context の下で構築し、missing
numeric type payload と partial formula checking で fail closed してよい。この
addendum は numeric type payload、inequality desugaring / equality semantic
checking、recorded fact、theorem acceptance、proof skeleton、`formula_statement`
runner support、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 118 addendum: `mizar-test` は task 106/107/108 が共有する builtin-binary
theorem producer を厳密化し、direct theorem token が exact
`theorem <label> : ;` である場合だけ equality、membership、inequality config を
選ぶ。status-prefixed または extra-token theorem shape は
`type_elaboration.external_dependency.ast_payload_extraction` に残る。この
guard-only repair は active `.miz` sidecar coverage、traceability coverage、spec
coverage credit、新しい checker payload support を追加しない。

Tasks 119-169 current-state override: この paragraph と以下の sibling task
paragraph は先行 MT-AUDIT-015 master row の authoritative qualification であり、
generic な term/formula unavailable wording は promoted exact Tasks 119-169
reserved-variable slice を超える payload を指す。`mizar-test` は task-119 exact active pass source
`reserve x for set; theorem ReservedVariableEqualityPayloadBoundary: x = x;`
を所有する。runner は既存の real reserve handoff と identifier-term / equality
extraction を組み合わせ、2 つの `x` term を source 順から導出した別々の use
ordinal で checker-owned `BindingEnv` に問い合わせ、
記述された builtin `set` type を distinct source-anchored result/expected role
site に投影する。run は 2 つの `Inferred` variable term と 1 つの `Checked`
equality formula を candidate/diagnostic/deferred/fact なしで生成する。
production validation は full payload invariant を強制し、real frontend/resolver
unit test は active sidecar 背後の同じ payload を観測する。credit するのは
type/well-formedness だけであり、implicit closure、equality truth/fact、theorem
acceptance、`formula_statement`、proof、CoreIr、ControlFlowIr、VC は deferred の
ままである。

Task 123 current-state override: `mizar-test` は exact active pass source
`reserve x, y for set; theorem DistinctReservedVariableEqualityPayloadBoundary: x = y;`
を所有する。runner は real two-binding reserve handoff を再利用し、binding
identity を collapse せずに共有された記述上の builtin `set` type range を保持し、
両 binding の後の source-order use ordinal を導出して、`x` と `y` を distinct
checker binding に解決する。operand ごとの result/expected role は 2
`Inferred` variable と 1 fact-free `Checked` equality を empty
candidate/deferred/diagnostic output とともに生成する。production validation、
near-miss matrix、real frontend/resolver sidecar test が exact slice を guard する。
generic MT-AUDIT-015 term/formula gap は task 119 の same-binding equality と task
123 の distinct-binding equality、および他の promoted exact sibling を超える
coverage を指す。implicit closure/order node、equality truth/fact、theorem
acceptance、proof、CoreIr、ControlFlowIr、VC は deferred のままである。

Task 130 current-state override: `mizar-test` は exact active pass source
`LocalModeInequality -> set`、reserve 1 個、
`LocalModeReservedVariableInequalityPayloadBoundary: x <> x;` だけを所有する。
runner は 4 raw mode input を保持し、real expansion を `TermFormulaChecker` に
渡して RHS 起点の builtin-set identity 1 個と fact-free pre-desugaring
`Checked` inequality 1 個を要求し、non-exact shape は fail closed とする。
declaration acceptance/inhabitation、desugaring、truth/fact、theorem acceptance、
proof/Core/ControlFlow/VC は deferred のままである。

Task 131 current-state override: `mizar-test` は exact active pass source
`LocalObjectModeInequality -> object`、reserve 1 個、
`LocalObjectModeReservedVariableInequalityPayloadBoundary: x <> x;` だけを
所有する。runner は 4 raw object-mode input を保持し、real expansion を
`TermFormulaChecker` に渡して RHS 起点の builtin-object identity 1 個と fact-free
pre-desugaring `Checked` inequality 1 個を要求し、non-exact shape は fail closed
とする。mode declaration acceptance/inhabitation、desugaring、closure/order、
truth/fact、theorem acceptance、proof/Core/ControlFlow/VC は deferred のままである。

Task 132 current-state override: `mizar-test` は exact active pass source
`ChainModeInequality -> BaseModeInequality -> set`、outer reserve 1 個、
`ChainedLocalModeReservedVariableInequalityPayloadBoundary: x <> x;` だけを
所有する。runner は 4 raw outer-mode input を保持し、real expansion 2 本を
`TermFormulaChecker` に渡して terminal-RHS builtin-set identity 1 個と fact-free
pre-desugaring `Checked` inequality 1 個を要求し、non-exact link/block/formula
shape は fail closed とする。mode declaration acceptance/inhabitation、
object-terminal、direct/longer shape、desugaring、closure/order、truth/fact、
theorem acceptance、proof/Core/ControlFlow/VC は deferred のままである。

Task 133 current-state override: `mizar-test` は exact active pass source
`ChainObjectModeInequality -> BaseObjectModeInequality -> object`、outer reserve
1 個、`ChainedLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;`
だけを所有する。runner は 4 raw outer-mode input を保持し、real expansion 2 本を
`TermFormulaChecker` に渡して terminal-RHS builtin-object identity 1 個と
fact-free pre-desugaring `Checked` inequality 1 個を要求し、non-exact
link/block/formula shape は fail closed とする。declaration
acceptance/inhabitation、set-terminal、direct/longer shape、desugaring、closure/order、truth/fact、
theorem acceptance、proof/Core/ControlFlow/VC は deferred のままである。

Task 134 current-state override: `mizar-test` は exact active pass source
`OuterTwoEdgeModeEquality -> MiddleTwoEdgeModeEquality -> BaseTwoEdgeModeEquality -> set`、
outer reserve 1 個、`TwoEdgeLocalModeReservedVariableEqualityPayloadBoundary: z = z;`
だけを所有する。runner は 4 raw outer-mode input を保持し、real expansion 3 本を
`TermFormulaChecker` に渡して terminal-RHS builtin-set identity 1 個と fact-free
`Checked` equality 1 個を要求し、non-exact link/block/formula shape は fail closed
とする。declaration acceptance/inhabitation、object-terminal、direct、one-edge、
longer shape、implicit closure/order、truth/fact、theorem acceptance、
proof/Core/ControlFlow/VC は deferred のままである。

Task 135 current-state override: `mizar-test` は exact active pass source
`OuterTwoEdgeObjectModeEquality -> MiddleTwoEdgeObjectModeEquality -> BaseTwoEdgeObjectModeEquality -> object`、
outer reserve 1 個、
`TwoEdgeLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;` だけを
所有する。runner は 4 raw outer-mode input を保持し、real expansion 3 本を
`TermFormulaChecker` に渡して terminal-RHS builtin-object identity 1 個と
fact-free `Checked` equality 1 個を要求し、non-exact link/block/formula shape は
fail closed とする。declaration acceptance/inhabitation、task 134 を超える
set-terminal semantics、direct、one-edge、longer shape、implicit closure/order、
truth/fact、theorem acceptance、proof/Core/ControlFlow/VC は deferred のままである。

Task 136 current-state override: `mizar-test` は exact active pass source
`OuterTwoEdgeModeInequality -> MiddleTwoEdgeModeInequality -> BaseTwoEdgeModeInequality -> set`、
outer reserve 1 個、
`TwoEdgeLocalModeReservedVariableInequalityPayloadBoundary: z <> z;` だけを
所有する。runner は 4 raw outer-mode input を保持し、real expansion 3 本を
`TermFormulaChecker` に渡して terminal-RHS builtin-set identity 1 個と fact-free
pre-desugaring `Checked` inequality 1 個を要求し、non-exact link/block/formula
shape は fail closed とする。mode declaration acceptance/inhabitation、
object-terminal、direct、one-edge、longer shape、inequality desugaring、implicit
closure/order、truth/fact、theorem acceptance、proof/Core/ControlFlow/VC は
deferred のままである。

Task 137 current-state override: `mizar-test` は exact active pass source
`OuterTwoEdgeObjectModeInequality -> MiddleTwoEdgeObjectModeInequality -> BaseTwoEdgeObjectModeInequality -> object`、
outer reserve 1 個、
`TwoEdgeLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;`
だけを所有する。runner は 4 raw outer-mode input を保持し、real expansion 3 本を
`TermFormulaChecker` に渡して terminal-RHS builtin-object identity 1 個と
fact-free pre-desugaring `Checked` inequality 1 個を要求し、non-exact
link/block/formula shape は fail closed とする。declaration
acceptance/inhabitation、set-terminal、direct、one-edge、longer shape、inequality
desugaring、implicit closure/order、truth/fact、theorem acceptance、
proof/Core/ControlFlow/VC は deferred のままである。

Task 138 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare mode definition 1 個
`LocalModeTypeAssertion -> set`、その mode の reserve 1 個、
`LocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;` から成る
exact active source だけを所有する。runner は raw local-mode subject-result
source と独立した formula-side builtin-set asserted source を保持し、real
expansion 1 本を checker に渡し、RHS-anchored normalized builtin-set identity 1
個、`BindingId(0)`、1 `Inferred` term、1 fact-free `Checked` type assertion を
要求する。non-exact mode/definition/reserve/formula shape はすべて fail closed
とする。formula-side local-mode asserted-head extraction、mode declaration
acceptance/inhabitation、general reachability/widening/`qua`、fact、
theorem/proof、Core/ControlFlow/VC は deferred のままである。production bridge
と real frontend/resolver sidecar が pass したため active runner count は 89 件である。

Task 139 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare mode definition 1 個
`LocalModeMembership -> set`、その mode の `x` と explicit `set` の `y` から成る
ordered reserve、`LocalModeReservedVariableMembershipPayloadBoundary: x in y;`
から成る exact active source だけを所有する。runner は raw local-mode left-result
source と独立した right builtin-set result/expected source を保持し、real
expansion 1 本を checker に渡して、RHS-anchored normalized builtin-set identity 1
個、`BindingId(0/1)`、2 `Inferred` term、1 fact-free `Checked` membership、right
だけの expected constraint を要求する。non-exact
mode/definition/reserve/formula shape はすべて fail closed とする。unit coverage
は sole real expansion と independently reserve-derived right expected-`set`
projection/constraint を独立に corrupt し、どちらも dedicated invalid-payload
key を報告することを要求する。exact case は left expected input なしと
right-owned formula constraint 1 個だけを証明する。mode declaration
acceptance/inhabitation、membership truth/fact、implicit
closure/order、theorem/proof、Core/ControlFlow/VC は deferred のままである。
production bridge と real frontend/resolver sidecar が pass したため active
runner count は 90 件である。

Task 140 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare mode definition 1 個
`LocalObjectModeMembership -> object`、その mode の `x` と explicit `set` の
`y` から成る ordered reserve、
`LocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;` から成る
exact active source だけを所有する。runner は raw local object-mode left-result
source と独立した right builtin-set result/expected source を保持し、real
expansion 1 本を checker に渡して、RHS-anchored builtin-object identity と
explicit-reserve-anchored builtin-set identity、`BindingId(0/1)`、2 `Inferred`
term、1 fact-free `Checked` membership、right だけの expected constraint を要求する。
non-exact mode/definition/reserve/formula shape はすべて fail closed とする。unit
coverage は sole real expansion と independently reserve-derived right
expected-`set` projection/constraint を独立に corrupt し、どちらも dedicated
invalid-payload key を報告することを要求する。exact case は 2 semantic
identity、left expected input なし、right-owned formula constraint 1 個だけを
証明する。mode declaration acceptance/inhabitation、membership truth/fact、
object/set coercion、implicit closure/order、theorem/proof、
Core/ControlFlow/VC は deferred のままである。production bridge と real
frontend/resolver sidecar が pass したため active runner count は 91 件である。

Task 141 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare mode definition 2 個
`ChainModeMembership -> BaseModeMembership -> set`、outer mode の `x` と
explicit `set` の `y` から成る ordered reserve、
`ChainedLocalModeReservedVariableMembershipPayloadBoundary: x in y;` から成る
exact active source だけを所有する。runner は raw outer-mode left-result source
と独立した right builtin-set result/expected source を保持し、real expansion 2
本を checker に渡して、terminal-RHS-anchored builtin-set identity 1 個、
`BindingId(0/1)`、2 `Inferred` term、1 fact-free `Checked` membership、right
だけの expected constraint を要求する。non-exact
mode/definition/reserve/formula shape はすべて fail closed とする。unit coverage
は real chain link 2 本と independently reserve-derived right expected-`set`
projection/constraint を独立に corrupt し、すべて dedicated invalid-payload key
を報告することを要求する。exact case は semantic identity 1 個、left expected
input なし、right-owned formula constraint 1 個だけを証明する。mode declaration
acceptance/inhabitation、membership truth/fact、implicit closure/order、
theorem/proof、Core/ControlFlow/VC は deferred のままである。production bridge
と real frontend/resolver sidecar が pass したため active runner count は 92 件である。

Task 142 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare mode definition 2 個
`ChainObjectModeMembership -> BaseObjectModeMembership -> object`、outer mode
の `x` と explicit `set` の `y` から成る ordered reserve、
`ChainedLocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;`
から成る exact source だけを所有する。runner は raw outer-mode left-result
source と独立した right builtin-set result/expected source を保持し、real
expansion 2 本を checker に渡して、distinct terminal-RHS-anchored
builtin-object / explicit-reserve-anchored builtin-set identity、
`BindingId(0/1)`、2 `Inferred` term、1 fact-free `Checked` membership、right
だけの expected constraint、left expected type なしを要求する。non-exact
mode/definition/reserve/formula shape はすべて fail closed とする。特に unit
coverage は base definition の wrong label、chain definition の wrong label、
argument-bearing chain radix
`ChainObjectModeMembership is BaseObjectModeMembership(...)` を独立に reject
しなければならない。また real chain link 2 本と independently reserve-derived right expected-`set`
projection/constraint を独立に corrupt し、すべて dedicated invalid-payload
key を報告することを要求する。exact case は semantic identity 2 個、left
expected input なし、right-owned formula constraint 1 個だけを証明する。mode
declaration acceptance/inhabitation、membership truth/fact、object/set coercion、
implicit closure/order、theorem/proof、Core/ControlFlow/VC は deferred のまま
である。production bridge と real frontend/resolver sidecar が pass したため
active runner count は 93 件である。

Task 124 current-state override: `mizar-test` は exact active pass source
`reserve x for set; reserve y for set; theorem MultipleReserveDeclarationEqualityPayloadBoundary: x = y;`
を所有する。runner は exactly two ordered reserve item だけを受理し、2 binding
identity と 2 distinct written builtin `set` range を保持し、それらの range を
operand ごとの 4 pre-normalization result/expected input に保持する。checker は
semantically equal input を deterministic な最初の source representative を持つ
1 normalized type に intern する。production validation は original input を独立に
検証するため、semantic identity と written provenance を混同しない。exact
near-miss matrix、task-specific invalid key、real frontend/resolver sidecar が bridge
を guard する。MT-AUDIT-015 は引き続き broader term/formula shape 全体を cover する。
implicit closure/order、equality truth/fact、theorem acceptance、proof、CoreIr、
ControlFlowIr、VC は deferred のままである。

Task 125 current-state override: `mizar-test` は exact active pass source
`reserve x for object; reserve y for set; theorem HeterogeneousReserveMembershipPayloadBoundary: x in y;`
を所有する。binary bridge は binding-specific builtin shape を検証する。左の
source-derived `object` result と右の source-derived `set` result/expected input を
保持し、右の両 role が `set` を共有する exactly two normalized identity を要求する。
production invariant は identity partition と type ごとの deterministic source
representative を検証し、task invalid key、exact near-miss matrix、real
frontend/resolver sidecar が slice を guard する。generic MT-AUDIT-015 gap は broader
term/formula shape を引き続き cover する。membership truth/fact、object/set coercion
evidence、implicit closure/order、theorem acceptance、proof、CoreIr、ControlFlowIr、
VC は deferred のままである。

Task 126 current-state override: `mizar-test` は direct bare-set
`LocalModeFormula` definition 1 個、その reserve、
`LocalModeReservedVariableEqualityPayloadBoundary: x = x;` の exact active pass
source を所有する。4 raw input は mode provenance を保持し、real AST-derived
expansion は `TermFormulaChecker` に渡され、1 builtin-set identity と fact-free
checked equality を生成する。exact guard、invalid key、near miss、real sidecar が
slice を cover する。mode acceptance/inhabitation、broader mode、closure/order、
truth/fact、theorem acceptance、proof、CoreIr、ControlFlowIr、VC は deferred のまま。

Task 127 current-state override: `mizar-test` は separate `BaseModeFormula -> set` と
`ChainModeFormula -> BaseModeFormula` definition block、outer-mode reserve 1 個、
`ChainedLocalModeReservedVariableEqualityPayloadBoundary: x = x;` を持つ exact
active pass source だけを所有する。runner は 4 raw outer-mode input を保持し、2
real AST-derived expansion を `TermFormulaChecker` に渡し、1 terminal builtin-set
identity を terminal RHS に anchor し、non-exact block/chain shape はすべて fail closed にする。mode
acceptance/inhabitation、object terminal、longer-chain formula、closure/order、
truth/fact、theorem acceptance、proof、CoreIr、ControlFlowIr、VC は deferred のまま。

Task 128 current-state override: `mizar-test` は task 55 の direct
`LocalObjectMode -> object` definition、reserve 1 個、
`LocalObjectModeReservedVariableEqualityPayloadBoundary: x = x;` を持つ exact
active pass source だけを所有する。runner は 4 raw object-mode input を保持し、real
expansion を `TermFormulaChecker` に渡して real RHS 由来の 1 terminal
builtin-object identity を要求し、non-exact mode/formula shape は fail closed にする。
mode acceptance/inhabitation、closure/order、truth/fact、theorem acceptance、proof、
CoreIr、ControlFlowIr、VC は deferred のまま。

Task 129 current-state override: `mizar-test` は task 56 の
`ChainObjectMode -> BaseObjectMode -> object` definition に outer-mode reserve 1 個と
`ChainedLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;` を追加する
exact active pass source だけを所有する。runner は 4 raw outer-mode input を保持し、
両 real expansion を `TermFormulaChecker` に渡して 1 terminal-RHS
builtin-object identity を要求し、non-exact block/chain/formula shape は fail closed
にする。mode acceptance/inhabitation、closure/order、truth/fact、theorem
acceptance、proof、CoreIr、ControlFlowIr、VC は deferred のままである。

Task 143 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare mode definition 3 個
`OuterTwoEdgeModeMembership -> MiddleTwoEdgeModeMembership -> BaseTwoEdgeModeMembership -> set`、
outer mode の `x` と explicit `set` の `y` から成る ordered reserve、
`TwoEdgeLocalModeReservedVariableMembershipPayloadBoundary: x in y;` から成る
exact active source だけを所有する。runner は raw outer-mode left-result source
と独立した right builtin-set result/expected source を保持し、real expansion 3
本を checker に渡して、terminal-RHS-anchored builtin-set identity 1 個、
`BindingId(0/1)`、2 `Inferred` term、1 fact-free `Checked` membership、right
だけの expected constraint を要求する。non-exact mode/definition/reserve/
formula shape はすべて fail closed とする。unit coverage は base / middle /
outer definition の wrong label と、両 chain link の argument-bearing radix を
独立に拒否する。また real expansion 3 本と right expected-`set` projection/
constraint を独立に corrupt し、すべて dedicated invalid-payload key を報告する。
mode declaration acceptance/inhabitation、membership truth/fact、implicit
closure/order、theorem/proof、Core/ControlFlow/VC は deferred のままである。
production bridge と real frontend/resolver sidecar が pass したため active
runner count は 94 件である。

Task 144 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare mode definition 3 個
`OuterTwoEdgeObjectModeMembership -> MiddleTwoEdgeObjectModeMembership -> BaseTwoEdgeObjectModeMembership -> object`、
outer mode の `x` と explicit `set` の `y` から成る ordered reserve、
`TwoEdgeLocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;` から
成る exact active source だけを所有する。runner は raw outer-mode
left-result source と独立した right builtin-set result/expected source を保持し、
real expansion 3 本を checker に渡して、terminal object-RHS-anchored
builtin-object identity と explicit-reserve-anchored builtin-set identity、
`BindingId(0/1)`、2 `Inferred` term、1 fact-free `Checked` membership、right
だけの expected constraint を要求する。non-exact mode/definition/reserve/
formula shape はすべて fail closed とする。unit coverage は base / middle /
outer definition の wrong label と、両 chain link の argument-bearing radix を
独立に拒否する。また real expansion 3 本と right expected-`set` projection/
constraint を独立に corrupt し、すべて dedicated task-144 invalid-payload key
を報告する。mode declaration acceptance/inhabitation、membership truth/fact、
object/set coercion、implicit closure/order、theorem/proof、
Core/ControlFlow/VC は deferred のままである。production bridge と real
frontend/resolver sidecar が pass したため active runner count は 95 件である。

Task 145 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-object mode definition 1 個
`LocalObjectModeTypeAssertion -> object`、その mode の reserve `x` 1 個、
`LocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`
から成る exact active source だけを所有する。runner は raw
local-mode subject result と独立した formula-side builtin-object asserted
source を保持し、real expansion 1 本を checker に渡して、terminal-RHS-
anchored builtin-object identity 1 個、`BindingId(0)`、source-order use ordinal
1、1 `Inferred` term、1 fact-free `Checked` type assertion を general
reachability / object-set coercion なしで要求する。non-exact definition/
reserve/formula shape はすべて fail closed とする。unit coverage は wrong
definition label を独立に拒否し、real expansion corruption が dedicated
task-145 invalid-payload key を報告することを要求する。mode declaration
acceptance/inhabitation、formula-side local-mode asserted-head extraction、
general reachability/widening/`qua`、truth/fact、implicit closure/order、
theorem/proof、Core/ControlFlow/VC は deferred のままである。production
bridge と real frontend/resolver sidecar が pass したため active runner count
は 96 件である。

Task 146 current-state override: `mizar-test` は unique、unrecovered、source-
preceding、no-argument bare-set mode definition 2 個
`ChainModeTypeAssertion -> BaseModeTypeAssertion -> set`、outer mode の reserve
`x` 1 個、
`ChainedLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`
から成る exact active source だけを所有する。runner は raw outer-
mode subject result と独立した formula-side builtin-set asserted source を保持
し、real expansion 2 本を checker に渡して、terminal-RHS-anchored builtin-set
identity 1 個、`BindingId(0)`、source-order use ordinal 1、1 `Inferred` term、1
fact-free `Checked` type assertion を general reachability なしで要求する。
non-exact definition/reserve/formula shape はすべて fail closed とする。unit
coverage は両 wrong definition label を独立に拒否し、real expansion entry 2
本の corruption が dedicated task-146 invalid-payload key を報告することを
要求する。mode declaration acceptance/inhabitation、formula-side local-mode
asserted-head extraction、general reachability/widening/`qua`、truth/fact、
implicit closure/order、theorem/proof、Core/ControlFlow/VC は deferred のまま
である。production bridge と real frontend/resolver sidecar が pass したため
active runner count は 97 件である。

Task 147 current-state override: `mizar-test` は unique、unrecovered、source-
preceding、no-argument bare-object mode definition 2 個
`ChainObjectModeTypeAssertion -> BaseObjectModeTypeAssertion -> object`、outer
mode の reserve `x` 1 個、
`ChainedLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`
から成る exact active source だけを所有する。runner は raw outer-
mode subject result と独立した formula-side builtin-object asserted source を
保持し、real expansion 2 本を checker に渡して、terminal-RHS-anchored
builtin-object identity 1 個、`BindingId(0)`、source-order use ordinal 1、1
`Inferred` term、1 fact-free `Checked` type assertion を general reachability /
object-set coercion なしで要求する。non-exact definition/
reserve/formula shape はすべて fail closed とする。unit coverage は両 wrong
definition label を独立に拒否し、real expansion entry 2 本の corruption が
dedicated task-147 invalid-payload key を報告する。mode
declaration acceptance/inhabitation、formula-side local-mode asserted-head
extraction、general reachability/widening/`qua`、truth/fact、implicit closure/
order、theorem/proof、Core/ControlFlow/VC は deferred のままである。production
bridge と real frontend/resolver sidecar が pass したため active runner count
は 98 件である。

Task 148 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-set mode definition 3 個
`OuterTwoEdgeModeTypeAssertion -> MiddleTwoEdgeModeTypeAssertion -> BaseTwoEdgeModeTypeAssertion -> set`、
outer mode の reserve `x` 1 個、
`TwoEdgeLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`
から成る exact active source だけを所有する。runner は raw outer-mode subject result
と独立した formula-side builtin-set asserted source を保持し、real expansion
3 本を checker に渡して、terminal-RHS-anchored builtin-set identity 1 個、
`BindingId(0)`、source-order use ordinal 1、1 `Inferred` term、1 fact-free
`Checked` type assertion を general reachability なしで要求する。non-exact
definition/reserve/formula shape はすべて fail closed とする。unit coverage
は wrong definition label 3 個を独立に拒否し、real expansion entry 3 本の
corruption が dedicated task-148 invalid-payload key を報告する。mode
declaration acceptance/inhabitation、formula-side local-mode asserted-head
extraction、general reachability/widening/`qua`、truth/fact、implicit closure/
order、theorem/proof、Core/ControlFlow/VC は deferred のままである。
production bridge と real frontend/resolver sidecar が pass したため active
runner count は 99 件である。

Task 149 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-object mode definition 3 個
`OuterTwoEdgeObjectModeTypeAssertion -> MiddleTwoEdgeObjectModeTypeAssertion -> BaseTwoEdgeObjectModeTypeAssertion -> object`、
outer mode の reserve `x` 1 個、
`TwoEdgeLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`
から成る exact active source だけを所有する。runner は raw outer-mode
subject result と独立した formula-side builtin-object asserted source を保持し、
real expansion 3 本を checker に渡して、terminal-RHS-anchored builtin-object
identity 1 個、`BindingId(0)`、source-order use ordinal 1、1 `Inferred` term、
1 fact-free `Checked` type assertion を general reachability / object-set
coercion なしで要求する。non-exact definition/reserve/formula
shape はすべて fail closed とする。unit coverage は wrong definition label 3 個
を独立に拒否し、real expansion entry 3 本の corruption が dedicated task-149
invalid-payload key を報告する。mode declaration acceptance/
inhabitation、formula-side local-mode asserted-head extraction、general
reachability/widening/`qua`、truth/fact、implicit closure/order、theorem/proof、
Core/ControlFlow/VC は deferred のままである。production route と real
frontend/resolver sidecar が pass したため active
runner count は 100 件である。

Task 150 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-set mode definition 4 個
`OuterThreeEdgeModeTypeAssertion -> MiddleThreeEdgeModeTypeAssertion -> InnerThreeEdgeModeTypeAssertion -> BaseThreeEdgeModeTypeAssertion -> set`、
outer mode の reserve `x` 1 個、
`ThreeEdgeLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`
から成る exact source だけを所有する。runner は raw outer-mode subject result
と独立した formula-side builtin-set asserted source を保持し、real task 73
compatible expansion 4 本を checker に渡して、terminal-RHS-anchored builtin-
set identity 1 個、`BindingId(0)`、source-order use ordinal 1、1 `Inferred`
term、1 fact-free `Checked` type assertion を general reachability なしで要求
しなければならない。non-exact definition/reserve/formula shape はすべて fail
closed とする。unit coverage は wrong definition label 4 個、wrong chain radix
3 個を独立に拒否し、real expansion entry 4 本の corruption が dedicated task-
150 invalid-payload key を報告しなければならない。mode declaration
acceptance/inhabitation、formula-side local-mode asserted-head extraction、
general reachability/widening/`qua`、truth/fact、implicit closure/order、theorem/
proof、Core/ControlFlow/VC は deferred のままである。production route と
real frontend/resolver sidecar が pass したため active runner count は 101 件である。

Task 151 current-state override: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-object mode definition 4 個
`OuterThreeEdgeObjectModeTypeAssertion -> MiddleThreeEdgeObjectModeTypeAssertion -> InnerThreeEdgeObjectModeTypeAssertion -> BaseThreeEdgeObjectModeTypeAssertion -> object`、
outer mode の reserve `x` 1 個、
`ThreeEdgeLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`
から成る exact source だけを所有する。runner は raw outer-mode subject result
と独立した formula-side builtin-object asserted source を保持し、real task 73
compatible expansion 4 本を checker に渡して、terminal-RHS-anchored builtin-
object identity 1 個、`BindingId(0)`、source-order use ordinal 1、1 `Inferred`
term、1 fact-free `Checked` type assertion を general reachability / object-set
coercion なしで要求しなければならない。non-exact definition/reserve/formula
shape はすべて fail closed とする。unit coverage は wrong definition label 4 個、
wrong chain radix 3 個を独立に拒否し、real expansion entry 4 本の corruption が
dedicated task-151 invalid-payload key を報告しなければならない。mode
declaration acceptance/inhabitation、formula-side local-mode asserted-head
extraction、general reachability/widening/`qua`、object/set coercion、truth/fact、
implicit closure/order、theorem/proof、Core/ControlFlow/VC は deferred のままで
ある。production route と real frontend/resolver sidecar が pass したため
active runner count は 102 件である。

Task 152 current-state override: `mizar-test` は unique、unrecovered、source-
preceding、no-argument bare-set mode definition 5 個
`TooDeepFourEdgeModeTypeAssertion -> OuterFourEdgeModeTypeAssertion -> MiddleFourEdgeModeTypeAssertion -> InnerFourEdgeModeTypeAssertion -> BaseFourEdgeModeTypeAssertion -> set`、
outermost mode の reserve `x` 1 個、
`FourEdgeLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;`
から成る exact source だけを所有する。runner は raw outermost-mode
subject result と独立した formula-side builtin-set asserted source を保持し、real
task 74 compatible expansion 5 本を checker に渡して、terminal-RHS-anchored
builtin-set identity 1 個、`BindingId(0)`、source-order use ordinal 1、1
`Inferred` term、1 fact-free `Checked` type assertion を general reachability
なしで要求する。non-exact definition/reserve/formula shape はすべて fail closed
とする。unit coverage は wrong definition label 5 個、wrong chain radix 4 個を
独立に拒否し、real expansion entry 5 本の corruption が dedicated task-152
invalid-payload key を報告しなければならない。mode declaration acceptance/
inhabitation、formula-side local-mode asserted-head extraction、general
reachability/widening/`qua`、truth/fact、implicit closure/order、theorem/proof、
Core/ControlFlow/VC は deferred のままである。production route と real
frontend/resolver sidecar が pass したため active runner count は 103 件である。

Task 153 current-state override: `mizar-test` は unique、unrecovered、source-
preceding、no-argument bare-object mode definition 5 個
`TooDeepFourEdgeObjectModeTypeAssertion -> OuterFourEdgeObjectModeTypeAssertion -> MiddleFourEdgeObjectModeTypeAssertion -> InnerFourEdgeObjectModeTypeAssertion -> BaseFourEdgeObjectModeTypeAssertion -> object`、
outermost mode の reserve `x` 1 個、
`FourEdgeLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`
から成る exact active source だけを所有する。runner は raw outermost-mode
subject result と独立した formula-side builtin-object asserted source を保持し、
real task 74 compatible expansion 5 本を checker に渡して、terminal-RHS-
anchored builtin-object identity 1 個、`BindingId(0)`、source-order use ordinal
1、1 `Inferred` term、1 fact-free `Checked` type assertion を general
reachability/object-set coercion なしで要求する。non-exact definition/reserve/
formula shape は fail closed とする。wrong definition label 5 個、wrong chain
radix 4 個、real expansion corruption 5 本を独立に guard し、corruption は
dedicated task-153 invalid-payload key を報告する。declaration acceptance/
inhabitation、formula-side local asserted-head、general reachability/widening/
`qua`、object/set coercion、truth/fact、closure/order、theorem/proof/Core/
ControlFlow/VC は deferred のままである。production route と real sidecar が
pass したため active runner count は 104 件である。

Task 154 implementation specification: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-set mode definition 4 個
`OuterThreeEdgeModeEquality -> MiddleThreeEdgeModeEquality -> InnerThreeEdgeModeEquality -> BaseThreeEdgeModeEquality -> set`、
outer mode の reserve `z` 1 個、
`ThreeEdgeLocalModeReservedVariableEqualityPayloadBoundary: z = z;` から成る
test-first exact source だけを所有する。runner は raw outer-mode result/expected
input 4 個を保持し、両 operand を ordinal 1、2 で `BindingId(0)` へ解決し、real
task 73 compatible expansion 4 本を checker に渡して terminal-RHS builtin-set
identity 1 個、2 `Inferred` variable、1 fact/deferred-free `Checked` equality を
要求する。全 definition label/radix/expansion entry と withheld family を guard
する。mode declaration acceptance/inhabitation、equality truth/fact、closure/order、
theorem/proof/Core/ControlFlow/VC は deferred のままである。fixture、expectation、
trace row、production route、full near-miss/corruption matrix、real frontend/
resolver sidecar が slice を guard するため active runner count は 105 件である。

Task 155 implementation specification: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-object mode definition 4 個
`OuterThreeEdgeObjectModeEquality -> MiddleThreeEdgeObjectModeEquality -> InnerThreeEdgeObjectModeEquality -> BaseThreeEdgeObjectModeEquality -> object`、
outer mode の reserve `z` 1 個、
`ThreeEdgeLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;`
から成る test-first exact source だけを所有する。runner は raw outer-mode
result/expected input 4 個を保持し、両 operand を ordinal 1、2 で
`BindingId(0)` へ解決し、real task 73 compatible expansion 4 本を checker に
渡して terminal-RHS builtin-object identity 1 個、2 `Inferred` variable、1
fact/deferred-free `Checked` equality を要求する。全 definition label/radix/
expansion entry と withheld family を guard する。mode declaration acceptance/
inhabitation、object/set coercion、equality truth/fact、closure/order、theorem/
proof/Core/ControlFlow/VC は deferred のままである。fixture、expectation、trace
row、production route、full near-miss/corruption matrix、real frontend/resolver
sidecar が slice を guard するため active runner count は 106 件である。

Task 156 implementation specification: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-set mode definition 4 個
`OuterThreeEdgeModeInequality -> MiddleThreeEdgeModeInequality -> InnerThreeEdgeModeInequality -> BaseThreeEdgeModeInequality -> set`、
outer mode の reserve `z` 1 個、
`ThreeEdgeLocalModeReservedVariableInequalityPayloadBoundary: z <> z;` から成る
test-first exact source だけを所有する。runner は raw outer-mode result/
expected input 4 個を保持し、両 operand を ordinal 1、2 で `BindingId(0)` へ
解決し、real task 73 compatible expansion 4 本を checker に渡して terminal-
RHS builtin-set identity 1 個、2 `Inferred` variable、1 fact/deferred-free pre-
desugaring `Checked` inequality を要求する。全 definition label/radix/expansion
entry と withheld family を guard する。mode declaration acceptance/
inhabitation、inequality desugaring、truth/fact、closure/order、theorem/proof/
Core/ControlFlow/VC は deferred のままである。fixture、expectation、trace row、
production route、full near-miss/corruption matrix、real frontend/resolver
sidecar が slice を guard するため active runner count は 107 件である。

Task 157 implementation specification: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-object mode definition 4 個
`OuterThreeEdgeObjectModeInequality -> MiddleThreeEdgeObjectModeInequality -> InnerThreeEdgeObjectModeInequality -> BaseThreeEdgeObjectModeInequality -> object`、
outer mode の reserve `z` 1 個、
`ThreeEdgeLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;`
から成る test-first exact source だけを所有する。runner は raw outer-mode
result/expected input 4 個を保持し、両 operand を ordinal 1、2 で
`BindingId(0)` へ解決し、real task 73 compatible expansion 4 本を checker に
渡して terminal-RHS builtin-object identity 1 個、2 `Inferred` variable、1
fact/deferred-free pre-desugaring `Checked` inequality を要求する。全 definition
label/radix/expansion entry と withheld family を guard する。mode declaration
acceptance/inhabitation、object/set coercion、inequality desugaring、truth/fact、
closure/order、theorem/proof/Core/ControlFlow/VC は deferred のままである。
fixture、expectation、trace row、production route、full near-miss/corruption
matrix、real frontend/resolver sidecar が contract を guard するため active
runner count は 108 件である。

Task 158 implementation specification: `mizar-test` は unique、unrecovered、
source-preceding、no-argument bare-set mode definition 4 個
`OuterThreeEdgeModeMembership -> MiddleThreeEdgeModeMembership -> InnerThreeEdgeModeMembership -> BaseThreeEdgeModeMembership -> set`、
outer mode の `x` と explicit `set` の `y` から成る ordered reserve、
`ThreeEdgeLocalModeReservedVariableMembershipPayloadBoundary: x in y;` から
成る exact active source だけを所有する。runner は raw outer-mode left
result と独立した explicit-set right result/sole expected input を保持し、left
expected input は持たず、ordinal 2/3 で `BindingId(0/1)` へ解決し、real task-
73 expansion 4 本を `TermFormulaChecker` に渡して terminal-RHS builtin-set
identity 1 個、2 `Inferred` variable、1 fact/deferred-free `Checked` membership、
exactly one right-owned expected-type constraint を要求する。全 definition
label/radix/expansion entry を独立に guard し、withheld chain、terminal、reserve、
formula、recovery、context、parameter、argument、cycle、depth、extra-item family
は extraction gap のままにする。mode declaration acceptance/inhabitation、
membership truth/fact、closure/order、theorem/proof/Core/ControlFlow/VC、object-
terminal behavior、broader semantics は deferred のままである。fixture、
expectation、trace row、production route、full near-miss/corruption matrix、
real frontend/resolver sidecar が active contract を guard するため active
runner count は 109 件である。Chapter 3 の新規 coverage credit はない。

Task 159 current-state override: `mizar-test` は task 123 の real one-
item/two-binding/shared-range reserve producer と tasks 120/125 の right-only
expected-set membership consumer を exact
`reserve x, y for set; theorem DistinctReservedVariableMembershipPayloadBoundary: x in y;`
だけについて compose しなければならない。runner は source-order ordinal
2/3 の `BindingId(0/1)` と、両 binding および left result、right result、sole
right expected input にわたる written builtin-set range 1 個を保持し、left
expected input は持たない。この raw role 3 個は 2 `Inferred` variable と 1
fact/deferred-free `Checked` membership、exactly one right-owned expected-type
constraint を生成する前に shared-source-anchored builtin-set identity 1 個へ
intern されなければならない。exact source guard、full near-miss/corruption
matrix、real frontend/resolver sidecar が active route を保護し、separate reserve declaration、
non-set type、broader multi-name reserve/formula shape、membership truth/fact、
closure/order、theorem/proof、Core/ControlFlow/VC は deferred のままにする。
active runner count は 110 件である。Chapter 3 の新規 coverage credit はない。

Task 160 current-state override: `mizar-test` は task 123 の real one-item/two-
binding/shared-range reserve producer と task 121 の pre-desugaring inequality
consumer を exact `reserve x, y for set; theorem
DistinctReservedVariableInequalityPayloadBoundary: x <> y;` だけについて compose
しなければならない。runner は source-order ordinal 2/3 の `BindingId(0/1)` と、
両 binding および left/right result/expected role 4 個にわたる written builtin-set
range 1 個を保持しなければならない。これらは 2 `Inferred` variable と 2 ordered
operand-owned constraint を持つ 1 fact/deferred-free `Checked` inequality の前に
shared-source-anchored builtin-set identity 1 個へ intern される。exact source/
route guard、full near-miss/corruption matrix、real frontend/resolver sidecar が
active route を保護し、separate declaration、non-set type、broader reserve/formula shape、
desugaring/truth/fact、closure/order、theorem/proof、Core/ControlFlow/VC は deferred
のままにする。active runner は 111 件である。Chapter 3 の新規 coverage credit
はない。

Task 161 current-state override: `mizar-test` は task 124 の real two-item/two-
binding/distinct-written-range reserve producer と task 160 の pre-desugaring
inequality consumer を exact `reserve x for set; reserve y for set; theorem
MultipleReserveDeclarationInequalityPayloadBoundary: x <> y;` だけについて compose
しなければならない。runner は ordinal 2/3 の `BindingId(0/1)`、first reserve
range を left result/expected role、second range を right role に保持する。4 role
は earlier `x` range に canonical anchor された builtin-set identity 1 個へ intern
されてから、2 `Inferred` variable と 2 ordered constraint を持つ 1 fact/deferred-
free `Checked` inequality を生成する。exact source/route guard、full near-miss/
corruption coverage、real frontend/resolver sidecar が active route を保護する。shared range、
non-set type、broader shape、desugaring/truth/fact、closure/order、theorem/proof、
Core/ControlFlow/VC は deferred のままである。active runner は 112 件である。
Chapter 3 の新規 coverage credit はない。

Task 162 current-state override: `mizar-test` は Task 124 の real two-item/two-
binding/distinct-written-range reserve producer と Task 120/159 の right-only
expected-set membership consumer を exact `reserve x for set; reserve y for
set; theorem MultipleReserveDeclarationMembershipPayloadBoundary: x in y;`
だけについて compose する。runner は ordinal 2/3 の `BindingId(0/1)`、first
reserve range を left result、second range を right result と sole right expected
input に保持し、left expected input を持たない。3 role は earlier `x` range に
canonical anchor された builtin-set identity 1 個へ intern されてから、2
`Inferred` variable と exactly one right-owned constraint を持つ 1 fact/deferred-
free `Checked` membership を生成する。exact source/route guard、full near-miss/
corruption coverage、real frontend/resolver sidecar が active route を保護する。
shared range、non-set type、broader shape、membership truth/fact、closure/order、
theorem/proof、Core/ControlFlow/VC は deferred のままである。active runner は
113 件である。Chapter 3 の新規 coverage credit はない。

Task 163 current-state override: `mizar-test` は task 73/151/155/157 が既に実行する
real four-expansion object-terminal producer と task 144 の real object-left/set-
right membership consumer を新規 test-first source だけについて compose する。
runner は ordinal 2/3 の `BindingId(0/1)`、raw outer-mode left result、独立した
explicit-set right result / sole right expected input、no left expected input、
distinct terminal-object-RHS / explicit-set identity を保持してから、2
`Inferred` variable と exactly one right-owned constraint を持つ 1 fact/deferred-
free `Checked` membership を生成しなければならない。exact route、full near-
miss/corruption coverage、real frontend/resolver sidecar を必須とする。truth/
fact、object/set coercion、closure/order、theorem/proof/Core/ControlFlow/VC、他の
depth、broader shape は deferred のままである。fixture/expectation、6 trace
backlink、production route、full near-miss/corruption coverage、real sidecar が
active runner 114 を保護する。

Task 164 current-state override: `mizar-test` は task 74/152 が既に
実行する real five-expansion set-terminal producer と task 158 の real set-
left/set-right membership consumer を新規 test-first source だけについて
compose する。runner は ordinal 2/3 の `BindingId(0/1)`、raw outermost-mode
left provenance、独立した explicit-set right result / sole right expected
provenance、no left expected input、terminal-set-RHS identity 1 個を保持して
から、2 `Inferred` variable と exactly one right-owned constraint を持つ 1
fact/deferred-free `Checked` membership を生成しなければならない。exact
five-definition chain/reserve/formula route、link/order/recovery/context/
parameterization/terminal/depth/reserve/formula/symbol/matched-output/
expansion-corruption guard、real frontend/resolver sidecar を必須とする。
truth/fact、closure/order、theorem/proof/Core/ControlFlow/VC、object-terminal
behavior、他 depth、broader shape は deferred のままである。fixture/
expectation、6 trace backlink、production route、full near-miss/corruption
coverage、real sidecar が active runner 115 を保護する。

Task 165 current-state override: `mizar-test` は task 74/153 が既に実行する real
five-expansion object-terminal producer と task 163 の real object-left/set-right
membership consumer を新規 test-first source だけについて compose する。
runner は ordinal 2/3 の `BindingId(0/1)`、raw outermost-mode left provenance、
独立した explicit-set right result / sole right expected provenance、no left
expected input、distinct terminal-object-RHS / explicit-set identity を保持して
から、2 `Inferred` variable と exactly one right-owned constraint を持つ 1
fact/deferred-free `Checked` membership を生成しなければならない。exact five-
definition chain/reserve/formula route、link/order/recovery/context/
parameterization/terminal/depth/reserve/formula/symbol/matched-output/
expansion-corruption guard、real frontend/resolver sidecar を必須とする。
truth/fact、object/set coercion、closure/order、theorem/proof/Core/ControlFlow/
VC、他 depth、broader shape は deferred のままである。fixture/expectation、
6 trace backlink、production route、full guard、real sidecar が active runner
116 を保護する。

Task 166 current-state override: `mizar-test` は task 74/152 が既に
実行する real five-expansion set-terminal producer と task 154 の equality
consumer を新規 test-first source だけについて合成する。runner は ordinal
1/2 の `BindingId(0)`、raw outermost-mode result/expected input 4 個、real
expansion 5 本、terminal-set-RHS identity 1 個を保持してから、2 `Inferred`
variable、1 fact/deferred-free `Checked` equality、ordered operand-owned
expected constraint 2 個を生成しなければならない。
exact five-definition chain/reserve/formula route、link/order/recovery/context/
parameterization/terminal/depth/reserve/formula/symbol/matched-output/expansion-
corruption guard、real frontend/resolver sidecar を必須とする。declaration
acceptance/inhabitation、truth/fact、closure/order、theorem/proof/Core/
ControlFlow/VC、object-terminal behavior、他 depth、broader shape は deferred
のままである。fixture/expectation、trace backlink 6 件、production route、full
guard matrix、real frontend/resolver sidecar が active runner 117 を保護する。

Task 167 current-state override: `mizar-test` は tasks 74/153 が既に
実行する real five-expansion object-terminal producer と task 155 の equality
consumer を新規 test-first source だけについて合成する。runner は ordinal
1/2 の `BindingId(0)`、raw outermost-mode result/expected input 4 個、real
expansion 5 本、terminal-object-RHS identity 1 個を保持してから、2
`Inferred` variable、1 fact/deferred-free `Checked` equality、ordered operand-
owned expected constraint 2 個を object/set coercion なしで生成しなければ
ならない。exact five-definition chain/reserve/formula route、link/order/
recovery/context/parameterization/terminal/depth/reserve/formula/symbol/
matched-output/expansion-corruption guard、real frontend/resolver sidecar を
必須とする。declaration acceptance/inhabitation、truth/fact、closure/order、
theorem/proof/Core/ControlFlow/VC、set-terminal behavior、他 depth、broader
shape は deferred のままである。fixture/expectation、trace backlink 6 件、
production route、full guard matrix、real frontend/resolver sidecar が active
runner 118 を保護する。

Task 168 current-state override: `mizar-test` は tasks 74/152 が既に実行する real five-
expansion set-terminal producer と task 156 の pre-desugaring inequality
consumer を新規 test-first source だけについて合成する。runner は ordinal
1/2 の `BindingId(0)`、raw outermost-mode result/expected input 4 個、real
expansion 5 本、terminal-set-RHS identity 1 個を保持してから、2 `Inferred`
variable、1 fact/deferred-free pre-desugaring `Checked` inequality、ordered
operand-owned expected constraint 2 個を生成しなければならない。exact five-
definition chain/reserve/formula route、link/order/recovery/context/
parameterization/terminal/depth/reserve/formula/symbol/matched-output/expansion-
corruption guard、real frontend/resolver sidecar を必須とする。declaration
acceptance/inhabitation、inequality desugaring/truth/fact、closure/order、theorem/
proof/Core/ControlFlow/VC、object-terminal behavior、他 depth、broader shape は
deferred のままである。fixture/expectation、trace backlink 6 件、production
route、full guard matrix、real frontend/resolver sidecar が active runner 119 を
保護する。

Task 169 current-state override: `mizar-test` は tasks 74/153 が既に実行する real five-
expansion object-terminal producer と task 157 の pre-desugaring inequality
consumer を新規 test-first source だけについて合成する。runner は ordinal
1/2 の `BindingId(0)`、raw outermost-mode result/expected input 4 個、real
expansion 5 本、terminal-object-RHS identity 1 個を保持してから、2
`Inferred` variable、1 fact/deferred-free pre-desugaring `Checked` inequality、
ordered operand-owned expected constraint 2 個を object/set coercion なしで
生成しなければならない。exact five-definition chain/reserve/formula route、
link/order/recovery/context/parameterization/terminal/depth/reserve/formula/
symbol/matched-output/expansion-corruption guard、real frontend/resolver sidecar
を必須とする。declaration acceptance/inhabitation、inequality desugaring/truth/
fact、closure/order、theorem/proof/Core/ControlFlow/VC、set-terminal behavior、
他 depth、broader shape は deferred のままである。fixture/expectation、trace
backlink 6 件、production route、full guard matrix、real frontend/resolver
sidecar が active runner 120 を保護する。

Task 172 current-state override: `mizar-test` は task 74 の real seven-expansion set-
terminal long-chain producer と task 166 の equality consumer を新規 test-first
source だけについて合成する。route は raw `ChainMode6` result/expected input
4 個を保持し、両 identifier を ordinal 1/2 の `BindingId(0)` に解決し、real
AST-derived expansion 7 本をすべて消費して、全 role を terminal `BaseMode`
RHS の builtin-set identity 1 個へ normalize してから、2 `Inferred` variable、
1 fact/deferred-free `Checked` equality、ordered operand-owned expected
constraint 2 個を生成しなければならない。exact definition 7 本、reserve、
formula と、full link/order/recovery/context/parameterization/terminal/depth/
reserve/formula/symbol/matched-output/expansion-corruption guard、real frontend/
resolver sidecar を必須とする。declaration acceptance/inhabitation、truth/
fact、closure/order、theorem/proof/Core/ControlFlow/VC、imported/attributed/
argument-bearing または別 chain shape、general unbounded semantics は deferred
のままである。fixture、expectation、trace backlink 6 件、production route、
full guard matrix、real frontend/resolver sidecar が active runner 121 を保護
する。

Task 173 current-state override: harness は task 172 の exact seven-expansion source
route と task 168 の pre-desugaring inequality consumer を exact
`LongLocalModeReservedVariableInequalityPayloadBoundary: z <> z;` だけについて
再利用する。raw `ChainMode6` role 4 個、ordinal 1/2 の `BindingId(0)`、terminal
`BaseMode` RHS identity、2 `Inferred` term、ordered constraint 2 個、1 fact/
deferred-free checked inequality を保持する。full exact/near-miss/corruption
と real frontend/resolver sidecar guard が active runner 122 を保護し、broader
semantics は deferred のままとする。

Task 174 current-state override: harness は task 74 の real seven-expansion source
producer と task 164 の right-only expected-set membership consumer を合成し、
task 172 の exact source-shape/structural guard pattern を再利用して exact
`LongLocalModeReservedVariableMembershipPayloadBoundary: x in y;` だけについて
合成する。raw `ChainMode6` left result と独立した explicit-set right result/
sole expected input、ordinal 2/3 の `BindingId(0/1)`、terminal `BaseMode` RHS
identity 1 個、left expected input なし、2 `Inferred` term、1 fact/deferred-free
checked membership、right-owned constraint 1 個を保持する。full exact/near-
miss/corruption と real frontend/resolver sidecar guard が active runner 123 を
保護する。

Task 175 current-state override: harness は task 74 の real seven-expansion source
producer と task 152 の normalized-reflexive type-assertion consumer を合成し、
task 172 の exact source-shape/structural guard pattern を再利用して exact
`LongLocalModeReservedVariableTypeAssertionPayloadBoundary: x is set;` だけを
扱う。raw `ChainMode6` subject と独立した formula-side builtin-set asserted
provenance、ordinal 1 の `BindingId(0)`、terminal `BaseMode` RHS identity 1 個、
1 `Inferred` term、general reachability を用いない 1 fact/deferred-free checked
type assertion を保持する。full exact/near-miss/corruption と real frontend/
resolver sidecar guard が active runner 124 を保護する。

Task 176 current-state override: harness は task 74 の real AST-bounded builtin-object-
terminal chain producer と task 167 の object-normalizing equality consumer を
合成し、task 172 の seven-definition structural guard pattern を再利用して exact
`LongLocalObjectModeReservedVariableEqualityPayloadBoundary: z = z;` だけを扱う。
raw `ChainObjectMode6` result/expected input 4 個、ordinal 1/2 の
`BindingId(0)`、real expansion 7 本、terminal `BaseObjectMode` RHS identity
1 個、2 `Inferred` term、ordered constraint 2 個、object/set coercion を用いない
1 fact/deferred-free checked equality を保持する。full exact/near-miss/
corruption と real frontend/resolver sidecar guard が active runner 125 を保護する。

Task 177 current-state override: harness は task 74 の real AST-bounded builtin-object-
terminal chain producer と task 169 の object-normalizing pre-desugaring
inequality consumer を合成し、task 172 の seven-definition structural guard
pattern を再利用して exact
`LongLocalObjectModeReservedVariableInequalityPayloadBoundary: z <> z;` だけを
扱う。raw `ChainObjectMode6` result/expected input 4 個、ordinal
1/2 の `BindingId(0)`、real expansion 7 本、terminal `BaseObjectMode` RHS identity
1 個、2 `Inferred` term、ordered constraint 2 個、object/set coercion を用いない
1 fact/deferred-free pre-desugaring checked inequality を保持する。full exact/
near-miss/corruption と real frontend/resolver sidecar guard が active runner 126
を保護する。

Task 178 current-state override: harness は task 74 の real AST-bounded builtin-object-
terminal chain producer と task 165 の object-left/set-right membership consumer
を合成し、task 172 の seven-definition structural guard pattern を再利用して exact
`LongLocalObjectModeReservedVariableMembershipPayloadBoundary: x in y;` だけを
扱う。raw `ChainObjectMode6` left result、独立した explicit-
set right result/sole expected input、ordinal 2/3 の `BindingId(0/1)`、real
expansion 7 本、distinct terminal-object-RHS と explicit-set identity、left
expected input なし、2 `Inferred` term、right-owned constraint 1 個、object/set
coercion を用いない 1 fact/deferred-free checked membership を保持する。full
exact/near-miss/corruption と real frontend/resolver sidecar guard、production
routing が active runner 127 を保護する。

Task 179 current-state override: harness は task 74 の real AST-bounded builtin-object-
terminal chain producer と task 153 の object-normalizing type-assertion consumer
を合成し、task 172 の seven-definition structural guard と task 175 long-chain
sibling pattern を再利用して exact
`LongLocalObjectModeReservedVariableTypeAssertionPayloadBoundary: x is object;`
だけを扱う。raw `ChainObjectMode6` subject result、独立した
formula-side builtin-object asserted input、ordinal 1 の `BindingId(0)`、real
expansion 7 本、terminal-object-RHS identity 1 個、1 `Inferred` term、general
reachability と object/set coercion を用いない 1 fact/deferred-free normalized-
reflexive checked type assertion を保持する。full exact/near-miss/corruption、
real frontend/resolver sidecar guard、production routing が active runner 128 を
保護する。

task 180 current-state override: harness は
`theorem SourceDerivedContradictionConstantBoundary: contradiction;` だけを
昇格する。existing contradiction-kind mapping と standalone theorem-shape
validation pattern を再利用して standalone exact leaf extractor を追加し、exact
leaf site/range を module-root context で deferred reason なしに既存
`FormulaKind::Contradiction` checker consumer へ渡す。expected output は term、
asserted type、expected constraint、candidate、fact、deferred reason、diagnostic
を持たない 1 `Checked` formula である。exact-source、near-miss、corruption、real parser/resolver sidecar test は
thesis、connective、quantified、recovered、wrong-label、duplicate、extra-item
source を既存 path に保つ。runner は truth/fact publication、theorem
acceptance、proof-goal closure、`formula_statement`、proof、CoreIr、
ControlFlowIr、VC coverage を主張してはならない。
これらの guard と sidecar が active runner 129 を保護する。

task 181 current-state repair は active corpus と runner count 129 を保持し、
generic imported attributed-reserve source gate を既に trace 済みの task-84/
85/116/171 fixture 5 件へ限定する。source-shaped unit test は credit 済み ordered
`reserve x for set; reserve y for non empty set;` mixed source と、extraction
gap に残る missing/wrong/duplicate import、duplicate/mixed attribute、argument、
wrong polarity/head、broader binding/item/order shape、definition、recovery node
を区別する。既存 positive `TypeCaseAttr set`、positive/negative `empty set`、
negative `empty object`、exact mixed source は同じ checker evidence-query
diagnostic へ到達し続ける。`.miz`、expectation、traceability、runner-count、
semantic-coverage の変更はこの repair に含めない。

task 182 current-state override は最初の formula-side local-mode asserted head
向け active pass source 1 件を追加する。exact source は `mode
LocalModeAssertedHeadDef: LocalModeAssertedHead is set;` を含む `definition`
block、その mode の reserve 1 個、
`LocalModeAssertedHeadPayloadBoundary: x is LocalModeAssertedHead;` を所有する。
runner は独立した reserve-subject と formula-side asserted raw mode provenance を
保持し、real expansion 1 個を消費し、ordinal 1 を `BindingId(0)` に解決し、
known type entry 3 個を terminal-RHS builtin-set identity 1 個へ intern し、
1 `Inferred` variable と 1 fact/deferred-free normalized-reflexive `Checked` type
assertion を要求する。source-shaped exact/near-miss、matched-output corruption、
production-route、real frontend/resolver-sidecar tests は collapsed provenance と
broader asserted-head/definition shape を reject する。新規 case は
既存 expectation を変えず active runner を 129 から 130 に増やす。declaration
acceptance/inhabitation、widening/`qua`、truth/fact、theorem/proof/CoreIr/
ControlFlowIr/VC、chain/object-terminal asserted head、general semantics は
credit しない。

task 183 current-state override は direct object-terminal sibling を追加する。
exact source は `mode LocalObjectModeAssertedHeadDef: LocalObjectModeAssertedHead
is object;` を含む definition block 1 個、その mode の reserve 1 個、
`LocalObjectModeAssertedHeadPayloadBoundary: x is LocalObjectModeAssertedHead;`
を所有する。runner は独立した raw reserve-subject/formula-side asserted mode
provenance を保持し、real object-terminal expansion 1 個を消費し、ordinal 1 を
`BindingId(0)` に解決し、known type entry 3 個を terminal-RHS builtin-object
identity 1 個へ intern し、general reachability と object/set coercion を用いず
1 `Inferred` variable と 1 fact/deferred-free normalized-reflexive `Checked`
type assertion を要求する。source-shaped exact/near-miss、
corruption、production-route、real frontend/resolver-sidecar test は set terminal、
builtin/other asserted head、chain、attribute/argument、recovery、extra item、
collapsed provenance を reject する。新規 case は既存 expectation
を変えず active runner を 130 から 131 に増やす。declaration acceptance/
inhabitation、truth/fact、theorem/proof/CoreIr/ControlFlowIr/VC、他 asserted-head
family、general semantics は credit しない。

task 184 current-state override は exact one-edge set-terminal
same-outer-mode asserted-head chain 向け active pass source 1 件を追加する。
source は `mode BaseModeAssertedHeadDef: BaseModeAssertedHead is set;` と
`mode ChainModeAssertedHeadDef: ChainModeAssertedHead is
BaseModeAssertedHead;` を含む ordered definition block 2 個、outer mode の
reserve 1 個、`ChainedLocalModeAssertedHeadPayloadBoundary: x is
ChainModeAssertedHead;` を含む。runner は独立した reserve-subject と formula-side
asserted raw outer-mode provenance を保持し、real expansion 2 個を消費し、
ordinal 1 を `BindingId(0)` に解決し、known type entry 3 個を terminal base-
definition-RHS builtin-set identity 1 個へ intern し、general reachability を
用いず、1 `Inferred` variable と 1 fact/deferred-free normalized-reflexive
`Checked` type assertion を要求する。source-shaped exact/near-
miss、corruption、production-route、real frontend/resolver-sidecar test は wrong
link/terminal/order/depth、builtin/base/other asserted head、attribute/argument、
recovery、extra item、collapsed provenance を reject する。
新規 case は existing expectation を変えず active runner を 131 から 132 に
増やす。declaration acceptance/inhabitation、widening/`qua`、truth/
fact、closure/order、theorem/proof/CoreIr/ControlFlowIr/VC、object/deeper/他
asserted-head chain、general chain semantics は credit しない。

task 185 current-state override は exact one-edge object-terminal same-outer-
mode asserted-head sibling を追加する。source は `mode
BaseObjectModeAssertedHeadDef: BaseObjectModeAssertedHead is object;` と `mode
ChainObjectModeAssertedHeadDef: ChainObjectModeAssertedHead is
BaseObjectModeAssertedHead;` を含む ordered definition block 2 個、outer mode の
reserve 1 個、`ChainedLocalObjectModeAssertedHeadPayloadBoundary: x is
ChainObjectModeAssertedHead;` を含む。runner は distinct range と同じ resolved
outer symbol を持つ独立した reserve-subject/formula-side asserted raw provenance
を保持し、real expansion 2 個を消費し、ordinal 1 を `BindingId(0)` に解決し、
known type entry 3 個を terminal base-definition-RHS builtin-object identity 1 個へ
intern し、general reachability、widening、`qua`、object/set coercion を用いず、
1 `Inferred` variable と 1 fact/deferred-free normalized-reflexive `Checked` type
assertion を要求する。exact/near-miss/corruption、production-route、real frontend/
resolver-sidecar test は wrong link/terminal/order/depth、builtin/base/other
asserted head、attribute/argument、imported provenance、recovery、extra item、
collapsed provenance、builtin-set corruption を reject しなければならない。
shared trace backlink 5 個と dedicated row 1 個が existing expectation を変えず
active runner 133 を保護する。imported/declaration/attribute、
broader term/formula/child graph、truth/fact、theorem/proof/CoreIr/ControlFlowIr/
VC、deeper/他 asserted head、general chain coverage は deferred のままである。
module layout 更新は不要である。

task 186 current-state override は exact two-edge set-terminal same-outer-mode
asserted-head sibling を追加する。source は `BaseTwoEdgeModeAssertedHeadDef:
BaseTwoEdgeModeAssertedHead is set`、`MiddleTwoEdgeModeAssertedHeadDef:
MiddleTwoEdgeModeAssertedHead is BaseTwoEdgeModeAssertedHead`、
`OuterTwoEdgeModeAssertedHeadDef: OuterTwoEdgeModeAssertedHead is
MiddleTwoEdgeModeAssertedHead` の ordered definition 3 個、outer mode の `x`
reserve、`TwoEdgeLocalModeAssertedHeadPayloadBoundary: x is
OuterTwoEdgeModeAssertedHead` を含む。runner は同じ outer symbol 向けの distinct
reserve/asserted site/range、real expansion 3 個、ordinal 1 の `BindingId(0)`、
terminal base RHS builtin-set identity 1 個へ normalize する known entry 3 個、
reachability、widening、`qua` を用いない 1 `Inferred` variable と 1 fact/deferred-
free normalized-reflexive `Checked` assertion を保持する。imported/ambiguous
provenance と全 structural chain failure を含む exact/near-miss/corruption/real
frontend-resolver sidecar guard を必須とする。shared 5 + dedicated 1 trace row
により rebaseline せず active runner 133 を 134 に増やす。object/deeper/imported
semantics、declaration/attribute、broader term/formula/child graph、truth/fact、
proof/CoreIr/ControlFlowIr/VC、general chain semantics は deferred のままである。
module layout 更新は不要である。

task 187 current-state override は exact two-edge object-terminal same-outer-mode
asserted-head sibling を追加する。source は ordered definition
`mode BaseTwoEdgeObjectModeAssertedHeadDef: BaseTwoEdgeObjectModeAssertedHead is
object;`、`mode MiddleTwoEdgeObjectModeAssertedHeadDef:
MiddleTwoEdgeObjectModeAssertedHead is BaseTwoEdgeObjectModeAssertedHead;`、
`mode OuterTwoEdgeObjectModeAssertedHeadDef: OuterTwoEdgeObjectModeAssertedHead
is MiddleTwoEdgeObjectModeAssertedHead;`、
outer mode の `x` reserve、`TwoEdgeLocalObjectModeAssertedHeadPayloadBoundary: x
is OuterTwoEdgeObjectModeAssertedHead` を含む。runner は同じ local outer symbol
向けの distinct raw subject/asserted site/range、real expansion 3 個、ordinal 1
の `BindingId(0)`、base-definition-RHS builtin-object identity 1 個へ normalize
する known entry 3 個、expected constraint、reachability、widening、`qua`、
object/set coercion を持たない 1 `Inferred` variable と 1 fact/deferred-free
normalized-reflexive `Checked` assertion を保持する。imported Base/Middle/Outer、
imported/ambiguous asserted provenance、collapsed provenance、`BuiltinSet`
output corruption、wrong label、attributed/argument-bearing formula-side
asserted head を含む exact/near-miss/corruption/real frontend-resolver sidecar
guard を備える。shared 5 + dedicated 1 trace row により rebaseline
せず active runner 135 を保護する。positive imported semantics、
declaration/attribute、broader term/formula/child graph、truth/fact、implicit
closure/order、theorem acceptance、proof/CoreIr/ControlFlowIr/VC、general chain
semantics は deferred のままである。Step 5 は active、Steps 6/7 は deferred の
まま。module layout 更新は不要である。

task 188 は exact active pass source
`reserve x for object; theorem ReservedObjectVariableEqualityPayloadBoundary:
x = x;` を追加する。これは `test_gap` と narrow `source_drift`、`design_drift`
であり、blocking `spec_gap` はない。Chapter 03/04/13/14/16 から builtin object、
reserve/free-theorem-variable、identifier term、equality、theorem syntax の intent
を直接導ける。runner は real object reserve producer と equality consumer を再利用し、
ordinal 1/2 の `BindingId(0)` lookup と one written range 上の distinct result/
expected role site 4 個を保持し、canonical builtin-object identity 1 個、inferred
variable 2 個、ordered constraint 2 個、fact/deferred-free checked equality 1 個を
要求する。既存 expectation を変更せず、shared trace backlink 5 個 + dedicated row
1 個により active runner 136 を保護する。general object
equality、coercion、truth/fact、acceptance、proof、downstream payload は deferred
のままである。

task 189 は exact active pass source
`reserve x for object; theorem
ReservedObjectVariableTypeAssertionPayloadBoundary: x is object;` を追加する。
これは `test_gap` と narrow `source_drift`、`design_drift` であり、blocking
`spec_gap` はない。Chapter 03/04/13/14/16 から builtin object、reserve/free-
theorem-variable、identifier term、type assertion、theorem syntax の intent を
直接導ける。runner は real object reserve producer と normalized-reflexive
assertion consumer を再利用し、ordinal 1 の `BindingId(0)` lookup と distinct
reserve-subject result/formula-side asserted site/range を保持し、reserve を anchor
とする canonical builtin-object identity 1 個、inferred variable 1 個、known type
entry 3 個、expected constraint 0 個、fact/deferred-free checked assertion 1 個を
要求する。既存 expectation を変更せず、shared trace backlink 5 個 + dedicated row
1 個により active runner 137 を保護する。reachability/widening/
`qua`、object/set coercion、truth/fact、closure/order、acceptance、proof、
downstream payload は deferred のままとする。

task 190 は exact active pass source `reserve x for object; theorem
ReservedObjectVariableInequalityPayloadBoundary: x <> x;` を追加する。これは
`test_gap` と narrow `source_drift`、`design_drift` であり、blocking `spec_gap`
はない。Chapter 03/04/13/14/16 から builtin object、reserve/free-theorem-
variable、identifier term、inequality、theorem syntax の intent を直接導ける。
runner は real object reserve producer と pre-desugaring inequality consumer を
再利用し、ordinal 1/2 の `BindingId(0)` lookup、one written range 上の distinct
result/expected role site 4 個、canonical builtin-object identity 1 個、inferred
variable 2 個、known type entry 6 個、ordered expected constraint 2 個、fact/
candidate/diagnostic/deferred-free checked inequality 1 個を要求する。既存
expectation を変更せず shared trace backlink 5 個 + dedicated row 1 個により
active runner 138 を保護する。inequality desugaring/equality
truth、object/set coercion、fact、closure/order、acceptance、proof、downstream
payload は deferred のままとする。

task 191 は exact active pass source `reserve x, y for object; theorem
DistinctReservedObjectVariableEqualityPayloadBoundary: x = y;` を追加する。
これは `test_gap` と narrow `source_drift`、`design_drift` であり、blocking
`spec_gap` はない。Chapters 03、04、13、14、16 は builtin object、multi-name
reserve/free-theorem-variable、identifier term、equality、theorem intent を直接
提供する。runner は task 123 の real one-item/two-binding shared-range producer
と task 188 の builtin-object equality consumer を合成し、ordinal 2/3 の
`BindingId(0/1)` lookup と shared written range 1 個上の distinct result/
expected role site 4 個を保持し、reserve range を anchor とする canonical
builtin-object identity 1 個、inferred variable 2 個、known type entry 6 個、
ordered expected constraint 2 個、fact/candidate/diagnostic/deferred-free
checked equality 1 個を要求する。shared trace backlink 5 個 + dedicated row
1 個により既存 expectation を変更せず active runner 139 を保護する。equality
truth、object/set coercion、fact、closure/order、
acceptance、proof、downstream payload は deferred のままとする。

task 192 は exact active pass source `reserve x, y for object; theorem
DistinctReservedObjectVariableInequalityPayloadBoundary: x <> y;` を追加する。
これは `test_gap` と narrow `source_drift`、`design_drift` であり、blocking
`spec_gap` はない。Chapters 03、04、13、14、16 は builtin object、multi-name
reserve/free-theorem-variable、identifier term、inequality、theorem intent を直接
提供する。runner は tasks 123/191 の real one-item/two-binding shared-range
producer と tasks 121/160/190 の real pre-desugaring inequality consumer を合成し、
ordinal 2/3 の `BindingId(0/1)` lookup と shared written range 1 個上の distinct
result/expected role site 4 個を保持し、reserve range を anchor とする canonical
builtin-object identity 1 個、inferred variable 2 個、known type entry 6 個、
ordered expected constraint 2 個、fact/candidate/diagnostic/deferred-free checked
inequality 1 個を要求する。shared trace backlink 5 個 + dedicated row 1 個により
既存 expectation を変更せず active runner 140 を保護する。inequality
desugaring/equality truth、object/set coercion、fact、closure/order、acceptance、
proof、downstream payload は deferred のままとする。

task 193 は exact active pass source `reserve x for object; reserve y for
object; theorem MultipleObjectReserveDeclarationEqualityPayloadBoundary: x =
y;` を追加する。これは `test_gap` と narrow `source_drift`、`design_drift`
であり、blocking `spec_gap` はない。Chapters 03、04、13、14、16 は builtin
object、separate reserve/free-theorem-variable、identifier term、equality、
theorem intent を直接提供する。runner は Task 124 の real two-item/two-
binding/distinct-written-range producer と tasks 188/191 の builtin-object
equality consumer を合成し、ordinal 2/3 の `BindingId(0/1)` lookup と binding
ごとの written range 2 個上の distinct result/expected role site 4 個を保持し、
先行する `x` range を anchor とする canonical builtin-object identity 1 個、
inferred variable 2 個、known type entry 6 個、ordered expected constraint 2 個、
fact/candidate/diagnostic/deferred-free checked equality 1 個を要求する。shared
trace backlink 5 個 + dedicated row 1 個により既存 expectation を変更せず、
356 cases / 320 requirements 内の active runner 141 を保護する。equality
truth、object/set coercion、fact、closure/order、acceptance、proof、shared-
range shape、downstream payload は deferred のままとする。

task 194 は exact active pass source `reserve x for object; reserve y for
object; theorem MultipleObjectReserveDeclarationInequalityPayloadBoundary: x <>
y;` を追加する。これは `test_gap` と narrow `source_drift`、`design_drift`
であり、blocking `spec_gap` はない。Chapters 03、04、13、14、16 は builtin
object、separate reserve/free-theorem-variable、identifier term、inequality、
theorem intent を直接提供する。runner は Task 193 の real ordered two-item/
two-binding/distinct-written-object-range producer と tasks 190/192 の pre-
desugaring builtin-object inequality consumer を合成し、ordinal 2/3 の
`BindingId(0/1)` lookup と binding ごとの written range 2 個上の distinct raw
result/expected role 4 個を保持し、先行する `x` range を anchor とする
canonical builtin-object identity 1 個、inferred variable 2 個、known type entry
6 個、ordered expected constraint 2 個、fact/candidate/diagnostic/deferred-free
checked inequality 1 個を要求する。shared trace backlink 5 個 + dedicated row 1
個により既存 expectation を変更せず、357 cases / 321 requirements 内の
active runner 142 を保護する。inequality desugaring/equality truth、object/set
coercion、fact、closure/order、acceptance、proof、shared-range shape、downstream
payload は deferred のままとする。

task 195 は ordered local mode definition 4 個 `Outer -> Middle -> Inner ->
Base -> set`、`reserve x for OuterThreeEdgeModeAssertedHead`、theorem
`ThreeEdgeLocalModeAssertedHeadPayloadBoundary: x is
OuterThreeEdgeModeAssertedHead;` を持つ exact active pass source を追加する。
これは `test_gap` と narrow `source_drift`、`design_drift` であり、blocking
`spec_gap` はない。Chapters 03、04、07、13、14、16 は local mode expansion、
reserve/free variable、identifier term、asserted type、theorem intent を直接提供
する。runner は Task 73 の real four-expansion producer と Task 186 の same-
outer formula-side asserted-head consumer を合成し、distinct raw subject/
asserted site/range を保持し、ordinal 1 を `BindingId(0)` に解決し、known type
entry 3 個を base-definition-RHS anchor の builtin-set identity 1 個へ normalize
し、expected constraint なしで inferred variable 1 個と fact/candidate/
diagnostic/deferred-free checked type assertion 1 個を要求する。shared trace
backlink 5 個 + dedicated row 1 個により既存 expectation を変更せず、358
cases / 322 requirements 内の active runner 143 を保護する。object-terminal/
deeper/imported/attributed/argument-bearing/other asserted head、reachability/
widening/`qua`、acceptance、truth/fact、closure/order、broader semantic payload、
proof、downstream IR は deferred のままとする。

task 196 は ordered local mode definition 4 個 `Outer -> Middle -> Inner ->
Base -> object`、`reserve x for OuterThreeEdgeObjectModeAssertedHead`、theorem
`ThreeEdgeLocalObjectModeAssertedHeadPayloadBoundary: x is
OuterThreeEdgeObjectModeAssertedHead;` を持つ exact active pass source を追加
する。これは `test_gap` と narrow `source_drift`、`design_drift` であり、
blocking `spec_gap` はない。Chapters 03、04、07、13、14、16 は object-
terminal local mode expansion、reserve/free variable、identifier term、asserted
type、theorem intent を直接提供する。runner は Tasks 73/151 の real four-
expansion object producer と Task 187 の same-outer formula-side asserted-head
consumer を合成し、distinct raw subject/asserted site/range を保持し、ordinal 1
を `BindingId(0)` に解決し、known type entry 3 個を base-definition-RHS
anchor の builtin-object identity 1 個へ normalize し、expected constraint と
object/set coercion なしで inferred variable 1 個と fact/candidate/diagnostic/
deferred-free checked type assertion 1 個を要求する。shared trace backlink 5
個 + dedicated row 1 個により既存 expectation を変更せず、359 cases / 323
requirements 内の active runner 144 を保護する。deeper/imported/attributed/
argument-bearing/other asserted head、reachability/widening/`qua`、acceptance、
truth/fact、closure/order、broader semantic payload、proof、downstream IR は
deferred のままとする。

task 197 は ordered local mode definition 5 個 `TooDeep -> Outer -> Middle ->
Inner -> Base -> set`、`reserve x for TooDeepFourEdgeModeAssertedHead`、theorem
`FourEdgeLocalModeAssertedHeadPayloadBoundary: x is
TooDeepFourEdgeModeAssertedHead;` を持つ exact active pass source を追加する。
これは `test_gap` と narrow `source_drift`、`design_drift` であり、blocking
`spec_gap` はない。Chapters 03、04、07、13、14、16 は local mode expansion、
reserve/free variable、identifier term、asserted type、theorem intent を直接
提供する。runner は Tasks 74/152 の real five-expansion set producer と Tasks
186/195 の same-outermost formula-side asserted-head consumer を合成し、
distinct raw subject/asserted site/range を保持し、ordinal 1 を `BindingId(0)`
に解決し、known type entry 3 個を base-definition-RHS anchor の builtin-set
identity 1 個へ normalize し、expected constraint なしで inferred variable 1
個と fact/candidate/diagnostic/deferred-free checked type assertion 1 個を要求
する。shared trace backlink 5 個 + dedicated row 1 個により既存 expectation
を変更せず、360 cases / 324 requirements 内の active runner 145 を保護する。
object-terminal/other-depth/imported/attributed/argument-bearing/other asserted
head、reachability/widening/`qua`、acceptance、truth/fact、closure/order、
broader semantic payload、proof、downstream IR は deferred のままとする。

task 198 は ordered local mode definition 5 個 `TooDeep -> Outer -> Middle ->
Inner -> Base -> object`、`reserve x for TooDeepFourEdgeObjectModeAssertedHead`、
theorem `FourEdgeLocalObjectModeAssertedHeadPayloadBoundary: x is
TooDeepFourEdgeObjectModeAssertedHead;` を持つ exact active pass source を追加
する。これは `test_gap` と narrow `source_drift`、`design_drift` であり、
blocking `spec_gap` はない。Chapters 03、04、07、13、14、16 は local mode
expansion、reserve/free variable、identifier term、asserted type、theorem
intent を直接提供する。runner は Tasks 74/153 の real five-expansion object
producer と Tasks 187/196 の same-outermost formula-side asserted-head consumer
を合成し、distinct raw subject/asserted site/range を保持し、ordinal 1 を
`BindingId(0)` に解決し、known type entry 3 個を base-definition-RHS anchor
の builtin-object identity 1 個へ normalize し、expected constraint と object/
set coercion なしで inferred variable 1 個と fact/candidate/diagnostic/deferred-
free checked type assertion 1 個を要求する。shared trace backlink 5 個 +
dedicated row 1 個により既存 expectation を変更せず、361 cases / 325
requirements 内の active runner 146 を保護する。set-terminal/other-depth/
imported/attributed/argument-bearing/other asserted head、reachability/
widening/`qua`、acceptance、truth/fact、closure/order、broader semantic payload、
proof、downstream IR は deferred のままとする。

task 199 は `BaseMode -> set`、`ChainMode6 -> ChainMode5` までの ordered local
link 6 個、`reserve x for ChainMode6`、theorem
`LongLocalModeAssertedHeadPayloadBoundary: x is ChainMode6;` を持つ exact
active pass source を追加する。これは `test_gap` と narrow `source_drift`、
`design_drift` であり、blocking `spec_gap` はない。Chapters 03、04、07、13、
14、16 は local mode expansion、reserve/free variable、identifier term、
asserted type、theorem intent を直接提供する。runner は Tasks 74/175 の real
seven-expansion set producer と Tasks 186/195/197 の same-symbol formula-side
asserted-head consumer を合成し、distinct raw subject/asserted site/range を
保持し、ordinal 1 を `BindingId(0)` に解決し、known type entry 3 個を
`BaseModeDef` RHS anchor の builtin-set identity 1 個へ normalize し、expected
constraint なしで inferred variable 1 個と fact/candidate/diagnostic/deferred-
free checked type assertion 1 個を要求する。shared trace backlink 5 個 +
dedicated row 1 個により既存 expectation を変更せず、362 cases / 326
requirements 内の active runner 147 を保護する。object-terminal/other-depth/
imported/attributed/argument-bearing/other asserted head、reachability/
widening/`qua`、acceptance、truth/fact、closure/order、broader semantic payload、
proof、downstream IR は deferred のままとする。

task 200 は `BaseObjectMode -> object`、`ChainObjectMode6 ->
ChainObjectMode5` までの ordered local link 6 個、`reserve x for
ChainObjectMode6`、theorem
`LongLocalObjectModeAssertedHeadPayloadBoundary: x is ChainObjectMode6;` を
持つ exact active pass source を追加する。これは `test_gap` と narrow
`source_drift`、`design_drift` であり、blocking `spec_gap` はない。Chapters
03、04、07、13、14、16 は local mode expansion、reserve/free variable、
identifier term、asserted type、theorem intent を直接提供する。runner は
Tasks 74/179 の real seven-expansion object producer と Tasks 187/196/198 の
same-symbol formula-side asserted-head consumer を合成し、distinct raw
subject/asserted site/range を保持し、ordinal 1 を `BindingId(0)` に解決し、
known type entry 3 個を `BaseObjectModeDef` RHS anchor の builtin-object
identity 1 個へ normalize し、expected constraint、general reachability、
object/set coercion なしで inferred variable 1 個と fact/candidate/diagnostic/
deferred-free checked type assertion 1 個を要求する。shared trace backlink 5
個 + dedicated row 1 個により既存 expectation を変更せず、363 cases / 327
requirements 内の active runner 148 を保護する。set-terminal/other-depth/
imported/attributed/argument-bearing/other asserted head、reachability/
widening/`qua`、acceptance、truth/fact、closure/order、broader semantic
payload、proof、downstream IR は deferred のままとする。

Task 120 current-state override: `mizar-test` は exact active membership pass
source
`reserve x for set; theorem ReservedVariableMembershipPayloadBoundary: x in x;`
を所有する。runner は task 119 の独立した source-order lookup producer を共有し、
2 つの source-derived result role と右 expected-`set` role だけを記録し、2 つの
`Inferred` variable と 1 つの no-fact `Checked` membership を要求する。
production invariant validation と real frontend/resolver payload test が active
sidecar を guard する。MT-AUDIT-015 の generic term/formula gap は task 119/123
equality と task 120 membership を超える coverage を指し、truth/fact、implicit
closure、theorem acceptance、proof、CoreIr、ControlFlowIr、VC は deferred の
ままである。

Task 121 current-state override: `mizar-test` は exact active inequality pass
source
`reserve x for set; theorem ReservedVariableInequalityPayloadBoundary: x <> x;`
を所有する。runner は checker-owned two-expected-type inequality API と task 119
の real reserve binding/use producer を使い、2 組の linked source-derived
result/expected role pair を記録して、2 つの `Inferred` variable と 1 つの
fact-free pre-desugaring `Checked` inequality を要求する。task 107 の numeral
inequality bridge は expected type を渡さない partial のままである。production
invariant validation、full near-miss matrix、real frontend/resolver payload test
が active sidecar を guard する。したがって MT-AUDIT-015 の generic
term/formula gap は task 119/123 equality、task 120 membership、task 121 inequality
を超える coverage を指し、desugaring/truth/fact、implicit closure、theorem
acceptance、proof、CoreIr、ControlFlowIr、VC は deferred のままである。

Task 122 current-state override: `mizar-test` は exact active type-assertion pass
source
`reserve x for set; theorem ReservedVariableTypeAssertionPayloadBoundary: x is set;`
を所有する。runner は task 119 の real reserve lookup/result producer と task 109
の formula-side asserted-type AST producer を結合し、normalization 前の両 input
と independent source anchor を保持する。checker は両者の normalized identity
を reflexive admissibility として受理し、1 `Inferred` variable と 1 fact-free
`Checked` type assertion を記録する。known non-identical type は external
reachability payload gap 上の partial のままである。production validation、full
near-miss matrix、real frontend/resolver payload test が active sidecar を guard
する。MT-AUDIT-015 の generic term/formula gap は promoted tasks 119-169 を
超える coverage を指す。general
reachability/widening/`qua`、attribute、truth/fact、implicit
closure、theorem acceptance、proof、CoreIr、ControlFlowIr、VC は deferred の
ままである。

Task 109 addendum: `mizar-test` は task 102 が所有していた exact builtin
type-assertion theorem source を supersede する。active sidecar は
`theorem BuiltinTypeAssertionPayloadBoundary: 1 is set;` を `type_elaboration`
で parser / resolver まで実行し、source-derived checker `TermInput`、
`FormulaInput`、asserted builtin `set` `TypeExpressionInput` payload を渡してから
missing numeric type payload と partial formula checking で fail closed してよい。
この addendum はより広い asserted type payload extraction、type-assertion
semantic checking、recorded fact、theorem acceptance、dedicated
`formula_statement` runner、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 113 addendum: `mizar-test` は exact imported attribute assertion theorem
formula checker bridge について task 103 を supersede する。runner は
`parser.type_fixtures` を import して
`ImportedAttributeAssertionPayloadBoundary: 1 is empty` を示す source だけを認識し、
imported `empty` attribute provenance を検証し、source-derived numeral
`TermInput` と attribute-assertion `FormulaInput` payload を `type_elaboration`
で渡してから、missing numeric type payload、missing formula / attribute semantic
payload、partial formula checking で fail closed してよい。この addendum は
imported module AST extraction、attribute-chain semantic payload、theorem formula
向け checker `AttributeInput` payload extraction、term inference、attribute
admissibility/semantic checking、formula checking、recorded fact、theorem
acceptance、dedicated `formula_statement` runner、CoreIr、ControlFlowIr、VC、proof
payload を昇格しない。

Task 114 addendum: `mizar-test` は exact attribute-level non-empty imported
attribute assertion theorem formula sidecar について task 104 を supersede する。
active `type_elaboration` runner は `parser.type_fixtures` を import し、direct
`non` surface と documented imported `empty` attribute provenance を検証し、
source-derived numeral と attribute-assertion formula checker payload を渡してから
missing numeric type payload、missing formula / attribute semantic payload、
partial formula checking で fail closed する。この addendum は imported module AST
extraction、theorem formula 向け checker `AttributeInput` payload extraction、
negated attribute-chain semantic payload、term inference、negated attribute
admissibility/semantic checking、formula checking、recorded fact、theorem
acceptance、dedicated `formula_statement` runner、CoreIr、ControlFlowIr、VC、proof
payload を昇格しない。

Task 111 addendum: `mizar-test` は task-105 set-enumeration theorem formula
extraction-gap boundary のうち exact
`SetEnumerationPayloadBoundary: {1, 2} = {1, 2}` source だけを supersede する。
active `type_elaboration` runner は 4 つの numeral item term、2 つの
set-enumeration term、builtin equality formula の real checker payload を渡し、
missing numeric type payload、missing set-enumeration result-type
payload、partial formula checking の checker diagnostic で fail closed する。この
addendum は broader set-enumeration payload、term inference、equality/formula
checking、theorem acceptance、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

Task 112 addendum: `mizar-test` は task-99 connective/quantifier theorem formula
extraction-gap boundary のうち exact
`FormulaConnectiveQuantifierPayloadBoundary: contradiction implies for x being set holds not contradiction`
source だけを supersede する。active `type_elaboration` runner は implication、
quantified formula、negation の source-derived checker `FormulaInput` shell を渡し、
missing formula payload と missing quantifier payload の checker diagnostic で
fail closed する。task 117 はこの exact source の 2 つの `contradiction`
constant だけを `FormulaKind::Contradiction` payload に進める。この addendum は
formula constant semantic truth value、child-formula graph payload、quantifier
binder/context payload、formula checking、theorem acceptance、CoreIr、ControlFlowIr、
VC、proof payload を昇格しない。

Task 88 addendum: `mizar-test` は proof-block / proof-skeleton extraction-gap
boundary の active runner metadata も所有する。新しい theorem source sidecar は
`theorem ProofSkeletonPayloadBoundary: thesis proof thus thesis; end;` を
`type_elaboration` で parser / resolver まで実行してよいが、checker-owned proof
skeleton payload extraction、local proof context、formula payload extraction、
recorded fact、theorem acceptance、dedicated `formula_statement` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は proof skeleton payload、formula payload、
local fact、theorem acceptance、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。
task 89 と合わせ、MT-AUDIT-015 の formula/theorem/proof gap wording は
task-106 builtin equality theorem checker bridge、task-107 builtin inequality theorem checker bridge、task-108 builtin membership theorem checker bridge、task-109 builtin type assertion theorem checker bridge、task-110 imported predicate/functor theorem checker bridge、task-111 set-enumeration theorem checker bridge、task-112/task-117 connective/quantifier formula shell checker bridge、task-113 imported attribute assertion checker bridge、task-114 exact attribute-level non-empty imported attribute assertion checker bridge、task-117 exact formula statement checker bridge、および task-105/task-88/task-89 extraction-gap boundary にだけ狭まり、その
boundary を超える semantic term/formula/proof payload extraction は未対応の
ままである。

Task 89 addendum: `mizar-test` は statement-level proof-justification
extraction-gap boundary の active runner metadata も所有する。新しい theorem source
sidecar は labeled `A: thesis proof ... end;` statement と final
`thus thesis proof ... end;` conclusion を `type_elaboration` で parser /
resolver まで実行してよいが、checker-owned statement proof payload extraction、
nested proof skeleton payload、local proof context、formula payload extraction、
label-reference semantic checking、recorded fact、theorem acceptance、dedicated
`formula_statement` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は statement proof payload、proof skeleton
payload、formula payload、local fact、theorem acceptance、CoreIr、ControlFlowIr、
VC、proof payload を昇格しない。MT-AUDIT-015 の formula/theorem/proof gap wording
は task-106 builtin equality theorem checker bridge、task-107 builtin inequality theorem checker bridge、task-108 builtin membership theorem checker bridge、task-109 builtin type assertion theorem checker bridge、task-110 imported predicate/functor theorem checker bridge、task-111 set-enumeration theorem checker bridge、task-112/task-117 connective/quantifier formula shell checker bridge、task-113 imported attribute assertion checker bridge、task-114 exact attribute-level non-empty imported attribute assertion checker bridge、task-117 exact formula statement checker bridge、および task-105/task-88/task-89 extraction-gap boundary にだけ狭まり、その
boundary を超える semantic term/formula/proof payload extraction は未対応の
ままである。

Task 90 addendum: `mizar-test` は predicate/functor definition extraction-gap
boundary の active runner metadata も所有する。新しい definition source sidecar は
predicate definition と functor definition を `type_elaboration` で parser /
resolver まで実行してよいが、checker-owned definition declaration payload
extraction、definition-local context、definiens formula/term payload、overload
payload、recorded fact、dedicated `formula_statement` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は definition payload、formula/term body
payload、overload payload、fact、CoreIr、ControlFlowIr、VC、proof payload を昇格
しない。MT-AUDIT-015 の non-builtin declaration gap wording は task-90
extraction-gap boundary にだけ狭まり、その boundary を超える semantic definition
payload extraction は未対応のままである。

Task 91 addendum: `mizar-test` は attribute definition extraction-gap boundary
の active runner metadata も所有する。新しい definition source sidecar は
attribute definition を `type_elaboration` で parser / resolver まで実行してよいが、
checker-owned attribute definition declaration payload extraction、
definition-local context、formula-definiens payload、attributed-type evidence、
recorded fact、dedicated `formula_statement` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は definition payload、formula body payload、
evidence、fact、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。
MT-AUDIT-015 の non-builtin declaration gap wording は task-91 extraction-gap
boundary にだけ狭まり、その boundary を超える semantic attribute definition
payload extraction は未対応のままである。

Task 92 addendum: `mizar-test` は mode/structure definition extraction-gap
boundary の active runner metadata も所有する。新しい definition source sidecar は
mode definition と structure definition を `type_elaboration` で parser / resolver
まで実行してよいが、checker-owned mode/structure definition declaration payload
extraction、mode expansion、structure base-shape / constructor / selector
evidence、definition-local context、recorded fact、dedicated `formula_statement`
runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は definition payload、mode-expansion payload、
structure evidence、fact、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。
MT-AUDIT-015 の non-builtin declaration gap wording は task-92 extraction-gap
boundary にだけ狭まり、その boundary を超える semantic mode/structure
definition payload extraction は未対応のままである。

Task 93 addendum: `mizar-test` は proof-local declaration extraction-gap
boundary の active runner metadata も所有する。新しい theorem-proof source
sidecar は proof-local `let`、`given`、`consider`、`set`、`reconsider`
statement を `type_elaboration` で parser / resolver まで実行してよいが、
checker-owned proof-local declaration payload extraction、local proof context、
formula / term payload extraction、RHS term inference、reconsider coercion /
obligation evidence、recorded fact、theorem acceptance、dedicated
`formula_statement` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は proof-local declaration payload、
formula / term payload、fact、CoreIr、ControlFlowIr、VC、proof payload を
昇格しない。MT-AUDIT-015 の non-builtin declaration gap wording は task-93
extraction-gap boundary にだけ狭まり、その boundary を超える semantic
proof-local declaration payload extraction は未対応のままである。

Task 94 addendum: `mizar-test` は proof-local inline definition extraction-gap
boundary の active runner metadata も所有する。新しい theorem-proof source
sidecar は proof-local `deffunc` と `defpred` statement を `type_elaboration`
で parser / resolver まで実行してよいが、checker-owned inline definition
formal/body payload extraction、local abbreviation expansion、term / formula
body payload、guard evidence、recorded fact、theorem acceptance、dedicated `formula_statement`
runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は inline definition payload、term /
formula body payload、fact、theorem acceptance、CoreIr、ControlFlowIr、VC、proof
payload を昇格しない。
MT-AUDIT-015 の non-builtin declaration gap wording は task-94
extraction-gap boundary にだけ狭まり、その boundary を超える semantic inline
definition payload extraction は未対応のままである。

Task 95 addendum: `mizar-test` は registration-block extraction-gap boundary の
active runner metadata も所有する。新しい source sidecar は existential cluster と
conditional cluster を含む top-level registration block を `type_elaboration` で
parser / resolver まで実行してよいが、checker-owned registration-item payload
extraction、correctness-condition / proof-obligation payload、accepted
activation / evidence status、cluster / reduction semantics、dedicated
`formula_statement` または `advanced_semantics` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は Chapter 17 semantic row、registration
payload、activation status、fact、CoreIr、ControlFlowIr、VC、proof payload を
昇格しない。

Task 96 addendum: `mizar-test` は redefinition / notation extraction-gap
boundary の active runner metadata も所有する。新しい source sidecar は
top-level と definition-local の synonym / antonym alias、および attribute、
predicate、functor redefinition declaration を `type_elaboration` で parser /
resolver まで実行してよいが、checker-owned redefinition payload extraction、
notation alias relation payload、target inference、coherence proof-obligation
payload、overload candidate payload、dedicated `formula_statement` または
`advanced_semantics` runner が存在するまでは
`type_elaboration.external_dependency.ast_payload_extraction` で fail closed
しなければならない。この addendum は Chapter 11 alias semantic resolution、
Chapter 19 overload / redefinition semantic row、redefinition payload、overload
fact、CoreIr、ControlFlowIr、VC、proof payload を昇格しない。

task 15 の再監査では、blocking な `spec_gap`、採用すべき
`repo_metadata_conflict`、必要な language behavior change、または既存 expectation の
意味変更が必要な箇所は記録していない。

## タスク分解

### Task 1: Lint-policy guard

Purpose: `mizar-test` を workspace lint に opt-in させ、lint exception の
根拠を必須にする。

Completion: `0b24d1a` として commit 済み。

### Task 2: Source/spec gap audit and status sync

Purpose: audit baseline を記録し、drift/gap を分類し、module status を同期する。

Completion: この crate plan と `todo.md` の status/follow-up updates。

Forbidden behavior: この audit commit で `doc/spec`、既存 `.miz`、既存
expectation を修復すること。

### Task 3: Runner modes and CLI completion

Purpose: corpus と coverage metadata に対する validation-mode CLI behavior、
exit-code fixtures、deterministic output を完成させる。

Spec refs: `minimal_crate.md`, `harness.md`.

Completion: validation-mode CLI parsing、strict/permissive unknown-root
policy、profile metadata retention/filtering、plan CLI fixtures、
expectation-schema fixtures、harness documentation sync。

### Task 4: Snapshot API and canonicalization

Purpose: general snapshot module と canonical record API を導入する。

Spec refs: `snapshot.md`.

Completion: general in-memory `SnapshotRecord` API、canonical hash input、
line-ending normalization、local absolute path validation、deterministic
profile metadata ordering、comparison diffs。

### Task 5: Snapshot update policy and determinism checks

Purpose: explicit update flow と repeat-render determinism checks を実装する。

Spec refs: `snapshot.md`.

Completion: explicit `VerifyOnly` versus `Update { reason }` baseline helper、
safe `.snap` baseline path validation、create/update round-trips、no-write
verify failures、repeat-render determinism mismatch reports。

### Task 6: Coverage and pass/fail-mix reporting

Purpose: discovery と traceability data から trace coverage、stored status drift、
corpus pass/fail mix を compute/report する。

Spec refs: `traceability.md`, architecture test strategy.

Completion: per-requirement/per-stage coverage report、mode-aware
coverage/status gate、manifest id-order validation、obsolete-reference
diagnostics、link-validator regression fixtures、deterministic CLI pass/fail
mix output。

### Task 7: Stage-prerequisite validation

Purpose: prerequisites が covered、built-in 宣言済み、または `depends_on` により
accept されるまで higher-stage coverage credit を与えない。

Spec refs: `staged_model.md`, `traceability.md`.

Completion: manifest `depends_on` validation、`built_in` declarations、
stage-mismatch diagnostics、unsatisfied-prerequisite diagnostics、mode-aware
completeness check 前の coverage-credit withholding。

### Task 8: Fail/soundness contract support

Purpose: fail/soundness required-domain bookkeeping、typed failure identity
checks、false-arithmetic coverage、weakening/deletion regression rules を実装する。

Spec refs: `fail_soundness.md`.

Completion: certificate/kernel `rejection_reason` validation、認識済み
`soundness.*` required-case の shape/profile/phase validation、fail/soundness
manifest に対する mode-aware missing-case diagnostics、false-arithmetic stable
key gate、regression fixture。所有する consumer runner が存在する前に real
proof/certificate/kernel execution は捏造しない。

### Task 9: Corpus size and review-rule validation

Purpose: corpus naming、size、class placement、generation metadata、
corpus-policy profile constraints、stress exclusion、fuzz provenance rules を
強制する。

Spec refs: `miz_corpus.md`.

Completion: `[origin]` provenance の parsing/retention、generated/fuzz/property
placement gates、stress root discovery、profile constraints、fuzz-category
preservation、upper-bound `.miz` size diagnostics、naming diagnostics、clean /
violating corpora の metadata fixtures。

### Task 10: Consumer-runner support

Purpose: consumer runner の着地ごとに discovery、expectations、stages、
snapshots、active/planned gates、reporting を同期する。

Spec refs: `harness.md`.

Current increment: `parse-only`、`declaration-symbol`、task-12 の
`type-elaboration` external-gap runner、task-16 の source-derived builtin
type-expression normalization slice、task-17 の builtin `ResolvedTypedAst`
handoff、task-18 の reserve declaration semantic bridge、task-19 の
`ResolvedTypedAstSummary::from_ast` readiness check と次の builtin declaration
inventory は active runner command として prepared/implemented であり検証済み。
task-20 の binder-only `CoreContext` readiness check も active runner command として
prepared/implemented であり検証済み。post-task-20 resolver R-G007 の parser-backed
same-signature/different-return functor conflict active declaration-symbol seed も
active runner command として prepared/implemented であり検証済み。表現済みの
kind、visibility、export status に対する exact SymbolEnv-derived
declaration-symbol pass payload assertion と checker task 50 の same-module
attributed reserve evidence-query active fail slice、checker task 55 の bare
local-mode expansion pass slice、checker task 56 の one-edge same-module
local-mode expansion chain pass/gap slice も prepared/implemented
であり検証済み。checker task 57 の same-module local-mode structure-RHS
evidence-query active fail slice と checker task 58 の same-module local-mode
attributed-builtin-RHS evidence-query active fail slice も prepared/implemented
であり検証済み。checker task 59 の same-module attributed local-mode reserve
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 60 の same-module attributed local-mode structure-RHS
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 61 の same-module attributed local-mode attributed-builtin-RHS
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 62 の same-module local-mode structure-RHS chain evidence-query
active fail slice も prepared/implemented であり検証済み。
checker task 63 の same-module local-mode attributed-RHS chain evidence-query
active fail slice も prepared/implemented であり検証済み。
checker task 64 の same-module attributed local-mode bare-builtin chain
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 65 の same-module attributed local-mode structure-RHS chain
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 66 の same-module attributed local-mode attributed-builtin-RHS chain
evidence-query active fail slice も prepared/implemented であり検証済み。
checker task 67 の structure-qualified attribute extraction-gap active boundary
slice も prepared/implemented であり検証済み。
checker task 68 の argument-bearing mode reserve extraction-gap active boundary
slice も prepared/implemented であり検証済み。
checker task 69 の argument-bearing structure reserve extraction-gap active boundary
slice も prepared/implemented であり検証済み。
checker task 70 の bracket-form local mode reserve extraction-gap active boundary
slice、checker task 71 の bracket-form local structure reserve extraction-gap active boundary
slice、checker task 72 の two-edge bare local-mode chain active pass slice、checker task 73 の three-edge bare local-mode chain active pass slice、checker task 74 の structural bare local-mode chain active pass slice、checker task 75 の lower-stage forward local-mode active-range boundary、checker task 76 の lower-stage forward local-structure active-range boundary、checker task 77 の lower-stage forward local-attribute active-range boundary も prepared/implemented であり検証済み。
checker task 48 の syntax-free reserve source declaration producer seam は
active type-elaboration runner が消費する checker-owned seam として
prepared/implemented である。
より広い checker semantic、CoreIr / ControlFlowIr、VC / proof-verification
coverage は不足している extraction seam に律速される。checker task 29、VC task 15、ATP task 20、kernel task 17 は deferred
trace rows または metadata-only handoff fixtures を持つ `paced/open` のままで、所有する
seam が存在する前に placeholder runner や fake active fixture は作らない。

### Task 11: Determinism suite

Purpose: discovery、plans、diagnostics、reports、snapshot comparisons、将来の
parallel-equivalence surface が byte-stable であることを検証する。

Spec refs: `harness.md`.

Completion: metadata plan と active runner report の canonical-byte stability
tests、`SurfaceAst` 外の generic snapshot nondeterminism diagnostics、
snapshot-level sequential/parallel observable equivalence 用の
`verify_snapshot_parallel_equivalence`。

### Task 12: Public-enum forward-compatibility policy

Purpose: crate API の public enum exhaustiveness/non-exhaustiveness を決め、
文書化する。

Spec refs: all module specs.

Completion: public `mizar-test` source tree のすべての public enum は downstream
`#[non_exhaustive]` であり、所有する英語/日本語 module spec は enum inventory と
decision を記録する。lint coverage は source attributes と EN/JA documentation
inventory の両方を検査する。exhaustive public enum exception は記録しない。

### Task 13: Bilingual documentation sync audit

Purpose: すべての English canonical document と Japanese companion を同期する。

Spec refs: repository documentation policy.

Completion: `bilingual_sync_audit.md` は `doc/design/mizar-test/en/` と
`doc/design/mizar-test/ja/` 配下の全 document に対する paired-file audit を記録する。
task 14 の完了は下に記録する。

### Task 14: Incremental/parallel verification regression matrix

Purpose: architecture-22 regression matrix scenarios の metadata/reporting
support を追加し、実行は consumer crate に置いたままにする。

Spec refs: architecture test strategy and incremental verification contract.

Completion: task 14 は `architecture22_scenarios`、
`architecture22_equivalence_class`、`architecture22_gate` sidecar metadata、
scenario registry、deterministic plan matrix report、18 個すべての required
architecture-22 row の metadata-only planned anchor である
`tests/property/architecture22_matrix_001` を追加した。task 14 scenario はすべて
active eligibility `none` であり、将来の consumer-specific increment が real
execution を所有するまで active gate を reject する。

### Task 15: Architecture-22 follow-up audit

Purpose: task 14 後に source/spec と bilingual documentation audit を再実行し、
残る matrix gap を記録する。

Spec refs: architecture test strategy、incremental verification contract、
repository documentation policy.

Completion: task 15 は task 14 後に source/spec と EN/JA documentation sync audit
を再実行した。`bilingual_sync_audit.md` は follow-up review を記録し、
MT-AUDIT-014 は残る architecture-22 active execution work を consumer-paced
`test_gap` と分類する。task 14 の scenario id、
equivalence class、planned-only gating、traceability anchor は architecture 20 と
22 に一致している。task 10 は consumer-paced のままで、新しく準備済みの runner
increment はない。`spec_gap`、`repo_metadata_conflict`、language behavior change、
既存 expectation の semantic change は不要である。

### Task 16: Source-derived builtin type-expression bridge

Purpose: builtin type expression だけについて `.miz` source から real
checker-owned payload を構築し、checker payload-extraction gap の最初の安全な部分を
閉じる。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、checker
MC-G020。

Completion: task 16 は `mizar-checker` を type-elaboration runner dependency として
追加し、reserve-only の unrecovered な builtin `set` / `object` `TypeExpression`
node を `TypeExpressionInput` に抽出し、`TypeNormalizer` で normalize し、最小の
`TypedAst` shell を組み立て、この対応済み slice の active pass coverage と
imported non-builtin type head の active boundary fail coverage を追加した。未対応の
declaration、term、formula、coercion、attribute、mode、structure、overload、
fact、CoreIr、ControlFlowIr、VC、proof seed は explicit external gap のままにする。

### Task 17: Source-derived builtin ResolvedTypedAst bridge

Purpose: 対応済み source-derived builtin type-expression runner slice を
checker-owned `TypedAst` shell から checker-owned `ResolvedTypedAst` へ昇格する。

Spec refs: `harness.md`、checker `resolved_typed_ast.md`、checker
MC-G020/MC-G027。

Completion: task 17 は task 16 の normalized builtin `set` / `object`
type-expression payload を、real expression metadata、source-preserved node hint、
empty cluster/overload predecessor output により `ResolvedTypedAst::assemble` へ
投影する。runner は対応済み source type site がすべて resolved node、
expression metadata、final type に diagnostic なしで到達することを確認する。
未対応の declaration、non-builtin type head、attribute、mode / structure expansion、
task 固有 theorem bridge を超える term/formula payload、task 112 を超える formula
child/binder semantics、coercion site、overload payload、recorded fact、proof
evidence、CoreIr、ControlFlowIr、VC seed、`proof_verification` row は explicit
external gap のままにする。

### Task 18: Source-derived reserve declaration semantic bridge

Purpose: 対応済み source-derived type-elaboration pass slice を builtin
type-expression node から reserve-only builtin declaration payload へ広げる。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、checker
MC-G011/MC-G016/MC-G020。

Completion: task 18 は bare builtin `set` / `object` type head を持つ
unrecovered top-level reserve item を対応済み reserve declaration slice へ抽出する。
checker task 48 は checker-owned module `BindingEnv`、binding ごとの
`DeclarationInput`、binding 固有の `TypeExpressionInput` site、`DeclarationChecker`
output を構築する syntax-free producer seam を所有し、runner はその handoff を
`TypedAst`、`ResolvedTypedAst` へ継続する。
multi-variable reserve segment は共有 source type range を保つが、distinct typed
site を受け取る。task 96 の redefinition/notation extraction-gap boundary、task 95 の registration block extraction-gap boundary を超える
未対応の non-builtin declaration、imported symbol、
argument-bearing / attributed symbol head、type-argument / term-argument provenance、
structure evidence payload、proof-local declaration payload、inline definition
payload、registration payload、activation / correctness payload、redefinition/notation payload、term、formula、coercion、overload payload、fact、CoreIr / ControlFlowIr / VC payload、proof
evidence は explicit external gap のままである。real payload execution なしに
downstream CoreIr / ControlFlowIr / VC / proof trace row は昇格しない。

### Task 19: Reserve bridge core summary readiness and builtin declaration inventory

Purpose: real reserve-only checker payload を summary boundary で `mizar-core`
が読めることを確認し、未準備の semantics を昇格せずに次の builtin declaration
extraction slice を棚卸しする。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、core
`elaborator.md`。

Completion: task 19 は active runner 専用に `mizar-core` を type-elaboration
runner dependency として追加し、既存の reserve-only builtin `ResolvedTypedAst`
payload を `ResolvedTypedAstSummary::from_ast` に通し、successful active pass case
について source/module identity と empty checker-site output を確認する。inventory
では、まだ次の builtin declaration family は安全に昇格できないと記録する。`let`、
`given`、`consider`、quantified declaration は local scope、assumption、formula、
constraint-discharge payload を必要とし、`set` は RHS term inference、`reconsider`
は coercion / obligation evidence、`deffunc` / `defpred` は body / formal payload を
必要とする。`CoreIr`、`ControlFlowIr`、VC、proof、public diagnostic-code、`.miz`、
expectation semantic change は導入しない。

### Task 20: Reserve bridge core context readiness

Purpose: 同じ real reserve-only checker payload が次の安全な `mizar-core`
boundary を越え、binder-only core context data を準備できることを確認する。
`CoreIr` や downstream verification row は昇格しない。

Spec refs: `harness.md`、`expectation_schema.md`、`traceability.md`、core
`elaborator.md`。

Completion: task 20 は active `type-elaboration` runner を拡張し、reserve-only
builtin `BindingEnv` と `ResolvedTypedAst` handoff から、抽出済み reserve binding
ごとに 1 個の `CoreVariableSeed` と `CoreBinderSeed` を持つ `CoreContextInput` を
構築し、`CoreItemSeed` は渡さない。runner は successful pass case について
source/module identity、binding count、binder source range、checker provenance、
empty item registry、empty core diagnostics、empty core worklist を確認する。
reserve declaration は owner item、term、formula、proof、
algorithm、obligation payload をまだ持たないため、`CoreIr`、`ControlFlowIr`、VC、
proof、public diagnostic-code、`.miz`、expectation semantic change は導入しない。

### Task 21: 訂正後経路の soundness 語彙

Purpose: kernel audit F7 の訂正後 kernel rejection vocabulary を
fail/soundness required-case registry と文書へ追加する。拒否挙動は変えない。

Spec refs: architecture test strategy と kernel `soundness_argument.md` F7。

Completion: task 21 は `invalid_sat_refutation`、`context_mismatch`、
`missing_provenance`、normal policy 下の unsupported legacy certificate に対する
訂正後 `soundness.certificate.*` registry key を追加し、legacy
`invalid_sat_proof` audit key を保持する。既存 corrected-path certificate sidecar は
`domain = "certificate"` とそれらの stable identity へ付け替えられた。proof、
certificate、kernel runner behavior は変えない。

Spec coverage audit impact: unchanged。traceability status、follow-up owner、
owner crate、deferred coverage rationale は変えない。

### Task 22: certificate corpus root 命名の調停

Purpose: kernel audit F8 を解決するため、architecture test strategy、kernel
audit note、certificate corpus README を、実装済み `tests/certificates/` root に
一致させる。

Spec refs: architecture test strategy と kernel `soundness_argument.md` F8。

Completion: task 22 は architecture 20(英日)、certificate corpus README、本
crate plan、kernel soundness argument(英日)を更新し、`tests/certificates/` を
certificate/kernel-evidence corpus の正準 root とし、`tests/kernel_evidence/` を
退役済みの監査 draft 名だけにする。Rust source、`doc/spec`、`.miz` tests、
expectations、runner behavior、traceability metadata、semantic intent は変更しない。

Spec coverage audit impact: spec chapter coverage、design mapping、follow-up
ownership、traceability status、deferred coverage rationale は変わらないため、
`doc/design/spec_coverage_audit.md` 更新は不要。

## Exit Criteria

Hard gates:

- blocking な `spec_gap`、採用すべき `repo_metadata_conflict`、language
  behavior change が隠れたまま残らない
- crate exit 時点で source behavior が undocumented のまま残らない
- task-owned gap は修正済み、または理由付きで明示的に deferred
- `doc/design/mizar-test/en/` と `doc/design/mizar-test/ja/` が同期している
- 将来の user request が明示的に意図を変えない限り、`doc/spec`、既存
  `.miz`、既存 expectation は触らない

Verification commands:

- `cargo test -p mizar-test`
- `cargo clippy -p mizar-test --all-targets -- -D warnings`
- `cargo fmt --check`

Review expectations:

- task-specific spec/docs review
- test sufficiency review
- implementation review
- source/documentation consistency review


## task 201 immediate-radix asserted-head active slice

task 201 は `BaseModeRadixAssertedHead -> set`、`OuterModeRadixAssertedHead -> BaseModeRadixAssertedHead`、outer-mode reserve 1 個、theorem `ChainedLocalModeRadixAssertedHeadPayloadBoundary: x is BaseModeRadixAssertedHead;` を持つ exact active pass source 1 件を追加する。これは `test_gap` と narrow `source_drift`、`design_drift` であり、`spec_gap` はない。runner は real AST/resolver expansion 2 個を消費し、distinct Outer/Base symbol/site/range を保持し、formula asserted symbol が binding expansion の resolved immediate radix であることを要求し、ordinal 1 を `BindingId(0)` に解決し、known entry 3 個を Base-definition-RHS builtin-set identity 1 個へ normalize する。inferred variable 1 個と checked type assertion 1 個は expected constraint/candidate/fact/diagnostic/deferred reason 0 個を持つ。shared backlink 5 個 + dedicated requirement 1 個が既存 expectation を rebaseline せず、364 cases / 328 requirements 内の active runner 149 を保護する。broader asserted head/semantics は deferred のままであり、Step 5 は active、Steps 6/7 は deferred である。


## task 202 object immediate-radix asserted-head active slice

task 202 は exact active object-terminal source `BaseObjectModeRadixAssertedHead -> object`、`OuterObjectModeRadixAssertedHead -> BaseObjectModeRadixAssertedHead`、outer reserve 1 個、Base asserted type を追加する。変更しない closed immediate-radix relation は real expansion 2 個を消費し、distinct Outer/Base provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、known entry 3 個を Base-definition-RHS builtin-object identity 1 個へ normalize する。inferred variable 1 個と checked assertion 1 個は constraint/candidate/fact/diagnostic/deferred 0 個で、object/set coercion を持たない。shared 5 + dedicated 1 backlink が 365 cases / 329 requirements 内の active runner 150 を保護する。broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。


## task 203 two-edge immediate-radix asserted-head active slice

task 203 は Base/Middle/Outer local-mode definition 3 個、Outer reserve 1 個、immediate Middle radix の assertion を持つ exact active set-terminal source 1 件を追加する。Task 72 の real expansion 3 個と Task 186 の formula consumer が、変更しない closed immediate-radix relation へ入力される。runner は distinct Outer/Middle provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 3 個を消費し、known entry 3 個を Base-definition-RHS builtin-set identity 1 個へ normalize し、constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を出力する。shared 5 + dedicated 1 backlink と exact/corruption/reordering/duplicate/spelling/imported/ambiguous/deeper/real-route isolation/sidecar guard が 366 cases / 330 requirements 内の active runner 151 を保護する。two-hop Base assertion、object sibling、broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。


## task 204 two-edge object immediate-radix asserted-head active slice

task 204 は Base/Middle/Outer local-mode definition 3 個、Outer reserve 1 個、immediate Middle radix の assertion を持つ exact active object-terminal source 1 件を追加する。Task 72 の real object expansion 3 個と Task 187 の formula consumer が、変更しない closed immediate-radix relation へ入力される。runner は distinct Outer/Middle provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 3 個を消費し、known entry 3 個を Base-definition-RHS builtin-object identity 1 個へ normalize し、constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を object/set coercion なしで出力する。shared 5 + dedicated 1 backlink と exact/corruption/order/duplicate/spelling/imported/ambiguous/deeper/real-route isolation/sidecar guard が 367 cases / 331 requirements 内の active runner 152 を保護する。two-hop Base assertion と broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。

## task 205 three-edge set immediate-radix asserted-head active slice

task 205 は Base/Inner/Middle/Outer local-mode definition 4 個、Outer reserve 1 個、immediate Middle radix の assertion を持つ exact active set-terminal source 1 件を追加する。Task 73 の real set-terminal expansion 4 個と Task 195 の formula consumer が、変更しない closed immediate-radix relation へ入力される。runner は distinct Outer/Middle provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 4 個を消費し、known entry 3 個を Base-definition-RHS builtin-set identity 1 個へ normalize し、constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を出力する。shared 5 + dedicated 1 backlink と exact/corruption/all-23-orders/missing/duplicate/label/spelling/radix/imported/ambiguous/deeper/multi-hop/bidirectional-16-route/sidecar guard が 368 cases / 332 requirements 内の active runner 153 を保護する。multi-hop Inner/Base assertion、matching object sibling、broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。

## task 206 three-edge object immediate-radix asserted-head active slice

task 206 は Base/Inner/Middle/Outer local-mode definition 4 個、Outer reserve 1 個、immediate Middle radix の assertion を持つ exact active object-terminal source 1 件を追加する。Task 73 の real object-terminal expansion 4 個と Task 196 の formula consumer が、変更しない closed immediate-radix relation へ入力される。runner は distinct Outer/Middle provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 4 個を消費し、known entry 3 個を Base-definition-RHS builtin-object identity 1 個へ normalize し、constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を object/set coercion なしで出力する。shared 5 + dedicated 1 backlink と exact/corruption/all-23-orders/per-definition/imported/ambiguous/deeper/multi-hop/local-other/bidirectional-17-route/sidecar guard が 369 cases / 333 requirements 内の active runner 154 を保護する。multi-hop Inner/Base assertion と broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。

## task 207 four-edge set immediate-radix asserted-head active slice

task 207 は Base/Inner/Middle/Outer/TooDeep local-mode definition 5 個、TooDeep reserve 1 個、immediate Outer radix の assertion を持つ exact active set-terminal source 1 件を追加する。Task 74 の real set-terminal expansion 5 個と Task 197 の formula consumer が、変更しない closed immediate-radix relation へ入力される。runner は distinct TooDeep/Outer provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 5 個を消費し、known entry 3 個を Base-definition-RHS builtin-set identity 1 個へ normalize し、constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を出力する。shared 5 + dedicated 1 backlink と exact/corruption/all-119-orders/per-definition/imported/ambiguous/deeper/multi-hop/local-other/bidirectional-20-route/sidecar guard が 370 cases / 334 requirements 内の active runner 155 を保護する。multi-hop Middle/Inner/Base assertion、matching object sibling、broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。

## task 208 four-edge object immediate-radix asserted-head active slice

task 208 は Base/Inner/Middle/Outer/TooDeep local-mode definition 5 個、TooDeep reserve 1 個、immediate Outer radix の assertion を持つ exact active object-terminal source 1 件を追加する。Tasks 74/153 の real object-terminal expansion 5 個と Task 198 の formula consumer が、変更しない closed immediate-radix relation へ入力される。runner は distinct TooDeep/Outer provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 5 個を消費し、known entry 3 個を Base-definition-RHS builtin-object identity 1 個へ normalize し、constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を object/set coercion なしで出力する。shared 5 + dedicated 1 backlink と exhaustive all-119-orders/source/provenance/corruption/bidirectional-21-route/sidecar guard が 371 cases / 335 requirements 内の active runner 156 を保護する。multi-hop Middle/Inner/Base assertion と broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。

## task 209 seven-expansion set immediate-radix asserted-head active slice

task 209 は ordered `BaseMode -> set` から `ChainMode6 -> ChainMode5` までの definition 7 個、ChainMode6 reserve 1 個、immediate ChainMode5 radix の assertion を持つ exact active set-terminal source 1 件を追加する。Task 74 の real seven-expansion producer と Task 199 の formula consumer が、変更しない closed immediate-radix relation へ入力され、Task 175 は builtin asserted-type sibling/owner route と reusable output guard を提供する。runner は distinct ChainMode6/ChainMode5 provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 7 個を消費し、known entry 3 個を BaseModeDef-RHS builtin-set identity 1 個へ normalize し、constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を出力する。shared 5 + dedicated 1 backlink、全 5,039 nonidentity order、宣言済み finite source/provenance/corruption matrix、Task 209 実装前の既存 type-assertion owner route 34 件すべてとの bidirectional isolation、immutable output、real sidecar が 372 cases / 336 requirements 内の active runner 157 を保護する。multi-hop ChainMode4 から BaseMode、object sibling、broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。

## task 210 seven-expansion object immediate-radix asserted-head active slice

task 210 は ordered `BaseObjectMode -> object` から `ChainObjectMode6 -> ChainObjectMode5` までの definition 7 個、ChainObjectMode6 reserve 1 個、immediate ChainObjectMode5 radix の assertion を持つ exact active object-terminal source 1 件を追加する。Task 74 の real seven-expansion object producer と Task 200 の formula consumer が変更しない closed immediate-radix relation へ入力され、Task 179 は builtin-object asserted-type sibling と reusable output guard、Task 209 は matching set-terminal immediate-radix sibling を提供する。runner は distinct ChainObjectMode6/ChainObjectMode5 provenance を保持し、ordinal 1 を `BindingId(0)` に解決し、expansion 7 個を消費し、known entry 3 個を BaseObjectModeDef-RHS builtin-object identity 1 個へ normalize し、object/set coercion なしで constraint/candidate/fact/diagnostic/deferred reason 0 個の inferred variable 1 個と checked assertion 1 個を出力する。shared 5 + dedicated 1 backlink、全 5,039 nonidentity order、宣言済み finite source/provenance/corruption matrix、Task 210 実装前の既存 type-assertion owner route 35 件すべてとの bidirectional isolation、immutable output、real sidecar が 373 cases / 337 requirements 内の active runner 158 を既存 expectation を変更せず保護する。multi-hop ChainObjectMode4 から BaseObjectMode、imported-positive expansion、broader semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。

## task 211 two-edge set two-hop asserted-head active slice

task 211 は `BaseTwoHopModeAssertedHead -> set`、`MiddleTwoHopModeAssertedHead -> BaseTwoHopModeAssertedHead`、`OuterTwoHopModeAssertedHead -> MiddleTwoHopModeAssertedHead`、Outer reserve 1 個、書かれた link 2 本をまたぐ Base formula assertion を持つ exact active set-terminal source 1 件を追加する。Task 72 は real AST-derived expansion 3 個を供給し、既存 reserved-variable formula bridge と checker normalizer は raw Outer subject と独立に resolve された Base asserted type を消費する。新しい独立 closed relation は pairwise-distinct symbol を持つ actual bare edge 2 本と exact Base-to-set terminal を検証し、generic terminal traversal だけを evidence にしない。output は ordinal 1 / `BindingId(0)`、real expansion 3 個、Base-definition-RHS builtin-set identity 1 個へ normalize する known entry 3 個、inferred variable 1 個、constraint/candidate/fact/diagnostic/deferred reason 0 個の checked type assertion 1 個である。全5 nonidentity definition order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 211 実装前の既存 type-assertion owner route 36 件すべてとの bidirectional isolation が route を保護する。test-first fixture 1 件、shared 5 + dedicated 1 backlink により、既存 expectation を変更せず 374 cases / 338 requirements 内の active runner を 158 から 159 へ増やす。object sibling、他 distance、imported-positive/attributed/argument-bearing head、general reachability/widening/`qua`、acceptance、truth/fact、proof、CoreIr/ControlFlowIr/VC、general chain semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。checker source/module-layout change は不要であった。

## task 212 two-edge object two-hop asserted-head active slice

task 212 は `BaseTwoHopObjectModeAssertedHead -> object`、`MiddleTwoHopObjectModeAssertedHead -> BaseTwoHopObjectModeAssertedHead`、`OuterTwoHopObjectModeAssertedHead -> MiddleTwoHopObjectModeAssertedHead`、Outer reserve 1 個、書かれた link 2 本をまたぐ Base formula assertion を持つ exact active object-terminal source 1 件を追加する。Task 72 は real AST-derived object expansion 3 個を供給し、既存 reserved-variable formula bridge と checker normalizer は raw Outer subject と独立に resolve された Base asserted type を消費する。Task 211 の独立 closed relation は pairwise-distinct symbol を持つ actual bare edge 2 本と exact Base-to-object terminal を検証し、generic terminal traversal だけを evidence にしない。output は ordinal 1 / `BindingId(0)`、real expansion 3 個、Base-definition-RHS builtin-object identity 1 個へ normalize する known entry 3 個、inferred variable 1 個、constraint/candidate/fact/diagnostic/deferred reason 0 個の checked type assertion 1 個であり、object/set coercion はない。全5 nonidentity definition order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 212 実装前の既存 type-assertion owner route 37 件すべてとの bidirectional isolation が route を保護する。test-first fixture 1 件、shared 5 + dedicated 1 backlink により、既存 expectation を変更せず 375 cases / 339 requirements 内の active runner を 159 から 160 へ増やす。他 distance、imported-positive/attributed/argument-bearing head、general reachability/widening/`qua`、acceptance、truth/fact、proof/CoreIr/ControlFlowIr/VC、object/set coercion、general chain semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。checker source/module-layout change は不要であった。

## task 213 three-edge set two-hop asserted-head active slice

task 213 は `BaseThreeEdgeModeTwoHopAssertedHead -> set`、`InnerThreeEdgeModeTwoHopAssertedHead -> BaseThreeEdgeModeTwoHopAssertedHead`、`MiddleThreeEdgeModeTwoHopAssertedHead -> InnerThreeEdgeModeTwoHopAssertedHead`、`OuterThreeEdgeModeTwoHopAssertedHead -> MiddleThreeEdgeModeTwoHopAssertedHead`、Outer reserve 1 個、書かれた link 2 本をまたぐ Inner formula assertion を持つ exact active set-terminal source 1 件を追加する。Task 73 は real AST-derived expansion 4 個を供給し、Task 195 の formula/checker seam は raw Outer subject と独立に resolve された Inner asserted type を消費する。refine した closed two-link relation は pairwise-distinct な explicit Outer-to-Middle/Middle-to-Inner edge check を維持し、残る Inner-to-Base-to-set tail は relation evidence ではなく terminal normalization としてだけ検証する。output は ordinal 1 / `BindingId(0)`、real expansion 4 個、Base-definition-RHS builtin-set identity 1 個へ normalize する known entry 3 個、inferred variable 1 個、constraint/candidate/fact/diagnostic/deferred reason 0 個の checked type assertion 1 個である。全23 nonidentity definition order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 211/212 focused regression、Task 213 実装前の既存 type-assertion owner route 38 件すべてとの bidirectional isolation が route を保護する。test-first fixture 1 件、shared 5 + dedicated 1 backlink により、既存 expectation を変更せず active runner 160 から 161、cases 375 から 376、requirements 339 から 340、type-elaboration coverage 207/195 から 208/196 へ増やす。object sibling、Base/full-distance assertion、deeper/imported-positive/attributed/argument-bearing head、general reachability/widening/`qua`、acceptance、truth/fact、proof/CoreIr/ControlFlowIr/VC、general chain semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。checker source/module-layout change は不要であった。

## task 214 three-edge object two-hop asserted-head active slice

task 214 は `BaseThreeEdgeObjectModeTwoHopAssertedHead -> object`、`InnerThreeEdgeObjectModeTwoHopAssertedHead -> BaseThreeEdgeObjectModeTwoHopAssertedHead`、`MiddleThreeEdgeObjectModeTwoHopAssertedHead -> InnerThreeEdgeObjectModeTwoHopAssertedHead`、`OuterThreeEdgeObjectModeTwoHopAssertedHead -> MiddleThreeEdgeObjectModeTwoHopAssertedHead`、Outer reserve 1 個、書かれた link 2 本をまたぐ Inner formula assertion を持つ exact active object-terminal source 1 件を追加する。Task 73 は real AST-derived object expansion 4 個を供給し、Task 196 の formula/checker seam は raw Outer subject と独立に resolve された Inner asserted type を消費する。変更しない closed two-link relation は pairwise-distinct な explicit Outer-to-Middle/Middle-to-Inner edge check を維持し、残る Inner-to-Base-to-object tail は relation evidence ではなく terminal normalization としてだけ検証する。output は ordinal 1 / `BindingId(0)`、real expansion 4 個、Base-definition-RHS builtin-object identity 1 個へ normalize する known entry 3 個、inferred variable 1 個、object/set coercion なしの constraint/candidate/fact/diagnostic/deferred reason 0 個の checked type assertion 1 個である。全23 nonidentity definition order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 211/212/213 focused regression、Task 214 実装前の既存 type-assertion owner route 39 件すべてとの bidirectional isolation が route を保護する。test-first fixture 1 件、shared 5 + dedicated 1 backlink により、既存 expectation を変更せず active runner 161 から 162、cases 376 から 377、requirements 340 から 341、type-elaboration coverage 208/196 から 209/197、pass/fail 192/184 から 193/184 へ増えた。Base/full-distance assertion、deeper/imported-positive/attributed/argument-bearing head、general reachability/widening/`qua`、acceptance、truth/fact、proof/CoreIr/ControlFlowIr/VC、object/set coercion、general chain semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。checker source/module-layout change は不要であった。

## task 215 four-edge set two-hop asserted-head active slice

task 215 は `BaseFourEdgeModeTwoHopAssertedHead -> set`、`InnerFourEdgeModeTwoHopAssertedHead -> BaseFourEdgeModeTwoHopAssertedHead`、`MiddleFourEdgeModeTwoHopAssertedHead -> InnerFourEdgeModeTwoHopAssertedHead`、`OuterFourEdgeModeTwoHopAssertedHead -> MiddleFourEdgeModeTwoHopAssertedHead`、`TooDeepFourEdgeModeTwoHopAssertedHead -> OuterFourEdgeModeTwoHopAssertedHead`、TooDeep reserve 1 個、書かれた link 2 本をまたぐ Middle formula assertion を持つ exact active set-terminal source 1 件を追加する。Task 74 は real AST-derived set expansion 5 個を供給し、Task 197 の formula/checker seam は raw TooDeep subject と独立に resolve された Middle asserted type を消費する。byte-for-byte 変更しない `BindingTwoHopRadix` relation は pairwise-distinct な explicit TooDeep-to-Outer/Outer-to-Middle edge check を維持し、残る Middle-to-Inner-to-Base-to-set tail は relation evidence ではなく terminal normalization としてだけ検証する。output は ordinal 1 / `BindingId(0)`、real expansion 5 個、Base-definition-RHS builtin-set identity 1 個へ normalize する known entry 3 個、inferred variable 1 個、constraint/candidate/fact/diagnostic/deferred reason 0 個の checked type assertion 1 個である。全119 nonidentity definition order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 211/212/213/214 focused regression、Task 215 実装前の既存 type-assertion owner route 40 件すべてとの bidirectional isolation が route を保護する。test-first fixture 1 件、shared 5 + dedicated 1 backlink により、既存 expectation を変更せず active runner 162 から 163、cases 377 から 378、requirements 341 から 342、type-elaboration coverage 209/197 から 210/198、pass/fail 193/184 から 194/184 へ増えた。object sibling、three-hop Inner/full-distance Base assertion、deeper/imported-positive/attributed/argument-bearing head、general reachability/widening/`qua`、acceptance、truth/fact、proof/CoreIr/ControlFlowIr/VC、general chain semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。checker source/module-layout change は不要であった。

## task 216 four-edge object two-hop asserted-head active slice

task 216 は `BaseFourEdgeObjectModeTwoHopAssertedHead -> object`、`InnerFourEdgeObjectModeTwoHopAssertedHead -> BaseFourEdgeObjectModeTwoHopAssertedHead`、`MiddleFourEdgeObjectModeTwoHopAssertedHead -> InnerFourEdgeObjectModeTwoHopAssertedHead`、`OuterFourEdgeObjectModeTwoHopAssertedHead -> MiddleFourEdgeObjectModeTwoHopAssertedHead`、`TooDeepFourEdgeObjectModeTwoHopAssertedHead -> OuterFourEdgeObjectModeTwoHopAssertedHead`、TooDeep reserve 1 個、書かれた link 2 本をまたぐ Middle formula assertion を持つ exact active object-terminal source 1 件を追加する。Task 74 は real AST-derived object expansion 5 個を供給し、Task 198 の formula/checker seam は raw TooDeep subject と独立に resolve された Middle asserted type を消費する。byte-for-byte 変更しない `BindingTwoHopRadix` relation は pairwise-distinct な explicit TooDeep-to-Outer/Outer-to-Middle edge check を維持し、残る Middle-to-Inner-to-Base-to-object tail は relation evidence ではなく terminal normalization としてだけ検証する。output は ordinal 1 / `BindingId(0)`、real expansion 5 個、Base-definition-RHS builtin-object identity 1 個へ normalize する known entry 3 個、inferred variable 1 個、object/set coercion なしの constraint/candidate/fact/diagnostic/deferred reason 0 個の checked type assertion 1 個である。全119 nonidentity definition order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Tasks 211-215 focused regression、Task 216 実装前の既存 type-assertion owner route 41 件すべてとの bidirectional isolation が route を保護する。test-first fixture 1 件、shared 5 + dedicated 1 backlink により、既存 expectation を変更せず active runner 163 から 164、cases 378 から 379、requirements 342 から 343、type-elaboration coverage 210/198 から 211/199、pass/fail 194/184 から 195/184 へ増えた。three-hop Inner/full-distance Base assertion、deeper/imported-positive/attributed/argument-bearing head、general reachability/widening/`qua`、acceptance、truth/fact、proof/CoreIr/ControlFlowIr/VC、general chain semantics は deferred のまま、Step 5 は active、Steps 6/7 は deferred である。checker source/module-layout change は不要であった。

## task 217 three-edge set three-hop asserted-head active slice

task 217 は exact active source として ordered local-mode definition 4 個、Outer reserve 1 個、書かれた link 3 本すべてをまたぐ Base formula assertion を持つ exact set-terminal source 1 件を追加する。Task 73 は real AST-derived expansion 4 個、Task 195 は real formula/checker consumer を供給する。新しい closed three-link relation は Outer-to-Middle、Middle-to-Inner、Inner-to-Base を直接検証し、Base-to-set は terminal-normalization evidence のみに保つ。全23 nonidentity order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Tasks 211-216 focused regression、先行 owner route 42 件すべてとの bidirectional isolation が route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 380 cases / 344 requirements、type-elaboration coverage 212/200、pass/fail 196/184 内の active runner 165 を保護する。object sibling、他 depth、generic reachability、proof、CoreIr、ControlFlowIr、VC は deferred のまま、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 218 three-edge object three-hop asserted-head active slice

task 218 は ordered local-mode definition 4 個、Outer reserve 1 個、書かれた link 3 本すべてをまたぐ Base formula assertion を持つ exact active object-terminal source 1 件を追加する。Task 73 は real AST-derived object expansion 4 個、Task 196 は real formula/checker consumer を供給する。byte-for-byte 変更しない `BindingThreeHopRadix` relation は Outer-to-Middle、Middle-to-Inner、Inner-to-Base を直接検証し、Base-to-object は terminal-normalization evidence のみに保つ。全23 nonidentity order、finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Tasks 211-217 focused regression、先行 owner route 43 件すべてとの bidirectional isolation が route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 381 cases / 345 requirements、type-elaboration coverage 213/201、pass/fail 197/184 内の active runner 166 を保護する。他 depth、generic reachability、object/set coercion、proof、CoreIr、ControlFlowIr、VC は deferred のまま、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 219 four-edge set three-hop asserted-head active slice

task 219 は ordered local-mode definition 5 個、TooDeep reserve 1 個、書かれた link 3 本をまたぐ Inner formula assertion を持つ exact active set-terminal source 1 件を追加する。Task 74 は real AST-derived set expansion 5 個、Task 197 は real formula/checker consumer を供給する。byte-for-byte 変更しない `BindingThreeHopRadix` relation は TooDeep-to-Outer、Outer-to-Middle、Middle-to-Inner を直接検証し、Inner-to-Base-to-set tail は terminal-normalization evidence のみに保つ。全119 nonidentity order、unconnected unsupported deeper asserted head と actual connected sixth-definition/sixth-edge asserted head の独立 guard を含む finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 207 と Tasks 211-218 focused regression、先行 owner route 44 件すべてとの bidirectional isolation が route を保護する。active fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 382 cases / 346 requirements、type-elaboration coverage 214/202、pass/fail 198/184 内の active runner 167 を保護する。object sibling、Base full-distance assertion、generic reachability、proof、CoreIr、ControlFlowIr、VC は deferred のまま、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 220 four-edge object three-hop asserted-head active slice

task 220 は ordered local-mode definition 5 個、TooDeep reserve 1 個、書かれた link 3 本をまたぐ Inner formula assertion を持つ exact active object-terminal source 1 件を追加する。Task 74 は real AST-derived object expansion 5 個、Task 198 は real formula/checker consumer を供給し、Task 208 は immediate-edge sibling を guard する。byte-for-byte 変更しない `BindingThreeHopRadix` relation は TooDeep-to-Outer、Outer-to-Middle、Middle-to-Inner を直接検証し、Inner-to-Base-to-object tail は terminal-normalization evidence のみに保つ。全119 nonidentity order、unconnected unsupported deeper asserted head と actual connected sixth-definition/sixth-edge asserted head の独立 guard を含む finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Tasks 208 と 211-219 focused regression、先行 owner route 45 件すべてとの bidirectional isolation が route を保護する。active fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 383 cases / 347 requirements、type-elaboration coverage 215/203、pass/fail 199/184 内の active runner 168 を保護する。Base full-distance assertion、generic reachability、object/set coercion、proof、CoreIr、ControlFlowIr、VC は deferred のまま、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 221 four-edge set four-hop asserted-head active slice

task 221 は ordered local-mode definition 5 個、TooDeep reserve 1 個、書かれた link 4 本すべてをまたぐ Base formula assertion を持つ exact active set-terminal source 1 件を追加する。Task 74 は real AST-derived set expansion 5 個、Task 197 は real formula/checker consumer を供給する。active closed `BindingFourHopRadix` relation は TooDeep-to-Outer、Outer-to-Middle、Middle-to-Inner、Inner-to-Base を直接検証し、Base-to-set は terminal-normalization evidence のみに保つ。全119 nonidentity order、unconnected-deeper と actual connected fifth-link の独立 guard を含む exhaustive finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 207 と Tasks 211-220 focused regression、先行 owner route 46 件すべてとの bidirectional isolation が route を保護する。active fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 384 cases / 348 requirements、type-elaboration coverage 216/204、pass/fail 200/184 内の active runner 169 を計上する。object sibling、longer chain、imported-positive definition、attributed/argument-bearing behavior、generic reachability、proof、CoreIr、ControlFlowIr、VC は deferred のまま、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 222 four-edge object four-hop asserted-head active slice

task 222 は ordered local-mode definition 5 個、TooDeep reserve 1 個、書かれた link 4 本すべてをまたぐ Base formula assertion を持つ exact active object-terminal source 1 件を追加する。Task 74 は real AST-derived object expansion 5 個、Task 198 は real formula/checker consumer を供給する。変更しない closed `BindingFourHopRadix` relation は TooDeep-to-Outer、Outer-to-Middle、Middle-to-Inner、Inner-to-Base を直接検証し、Base-to-object は terminal-normalization evidence のみに保つ。全119 nonidentity order、unconnected-deeper と actual connected fifth-link の独立 guard を含む exhaustive finite structural/provenance/corruption matrix、immutable output、real frontend/resolver sidecar、Task 208 と Tasks 211-221 focused regression、先行 owner route 47 件すべてとの bidirectional isolation が route を保護する。active fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 385 cases / 349 requirements、type-elaboration coverage 217/205、pass/fail 201/184 内の active runner 170 を計上する。relevant-crate と workspace verification は成功した。longer chain、imported-positive definition、attributed/argument-bearing behavior、generic reachability、object/set coercion、proof、CoreIr、ControlFlowIr、VC は deferred のまま、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 223 parenthesized reserved-variable equality active slice

task 223 は exact active source `reserve x for set; theorem ParenthesizedReservedVariableEqualityPayloadBoundary: (x) = x;` 1 件を追加する。route は real parser wrapper と inner identifier を検証し、inner/direct-right reference を Task 119 の real reserve `BindingEnv` で解決し、inner reference の source-derived builtin-set type/value を既存 equality consumer へ透明に再利用する。parenthesis 独自 checker payload は捏造しない。finite matrix は direct/right/both/nested/empty/non-identifier/recovered/malformed と non-exact theorem/reserve/formula variant を reject し、独立 wrapper/inner/right provenance、matched/immutable output、先行 reserved-variable binary-formula owner 52 件との bidirectional isolation を保護し、real frontend/resolver sidecar を含む。active fixture と shared 4 + dedicated 1 backlink は既存 expectation を変更せず 386 cases / 350 requirements、type-elaboration coverage 218/206、pass/fail 202/184 内の active runner 171 を計上する。focused、relevant-crate、workspace verification は成功した。arbitrary nesting/operand/precedence、formula grouping、closure materialization、truth/fact、acceptance、proof、child graph、downstream IR は deferred のまま、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 224 seven-expansion set two-hop asserted-head active slice

task 224 は ordered bare definition 7 個 `BaseMode -> set` から `ChainMode6 -> ChainMode5`、`ChainMode6` reserve 1 個、`LongLocalModeTwoHopAssertedHeadPayloadBoundary: x is ChainMode4` を持つ exact active source 1 件を追加する。Task 74 は real AST-derived expansion 7 個、Task 199 は real formula/checker consumer、Task 211 は変更しない closed `BindingTwoHopRadix` を供給し、Task 209 は immediate-edge sibling regression のみに使う。relation は pairwise-distinct `ChainMode6 -> ChainMode5 -> ChainMode4` link を直接検証し、残る tail は terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 48 件との bidirectional isolation、immutable output、focused relation sibling、real sidecar が route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 387 cases / 351 requirements、type-elaboration 219/207、pass/fail 203/184 内の active runner 172 を計上する。focused、relevant-crate、workspace verification は成功した。broader chain/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとする。checker source/module-layout change は不要であった。

## task 225 seven-expansion object two-hop asserted-head active slice

task 225 は ordered bare definition 7 個 `BaseObjectMode -> object` から `ChainObjectMode6 -> ChainObjectMode5`、`ChainObjectMode6` reserve 1 個、`LongLocalObjectModeTwoHopAssertedHeadPayloadBoundary: x is ChainObjectMode4` を持つ exact active source 1 件を追加する。Task 74 は real AST-derived object expansion 7 個、Task 200 は real formula/checker consumer、Task 211 は変更しない closed `BindingTwoHopRadix` を供給し、Task 210 は immediate-edge sibling、Task 224 は set-terminal two-hop sibling とする。relation は pairwise-distinct `ChainObjectMode6 -> ChainObjectMode5 -> ChainObjectMode4` link を直接検証し、残る tail は object-terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 49 件との bidirectional isolation、immutable output、focused sibling、real sidecar が route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 388 cases / 352 requirements、type-elaboration 220/208、pass/fail 204/184 内の active runner 173 を計上する。focused、relevant-crate、workspace verification は成功した。broader chain/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとし、checker source/module-layout change は不要であった。

## task 226 seven-expansion set three-hop asserted-head active slice

task 226 は ordered bare definition 7 個 `BaseMode -> set` から `ChainMode6 -> ChainMode5`、`ChainMode6` reserve 1 個、`LongLocalModeThreeHopAssertedHeadPayloadBoundary: x is ChainMode3` を持つ exact active source 1 件を追加する。Task 74 は real AST-derived set expansion 7 個、Task 199 は real formula/checker consumer、Task 217 は変更しない closed `BindingThreeHopRadix` を供給する。Task 219 は set-terminal three-hop longer-tail sibling、Tasks 209/224 は immediate/two-hop long-chain sibling とする。relation は pairwise-distinct `ChainMode6 -> ChainMode5 -> ChainMode4 -> ChainMode3` link を直接検証し、残る tail は set-terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 50 件との bidirectional isolation、immutable output、focused sibling、real sidecar が route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 389 cases / 353 requirements、type-elaboration 221/209、pass/fail 205/184 内の active runner 174 を計上する。focused、relevant-crate、workspace verification は成功した。object sibling/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとし、checker source/module-layout change は不要であった。

## task 227 seven-expansion object three-hop asserted-head active slice

task 227 は ordered bare definition 7 個 `BaseObjectMode -> object` から `ChainObjectMode6 -> ChainObjectMode5`、`ChainObjectMode6` reserve 1 個、`LongLocalObjectModeThreeHopAssertedHeadPayloadBoundary: x is ChainObjectMode3` を持つ exact active source 1 件を追加する。Task 74 は real AST-derived object expansion 7 個、Task 200 は real formula/checker consumer、Task 217 は変更しない closed `BindingThreeHopRadix` を供給する。Task 220 は object-terminal three-hop longer-tail sibling、Task 226 は depth-matched set sibling、Tasks 210/225 は immediate/two-hop object long-chain sibling とする。relation は pairwise-distinct `ChainObjectMode6 -> ChainObjectMode5 -> ChainObjectMode4 -> ChainObjectMode3` link を直接検証し、残る tail は object-terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 51 件との bidirectional isolation、immutable output、focused sibling、real sidecar が route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 390 cases / 354 requirements、type-elaboration 222/210、pass/fail 206/184 内の active runner 175 を計上する。focused、relevant-crate、workspace verification は成功した。broader semantics/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとし、checker source/module-layout change は不要であった。

## task 228 seven-expansion set four-hop asserted-head active slice

task 228 は ordered bare definition 7 個 `BaseMode -> set` から `ChainMode6 -> ChainMode5`、`ChainMode6` reserve 1 個、`LongLocalModeFourHopAssertedHeadPayloadBoundary: x is ChainMode2` を持つ exact active source 1 件を追加する。Task 74 は real AST-derived set expansion 7 個、Task 199 は real formula/checker consumer、Task 221 は変更しない closed `BindingFourHopRadix` を供給する。Tasks 224/226 は shorter-distance long-chain sibling、Task 222 は object-terminal relation sibling、Task 227 は latest terminal sibling とする。relation は pairwise-distinct `ChainMode6 -> ChainMode5 -> ChainMode4 -> ChainMode3 -> ChainMode2` link を直接検証し、残る tail は set-terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 52 件との bidirectional isolation、immutable output、focused sibling、real sidecar が route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 391 cases / 355 requirements、type-elaboration 223/211、pass/fail 207/184 内の active runner 176 を計上する。focused、relevant-crate、workspace verification は成功した。object sibling/broader semantics/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとし、checker source/module-layout change は不要であった。

## task 229 seven-expansion object four-hop asserted-head active slice

task 229 は ordered bare definition 7 個 `BaseObjectMode -> object` から `ChainObjectMode6 -> ChainObjectMode5`、`ChainObjectMode6` reserve 1 個、`LongLocalObjectModeFourHopAssertedHeadPayloadBoundary: x is ChainObjectMode2` を持つ exact active source 1 件を追加する。Task 74 は real AST-derived object expansion 7 個、Task 200 は real formula/checker consumer、Task 221 は変更しない closed `BindingFourHopRadix` を供給する。Tasks 225/227 は shorter-distance object sibling、Task 222 は object-terminal relation sibling、Task 228 は depth-matched set sibling とする。relation は pairwise-distinct `ChainObjectMode6 -> ChainObjectMode5 -> ChainObjectMode4 -> ChainObjectMode3 -> ChainObjectMode2` link を直接検証し、残る tail は object-terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 53 件との bidirectional isolation、immutable output、focused sibling、real sidecar が active route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 392 cases / 356 requirements、type-elaboration 224/212、pass/fail 208/184 内の active runner 177 を計上する。focused、relevant-crate、workspace verification は成功した。imported-positive definition/broader semantics/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとし、checker source/module-layout change は不要であった。

## task 230 seven-expansion set five-hop asserted-head active slice

task 230 は ordered bare definition 7 個 `BaseMode -> set` から `ChainMode6 -> ChainMode5`、`ChainMode6` reserve 1 個、`LongLocalModeFiveHopAssertedHeadPayloadBoundary: x is ChainMode1` を持つ exact active source 1 件を追加する。Task 74 は real AST-derived set expansion 7 個、Task 199 は real formula/checker consumer を供給する。新規 closed `BindingFiveHopRadix` は pairwise-distinct `ChainMode6 -> ChainMode5 -> ChainMode4 -> ChainMode3 -> ChainMode2 -> ChainMode1` link を直接検証し、`ChainMode1 -> BaseMode -> set` は terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 54 件との bidirectional isolation、immutable output、focused sibling、real sidecar が active route を保護する。test-first fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 393 cases / 357 requirements、type-elaboration 225/213、pass/fail 209/184 内の active runner 178 を計上する。focused、relevant-crate、workspace verification は成功した。object-terminal five-hop/broader semantics/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとし、checker source/module-layout change は不要であった。

## task 231 seven-expansion object five-hop asserted-head active slice

task 231 は ordered bare definition 7 個 `BaseObjectMode -> object` から `ChainObjectMode6 -> ChainObjectMode5`、`ChainObjectMode6` reserve 1 個、`LongLocalObjectModeFiveHopAssertedHeadPayloadBoundary: x is ChainObjectMode1` を持つ exact source 1 件を追加する。Task 74 は real AST-derived object expansion 7 個、Task 200 は real formula/checker consumer、Task 230 は byte-for-byte unchanged closed `BindingFiveHopRadix` を供給する。active relation は pairwise-distinct `ChainObjectMode6 -> ChainObjectMode5 -> ChainObjectMode4 -> ChainObjectMode3 -> ChainObjectMode2 -> ChainObjectMode1` link を直接検証し、`ChainObjectMode1 -> BaseObjectMode -> object` は terminal normalization のみに使う。全5,039 nonidentity order、finite structural/provenance/corruption guard、先行 type-assertion owner 55 件との bidirectional isolation、immutable output、focused sibling、real sidecar が route を保護する。fixture と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 394 cases / 358 requirements、type-elaboration 226/214、pass/fail 210/184 内の active runner 179 を計上する。focused、relevant-crate、workspace verification は成功した。imported-positive definition/broader semantics/downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred のままとし、checker source/module-layout change は不要であった。

## task 233 parenthesized builtin-object equality active slice

task 233 は exact source `reserve x for object; theorem ParenthesizedReservedObjectVariableEqualityPayloadBoundary: (x) = x;` だけを追加する。Task 223 の real single-left `ParenthesizedTerm` producer と Task 188 の real builtin-object reserve/BindingEnv/equality consumer を exact object-specific config で合成し、先行 public owner は一般化しない。finite contract は独立 wrapper/inner/right provenance、ordinal 1/2 の `BindingId(0)` lookup、canonical `BuiltinObject` identity 1 個の透明な再利用、inferred term 2 個、ordered expected constraint 2 個、object/set coercion のない checked equality 1 個、immutable output、先行 binary-formula owner 53 件との bidirectional isolation、real sidecar を保護する。test-first fixture 1 件と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 395 cases / 359 requirements、type-elaboration 227/215、pass/fail 211/184 内の active runner 180 を計上する。arbitrary parenthesization/operand、formula grouping、truth/fact、acceptance、proof/IR/VC、child graph、broader semantics は deferred、Step 5 は active、Steps 6/7 は deferred。checker source/module-layout change は不要であった。

## task 234 six-hop long-chain asserted-head active slice

task 234 は ordered bare definition 7 個 `BaseMode -> set` から `ChainMode6 -> ChainMode5`、`ChainMode6` reserve 1 個、`LongLocalModeSixHopAssertedHeadPayloadBoundary: x is BaseMode` を持つ test-first exact source 1 件を追加する。Task 74 は real AST-derived expansion 7 個すべて、Task 199 は real formula/checker consumer を供給する。新規 closed `BindingSixHopRadix` は `BaseMode` までの pairwise-distinct link 6 本を直接検証し、`BaseMode -> set` は terminal normalization のみに使う。finite contract は全5,039 nonidentity order、exact structural/provenance/removal/corruption guard、先行 type-assertion owner 56 件との bidirectional isolation、immutable output、focused sibling、real sidecar を検証する。fixture 1 件と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 396 cases / 360 requirements、type-elaboration 228/216、pass/fail 212/184 内の active runner 181 を計上する。object-terminal six-hop、imported-positive definition、broader semantics、downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred。checker source/module-layout change は不要であった。

## task 236 object-terminal six-hop long-chain asserted-head active slice

task 236 は ordered bare definition 7 個 `BaseObjectMode -> object` から `ChainObjectMode6 -> ChainObjectMode5`、`ChainObjectMode6` reserve 1 個、`LongLocalObjectModeSixHopAssertedHeadPayloadBoundary: x is BaseObjectMode` を持つ test-first exact source 1 件を追加する。Task 74 は real AST-derived object expansion 7 個すべて、Task 200 は real formula/checker consumer を供給する。byte-for-byte unchanged closed `BindingSixHopRadix` は `BaseObjectMode` までの pairwise-distinct link 6 本を直接検証し、`BaseObjectMode -> object` は terminal normalization のみに使う。finite contract は全5,039 nonidentity order、exact structural/provenance/removal/corruption guard、先行 type-assertion owner 57 件との bidirectional isolation、immutable output、focused sibling、real sidecar を検証する。fixture 1 件と shared 5 + dedicated 1 backlink は既存 expectation を変更せず 397 cases / 361 requirements、type-elaboration 229/217、pass/fail 213/184 内の active runner 182 を計上する。imported-positive definition、broader semantics、downstream payload は deferred、Step 5 は active、Steps 6/7 は deferred。checker source/module-layout change は不要であった。

## Task 241 Parenthesized Reserved-Variable Inequality Active Slice

Task 241 は exact source `reserve x for set; theorem
ParenthesizedReservedVariableInequalityPayloadBoundary: (x) <> x;` だけを追加
する。Task 223 の real single-left `ParenthesizedTerm` producer と Task 121 の
real builtin-set reserve/`BindingEnv`/inequality consumer を、どちらの owner も
一般化せず合成する。closed contract は独立した wrapper/inner/right
provenance、ordinal 1/2 の `BindingId(0)` lookup、canonical `BuiltinSet` 1 個の
透過的再利用、inferred term 2 個、ordered expected constraint 2 個、checked
inequality 1 個、immutable output、先行 binary-formula owner 54 件との
bidirectional isolation、real sidecar を保持する。test-first fixture 1 件と
shared 4 + dedicated 1 backlink は既存 expectation を rebaseline せず active
runner 183、398 cases / 362 requirements、type-elaboration 230/218、pass/fail
214/184 を計上する。parenthesized membership、imported/other parenthesized
variant、desugaring/truth、acceptance、proof/IR/VC、child graph、downstream
payload は Task 241 の credit 外。Step 5 は active、Steps 6/7 は deferred。
checker source/API/module-layout change は不要であった。

## Task 242 Parenthesized Builtin-Object Inequality Active Slice

Task 242 は exact source `reserve x for object; theorem
ParenthesizedReservedObjectVariableInequalityPayloadBoundary: (x) <> x;` だけを
追加する。Task 233 の real single-left object `ParenthesizedTerm` producer と
Task 190 の real builtin-object reserve/`BindingEnv`/inequality consumer を、
どちらの owner も一般化せず合成する。closed contract は独立した wrapper/
inner/right provenance、ordinal 1/2 の `BindingId(0)` lookup、written `object`
anchor の canonical `BuiltinObject` 1 個の透過的再利用、inferred term 2 個、
type entry 6 個、ordered expected constraint 2 個、object/set coercion と独立
wrapper payload のない checked inequality 1 個を保持する。test-first fixture
1 件と shared 5 + dedicated 1 backlink は既存 expectation を rebaseline せず
active runner 184、399 cases / 363 requirements、type-elaboration 231/219、
pass/fail 215/184 を計上する。finite matrix、先行 binary-formula owner 55 件
との bidirectional isolation、focused Tasks 190/223/233/241、immutable output、
real sidecar が route を保護する。parenthesized membership と active imported
provenance は Task 242 の credit 外。未成立 imported expansion/evidence/
signature payload と proof/CoreIr/ControlFlowIr/VC は deferred。Step 5 は
active、Steps 6/7 は deferred。checker source/API/module-layout change は不要で
あった。

## Task 243 Parenthesized Reserved-Variable Membership Active Slice

Task 243 は exact source `reserve x for set; theorem
ParenthesizedReservedVariableMembershipPayloadBoundary: (x) in x;` だけを追加
する。Task 223 の real single-left `ParenthesizedTerm` producer と Task 120 の
real reserve/`BindingEnv`/membership consumer を、どちらの owner も一般化せず
合成する。変更しない direct-right producer が唯一の expected-set input を
供給する。closed contract は独立 wrapper/inner/right provenance、ordinal 1/2
の `BindingId(0)` lookup、written `set` anchor の canonical `BuiltinSet` 1 個、
inferred term 2 個、type entry 5 個、left expected input 0 個、right-owned
expected constraint 1 個、独立 wrapper payload のない checked membership 1
個を保持する。test-first fixture 1 件と shared 4 + dedicated 1 backlink は既存
expectation を rebaseline せず active runner 185、400 cases / 364 requirements、
type-elaboration 232/220、pass/fail 216/184 を計上する。finite matrix、先行
binary-formula owner 56 件との bidirectional isolation、focused Tasks 120/223/
233/241/242、immutable output、real sidecar が route を保護する。旧 extraction
gap の解除はこの exact source だけ。object-left/set-right parenthesized
membership と active imported provenance は Task 243 credit 外。未成立
imported expansion/evidence/signature payload と proof/CoreIr/ControlFlowIr/VC
は deferred。Step 5 は active、Steps 6/7 は deferred。checker source/API/
module-layout change は不要であった。

## Task 244 Parenthesized Heterogeneous Reserve Membership Active Slice

Task 244 は exact source `reserve x for object; reserve y for set; theorem
ParenthesizedHeterogeneousReserveMembershipPayloadBoundary: (x) in y;` だけを
追加する。この `test_gap` + narrow `source_drift` + `design_drift` slice は
Chapters 03/04/13/14/16、Task 233 の real object `ParenthesizedTerm` producer、
既存 Task 125 two-binding membership intent から導く。`spec_gap` ではない。

active runner は real frontend/resolver から finite config-driven private bridge
へ到達する。ordered distinct written reserve 2件から ordinal 2/3、
`BindingId(0/1)`、別々の object/set normalized identity、inferred term 2件、
type entry 5件、left expected なし、right expected-set constraint 1件、fact/
candidate/diagnostic/deferred/coercion/wrapper semantic reference なしの checked
membership を生成する。従来5 parenthesized config は不変。

exact/near-miss source matrix、provenance/range と immutable-output corruption、
既存 binary owner 57件、Tasks 120/125/223/233/241/242/243 focused regression、
real imported-mode-gap diagnostic guard、real frontend/resolver sidecar を含む。
runner は active 186件。repository plan は cases/requirements 401/365、type
233/221、pass/fail 217/184。shared backlink 5件 + dedicated Task 244 requirement
1件を追加する。extraction gap を解除するのは exact source だけ。その他
parenthesized shape と imported-positive provenance は Task 244 credit 外。
未成立 imported expansion/evidence/signature payload と proof/CoreIr/
ControlFlowIr/VC は deferred。Step 5 は active、Steps 6/7 は deferred。
checker source/API/module-layout change は不要。

## Task 245 Right-Parenthesized Reserved-Variable Membership Active Slice

Task 245 は Chapters 04/13/14/16 から exact `reserve x for set; theorem
RightParenthesizedReservedVariableMembershipPayloadBoundary: x in (x);` だけを
追加する。real frontend/resolver の right-side `ParenthesizedTerm` producer を
Task 120 の unchanged membership/expected-set consumer へ接続する。

contract は explicit `Right` side、ordinal 1/2 の双方 `BindingId(0)`、written-
set `BuiltinSet` 1件、inferred term 2件、type entry 5件、left expected なし、
right-inner-owned expected constraint 1件、wrapper semantic output なしを保持。
finite corruption、Task-243 cross-route、immutable/module、既存 owner 58件の
双方向、Left route 6件、real sidecar を cover。runner 187、plan 402/366、
type 234/222、pass/fail 218/184、shared 4 + dedicated 1 backlink。その他 shape/
imported-positive は credit 外、未成立 imported/proof/downstream payload は
deferred。Step 5 active、Steps 6/7 deferred。checker source/API/module-layout
change は不要。

## Task 246 Parenthesized Two-Edge Local-Mode Equality Active Slice

Task 246 は Chapters 04/07/13/14/16 から exact fixture 1件を追加する:
`set` 終端の ordered bare local mode 3件、Outer reserve、`(z) = z`。harness
は real Left `ParenthesizedTerm`、real expansion 3件、raw Outer role input
4件、ordinal 1/2 の `BindingId(0)`、terminal Base-RHS `BuiltinSet` 1件、
inferred term 2件、entry 6件、ordered constraint 2件、clean checked equality
1件、wrapper semantic output なしを要求する。finite order/shape/provenance/
corruption、conditional node-allowlist regression、Tasks 134/223 cross-
rejection、immutable/module、既存 owner 59件、real sidecar が runner 188 を
guard。shared 5 + dedicated 1、plan 403/367、type 235/223、pass/fail 219/184。
broader/downstream は credit 外または deferred、Step 5 active、Steps 6/7
deferred。

## Runner Module-Boundary Refactor Task Decomposition

Task 248 は runner layout を MT-AUDIT-022 `design_drift` と分類し、
[module_boundary_audit.md](./module_boundary_audit.md) に behavior-preserving gate
を定める。Chapter 03、04、07、13、14、16、既存 `.miz` test、trace row、
expectation、harness contract は変更しない authority のままである。以下は編集前に
fresh inventory する。large nonempty family は bounded subtask へ分割してよいが、
no-op task/commit は禁止する。

| Task | Spec/design refs | Affected files/tests | Spec coverage audit impact | Completion | Forbidden behavior |
|---|---|---|---|---|---|
| 248 | `harness.md`、`minimal_crate.md`、`expectation_schema.md`、internal 07 | paired module audit、crate plan、todo、README、bilingual audit。source/test edit なし | unchanged | current ownership、dependency map、target layout、move order、preservation matrix、exit criteria が paired/review 済み | source move、semantic change、test/expectation/trace edit |
| 249 | module-boundary audit | `src/runner.rs`、`src/runner/tests.rs`、unit test 272件 | unchanged | Task 249 complete。inline private module 全体を機械的に移し、exact test list、privacy、behavior が baseline と一致 | rename、dedup、generalization、child-module test path、cleanup |
| 250 | module-boundary audit test-support owner | `src/runner/tests.rs`、`src/runner/tests/support.rs`、shared builder/helper | unchanged | Task 250 complete。nonempty support fragment を root-include し、qualified test name が完全一致 | behavior edit、helper redesign、visibility expansion |
| 251 | `harness.md` parse-only contract | `src/runner/tests.rs`、`src/runner/tests/parse_only.rs`、既存 parse-only provider test | unchanged | Task 251 complete。nonempty parse-only family を root-include し name/behavior が一致 | new test、declaration/type semantics、provider change |
| 252 | `harness.md` type-elaboration contract | `src/runner/tests/type_elaboration/source_extraction.rs`、既存 reserve extractor/handoff test | unchanged | Task 252 complete。baseline real source extraction/checker-core handoff test を nonempty family として移動 | assertion/payload change、semantic generalization |
| 253A | 同上と Chapters 04/07/13/14/16 derived intent | `src/runner/tests/type_elaboration/reserved_binary.rs`、先頭の exact bridge test 23件 | unchanged | Task 253A complete。先頭の baseline reserved/binary block を name/order/isolation 不変で移動。埋め込まれた Task 189 は baseline reserved-object boundary、Task 246 は parenthesized binary route の owner に保持し、Task 253 は pending | route/owner transfer、rename、dedup、expectation rebaseline |
| 254 | `harness.md` と Chapters 04/07/13/14/16 derived intent | `src/runner/tests/type_elaboration/mode_chain.rs`、既存 non-long-chain local-mode/object-mode test 26件 | unchanged | Task 254 complete。連続する direct から four-edge までの set/object membership/equality/inequality family を name/order/isolation、expansion/provenance assertion、retained separator、直後の Task 253B boundary 不変で移動 | chain generalization、fixture/diagnostic edit、long-chain/helper transfer、Task 253B reorder |
| 253B | 同上と Chapters 04/13/14/16 derived intent | mode-chain block 後の direct-reserved fragment、既存 membership/inequality bridge test | unchanged | 2 direct reserved-variable test を元の order 位置で name/isolation 不変のまま移動し Task 253 を完了 | reorder、route merge、type-assertion transfer、rename、dedup、expectation rebaseline |
| 255 | 同上と Chapter 14 derived intent | asserted-head fragment、既存 type-assertion test | unchanged | type-assertion/asserted-head family を exact owner relation 不変で移動 | relation change、assertion weakening、new coverage credit |
| 256 | 同上と既存 long-chain `.miz` intent | long-chain fragment、既存 seven-expansion test | unchanged | long-chain test を finite guard 不変の cohesive nonempty family として移動 | loop/generalization refactor、fixture edit、semantic cleanup |
| 257 | module-boundary audit preservation matrix | corruption/isolation fragment、既存 Task 216-222 nested module と cross-owner test | unchanged | corruption/bidirectional isolation test を nested/qualified name 不変で移動 | module flatten、test deletion/ignore、matrix reduction |
| 258 | `harness.md` runner orchestration | `src/runner.rs`、`src/runner/shared.rs`、active runner integration test | unchanged | test layout 安定後に shared source/frontend staging を private move。output byte-stable | phase ownership change、API exposure、diagnostic edit |
| 259 | `harness.md` parse-only contract | `src/runner.rs`、`src/runner/parse_only.rs`、parse-only unit/integration test | unchanged | parse-only execution/snapshot/failure helper を unchanged facade 背後へ移動 | parser behavior/snapshot semantic change |
| 260 | `harness.md` declaration-symbol contract | `src/runner.rs`、`src/runner/declaration_symbol.rs`、既存 `tests/metadata.rs` declaration test | unchanged | existing resolver observation/payload/failure helper を private move。integration owner 不変 | invented private test、resolver behavior、payload-key change |
| 261 | `harness.md` parser fixture exception | `src/runner.rs`、`src/runner/import_fixtures.rs`、parse/declaration/type integration test | unchanged | existing fixture summary/adapter を symbol/provenance order 不変で移動 | fixture vocabulary、synthetic payload、import behavior change |
| 262 | `harness.md` type-elaboration source bridge | `src/runner.rs`、`src/runner/type_elaboration/` source-extraction leaf、全 active type case | unchanged | exact AST/resolver extraction leaf を fail-closed boundary 不変で移動 | semantic expansion、route generalization、accepted-shape change |
| 263 | `harness.md` と `expectation_schema.md` | `src/runner.rs`、type-elaboration validation/detail/diagnostic leaf、runner/unit/integration test | unchanged | payload validation、detail/expected key、ordering、failure assembly を minimal visibility 背後へ移動 | key/text/order change、assertion weakening、behavior cleanup |
| 264 | module-boundary audit exit criteria | paired source-layout/path table、plan、todo、harness docs、bilingual audit、ownership guard | unchanged | `runner.rs` は facade/top-level orchestration のみ。188、403/367、235/223、219/184、272、exact test list、全 verification 不変で Step 5 再開 | semantic/docs drift、deferred-status promotion、no-op audit update |
