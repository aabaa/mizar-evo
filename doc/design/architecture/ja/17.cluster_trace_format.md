# アーキテクチャ: cluster トレース形式

> 正本は英語です。英語版: [../en/17.cluster_trace_format.md](../en/17.cluster_trace_format.md)。

## 目的

この文書は、cluster／registration の展開の、再生可能なトレース形式を定義します。

cluster 探索はカーネルの外側で行います。カーネルとアーティファクトの消費者が受け取るのは、どの cluster または registration のステップが使われたかを説明する明示的なトレースだけです。

## 関連文書

- [00.pipeline_overview.md](./00.pipeline_overview.md) — 局所検証と決定的ビルドの原則
- [04.type_and_registration_resolution.md](./04.type_and_registration_resolution.md) — registration 解決
- [07.vc_generation.md](./07.vc_generation.md) — VC に含める型事実
- [11.artifact_and_incremental_build.md](./11.artifact_and_incremental_build.md) — cluster のキャッシュ更新
- [18.dependency_fingerprint.md](./18.dependency_fingerprint.md) — cluster 依存のフィンガープリント
- [19.failure_semantics.md](./19.failure_semantics.md) — cluster のループ失敗

## 設計判断

### cluster ステップ

各 cluster 展開ステップは、次の形で表します。

```text
ClusterStep
  source_type
  applied_cluster
  generated_attribute
  generated_type
  dependency
```

`dependency` は、その展開を正当化する定理・定義・registration・mode・属性、またはそれより前の cluster ステップを記録します。

### 連鎖トレース

推移的な展開は、明示的な順序付き連鎖として保存します。

```text
A -> B
B -> C
C -> D
```

トレースはすべての中間ステップを保持しなければなりません。アーティファクトは、元の再生ステップをコンテンツアドレスによる参照として保存しない限り、連鎖を `A -> D` に圧縮してはなりません。

### 決定的な走査

cluster の走査順は、次で固定します。

1. ソース型の正準 ID
2. cluster の由来モジュールパス
3. 宣言のソース順
4. 生成される属性の正準 ID
5. registration のフィンガープリント

ワーカーの完了順・ハッシュマップの反復順・キャッシュの挿入順が、選択されるトレースに影響してはなりません。

### ループ検出

ループ検出は決定的です。

- アクティブな展開スタック内の循環は `cluster_loop` として却下する。
- すでに導出された事実の繰り返しは、正準的なフィンガープリントが一致した後にのみ無視する。
- 最大展開深さと最大生成事実数は検証器設定であり、キャッシュキーに含める。
- 上限の超過は上限つきの失敗であり、必要な事実を黙って切り詰めてはならない。

### トレースの消費者

トレースの消費者は、次を行ってよいです。

- 診断とアーティファクト検証のために cluster の導出を再生する。
- 生成された型事実を VC に含める。
- 依存スライスをフィンガープリントする。
- `@show_resolution` の説明を表示する。

トレースの消費者は、トレースに存在しない追加の cluster を推論してはなりません。

## インターフェイス定義

```rust
struct ClusterTrace {
    source_item: ItemId,
    steps: Vec<ClusterStep>,
    derived_facts: Vec<ClusterFact>,
    traversal_profile: ClusterTraversalProfile,
}

struct ClusterStep {
    source_type: TypeFingerprint,
    applied_cluster: ClusterFingerprint,
    generated_attribute: AttributeFingerprint,
    generated_type: TypeFingerprint,
    dependency: ClusterDependencyRef,
}
```

## 制約と前提

- cluster トレースは意味論的なアーティファクトであり、決定的なハッシュに含める。
- coherence 証明が受理されていない registration は、cluster トレースから不可視である。
- 隠れた推移的展開と暗黙の cluster 挿入は禁止する。
- 失敗系テストには、無限連鎖・循環する registration・意図しない型強制・隠れた推移的展開を含める。
