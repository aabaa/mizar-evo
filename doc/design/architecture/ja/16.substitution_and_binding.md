# アーキテクチャ: 置換と束縛

> 正本は英語です。英語版: [../en/16.substitution_and_binding.md](../en/16.substitution_and_binding.md)。

## 目的

この文書は、evo2 における束縛子・変数・α 同値・置換の意味論を固定します。

これは名前解決・型検査・elaboration・VC 生成・証明書検査・診断に適用されます。目的は、束縛の挙動を決定的かつカーネルが検査可能なものにすることです。

## 関連文書

- [00.pipeline_overview.md](./00.pipeline_overview.md) — イミュータブル IR と小さなカーネルの原則
- [06.elaboration_and_core_ir.md](./06.elaboration_and_core_ir.md) — 表層構文から中核論理への低位化
- [07.vc_generation.md](./07.vc_generation.md) — 義務の生成
- [15.kernel_certificate_format.md](./15.kernel_certificate_format.md) — 証明書の置換ステップ
- [19.failure_semantics.md](./19.failure_semantics.md) — 置換失敗の診断

## 設計判断

### 変数の分類

実装は、次の変数の分類を区別します。

- 束縛子が導入する束縛変数
- ローカルコンテキスト由来の自由変数
- パターン・規則・証明書テンプレートで用いる schematic 変数
- elaboration または証明検査の際に導入される、生成された新鮮な変数

各変数は、安定した内部 ID と、ソース側の表示名を持ちます。等価性は束縛の解決後の安定した ID で判定し、表示名では判定しません。

### 束縛変数と自由変数

自由変数の集合は、正規化された束縛子の構造から計算します。

- 束縛子に入ると、その束縛子の変数は本体の自由変数集合から除かれる。
- シャドーイングは、相異なる安定した ID で表す。
- ソース名だけで捕獲を判定しない。
- 受理に必要な自由変数の条件は、証明書にシリアライズする。

### α 同値

2 つの項は、束縛変数の一貫したリネームだけが異なる場合に、α 同値です。

チェッカは、その場限りのテキスト比較ではなく、正準的な束縛子の正規化によって α 同値を判定します。

### 正準的な束縛子の正規化

束縛子の正規化は、字句上の束縛子の順序に従って、束縛子ローカルな正準 ID を割り当てます。

```text
for x being set holds
  ex y being set st P[x, y]
```

これは `x` や `y` という名前ではなく、束縛子の位置によって正規化されます。束縛子の名前だけが異なる同値な論理式は、同一の正規化エンコードとハッシュを生成します。

### 捕獲回避

置換によって、置換される項の自由変数が、対象コンテキストの束縛子に捕獲されてはなりません。

衝突が起こる場合、実装は決定的な新鮮性の戦略により束縛子を α リネームしてから、置換を適用します。決定的なリネームが、記録された副条件を満たせない場合、その置換は却下します。

### リネームの戦略

新鮮な ID は、次から生成します。

- 所有するソースファイルまたは証明書 ID
- 正規化された束縛子のパス
- 元の変数の役割
- 正規化された項に閉じた決定的なカウンタ

生成される表示名は診断専用です。ハッシュや証明の受理に影響してはなりません。

### 置換の再生

置換の再生は純粋関数です。

```text
(normalized_source, normalized_target, binder_context, side_conditions)
  -> accepted normalized result
  -> or invalid_substitution
```

再生は、アクティブな構文解析器の状態・オーバーロードテーブル・cluster テーブル・ATP バックエンドの出力を参照してはなりません。

## インターフェイス定義

```rust
struct BinderContext {
    binders: Vec<BinderFrame>,
    free_variables: Vec<VariableId>,
}

struct BinderFrame {
    binder_id: BinderId,
    canonical_index: u32,
    source_name: Option<String>,
    variable_id: VariableId,
}

struct FreeVariableConstraint {
    variable: VariableId,
    must_remain_free_in: TermPath,
}
```

## 制約と前提

- 束縛子の正規化は、プラットフォームやワーカー数に依存せず安定でなければならない。
- ソースの表示名は診断のために保持するが、解決後の意味論的な等価性からは除外する。
- 不正な置換の根拠はカーネルによる棄却であり、ヒューリスティックな修復の機会ではない。
- ファジングと失敗系テストには、変数の捕獲・束縛子の衝突・不正な置換・α 変換の失敗を含める。
