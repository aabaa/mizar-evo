# Architecture: Reasoning Boundary

> Canonical language: English. Japanese companion: [../ja/08.reasoning_boundary.md](../ja/08.reasoning_boundary.md).

## Purpose

This document defines the reasoning responsibility boundary in Mizar Evo: which checks are performed by the Mizar-side verifier, which obligations are delegated to external ATP/SMT backends, and which results must be accepted by the trusted kernel.

## Context

- [00.pipeline_overview.md](./00.pipeline_overview.md) — overall pipeline; this document refines phases 12-14
- [doc/spec/en/16.theorems_and_proofs.md](../../../spec/en/16.theorems_and_proofs.md) — proof syntax and `by` justification
- [doc/spec/en/17.clusters_and_registrations.md](../../../spec/en/17.clusters_and_registrations.md) — clusters and registrations
- [doc/spec/en/20.algorithm_and_verification.md](../../../spec/en/20.algorithm_and_verification.md) — algorithm verification and verification conditions
- [doc/spec/en/21.source_code_annotation_and_atp.md](../../../spec/en/21.source_code_annotation_and_atp.md) — annotations and ATP integration
- [doc/spec/en/23.package_management_and_build_system.md](../../../spec/en/23.package_management_and_build_system.md) — build lifecycle, cluster-db, and artifact output
- [21.ai_agent_interface.md](./21.ai_agent_interface.md) — extends this trust boundary to AI agent operability: the Red edit class is exactly the set of actions an agent must not use to relax kernel acceptance, policy statuses, or `require_kernel_certificates`

### Pipeline Position

| Phase | Responsibility |
|---|---|
| 12. Pre-ATP Discharge | Discharge obligations that can be solved by Mizar-side deterministic machinery |
| 13. ATP Translation / Dispatch | Translate open VCs to ATP problems and dispatch external provers |
| 14. Kernel Certificate Check | Independently validate ATP evidence and finalize proof status |

## Design Decisions

### Mizar-Side Reasoning

The Mizar-side verifier owns deterministic semantic processing:

- type checking, subtype checks, and coercion checks;
- sethood checks for Fraenkel expressions;
- cluster and registration resolution;
- overload resolution;
- elaboration from surface syntax to core logical IR;
- definitional expansion boundaries;
- trivial VC discharge, computation-based discharge, and type-derived facts.

These responsibilities must be completed before ATP translation. When they close a VC, they must produce replayable evidence, such as a deterministic discharge explanation, a registration `ResolutionTrace`, or a bounded computation trace. The closed VC is not accepted merely because the Mizar-side verifier marked it discharged; phase 14 must either replay the evidence or classify it under an explicit policy status.

### ATP-Side Reasoning

External provers are used for search-heavy logical reasoning:

- first-order reasoning for `by` justification steps;
- equational reasoning over cited premises and local hypotheses;
- property-based reasoning such as commutativity, symmetry, and reflexivity;
- refutation-style proof search when supported by the backend.

ATP backends do not perform type inference, overload resolution, cluster resolution, or namespace resolution.

Properties passed to ATP backends are never invented by the backend. They must come from already resolved Mizar-side facts: checked definitions, validated registrations, local hypotheses, cited premises, or kernel-accepted dependency artifacts. ATP translation may encode those properties as declarations or axioms, but it must preserve enough metadata for certificate checking to confirm that every used property was available in the VC context.

### Kernel Responsibility

The kernel accepts only independently checkable evidence.

- ATP results are evidence, not trusted proof status.
- Proof certificates or replayable witnesses must be checked before acceptance.
- Pre-ATP discharge evidence must be checked with the same acceptance discipline as ATP evidence.
- `externally_attested` proofs may be recorded as policy-controlled exceptions, but they are not equivalent to kernel-verified proofs.

Policy statuses are separate from verified proof status. `externally_attested`, `PolicyOpen`, and `AssumedByPolicy` may appear in artifacts only when the active verifier policy permits them. When `require_kernel_certificates` or an equivalent release policy is enabled, such statuses are reported as errors rather than being promoted to kernel-verified results.

## Alternatives Considered

1. **All reasoning inside Mizar**: simpler trust story, but poor automation and a larger verifier.
2. **All reasoning delegated to ATPs**: powerful search, but weak control over language-specific semantics and trust.
3. **Hybrid boundary**: Mizar owns semantic processing; ATPs own logical search; the kernel owns acceptance.

## Adopted Approach

Mizar Evo adopts the hybrid boundary. This keeps the trusted base small, keeps language-specific checks deterministic, and still uses ATP/SMT backends where they are strongest.

## Interface Definitions

```text
Typed and elaborated Mizar context
  -> local VC + cited premises
  -> pre-ATP discharge evidence or NeedsAtp
  -> ATP translation and backend certificate/witness, when NeedsAtp
  -> kernel certificate check
  -> kernel_verified, discharged_builtin, externally_attested, open, or policy status
```

Only `kernel_verified` and kernel-checked deterministic discharge statuses are trusted proof results. Other statuses are build-policy outcomes and must remain distinguishable in diagnostics, artifacts, and downstream dependency summaries.

## Affected Modules

- `doc/design/mizar-checker/types.md` — type checking and type-derived facts
- `doc/design/mizar-checker/registrations.md` — cluster and registration resolution
- `doc/design/mizar-vc/generator.md` — verification condition generation
- `doc/design/mizar-atp/translator.md` — ATP problem translation
- `doc/design/mizar-atp/backend.md` — ATP backend invocation
- `doc/design/mizar-kernel/certificate.md` — proof certificate validation
- [09.atp_interface_protocol.md](./09.atp_interface_protocol.md)
- [10.atp_backend_integration.md](./10.atp_backend_integration.md)

## Constraints and Assumptions

- The kernel must not blindly trust ATP output.
- The kernel must not blindly trust Mizar-side discharge output.
- ATP unavailability must not prevent parsing, name resolution, type checking, or cluster resolution.
- Certificate validation failure is a proof error, even if the backend reported success.
- Every property encoded for ATP use must be traceable to a resolved and available Mizar-side fact.
