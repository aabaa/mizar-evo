# Task 277B-L: template type-parameter association transport prerequisite

> canonical English: [../en/277B-L.md](../en/277B-L.md)。正本は英語です。

Owner plan は [mizar-checker](../../mizar-checker/ja/00.crate_plan.md#task-index)
と [mizar-test](../../mizar-test/ja/00.crate_plan.md#task-index)。

Stable owner section は checker の [association module](../../mizar-checker/ja/source_template_type_parameter_association.md#task-277b-l-template-type-parameter-association)、[source/spec audit](../../mizar-checker/ja/source_spec_audit.md#task-277b-l-frozen-sourcespecification-mapping)、[module boundary](../../mizar-checker/ja/module_boundary_audit.md#task-277b-l-frozen-module-boundary)、[TODO](../../mizar-checker/ja/todo.md#task-277b-l-template-type-parameter-association)、[bilingual audit](../../mizar-checker/ja/bilingual_sync_audit.md#task-277b-l-contract-parity)、mizar-test の [harness](../../mizar-test/ja/harness.md#checker-task-277b-l-private-association-probe)、[module boundary](../../mizar-test/ja/module_boundary_audit.md#checker-task-277b-l-private-association-probe)、[TODO](../../mizar-test/ja/todo.md#checker-task-277b-l-private-association-probe)、[bilingual audit](../../mizar-test/ja/bilingual_sync_audit.md#task-277b-l-contract-parity) である。

## Status、authority、classification

| 項目 | freeze 値 |
|---|---|
| Status | documentation prerequisite は `HEAD=be3d75b57256906655f1233358c8cdc8b7ad2839` でfreeze済み、implementationは未開始。これはephemeral structural proofだけで、Task 277Bを**readyにしない**。 |
| Authority | canonical authority は `doc/spec/en/18.templates.md` と `doc/spec/en/13.term_expression.md`。immutable prerequisite は [277P1](./PARSER-TEMPLATE-TYPEHEAD-277P1.md) と [277R1](./RESOLVE-TEMPLATE-TYPEPARAM-277R1.md)。ここで新しいspecification/test intentはderiveしない。 |
| Input owner | complete 277R1 が `TemplateTypeParameterSourceCollection`、resolver binding identity、source order、source ordinal、ambiguity policyを所有する。checkerはrecreate/reinterpretせずconsumeする。 |
| Classification | missing neutral bridge は `design_drift`、`source_drift`、Rust `test_gap`。`spec_gap` はない。 |
| Deferral | production transport/consumer、generator binding/mapper/condition owner、type/sethood interpretation、diagnostic、active-stage route、Task 277B semantic readinessはseparate ownerにdeferする。 |

本contractはparser/resolver shapeを変えず、Task 277B implementation、generic template
resolution、formal-to-actual substitution、verdictを選択またはauthorizeしない。

## Freeze した checker API と structural invariant

future standalone module は
`crates/mizar-checker/src/source_template_type_parameter_association.rs`、paired durable
owner は `doc/design/mizar-checker/{en,ja}/source_template_type_parameter_association.md`。
completed 277A `source_template` ownershipとは別である。

exact public surface:

- `SourceTemplateTypeParameterAssociationId` (`new` / `index`)
- immutable `SourceTemplateTypeParameterAssociation`
- `get` / `iter` / `len` / `is_empty` を持つ `SourceTemplateTypeParameterAssociationTable`
- `SourceTemplateTypeParameterAssociationHandoff`
- non-exhaustive `SourceTemplateTypeParameterAssociationError`
- `SourceTemplateTypeParameterAssociationProducer::build(collection: &TemplateTypeParameterSourceCollection, typed_ast: &TypedAst) -> Result<SourceTemplateTypeParameterAssociationHandoff, SourceTemplateTypeParameterAssociationError>`

caller DTO はない。handoff はsource、module、association table、`debug_text` をownする。
row はR1 binding ID、`definition_block`、`parameter`、`binder`、`type_head`、`identifier`
のmapped `TypedNodeId`、R1 parameter/type-head rangeとsource ordinalを保持する。tableは
immutable、existing R1 generator-link orderにdenseである。

exhaustive getter surface は `SourceTemplateTypeParameterAssociationId::new(index: usize) ->
Self` と `index(self) -> usize`; row の `binding() ->
TemplateTypeParameterBindingId`、`definition_block()`、`parameter()`、`binder()`、
`type_head()`、`identifier()` は各 `TypedNodeId`、`parameter_range()` / `type_head_range()`
は各 `SourceRange`、`parameter_source_ordinal()` / `type_head_source_ordinal()` は各
`usize`。table は `get(id) -> Option<&SourceTemplateTypeParameterAssociation>`、
`iter() -> impl Iterator<Item = (SourceTemplateTypeParameterAssociationId,
&SourceTemplateTypeParameterAssociation)>`、`len() -> usize`、`is_empty() -> bool`。
handoff は `source_id() -> SourceId`、`module_id() -> &ModuleId`、`associations() ->
&SourceTemplateTypeParameterAssociationTable`、`debug_text() -> String`だけ。
non-exhaustive error はexactに `EnvironmentMismatch` と
`InvalidAssociation { association: SourceTemplateTypeParameterAssociationId }` を持つ。

`build` のvalidation順序:

1. source/module disagreementは `EnvironmentMismatch`;
2. 各R1 linkはbinding lookup後、`typed_ast.nodes()`をscanし、
   `TypedNode.resolved_node == Some(R1 ResolvedNodeId)`のnodeだけをmatchする。5 resolver
   IDのいずれもmatchが0件または複数ならfail-close;
3. recoveryをrejectし、exact node kindを要求する;
4. Range anchor、次にR1 parameter/type-head range equalityを要求する;
5. binder/definition anchorがnonemptyかつcontainedであることを要求する;
6. direct edgeは `definition -> parameter`、`parameter -> binder`、`type_head -> identifier`;
7. その他のfailureは上記invalid-association variant。

R1がorder/ambiguityを所有するのでreorder/duplicate variantはない。producerは`TypedAst`を
mutateせず、nameをinferせず、resolver IDをdense typed indexへcastせず、semanticをpublish
しない。range/spelling similarityはexact `resolved_node` matchの代用にならず、新しい
Typed/Resolved link slotも追加しない。

## Freeze した real fixture profile

inactive immutable fixture は
`tests/miz/fail/templates/fail_template_fraenkel_over_type_param_001.miz`、701 bytes、
final LF、SHA-256
`32c4a1c1b6c9d98dcb085558a084929e07d4005bf92595865f144456e95854ec`。839-byte
sidecar SHA-256は
`b47ac5113c89cd5703adb0ffd660b52d3e16908c92623dd2f63196aa6a215cb2`、
`advanced_semantics` inactiveのまま。

| role | resolver / typed relation | freeze 値 |
|---|---|---:|
| declaration | `DefinitionBlockItem#53` | typed arena 57 nodes、root 56 |
| parameter | `TemplateParameter#31` | range `606..620`、ordinal 0 |
| binder | `Identifier#2` | range `610..611` |
| generator type head / identifier | `TypeHead#39` / `Identifier#21` | range `678..679`、ordinal 0 |
| binding | R1 binding | `0` |

private probeはidentity transportだけをestablishする。fixture activation、canonical
artifact edit、semantic coverage creditは行わない。

## Future implementation と test boundary

exact future Rust scopeは5 paths:

1. `crates/mizar-checker/src/source_template_type_parameter_association.rs`
2. `crates/mizar-checker/src/lib.rs`
3. `crates/mizar-checker/tests/lint_policy.rs`
4. `crates/mizar-test/src/runner/tests/type_elaboration/template_type_parameter_association.rs`
5. `crates/mizar-test/src/runner/tests.rs`

checkerはexact 4 testを追加、raw list `538 -> 542`:

- `task277bl_maps_exact_resolver_association_to_typed_nodes`
- `task277bl_rejects_environment_missing_and_ambiguous_resolved_nodes`
- `task277bl_rejects_kind_range_recovery_and_direct_edge_corruption`
- `task277bl_rebuilds_deterministically_without_mutating_typed_ast`

sole mizar-test testは
`task277bl_real_fixture_builds_exact_template_type_parameter_association`、raw list
`609 -> 610`。private leafは同じF5 `SurfaceAst`とvalidated `SurfaceResolvedArena`から
test-only `TypedAst`を構築し、frozen 57-node/root-56 kind/range/recovery/child edgeを保持して、
resolver identityをarena mappingだけからattachする。その後producerをdirect callする。
existing helperや277A routeはこの`TypedAst`を供給しない。production runner、facade、
dispatcher、detail key、active routeではない。

277A、`source_template.rs`、Typed/Resolved slot/install、resolver source、Cargo manifest、
specification、`.miz`/expectation/trace/coverage artifact、production runner codeはeditしない。

5-path implementationはchecker `tests/lint_policy.rs`のpublic-enum module list、
source/spec module list、public-API path allowlist、`lib.rs` public-module allowlistへnew moduleを
same commitで追加する。completion documentationは両source/spec auditへnew crate-export rowと
`source_template_type_parameter_association` headingのpublic-surface inventoryを追加する。
本prerequisiteは未存在moduleをcurrent exportとはclaimしない。

## Baseline、documentation scope、audit decision

pre-documentation checkpoint はclean
`be3d75b57256906655f1233358c8cdc8b7ad2839`、`origin/main...HEAD=0/8`、protected
`stash@{0}=f65cf4a13752ec380710814a9ac6392ccb9d75d4` unchanged。contract treeは
`84/84 -> 85/85`。

| inventory | baseline | frozen hash |
|---|---|---|
| checker production source | 31 paths / 187955 lines | path `3235caf1ca8f89617e7db3993f1421547b76b8d203a4d6a550201de4050beebd`; content `eda7f987a775f008de2cfe46aa1e47c256f442980c7e554dd0799b8d63f440ad` |
| checker expected manifest | module追加後32 paths | `9dc5b02f26679677e593ea755394d68533173d2be988b7ef1ddcfd84a41b9787` |
| checker raw library list | 538 tests | `2c367e197c410b49e127e187c8f1172edc7bf6313805cc32a53f22fa04c71767` |
| mizar-test raw library list | 609 tests | `ea6e33af0de7353fa13517962c3b0e182cbcb3fc64bb06e5a61e3113daadb82c` |
| mizar-test production source | 38 paths / 80090 lines | path `0ef395004f7feaadf60da0daba7b5da9c52ea4974850adfa2bd9d09081b242aa`; content `990b5ad4798786d9f87c03f76fdbad92fc2edf1f6d84ef3baad67254c79fdd70` |

このprerequisiteはexact 24 Markdown pathsを変更する: paired contract、EN/JA plan-index
4件、new paired checker module owner、paired checker source/spec・module-boundary・bilingual・
TODO、paired mizar-test harness・module-boundary・bilingual・TODO。future completion
documentationはplan-index 4件を除くexact 20 paths、Rust 5 pathsを含むimplementation totalは25 paths。

`doc/design/spec_coverage_audit.md`はno deltaでSHA-256
`a31f6fb3bd2b561610630497c58284484d00716dd0b7f210f55bef3bc4bfa6db`のまま。
`tests/coverage/spec_trace.toml`は
`55b754c8c4d0d293a1c44e2ba4b0090f407bba1d429b461b6cb4d6ddca9ca2b3`のまま。
protected 277A/R1 count/hash、active behavior、traceability、coverageはunchanged。
legacy compaction section/redirect/ledgerはscope外。full protected inventoryはspecification
`64`、path/content SHA-256
`d900ba9e43ab094925f36493f830e0c6a2964be2911d5d229014a58842067a25` /
`b30dd5519191a4407399826faf91cc58853d7944df7630734b5ab05de48c9f7b`; `.miz`
`343`、`d94980e167b4b8ac196f91e7694ff044080c6fb4d04c135b3cd5e65b9a019f22` /
`54e6ea1254a0bd963c39026d788711507f353e9c6df3b4f9fd268b2e9f240afb`; expectations
`435`、`22a5ee257a294e3f2ed4b24bf9ca243d037bbd798f009d0d1e3a176dad8b4fea` /
`b5f0ed1a8d73bbfb78af5cad87a8d426e97269f7f70163750893a9fe1f39d2ea`; Cargo `21`、
`d93f2816b760d8ba45430d6d8570e598864aa7b20b19a001f45171d36fd3a030` /
`146e9b4024d2c344b2eca9c6f5ca6d6a80a3de427e382953a2280bc63cb3ecca`。
five CLI outputは全てunchangedで、later implementationはdocumentation-time evidenceをclaimせず
frozen stdout hashをremeasureしなければならない。

## Review、verification、exit

independent specification/contract、bilingual、source/documentation-boundary、
test-sufficiency、final-quality reviewが必要で、Task 277B readyをclaimできない。
documentation hard gateはexact 24-path scope、EN/JA parity、valid reciprocal/local link、
protected artifact unchanged、`git diff --check`、recursive task-contract/link lint。

later implementationはfresh authority/baseline inventoryから開始し、five Rust pathsとexact testを
recheck、focusedからrelevant package/workspace verificationへ進み、frozen count/hashを全て
remeasureする。no-findings reviewとseparately authorized commit前にはexitしない。

## Next-task handoff

fresh preflight後だけstandalone association producerをimplementする。parent authority、
public-API acceptance、final scoringはGPT-5.6 Sol `xhigh`。contract-bounded implementation/
first-pass reviewはGPT-5.6 Terra `high`を使用でき、boundary/resolver-identity/semantic
ambiguityはescalateする。repeatable quality gainがある場合だけTerraを`xhigh`へ上げ、
contract freeze後のdeterministic inventoryだけにlower settingを使う。GPT-5.6 Lunaは
本runtimeにexposeされず、本prerequisiteのeffective parent/review routeはGPT-5.6 Sol
`xhigh`である。
