# Task CHECKER-SOURCE-PROPERTY-EQUALS-SELECTOR-IDENTITY-264D: Task264 equals selector identity

> canonical English:
> [../en/CHECKER-SOURCE-PROPERTY-EQUALS-SELECTOR-IDENTITY-264D.md](../en/CHECKER-SOURCE-PROPERTY-EQUALS-SELECTOR-IDENTITY-264D.md)。

Status: task commitでcomplete。これはrepresentation-only、zero-semantic、zero-credit checker
prerequisiteであり、language behavior、diagnostic、obligation、
accepted test intent、trace status、metadata、coverage creditを変更しない。

## Identity、authority、classification

| Field | Frozen value |
|---|---|
| Task | `CHECKER-SOURCE-PROPERTY-EQUALS-SELECTOR-IDENTITY-264D` |
| Primary owner | `mizar-checker::source_property_implementation` |
| Required predecessor | Task264C carrier identityとcomplete Task264 equals transaction |
| Inputs | Task264 `SymbolEnv`とcomplete property、Task252 primary-term、Task254 structure handoff |
| Consumer | Separate parameter/Core-variable prerequisite後のTask264 Core35 equals body input |
| Owning plan | [`mizar-checker` Task Index](../../mizar-checker/ja/00.crate_plan.md#task-index) |
| Coverage | semantic/execution/trace/metadata/coverage creditすべてzero |

Authorityは`doc/spec/en/`、existing `.miz`、trace metadata、expectation、design、
sourceの順を維持する。Chapter 7はequalsをdirect-term definition formとして固定し、
existing Task264 `.miz`がそのexact termを`M.carrier`として固定する。Checker transactionは
property parameter binding 0、structure term/member/base graph、local `carrier` resolver
identityを既にauthenticateする。本taskは欠けていたtyped associationだけをmaterializeする。

`spec_gap`はない。Unassociated Task254 `MemberIdentity` requestはbounded
`source_drift`、public assertion不足はbounded `test_gap`、Core selector termを形成
できない状態は本task完了まで`design_drift`である。他のgap分類はない。

## Frozen APIとexact association

Existing Task264 ownerの隣にprivate-fieldのimmutable association/handoff/error/
producerを追加する。Exact public surfaceはEN contractのRust blockをcanonicalとする。
Handoffはproperty、primary-term、structure handoffをby-value retainし、associationは
implementation、definiens、structure term、member、member-identity request、base edge、
base term、base reference、base binding、whole selector symbolのread-only getterだけを持つ。

Exact scalar associationは`0/0/0/0/0/0/0/0/0`、`selector_symbol`は
`carrier_identity().field_symbol()`全体である。Construction/replayはsame
source/module、exact equals profileとfingerprint、definiens→structure term、exact
selector/member/request/base edge、primary `M`→reference→binding 0、parameter/target
subject joinを検証する。Constructionはexplicit `SymbolEnv`からexact field
symbol/definition/contribution、public/exported Selector kind、sole local-source
contribution effects、normal retained origin、member spellingとの一致を検証し、private
associationは全fieldとそのwhole resolver resultを再現する。

Validation precedenceはenvironment、equals profile、dependency fingerprint、
association。Meansは`UnsupportedProfile`、mixed/foreign inputはpublish前にrejectする。
これはgeneric member resolutionではない。Spelling/shapeだけではresultをauthenticateせず、
explicit `SymbolEnv`、carrier-domain receipt、whole identity、normal provenanceをjointly使う。
Replayは`SymbolEnv`を持たず、private-field immutabilityとretained resolver receiptへ依存し、
independent lower identity oracleをclaimしない。

New debugは`source-property-equals-selector-identity-debug-v1`でmodule、3 retained
fingerprint、9 scalar id、whole selector FQNをdeterministically出力する。Existing Task264
owner debug bytesは不変である。

## Scope、test、forbidden behavior

Rust editはchecker owner/existing unit supportとexisting private Task264 assertion leafに
限定する。Equals positive/replay/getter/debug、Means rejection、same-environment profile
mix、foreign transaction、internal fingerprint/term/reference/member/edge/request/binding/
symbol corruptionをfail closedで検証する。`.miz`、expectation、trace、runner selection、
Typed/Resolved slot、existing Task264 owner debugは変更しない。

Generic resolved-member table、property/field Core item、Core term/formula/definition、
Core variable、normalized type/guard、`it` representation、property-value evaluation、
correctness/coherence seed、diagnostic、obligation、proof、VC、active route、coverage creditを
追加しない。`doc/spec`、existing `.miz`/expectation/trace/snapshotを編集しない。

## Artifact、baseline、review、exit

Durable ownerはpaired contract、paired checker plan/property design/TODO/source-spec/
bilingual/boundary、paired Core readiness/decomposition、paired mizar-test private harness、
central coverage auditである。Stable owner linkはchecker property designの
[equals selector identity association](../../mizar-checker/ja/source_property_implementation.md#equals-selector-identity-association)
とCore decompositionの[Task264 Core35 readiness entry](../../mizar-core/ja/source_family_decomposition.md#task-264d-task264-core35-selector-readiness)である。

Clean baselineはHEAD `b73bf407aa9382f0245a997877e8f66d7640b982`。
Checker source `2625 / 94187`, SHA-256
`0a0f9d887aa6cda7ef11c18936cd27503326c587ad7c8bc3193565828e91fe58`、
checker unit support `2152 / 77946`, SHA-256
`7a7ddb8730f0a39d5739a11a3a2ca094e3997a446fb5b8556b168e34cc48b54d`、
private Task264 leaf `1064 / 46418`, SHA-256
`b474a1bc55997d79fe1a0e83ee194c25c28ee06a0f86f6a0abaa8b9f8bcf5b4d`、
central audit `7432 / 561797`, SHA-256
`4f8b5be69030061211b6b6ea87a1febcda3f390c7aaf099eba46b2b960f3b197`。
Contract treeは`121/121 -> 122/122`、checker/Core/mizar-test unit countは
`580/164/646`から始まる。Protected hashはTask34D264値、stash
`f65cf4a13752ec380710814a9ac6392ccb9d75d4`を維持する。

Post-source measured artifactはchecker owner `3095 / 113368`、SHA-256
`93590a77a860bcc2bf229ac7ecc369770df0f4b36af363e4c42fddead26ab82a`、
checker unit support `2516 / 91066`、SHA-256
`621a904bbda4613fca53845a4c74f48a9114ac6a71484cc921798f9b62dec090`、
private Task264 leaf `1143 / 49788`、SHA-256
`c5d746b3d16bca7088aedfc3a31a286a84737790f8936d1ff48488a95e0b196d`、
central audit `7444 / 562566`、SHA-256
`9da542688b7e83bdaa4204576ee061aef36080a4c2f49f52163b6bfe40ad9de5`。
Contract treeは`122/122`、checker/Core/mizar-test unit countは`582/164/646`。

Pre-source specification/APIとbilingual/boundary reviewはblocking findingなし、
post-source test/implementation/source-doc/API/bilingual/final-quality reviewは
blocking/high/medium findingなしまでrepeatする。Focused/full tests、lint/metadata、fmt、
warnings-denied Clippy、enlarged-stack workspace all-feature tests/doctestsを実行する。
Exitはhard gates `9/9`、quality `>=90/100`、task-only commit、clean postcommit proof、
Task264 parameter/Core-variable contextのfresh inventoryを要求する。

## Completion evidence

Pre-source specification/APIとbilingual/boundary reviewはconstruction-only
`SymbolEnv`、public-enum policy、authority wording、Core readiness link、EN/JA statusの
repair後、blocking/high/medium findingなしで完了した。Post-source test sufficiencyと
implementation/default-deny reviewもbase-edge coordinate、exact `3/3/1` environment
authentication、same-module foreign-source precedence、resolver provenance matrix、全retained
lower coordinateのうちowner-fixed implementation/definiens pairを除くcorruption coverage
追加後にfindingなしで完了した。このpairはpositive assertion/replay validation済みである。
Source/documentation/APIとbilingual/boundary reviewはpublic inventory、measured layout、
legacy-compaction topology、canonical marker、lifecycle state同期後にfindingなしで完了した。

Focused checker Task264D `2/2`、complete source-property family `7/7`、private Task264
selector/type `2/2`がPASS。Checker `582` unit/`16` lint、Core `164`、mizar-test
`646` unit/`3` layout/`15` lint/`137` metadata/`2` public-enum/`21` snapshotがPASS。
Formatting、offline metadata、`git diff --check`、warnings-denied all-target/all-feature
Clippy、coverage plan（`430` cases、`396` requirements、error zero、既存`23` warning不変）、
enlarged-stack all-feature workspace tests/doctestsがPASS。Protected `.miz`/expectation/
trace/stashはfrozen hashを維持する。Parent hard gatesは`9/9` PASS、task-only commit前に
independent final read-only auditもfindingなし、score capなし、hard gates `9/9`、valid
`100/100`で完了した。Operational closureとしてtask-only commitとclean postcommit proofだけが残る。
