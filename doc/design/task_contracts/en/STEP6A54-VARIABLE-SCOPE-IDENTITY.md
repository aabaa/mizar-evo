# Task STEP6A54-VARIABLE-SCOPE-IDENTITY: distinct variable scope boundaries
Canonical language: English; [Japanese pointer](../ja/STEP6A54-VARIABLE-SCOPE-IDENTITY.md). Status: implemented. Tier: full. Owners: [resolver](../../mizar-resolve/en/00.crate_plan.md), [consumer tests](../../mizar-test/en/00.crate_plan.md).
Purpose/source_drift: sibling theorem proofs and quantifiers can receive the same lexical scope path through distinct non-boundary ancestors, allowing a later proof to capture an earlier proof's local variable.
Authority: [spec 4.6](../../../spec/en/04.variables_and_constants.md#46-scoping-and-shadowing), [spec 16.4.2](../../../spec/en/16.theorems_and_proofs.md#1642-scope-and-visibility); unchanged `tests/miz/pass/resolve/pass_declaration_symbol_private_theorem_visibility_001.miz` and a parser-clean scratch variant changing only the second proof's let X to let Y.
Scope: [source variable resolver](../../mizar-resolve/en/names.md#step-5c1-variable-scope-resolution) allocates distinct boundary components per lexical parent. Retain the checked child-index component when unused; on collision allocate checked maximum-used-plus-one. Preserve prefix nesting, noncolliding trees and binding/reference ordering; overflow fails with existing InvalidShape. Reuse existing scope types, without a public seam.
Affected: names.rs, existing runner declaration_symbol tests, paired names owner/plans. Test direct resolve_occurrences against real Frontend/symbol output: own quantifier/proof binding identities and sibling-proof unresolved-reference rejection. Retain existing exact single-quantifier path regressions and legacy resolve multi-theorem rejection. Declaration-symbol admission remains unchanged.
Forbidden/deferred: no spec/.miz/expectation/trace edits, new diagnostic, proof acceptance, complete ResolvedAst or ModuleResolver publication. A collision may displace later numerical components; scope containment and identity, not their numeric spelling, are semantic.
Require independent specification, test-sufficiency, implementation, volume/scope and consistency reviews; cargo fmt --check, cargo clippy --all-targets --all-features -- -D warnings, cargo test. Exit: the genuine cross-proof capture is rejected and existing resolver/consumer behavior outside colliding scopes remains verified.
