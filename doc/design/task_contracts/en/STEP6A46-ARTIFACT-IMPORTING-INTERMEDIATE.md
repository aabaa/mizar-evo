# Task STEP6A46-ARTIFACT-IMPORTING-INTERMEDIATE: artifact-importing workspace intermediates
Canonical language: English; [Japanese pointer](../ja/STEP6A46-ARTIFACT-IMPORTING-INTERMEDIATE.md). Status: implemented. Tier: full. Owner: [driver plan](../../mizar-driver/en/00.crate_plan.md).
Purpose/gap: join the existing artifact lexical provider to the supplied-intermediate consumer; its rejection of captured DependencySummary imports is an external_dependency_gap. Authority: [spec 2](../../../spec/en/02.lexical_structure.md), [spec 12.3/12.4](../../../spec/en/12.modules_and_namespaces.md#123-import-statements), [owner](../../mizar-driver/en/frontend_adapter.md#workspace-intermediate-summaries); dependencies: existing artifact provider, direct source export and sealed publication.
Scope: accept captured artifact imports in clean same-package supplied intermediates while retaining exact workspace parent lineage, canonical named inputs and direct-only public lexical exports. The existing producer validates artifact payloads; consumption does not reread them or claim transitive/semantic acceptance.
Tests: real artifact→intermediate→importer, nonempty named dependencies including an unrelated captured summary, direct-only exports and authentic producer failure without output; retain existing leaf/diagnostic/provider tests.
Affected: frontend_adapter.rs, source_load.rs, paired owner/plan and chapter 2 audit. Artifact validation remains with its existing producer/reader; workspace cross-package targets, reexports, overlay discovery, E0225 and full build acceptance remain deferred. Forbidden: new public API/type/output, graph/scheduler changes, spec or existing .miz/expectation/trace changes, new diagnostics, proof/cache/artifact acceptance.
Require independent specification, test-sufficiency, implementation, volume/scope and consistency reviews; cargo fmt --check, cargo clippy --all-targets --all-features -- -D warnings, cargo test. Exit: a genuine artifact-importing intermediate supplies only its own public lexical symbols with current sealed lineage; later phase gaps still block.
