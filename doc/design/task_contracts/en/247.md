# Task 247: Historical Payload-Family Decomposition

> Canonical language: English. Japanese companion: [../ja/247.md](../ja/247.md).

This historical record owns the shared orchestration and completion evidence
for checker Task 247. The accepted graph, semantic/source audits, runner
consumer state, trace metadata, and coverage mapping remain in their durable
owners.

## Identity And Owners

| Field | Historical value |
|---|---|
| Task | `247` |
| Sequence | Entry baseline `b0930a0c44a4f306d1a1ef2f9e66b4a7bd7f5cf6` -> documentation/traceability completion `0154ad746b00205ae574d5bbb268ac3df85f5640` |
| Purpose | Exhaustively assign the remaining source-payload families without implementing them or granting coverage credit. |
| Primary owner | [Accepted payload-family graph](../../mizar-checker/en/payload_family_decomposition.md) |
| Plan indexes | [checker plan](../../mizar-checker/en/00.crate_plan.md#task-index) and [runner plan](../../mizar-test/en/00.crate_plan.md#task-index) |
| Checker audit owners | [semantic mapping](../../mizar-checker/en/semantic_spec_audit.md#task-247-producer-and-activation-mapping) and [source mapping](../../mizar-checker/en/source_spec_audit.md#task-247-remaining-family-ownership-reconciliation) |
| Runner and trace owners | Existing runner plan/TODO state and [Task-247 traceability](../../mizar-test/en/traceability.md#task-247-source-payload-ownership-traceability) |
| Coverage owner | [Task-247 coverage update](../../spec_coverage_audit.md#checker-task-247-remaining-family-ownership-update) and the five current Task-247 `deferred_reason` records in `tests/coverage/spec_trace.toml` |
| Consumers | Checker Tasks 248--264 and 269--279, `MT10-FS`, `MT10-AS`, resolver Task 31, Task 49, Core Task 32, and explicit external gates |

## Completion Evidence

Task 247 had no dependency on Task 266. Its inventory could reuse the exact
Task-266--268 slice, but it had to cover every remaining family instead of
generalizing the contradiction profile. Commit `0154ad74` accepted the paired
payload graph and assigned bounded declaration/binding, type/attribute/request,
term, formula, statement, definition, proof-local/skeleton, registration,
cluster/reduction-trace, template/overload, and redefinition/notation families
to checker Tasks 248--264 and 269--279 or an explicit external gate. Each
eventual producer must travel transactionally through its applicable
`TypedAst` and `ResolvedTypedAst` owner and a real Task-10 consumer.

`MT10-FS` and `MT10-AS` own the later non-placeholder formula-statement and
advanced-semantics runners. Resolver Task 31 solely activates the same-return
member through `declaration_symbol`; Task 49 activates the other 23 members and
then reconciles and deduplicates the exact 24-fixture set. The already active
different-return conflict remains an unchanged control outside that set.

Task 274 remains blocked-reserved on Gate A1 until a canonical accepted
verifier/artifact-status owner and schema are named. Task 277 owns only the
direct template-role producer; missing scheme/theorem roles remain behind
Gate S1. MC-G004 artifact/schema integration and MC-G005 public diagnostic-code
allocation remain external gates, with MC-G005 the existing nonblocking
`spec_gap`. Task 49 and Steps 6/7 remain gated or deferred. Completion
dependency-authorized Core Task 32 to consume the accepted graph; it did not
authorize Core, CFG, VC, proof acceptance, or any source-payload implementation.

Task 247 changed follow-up ownership only. It changed no production source,
fixture, expectation, active route, trace status/test list, coverage class or
credit, runner count, or baseline count/hash oracle. The completion commit
updated exactly five current Task-247 trace `deferred_reason` owner records;
those records remain deferred and are not executable credit.

## Classification And Preserved State

| Class | Historical decision |
|---|---|
| `design_drift` | Closed by accepting the exhaustive owner graph. |
| `source_drift` / `test_gap` | Assigned to the bounded descendant producers and real consumers; not closed by Task 247 itself. |
| `spec_gap` | No new payload-family gap; MC-G005 remains the nonblocking external diagnostic-code gate. |
| `source_undocumented_behavior` | None inferred or introduced. |
| `test_expectation_drift` | The omitted-`reconsider` disagreement remains owned by parser Task 47; Task 247 did not repair or rebaseline it. |
| `boundary_violation` | None in the accepted graph; inventing unnamed owners, implementations, or credit was forbidden. |
| `repo_metadata_conflict` | None in the historical Task-247 identity. |

The durable graph retains its exact specification and `.miz` references,
family dependencies, consumers, gates, no-credit boundaries, and exit criteria.
The semantic/source, bilingual, runner, trace, coverage, root-roadmap, and Core
owners retain the state they own. This record does not replace or override any
of them.

## Completion Boundary And Handoff

Git history and the retained owner documents authenticate completion. No
historical review score or verification result absent from those artifacts is
invented here. Task 247 completed only the documentation/traceability split;
the descendant tasks and named external gates remain responsible for executable
behavior and coverage.

Handoff: Core Task 32 may consume the accepted graph for its own docs-only
decomposition. Task 49, all unimplemented graph members, Gates A1/S1,
MC-G004/MC-G005, and Steps 6/7 retain their existing owners and unblock
conditions.
