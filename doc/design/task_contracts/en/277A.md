# Task 277A: Direct Parser-Origin Template Transport

> Canonical language: English. Japanese companion: [../ja/277A.md](../ja/277A.md).

This derived task contract freezes only the documentation prerequisite for the
first child slice of umbrella Task 277. It does not override the specification,
existing tests, traceability, or expectations.

## Identity, authority, and readiness

| Field | Frozen value |
|---|---|
| Task | `277A` |
| Status | Task 277A is complete. Independent reviews and final-quality re-review report NO FINDINGS, all nine hard gates PASS without a score cap, and the valid score is `100/100` (`20/20/15/15/10/10/5/5`). Exact staging/cached-diff review, implementation commit `b67b028e07337ff5b72422bc8f16fb8f187b5c06`, and its immediate read-only post-implementation checkpoint are complete. Umbrella Task 277 remains partial. |
| Plans | [checker](../../mizar-checker/en/00.crate_plan.md#task-index); [runner](../../mizar-test/en/00.crate_plan.md#task-index) |
| Primary owner | [Source Template Transport](../../mizar-checker/en/source_template.md) |
| Ownership consumers | [Typed](../../mizar-checker/en/typed_ast.md#task-277a-typed-ownership), [Resolved](../../mizar-checker/en/resolved_typed_ast.md#task-277a-resolved-ownership), [private runner](../../mizar-test/en/harness.md#checker-task-277a-frozen-runner-boundary) |
| Authority | `doc/spec/en/18.templates.md` grammar and roles 90--118, parameter meaning 121--203, scope 388--394, complete grammar 1127--1190 |
| Implementation post-commit checkpoint | Immediately after implementation commit `b67b028e07337ff5b72422bc8f16fb8f187b5c06`, the read-only checkpoint observed `HEAD=b67b028e07337ff5b72422bc8f16fb8f187b5c06`, a clean worktree, `origin/main...HEAD=0/1`, and unchanged protected `stash@{0}=f65cf4a13752ec380710814a9ac6392ccb9d75d4`. |

This slice depends only on completed parser Task 31 syntax direct nodes and the
generic Typed/Resolved framework. It consumes and publishes no incomplete
Task-257/258 formula or theorem artifact. Gate S1 excludes scheme and theorem
source roles. The contradictory pred/func schema-parameter wording (theorem
only at lines 293/316 versus theorem plus algorithm at 391/782) is a blocking
`spec_gap` for algorithm/pred/func-parameter slices, but not for this fixture:
it has neither an algorithm nor pred/func schema parameters.

## Frozen fixture and bounded meaning

The immutable parse-only fixture is
`tests/miz/pass/parser/pass_parser_template_arguments_001.miz`: 207 bytes,
final LF, SHA-256
`f7f564754b8e6d5d3a788fa3889ebd21909b9b9403a94513836221c508d0fda7`.
Its expectation remains `parse_only`; diagnostics/recovery are `0`, token
leaves `60`, nodes `116`, root `115 Root 0..206`, and snapshot is 16620 bytes,
SHA-256 `f0b7330189c83ccadfa86bbe554657f9df100508a1cf91bf48b1b17a687a8306`.

All five tables contain two Normal rows:

| Table | Exact rows |
|---|---|
| Parameter | node 60 `13..27`, parent 112 `DefinitionBlockItem`; node 63 `30..41`, parent 112 |
| Loci | group 65 `75..78`, parent 66 `PredicatePattern`; group 76 `120..123`, parent 77 `FunctorPattern` |
| Locus | node 64 `76..77`, group 65; node 75 `121..122`, group 76 |
| Arguments | group 91 `178..181`, parent 92 `PredicateHead`; group 100 `190..193`, parent 101 `TermReference` |
| Argument | node 90 `179..180`, group 91; node 99 `191..192`, group 100 |

Use-site groups are targetless raw suffixes only. Parent kinds are provenance,
not semantics. This task must not create actual kinds, a callee/target,
formal-to-actual mapping, theorem/formula relation, substitution request or
result, inference, compatibility, or a verdict.

## Frozen ABI, ownership, and implementation scope

The new checker owner `source_template.rs` exposes public
`SourceTemplate{Parameter,Loci,Locus,Arguments,Argument}Id`,
`SourceTemplateHandoffInput { source_id, module_id, ...five vectors }`, five
input row types, `SourceTemplateRecovery::{Normal, Recovered}`,
`SourceTemplateParentKind::{DefinitionBlockItem, PredicatePattern,
FunctorPattern, PredicateHead, TermReference}`, and
`SourceTemplateParameterKind::{AbstractTypeSyntax, TypedValueSyntax}`, and
`SourceTemplateProducer::build(input, &TypedArena)`. The immutable handoff,
tables, getters, and debug rendering are public; `validate_installation` is
crate-private. Parameter inputs and rows retain this kind, with the exact
fixture rows ordered `AbstractTypeSyntax` (`T`) then `TypedValueSyntax` (`x`).
Parent-bearing Parameter/Loci/Arguments inputs carry site/range/source ordinal/
recovery/parent/parent kind; child Locus/Argument inputs carry group/ordinal/
site/range/source ordinal/recovery. There is no spelling, symbol, definition,
contribution, resolver identity, or request table.

The five generated public IDs are `SourceTemplateParameterId`,
`SourceTemplateLociId`, `SourceTemplateLocusId`,
`SourceTemplateArgumentsId`, and `SourceTemplateArgumentId`. The literal
public surface is exactly the handoff input, the five row inputs, the three
data enums above plus `SourceTemplateError`, the five immutable row/table pairs,
`SourceTemplateHandoff`, and `SourceTemplateProducer`. All four public enums
(`SourceTemplateRecovery`, `SourceTemplateParentKind`,
`SourceTemplateParameterKind`, and `SourceTemplateError`) are
`#[non_exhaustive]`; no exhaustive exception is authorized.

Error precedence is exactly `EnvironmentMismatch`, `InvalidParameter`,
`InvalidLoci`, `InvalidLocus`, `InvalidArguments`, `InvalidArgument`,
`DuplicateTemplateSite`, `ReorderedParameter`, `ReorderedLoci`,
`ReorderedLocus`, `ReorderedArguments`, `ReorderedArgument`. IDs are dense;
validation directly checks TypedArena adjacency/order/source/range/recovery.
`EnvironmentMismatch` is reserved for source-identity mismatch; a zero-length
or inverted row range is rejected by that row family's `Invalid*` error.
`TypedAst` and `ResolvedTypedAst` each own one neutral
`Option<SourceTemplateHandoff>`: duplicate same-slot installation fails, and
Resolved revalidates then clones. This slot may coexist with unrelated semantic
slots and must not fan out into semantic profile gates.

The cfg-test-only private runner selects the exact nodes, builds the indexed
all-surface `TypedArena`, and exposes `source_template_output` plus a mutation
variant. It has no active dispatcher or detail key. Exactly four checker and
four runner tests use prefix `task277a_`:

- Checker: `task277a_producer_preserves_exact_direct_template_profile`,
  `task277a_rejects_invalid_parent_kind_and_direct_edge`,
  `task277a_rejects_group_order_and_duplicate_sites`, and
  `task277a_rejects_environment_recovery_range_and_duplicate_installation`.
- Runner: `task277a_runner_extracts_exact_real_surface_profile`,
  `task277a_runner_replays_deterministically_through_typed_and_resolved`,
  `task277a_runner_rejects_corrupt_template_edges_and_order`, and
  `task277a_runner_stays_private_targetless_and_semantic_free`.

The exact Rust scope is nine physical paths:

1. `crates/mizar-checker/src/source_template.rs`
2. `crates/mizar-checker/src/lib.rs`
3. `crates/mizar-checker/tests/lint_policy.rs`
4. `crates/mizar-checker/src/typed_ast.rs`
5. `crates/mizar-checker/src/resolved_typed_ast.rs`
6. `crates/mizar-test/src/runner/type_elaboration/source_template.rs`
7. `crates/mizar-test/src/runner/type_elaboration.rs`
8. `crates/mizar-test/src/runner/tests/type_elaboration/source_template.rs`
9. `crates/mizar-test/src/runner/tests.rs`

`runner.rs` is expressly excluded. Parser/syntax/resolver sources and docs,
`doc/spec`, all `.miz`/expectations/trace metadata, diagnostics, active routes,
coverage credit, root TODO, `spec_coverage_audit`, traceability, semantic audit,
`source_application`, legacy-compaction files and historical redirect sections
are also forbidden. Documentation and implementation require separate commits.

The documentation prerequisite changes exactly these 28 Markdown paths:

1. `doc/design/task_contracts/en/277A.md`
2. `doc/design/task_contracts/ja/277A.md`
3. `doc/design/mizar-checker/en/00.crate_plan.md`
4. `doc/design/mizar-checker/ja/00.crate_plan.md`
5. `doc/design/mizar-test/en/00.crate_plan.md`
6. `doc/design/mizar-test/ja/00.crate_plan.md`
7. `doc/design/mizar-checker/en/source_template.md`
8. `doc/design/mizar-checker/ja/source_template.md`
9. `doc/design/mizar-checker/en/typed_ast.md`
10. `doc/design/mizar-checker/ja/typed_ast.md`
11. `doc/design/mizar-checker/en/resolved_typed_ast.md`
12. `doc/design/mizar-checker/ja/resolved_typed_ast.md`
13. `doc/design/mizar-checker/en/source_spec_audit.md`
14. `doc/design/mizar-checker/ja/source_spec_audit.md`
15. `doc/design/mizar-checker/en/module_boundary_audit.md`
16. `doc/design/mizar-checker/ja/module_boundary_audit.md`
17. `doc/design/mizar-checker/en/bilingual_sync_audit.md`
18. `doc/design/mizar-checker/ja/bilingual_sync_audit.md`
19. `doc/design/mizar-checker/en/todo.md`
20. `doc/design/mizar-checker/ja/todo.md`
21. `doc/design/mizar-test/en/harness.md`
22. `doc/design/mizar-test/ja/harness.md`
23. `doc/design/mizar-test/en/module_boundary_audit.md`
24. `doc/design/mizar-test/ja/module_boundary_audit.md`
25. `doc/design/mizar-test/en/bilingual_sync_audit.md`
26. `doc/design/mizar-test/ja/bilingual_sync_audit.md`
27. `doc/design/mizar-test/en/todo.md`
28. `doc/design/mizar-test/ja/todo.md`

The implementation commit may update exactly the same list except the four
crate-plan indexes (items 3--6), so its completion-document scope is items
1--2 and 7--28 (`24` paths). Those updates are limited to implemented status,
the source-spec module/export/public-surface inventories, public-enum policy,
final counts/hashes, review/verification evidence, and handoff state. Together
with the nine Rust paths, the exact implementation scope is `33` paths.

## Baselines, audit impact, and gates

Checker tests project `534 -> 538` and runner tests `604 -> 608`. Checker paths
project `30 -> 31`, path SHA-256
`3235caf1ca8f89617e7db3993f1421547b76b8d203a4d6a550201de4050beebd`; runner
paths project `37 -> 38`, path SHA-256
`0ef395004f7feaadf60da0daba7b5da9c52ea4974850adfa2bd9d09081b242aa`.
Current checker is 186162 lines/content SHA-256
`aeb472fb32ba2c3252b65fc9b0ceb81001a1b36a6486834bec113bd2bc4142fb`; runner is
79769 lines/content SHA-256
`2b642db1b23a8bb932a434ef7914f696951c998748644999486a107057effdfa`.
Final line and content measurements are fresh implementation evidence.

Current raw test-list SHA-256 values are checker
`542b3ed2ca7f84d1a78603e1ef3e2ee4ac963b50b4f764cdc819f5a4a43b3ad3` and runner
`4ca6de65d417874fea0c9d8491beb41a10ccfc2c188b4a7ddc3971a27db55c68`. Protected
sets remain specification `64`, `.miz` `343`, expectations `435`, and Cargo
`21`; path/content SHA-256 pairs are respectively
`d900ba9e43ab094925f36493f830e0c6a2964be2911d5d229014a58842067a25` /
`b30dd5519191a4407399826faf91cc58853d7944df7630734b5ab05de48c9f7b`,
`d94980e167b4b8ac196f91e7694ff044080c6fb4d04c135b3cd5e65b9a019f22` /
`54e6ea1254a0bd963c39026d788711507f353e9c6df3b4f9fd268b2e9f240afb`,
`22a5ee257a294e3f2ed4b24bf9ca243d037bbd798f009d0d1e3a176dad8b4fea` /
`b5f0ed1a8d73bbfb78af5cad87a8d426e97269f7f70163750893a9fe1f39d2ea`, and
`d93f2816b760d8ba45430d6d8570e598864aa7b20b19a001f45171d36fd3a030` /
`146e9b4024d2c344b2eca9c6f5ca6d6a80a3de427e382953a2280bc63cb3ecca`.

Corpus/trace remains `428/395`, `235/193`, `23/0`, `101/7/205/1`,
`type259=247+12`, trace SHA-256
`55b754c8c4d0d293a1c44e2ba4b0090f407bba1d429b461b6cb4d6ddca9ca2b3`.
Task-contract pairs project `81/81 -> 82/82`. `spec_coverage_audit.md` has no
owned-state impact and remains unchanged. Gap classification is
`design_drift`, `source_drift`, and `test_gap`; the algorithm wording
`spec_gap` is deferred, there is no `test_expectation_drift`, and any existing
repository metadata conflict is report-only.

The committed implementation adds `source_template` to the generic module/export,
public-surface, and public-enum inventories in checker `tests/lint_policy.rs`
and the paired source-spec audit. This is policy integration only: it adds no
new lint test count, semantic admission, or syntax dependency.

Before implementation, independently review contract/spec completeness and
EN/JA parity. Afterwards review test sufficiency, implementation,
source/documentation consistency, and bilingual parity; all nine hard gates
must pass and final read-only quality is at least `90/100`. Verify focused
`task277a_` tests, relevant libraries, `cargo fmt --check`, Clippy, tests,
protected count/hash replay, `git diff --check`, and the recursive contract/link
lint. The evidence below records the completed verification and final-quality
result. Exact staging/cached-diff review passed, and the implementation and
clean post-commit evidence are recorded below.

## Implementation and Post-Commit Evidence

The frozen implementation is present in exactly the nine Rust paths and the
completion-state delta is confined to exactly the 24 Markdown paths declared
above: `9 + 24 = 33` paths. The four crate-plan indexes, `runner.rs`, canonical
specification and test artifacts, traceability, coverage, Cargo, active routes,
diagnostics, semantic owners, and legacy redirects remain unchanged.

Checker library state is `538` tests with raw-list SHA-256
`a7c7ff8ea0fbb59aacb73a82f8f0c919d2bc87e3d6f6d59d56992e44032a0cac`.
Checker production is `31` paths / `187955` lines, with path/content SHA-256
`3235caf1ca8f89617e7db3993f1421547b76b8d203a4d6a550201de4050beebd` /
`eda7f987a775f008de2cfe46aa1e47c256f442980c7e554dd0799b8d63f440ad`.
The new `source_template.rs` is `1745` lines with physical SHA-256
`fdd6ac38557979ed37fd7c9ba13300b8577416e4ebbdaefe64b986f22aceb85b`.

Runner library state is `608` tests with raw-list SHA-256
`1025494e7948e8997a522178b0e8f409effee7c350e8527d32412863af73e3d6`.
Runner production is `38` paths / `80090` lines, with path/content SHA-256
`0ef395004f7feaadf60da0daba7b5da9c52ea4974850adfa2bd9d09081b242aa` /
`990b5ad4798786d9f87c03f76fdbad92fc2edf1f6d84ef3baad67254c79fdd70`.
The production leaf, test-only facade, test leaf, and test registry are
`315/977/313/61` lines; the test leaf physical SHA-256 is
`eaabf36e56ccbe60fd556bd1e74f2dfc143600edbb82831af7be84439395f1c2`.

Focused checker and runner tests pass `4/4` each; complete libraries pass
`538/538` and `608/608`; checker/runner lint policies pass `15/15` each; and
metadata passes `137/137`. Offline Cargo metadata, `cargo fmt --check`,
warnings-denied all-target/all-feature Clippy, full workspace `cargo test`,
protected/count/hash replay, recursive links, and `git diff --check` pass. All
five CLIs exit zero and reproduce frozen stdout SHA-256 values, respectively
`700f4bf503783742cefd8004fa095675b7476d46e9a3a6dd439916d237eb6718`,
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50be935f808600f446da1bfbda56`,
`71e83ba0b20d4015e07b3bd2c0c4db2837b6151d1251812caed7954530d53c74`,
`4b2c7bd5ec3cc56e5672fb351126126230ec84fba9bd2bd9049a516d378fab7f`,
and `ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.

Independent test-sufficiency, implementation, source/documentation, and
bilingual reviews report **NO FINDINGS**. Protected specification, `.miz`,
expectation, and Cargo inventories, the 207-byte final-LF fixture, corpus/trace
counts, trace hash, and `82/82` contract pairs remain unchanged. The
contradictory algorithm wording remains the deferred `spec_gap`; the targetless
transport closes only the bounded `design_drift`, `source_drift`, and
eight-test `test_gap`. Any repository metadata conflict remains report-only.

Final quality re-review reports **NO FINDINGS**: all nine hard gates PASS, no
score cap applies, and the valid score is `100/100`
(`20/20/15/15/10/10/5/5`). Exact staging/cached-diff review passed.
Immediately after implementation commit
`b67b028e07337ff5b72422bc8f16fb8f187b5c06`, the read-only post-implementation
checkpoint observed `HEAD=b67b028e07337ff5b72422bc8f16fb8f187b5c06`, a clean
worktree, `origin/main...HEAD=0/1`, and unchanged protected
`stash@{0}=f65cf4a13752ec380710814a9ac6392ccb9d75d4`. Task 277A is complete while
umbrella Task 277 remains partial. Fresh inventory authorizes no successor
semantics: any successor must be separately frozen and reviewed.
