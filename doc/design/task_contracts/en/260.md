# Task 260: Historical Functor-Definition Intake

> Canonical language: English. Japanese companion: [../ja/260.md](../ja/260.md).

This historical record owns the shared documentation-prerequisite review and
checklist evidence for completed checker Task 260. Durable source transport,
public API, ownership, runner, traceability, and implementation evidence remain
in their component owners.

## Identity And Owners

| Field | Historical value |
|---|---|
| Task | `260` |
| Predecessor | Task-259 implementation `b61be7e567b92d31b3544b86e5c7a68537625743` |
| Documentation prerequisite | `b587038f12f84a77720f6441a000ddb84c7b996f` |
| Lower correction | Task-249R documentation `b292b8002f9656c4ab2a6c3b606743b1bda7d551` and implementation `c233bfdff8317a1f4ffdd5750e62a29ee6e69b2f` |
| Implementation | `c83e424a485a24dd0f00ddea687903a235d85850` |
| Authority | Chapter 10 Sections 10.1--10.6; Chapter 16 Sections 16.4, 16.6.1, and the corresponding 16.7.2 initial-obligation rows; the parser/resolver functor-definition fixtures; the mixed predicate/functor gap fixture; its sidecar/trace intent; and committed Tasks 248--256 and 259 public APIs |
| Checker owners | [source and API](../../mizar-checker/en/source_functor_definition.md#task-260-scope-and-authority), [active public-surface audit](../../mizar-checker/en/source_spec_audit.md#task-260-active-public-surface-audit), [frozen boundary](../../mizar-checker/en/module_boundary_audit.md#task-260-frozen-boundary), [Typed ownership](../../mizar-checker/en/typed_ast.md#task-260-typed-functor-definition-installation), and [final ownership](../../mizar-checker/en/resolved_typed_ast.md#task-260-final-functor-definition-ownership) |
| Runner owners | [consumer plan](../../mizar-test/en/00.crate_plan.md#checker-task-260-frozen-consumer-prerequisite), [private route](../../mizar-test/en/harness.md#checker-task-260-frozen-functor-definition-route), [active trace](../../mizar-test/en/traceability.md#checker-task-260-active-trace-result), and [consumer TODO](../../mizar-test/en/todo.md#checker-task-260-active-consumer) |

The exact active pair is
`tests/miz/pass/types/pass_type_elaboration_functor_definition_payload_001.miz`
and its same-stem expectation. Requirement
`spec.en.checker.type_elaboration.source_functor_definition_payload` credits
transport and pending-obligation intake only. The existing mixed
`fail_type_elaboration_predicate_functor_definition_gap_001` artifact remains
a fail boundary and receives no mixed-acceptance credit.

## Frozen Contract And Deferrals

The reviewed source is exactly 262 bytes with final LF and SHA-256
`9bbf50016c72faf8b86342a9a65f8d59bf7747b85b43b6c5bc3c624c7212416a`.
It authenticates 108 Surface rows, three resolver shells, two functor symbols
and definitions, two context parameters, one `assume` guard, two independently
written return types, one `equals` primary-term definiens, and one `means`
atomic-formula definiens. The lower owners remain Task 248 context/bindings,
Task 249 plus 249R type applications and return rows, Task 252 primary terms,
and Task 256 atomic formulas; Tasks 253--255 are optional absent fingerprints.

The syntax-free handoff publishes five dense tables at `2/2/1/2/2` and retains
the complete lower fingerprints. `Equals` appends no existence or uniqueness
obligation.
The explicit `means` clauses append exactly one Pending `FunctorExistence` and
one Pending `FunctorUniqueness`, in source order, with empty assumptions and
opaque deterministic goal/provenance identities. Empty assumptions do not
claim an unguarded semantic obligation. Typed ownership is one-shot and
atomic; final ownership clone-preserves and revalidates it. Task 259 predicate
and Task 260 functor transactions are mutually excluded, with no install-order
or mixed-acceptance promise.

Parameter/guard/return-type goal composition, FOL goal construction,
proof/discharge/acceptance, fact or axiom publication, symbol activation,
overload semantics, calls, conditional definientia, Core/CFG/VC, and every
Task-261+ family remained deferred. The computation subtrees are preserved but
unconsumed. The durable owner records the exact public structs, enums, rows,
getters, producer/projection/error ABI, debug grammar, validation matrix,
fingerprints, serializers, and the five checker plus four runner tests; this
historical record does not duplicate those owner-local details.

## Completion Evidence

Fresh post-Task-259 inventory selected Task 260 and classified origin-reference
divergence as report-only `repo_metadata_conflict`. The prerequisite froze the
exact source/AST/resolver/lower/obligation/Typed/final/runner boundaries above,
the pass-pair and trace intent, the nine-test matrix, write scope, exclusions,
count projections, and semantic deferrals. Canonical EN and JA were logically
synchronized without exception.

Repeated specification, test-sufficiency, implementation-boundary, and
source/documentation-consistency reviews ended **NO FINDINGS**. The docs-only
gate proved no diff under `doc/spec`, `tests`, `crates`, or Cargo metadata.
Focused syntax/resolver and Tasks 248/252/256/259 preflights passed, as did
checker `435`, runner `512`, metadata `137`, formatting, warnings-denied
Clippy, and the full workspace suite.

The five CLIs reproduced cases/requirements `422/390`, pass/fail `229/193`,
active parse/declaration/type/proof `101/7/199/1`, type requirements `254/242`,
and warnings/errors `23/0`. Their historical hash prefixes were plan
`d0fb0ece...`, parse `a8a7aa63...`, declaration `71e83ba0...`, type
`6b310d50...`, and proof `ccf3d2d4...`. Raw/normalized test-list evidence was
checker `435` / `092c432d...` / `740d9867...`, runner `512` / `1174eea0...` /
`d282fae4...`, resolver `144` / `727e09ff...` / `8cf9e92d...`, and syntax `59`
/ `51277525...` / `c11a29c9...`. Production inventories stayed checker
`24/147030`, runner `31/63248`, resolver `15/18871`, and syntax `6/4314`, with
their recorded path/content hashes unchanged.

Final independent read-only quality ended **NO FINDINGS**; all nine hard gates
passed without a score cap at `100/100`
(`20/20/15/15/10/10/5/5`). Exact staging produced the dedicated prerequisite
commit above. Fresh preflight then found the binding-linked return-type model
to be lower `design_drift` plus `source_drift`, not a `spec_gap`; the retained
[lower-stage selection](../../mizar-checker/en/00.crate_plan.md#task-260-lower-stage-preflight-and-task-249r-selection)
records the separately committed Task-249R correction. The retained
[implementation verification](../../mizar-checker/en/00.crate_plan.md#task-260-implementation-verification)
and [post-commit closure](../../mizar-checker/en/00.crate_plan.md#task-260-post-commit-closure-and-task-261-selection)
own the later executable evidence and Task-261 handoff.

## Migration Boundary

`DOC-260-DOC-REVIEW-COMPACT` may replace only its six inventory-listed
documentation-prerequisite verification, bilingual synchronization, and
completed-checklist sections. Both implementation-verification sections remain
because schema v1 permits only one redirect per `(source path, task id)`.
The frozen plan/API, lower-stage correction, post-commit closure, source/API,
module, payload, Typed/final, semantic, active TODO, every runner, fixture,
sidecar, trace, coverage, and all unlisted content remain.
