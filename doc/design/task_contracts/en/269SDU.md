# Task 269SDU: Descendant Given Occurrence/Reference Transport

> Canonical language: English. Japanese companion: [../ja/269SDU.md](../ja/269SDU.md).

This contract freezes one zero-credit, dormant Checker consumer. It is derived
orchestration evidence and does not override the canonical specification,
existing `.miz` tests, traceability metadata, or expectations.

## Identity, Authority, And Readiness

| Field | Frozen value |
|---|---|
| Task | `269SDU` |
| Status | Complete: documentation prerequisite `d3a1473e679f32a408e5fa198ca1849269df23d9`, completion-scope correction `c87e802925c837047a6b60b12a5ef9cf27f3ac27`, and implementation `3bddc5fd6383226bbb66d7d5757e7b77f80678d9` are committed; independent reviews, verification, final hard-gate scoring, and clean post-commit inventory are complete |
| Plans | [checker](../../mizar-checker/en/00.crate_plan.md#task-index), [runner](../../mizar-test/en/00.crate_plan.md#task-index) |
| Purpose | Transport only descendant `y@118..119` as a variable-reference occurrence resolved to the inherited proof-local `given` binding |
| Primary owner | [checker source term](../../mizar-checker/en/source_term.md#task-269sdu-descendant-given-occurrencereference-contract) |
| Ownership consumers | [Typed](../../mizar-checker/en/typed_ast.md#task-269sdu-typed-ownership), [Resolved](../../mizar-checker/en/resolved_typed_ast.md#task-269sdu-resolved-ownership), and [private runner](../../mizar-test/en/harness.md#task-269sdu-private-runner-route) |
| Dependency | Complete Task-269SDT implementation `c5389023eddf84600c5f7972b240712673e76d95`, through its exact `SourceProofLocalGivenDescendantTypeHandoff` |
| Selection state | Fresh-selected from clean `0343f8e7ef47d6b24a64e8b14b3a85f600a95380`; `origin/main...HEAD=0/3`; protected `stash@{0}=f65cf4a13752ec380710814a9ac6392ccb9d75d4` |

Canonical authority is Chapter 13 §13.1.1: a variable identifier references a
previously declared variable under Chapter 4 scope rules; Chapters 4 §4.6.1,
15 §§15.3.3 and 15.10, and 16 §§16.4.2--16.4.3: a proof-local `given`
variable is visible through the remainder of its enclosing proof/reasoning
block and inherited by nested child blocks unless shadowed. The existing
diagnostic fixture
`tests/miz/fail/types/fail_type_elaboration_proof_local_declaration_gap_001.miz`
confirms proof-local declarations remain an active payload boundary; its
expectation and trace status are unchanged and grant this dormant route no
executable coverage credit.

All prerequisites are committed ancestors. Fresh inventory found no
schema-v2-ready checker compaction family: the remaining B2C implementation
sections share source files with an existing same-task batch, so migrating
them would be a `boundary_violation`. Task 269SDU is therefore the next
dependency-ready Checker task.

## Gap Classification And Semantic Boundary

- `design_drift`: no durable descendant occurrence/reference owner or frozen
  task contract existed after Task 269SDT.
- `source_drift`: the authenticated `y@118..119` subtree has no exact
  `SourcePrimaryTermHandoff`, public composite, or Typed/Resolved owner.
- `test_gap`: there are no focused checker/runner tests for this exact
  descendant reference transaction.
- `spec_gap`: Chapters 4 §4.4.1 and 15 §15.2.2 disagree about whether `set`
  adds an equality axiom or is only a capture-avoiding abbreviation. This is
  nonblocking for resolving `y@118..119` to the already-authenticated Given
  binding and remains blocking for every `z`/`q` binding, abbreviation,
  closure, and capture decision.
- No `test_expectation_drift` is present. Existing repository metadata
  conflicts remain report-only.

The task transports one identifier occurrence and its binding reference. It
does not infer a `set` binding, expand an abbreviation, record capture, create
an equality/formula/fact/guard, close a goal, accept a proof, publish a generic
term route, activate dispatch, change diagnostics, or earn trace/coverage
credit. Source behavior is not authority for any excluded meaning.

## Frozen Checker ABI And Exact Payload

`source_term.rs` owns a public
`SourceProofLocalGivenDescendantUseTermHandoff`,
`SourceProofLocalGivenDescendantUseTermProducer`, and non-exhaustive
`SourceProofLocalGivenDescendantUseTermError` with ordered
`InvalidDependency`, `InvalidSourceTerm`, and `InvalidInstallation` failures.
The handoff consumes Task-269SDT by value and preserves its exact dependency
and debug fingerprint.

The only source-term profile is:

| Field | Frozen value |
|---|---|
| Term rows | `1` |
| Reference rows | `1` |
| Numeric requests | `0` |
| Term site/range | typed node `3`, `118..119` |
| Spelling/kind/role | `y`, `VariableReference`, `Value` |
| Source ordinal/context | `0`, descendant context `2` (`[0,0]`) |
| Reference | term `0` -> binding `1`, role `Variable` |
| Derived lookup | lexical scope `[0,0]`, use ordinal `2` |
| Binding rule | only the profile-scoped `GivenWitness -> Variable` admission; generic admission is unchanged |

The exact complete arena has the unchanged three-node SDT prefix, node `3`
`source.term.variable-reference@118..119`, and root node `4`
`source.proof-local.given-descendant-use.term-root@0..179` with children
`[2,3]`. All nodes remain Unknown/Normal, unresolved, and unlinked. Validation
orders exact SDT dependency/prefix, exact term input and source-term
fingerprint, complete arena, then one-shot ownership. Failure is atomic.

`TypedAst` and `ResolvedTypedAst` each own exactly one optional boxed SDU
composite and expose a read-only getter. Installing SDU atomically replaces
standalone SDT ownership: no direct binding/type owner or sibling proof-local
transaction may coexist in either order. No semantic table, node hint, fact,
diagnostic, or downstream IR entry is created.

## Private Runner Route, Scope, And Tests

The private dormant runner exposes
`SourceProofLocalGivenDescendantUseTermRouteOutput { typed_ast, resolved }`,
`source_proof_local_given_descendant_use_term_output`, and a cfg-test mutation
seam with only `None`, `WrongDependencyModule`, `WrongTermRange`,
`WrongReferenceBinding`, `WrongArenaRoot`, and `WrongArenaKind`. A missing SDT
dependency reports `Task269SDU SDT dependency is missing`. No public dispatcher
or active CLI route is added.

Rust implementation scope is exactly these seven existing files:

1. `crates/mizar-checker/src/source_term.rs`
2. `crates/mizar-checker/src/typed_ast.rs`
3. `crates/mizar-checker/src/resolved_typed_ast.rs`
4. `crates/mizar-test/src/runner.rs`
5. `crates/mizar-test/src/runner/type_elaboration.rs`
6. `crates/mizar-test/src/runner/type_elaboration/source_proof_local_declaration.rs`
7. `crates/mizar-test/src/runner/tests/type_elaboration/source_proof_local_declaration.rs`

The implementation commit also updates exactly these 26 derived completion-
state documents so that durable owners, audits, and TODO status describe the
implemented source rather than a future plan:

1. `doc/design/task_contracts/en/269SDU.md`
2. `doc/design/task_contracts/ja/269SDU.md`
3. `doc/design/mizar-checker/en/source_term.md`
4. `doc/design/mizar-checker/ja/source_term.md`
5. `doc/design/mizar-checker/en/typed_ast.md`
6. `doc/design/mizar-checker/ja/typed_ast.md`
7. `doc/design/mizar-checker/en/resolved_typed_ast.md`
8. `doc/design/mizar-checker/ja/resolved_typed_ast.md`
9. `doc/design/mizar-test/en/harness.md`
10. `doc/design/mizar-test/ja/harness.md`
11. `doc/design/mizar-checker/en/source_spec_audit.md`
12. `doc/design/mizar-checker/ja/source_spec_audit.md`
13. `doc/design/mizar-checker/en/module_boundary_audit.md`
14. `doc/design/mizar-checker/ja/module_boundary_audit.md`
15. `doc/design/mizar-test/en/module_boundary_audit.md`
16. `doc/design/mizar-test/ja/module_boundary_audit.md`
17. `doc/design/mizar-checker/en/bilingual_sync_audit.md`
18. `doc/design/mizar-checker/ja/bilingual_sync_audit.md`
19. `doc/design/mizar-test/en/bilingual_sync_audit.md`
20. `doc/design/mizar-test/ja/bilingual_sync_audit.md`
21. `doc/design/mizar-checker/en/todo.md`
22. `doc/design/mizar-checker/ja/todo.md`
23. `doc/design/mizar-test/en/todo.md`
24. `doc/design/mizar-test/ja/todo.md`
25. `doc/design/spec_coverage_audit.md`
26. `doc/design/todo.md`

Those Markdown edits may change only tense/status, final measured evidence,
review outcomes, verification results, and next-handoff state already owned by
the frozen contract. They must not add authority, behavior, test intent,
coverage credit, or a new task narrative. The four crate-plan Task Index rows
are already accurate and remain unchanged.

Exactly four checker and four private runner tests use the `task269sdu_`
prefix and cover: exact occurrence/reference/fingerprints and replay;
dependency/input/arena corruption with error precedence; one-shot
Typed/Resolved ownership and empty semantics; and near-miss, predecessor,
sibling, and active-route isolation. Tests must prove that `z@114..115`,
`q@129..130`, and `z@133..134` remain absent.

No `doc/spec`, `.miz`, fixture, sidecar, expectation, trace, metadata,
diagnostic, Cargo manifest, parser/resolver output, lower output, active CLI
result, or production path set may change. The documentation prerequisite and
Rust implementation are separate logical tasks and separate commits.

## Baselines, Expected Impact, And Verification

The exact derived source remains 180 bytes (range `0..179` plus final newline),
source SHA-256
`efa21af05a15f611815a4eb573577d0a368a3134693b225bdb56177f3637c2a8`,
and Surface `68` nodes/root `67` with SHA-256
`cbeae821434b0db13d77d7dac9984d8d6bf8012de9e7c680be12e8371e87ceaa`.
These authenticate only the derived fixture profile.

Fresh library baselines are checker `530` and runner `600`, with raw list
SHA-256 respectively
`e99f525f1839f730cfe03f2d0e80f7917e3564df2e2b58c3810502631f4c3e35`
and `28f35ad5f4496714e9fd0882b39bfb751cff444a0460e0df6d17f03a7745776b`.
The exact expected impact is `+4/+4`, projecting `534/604`; fresh measured
lists are authoritative.

Checker production is `30` paths / `185077` lines, path SHA-256
`c89f43f6abebf7ebeb3ac9394ecd8ea3186ad28934c75526d2cc0b85a66ebad5`
and aggregate per-file-content SHA-256
`bdd7839d14fc6870d8c75949ab817983cc2d73ef2d4c52cf930fe80ba2a7a8a1`.
Runner production is `37` paths / `79521` lines, path SHA-256
`1f9e2c9c6589412d832eb92015d913c1b2e0f1309cba9c5c991e08b04d67a73d`
and aggregate per-file-content SHA-256
`a1dae596a30d9f138738276afd88e3f5c805f107de0daeb30f9a3421b4480372`.
Path counts/hashes must remain; line/content hashes are remeasured after the
implementation.

Corpus/requirements remain `428/395`, pass/fail `235/193`, warnings/errors
`23/0`, stages `101/7/205/1`, type `259=247+12`, and trace SHA-256
`55b754c8c4d0d293a1c44e2ba4b0090f407bba1d429b461b6cb4d6ddca9ca2b3`.
Plan/parse/declaration/type/proof CLI hashes remain respectively
`700f4bf503783742cefd8004fa095675b7476d46e9a3a6dd439916d237eb6718`,
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`,
`71e83ba0b20d4015e07b3bd2c0c4db2837b6151d1251812caed7954530d53c74`,
`4b2c7bd5ec3cc56e5672fb351126126230ec84fba9bd2bd9049a516d378fab7f`,
and `ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.

Reviews are independent specification/equivalence and EN/JA reviews for the
prerequisite, then test-sufficiency, implementation, source/documentation,
and bilingual reviews for Rust. Final read-only review requires all nine hard
gates PASS, no score cap, and at least `90/100`. Verification includes focused
`task269sdu_` tests, complete checker/runner libraries, lint-policy and
metadata, `cargo fmt --all --check`, workspace Clippy/tests, all five CLIs,
protected count/hash replay, `git diff --check`, and explicit cached plus
unstaged review.

## Documentation Prerequisite Evidence

- Fresh selection used clean `0343f8e7ef47d6b24a64e8b14b3a85f600a95380`,
  local tracking distance `0/3`, and unchanged protected stash
  `f65cf4a13752ec380710814a9ac6392ccb9d75d4`.
- Scope is exactly 26 Markdown paths under `doc/design`: the paired contract,
  paired checker/runner indexes and owners, paired bilingual/TODO surfaces,
  root coverage mapping, and root status. There is no Rust, specification,
  `.miz`, expectation, trace, Cargo, or legacy-ledger delta.
- Independent specification/contract review and bilingual/boundary review
  both reached **NO FINDINGS** after the missing owner/bilingual surfaces,
  canonical marker, and immutable redirect placement were corrected. A final
  quality first pass found the missing current status/completion-evidence
  record; this synchronized section resolved that `design_drift`, and the
  finding-specific final re-review reported **NO FINDINGS**, all nine gates
  **PASS**, no score cap, and `100/100` for the documentation slice.
- Checker lint-policy `15/15`, runner lint-policy `15/15`, metadata `137/137`,
  `cargo fmt --all --check`, offline Cargo metadata, protected-scope review,
  and `git diff --check` pass. The focused recursive contract/link/redirect
  test also passes.
- The documentation prerequisite committed as `d3a1473e679f32a408e5fa198ca1849269df23d9`;
  its immediate replay was clean at `origin/main...HEAD=0/4`, with protected
  `stash@{0}=f65cf4a13752ec380710814a9ac6392ccb9d75d4` unchanged.

## Completion-State Scope Correction

The documentation prerequisite committed as
`d3a1473e679f32a408e5fa198ca1849269df23d9`. Its immediate post-commit replay
was clean at local tracking distance `0/4`, and protected `stash@{0}` remained
`f65cf4a13752ec380710814a9ac6392ccb9d75d4`. Fresh implementation review found
that the seven-file Rust boundary was correct but that final owner/TODO tense
could not become source-documentation consistent without a completion-state
delta. That omission is `design_drift`, not new semantic authority.

This paired amendment freezes the exact 26-document completion-state scope
above before the implementation commit. It does not modify the Rust payload,
test intent, expected counts/hashes, specification, corpus, expectations,
trace, coverage, diagnostics, dispatch, or CLI bytes. The correction is a
separate docs-only prerequisite and must pass independent equivalence and
bilingual review, repository link/lint checks, all nine documentation gates,
task-only staging, and a dedicated commit before implementation resumes.

## Implementation Completion Evidence

The implementation is confined to the exact seven Rust paths and 26 derived
completion documents frozen above (`33` paths total). Exactly four checker and
four private-runner `task269sdu_` tests are added inside the Rust paths. The
route remains private and dormant, replaces standalone SDT ownership only for
the exact authenticated profile, publishes no semantic state or coverage
credit, and leaves every `z`/`q` occurrence absent.

Checker library state is `534` tests with raw list SHA-256
`542b3ed2ca7f84d1a78603e1ef3e2ee4ac963b50b4f764cdc819f5a4a43b3ad3`.
Production remains `30` paths with unchanged path SHA-256
`c89f43f6abebf7ebeb3ac9394ecd8ea3186ad28934c75526d2cc0b85a66ebad5`;
it measures `186162` lines with aggregate per-file-content SHA-256
`aeb472fb32ba2c3252b65fc9b0ceb81001a1b36a6486834bec113bd2bc4142fb`.
The changed source-term, Typed, and Resolved owners measure
`6451/6869/8889` lines.

Runner library state is `604` tests with raw list SHA-256
`4ca6de65d417874fea0c9d8491beb41a10ccfc2c188b4a7ddc3971a27db55c68`.
Production remains `37` paths with unchanged path SHA-256
`1f9e2c9c6589412d832eb92015d913c1b2e0f1309cba9c5c991e08b04d67a73d`;
it measures `79769` lines with aggregate per-file-content SHA-256
`2b642db1b23a8bb932a434ef7914f696951c998748644999486a107057effdfa`.
The proof-local leaf, facade, root, and test leaf measure
`3495/971/2804/10509` lines.

The 180-byte source and 68-node Surface fingerprints, corpus/requirement,
pass/fail, warning/error, stage/type-coverage counts, trace hash, and all five
CLI hashes reproduce the frozen values. Focused checker and runner tests pass
`4/4`; complete libraries pass `534/534` and `604/604`; checker and runner
lint-policy pass `15/15`; metadata passes `137/137`; formatting, offline Cargo
metadata, warnings-denied all-target/all-feature Clippy, full workspace tests,
all five CLIs, and `git diff --check` pass. Independent test-sufficiency and
implementation reviews report **NO FINDINGS** after the missing-dependency,
reciprocal-ownership, active-route-isolation, and lint-rationale findings were
closed. Independent source/documentation, bilingual/boundary, and exact
completion-evidence reviews also report **NO FINDINGS**. Luna was unavailable;
bounded implementation and first-pass reviews used the documented Terra
`high` fallback while the parent retained Sol
`xhigh` for authority, integration, and final acceptance.
The final read-only quality review reports **NO FINDINGS**, all nine hard gates
**PASS**, no score cap, and `100/100`.

The implementation committed as
`3bddc5fd6383226bbb66d7d5757e7b77f80678d9`. Its immediate post-commit replay
was clean at `origin/main...HEAD=0/6`, with protected
`stash@{0}=f65cf4a13752ec380710814a9ac6392ccb9d75d4` unchanged. No semantic
successor is assigned by this completion record.

## Audit Impact, Exit Criteria, And Handoff

`doc/design/spec_coverage_audit.md` receives a zero-credit design mapping
because this task records an implemented derived owner/test route for Chapters
4, 13, 15, and 16. It does not change trace status, requirement counts, or
executable coverage. Source, semantic, bilingual, boundary, and runner audits
change only where they own the new public/private route or current task state.

The prerequisite exits only after synchronized documentation, independent
NO-FINDINGS reviews, all nine gates, task-only staging/commit, and clean
post-commit inventory. The correction prerequisite has the same docs-only
gates. Implementation exits only with the exact seven-Rust-file,
26-completion-document, eight-test scope (33 paths total), every exclusion
preserved, independent NO-FINDINGS reviews, all verification and hard gates,
a separate task-only commit, and clean post-commit inventory. The next task is
selected only from that fresh state; no `z`/`q` or capture successor is
preassigned.
