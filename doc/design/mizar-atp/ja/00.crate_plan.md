# Crate Plan: mizar-atp

> 正本は英語です。英語版:
> [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## 責務

所有する挙動:

- deterministic discharge 後の `VcStatus::NeedsAtp` VC obligation に対する phase 13 の
  ATP candidate production。
- backend-neutral な `AtpProblem` data、決定的 encoding、backend process execution、
  portfolio candidate collection。
- backend-visible な各 formula を VC/kernel evidence input に結び付ける provenance。
- 後続の kernel checking 向け untrusted formula/substitution evidence candidate。
- backend run の diagnostic と reproducibility metadata。

範囲外:

- trusted proof acceptance、kernel evidence checking、SAT checking、proof policy decision。
- winner selection、proof-status projection、witness publication、proof reuse policy。
  これらは downstream proof/cache/artifact integration に属する。
- premise invention、substitution invention、overload resolution、cluster search、
  implicit coercion insertion、fallback inference。
- backend proof method、resolution trace、SMT proof object、backend log、instantiated
  formula を trusted acceptance material として扱うこと。

## 仕様項目

| Spec ref | 要件 | 状態 |
|---|---|---|
| [architecture 09](../../architecture/ja/09.atp_interface_protocol.md) | ATP-facing problem layer、encoding、provenance、supported protocol。 | module spec と implementation task 2-12 に deferred。 |
| [architecture 10](../../architecture/ja/10.atp_backend_integration.md) | backend process model、result classification、portfolio execution、diagnostic。 | task 13-14 は generic backend runner を仕様化・実装する。first real-backend extraction は ATP-G-015 により deferred、task 17 は policy-neutral な portfolio planning と candidate collection を仕様化し、task 18 は prebuilt run/result に対する no-early-stop source を実装する。 |
| [architecture 15](../../architecture/ja/15.kernel_certificate_format.md) | kernel-facing trusted material は formula/substitution/provenance/target binding evidence であり、legacy certificate trace は trusted ではない。 | kernel/vc evidence contract は利用可能である。`mizar-atp` は real backend formula/substitution candidate を生成する前に paired extraction spec が必要である。 |
| [architecture 19](../../architecture/ja/19.failure_semantics.md) | ATP timeout、backend failure、unsupported legacy evidence、fail-closed/open-status behavior は trusted acceptance になってはならない。 | task 14 は fail-closed backend process status を実装する。real-output classification は concrete extraction route が存在した後の task 16 まで deferred。 |
| [internal 04](../../internal/ja/04.atp_portfolio_and_kernel_check_integration.md) | ATP は candidate evidence を生産し、kernel が検査し、proof policy が artifact-facing status を選択する。 | backend runner と no-early-stop portfolio collection は実装済みである。想定される `evidence.md` extraction module と downstream proof/cache/artifact handoff は task-scoped/deferred のままである。 |
| [internal 07](../../internal/ja/07.crate_module_layout.md) | crate/module layout は文書化済み ownership と spec-before-source に従う。 | task 1 は crate shell だけを作る。semantic module は spec を待つ。 |

## 関連テスト

| Test path | 意図 | Spec refs |
|---|---|---|
| `crates/mizar-atp/tests/lint_policy.rs` | workspace membership、lint inheritance、task-1 dependency boundary、package metadata、spec 前に semantic module がないことを守る。 | `todo.md` task 1、internal 07 の crate ownership。 |
| `crates/mizar-atp/src/problem/tests.rs` unit tests | validated `AtpProblem` construction、deterministic identity/rendering、provenance completeness、unsupported-profile diagnostic、`Unsat` polarity、trusted backend material がないことを守る。 | `problem.md`、`todo.md` task 3、architecture 09/15/19。 |
| `crates/mizar-atp/src/translator/tests.rs` unit tests | task-5 status/target gate、deterministic declaration/symbol-map translation、projection fail-closed behavior、soft-type guard validation、imported proof-status rejection、task-6 formula projection / handoff agreement、axiom/conjecture materialization、duplicate premise rejection、proof-hint non-pruning、`Unsat` polarity、prohibited-material absence を守る。 | `translator.md`、`todo.md` tasks 5-6、architecture 09/15/19。 |
| `doc/design/mizar-atp/en/property_encoding.md` と日本語 companion | task 8 に向けて property family coverage、axiom/native strategy、provenance、deterministic identity、fail-closed/deferred behavior を仕様化する。 | `todo.md` task 7、architecture 09「Property Encoding」、architecture 08/15/19。 |
| `doc/design/mizar-atp/en/tptp_encoder.md` と日本語 companion | task 10 に向けて deterministic TPTP FOF emission、name mangling、provenance side metadata、fail-closed/deferred dialect coverage を仕様化する。 | `todo.md` task 9、architecture 09「Supported Formats」「Interface Definitions」、`problem.md`、`property_encoding.md`。 |
| `doc/design/mizar-atp/en/smtlib_encoder.md` と日本語 companion | task 12 に向けて deterministic uninterpreted SMT-LIB emission、fixed universe sort encoding、logic selection、named assertion metadata、fail-closed/deferred dialect coverage を仕様化する。 | `todo.md` task 11、architecture 09「Supported Formats」「Validity Checking Polarity」「Interface Definitions」、`problem.md`、`property_encoding.md`。 |
| `doc/design/mizar-atp/en/backend.md` と日本語 companion | task 14 に向けて backend child-process execution、resource limit、version / run metadata、fail-closed result classification、そして `Proved` は matching `Unsat` polarity と formula/substitution candidate evidence を要する規則を仕様化する。 | `todo.md` task 13、architecture 10「Process Model」「Result Classification」、internal 04「Backend Runner」、architecture 15/19。 |
| `doc/design/mizar-atp/en/portfolio.md` と日本語 companion | policy-neutral な portfolio planning、candidate collection、deterministic candidate ordering、resource budget、cancellation / early-stop constraint、kernel / proof-policy handoff boundary を仕様化する。 | `todo.md` task 17、architecture 10「Portfolio Execution」、internal 04「ATP Portfolio Service」、architecture 15/19。 |
| `crates/mizar-atp/src/property_encoding/tests.rs` unit tests | task-8 property-family axiom form、generated-binder declaration / symbol-map / provenance、target declaration validation、profile rejection、connectedness disjunction、duplicate rejection、native-declaration deferral、`AtpProblem` integration を守る。 | `property_encoding.md`、`todo.md` task 8、architecture 09/15/19。 |
| `crates/mizar-atp/src/tptp_encoder/tests.rs` unit tests | task-10 deterministic FOF emission、exact rendering grammar、profile gate、variable scope validation、native-declaration rejection、raw-name injection avoidance、provenance side metadata、diagnostics exclusion、name/label collision check を守る。 | `tptp_encoder.md`、`todo.md` task 10、architecture 09/15/19。 |
| `crates/mizar-atp/src/smtlib_encoder/tests.rs` unit tests | task-12 deterministic uninterpreted SMT-LIB emission、`QF_UF` / `UF` logic selection、fixed universe sort と explicit guard preservation、negated-conjecture `Unsat` polarity、profile gate、variable scope validation、native-declaration rejection、unused-sort tolerance、raw-name injection avoidance、provenance side metadata、diagnostics/proof-object exclusion、name/label collision check を守る。 | `smtlib_encoder.md`、`todo.md` task 12、architecture 09/15/19。 |
| `crates/mizar-atp/src/backend/tests.rs` unit tests | task-14 generic backend process execution、exact stdin/private-file input delivery、deterministic command fingerprint、version probe、timeout/cancellation/crash/missing-executable handling、drain-safe stdout/stderr capture、private temp cleanup、unsupported required-limit error、mock `Proved` invariant、counterexample/unknown/error status boundary、prohibited trusted backend material がないことを守る。 | `backend.md`、`todo.md` task 14、architecture 10/15/19、internal 04。 |
| `crates/mizar-atp/src/portfolio/tests.rs` unit tests | task-18 no-early-stop portfolio planning / collection、input/result order shuffle 下の deterministic run/candidate ordering、partial candidate のない cancellation、missing/unknown/duplicate result の fail-closed handling、metadata mismatch rejection、policy/kernel/witness/cache/trusted backend material がないことを守る。 | `portfolio.md`、`todo.md` task 18、architecture 10/15/19、internal 04。 |
| `crates/mizar-atp/src/backend/tests.rs` task-19 unit tests | seed、timeout setting、backend identity / version record、stream/resource usage、diagnostic に対する stable run-metadata projection の完全性を守り、projection が command fingerprint を変更せず、runtime observation が candidate / trusted-acceptance hash の外に残ることを確認する。 | `backend.md`、`todo.md` task 19、architecture 00 incrementality and reproducibility。 |
| `crates/mizar-atp/tests/mock_backend_corpus.rs` と `tests/property/atp_mock_backend_integration_001.*` | task-20 metadata-only `advanced_semantics` corpus fixture を、active `.miz` advanced-semantics runner に依存せず、mock backend runner、mock classification、portfolio collection に通すことを守る。 | `portfolio.md`、`todo.md` task 20、`mizar-test` staged model、architecture 10/15/19、internal 04。 |
| `crates/mizar-atp/tests/determinism_suite.rs` | task-21 の cross-module determinism を守る。同一の public `VcIr` input から kernel handoff construction、ATP problem translation、実装済み両 concrete encoder、mock backend classification、完了順 shuffle 下の portfolio candidate ordering までを検査する。 | `portfolio.md`、`todo.md` task 21、architecture 20、architecture 10/15/19、internal 04。 |
| `crates/mizar-atp/tests/lint_policy.rs` task-22 public-enum guard | public ATP module のすべての public enum が downstream 向け `#[non_exhaustive]` を持ち、英語/日本語の所有 spec が source と完全一致する enum inventory を公開することを守る。 | 全 module spec、`todo.md` task 22、architecture 19 fail-closed semantics。 |
| `crates/mizar-atp/tests/lint_policy.rs` task-23 source/spec audit guard | paired `source_spec_audit.md` が現在の public module、public top-level item、public entry function、分類済み external/deferred gap register を cover することを守る。 | `source_spec_audit.md`、全 module spec、`todo.md` task 23、autonomous crate development hard gates。 |
| `crates/mizar-atp/tests/lint_policy.rs` task-24 bilingual sync guard | 英語/日本語 `doc/design/mizar-atp` Markdown filename が完全に一致し、paired `bilingual_sync_audit.md` がすべての design doc pair と trust-boundary/deferred marker を inventory することを守る。 | `bilingual_sync_audit.md`、repository documentation policy、`todo.md` task 24。 |

予定している test-first 追加:

- task 1 は infrastructure crate を作るだけで language behavior を変更しないため、
  `.miz` test は不要。
- 後続 task は、実装する spec の module 近くに focused unit、golden、mock-backend、
  determinism test を追加する。

## design と source の棚卸し

Design files:

- `doc/design/mizar-atp/en/todo.md`
- `doc/design/mizar-atp/ja/todo.md`
- この crate plan と日本語 companion
- task 23 は `doc/design/mizar-atp/en/source_spec_audit.md` を追加する。
- task 23 は `doc/design/mizar-atp/ja/source_spec_audit.md` を追加する。
- task 24 は `doc/design/mizar-atp/en/bilingual_sync_audit.md` を追加する。
- task 24 は `doc/design/mizar-atp/ja/bilingual_sync_audit.md` を追加する。
- task 27 は `doc/design/mizar-atp/en/module_boundary_audit.md` を追加する。
- task 27 は `doc/design/mizar-atp/ja/module_boundary_audit.md` を追加する。
- task 28 は `doc/design/mizar-atp/en/crate_exit_report.md` を追加する。
- task 28 は `doc/design/mizar-atp/ja/crate_exit_report.md` を追加する。
- task 2 は `doc/design/mizar-atp/en/problem.md` を追加する。
- task 2 は `doc/design/mizar-atp/ja/problem.md` を追加する。
- task 4 は `doc/design/mizar-atp/en/translator.md` を追加する。
- task 4 は `doc/design/mizar-atp/ja/translator.md` を追加する。
- task 7 は `doc/design/mizar-atp/en/property_encoding.md` を追加する。
- task 7 は `doc/design/mizar-atp/ja/property_encoding.md` を追加する。
- task 9 は `doc/design/mizar-atp/en/tptp_encoder.md` を追加する。
- task 9 は `doc/design/mizar-atp/ja/tptp_encoder.md` を追加する。
- task 11 は `doc/design/mizar-atp/en/smtlib_encoder.md` を追加する。
- task 11 は `doc/design/mizar-atp/ja/smtlib_encoder.md` を追加する。
- task 13 は `doc/design/mizar-atp/en/backend.md` を追加する。
- task 13 は `doc/design/mizar-atp/ja/backend.md` を追加する。
- task 17 は `doc/design/mizar-atp/en/portfolio.md` を追加する。
- task 17 は `doc/design/mizar-atp/ja/portfolio.md` を追加する。
- TODO の module table に並ぶ後続 module spec

Source files:

- task 1 は `crates/mizar-atp/Cargo.toml` を追加する。
- task 1 は `crates/mizar-atp/src/lib.rs` を追加する。
- task 1 は `crates/mizar-atp/tests/lint_policy.rs` を追加する。
- task 2 は Rust source を追加しない。
- task 3 は `crates/mizar-atp/src/problem.rs` を追加し、`problem` を export する。
- task 5 は `crates/mizar-atp/src/translator.rs` を追加し、`translator` を export する。
- task 6 は `crates/mizar-atp/src/translator.rs` を full axiom/conjecture problem
  construction で拡張する。
- task 8 は `crates/mizar-atp/src/property_encoding.rs` を追加し、
  `property_encoding` を export する。
- task 10 は `crates/mizar-atp/src/tptp_encoder.rs` を追加し、
  `tptp_encoder` を export する。
- task 12 は `crates/mizar-atp/src/smtlib_encoder.rs` を追加し、
  `smtlib_encoder` を export する。
- task 14 は `crates/mizar-atp/src/backend.rs` を追加し、`backend` を export する。
- task 18 は `crates/mizar-atp/src/portfolio.rs` を追加し、`portfolio` を export する。
- task 20 は `crates/mizar-atp/tests/mock_backend_corpus.rs` と
  `tests/property/` 配下の metadata-only corpus fixture を追加する。
- task 21 は `crates/mizar-atp/tests/determinism_suite.rs` を追加する。
- task 27 は inline unit-test suite を private child module に移す:
  `crates/mizar-atp/src/backend/tests.rs`、
  `crates/mizar-atp/src/portfolio/tests.rs`、
  `crates/mizar-atp/src/problem/tests.rs`、
  `crates/mizar-atp/src/property_encoding/tests.rs`、
  `crates/mizar-atp/src/smtlib_encoder/tests.rs`、
  `crates/mizar-atp/src/tptp_encoder/tests.rs`、
  `crates/mizar-atp/src/translator/tests.rs`
- 後続 source module は、対応する spec が存在した後、TODO の module table に従う。

Observed behavior:

- task 1 の前には、workspace に `mizar-atp` crate は存在しない。
- `mizar-kernel` task 23-28 と `mizar-vc` task 24-25 は、postponement gate が要求する
  formula/substitution evidence と VC handoff contract を確立している。
- workspace lint baseline はすでに rustc warning と `clippy::all` を deny している。

## active な既知の gap と drift

| ID | Class | 根拠 | 対応 |
|---|---|---|---|
| ATP-G-001 | resolved `source_drift` | design doc は `mizar-atp` を指定していたが、task 1 前には workspace crate が存在しなかった。 | task 1 が crate shell、dependency boundary、lint guard を追加済み。 |
| ATP-G-002 | `deferred` | translator source は declaration/symbol-map と axiom/conjecture problem construction を cover し、property encoding source は axiom-form property を cover し、TPTP/SMT-LIB encoder は deterministic protocol text を cover し、backend source は generic runner / mock classification slice を cover する。real backend adapter、real output からの candidate extraction、portfolio は task ごとに残っている。 | 完了した spec / implementation task の範囲を超えて semantic module を公開しない。 |
| ATP-G-003 | resolved workspace-existence gap with remaining `external_dependency_gap` integration | task 1 時点では `mizar-proof` が存在せず、proof-policy winner selection、witness publication、portfolio early-stop policy は利用できないと分類した。現在 `mizar-proof` は workspace proof-policy owner だが、`mizar-atp` はその API を消費する integration task をまだ持たない。 | proof-policy integration は専用 ATP/proof task まで deferred とし、`mizar-atp` 内で proof policy を stub しない。 |
| ATP-G-004 | `external_dependency_gap` | concrete backend availability と first-backend evidence extraction は意図的に後続 task である。 | task 1 では real backend を使わない。後続 test は mock backend を使ってよい。 |
| ATP-G-005 | resolved `source_drift` | `problem.md` は `AtpProblem` を仕様化する。task 3 は対応する `src/problem.rs` data model、validation、deterministic rendering、test を追加する。 | 後続 module は、それぞれの spec まで deferred のままにする。 |
| ATP-G-006 | resolved `deferred` | `translator.md` は `VcIr` から `AtpProblem` への translation boundary を仕様化し、task 5 は declaration / symbol-map slice を実装し、task 6 は axiom/conjecture materialization と final problem construction を実装する。 | paired spec の後で property encoding に進む。 |
| ATP-G-007 | `deferred` | `mizar-vc` handoff は現在、local、generated、imported、final-goal、policy-builtin formula source を明示するが、dedicated checker-owned / type-predicate source class はまだ公開していない。 | `mizar-atp` は対応する explicit handoff source/projection が存在しない限りそれらの premise ref を fail closed し、placeholder source class を発明しない。 |
| ATP-G-008 | resolved `source_drift` with remaining `deferred` native path | `src/property_encoding.rs` は task-8 axiom-form property projection、generated-binder row、provenance、profile gate、duplicate rejection、`AtpProblem` integration を実装する。native declaration は concrete encoder spec が後続 task のため deferred のままである。 | concrete encoder spec が exact semantics を定義するまで native declaration は fail-closed/deferred に保つ。backend-native shortcut を追加しない。 |
| ATP-G-009 | resolved `deferred` spec gap | `tptp_encoder.md` は task-10 source が存在する前に deterministic FOF emission、label、name mangling、provenance metadata、profile gate、deferred typed/native/backend behavior を仕様化した。 | task 10 で closed。TFF-like typed output、native declaration、backend execution、evidence extraction は ATP-G-010 の下で deferred のままである。 |
| ATP-G-010 | resolved `source_drift` with remaining `deferred` typed/backend paths | `src/tptp_encoder.rs` は deterministic FOF text emission、side metadata、profile gate、scope validation、diagnostics exclusion、native-declaration rejection、raw-name injection avoidance、lint/API guard を実装する。 | TFF-like typed output、CNF、include file、native declaration、backend execution、evidence-candidate extraction はそれぞれの spec/task まで deferred に保つ。 |
| ATP-G-011 | resolved `deferred` spec gap | `smtlib_encoder.md` は task-12 source が存在する前に deterministic uninterpreted SMT-LIB emission、fixed `mizar_universe` sort encoding、profile-driven `QF_UF` / `UF` logic selection、named assertion、provenance metadata、fail-closed/deferred behavior を仕様化した。 | task 12 で closed。arithmetic theory、sorted signature、backend sort strategy、native declaration、solver option、proof/unsat-core command、backend execution、evidence-candidate extraction は ATP-G-012 の下で deferred のままである。 |
| ATP-G-012 | resolved `source_drift` with remaining `deferred` theory/sorted/backend paths | `src/smtlib_encoder.rs` は deterministic uninterpreted SMT-LIB text emission、fixed universe sort declaration、function/predicate declaration、named assertion、negated conjecture polarity、side metadata、profile gate、scope validation、diagnostics/proof-object exclusion、native-declaration rejection、raw-name injection avoidance、unused-sort tolerance、lint/API guard を実装する。 | arithmetic theory、sorted signature、backend sort strategy、native declaration、solver option、proof/unsat-core command、backend execution、evidence-candidate extraction はそれぞれの spec/task まで deferred に保つ。 |
| ATP-G-013 | resolved `deferred` spec gap | `backend.md` は `src/backend.rs` が存在する前に generic child-process execution、resource limit、version/run metadata、deterministic fingerprint、timeout/cancellation/crash handling、fail-closed result classification、candidate-evidence-only `Proved` invariant を仕様化する。 | task 14 で closed。 |
| ATP-G-014 | resolved `source_drift` with remaining `deferred` real-backend/downstream paths | `src/backend.rs` は generic direct-spawn runner、deterministic hash、stdin/private-file input、version probe、timeout/cancellation/crash handling、drain-safe capture、private temp cleanup、unsupported required-limit error、mock candidate classification を実装する。 | real backend adapter、backend-specific proof/output parser、real backend output からの formula/substitution candidate extraction、portfolio execution、proof policy、witness/cache publication、kernel checking はそれぞれの task/crate まで deferred に保つ。 |
| ATP-G-015 | `external_dependency_gap` / `deferred` | task 15 は kernel task 25-28 と VC handoff 完了後に最初の concrete backend route を再評価した。real backend output を kernel が parse できる formula/substitution candidate payload に変換する paired `mizar-atp` evidence-extraction spec/source module はなく、supported backend executable も verification environment で利用できなかった。 | real adapter、backend-output parser、fake candidate schema、kernel call、proof-policy hook、witness/cache output、trusted backend proof material を追加しない。paired EN/JA extraction spec と guarded supported backend route が存在した後に再開する。 |
| ATP-G-016 | `external_dependency_gap` / `deferred` | task 16 は task-15 real-output extraction route に依存する。task 14 は process-status と mock candidate classification invariant を既に guard している。real-output parsing と polarity fixture には backend-specific output grammar と candidate extraction が必要だが、まだ仕様化されていない。 | fake observed-output schema、backend-specific classification table、adapter、kernel call、trusted backend proof material を追加しない。extraction spec と guarded backend fixture が存在した後、task 15 と共に再開する。 |
| ATP-G-017 | resolved `deferred` spec gap with remaining `external_dependency_gap` policy paths | `portfolio.md` は policy-neutral な portfolio planning、candidate collection、deterministic ordering、resource budget、cancellation / early-stop constraint、handoff boundary を仕様化する。 | task 18 はこの境界内でだけ source を実装した。proof-policy finality、artifact-facing winner selection、witness publication、proof-cache promotion、real-output evidence extraction は、所有 spec と crate が存在するまで `mizar-atp` の外に保つ。 |
| ATP-G-018 | resolved `source_drift` with remaining `external_dependency_gap` policy paths | `src/portfolio.rs` は prebuilt backend run/result に対する deterministic no-early-stop planning と candidate collection、same-problem validation、stable portfolio/candidate hash、partial candidate を出さない cancellation、focused test を実装する。 | proof-policy finality、kernel check、witness/cache/artifact publication、real-output evidence extraction は所有 contract が存在するまでこの module の外に保つ。 |
| ATP-G-019 | resolved `source_drift` with remaining `external_dependency_gap` artifact paths | `BackendRunMetadata` は artifact / reproducibility note のために、seed、timeout setting、backend identity / version record、command fingerprint、stream/resource usage、elapsed time、diagnostic を `BackendRunResult` から project する。 | runtime observation は trusted evidence と candidate / trusted-acceptance hash の外に残る。artifact serialization は artifact/proof/cache owner が handoff を定義するまで外部に残す。 |
| ATP-G-020 | resolved `source_drift` with remaining `external_dependency_gap` active-runner paths | `tests/property/atp_mock_backend_integration_001.*` は metadata-only `advanced_semantics` corpus intent を記録し、`tests/coverage/spec_trace.toml` はそれを task 20 に link し、`tests/mock_backend_corpus.rs` はそれらの fixture case を既存の mock backend classification と portfolio API に通す。`mizar-test` にはまだ active `advanced_semantics` runner または tag gate がない。 | active `.miz` advanced semantics execution、source-derived ATP extraction、real-output extraction、kernel check、proof policy、witness/cache/artifact publication、placeholder evidence schema はこの task の外に保つ。 |
| ATP-G-021 | resolved `test_gap` with remaining `external_dependency_gap` downstream paths | task 21 は crate-local determinism suite を追加する。public `mizar-vc` API で同一の `VcIr` fixture を構築し、kernel handoff を作り、`AtpProblem` に translate し、TPTP FOF と SMT-LIB uninterpreted の両 encoding を cover し、reversed backend completion order 下の mock portfolio candidate ordering を検査する。 | real backend extraction、kernel checking、proof policy、artifact witness publication、proof cache promotion、active `.miz` advanced-semantics execution はこの test の外に保つ。 |
| ATP-G-022 | resolved `source_undocumented_behavior` | public ATP enum は既に downstream 向け `#[non_exhaustive]` attribute を持っていたが、所有 module spec は exact enum inventory や variant-extension policy を記録していなかった。task 22 は英語/日本語の exact inventory を記録し、source と両 spec を比較する lint-policy guard を追加する。 | 将来の variant は paired spec/test review を必要とする。behavior-sensitive match は、意図的 fallback が所有 spec に記録されていない限り explicit/fail-closed のままにする。この互換性 task は backend、kernel、witness、proof-policy behavior を追加しない。 |
| ATP-G-023 | resolved `source_undocumented_behavior` audit with remaining classified external/deferred follow-ups | task 23 は paired source/spec correspondence audit と lint guard を追加し、public module、top-level public item、public entry function、cross-module evidence、ATP-AUDIT gap id/class を cover する。 | audit は未分類の source/spec drift を記録しない。real-output extraction、active source-derived corpus execution、producer/proof/cache integration、real artifact witness publication、typed/native encoder extension、proof-policy early-stop は external/deferred に残し、placeholder implementation は追加しない。 |
| ATP-G-024 | resolved bilingual documentation audit with remaining classified external/deferred follow-ups | task 24 は paired bilingual sync audit と lint guard を追加し、EN/JA design-doc filename の完全一致、pair inventory、trust-boundary/deferred marker を cover する。 | audit は bilingual drift や repo metadata conflict を記録しない。source/API behavior は変更せず、real-output extraction、active source-derived corpus execution、proof/cache integration、real artifact witness publication、typed/native encoder extension、proof-policy early-stop は external/deferred のままにする。 |
| ATP-G-025 | `external_dependency_gap` / `deferred` | task 25 は no-early-stop portfolio と determinism suite の後に portfolio completion-order independence gate を再評価した。現在 `mizar-proof` は存在し release-policy finality、winner selection、tie-breaking、candidate-displacement metadata を所有するが、`mizar-atp` はその API を呼び出す integration task をまだ持たない。 | `mizar-atp` に mock proof-policy oracle、placeholder `mizar-proof` adapter、accepted proof state、kernel call、witness/cache output、trusted backend proof material を追加しない。formal proof-policy API を消費する専用 integration task まで、crate-local guarantee は shuffled completion order 下の deterministic no-early-stop candidate handoff に保つ。 |
| ATP-G-026 | resolved architecture-22 follow-up audit with remaining `external_dependency_gap` ATP-AUDIT-G005 | task 26 は task 25 について source/spec audit と bilingual audit を再実行する。Architecture 22 は active proof policy 下の deterministic portfolio winner selection を要求し、"first backend to finish" や runtime duration を semantic proof identity にすることを禁じる。現在の `mizar-atp` source は no-early-stop candidate handoff producer のままで、proof-policy integration contract を持たない。 | 新しい follow-up id は不要である。専用 ATP/proof integration task が `mizar-proof` の release-policy finality、tie-breaking、candidate-displacement decision を消費するまで ATP-AUDIT-G005 を open のままにする。 |
| ATP-G-027 | resolved module-boundary review-bottleneck layout audit | task 27 は inline unit-test suite による oversized file を発見し、private `cfg(test)` child module に分割した。production module / spec mapping は一対一のままである。 | public API、production behavior、diagnostic、deterministic rendering、artifact schema、trust-boundary behavior は変更しない。production helper extraction は paired module spec が具体的な helper boundary を定義するまで deferred に保つ。 |
| ATP-G-028 | resolved crate exit report and quality review with post-closeout metadata correction | task 28 は autonomous crate exit report、hard gate evidence、quality score 94/100、残る ATP-CLOSEOUT gap を記録する。Post-closeout correction は proof crate milestone 後の古い `mizar-proof` placeholder metadata を更新する。 | `mizar-cache` は現在存在し cache validation を所有するが、この ATP milestone は proof/cache API をまだ消費しない。`mizar-proof` は正式だがここでは消費しない。placeholder や local proof policy は追加しない。real-output extraction、proof/cache/artifact integration、active source-derived ATP execution は external/deferred に保つ。 |
| ATP-G-029 | resolved task-29 candidate-evidence conformance with remaining `external_dependency_gap` joint execution | task 29 は post-audit kernel 契約の ATP consumer 側を確認する: proof-obligation handoff polarity は refutation でなければならず、非 import source binding は producer label ではなく VC handoff identity に一致し、imported fact は kernel-validated statement projection を使う。focused regression は upstream F1 polarity rejection と ATP F2 local-source rejection を cover し、既存 imported-projection regression が F6 を cover する。 | fake payload、placeholder runner、expectation rebaseline、kernel call、proof policy、witness/cache output、trusted backend material を追加しない。task 29 で名指しされた source-derived kernel corpus fixture は、real route が存在するまで既存の real-output/source-derived ATP extraction gap により blocked のままである。 |

## Task 1: crate scaffold and lint-policy guard

Purpose: workspace member を追加し、production dependency を evidence-candidate
boundary に限定し、workspace lint の継承を guard する。

Spec refs: architecture 09、architecture 10、architecture 15、architecture 19、
internal 04、internal 07。

Tests: `crates/mizar-atp/tests/lint_policy.rs`。

Affected design files: この crate plan と両方の TODO。

Affected source files: workspace `Cargo.toml`、`crates/mizar-atp/Cargo.toml`、
`crates/mizar-atp/src/lib.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: lint-policy guard test が通り、workspace が新しい member を認識し、
`cargo test -p mizar-atp`、`cargo clippy -p mizar-atp --all-targets -- -D warnings`、
`cargo fmt --check` が通り、task が独立して commit されている。

Forbidden behavior: ATP problem semantics、encoder、backend process runner、portfolio
policy、proof acceptance、kernel checking、witness publication、cache reuse、trusted
backend proof material を導入しない。

## Task 2: Spec: problem.md

Purpose: semantic Rust module を公開する前に、backend-neutral な `AtpProblem` data
shape を定義する。

Spec refs: architecture 09「Backend-Neutral Problem Layer」、architecture 01 の
`VcIr` / `AtpProblem` layer ownership、architecture 15 の trusted evidence boundary、
architecture 19 の failure semantics、internal 04 の ATP/kernel handoff boundary。

Tests: task 2 では Rust test を追加しない。task 3 が construction、determinism、
provenance completeness、prohibited-material API test を追加する。

Affected design files: `problem.md`、この crate plan、両 TODO。

Affected source files: なし。

Completion condition: 英語仕様と日本語仕様が同期し、TODO で task 2 が checked off
され、残る implementation / external gap が分類され、review-only agent が
blocking/high finding なしを報告し、docs-only verification が separate commit 前に通ること。

Forbidden behavior: `src/problem.rs`、public API exposure、backend runner、
kernel checking、proof acceptance、placeholder proof/cache integration を追加しない。

## Task 3: `problem` data shape の実装

Purpose: spec-backed な backend-neutral `AtpProblem` data model、validation、
deterministic ordering、debug rendering を実装する。

Spec refs: `problem.md`、architecture 09、architecture 15、architecture 19、internal 04。

Tests: `problem` unit test と `tests/lint_policy.rs`。

Affected design files: この crate plan と両 TODO。

Affected source files: `crates/mizar-atp/src/lib.rs`、
`crates/mizar-atp/src/problem.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: construction、determinism、provenance completeness、
fail-closed missing required input、unsupported-profile、`Unsat` polarity、
prohibited-material test が通り、review-only agent が blocking/high finding なしを
報告し、crate verification が通り、この task が separate commit されること。

Forbidden behavior: translator、data representation を超える property encoder、
TPTP/SMT-LIB encoder、backend runner、portfolio policy、kernel checking、proof
acceptance、witness publication、cache promotion、trusted backend proof material を
追加しない。

## Task 4: Spec: `translator.md`

Purpose: translator source を追加する前に、deterministic な `VcIr` から
`AtpProblem` への translation を仕様化する。

Spec refs: `translator.md`、`problem.md`、architecture 09、architecture 15、
architecture 19、internal 04、`mizar-vc` kernel-evidence handoff ownership。

Tests: task 4 では Rust test を追加しない。task 5 と 6 が status/target rejection、
structured-projection、malformed/unsupported formula/profile、declaration、symbol-map、
duplicate-premise、proof-hint non-pruning、imported context / proof-status、premise-order、
provenance、soft-type preservation、`Unsat` polarity、prohibited-material absence test を
追加する。

Affected design files: `translator.md`、この crate plan、両 TODO。

Affected source files: なし。

Completion condition: 英語 translator spec と日本語 translator spec が同期し、TODO で
task 4 が checked off され、残る implementation / external gap が分類され、
review-only agent が blocking/high finding なしを報告し、docs-only verification が通り、
この task が separate commit されること。

Forbidden behavior: `src/translator.rs`、problem source edit、backend runner、
concrete encoder、kernel checking、proof acceptance、premise invention、
opaque handoff byte からの復元、placeholder proof/cache integration を追加しない。

## Task 5: Declaration and symbol-map translation

Purpose: task-5 translator slice を実装する。`NeedsAtp` status と target handoff を
検査した後、structured declaration / soft-type projection を deterministic な
`AtpDeclaration`、`AtpSymbolMapEntry`、`AtpTypeContext`、provenance、diagnostic、
`AtpTargetBinding` output に lower する。

Spec refs: `translator.md`、`problem.md`、architecture 09、architecture 15、
architecture 19、internal 04、`mizar-vc` kernel-evidence handoff ownership。

Tests: translator unit test と `tests/lint_policy.rs` が status/target rejection、
stale handoff rejection、deterministic declaration / symbol-map ordering、
duplicate/malformed projection fail-closed behavior、type guard の signature validation、
soft-type profile preservation、imported proof-status rejection、prohibited-material API guard
を cover する。

Affected design files: `translator.md`、この crate plan、両 TODO。

Affected source files: `crates/mizar-atp/src/lib.rs`、
`crates/mizar-atp/src/translator.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: task-5 translator source が spec-backed であり、TODO で task 5 が
checked off され、残る implementation / external gap が分類され、review-only agent が
blocking/high finding なしを報告し、crate verification が通り、この task が separate
commit されること。

Forbidden behavior: axiom/conjecture materialization、final `AtpProblem` construction、
opaque handoff byte の parse、premise selection、backend runner、concrete encoder、
kernel/SAT checking、proof acceptance、witness/cache integration、trusted backend proof
material を追加しない。

## Task 6: Axiom and conjecture translation

Purpose: structured formula projection を消費し、immutable な sorted VC premise list を
axiom として materialize し、VC goal を conjecture として materialize し、
`ExpectedBackendResult::Unsat` を持つ final backend-neutral `AtpProblem` を構築する
task-6 translator slice を実装する。

Spec refs: `translator.md`、`problem.md`、architecture 09、architecture 15、
architecture 19、internal 04、`mizar-vc` kernel-evidence handoff ownership。

Tests: translator unit test と `tests/lint_policy.rs` は non-`NeedsAtp` と stale handoff
rejection、missing/malformed/duplicate formula projection、handoff fingerprint/provenance
agreement、final-goal handoff polarity と `goal:1` binding agreement、unsupported profile
feature、duplicate premise ref、duplicate resolved formula/source identity、repeated imported
source tuple、proof-hint non-pruning、imported metadata/status/class/context rejection、
shuffled formula projection に対する deterministic problem output、soft-type preservation、
固定の `Unsat` polarity、prohibited-material API guard を cover する。

Affected design files: `translator.md`、この crate plan、両 TODO。

Affected source files: `crates/mizar-atp/src/translator.rs` と
`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: task-6 translator source が spec-backed であり、TODO で task 6 が
checked off され、残る implementation / external gap が分類され、review-only agent が
blocking/high finding なしを報告し、crate verification が通り、この task が separate
commit されること。

Forbidden behavior: opaque handoff formula byte の parse、premise selection/invention、
backend runner、concrete encoder、kernel/SAT checking、proof acceptance、witness/cache
integration、trusted backend proof material、unsupported premise family 用 placeholder source
class を導入しない。

## Task 7: Spec: `property_encoding.md`

Purpose: property-encoding source が存在する前に、英語/日本語ペアの property-encoding
spec を追加する。

Spec refs: architecture 09「Property Encoding」、architecture 08 reasoning boundary、
architecture 15 trusted evidence boundary、architecture 19 failure semantics、
`problem.md`、`translator.md`。

Tests: docs-only task。review-only agent が spec の completeness、consistency、supported-family
coverage、generated-binder declaration、symbol-map、provenance requirement、native declaration
deferral gate、connectedness disjunction coverage、property provenance requirement、
source/deferred boundary を確認する。
verification は `git diff --check` と `git diff --cached --check`。

Affected design files: `property_encoding.md`、この crate plan、両 TODO。

Affected source files: なし。

Completion condition: task-7 paired docs が存在し、TODO で task 7 が checked off され、
残る source gap が deferred に分類され、review-only agent が blocking/high finding なしを
報告し、docs-only verification が通り、この task が separate commit されること。

Forbidden behavior: Rust source module、property-source placeholder、concrete encoder、
backend runner、kernel/SAT checking、proof acceptance、witness/cache integration、trusted
backend-native property または proof material を追加しない。

## Task 8: property encoding

Purpose: review 済み property-encoding spec を source-backed producer module として実装する。

Spec refs: `property_encoding.md`、architecture 09「Property Encoding」、architecture 15
trusted evidence boundary、architecture 19 failure semantics、`problem.md`、`translator.md`。

Tests: `crates/mizar-atp/src/property_encoding.rs` unit tests は supported family すべての
axiom form、generated-binder declaration / symbol-map row / provenance、connectedness `Or`、
target kind/arity/declaration/source validation、profile rejection、duplicate source と encoded
identity、native-declaration deferral、`AtpProblem::try_new` integration を cover する。
`crates/mizar-atp/tests/lint_policy.rs` は task-8 public API surface を守り、backend runner、
proof policy、kernel check、trusted backend material、witness、cache surface が追加されていないことを確認する。

Affected design files: この crate plan と両 TODO。

Affected source files: `crates/mizar-atp/src/property_encoding.rs`、
`crates/mizar-atp/src/lib.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: property encoding が `EncodedProperty::axiom` row と generated binder
support row だけを emit し、native declaration は deferred のままであり、review-only agent が
blocking/high finding なしを報告し、Rust verification が通り、この task が separate commit されること。

Forbidden behavior: native backend property emission、concrete TPTP/SMT-LIB encoder、
backend runner、kernel/SAT checking、proof acceptance、witness/cache integration、trusted
backend-native property または proof material を追加しない。

## Task 9: 仕様: `tptp_encoder.md`

Purpose: concrete TPTP source が存在する前に paired English/Japanese TPTP encoder spec を
追加する。

Spec refs: architecture 09「Supported Formats」「Encoding Strategy」「Interface Definitions」、
architecture 08 reasoning boundary、architecture 15 trusted evidence boundary、architecture 19
failure semantics、`problem.md`、`translator.md`、`property_encoding.md`。

Tests: docs-only task。review-only agent は FOF dialect coverage、name mangling determinism、
exact formula/role rendering、variable scope validation、provenance side metadata、
fail-closed/deferred classification、evidence-boundary consistency を確認する。verification は
`git diff --check` と `git diff --cached --check` を使う。

Affected design files: `tptp_encoder.md`、この crate plan、両 TODO。

Affected source files: なし。

Completion condition: task-9 paired docs が存在し、TODO で task 9 が checked off され、
remaining source gap が deferred に分類され、review-only agent が blocking/high finding なしを
報告し、docs-only verification が通り、この task が separate commit されること。

Forbidden behavior: Rust source module、TPTP text emitter、SMT-LIB encoder、backend runner、
kernel/SAT checking、proof acceptance、witness/cache integration、trusted backend proof material、
TFF/native property shortcut、legacy certificate または resolution-trace acceptance を追加しない。

## Task 10: TPTP エンコーダ

Purpose: reviewed task-9 TPTP encoder spec を deterministic FOF source として実装する。

Spec refs: `tptp_encoder.md`、architecture 09「Supported Formats」「Encoding Strategy」
「Interface Definitions」、architecture 08 reasoning boundary、architecture 15 trusted evidence
boundary、architecture 19 failure semantics、`problem.md`、`translator.md`、
`property_encoding.md`。

Tests: `crates/mizar-atp/src/tptp_encoder.rs` unit tests は `ax_`、`tg_`、`prop_`、
`conj_` ordering を含む golden output、shuffled equivalent input での byte-identical output、
diagnostics exclusion、exact formula rendering、profile gate、native-declaration rejection、
free-variable / duplicate-binder / shadowing / sorted-binder failure、private malformed-formula
check、raw-name injection avoidance、symbol と formula-label metadata、duplicate/illegal name
check を cover する。`crates/mizar-atp/tests/lint_policy.rs` は task-10 public API を守り、
backend runner、proof policy、kernel check、trusted backend material、witness、cache surface が
追加されていないことを確認する。

Affected design files: この crate plan と両 TODO。

Affected source files: `crates/mizar-atp/src/tptp_encoder.rs`、
`crates/mizar-atp/src/lib.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: TPTP source が deterministic FOF text と side metadata だけを emit し、
typed/native/backend route は deferred のままであり、review-only agent が blocking/high finding
なしを報告し、Rust verification が通り、この task が separate commit されること。

Forbidden behavior: TFF/TXF/THF/CNF/include/native-property shortcut、SMT-LIB encoder、
backend runner、kernel/SAT checking、proof acceptance、witness/cache integration、trusted
backend proof material、legacy certificate または resolution-trace acceptance を追加しない。

## Task 11: 仕様: `smtlib_encoder.md`

Purpose: SMT-LIB encoder source を追加する前に、deterministic uninterpreted SMT-LIB emission
を仕様化する。

Spec refs: `smtlib_encoder.md`、architecture 09「Supported Formats」、
「Validity Checking Polarity」、「Encoding Strategy」、「Interface Definitions」、
architecture 08 reasoning boundary、architecture 15 trusted evidence boundary、
architecture 19 failure semantics、`problem.md`、`translator.md`、`property_encoding.md`。

Tests: docs-only task。review-only agent は、fixed `mizar_universe` sort encoding、
soft-type guard preservation、`QF_UF` / `UF` logic selection、premise plus negated
conjecture polarity、exact formula/command rendering、variable scope validation、
provenance side metadata、fail-closed/deferred classification、evidence-boundary
consistency を確認する。verification は `git diff --check` と `git diff --cached --check`
を使う。

Affected design files: `smtlib_encoder.md`、この crate plan、両 TODO。

Affected source files: none。

Completion condition: task-11 paired docs が存在し、TODO で task 11 が checked off され、
remaining source gap が deferred に分類され、review-only agent が blocking/high finding なしを
報告し、docs-only verification が通り、この task が separate commit されること。

Forbidden behavior: Rust source module、SMT-LIB text emitter、TPTP change、backend runner、
kernel/SAT checking、proof acceptance、witness/cache integration、trusted backend proof
material、solver-option または proof/unsat-core command shortcut、native property shortcut、
legacy certificate または resolution-trace acceptance を追加しない。

## Task 12: SMT-LIB エンコーダ

Purpose: reviewed task-11 SMT-LIB encoder spec を deterministic uninterpreted SMT-LIB
source として実装する。

Spec refs: `smtlib_encoder.md`、architecture 09「Supported Formats」、
「Validity Checking Polarity」、「Encoding Strategy」、「Interface Definitions」、
architecture 08 reasoning boundary、architecture 15 trusted evidence boundary、
architecture 19 failure semantics、`problem.md`、`translator.md`、`property_encoding.md`。

Tests: `crates/mizar-atp/src/smtlib_encoder.rs` unit tests は、`set-logic`、
`mizar_universe`、declaration、`ax_`、`tg_`、`prop_`、`neg_conj_`、`(check-sat)`
ordering を含む golden output、`QF_UF` / `UF` logic selection、exact formula rendering、
premise plus negated conjecture polarity、shuffled equivalent input と diagnostic 変更での
byte-identical output、fixed-sort guard/type-guard preservation、unsupported sort strategy、
sorted binder、sort-dependent use、equality、quantifier profile gate、未使用 sort declaration を
accept し sort declaration を render しないこと、native declaration rejection、scope/arity/source
failure case、raw-name injection と SMT-LIB symbol collision rejection、assertion-label と symbol
の side metadata、proof/unsat-core/backend-material trust がないことを cover する。
`crates/mizar-atp/tests/lint_policy.rs` は task-12 public API を守り、backend runner、proof
policy、kernel check、trusted backend material、witness、cache surface が追加されていないことを
確認する。

Affected design files: この crate plan と両 TODO。

Affected source files: `crates/mizar-atp/src/smtlib_encoder.rs`、
`crates/mizar-atp/src/lib.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: SMT-LIB source が deterministic uninterpreted SMT-LIB text と side
metadata だけを emit し、sorted/theory/native/backend route は deferred のままであり、
review-only agent が blocking/high finding なしを報告し、Rust verification が通り、この task が
separate commit されること。

Forbidden behavior: arithmetic-theory、array、datatype、bit-vector、sorted signature、
backend-sort、native-property、solver-option、proof-command、unsat-core shortcut、backend
runner、kernel/SAT checking、proof acceptance、witness/cache integration、trusted backend proof
material、legacy certificate または resolution-trace acceptance を追加しない。

## Task 13: 仕様: `backend.md`

Purpose: backend source を追加する前に backend runner contract を仕様化する。

Spec refs: `backend.md`、architecture 10「Process Model」「Result Classification」、
architecture 15 trusted evidence boundary、architecture 19 failure semantics、internal 04
「Backend Runner」、`problem.md`。

Tests: docs-only task。review-only agent は process execution scope、resource-limit と
timeout/cancellation/crash semantics、version と run metadata、deterministic command
fingerprint、result-classification invariant、evidence-boundary exclusion、bilingual
synchronization、task 14 test expectation を確認する。verification は `git diff --check` と
`git diff --cached --check` を使う。

Affected design files: `backend.md`、この crate plan、両 TODO。

Affected source files: なし。

Completion condition: task-13 paired docs が存在し、TODO で task 13 が checked off され、
remaining source / external gap が分類され、review-only agent が blocking/high finding なしを
報告し、docs-only verification が通り、この task が separate commit されること。

Forbidden behavior: Rust source module、child-process spawning、real backend adapter、
backend proof parser、candidate evidence extractor、portfolio、kernel/SAT checking、proof
acceptance、witness/cache integration、trusted backend proof method、unsat-core /
SMT-proof-object trust、legacy certificate または resolution-trace acceptance を追加しない。

## Task 14: backend runner

Purpose: reviewed backend spec を generic child-process runner と mock-backend test harness として
実装する。

Spec refs: `backend.md`、architecture 10「Process Model」「Result Classification」、
architecture 19 failure semantics、internal 04「Backend Runner」。

Tests: `crates/mizar-atp/src/backend.rs` unit tests は、stdin と private problem-file input
mode、shell interpretation を使わない direct executable / argument spawning、deterministic
command fingerprint、rewriting、normalization、appended proof command、unsat-core request、
inferred polarity change のない byte-exact input delivery、backend に path を露出せず fd 0 に接続する
private spool 経由の stdin delivery と verifier 側 writer-thread deadlock がないこと、process id、
temp path、timestamp、raw completion order、machine-local absolute executable / working-directory
path を除外する deterministic command fingerprint、sort 済み allowlisted environment recording、
version-probe success/failure metadata、timeout、cancellation、kill-grace、crash、non-zero
exit、missing executable、spawn-permission failure、stdout/stderr hashing と truncation
diagnostic、retained limit 後も drain して complete observed stream を hash すること、exclusive /
private-path semantics による private temporary directory creation と cleanup、
timeout/cancellation/crash 後に child process が残らないこと、resource-limit metadata と
unsupported-limit diagnostic、unsupported required limit が `Error` になること、polarity
mismatch、formula/substitution evidence payload/ref 不在、candidate metadata mismatch、
unsupported required limit、incomplete stream、または timeout/cancellation/crash/parsing
corruption 後の otherwise matching evidence での `Proved` rejection、
`ExpectedBackendResult::Unsat` と supported payload/ref および matching candidate metadata がある場合だけ
mock `Proved` になること、trusted backend proof method、resolution trace、unsat core、SMT proof
object、backend log、backend `used_axioms` が存在しないことを cover しなければならない。

Affected design files: この crate plan と両 TODO。

Affected source files: `crates/mizar-atp/src/backend.rs`、
`crates/mizar-atp/src/lib.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: 完了。generic runner source が spec-backed であり、TODO で task 14 が
checked off され、remaining real-backend / downstream integration gap が分類され、
review-only agent が blocking/high finding なしを報告し、Rust verification が通り、この task が
separate commit されること。

Forbidden behavior: real backend adapter、backend-specific proof/output parser、real backend
output からの formula/substitution candidate extraction、portfolio execution、proof policy、
witness/cache publication、kernel/SAT checking、trusted backend proof method、unsat-core /
SMT-proof trust、backend `used_axioms` trust、legacy certificate または resolution-trace
acceptance を追加しない。

## Task 15: 最初の具体バックエンド統合 gate

Purpose: task 14 後に real backend adapter と formula/substitution candidate
extractor を安全に実装できるかを解決する。

Spec refs: `backend.md`、architecture 10「Supported Backends」、architecture 15
「Formula/Substitution Evidence」、internal 04「Evidence-Format Correction」と
「Affected Modules」、`mizar-kernel` formula evidence、`mizar-vc` kernel evidence
handoff。

Gate result: deferred / external dependency gap。kernel formula/substitution evidence
parse/check と VC handoff は存在するが、internal 04 が `evidence.md` として挙げる paired
extraction spec/source module が `mizar-atp` にはまだない。この contract がなければ、
どの real backend output を formula/substitution candidate bytes または ref に変換するかを
placeholder schema なしに定義できない。task-15 environment には `vampire`、`eprover`、
`cvc5`、`z3` もなく、backend-available integration test で concrete adapter を検証できない。

Affected design files: この crate plan、両 TODO、両 backend spec companion。

Affected source files: なし。この gate では Rust source、adapter、backend parser、
kernel call、witness/cache/proof-policy integration、placeholder evidence schema を追加しない。

Completion condition: deferred gate として完了。TODO は task 15 を deferred /
`external_dependency_gap` として記録し、crate plan は ATP-G-015 を記録し、backend spec の
gap classification を更新し、review-only agent が blocking/high finding なしを報告し、
docs verification が通り、この gate が separate commit されること。

Reopen condition: selected backend から kernel-parseable formula/substitution candidate
payload/ref を構築する paired English/Japanese `evidence.md` spec（または同等の backend-specific
extraction spec）を追加し、architecture-10 supported backend 1 つ以上を guarded integration
environment で利用可能にすること。

## Task 16: 結果分類と極性検証 gate

Purpose: task 15 後に real backend-output classification fixture を実装できるかを判断する。

Spec refs: `backend.md`「Result Classification」、architecture 10「Result Classification」、
task-15 gate record。

Gate result: deferred / external dependency gap。task 14 は `Proved` を non-accepted かつ
fail-closed に保つ process-status と mock candidate-evidence invariant を既に実装している。
task 16 の残り scope は real-output parsing と polarity fixture であり、deferred の task-15
extraction route に依存する。selected supported backend、backend-output grammar、
kernel-parseable formula/substitution candidate extraction contract がなければ、real-output
classifier は placeholder になる。

Affected design files: この crate plan、両 TODO、両 backend spec companion。

Affected source files: なし。この gate では Rust source、backend-specific parser、
observed-output schema、adapter-specific classification table、kernel call、trusted backend proof
material を追加しない。

Completion condition: deferred gate として完了。TODO は task 16 を deferred /
`external_dependency_gap` として記録し、crate plan は ATP-G-016 を記録し、backend spec の
gap classification を更新し、review-only agent が blocking/high finding なしを報告し、
docs verification が通り、この gate が separate commit されること。

Reopen condition: task-15 extraction route が paired extraction spec と guarded supported
backend fixture により再開されること。その後 task 16 は trusted acceptance boundary を変えずに、
その route に対する real-output classification fixture を追加してよい。

## Task 17: 仕様: portfolio.md

Purpose: `src/portfolio.rs` implementation が存在する前に、policy-neutral な ATP portfolio
boundary を定義する。

Spec refs: architecture 10「Portfolio Execution」、internal 04「ATP Portfolio Service」、
architecture 15、architecture 19。

Status: docs-only task として完了。英語正本 `portfolio.md` と日本語 companion は、VC ごとの
portfolio planning、candidate evidence collection、deterministic candidate ordering、resource
budget、cancellation / early-stop constraint、failure semantics、kernel / proof-policy handoff
boundary を仕様化する。

Affected design files: `portfolio.md` と日本語 companion、この crate plan と日本語 companion、
両 TODO。

Affected source files: なし。Rust source、backend adapter、real-output parser、evidence
extractor、kernel call、proof policy evaluator、witness/cache publication、accepted proof status、
trusted backend proof material は追加しない。

Completion condition: `portfolio.md` が両言語で存在し、TODO task 17 が完了として印付けされ、
ATP-G-017 が source / policy gap を記録し、review-only agent が blocking/high finding なしを
報告し、docs verification が通り、この task が separate commit されること。

Deferred source boundary: task 18 は no-early-stop candidate collection を実装してよい。
stable な外部 proof-policy contract が存在した後に限って policy finality を尊重してよい。
ただし `mizar-proof` policy を local に実装したり、real-backend evidence extraction を
fabricate したり、artifact-facing proof winner を選択したりしてはならない。

## Task 18: portfolio 実行

Purpose: proof-policy、kernel、witness、cache、real-output extraction boundary を越えずに、
policy-neutral な no-early-stop portfolio planning と candidate collection を実装する。

Spec refs: `portfolio.md`、architecture 10「Portfolio Execution」、internal 04
「ATP Portfolio Service」、architecture 15、architecture 19。

Status: 完了。`src/portfolio.rs` は prebuilt `BackendRunInput` value から deterministic plan を
構築し、run と result が同じ `AtpProblem` に属することを検証し、terminal `BackendRunResult`
value を deterministic な `PortfolioEvidenceSet` に収集し、completion order から独立して
formula/substitution candidate を sort し、stable plan/candidate/evidence-set hash を記録し、
partial candidate のない cancelled evidence set を返す。

Affected design files: `portfolio.md` と日本語 companion、この crate plan と日本語 companion、
両 TODO。

Affected source files: `crates/mizar-atp/src/portfolio.rs`、
`crates/mizar-atp/src/lib.rs`、`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: source と docs が一致し、unit test が deterministic planning、shuffled
completion order、cancellation、same-problem validation、stopped-plan result matching、
missing/unknown/duplicate result、result と candidate の metadata mismatch、backend status
propagation、counterexample、prohibited boundary string を cover し、review-only agent が
blocking/high finding なしを報告し、Rust と docs verification が通り、この task が separate
commit されること。

Remaining external gaps: early-stop policy finality、artifact-facing winner selection、kernel
checking、witness/cache publication、real-output formula/substitution extraction は task 18 の外に残る。

## Task 19: ATP run metadata recording

Purpose: artifact と reproducibility note のための stable backend run-metadata projection を追加し、
runtime observation を trusted acceptance material と downstream candidate hash の外に保つ。

Spec refs: architecture 00「Incrementality and Reproducibility」、`backend.md`
「Backend Identity And Reproducibility」、architecture 10、internal 04。

Status: backend-runner metadata boundary 内で完了。`backend.rs` は `BackendRunResult` から
read-only `BackendRunMetadata` projection を公開し、focused test は metadata completeness と、
runtime observation が異なっても command fingerprint が安定することを cover する。artifact writing、
proof witness publication、cache promotion、proof-policy selection、kernel checking、real backend
output extraction、trusted backend proof material は追加しない。

Completion condition: source と docs が一致し、unit test が seed、timeout、backend identity/version、
resource/stream usage、diagnostic、semantic hash exclusion を cover し、review-only agent が
blocking/high finding なしを報告し、Rust と docs verification が通り、この task が separate commit
されること。

## Task 20: corpus and mock-backend integration suite

Purpose: committed `advanced_semantics` corpus anchor を追加し、active advanced-semantics
source runner を fabricate せずに、既存の mock backend と portfolio API に通す。

Spec refs: `mizar-test` staged model、`portfolio.md`「Task-20 Corpus And Mock-Backend
Coverage」、architecture 10、architecture 15、architecture 19、internal 04。

Status: metadata-only corpus と crate-local mock-backend boundary 内で完了。
`tests/property/atp_mock_backend_integration_001.*` は inert な `advanced_semantics`
corpus anchor を記録し、`tests/coverage/spec_trace.toml` は anchor を task-20 requirement に
link し、`tests/mock_backend_corpus.rs` は formula/substitution、counterexample、unknown/open
case を既存の mock backend classification と portfolio collection に通す。active `.miz`
advanced-semantics runner、source-derived ATP extraction、real-backend candidate extraction、
kernel checking、proof policy、artifact witness publication、cache promotion は external/deferred
のまま残る。

Affected design and corpus files: この crate plan と日本語 companion、`portfolio.md` と日本語
companion、両 TODO、`tests/coverage/spec_trace.toml`、`tests/property/` 配下の task-20
metadata-only corpus fixture。

Affected source files: `crates/mizar-atp/tests/mock_backend_corpus.rs` と
`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: fixture が metadata-only で `stage = "advanced_semantics"` を使い、
traceability が mizar-atp task-20 requirement に戻り、integration test が formula/substitution、
counterexample、unknown/open case を mock backend classification と portfolio collection に通し、
candidate handoff が deterministic のままであり、review-only agent が blocking/high finding なしを
報告し、Rust と metadata verification が通り、この task が separate commit されること。

## Task 21: determinism suite

Purpose: identical public `VcIr` input から開始する cross-module regression を追加し、
problem construction、concrete encoding、mock backend classification、portfolio candidate
ordering まで candidate-production pipeline が deterministic であることを確認する。

Spec refs: architecture 20「Incremental and Parallel Properties」、`portfolio.md`
「task-21 determinism suite」、architecture 10、architecture 15、architecture 19、
internal 04。

Status: mock-backend candidate-production boundary 内で完了。`tests/determinism_suite.rs`
は public `mizar-vc` type で同一の `VcSet` / `VcIr` fixture を構築し、public VC kernel
handoff を作り、実装済み concrete encoder profile ごとに同じ obligation を 2 回 translate し、
byte-identical な TPTP FOF と SMT-LIB uninterpreted output および side metadata を確認し、
mock backend completion order を反転しても portfolio candidate id、candidate hash、
evidence-set hash、ordering が安定することを検査する。

Affected design files: この crate plan と日本語 companion、`portfolio.md` と日本語 companion、
両 TODO。

Affected source files: `crates/mizar-atp/tests/determinism_suite.rs` と
`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: identical public `VcIr` input が両 encoder profile で同一の
`AtpProblem` id / debug rendering を生み、実装済み両 concrete encoder が stable text /
label / binding を emit し、mock backend formula/substitution candidate が reversed
completion order 下でも deterministic order で collect され、review-only agent が
blocking/high finding なしを報告し、Rust verification が通り、この task が separate commit
されること。

Remaining external gaps: real backend output extraction、kernel checking、proof-policy
winner selection、artifact witness publication、proof-cache promotion、active `.miz`
advanced-semantics execution は task 21 の外に残る。

## Task 22: public enum forward-compatibility policy

Purpose: source/spec correspondence audit の前に、downstream crate 向けの public enum
compatibility contract を明示する。

Spec refs: 全 module spec、この crate plan、両 TODO、architecture 19 fail-closed
semantics。

Status: public API compatibility boundary 内で完了。`problem`、`translator`、
`property_encoding`、`tptp_encoder`、`smtlib_encoder`、`backend`、`portfolio`
のすべての public enum は downstream 向け `#[non_exhaustive]` であり、所有する
英語/日本語 module spec は source と一致する exact inventory を記録し、lint-policy
test は source attribute と EN/JA inventory の両方を検査する。

Affected design files: この crate plan と英語 companion、`doc/design/mizar-atp/en/`
と `doc/design/mizar-atp/ja/` 配下の全 module spec、両 TODO。

Affected source files: `crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: public ATP module のすべての public enum が downstream 向け
`#[non_exhaustive]` attribute を持ち、各所有 EN/JA spec がその module の exact
inventory を公開し、lint-policy guard が source と両 spec を比較し、behavior-sensitive
match が意図的 fallback を document していない限り explicit/fail-closed のままであり、
review-only agent が blocking/high finding なしを報告し、verification が通り、この task
が separate commit されること。

Remaining external gaps: この task は trusted acceptance、kernel checking、real backend
extraction、proof-policy winner selection、artifact witness publication、proof-cache
promotion、downstream compatibility shim を追加しない。

## Task 23: source/spec correspondence audit

Purpose: bilingual sync audit の前に、現在の public API family と module が約束する挙動を
source、test、または分類済み follow-up へ trace する。

Spec refs: 全 module spec、`source_spec_audit.md`、この crate plan、両 TODO、autonomous
crate development hard gates。

Status: audit-only task として完了。paired English/Japanese `source_spec_audit.md`
document は `src/lib.rs` が export する public module、top-level public item、public entry
function、cross-module test evidence、残る ATP-AUDIT follow-up register を inventory する。
lint-policy guard は両 audit document が現在の source module list、public item list、public
entry function、gap id/class と同期していることを検査する。

Affected design files: この crate plan と英語 companion、paired `source_spec_audit.md`、
両 TODO。

Affected source files: `crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: audit が未分類の `source_drift`、`design_drift`、
`source_undocumented_behavior`、`test_expectation_drift`、`boundary_violation`、
`repo_metadata_conflict` を記録せず、残る unavailable behavior がすべて
`external_dependency_gap` または `deferred` として分類され、review-only agent が
blocking/high finding なしを報告し、verification が通り、この task が separate commit されること。

Remaining external gaps: real backend output extraction、active `.miz` advanced-semantics
execution、producer/proof/cache integration、real artifact witness publication、
typed/native encoder extension、proof-policy early-stop は `source_spec_audit.md` に分類済みとして残る。

## Task 24: bilingual documentation sync audit

Purpose: task-25 portfolio completion-order gate の前に、`doc/design/mizar-atp/en/`
配下の現在の英語正本 design document が、意味的に同期した日本語 companion を持つことを
確認する。

Spec refs: repository documentation policy、`bilingual_sync_audit.md`、この crate plan、
両 TODO、すべての paired module/audit docs。

Status: audit-only task として完了。paired English/Japanese `bilingual_sync_audit.md`
document は現在の mizar-atp design doc pair をすべて inventory し、bilingual drift や
`repo_metadata_conflict` がないことを分類し、external/deferred gap register を保持し、
behavior/API/backend/kernel/proof/cache/artifact integration が変更されていないことを記録する。
`lint_policy.rs` は EN/JA Markdown filename の完全一致と bilingual audit inventory /
trust-boundary marker を検査する。

Affected design files: この crate plan と英語 companion、paired `bilingual_sync_audit.md`、
両 TODO。

Affected source files: `crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: 現在の EN/JA design filename がすべて一致し、paired bilingual audit が
すべての pair を inventory し、drift がないことを記録し、review-only agent が
blocking/high finding なしを報告し、verification が通り、この task が separate commit されること。

Remaining external gaps: task 23 から不変。real backend extraction、active source-derived ATP
extraction、proof/cache integration、artifact witness publication、typed/native encoder
extension、proof-policy early-stop はこの audit では実装しない。

## Task 25: portfolio completion-order independence gate

Purpose: 現在の workspace boundary に照らして task-25 portfolio ordering /
early-stop regression gate を再評価し、`mizar-atp` 内に proof policy を発明せず、
既存の completion-order-independent no-early-stop handoff を保持する。

Spec refs: architecture 10 "Portfolio Execution"、architecture 14 parallel
verification scheduling、architecture 22 incremental verification contract、
`portfolio.md`、`source_spec_audit.md`、`bilingual_sync_audit.md`、この crate
plan、両 TODO。

Status: deferred/external_dependency_gap gate として完了。要求された
release-policy test は、後から返った kernel-verifiable candidate が先に返った
externally attested result を置き換えるか、policy/evidence-strength key で tie を
解くか、pending candidate が selected class を覆せないため early stop してよいかを
判断する downstream proof-policy owner を必要とする。`mizar-proof` は現在存在するが、
その policy API を消費するのは別の ATP/proof integration task である。それらの判断を
ここで local に実装すると boundary violation になる。既存の task-18/task-21 test は、
early stop なしで shuffled backend completion order 下の crate-local deterministic
candidate handoff を既に cover している。

Affected design files: この crate plan と英語 companion、paired `portfolio.md`、
paired `source_spec_audit.md`、paired `bilingual_sync_audit.md`、両 TODO。

Affected source files: `crates/mizar-atp/tests/lint_policy.rs` のみ。production Rust
source と public API は変更しない。

Completion condition: task-25 dependency gap が明示的に分類され、paired documentation
が同期し、lint policy が deferral と placeholder policy behavior の不在を guard し、
review-only agent が blocking/high finding なしを報告し、verification が通り、この task が
separate commit されること。

Remaining external gaps: proof-policy finality consumption、artifact-facing winner
selection integration、proof-cache promotion、real artifact witness publication、将来の
early-stop integration は、専用 integration task が正式な `mizar-proof` API を消費するまで
downstream owner に残る。

## Task 26: architecture-22 follow-up audit

Purpose: task-25 portfolio ordering / early-stop contract について、source/spec
correspondence audit と bilingual documentation sync audit を再実行し、残る
policy-boundary または completion-order follow-up work を記録する。

Spec refs: architecture 22 "Parallel Compatibility"、すべての mizar-atp module
spec、`source_spec_audit.md`、`bilingual_sync_audit.md`、この crate plan、両 TODO、
repository documentation policy。

Status: audit-only task として完了。audit は task-25 deferral を Architecture 22 に
明示的に接続した。raw backend completion order と runtime duration は semantic proof
identity にならず、deterministic winner selection は active proof policy に属する。
source/spec drift、bilingual drift、`repo_metadata_conflict`、新しい follow-up gap は
見つからなかった。

Affected design files: この crate plan と英語 companion、paired
`source_spec_audit.md`、paired `bilingual_sync_audit.md`、両 TODO。

Affected source files: `crates/mizar-atp/tests/lint_policy.rs` のみ。production Rust
source と public API は変更しない。

Completion condition: source/spec audit と bilingual audit が task-25 Architecture-22
result を記録し、lint policy が task-26 audit marker と不変の ATP-AUDIT gap set を
guard し、review-only agent が blocking/high finding なしを報告し、verification が通り、
この task が separate commit されること。

Remaining external gaps: ATP-AUDIT-G005 から不変。runtime completion-order winner test や
early-stop integration を実装できるのは、`mizar-proof` が release-policy finality、
winner selection、tie-breaking、candidate displacement を所有した後だけである。

## Task 27: module-boundary refactor gate

Purpose: production behavior や public API を変更せず、巨大な inline unit-test suite を
private module child へ移すことで、現在の `mizar-atp` source layout の review
bottleneck を減らす。

Spec refs: internal 07 crate/module layout、既存のすべての mizar-atp module spec、
[module_boundary_audit.md](./module_boundary_audit.md)、`source_spec_audit.md`、
`bilingual_sync_audit.md`、この crate plan、両 TODO。

Status: behavior-preserving private test module split として完了。監査では、新しい
public module boundary、helper extraction を要求する mixed production responsibility、
source/spec behavior drift は見つからなかった。Split は既存 unit test だけを
`src/<module>/tests.rs` へ移す。各 production source file は private `mod tests;`
child module declaration を持ち、既存 module spec と対応し続ける。

Affected design files: この crate plan と英語 companion、paired
`module_boundary_audit.md`、paired `source_spec_audit.md`、paired
`bilingual_sync_audit.md`、両 TODO。

Affected source/test files: `crates/mizar-atp/src/backend.rs`、
`crates/mizar-atp/src/backend/tests.rs`、
`crates/mizar-atp/src/portfolio.rs`、
`crates/mizar-atp/src/portfolio/tests.rs`、
`crates/mizar-atp/src/problem.rs`、
`crates/mizar-atp/src/problem/tests.rs`、
`crates/mizar-atp/src/property_encoding.rs`、
`crates/mizar-atp/src/property_encoding/tests.rs`、
`crates/mizar-atp/src/smtlib_encoder.rs`、
`crates/mizar-atp/src/smtlib_encoder/tests.rs`、
`crates/mizar-atp/src/tptp_encoder.rs`、
`crates/mizar-atp/src/tptp_encoder/tests.rs`、
`crates/mizar-atp/src/translator.rs`、
`crates/mizar-atp/src/translator/tests.rs`、
`crates/mizar-atp/tests/lint_policy.rs`。

Completion condition: task-27 audit が private test module split を記録し、source/spec
audit と bilingual audit が移動した test suite について再実行され、lint policy が新しい
source-file set と private test module に新しい public API がないことを guard し、
review-only agent が blocking/high finding なしを報告し、verification が通り、この
task が separate commit されること。

Remaining follow-ups: 新しい ATP-AUDIT gap はない。Real backend output extraction、
source-derived corpus execution、downstream proof/cache/artifact integration、
typed/native encoder extension、proof-policy finality は不変の external/deferred
follow-up に残る。Production helper extraction は、paired module spec が具体的な
helper boundary を定義するまで deferred のままである。

## Task 28: crate exit report and quality review

Purpose: crate exit report、quality score、task ledger、残る deferred/external gap、
verification、next-phase handoff を記録し、autonomous crate-development hard gate を
完了する。

Spec refs: [autonomous crate development](../../autonomous_crate_development.md)、
この crate plan、両 TODO、`crate_exit_report.md`、repository documentation policy。

Status: docs/lint closeout task として完了。Paired `crate_exit_report.md` は現在の
candidate-evidence producer milestone について status complete、quality score
94/100、score cap なし、すべての hard gate passed、残る `ATP-CLOSEOUT-*` gap を記録する。
Report はさらに、古い `mizar-proof` placeholder metadata が proof crate milestone 後に
補正されたことを記録する。`mizar-cache` は現在存在し cache validation を所有するが、
この ATP milestone は proof/cache API をまだ消費しない。`mizar-proof` は正式だがこの
ATP milestone からは消費しないため、未実装連携は local proof-policy placeholder ではなく
external_dependency_gap / deferred に残る。

Affected design files: この crate plan と英語 companion、paired
`crate_exit_report.md`、paired `bilingual_sync_audit.md`、両 TODO。

Affected source/test files: `crates/mizar-atp/tests/lint_policy.rs` のみ。production Rust
source と public API は変更しない。

Completion condition: paired exit report が存在し、bilingual audit が新しい document
pair を inventory し、lint policy が closeout marker と task-28 status を guard し、
read-only quality review が 90/100 以上の valid score を付け、required verification が
通り、この task が separate commit されること。

Remaining follow-ups: `crate_exit_report.md` から不変。Real backend output extraction、
active source-derived ATP execution、proof/cache/artifact integration、typed/native encoder
extension、proof-policy finality は external/deferred に残る。

## Task 29: post-audit kernel contract への candidate evidence 適合

Purpose: VC と kernel の owner task 到着後に、source-derived extraction、
kernel checking、proof policy、real backend output parsing を実装せず、ATP
candidate-evidence producer boundary を post-audit kernel F1/F2/F6 契約に
整合させる。

Spec refs: `translator.md`、`backend.md`、architecture 15 post-audit kernel
certificate contract、internal 04、`mizar-kernel` `soundness_argument.md`
F1/F2/F6、`mizar-vc` task 29、`mizar-kernel` task 33。

Status: crate-owned conformance として完了。既存 translator は VC kernel
handoff の final-goal polarity に `AssertFalseForRefutation` を要求し、
非 import formula source binding を VC handoff source identity と照合し、
imported-fact provenance に kernel-validated statement projection との一致を
要求する。public VC handoff builder は `AssertTrueForConsistency` を要求する
proof-obligation を ATP が handoff を受け取る前に reject するため、task 29 は
その path を upstream executable regression として記録し、ATP 側の polarity check は
source audit evidence として保持する。別の focused Rust regression は
`translate_problem` 内の producer-relabeled local-hypothesis rejection を cover する。

Affected design files: この crate plan と日本語 companion、paired `todo.md`、
paired `translator.md`、paired `backend.md`、paired `source_spec_audit.md`、
paired `bilingual_sync_audit.md`、paired `crate_exit_report.md`、top-level
`doc/design/todo.md`。

Affected source/test files: `crates/mizar-atp/src/translator/tests.rs` のみ。
production Rust source、public API、backend route、kernel call、proof policy、
witness/cache output、artifact schema は変更しない。

Completion condition: task-29 regression は、public invalid consistency-polarity
handoff path が ATP translation の前に reject されることと、relabel された
local hypothesis が `AtpProblem` / candidate construction に到達しないことを示す。
source/spec audit は ATP 側 private-handoff polarity guard を記録する。既存
imported-projection test は F6 consumer guard のままである。EN/JA docs は
crate-owned conformance と deferred joint kernel-corpus execution を分けて記録する。
review-only agent が findings なしを報告し、focused `mizar-atp`/`mizar-kernel`
verification が task commit 前に成功する。

Remaining follow-ups:
`fail_certificate_sat_goal_polarity_mismatch_001` と
`fail_certificate_symbols_unverifiable_local_hypothesis_001` を
source-derived ATP-built candidate 経由で joint 実行する部分は、ATP-G-015、
ATP-G-016、ATP-G-020、ATP-G-021、ATP-AUDIT-G001、ATP-AUDIT-G003、
ATP-CLOSEOUT-G001/G005 の下で `external_dependency_gap` / `deferred` のままである。
real source-derived ATP extraction route と backend-output candidate extractor が
存在した後にだけ再開する。

## Exit Criteria

Hard gates:

- paired English/Japanese module spec のない semantic ATP source module が存在しない。
- crate-owned behavior は実装前に module spec、`doc/spec`、test、traceability metadata
  のいずれかに記録されている。
- generated evidence は `mizar-kernel` が検査するまで untrusted のままである。
- backend proof method、log、resolution trace、instantiated formula、SAT problem は
  trusted acceptance material にならない。
- `mizar-atp` は proof policy、winner selection、proof witness publication、cache
  promotion を所有しない。
- 完了した各 task が独立した commit を持つ。
- 残る gap は `deferred`、`external_dependency_gap`、または human-owned に分類されている。

Verification commands:

- Rust source task ごと:
  - `cargo test -p mizar-atp`
  - `cargo clippy -p mizar-atp --all-targets -- -D warnings`
  - `cargo fmt --check`
- VC/kernel boundary に触れる task:
  - `cargo test -p mizar-vc`
  - `cargo test -p mizar-kernel`
- 広い crate completion:
  - `cargo clippy --all-targets --all-features -- -D warnings`
  - `cargo test`

Review expectations:

- implementation specification と documentation の review-only agent。
- test sufficiency の review-only agent。
- full implementation の review-only agent。
- source/documentation consistency の review-only agent。
