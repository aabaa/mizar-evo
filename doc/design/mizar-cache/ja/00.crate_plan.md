# Crate Plan: mizar-cache

> 正本は英語です。英語版:
> [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## 責務

所有する挙動:

- 内部 build `CacheKey` の正準構築と domain-separated key hash。
- dependency slice、dependency fingerprint、compatibility diff、保守的な
  rebuild-trigger evaluation。
- `cache_dir` 配下の内部 cache record と content-addressed blob storage。
- `mizar-proof` が export する metadata 上の fail-closed proof-reuse validation。
- 受理済み registration / cluster contribution だけを扱う `cluster-db/`
  contribution index、origin tracking、import-scoped view。

範囲外:

- proof authority、proof search、kernel acceptance、ATP backend execution、
  deterministic winner selection、proof status projection、trusted
  `used_axioms` projection、artifact manifest publication、scheduler ownership。
- owner-scoped な `mizar-build` cache-aware scheduling、既存の `mizar-ir`
  cache-adapter boundary、artifact committed-publication token との
  placeholder integration。
- cache record、externally attested evidence、backend diagnostic、backend log、
  timing metadata、arrival/write order を kernel-verified status や trusted
  proof fact へ昇格させること。

## 仕様項目

| Spec ref | 要件 | 状態 |
|---|---|---|
| [spec 23.5](../../../spec/ja/23.package_management_and_build_system.md#235-検証-toolのアーティファクトとビルド出力) | `cache_dir`、artifact output、`cluster-db/` は build-system surface であり、公開 artifact は manifest-first output である。 | cache/store/cluster task で扱う。下流 publication は external。 |
| [architecture 11](../../architecture/ja/11.artifact_and_incremental_build.md) | cache entry は内部最適化データであり、削除しても package semantics は変わらない。 | 全 task の milestone invariant。 |
| [architecture 17](../../architecture/ja/17.cluster_trace_format.md) | 受理済み registration と明示 trace だけが importer-visible cluster/reduction view へ入る。 | task 12-14 で扱う。 |
| [architecture 18](../../architecture/ja/18.dependency_fingerprint.md) | dependency slice は complete、canonical、かつ incomplete/unsupported では fail closed でなければならない。 | task 4-6 と task 20 で扱う。 |
| [architecture 22](../../architecture/ja/22.incremental_verification_contract.md) | cache reuse と proof reuse は slice、schema/toolchain/policy、VC/context fingerprint、witness/discharge hash の一致を要する。 | task 2-11 と task 20 で扱う。 |
| [internal 02](../../internal/ja/02.artifact_store_cache_key_and_manifest.md) | `CacheKeyBuilder` は key だけを作り、reuse は `CacheStore` compatibility と proof-reuse validation が決める。 | task 2、3、7-11 で扱う。 |
| [internal 06](../../internal/ja/06.ir_storage_and_snapshot_handles.md) | IR cache adapter は cache validation 後にだけ handle を復元し、cache hit を proof authority としない。 | `mizar-ir` は現在 adapter validation boundary を所有する。end-to-end build/driver rehydration は external integration gap のままである。 |
| [internal 07](../../internal/ja/07.crate_module_layout.md) | `mizar-cache` は cache key、dependency slice、proof reuse validation、cluster-db storage を所有する。 | この plan と task 1 で crate boundary を確立する。 |
| [mizar-proof status](../../mizar-proof/ja/status.md) | proof-reuse metadata は validation predicate であり、proof authority ではない。 | task 11 で policy/status ownership を移さずに消費する。 |
| [mizar-proof selection](../../mizar-proof/ja/selection.md) | deterministic winner selection と selected reuse metadata は proof-owned surface である。 | task 11 で消費し、再実装しない。 |
| [mizar-proof witness store](../../mizar-proof/ja/witness_store.md) | witness hash と committed ref は reuse validation に参加するが、信頼を与えない。 | task 11 で消費する。artifact committed-publication token は external のまま。 |

## 関連テスト

| Test path | 意図 | Spec refs |
|---|---|---|
| `crates/mizar-cache/tests/lint_policy.rs` | workspace lint opt-in、manifest metadata、dependency boundary、public module exposure、crate tree shape を guard する。 | task 1、internal 07。 |
| `crates/mizar-cache/src/cache_key/tests.rs` unit tests | deterministic canonical key construction、vector sorting、domain separation、input sensitivity を guard する。 | task 2-3、architecture 22。 |
| `crates/mizar-cache/src/dependency_fingerprint/tests.rs` unit tests | complete footprint、uncacheable marker、slice fingerprint、stable input、rebuild trigger を guard する。 | task 4-6、architecture 18。 |
| `crates/mizar-cache/src/cache_store/tests.rs` unit tests | record/blob round trip、compatibility miss、corruption detection、uncacheable record、concurrent blob convergence を guard する。 | task 7-9。 |
| `crates/mizar-cache/src/proof_reuse/tests.rs` unit tests | 全 proof-reuse field の fail-closed validation と、external/cache evidence が昇格しないことを guard する。 | task 10-11 と task 20。 |
| `crates/mizar-cache/src/cluster_db/tests.rs` unit tests | accepted-only contribution、stale-origin removal、import-scoped view、exact invalidation を guard する。 | task 12-14。 |
| `crates/mizar-cache/tests/determinism_suite.rs` | crate-owned cache surface の deterministic key/record と代表的な record/blob deletability を guard する。 | task 16 と task 20。 |
| `crates/mizar-cache/tests/incremental_contract.rs` | cache key、dependency footprint、cache store lookup/deletion、proof-reuse metadata validation、external-evidence non-promotion、cache non-authority を横断する crate-owned architecture-22 contract を guard する。 | task 20 と architecture 22。 |

予定する test-first 追加:

- `.miz` test は不要。この crate は上流 metadata 上の内部 cache optimization と
  validation predicate を所有し、language syntax や proof semantics を変更しない。
  coverage は crate-local Rust test と audit document で行う。

## design と source の棚卸し

Design files:

- `doc/design/mizar-cache/en/todo.md`
- `doc/design/mizar-cache/ja/todo.md`
- この crate plan と日本語 companion
- task 2、4、7、10、12 が作る module spec
- task 15 が作る `integration_readiness.md`
- task 18-23 が作る source/spec、bilingual、architecture-22、module-boundary、
  closeout report

Source files:

- task 1 は `crates/mizar-cache/Cargo.toml`、`src/lib.rs`、
  `tests/lint_policy.rs` を追加する。
- task 3 は `src/cache_key.rs` を追加する。
- task 5 は `src/dependency_fingerprint.rs` を追加する。
- task 8 は `src/cache_store.rs` を追加する。
- task 11 は `src/proof_reuse.rs` を追加する。
- task 13 は `src/cluster_db.rs` を追加する。
- task 16 は `tests/determinism_suite.rs` を追加する。
- task 20 は `tests/incremental_contract.rs` を追加する。
- task 22 は public API や production behavior を変えずに inline unit test を
  private `src/<module>/tests.rs` submodule へ分割する。
- 後続 refactor task は、独自の source/spec audit を伴い、public API を変えずに
  追加 private helper を分割してよい。

Observed behavior:

- task 1 の前には `crates/mizar-cache` は workspace に存在しない。
- `doc/design/mizar-cache/{en,ja}/todo.md` は formal task sequence をすでに
  定義しており、formal scaffold を開始してよい。
- `mizar-build` は現在 cache-aware scheduling seam を公開しているが、driver-selected
  execution を通じた end-to-end cache lookup/use は integration work として残る。
- `mizar-ir` は現在存在し cache adapter boundary を所有するが、build/driver execution を
  通じた end-to-end cache-record rehydration は `external_dependency_gap` として残る。
- `mizar-proof` は proof-reuse metadata を export するが、cache はそれを
  validation input としてのみ消費する。

## 既知の gap と drift

| ID | Class | 根拠 | 対応 |
|---|---|---|---|
| CACHE-G-001 | 解決済み `source_drift` | task 1 前には design task が `mizar-cache` を要求していたが、crate は存在しなかった。 | task 1 scaffold と後続 milestone implementation で解決済み。 |
| CACHE-G-002 | 解決済み `test_gap` | task 1 前には crate-local lint や behavior test が存在しなかった。 | task 1 lint guard と source task ごとの focused test で解決済み。 |
| CACHE-G-003 | `external_dependency_gap` | `mizar-build` cache-aware scheduling は現在存在するが、cache lookup/use は owner-scoped build/driver integration をまだ必要とする。 | task 15 は `integration_readiness.md` に記録する。`mizar-cache` に placeholder driver や scheduler wiring は追加しない。 |
| CACHE-G-004 | `external_dependency_gap` | `mizar-ir` は現在存在し cache adapter boundary を所有するが、build/driver execution を通じた end-to-end cache-record rehydration は未配線である。 | task 15 は `integration_readiness.md` に記録する。IR handle reconstruction や driver orchestration を `mizar-cache` に移さない。 |
| CACHE-G-005 | `external_dependency_gap` | artifact committed-publication proof token は artifact/proof owner 側に残っている。 | task 15 は `integration_readiness.md` に記録する。witness/publication metadata は validation input としてだけ保持する。 |
| CACHE-I-001 | invariant | cache record を proof authority として受理すると unsound になる。 | 全 module spec と source module は、cache hit が最適化であり trusted acceptance でないことを明記する。この規則に違反する source は `boundary_violation` と分類する。 |

## タスク分解

### Task 0: protocol-required crate plan

Purpose: 実装開始前に crate responsibility、authority reference、gap、task
decomposition、exit gate を確立する。

Spec refs: autonomous crate development protocol、internal 07、architecture 11、
17、18、22。

Tests: documentation と review のみ。

Affected design files: この plan、日本語 companion、task ledger。

Affected source files: なし。

Completion condition: paired plan と ledger が存在し、review が blocking/high
finding なしで終わり、diff check が通り、task が独立して commit されている。

Forbidden behavior: source implementation を行わない。

### Task 1: crate の足場と lint 方針のガード

Purpose: workspace member、minimal crate root、package metadata、dependency
boundary、lint-policy guard を追加する。

Spec refs: internal 02 と internal 07。

Tests: `crates/mizar-cache/tests/lint_policy.rs`。

Affected design files: TODO status と task ledger。

Affected source files: workspace `Cargo.toml`、
`crates/mizar-cache/Cargo.toml`、`crates/mizar-cache/src/lib.rs`、
`crates/mizar-cache/tests/lint_policy.rs`。

Completion condition: `cargo test -p mizar-cache`、
`cargo clippy -p mizar-cache --all-targets -- -D warnings`、`cargo fmt --check`
が通る。さらに AGENTS.md の Rust-source broad verification command
（`cargo clippy --all-targets --all-features -- -D warnings` と `cargo test`）も、
未実行理由を明示しない限り、task を独立 commit する前に通す。

Forbidden behavior: crate boundary 以外の cache behavior を導入しない。

### Task 2: Spec `cache_key.md`

Purpose: canonical cache-key field、sorting、hash framing、schema/policy input、
fail-closed validation input を定義する。

Spec refs: internal 02 と architecture 22。

Tests: documentation review のみ。

Affected design files: 両言語の `cache_key.md`、TODO status、task ledger。

Affected source files: なし。

Completion condition: EN/JA spec が同期し、review 済みである。

Forbidden behavior: source implementation を行わない。

### Task 3: Cache-key builder

Purpose: `CacheKeyBuilder` を identity、hash、schema version、policy からの
pure projection として実装する。

Spec refs: `cache_key.md`。

Tests: key determinism、canonical vector sorting、input sensitivity。

Affected source files: `src/cache_key.rs`、`src/lib.rs`、tests。

Completion condition: source/test/spec が一致し、crate verification が通る。

Forbidden behavior: trust decision、mutable task state、cache lookup、proof
acceptance を導入しない。

### Task 4: Spec `dependency_fingerprint.md`

Purpose: dependency slice、stable/excluded input、completeness、uncacheable
marker、compatibility diff、初期 granularity decision を定義する。

Spec refs: architecture 18。

Tests: documentation review のみ。

Affected design files: 両言語の `dependency_fingerprint.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期し、review 済みである。

Forbidden behavior: source implementation を行わない。

### Task 5: dependency slice と fingerprint の計算

Purpose: artifact interface/implementation input と VC dependency slice 上で、
文書化された粒度の保守的 fingerprint を計算する。
task 5 は共有 trigger enum shape を追加してよいが、trigger evaluation は task
6 の範囲に残す。

Spec refs: `dependency_fingerprint.md`。

Tests: stable fingerprint、exact dependent-slice flip、complete footprint
requirement、uncacheable marker。

Affected source files: `src/dependency_fingerprint.rs`、`src/lib.rs`、tests。

Completion condition: source/test/spec が一致し、crate verification が通る。

Forbidden behavior: false-negative reuse と producer integration stub を導入しない。

### Task 6: rebuild-trigger evaluation

Purpose: fingerprint delta を cached-phase invalidation へ写す保守的 trigger rule を
実装する。

Spec refs: `dependency_fingerprint.md`。

Tests: source/import/registration/cluster-reduction/policy/toolchain/schema/
proof-body/diagnostic-only/incomplete/unknown/uncacheable/missing-proof-reuse
change fixture と、conservative mode での false negative 不在。

Affected source files: `src/dependency_fingerprint.rs` と tests。

Completion condition: trigger test と crate verification が通る。

Forbidden behavior: semver linter verdict を cache-reuse predicate として使わない。
cluster-db、scheduler、cache-store、proof-authority integration を行わない。

### Task 7: Spec `cache_store.md`

Purpose: `.mizar-cache/` layout、record header、encoding、compatibility check、
uncacheable/incomplete handling、deletability を定義する。

Spec refs: internal 02 と architecture 11。

Tests: documentation review のみ。

Affected design files: 両言語の `cache_store.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期し、review 済みである。

Forbidden behavior: source implementation を行わない。

### Task 8: record store

Purpose: fail-closed header compatibility check を備えた record write/read/validate を
実装する。

Spec refs: `cache_store.md`。

Tests: round trip、incompatible header as miss、corruption detection、unknown
schema/toolchain miss、incomplete footprint miss、uncacheable miss。

Affected source files: `src/cache_store.rs`、`src/lib.rs`、tests。

Completion condition: source/test/spec が一致し、crate verification が通る。

Forbidden behavior: ordinary miss を hard failure にしない。proof authority を導入しない。

### Task 9: blob store

Purpose: hash-verified content-addressed blob と safe concurrent write convergence を
実装する。

Spec refs: `cache_store.md`。

Tests: blob round trip、hash mismatch detection、concurrent writer convergence。

Affected source files: `src/cache_store.rs` と tests。

Completion condition: source/test/spec が一致し、crate verification が通る。

Forbidden behavior: artifact manifest path を通じて publication しない。

### Task 10: Spec `proof_reuse.md`

Purpose: proof-reuse validation input と、record が evidence class を決して昇格しない
規則を定義する。

Spec refs: internal 02、internal 04、architecture 22、mizar-proof specs。

Tests: documentation review のみ。

Affected design files: 両言語の `proof_reuse.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期し、review 済みである。

Forbidden behavior: source implementation を行わない。

### Task 11: proof-reuse validation

Purpose: `mizar-proof` の `StatusReuseMetadata` から導いた proof-reuse metadata
snapshot 上の fail-closed validation を実装する。

Spec refs: `proof_reuse.md`。

Tests: kernel-verified と built-in-discharge evidence の matching reuse、trusted
used-axioms reference hash が export される場合を含め、必要 component の欠落または
mismatch が reuse を止めること、external evidence と cache record が
kernel-verified/trusted `used_axioms` にならないこと、lint guard が downstream stub
surface を拒否すること。

Affected source files: `Cargo.toml`、`Cargo.lock`、`src/proof_reuse.rs`、
`src/lib.rs`、tests。

Completion condition: source/test/spec が一致し、crate verification が通る。

Forbidden behavior: policy ownership、winner selection、status projection、
proof acceptance を導入しない。

### Task 12: Spec `cluster_db.md`

Purpose: cluster-db layout、accepted contribution origin metadata、import-scoped
view、visible-origin invalidation を定義する。

Spec refs: internal 02 と architecture 17。

Tests: documentation review のみ。accepted-only visibility、stale origin
invalidation、import-scoped view key、source implementation がないことを確認する。

Affected design files: 両言語の `cluster_db.md`。

Affected source files: なし。

Completion condition: EN/JA spec が同期し、review 済みである。

Forbidden behavior: source implementation を行わない。

### Task 13: cluster-db writes と origin tracking

Purpose: accepted contribution write、stale-origin removal、per-origin rebuild
summary を実装する。

Spec refs: `cluster_db.md`。

Tests: rename/removal cleanup、affected-origin rebuild、unaccepted contribution rejection。
task 13 は private/local-only accepted contribution、recovered/external/uncacheable
input、incomplete origin metadata、incomplete footprint、missing dependency-interface
hash、missing trace replay hash、proof-backed origin の missing proof witness/discharge
identity、unknown schema/toolchain compatibility、duplicate conflict、cross-module
origin-key collision、deterministic same-index ordering も cover する。

Affected source files: `src/cluster_db.rs`、`src/lib.rs`、tests。

Completion condition: source/test/spec が一致し、crate verification が通る。

Forbidden behavior: unaccepted/recovered/external proof material を visible index
へ入れない。scheduler、`mizar-ir`、artifact publication-token、proof-status、
import-scoped view implementation は導入しない。

### Task 14: import-scoped view と invalidation

Purpose: visible origin set で key 付けした import-scoped view と exact view
invalidation を、accepted aggregate row 上に実装する。

Spec refs: `cluster_db.md`。

Tests: unrelated-change reuse、visible-origin-change invalidation、visible origin への
filtering、missing origin、unsupported schema、missing producer schema metadata、
不明な compatibility、verifier-policy mismatch、mismatched producer compatibility
metadata に対する fail-closed request validation、record/write-order independence、
visible-origin request-order independence、explicit `ResolutionTrace` step を越える
hidden cluster/reduction step inference がないこと。

Affected source files: `src/cluster_db.rs` と tests。

Completion condition: source/test/spec が一致し、crate verification が通る。

Forbidden behavior: cache insertion/write order が view content に影響しない。
scheduler hook、`mizar-ir` adapter、durable な `views/` file、proof status
projection、trace construction を追加しない。

### Task 15: scheduler と IR-adapter integration readiness

Purpose: downstream readiness を評価し、owning seam が存在する場合だけ接続する。

Spec refs: internal 02、internal 06、`mizar-build` task 18、`mizar-ir` task 10。

Tests: downstream seam が ready でない限り docs/review のみ。task 15 では seam が
ready でないことを確認したため、source integration と end-to-end cache hit/miss
fixture は追加しない。

Affected design files: `integration_readiness.md`、TODO、task ledger、deferred gap
register。

Affected source files: seam が存在する場合のみ。

Completion condition: downstream owner が ready なら hit/miss と clean-build
equivalence の test とともに integration を実装する。そうでなければ placeholder
なしで `external_dependency_gap` を記録し、canonical TODO でも implementation
側が deferred であることを明示する。

Forbidden behavior: placeholder `mizar-build`、`mizar-ir`、artifact-token API を
作らない。

2026-07-02 roadmap sync 後の task 15 result: `mizar-build` と `mizar-ir` は現在 owner
seam を公開しているが、driver-selected execution を通じた end-to-end cache lookup/use と
cache-record rehydration はまだ未配線である。artifact committed-publication token は
artifact/proof-owned external gap のままである。integration は
`integration_readiness.md` で deferred として記録する。

### Task 16: determinism と deletability suite

Purpose: 同一入力が同一 key/record を生み、代表的な crate-owned record/blob
deletion は deterministic repopulation までの lookup availability だけを
変えることを property 風に覆う。

Spec refs: architecture 20 と architecture 22。

Tests: `tests/determinism_suite.rs`。

Affected source files: minor helper が不要なら test suite のみ。

Completion condition: determinism test と crate verification が通る。

Forbidden behavior: scheduler-level clean/incremental equivalence stub を作らない。

Task 16 result: `tests/determinism_suite.rs` は canonical cache-key vector
ordering、inline/blob record deletion と repopulation、proof-reuse diagnostic
ordering、policy-permitted external evidence を reusable trusted proof status
へ昇格しないことを覆う。task 20 は crate-owned clean/incremental cache
contract を cover する。scheduler-level の clean/incremental equivalence は
external integration gap として残る。

### Task 17: public enum forward-compatibility policy

Purpose: 全 public enum を `#[non_exhaustive]` として文書化・guard する。

Spec refs: module specs。

Tests: lint-policy public enum guard。

Affected design files: 必要に応じて module spec と TODO。

Affected source files: public enum declaration と lint policy。

Completion condition: guard と crate verification が通る。

Task 17 result: 現在の public enum はすべて既に `#[non_exhaustive]` であった。
task 17 はその policy を paired module spec に文書化し、source-declared public
enum を発見し、attribute を check し、各 enum が EN/JA policy table に現れることを
要求する lint guard を追加する。`mizar-cache` が所有する exhaustive public enum
exception はない。

Forbidden behavior: forward-compat annotation と documentation 以外の API behavior
change を行わない。

### Task 18: source/spec correspondence audit

Purpose: public API と約束された挙動を source/test へ trace し、gap を記録する。

Spec refs: 全 module spec と TODO。

Tests: documentation review と既存 crate test。

Affected design files: 両言語の `source_spec_audit.md`。

Affected source files: audit が drift を見つけた場合のみ。

Completion condition: unresolved blocking/high finding が残っていない。

Forbidden behavior: unrelated refactor をしない。

Task 18 result: paired `source_spec_audit.md` document が public module
export、public API group、約束された behavior、test、trust boundary、既存の
deferred / external-dependency gap を trace する。この audit は未分類
source/spec drift、新規 gap ID、behavior change を記録しない。唯一の Rust 変更は、
audit が現在の public module と gap register に結びついた paired document である
ことを保つ lint-policy guard である。

### Task 19: bilingual documentation sync audit

Purpose: すべての mizar-cache English canonical design document と Japanese companion
を同期する。

Spec refs: repository documentation policy。

Tests: documentation review と diff check。

Affected design files: 両言語の `bilingual_sync_audit.md` と、同期が必要な paired docs。

Affected source files: なし。

Completion condition: unresolved sync placeholder が残っていない。

Forbidden behavior: source implementation を行わない。

Task 19 result: paired `bilingual_sync_audit.md` document は、現在の各 English
design document に semantic content が同期した Japanese companion があることを
記録する。missing companion、未解決 sync placeholder、meaning-changing
bilingual drift、`repo_metadata_conflict` は見つからない。task 19 は
documentation のみを変更する。

### Task 20: incremental verification fail-closed cache contract

Purpose: cache key、dependency footprint、store compatibility、proof reuse 全体の
cross-module cache contract を test する。

Spec refs: architecture 11、18、22 と module specs。

Tests: missing/mismatched field が miss を強制すること、uncacheable/unknown
schema/toolchain miss、external evidence が昇格しないこと、crate-owned API で
cache timing/order が semantic でないこと。

Affected source files: 必要に応じて crate modules と tests。

Completion condition: contract test と、利用可能な adjacent producer test が通る。

Forbidden behavior: downstream integration placeholder を追加しない。

Task 20 result: `tests/incremental_contract.rs` は cache-key construction、
dependency-footprint projection、cache-store compatibility、proof-reuse
validation、deletion/non-semantic timing boundary、external-evidence
non-promotion をまたぐ architecture-22 fail-closed cache contract を
exercise する。crate-owned API を越える cross-crate clean/incremental
equivalence は scheduler と artifact publication integration に対する
`external_dependency_gap` のまま。

### Task 21: architecture-22 follow-up audit

Purpose: task-20 contract について source/spec と bilingual audit を再実行し、残る
fail-closed / trust-boundary gap を記録する。

Spec refs: 全 module spec と architecture 22。

Tests: documentation review と既存 crate test。

Affected design files: 両言語の `architecture_22_audit.md`。

Affected source files: audit が drift を見つけた場合のみ。

Completion condition: unresolved blocking/high fail-closed finding が残っていない。

Forbidden behavior: audit docs に新 behavior を隠さない。

Task 21 result: paired `architecture_22_audit.md` は task 20 後に
architecture-22 source/spec と bilingual check を再実行したことを記録する。
crate-owned cache contract について blocking または high の fail-closed /
trust-boundary finding は残っていない。残る scheduler、dependency-fingerprint
consumer、`mizar-ir`、artifact publication、accepted-contribution producer、
proof/cache/artifact consumer、fine-grained producer-slice、durable
cluster-db/view work は既存の external/deferred gap のままである。

### Task 22: module-boundary refactor gate

Purpose: source layout を監査し、具体的な review bottleneck がある場合だけ
behavior-preserving private split を行う。

Spec refs: internal 07 と全 module spec。

Tests: 既存 crate test と lint guard。

Affected design files: 両言語の `module_boundary_audit.md`。

Affected source files: 必要な場合だけ move-only private module。

Completion condition: source layout が正当化され、移動後も source/spec/bilingual
audit が一致する。

Forbidden behavior: behavior cleanup や API exposure を move と混ぜない。

Task 22 result: inline unit test を 5 つの public module の private
`src/<module>/tests.rs` submodule へ分割した。Runtime implementation code、
public API、deterministic output、artifact-facing schema、proof authority
boundary、downstream integration status は変更しない。paired
`module_boundary_audit.md` が layout decision を記録する。

### Task 23: crate exit report と quality review

Purpose: 全 hard gate pass 後、read-only quality review が 90/100 以上を付けて
crate を closeout する。

Spec refs: autonomous crate development protocol。

Tests: final broad verification。

Affected design files: 両言語の `crate_exit_report.md`。

Affected source files: final review が drift を見つけた場合以外なし。

Completion condition: hard gate が通り、有効な quality score が 90 以上で、
full verification が通り、closeout commit が作成されている。

Forbidden behavior: review が要求する fix 以外の新 feature implementation を
closeout に入れない。

## Exit Criteria

Hard gates:

- unresolved blocking/high specification inconsistency がない。
- crate scope 内の source behavior が undocumented / untested のまま残っていない。
- cache record、externally attested evidence、diagnostic、log、timing、arrival/write
  order が proof authority にならない。
- reusable record ごとに complete dependency footprint または明示的な
  uncacheable/miss behavior がある。
- unknown schema/toolchain/policy compatibility は fail closed する。
- English/Japanese docs と source が同期している。
- downstream gap は stub ではなく分類されている。
- required verification command が通るか、未実行理由が明示されている。

Verification commands:

```sh
cargo test -p mizar-cache
cargo clippy -p mizar-cache --all-targets -- -D warnings
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Review expectations:

- 各 task は AGENTS.md の review phases を使う。
- read-only crate quality review は有効な 90/100 以上の score を付ける。
- task commit は 1 task 1 commit とし、明示 staging だけを使う。
