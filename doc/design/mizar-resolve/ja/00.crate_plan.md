# crate plan: mizar-resolve

> 正本は英語です。英語版: [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## 責務

所有する挙動:

- `SurfaceAst` と phase 0 の module-index 入力を消費し、`ResolvedAst` と
  `SymbolEnv` を生成する。
- module path、import、export、namespace、label、安定した symbol identity を
  解決する。
- type checking を行わずに declaration signature と依存側に見える module
  summary を収集する。
- 未解決または曖昧な参照を明示的に表現し、recoverability を保つ。
- output ordering、id、debug rendering、diagnostics を決定的に保つ。

対象外:

- syntax ownership、parser recovery、AST vocabulary の変更。
- type inference、overload winner selection、cluster firing、coercion insertion。
- proof validity、VC generation、ATP interaction、kernel acceptance、artifact emission。
- `mizar-build` と `mizar-session` が所有する package manifest parsing、
  lockfile resolution、source loading、module discovery。
- driver lifecycle、salsa/query integration、phase registry ownership。

## 開始可能性

`mizar-resolve` は [todo.md](./todo.md) の task 1〜7 から開始できる。
`mizar-build` wave A は完了済みであるため、resolver 側の module-index seam は
[module_index.md](../../mizar-build/ja/module_index.md) の build-side provider を
相手に仕様化できる。parser/syntax surface は `mizar-parser` task 45 まで最新であり、
task 46 は deferred なので resolver foundation をブロックしない。

完全な user-facing resolver diagnostics は、[architecture 03](../../architecture/ja/03.module_and_symbol_resolution.md)、
[spec chapter 22](../../../spec/ja/22.error_handling_and_diagnostics.md)、および
`mizar-diagnostics` に残る name/import/label diagnostic adoption gap が解決
または明示的に deferred されるまでは開始しない。共有 diagnostic registry は
`Resolution` family を reserve しているが、resolver-owned な具体的 semantic
name、numeric descriptor、migration behavior、consumer projection は後続の
integration work に残る。

## 仕様項目

| spec ref | 要件 | 状態 |
|---|---|---|
| `spec.en.11.symbol_management.scope_visibility` | semantic lookup order は local binding、current-module declaration、import、re-export、built-in prelude candidate を扱う。 | R-014 は preliminary projection 上で current-module declaration-point filtering、imported public visibility、current-module shadowing、overload placeholder、builtin fallback をカバーする。R-016 は lexical `LocalTermScope` により dotted chain の local term shadowing をカバーする。full source-walk integration は後続 paired work に残る。 |
| `spec.en.11.symbol_management.import_conflicts` | 同じ spelling の import conflict は qualification または aliasing で解決されない限り semantic error。 | import/name task から適用。parser/lexical test は syntax と active-lexicon setup のみをカバーする。 |
| `spec.en.11.symbol_management.signatures` | signature collection は type checking なしで declaration、visibility、alias、overload family、public/private interface projection を登録する。 | R-019 `symbols.md` が declaration-pass contract を仕様化した。R-020 は explicit projection 上の opaque collection を実装し、R-021 は表現済み source role の parser-backed kind ごとの抽出と lexical-summary seed を実装する。R-023 は初期 parser-backed symbol 経路に active declaration-symbol corpus coverage を追加した。 |
| `spec.en.20.algorithm_and_verification.signatures` | public algorithm は signature と contract を公開し、body は importer から private のままにする。 | R-019 は algorithm signature shell を含め、body lowering、VC generation、termination decision を resolver scope 外に保つ。 |
| `spec.en.12.modules.import_prelude` | import は file-scoped prelude declaration。alias、branch import、relative path、cycle rejection は resolver-visible。 | syntax coverage は存在する。semantic resolution coverage は計画中。 |
| `spec.en.12.modules.exports_visibility` | export statement と public/private visibility が module interface を定義する。 | parser coverage は存在する。semantic legality は計画中。 |
| `spec.en.15.statements.label_scope` | statement label は enclosing reasoning block に scope され、same-scope duplicate と inner-scope shadowing は禁止される。 | R-017/R-018 が explicit projection contract を所有する。R-032A structural mappingとR-032B normal-source collectorはimplemented。active B5C confinement coverageは`mizar-test`で両unchanged APIをprivateにconsumeする。 |
| `spec.en.16.theorems_and_proofs.labels` | theorem/proof label は専用 scope と citation resolution を持つ。 | R-017/R-018 が core label resolution を所有する。R-032A が structural mapping、R-032B が B5C 用 represented proof-step/simple citation collection を行う。 |
| `spec.en.22.error_handling_and_diagnostics` | diagnostics は安定 code と source span を必要とする。 | 共有 `Resolution` family は存在する。resolver-specific な name/import/label descriptor adoption は `external_dependency_gap` / deferred のまま。R-G001 と R-030 を参照。 |
| `arch.03.phase_4_5` | resolver は phase 4-5、import graph、declaration shell、name resolution、signature collection を所有する。 | 全 task の正本 design input。 |
| `arch.03.step_1` | build/package module index は phase 0 input。resolver はそれを消費し import edge を精緻化する。 | build-side slice は完了。resolver-side seam は task 7。 |
| `arch.01.resolved_ast_symbol_env` | `ResolvedAst` と `SymbolEnv` は immutable IR snapshot であり checker input。 | task 2〜6 の基準。 |
| `mizar-build.module_index` | build-side module identity と provider contract は alias-independent かつ source-id free でなければならない。 | task 7 で利用可能。build-owned planning を重複実装しない。 |

## 関連テスト

| test path | 意図 | spec refs |
|---|---|---|
| `tests/lexical/pass/pass_lexical_import_prescan_*.src` | import pre-scan と active lexical environment の seed。 | `spec.en.12.modules.import_prelude` |
| `tests/lexical/fail/fail_lexical_import_prescan_*.src` | semantic validation 前の malformed import prelude recovery。 | `spec.en.12.modules.import_prelude` |
| `tests/miz/pass/parser/pass_parser_module_skeleton_001.miz` | parser が resolver の消費する module-level skeleton を生成する。 | `spec.en.12.modules.compilation_unit_skeleton` |
| `tests/miz/pass/parser/pass_parser_import_items_001.miz` | parser が alias と branch を含む import item syntax を生成する。 | `spec.en.12.modules.import_items.parser` |
| `tests/miz/pass/parser/pass_parser_export_visibility_001.miz` | parser が export と visibility modifier を生成する。 | `spec.en.12.modules.export_visibility.parser` |
| `tests/miz/fail/parser/fail_parser_import_*.miz` | parser が構文的に不正または誤配置の import を拒否する。 | `spec.en.12.modules.import_prelude` |
| `tests/miz/pass/resolve/pass_resolve_declaration_symbol_smoke_001.miz` | parser-backed declaration、visibility-bearing declaration、theorem/lemma declaration が internal diagnostic なしで symbol collection に到達し、表現済み kind、visibility、export status について exact SymbolEnv-derived symbol / definition fact assertion を持つ active declaration-symbol runner smoke coverage。 | `spec.en.11.symbol_management.signatures`, `spec.en.11.symbol_management.visibility`, `spec.en.12.modules.visibility.semantic`, `spec.en.16.theorems_and_proofs.labels.declaration_symbols` |
| `tests/miz/fail/resolve/fail_resolve_duplicate_theorem_symbol_001.miz` | proof checking 前の same-scope duplicate theorem label に対する active declaration-symbol fail coverage。 | `spec.en.16.theorems_and_proofs.labels.same_scope_uniqueness` |
| `tests/miz/fail/resolve/fail_resolve_same_signature_return_conflict_001.miz` | resolver-owned parser-backed functor signature conflict slice の active declaration-symbol fail coverage。same argument-signature functor declaration が異なる return signature を示す場合、declaration/signature collection で conflict になる。 | `spec.en.19.overload.definition_conflict.declaration` |
| `tests/miz/fail/resolve/fail_resolve_same_signature_same_return_conflict_001.miz` | R-031 active declaration-symbol fail coverage。exact parser-backed ordinary-functor same-signature/same-return definition conflictをcoverする。 | `spec.en.19.overload.definition_conflict.same_return.declaration` |

計画中の test-first 追加:

- task 4〜7 では deterministic id、arena、table、provider seam の resolver unit test。
- R-023 の declaration-symbol seed set を超える semantic `.miz` pass/fail case は、
  将来の runner assertion stage が成熟した時点で追加する。残る corpus work は
  R-G002 が追跡し、R-G007 が精緻化する。
- 独立 R-032A/R-032B lower prerequisite の後に、`mizar-test` `declaration_symbol`
  consumer が所有する Checker Task 258B5C active inner-to-outer / sibling
  proof-label confinement case を追加する。
- より広い semantic import/name/label/symbol obligation の traceability entry は、
  将来の assertion stage が存在した時点で追加する。
- 既存 `.miz` test や expectation を resolver source behavior に合わせる目的だけで
  rebaseline してはならない。

## design/source inventory

design files:

- [todo.md](./todo.md)
- [declarations.md](./declarations.md)
- [names.md](./names.md)
- [labels.md](./labels.md)
- [symbols.md](./symbols.md)
- [recovery.md](./recovery.md)
- [source_spec_correspondence.md](./source_spec_correspondence.md)
- [bilingual_documentation_synchronization.md](./bilingual_documentation_synchronization.md)
- [module_boundary_refactor.md](./module_boundary_refactor.md)
- [module_summary_reuse.md](./module_summary_reuse.md)
- [crate_exit_report.md](./crate_exit_report.md)
- [architecture 03](../../architecture/ja/03.module_and_symbol_resolution.md)
- [architecture 01](../../architecture/ja/01.ir_layers.md)
- [mizar-build module index](../../mizar-build/ja/module_index.md)
- [mizar-build planner](../../mizar-build/ja/planner.md)
- [mizar-frontend todo](../../mizar-frontend/ja/todo.md)
- [mizar-test staged model](../../mizar-test/ja/staged_model.md)

source files:

- `crates/mizar-resolve/` は manifest、crate-level documentation、
  lint-policy guard、foundation module を持つ workspace crate である。
- 現在の resolver source surface は `src/resolved_ast.rs`、`src/env.rs`、
  task R-007 の build-side provider contract 上の input seam である
  `src/module_index.rs`、task R-009/R-010 の import graph と source-shaped
  path-resolution seam である `src/imports.rs`、task R-011 の source-shaped
  declaration-shell collector である `src/declarations.rs`、task R-013/R-016 の
  namespace / symbol-name lookup / dot-chain finalization slice である
  `src/names.rs` と private `src/names/diagnostics.rs` の internal
  name-diagnostic assembly、task R-018 の theorem / lemma と proof-step label-resolution slice である
  `src/labels.rs`、task R-020/R-021 の symbol-collection と parser-backed
  signature-projection slice である `src/symbols.rs`、task R-022 の stage 別
  call site から使う recovered-syntax helper である `src/recovery.rs`。
- R-029 は public module root を `src/*.rs` に保ち、deterministic snapshot helper を
  private `src/env/snapshot.rs` と `src/resolved_ast/snapshot.rs` へ、resolved-AST
  validation helper を `src/resolved_ast/validation.rs` へ、inline unit test を
  module ごとの private `tests.rs` file へ移した。
- 現在の上流 source surface は `crates/mizar-syntax`、`crates/mizar-parser`、
  `crates/mizar-frontend`、`crates/mizar-session`、`crates/mizar-build`。
- direct `SurfaceAst` declaration-shell collection は、表現済み declaration-like
  item と export projection について存在する。executable namespace lookup は
  source-shaped namespace path candidate について存在し、executable symbol-name lookup は
  preliminary `NameSymbolProjection` record 上に存在する。crate-local/internal name
  diagnostic report は unresolved/ambiguous name and namespace record から生成される。
  source-shaped dotted chain は lexical local-term shadowing で finalization され、task
  R-018 は preliminary `LabelProjection` record 上の theorem / lemma と proof-step
  label resolution を追加する。R-019 は `symbols.md` で signature collection を仕様化し、
  R-020 は explicit projection 上の opaque declaration-symbol collection skeleton を
  実装し、R-021 は表現済み parser-backed signature extraction と resolver-local
  lexical-summary data shape を実装する。R-022 は横断 recovered-syntax policy を追加し、
  degraded table/env fact を保持しつつ、recovered origin または shell からの dependent
  name / label / symbol diagnostics を抑制する。direct import/export semantic validation、
  artifact-backed summary、grouped shared-prefix lowering、driver / corpus label and
  symbol lowering は R-022 より後の作業として残る。R-023 は
  declaration-symbol corpus collection だけを追加し、production `SurfaceAst`
  proof-label declaration/reference projection は追加していない。

observed behavior:

- `mizar-build` は session-local source id を割り当てずに deterministic `BuildPlan` と
  `ModuleIndex` construction を提供する。
- `mizar-session` は source identity、normalized path、snapshot identity を所有する。
- `mizar-frontend` は lexical environment と parser orchestration seam を提供する。
  semantic import validation は resolver-owned のままである。
- executable semantic import pass は R-010 の source-shaped candidate seam まで
  存在する。alias binding、relative-prefix interpretation、namespace/package
  binding、unresolved recovery、canonical graph construction は unit-test candidate 上で
  存在する。
- executable declaration pass は R-011 の source-shaped collector slice まで存在する。
  表現済み declaration-like `SurfaceAst` item、visibility wrapper、transparent annotation
  wrapper、recovered-shell state、export projection shell は semantic validation なしで
  inventory される。
- `names.md` は R-013〜R-016 の name-resolution contract として、scope、
  namespace-before-symbol lookup、visibility/shadowing、ambiguity、dot-chain
  finalization を仕様化する。executable names pass は R-013 namespace lookup slice まで
  存在する。import alias、reserved namespace root、package-name binding、
  current-package fallback、recovered namespace path、unresolved import-alias
  dependency、ambiguous alias candidate target、provider-error classification、
  earliest failing segment range が表現される。executable names pass は R-014
  symbol-name slice まで存在する: preliminary symbol projection、
  declaration-point filtering、qualified / unqualified visibility、
  current-module shadowing、overload-group placeholder collapse、enabled builtin
  fallback、deterministic `NameRefTable` outcome が selector type checking や overload
  winner selection なしで表現される。executable names pass は R-015 internal diagnostic
  slice まで存在する: deterministic `NameDiagnosticRootId` allocation、primary /
  cascade record、unresolved import-alias root、namespace/name dependent record、stable
  candidate payload、internal record ordering が public diagnostic code なしで表現される。
  executable names pass は R-016 dot-chain finalization slice まで存在する:
  `LocalTermScope`、`LocalTermBinding`、`DotChainCandidate`、`DotChainFinalizer` が
  dotted chain を checker-deferred selector または namespace-qualified symbol reference として
  分類し、selector validation は行わない。R-017 の label contract は `labels.md` に
  仕様化済みであり、R-018 は `LabelResolver`、`LabelScopePath`、`LabelProjection`、
  `LabelReferenceCandidate`、deterministic `LabelIndex` / `LabelRefTable` output、
  crate-local/internal label conflict diagnostics を実装する。R-019 は algorithm
  signature、exported summary、lexical-summary projection を含む `symbols.md`
  signature-collection contract を追加する。R-020 は `SymbolCollector`、
  `SymbolDeclarationProjection`、opaque `SignatureShell` population、duplicate/conflict
  diagnostics、overload grouping、registration indexing、contribution tracking を追加する。
  R-021 は `SignatureProjectionExtractor`、parser-owned opaque signature payload、
  owning declaration 内の template-role preservation、exported lexer-visible spelling 用の
  `ModuleLexicalSummaryIndex` seed を追加する。
  R-022 は resolver-local recovered-syntax policy を追加する: recovered namespace と
  name/dot-chain reference は internal name diagnostic root なしで保持し、recovered
  label projection は conflict diagnostic を出さず、recovered symbol projection は
  duplicate / illegal-overload diagnostic なしで `RecoveredShell` metadata を保持する。
  R-025 は crate root の public-seam determinism regression を追加し、import graph、
  name diagnostic、`ResolvedAst` snapshot、`SymbolEnv` snapshot を横断して確認する。
  詳細な deterministic id / table / ordering は module-local test が引き続きカバーする。
  R-026 は resolver-owned public enum family ごとの forward-compatibility decision を
  module spec に記録し、これら spec-owned module における将来の public-enum 追加を
  crate-local lint test で guard する。R-027 は source/spec correspondence audit を
  `source_spec_correspondence.md` に記録し、unclassified な blocking/high gap は
  見つからなかった。R-028 は bilingual documentation synchronization audit を
  `bilingual_documentation_synchronization.md` に記録し、resolver design file について
  残る英日不一致は見つからなかった。R-029 は module-boundary refactor gate を
  `module_boundary_refactor.md` に記録し、private helper / test split のみで、public
  API、behavior、diagnostic、rendering、artifact、boundary change はない。crate
  close-out は `crate_exit_report.md` に記録し、quality score 94/100 と full
  verification passing を記録する。
  direct import/export semantic validation、
  artifact-backed summary、grouped shared-prefix lowering、corpus-level label/symbol
  lowering は reverse engineering ではなく spec、test、design に従う。

## 既知 gap / drift

| ID | class | evidence | action |
|---|---|---|---|
| R-G001 | `spec_gap` から `external_dependency_gap` / deferred adoption へ精緻化 | Architecture 03 は安定した user-facing resolver diagnostics を要求する。spec chapter 22 と `mizar-diagnostics` は広い `Resolution` family を reserve しているが、resolver の name/import/label に対する具体的 semantic descriptor、numeric code assignment または alias、migration behavior、consumer projection は採用されていない。 | 実装で placeholder public resolver descriptor を創作しない。R-013 は shared diagnostic adoption を deferred とし、R-015 は name diagnostic を crate-local/internal に保つ。後続の user-facing resolver diagnostic integration の前に、R-030 が real adoption gate を所有する。 |
| R-G002 | `test_gap` | 履歴: 既存 test は lexical/parser の import/export syntax をカバーしていたが、semantic resolver behavior は未カバーだった。 | R-023、post-task-20 R-G007 signature-conflict slice、exact SymbolEnv-derived pass payload assertion により active declaration-symbol `.miz` fixture 3 件、expectation sidecar、`spec_trace.toml` requirement で部分的に解消した。残る具体的な corpus assertion gap は R-G007 が精緻化する。既存 test を rebaseline せず、将来の runner assertion 拡張で semantic coverage を増やす。 |
| R-G003 | R-024 で解決済み | `ModuleSummary` artifact reuse は当初 `mizar-artifact` task 5 に依存していた。R-024 は canonical な `mizar-artifact` `ModuleSummary` reader を消費し、検証済み public summary を resolver contribution index へ project する。 | resolver / artifact / build の ownership を分離したままにする。resolver-owned artifact schema、shim、fallback reader、public diagnostic は追加しない。 |
| R-G004 | `boundary_violation` risk | `mizar-build` は package planning、module discovery、build-side module-index provider construction をすでに所有する。 | task 7 は `mizar-resolve` 内で planner/source-loading 責務を再実装せず、build-side provider contract を消費または adapter 化する。 |
| R-G005 | design_drift | 履歴: R-007 前の top-level immediate-next-work guidance は wave A 前の `mizar-build` 作業を記述していた。 | close-out、2026-07-02 roadmap sync、R-024 completion で解消済み。top-level design TODO は現在の resolver milestone が R-024 まで完了していることを記録し、immediate queue を `mizar-test` foundation cleanup に向けている。 |
| R-G006 | external_dependency_gap | `mizar-parser` / `mizar-syntax` は direct template parameter を declaration payload role として、nested template loci を parser-owned pattern surface として公開しているが、module-level scheme declaration shell は現在存在しない。 | R-021 は direct template role を owning signature payload に保持し、nested loci を flattened parser-owned signature surface に保持し、scheme/template module symbol を創作しない。parser/syntax が scheme declaration source role を公開したら解除する。 |
| R-G007 | `test_gap` | R-023はdeclaration-symbol runnerをactive化し、later bounded incrementはsignature-conflict factとactive B5C inner-to-outer/sibling confinement negativeを追加する。import/name/dot-chain/other label-reference corpus factは残る。 | R-023/R-027とcommitted R-032A/R-032Bをcompleteのまま保つ。B5Cはprivate key `declaration_symbol.label.proof_scope_confinement`のspec-derived confinement negative 2件だけを閉じる。public codeを空に保ち、既存testをrebaselineしない。 |

Checker Task 258B5C inventory は Medium `source_drift` を発見した。resolver
core は explicit `LabelProjection` / `LabelReferenceCandidate` input に正しい
`LabelScopePath` prefix rule を適用していたが、pre-R-032B production source は
normal `SurfaceAst` から proof-step declaration と simple unqualified citation を
その input へ収集しなかった。R-023 をその full source walk と扱うことは
`design_drift` であり、R-023 が完了したのは declaration-symbol corpus
collection だけである。R-032Aとcommitted R-032B sourceがこのbounded lower
pathを閉じる。R-G001 は public diagnostic adoption の Low deferred
`spec_gap`。active B5CはR-G007内のbounded confinement negative 2件だけを
閉じる。

R-029 までに完了済みの foundation/name/label/symbol task と hardening audit/policy work を
妨げる blocking contradiction は見つかっていない。R-G001 は public canonical resolver
diagnostic-code commitment のみをブロックし、R-G003 は R-024 で解消済みであり、
R-G007 は残る import/name/dot-chain/label runner work 向けの non-blocking
corpus assertion expansion gap として残る。

crate-wide close-out report は non-deferred work の hard gate をすべて通過し、
`mizar-artifact` task 5 が存在するまで R-024 を deferred のままにしていた。R-024 は現在、
その canonical artifact contract の resolver-side consumer として実装済みである。

## task decomposition

### Task R-001: crate scaffold と lint policy

Purpose: `mizar-resolve` workspace member と lint guard を作成する。

Spec refs:
- architecture 03 phase 4-5 responsibility
- internal crate layout policy

Tests:
- `cargo test -p mizar-resolve`
- 既存 workspace crate と同型の lint-policy guard

Affected design files:
- `doc/design/mizar-resolve/en/todo.md`
- `doc/design/mizar-resolve/ja/todo.md`

Affected source files:
- `Cargo.toml`
- `crates/mizar-resolve/Cargo.toml`
- `crates/mizar-resolve/src/lib.rs`
- `crates/mizar-resolve/tests/lint_policy.rs`

Completion condition:
- `mizar-session` と `mizar-syntax` に依存する workspace が build できる。
- resolver language behavior を導入しない。

Forbidden behavior:
- parser 変更。
- `SymbolId` を創作する semantic resolution stub。
- user-facing resolver diagnostic-code decision。

### Task R-002: `resolved_ast.md` 仕様

Purpose: `ResolvedAst`、stable id、reference table、未解決/曖昧表現、recovered-shell
rule を仕様化する。

Spec refs:
- architecture 01 `ResolvedAst`
- architecture 03 interface definitions

Tests:
- code なし。R-004 の data-shape test を特定する。

Affected design files:
- 英日 `resolved_ast.md`
- 新たな gap が見つかった場合は本 crate plan

Affected source files:
- なし

Completion condition:
- 英語正本と日本語版が同期している。
- 実装前に source/spec gap が分類されている。

Forbidden behavior:
- resolver-owned name context を超える type-directed overload や selector behavior の決定。

### Task R-003: `env.md` 仕様

Purpose: `SymbolEnv`、symbol/label index、declaration dependency edge、
namespace graph、contribution tracking、invalidation note を仕様化する。

Spec refs:
- architecture 01 `SymbolEnv`
- architecture 03 signature collection
- spec chapter 11 scope and visibility

Tests:
- code なし。R-005 の index と contribution-tracking test を特定する。

Affected design files:
- 英日 `env.md`
- 新たな gap が見つかった場合は本 crate plan

Affected source files:
- なし

Completion condition:
- checker-facing boundary が明確で、type inference を含まない。

Forbidden behavior:
- checker-owned fact、cluster firing、overload winner selection。

### Task R-004: `resolved_ast` data shape 実装

Purpose: `resolved_ast.md` に基づき deterministic id、arena/table invariant、
typed accessor を実装する。

Spec refs:
- `resolved_ast.md`

Tests:
- id determinism。
- 有効な child id と arena cycle がないこと。
- node resolution state preservation と安定した node-to-table/import key。
- `NameRefTable`、`LabelRefTable`、`ResolvedImports` round-trip。

Affected design files:
- implementation が design drift を示した場合のみ `resolved_ast.md`。

Affected source files:
- `crates/mizar-resolve/src/resolved_ast.rs`
- `crates/mizar-resolve/src/lib.rs`

Completion condition:
- source が仕様と一致し、narrow test が通る。

Forbidden behavior:
- `resolved_ast.md` にない semantic meaning の割り当て。
- resolver 実装に合わせるための parser/syntax 変更。

### Task R-005: `env` data shape 実装

Purpose: `env.md` に基づき `SymbolEnv` index と source-contribution tracking を
実装する。

Spec refs:
- `env.md`

Tests:
- insert/lookup round-trip。
- deterministic ordering。
- source unit ごとの contribution tracking。

Affected design files:
- implementation が design drift を示した場合のみ `env.md`。

Affected source files:
- `crates/mizar-resolve/src/env.rs`
- `crates/mizar-resolve/src/lib.rs`

Completion condition:
- checker-owned semantics を含まない checker-facing index が存在する。

Forbidden behavior:
- type checking、registration activation、proof obligation generation。

### Task R-006: deterministic debug rendering

Purpose: data-shape snapshot 用の安定した human-readable rendering を追加する。

Spec refs:
- architecture 20 snapshot tests
- `resolved_ast.md`
- `env.md`

Tests:
- repeated rendering の byte identity。
- 現行 node/table kind をカバーする fixture。

Affected design files:
- `resolved_ast.md`
- `env.md`

Affected source files:
- `crates/mizar-resolve/src/resolved_ast.rs`
- `crates/mizar-resolve/src/env.rs`

Completion condition:
- debug output が deterministic であり、public artifact schema ではない。

Forbidden behavior:
- 既定で absolute path を埋め込むこと。
- 仕様化されていない render order を semantic order として使うこと。

### Task R-007: module-index input contract と interim seam

Purpose: import graph resolution が消費する resolver-side module-index input を定義・実装し、
driver integration が存在するまでは resolver test 用 workspace-stub provider を持つ。

Spec refs:
- architecture 03 Step 1
- `mizar-build` `module_index.md`

Tests:
- stub provider が multi-module fixture を供給する。
- canonical module identity が alias-independent である。
- provider error が deterministic である。

Affected design files:
- resolver seam documentation または task 7 notes。
- seam が closed になった場合は top-level open decision。

Affected source files:
- `crates/mizar-resolve/src/imports.rs` または dedicated provider module。
- `crates/mizar-resolve/tests/` 配下の test または unit test。

Completion condition:
- resolver-side input contract が明確である。
- `mizar-build` planner/source-loading responsibility を再実装しない。

Forbidden behavior:
- `mizar-build` 内の resolver-owned compatibility shim。
- `mizar-artifact` task 5 より前の dependency-summary artifact assumption。

### 後続 milestone group

- Imports R-008〜R-010: semantic import path resolution、import graph、cycle policy。
- Names R-011〜R-016: declaration shell、namespace/name lookup、ambiguity、
  dot-chain finalization。
- Labels R-017〜R-018: 専用 label scope、label reference table、theorem / lemma と
  proof-step の実行可能な label resolution。
- Symbols R-019〜R-021: signature collection spec、collection skeleton、
  duplicate detection、parser-backed kind ごとの抽出、lexical-summary seed は
  表現済み source role について完了。
- Hardening R-022〜R-029: recovered syntax、corpus runner、R-024 の
  ModuleSummary external-dependency gate、determinism、enum policy、audit、
  module-boundary refactor。
- 後続 bounded prerequisite: R-031 は ordinary-functor same-return conflict
  increment を完了した。R-032 は計画済み Checker Task 258B5C normal-source
  proof-label projection prerequisite である。

### Task R-024: ModuleSummary Reuse

Purpose: dependency module について、compatible summary が利用可能な場合に dependency source を
読み直さず、canonical な `mizar-artifact` `ModuleSummary` projection を消費する。
resolver-owned artifact schema は作成しない。

Spec refs:
- architecture 03 "Module Summary"
- architecture 18 dependency fingerprint
- [module_summary_reuse.md](./module_summary_reuse.md)
- [mizar-artifact module_summary.md](../../mizar-artifact/ja/module_summary.md)

Tests:
- shared fixture で summary-backed と source-backed の exported resolver-facing projection が一致する。
- incompatible schema または identity/hash mismatch は deterministic な crate-local record を伴って
  fallback する。
- summary consumption を繰り返しても deterministic である。

Affected design files:
- `todo.md`
- `00.crate_plan.md`
- `module_summary_reuse.md`
- status wording が変わる場合は `symbols.md`。

Affected source files:
- `crates/mizar-resolve/src/module_summary_reuse.rs`
- `crates/mizar-resolve/src/lib.rs`
- `crates/mizar-resolve/Cargo.toml`

Completion condition:
- 検証済み canonical `ModuleSummary` value が resolver summary contribution index を populate できる。
- summary がない、または incompatible な場合、同じ canonical module の source-backed input が
  caller から与えられている時に限って source-backed resolution へ fallback する。純粋な
  artifact dependency は visibility を広げたり identity を捏造したりせず、deterministic な
  unavailable/incompatible summary record を生成する。
- resolver / artifact / build の ownership boundary が明示されている。

Forbidden behavior:
- resolver-local artifact schema、reader、writer、hash framing を定義する。
- explicit contract なしに build `content_hash` を interface hash と解釈する。
- public resolver diagnostic code を割り当てる。
- `doc/spec`、既存 `.miz` test、既存 expectation を変更する。

## Step 5 Task 31 Addendum

R-031はcanonical Chapter 19、checker Task 37、既存deferred trace rowが要求する
bounded resolver-owned same-signature/same-return declaration conflictである。exact
unit/corpus/recovery/order test成功後にだけdeclaration/signature collectionとactive
`declaration_symbol` consumerを拡張する。checker semantic type equivalence、overload
selection、public-diagnostic adoption、expectation rebaselineは行わない。R-031は独立
checker-Task-49 prerequisiteであり、Task-180 contradiction vertical sliceのdependency
ではない。

exact represented familyはordinary parser-backed functor definition、または同じ
`SymbolKind::Functor` / `DefinitionKind::Functor` pairを持つexplicit test projectionである。
equalityはnamespace、primary spelling、normalized notation pattern、normalized definition
argument context、syntactic arityについてのresolver-syntactic equalityとする。return
signatureはnormalized surface textだけを使い、binder alpha-equivalenceやsemantic type
equivalenceは主張しない。predicate、mode、redefinition、registration、recovered declaration、
別kind pairのprojectionはこのclassifierへ入らない。

R-031はpublic non-exhaustive internal-metadata variant
`SymbolDiagnosticClass::SameSignatureDefinitionConflict`と
`DeclarationConflictClass::SameSignatureDefinitionConflict`をappendする。diagnostic Debug
spellingはRust variant名`SameSignatureDefinitionConflict`、definition-conflict variantの
explicit `SymbolEnv` snapshot spellingはexact
`same_signature_definition_conflict`とする。returnがすべて同じgroupは全candidateを含む
このdiagnosticをexactly 1件だけ受け、`mizar-test`では
`declaration_symbol.signature.same_signature_definition_conflict`へmapする。distinctな
normalized return signatureが複数あるgroupは、全candidateを含む既存
`SameSignatureReturnConflict` diagnostic/conflict exactly 1件と、変更しない
`declaration_symbol.signature.same_signature_return_conflict` keyを維持する。したがってmixed
groupではdifferent-return classが優先し、overlapping diagnosticは禁止する。diagnosticの
shell/rangeはcanonical source orderのfirst candidateを使い、candidate orderはinput
permutationに対してcanonicalなままにする。

implementation sliceは`crates/mizar-resolve/src/symbols.rs`、`src/env.rs`、
`src/env/snapshot.rs`、それらのunit/snapshot test、既存private declaration-symbol detail-key projection
`crates/mizar-test/src/runner/shared.rs`だけに限定する。既存same-return `.miz` sourceは
byte-identicalのままにする。そのsidecarにはactive declaration-symbol tag、exact
diagnostic payload、active wordingだけを追加し、既存different-return sidecarは
byte-identicalに保つ。deferredからcoveredへ変えるのは
`spec.en.19.overload.definition_conflict.same_return.declaration`だけである。paired resolver /
mizar-test design recordと`doc/design/spec_coverage_audit.md`がこのexactな新規corpus creditを
記録する。

exitにはparser-backed / explicit-projection same-return case、spelling/pattern、argument
context、arity、namespace/kind、nonordinary、recovered near miss、mixed-return priority、
source-order/candidate determinism、exact same-return runner keyと変更しないdifferent-return
runner key、exact `same_signature_definition_conflict` SymbolEnv snapshot assertion、
focused/workspace verificationを要求する。R-031はparser/checker behavior、public
numeric diagnostic code、overload viability/specificity/winner logic、このsole same-return seed
以外のTask-49 member、Core/CFG/VC/proof path、Step 6/7 statusを変更しない。

R-031はこのfrozen slice内で完了した。appendしたmetadata variant、explicit
snapshot/detail-key projection、exact / negative unit matrix、mixed-group priority、recovery
suppression、active same-return corpus case、sole covered trace row、paired coverage recordを
同時にlandした。different-return expectation、`.miz` source、semantic type/selection
behavior、deferred phase statusは変更していない。

## Checker Task 258B5C lower prerequisite: R-032A / R-032B

R-032 は canonical Chapter 15 §15.10 と Chapter 16 §§16.4.2/16.5.1 から
導出した resolver-owned umbrella である。S-026 preflight discovery 前の
frozen B5C record は docs prerequisite、R-032A validated structural
Surface-to-resolved arena/map、R-032B proof-label collector、active B5C の
順序だった。この historical four-task record は下記 overlay の effective
seven-task prerequisite order に execution 上 supersede される。各 task は
引き続き1 commit と fresh inventory を要求する。

R-032A は `resolved_ast.md` の exact `SurfaceResolvedArena` API と公開
non-exhaustive error table を所有する。complete one-to-one child-first
same-index structure、exact source/module/root/kind/children/range/recovery/
origin/path、validation、checked `usize` to `u32`、semantic key なしを freeze
する。owner は exact に `resolved_ast.rs` / `resolved_ast/tests.rs` /
sole R-026 public-enum decision entry 用 `tests/lint_policy.rs` / paired docs。

R-032B は `labels.md` の exact `ProofLabelSourceCollector` と公開
`ProofLabelSourceCollectionError` を所有する。R-032A arena を validate し、
existing projection/reference を `projections()` / `references()` で返す。
callback、unmapped side channel、fabricated id はない。implementation owner は
exact `labels.rs` / `labels/tests.rs` / sole R-026
`ProofLabelSourceCollectionError` / `labels.md` public-enum decision entry 用
`tests/lint_policy.rs` / paired docs。

exact `ProofLabelSourceCollector<'a>` impl は `SurfaceAst` /
`SurfaceResolvedArena` の `'a` borrow だけを store し、namespace/contribution を
own、constructor `ModuleId` は borrow/store しない。collection は validated
`resolved.module()` を使う。R-032A state/key mismatch と R-032B typed overflow
を含む exact public error declaration は canonical detailed docs にある。

top-level theorem owner root は module source-order `[0]`, `[1]`, ...、nested
scope は owner-relative child order。module-global one-based counter は supported
statement を exact depth-first preorder し theorem ごとに reset しない。
unsupported/recovered subtree は consume/descend しない。visibility は own proof
を含む labelled compact subtree completion 後。exact length-framed
`proof-step-v1` `LabelOriginPath` grammar、B5C paths、subtree/exclusion/mutation
matrix、exhaustive default-deny direct Surface edge table は `labels.md` に
freeze する。only upper traversal は exact `Root` -> `CompilationUnit` ->
`ItemList` -> direct normal `TheoremItem`。unlisted edge は row/ordinal/descent
なし。

test は R-032A wrong/stale/mismatch/overflow matrix、`[0] -> [0,1]`、inner/
sibling confinement、earlier theorem `[0]` label が otherwise eligible でも
later theorem `[1]` から unresolved かつ cross-theorem conflict なし、
own-proof pre-completion unresolved / same-block post-completion resolved、
exact ordinal/provenance、recovery/exclusion、deterministic mutation を覆う。
さらに permitted edge ごとの positive、parent relocation/wrapper/formula/
computation/qualified/grouped/bulk/template/unsupported-owner/recovery negative、
mixed reference-list filter、各 table row の representative all-other matrix を
要求する。upper 3 edge は別々の positive、missing/additional/wrong/relocated/
wrapped alternative は negative にする。

後続 consumer は private `mizar-test` `declaration_symbol` exact key
`declaration_symbol.label.proof_scope_confinement`。public checker handoff、
parser/frontend、Cargo、other resolver owner、semantic phase、public numeric
code、fixture/sidecar/trace status/count、active runner change は除外する。docs
commit はこれらを変更せず、各 implementation は finding-free review、verification、
clean task-only commit を要求する。active coverage を早期に主張しない。

## Task Index

| Task | Contract |
|---|---|
| STEP5C1-VARIABLE-SEMANTICS | [JA contract](../../task_contracts/ja/STEP5C1-VARIABLE-SEMANTICS.md) |
| RESOLVE-TEMPLATE-TYPEPARAM-277R1 | [JA contract](../../task_contracts/ja/RESOLVE-TEMPLATE-TYPEPARAM-277R1.md) |
| RESOLVE-FRAENKEL-GENERATOR-VAR-277R2 | [JA contract](../../task_contracts/ja/RESOLVE-FRAENKEL-GENERATOR-VAR-277R2.md) |
| RESOLVE-FRAENKEL-NESTED-CAPTURE-257C4C2 | [JA contract](../../task_contracts/ja/RESOLVE-FRAENKEL-NESTED-CAPTURE-257C4C2.md) |
| RESOLVE-FRAENKEL-NESTED-MULTI-CAPTURE-257C4C8R | [JA contract](../../task_contracts/ja/RESOLVE-FRAENKEL-NESTED-MULTI-CAPTURE-257C4C8R.md) |
| RESOLVE-FRAENKEL-CONTAINING-OWNER-33R | [JA contract](../../task_contracts/ja/RESOLVE-FRAENKEL-CONTAINING-OWNER-33R.md) |

## exit criteria

Hard gates:

- active milestone に blocking/high specification inconsistency が残っていない。
- source implementation 前に、全 public API と約束された挙動に design coverage がある。
- source behavior が `doc/spec`、既存 test、active design spec の外に残っていない。
- `.miz` test と expectation を implementation に合わせる目的だけで変更していない。
- resolver が parser、checker、proof、build、driver、artifact、session の責務を所有していない。
- diagnostics は明示的な spec/code-space decision に裏付けられているか、milestone 内の
  crate-local/internal として文書化されている。
- 残る risk は deferred、out of scope、human-owned として分類されている。

Verification commands:

```text
cargo fmt --check
cargo test -p mizar-resolve
cargo clippy -p mizar-resolve --all-targets --all-features -- -D warnings
```

boundary work では、必要な subset も実行する:

```text
cargo test -p mizar-build
cargo test -p mizar-session
cargo test -p mizar-syntax
cargo test -p mizar-frontend
cargo test -p mizar-test
```

Review expectations:

- active specification に対する review-only agent。
- implementation task を完了扱いにする前の test sufficiency review。
- 未解決 finding なしの implementation review。
- source/documentation consistency review。
- crate-wide completion には hard gate と 90/100 以上の read-only quality score が必要。

## R-032A preflight dependency overlay

B5C docs commit `1527ca611d3ea33af22675a31cc2d7eaa2bdb07e` 後の
fresh R-032A preflight は High `boundary_violation` risk を発見した。syntax
consumer API は valid disconnected compatibility node をその
`SurfaceNodeId` と共に列挙できず、以前の existing-API-sufficiency statement は
`design_drift`。semantic-free syntax accessor への authority は十分で、人間の
semantic decision は不要。

effective order は S-026 frozen documentation、S-026 syntax implementation、
R-032A lint-policy frozen-documentation correction、R-032A implementation、
R-032B lint-policy frozen-scope documentation correction、R-032B
implementation、active B5Cで、各commit間にfresh inventoryを挟む。S-026 は dense
compatibility-node view だけを所有し、resolver ownership と B5C
language/test intent は unchanged。exact S-026 contract は
`doc/design/mizar-syntax/en/ast.md`、refined R-032A validation order は
[resolved_ast.md](./resolved_ast.md) が canonical。

### R-032A lint-policy documentation-correction verification

implementation preflightはomitted mandatory R-026 enum-decision ownerを
High `design_drift`と分類し、blocking `spec_gap`、test-intent ambiguity、
repository metadata conflictはない。first specification reviewが残存
five-task ordering statement 1件を発見・修正し、repeat specification review、
independent test-sufficiency review、repeat source/documentation consistency
reviewはその後**NO FINDINGS**。

docs-only scopeはexact 31 design files、resolver 16、checker 8、
`mizar-test` 6、global ledger 1。`git diff --check`、focused
`mizar-resolve` tests `111 + 11 + 0`、`cargo fmt --check`、warnings-denied
all-target/all-feature Clippy、full offline workspace testsはPASS。five CLI
countsはplan `419/387`、pass/fail `228/191`、active
parse/declaration/type/proof `101/5/198/1`、warnings/errors `23/0`のまま。
stdout hashも
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`、
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`、
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`、
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`、
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`
のまま。

resolver library testsは111、raw/normalized list hash
`ec8fa003e020725a503d900c170ed0a0edb90ece013e6854ef165689948bc3be` /
`81a241fa6a271262527bf0af7c33df5492344538ac6f7b8636c26571e702aa4b`、
productionは`15/17921`、path/content
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`925ba16aaa8208a87fabaad4970666784f8e1c0a8de3ad81f191932786ad5723`
のまま。checker、runner、syntax productionもそれぞれ`23/145097`、
`c2eea2db9187c48dd830a010eff37f09b90467f9012a9fe6b3ac669b6d1dac42` /
`c39d43229e85e6136597f0f6cd52c15e1ab1d2057cf7866f6bbbf244307250dc`、
`30/59745`、
`98f3b264a59fed5b08c3e8f20e7ca58ff54efaa154eab16a7572a69ce923f275` /
`75d3e70b1eb6a5871486c1dc6b0ccde06aec4b0d3e23a1b4c5eecf33dfb9039b`、
`6/4314`、
`27622a38347b8b5f94559b9587d227b944c0f680c2f7729c115e7743253eb733` /
`fec701d87a95506058a6a7ceb4e500786084f4b00646df0bf3a43624a18617b8`。
library test countとraw/normalized list hashはchecker 430
`5dc6cff8c93d86911dca85f91da81501ddf226c42fd6338f4c4be6105782132e` /
`d7eb7a0d48d2c11b9c3f3b00ca025e1c7a1d5ce9b2b767ca94c2655c5d2dbf27`、
runner 500
`94aa81ba9af645c9de1e927aa06bf8d525e3510509a607074e604eafc00ff995` /
`e0d976ab223f0ac0c1b48bd9926bb3fcf785706bdd4a24ecfd0633c81f66f943`、
syntax 59
`512775259a51121a0c12ab9fbf0d1083273d3d140362d889cdd9e22184215da6` /
`c11a29c90fee3fe81d839f80f196dcd405cc43c7da86e83f37dc123042066540`
のまま。specification、`.miz`、expectation、
sidecar、trace、production/test source、Cargo metadata、coverage stateは
変更しない。chapter coverage、traceability、owner crate、deferred statusを
変更しないため`spec_coverage_audit.md`もunchanged。
independent final read-only quality reviewは**NO FINDINGS**、全9 hard gates
PASS、score capなし、valid `100/100`
（`20/20/15/15/10/10/5/5`）。

### R-032A implementation status

R-032Aはfrozen structural arenaをexact Rust 3 filesに実装し、
source/documentation reviewが特定したexact 17 live design-status recordを
同期する。resolverはcomplete dense same-index lowering、exact provenance、
bounded accessor、fail-closed validation、public forward-compatible error
tableを所有し、label collection / semantic resultは所有しない。focused unit
test 8件とdownstream-style rustdoc test 1件がcomplete
mapping/error/precedence/helper/determinism contractをcoverし、R-026 lint
tableはauthorizeされたnew decisionだけを追加する。

initial reviewはMedium equivalent-input `test_gap` 1件、Medium 32-bit test
portability `source_drift` 1件、High live-status `design_drift` 1件を発見し、
すべて修正済み。test-sufficiency / implementation re-reviewは
**NO FINDINGS**。R-032A完了時点ではR-032B streamが次となり、そのfirst
logical taskはseparate lint-policy docs correctionだった。R-032Aはactive `.miz`
mapping、trace/backlink/status/count、owner crate、deferred status、
coverage creditを変更しないため、`spec_coverage_audit.md`はunchanged。

final pre-commit verificationはfocused R-032A 8 tests、全
`mizar-resolve` tests（unit `119`、lint-policy `11`、rustdoc `1`）、
`cargo fmt --check`、
`cargo clippy --all-targets --all-features -- -D warnings`、full workspace
`cargo test`をPASS。plan/parse/declaration/type/proofのfive CLI hashは
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`、
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`、
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`、
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`、
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`
のまま。countはplan `419/387`、pass/fail `228/191`、active
parse/declaration/type/proof `101/5/198/1`、warnings/errors `23/0`のまま。

resolver library listは`119` testsとなり、raw/normalized hashは
`429e6a470d00316a7b12d5659c2b7609aca726d368f0637a149b7b109a6ad560` /
`f32a0e2768c986114b5bbaacde3ef5a9c9a8fb7e464d74ebd54f85f619d09c11`。
production manifestは15 pathsとunchanged path hash
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29`
を維持し、18,261 lines、ordered content hash
`7afdd9951182064b21d1be88a0a5330491c972fb78b1917e6e6ad68f3485e369`
となる。checker、runner、syntax productionとtest-list hashはpreceding
lint-policy correctionが記録したexact値のまま。final
`git diff --check`、explicit staging、cached-diff review、independent quality
review、commit、post-commit/fresh-inventory gateはこのpre-commit record時点で
pending。

### R-032B lint-policy frozen-scope correction（completed prerequisite record）

fresh R-032B implementation inventoryで、planned public
`ProofLabelSourceCollectionError`はmandatory R-026 guardに必ずscanされることが
判明した。旧two-Rust-file ownershipはHigh `design_drift`であり、semantic
`spec_gap`、`test_gap`、test-intent changeではない。later implementationは
exact `crates/mizar-resolve/src/labels.rs`、
`crates/mizar-resolve/src/labels/tests.rs`、
`crates/mizar-resolve/tests/lint_policy.rs`にfreezeし、last fileはsole
`ProofLabelSourceCollectionError` owning-spec decision
`spec_name: "labels.md"`だけを受けられる。

completed synchronized docs-only correctionのexact total scopeは31 design
files、resolver 16、checker 8、`mizar-test` 6、global ledger 1。
specification、semantic behavior、test intent、source、fixture、sidecar、
expectation、trace status/count、Cargo metadataは変更しない。chapter coverage、
traceability、owner crate、deferral、coverage creditを変更しないため、
`doc/design/spec_coverage_audit.md`はdeliberate no-op。independent
specification、test/scope、source/documentation consistency reviewはすべて
**NO FINDINGS**で、docs-only verification、count、hash gateはPASS。
independent final read-only quality reviewも**NO FINDINGS**で、全9 hard
gates PASS、capなし、valid `100/100`
（`20/20/15/15/10/10/5/5`）。そのpre-commit record時点ではtask-only
staging/cached-diff review、commit、post-commit invariant/fresh-inventory
gateだけがpendingだった。これらはdedicated correction commit
`f1cf0a5d15f2db51176e9e91a4f5a6447a88ad7a`とそのfresh inventoryで後に完了。

focused `lint_policy`は`11/11`、全`mizar-resolve` testはunit `119`、
lint-policy `11`、rustdoc `1`をPASSし、`cargo fmt --check`、
`cargo clippy --all-targets --all-features -- -D warnings`、full workspace
`cargo test`もPASS。CLI countはplan `419/387`、pass/fail `228/191`、active
parse/declaration/type/proof `101/5/198/1`、warnings/errors `23/0`のまま。
plan/parse/declaration/type/proof hashは
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`、
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`、
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`、
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`、
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`
のまま。

test-list countとraw/normalized hashはresolver `119`
`429e6a470d00316a7b12d5659c2b7609aca726d368f0637a149b7b109a6ad560` /
`f32a0e2768c986114b5bbaacde3ef5a9c9a8fb7e464d74ebd54f85f619d09c11`、
checker `430`
`5dc6cff8c93d86911dca85f91da81501ddf226c42fd6338f4c4be6105782132e` /
`d7eb7a0d48d2c11b9c3f3b00ca025e1c7a1d5ce9b2b767ca94c2655c5d2dbf27`、
runner `500`
`94aa81ba9af645c9de1e927aa06bf8d525e3510509a607074e604eafc00ff995` /
`e0d976ab223f0ac0c1b48bd9926bb3fcf785706bdd4a24ecfd0633c81f66f943`、
syntax `59`
`512775259a51121a0c12ab9fbf0d1083273d3d140362d889cdd9e22184215da6` /
`c11a29c90fee3fe81d839f80f196dcd405cc43c7da86e83f37dc123042066540`
のまま。

production path/line countとpath/content hashはresolver `15/18261`
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`7afdd9951182064b21d1be88a0a5330491c972fb78b1917e6e6ad68f3485e369`、
checker `23/145097`
`c2eea2db9187c48dd830a010eff37f09b90467f9012a9fe6b3ac669b6d1dac42` /
`c39d43229e85e6136597f0f6cd52c15e1ab1d2057cf7866f6bbbf244307250dc`、
runner `30/59745`
`98f3b264a59fed5b08c3e8f20e7ca58ff54efaa154eab16a7572a69ce923f275` /
`75d3e70b1eb6a5871486c1dc6b0ccde06aec4b0d3e23a1b4c5eecf33dfb9039b`、
syntax `6/4314`
`27622a38347b8b5f94559b9587d227b944c0f680c2f7729c115e7743253eb733` /
`fec701d87a95506058a6a7ceb4e500786084f4b00646df0bf3a43624a18617b8`
のまま。exact scopeは31 design filesを維持し、
`doc/design/spec_coverage_audit.md`はdeliberate no-opのまま。

### R-032B implementation result

dedicated correction commit
`f1cf0a5d15f2db51176e9e91a4f5a6447a88ad7a`とfresh inventory後、R-032Bは
frozen public `ProofLabelSourceCollector<'a>`、
`ProofLabelSourceCollection`、non-exhaustive
`ProofLabelSourceCollectionError`をexact
`crates/mizar-resolve/src/labels.rs`に追加した。focused evidenceは
`crates/mizar-resolve/src/labels/tests.rs`だけが所有し、
`crates/mizar-resolve/tests/lint_policy.rs`はsole R-026
`ProofLabelSourceCollectionError` / `labels.md` decisionだけを受ける。

collectorはconstruction時とcollection時にR-032A arenaをvalidateし、AST/arena
borrowだけをstoreしてnamespace/contributionを所有し、constructor moduleは
storeしない。exact default-deny direct-edge traversalはtheorem-rootとnested
proof scope、module-global one-based statement ordinal、whole-subtree completion
boundary、exact simple citation candidate、richer structural origin、
length-framed `proof-step-v1` identityを導出する。unsupported、recovered、
wrapped、semantic、qualified、grouped、bulk、template formはrow、ordinal、
descentを生成しない。collectorはexisting `LabelResolver`へ供給し、resolutionを
重複実装せずproof/checker semanticsも作らない。

preimplementation specification reviewは**NO FINDINGS**。test-sufficiency
streamはinitial High/Medium gapとfresh 2件のMedium gapをすべて修正し、final
fresh rereviewは**NO FINDINGS**。implementation streamはMediumの無関係な
third-child依存と、Mediumのunauthorized `Default` / `From` findingsを修正し、
final fresh rereviewは**NO FINDINGS**。final fresh source/documentation
consistency rereviewも**NO FINDINGS**。

final pre-commit verificationはfocused collector matrix `25/25`、complete
labels matrix `35/35`、全`mizar-resolve` tests（unit `144`、lint-policy
`11`、rustdoc `1`）、`cargo fmt --check`、workspace
`cargo clippy --all-targets --all-features -- -D warnings`、full workspace
`cargo test`、`git diff --check`をPASS。

CLI countはplan `419/387`、pass/fail `228/191`、active
parse/declaration/type/proof `101/5/198/1`、warnings/errors `23/0`のまま。
plan/parse/declaration/type/proof hashは
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`、
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`、
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`、
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`、
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`
のまま。

test-list countとraw/normalized hashはresolver `144`
`727e09ff53088f3cb5b7a0194789a109bcfe459789d0757124a32ff4346f9b29` /
`8cf9e92ddf79231302390978f600b23aa7db7428f552eb72e4d8990b29558bf2`、
checker `430`
`5dc6cff8c93d86911dca85f91da81501ddf226c42fd6338f4c4be6105782132e` /
`d7eb7a0d48d2c11b9c3f3b00ca025e1c7a1d5ce9b2b767ca94c2655c5d2dbf27`、
runner `500`
`94aa81ba9af645c9de1e927aa06bf8d525e3510509a607074e604eafc00ff995` /
`e0d976ab223f0ac0c1b48bd9926bb3fcf785706bdd4a24ecfd0633c81f66f943`、
syntax `59`
`512775259a51121a0c12ab9fbf0d1083273d3d140362d889cdd9e22184215da6` /
`c11a29c90fee3fe81d839f80f196dcd405cc43c7da86e83f37dc123042066540`。

production path/line countとpath/content hashはresolver `15/18871`
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4288b37b1ba0af59496ab1468f1df30aec95fa0527682b511ed471d780adcfac`、
checker `23/145097`
`c2eea2db9187c48dd830a010eff37f09b90467f9012a9fe6b3ac669b6d1dac42` /
`c39d43229e85e6136597f0f6cd52c15e1ab1d2057cf7866f6bbbf244307250dc`、
runner `30/59745`
`98f3b264a59fed5b08c3e8f20e7ca58ff54efaa154eab16a7572a69ce923f275` /
`75d3e70b1eb6a5871486c1dc6b0ccde06aec4b0d3e23a1b4c5eecf33dfb9039b`、
syntax `6/4314`
`27622a38347b8b5f94559b9587d227b944c0f680c2f7729c115e7743253eb733` /
`fec701d87a95506058a6a7ceb4e500786084f4b00646df0bf3a43624a18617b8`。
exact task scopeはRust owner 3件とlive-status record 17件の計20 files。
independent final read-only qualityは**NO FINDINGS**、全9 hard gates PASS、
score capなし、valid `100/100`（`20/20/15/15/10/10/5/5`）。task-only
restaging/cached-diff review、dedicated commit
`b3a7e79a6b60db2974e911c69bb56ff5f4609064`、post-commit invariant/fresh
inventoryはcomplete。

R-032Bでは`.miz` fixture、expectation、sidecar、trace row/status/count、active
runner、checker handoff、public diagnostic code、Cargo metadata、
checker/type/proof/Core/CFG/VC behaviorを変更しなかった。current production
consumerはprivate `mizar-test` B5C
`declaration_symbol.label.proof_scope_confinement`だけで、public checker
`SourceStatementReferenceHandoff`は除外されたまま。
`doc/design/spec_coverage_audit.md`はactive mapping、traceability、owner crate、
deferral、coverage creditのいずれも変わらないためdeliberate no-opのまま。

### Checker Task 258B5C active implementation status

R-032Bは`b3a7e79a6b60db2974e911c69bb56ff5f4609064`でcommit済み。historical B5Cは
unchanged R-032A `SurfaceResolvedArena`とR-032B
`ProofLabelSourceCollector` / `LabelResolver` APIを`mizar-test`でprivateに
consumeする。exact fail `.miz` fixture 2件、expectation sidecar 2件、covered
trace row 2件を追加する。exact `mizar-test` source/test consumerは
`crates/mizar-test/src/runner/declaration_symbol.rs`、
`crates/mizar-test/src/runner/tests.rs`、
`crates/mizar-test/src/runner/tests/declaration_symbol.rs`、
`crates/mizar-test/tests/metadata.rs`で、last fileのfrozen active-count/CLI
assertion 4件をdeclaration stage `5`から`7`へ更新する。
resolver production sourceとpublic APIは変更しない。

active planは`421/389`、pass/failは`228/193`、active
parse/declaration/type/proofは`101/7/198/1`、warnings/errorsは`23/0`。
public diagnostic codeは空のままで、private key
`declaration_symbol.label.proof_scope_confinement`だけがrouteをauthenticate
する。R-G007で閉じるのはinner-to-outer/sibling confinement negativeだけ。
import/name/dot-chain/other label-reference coverageはopenのまま。B5Cは後に
`33ac57e96f048dc40559565f54369cac854409a7`としてcommit済み。
test、implementation、source/documentation reviewと全verification gateは
完了。independent final qualityは**NO FINDINGS**、全9 hard gates PASS、
score capなし、valid `100/100`（`20/20/15/15/10/10/5/5`）。B5Cは
`33ac57e96f048dc40559565f54369cac854409a7`としてcommitされ、post-commit
fresh inventoryもpassしたhistorical completed checkpointである。

## Checker Task 263R frozen selector-owner resolver prerequisite

post-Task-262 fresh inventoryでChecker Task 263を選択した後、mandatory parser/
resolver preflightがlower-stage defectを検出した。future Task-263 exact sourceは
320 bytes、SHA-256
`078eaee4b17341c9d8ebeb8a1f631ca984873bd07eb4e5d9c1a9486b39ac6671`。
final LFを含むliteral oracleは次のとおり。

```mizar
definition
  struct Task263Base where
    field carrier -> set;
    property marker -> set;
  end;

  struct Task263Derived where
    field carrier -> set;
    property marker -> set;
  end;

  inherit Task263Derived extends Task263Base where
    field carrier from carrier;
    property marker from marker;
  end;
end;
```

frontend diagnostic 0、stored Surface node 75、declaration shell 10、
symbol projection 8だが、preimplementation collectionはsymbol 8件を保持しつつ
`carrier`/`marker`へ誤った`DuplicateDeclaration` 2件を出していた。

canonical authorityはChapter 5 §§5.2-5.5、特にper-structure member、root
declaration/path-view identity、`carrier`を繰り返すmultiple-structure example。
既存`pass_parser_structures_001`はparser-shape/control evidenceだが、selectorを
別structureで反復せずresolver conflict classificationも実行しない。owner-sensitive
authorityはChapter 5 §5.4である。Chapter 16はここでnew resolver proof obligationを
認可しない。disagreementはpreimplementation `symbols.rs`の
`source_drift`、name-level selector duplicate ruleの`design_drift`、canonical-derived
`test_gap`。blocking `spec_gap`、test-expectation drift、authority-free semantic
decisionはない。origin divergence `0/5`はreport-only `repo_metadata_conflict`、
protected `stash@{0}`は
`f65cf4a13752ec380710814a9ac6392ccb9d75d4`のまま変更しない。

implementation scopeはexact `crates/mizar-resolve/src/symbols.rs`と
`crates/mizar-resolve/src/symbols/tests.rs`。production ownerはnon-overloadable
conflict keyへinternal nearest-`StructureDefinition` selector-owner partitionだけを
追加し、missing ownerはone conservative partitionへfail closedする。extractor-backed
resolver test 2件がcross-structure coexistence、same-structure field/property collision、
projection-order determinismを証明し、既存duplicate-family testがnon-selector/
missing-owner policyを維持する。public API、
module、dependency、diagnostic class/code、fixture、sidecar、expectation、trace
row/status/count、active runner、checker source、Cargo metadataは変更しない。

test名は`selectors_in_distinct_structure_owners_do_not_conflict`と
`field_and_property_in_same_structure_keep_duplicate_conflict`にfreezeする。前者は
definition-block shell 0にliteral oracleのstructure subtree 2件をbuildし、structure
shell 1/4がそれぞれfield shell 2/5とproperty shell 3/6をownする。extractionは
structure symbol 2件/selector symbol 4件を生成し、その6-projection inputをreverseしても
symbol 6件、diagnostic 0、selector definition conflictなしを期待する。既存ownerless
explicit-projection duplicate testはnon-selector/missing-owner controlとして維持する。
後者は次のexact 114-byte/final-LF control、SHA-256
`f020065dfd37f637ac73ad0581efaa990dbf28b3e541ea3e5454a5fd7959477b`を使う。

```mizar
definition
  struct Task263Collision where
    field duplicate -> set;
    property duplicate -> set;
  end;
end;
```

measured profileはSurface node 30、shell 4、projection 3、symbol 3、
`DuplicateDeclaration` 1件。structure shell 1がfield shell 2/property shell 3をownし、
diagnosticはshell 2/range `47..70`へanchor、candidateはsource order、selector
definition 2件は両方`DuplicateSpelling`を持つ。projection inputをreverseしてもresultを
維持する。

docs prerequisiteではresolver test `144`、raw/normalized hash
`727e09ff53088f3cb5b7a0194789a109bcfe459789d0757124a32ff4346f9b29` /
`8cf9e92ddf79231302390978f600b23aa7db7428f552eb72e4d8990b29558bf2`、
production `15/18871`、path/content hash
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4288b37b1ba0af59496ab1468f1df30aec95fa0527682b511ed471d780adcfac`
を保持。implementationはresolver test `144 -> 146`をprojectし、全corpus/CLI/
checker/runner/syntax count/hashは不変。coverage auditはChapter-5 lower owner/
deferralだけを記録し、executable corpus creditを与えない。

exitは全review NO FINDINGS、exact probe diagnostic 0、same-owner collision exact
deterministic diagnostic 1件、全resolver/workspace gate PASS。本docs-only
prerequisiteを先にcommitし、fresh inventory後Task 263Rを別commitで実装して、
checker production/structure semanticsを混ぜずTask 263へ戻る。

## Checker Task 263R documentation-prerequisite verification

repeated specification review、test-sufficiency review、source/documentation
boundary reviewは**NO FINDINGS**。literal probe/exact test schemaを再現可能にし、
Chapter 5 §5.4をowner-sensitive authorityとし、historical R-032B/B5C statusを
同期し、later implementationまでauditのsource/test gapをopenに保つことで、全prior
`design_drift` findingを解消した。deltaはexact design file 28件で、executable source、
Rust test、corpus artifact、trace metadata、Cargo metadataは変更しない。

resolver test `144 + 11 + 1`、`mizar-test` metadata/lint/public-enum policy
`137 + 14 + 2`はPASS。workspace format、warnings-denied Clippy、full workspace
test、Cargo metadata、`git diff --check`、CLI 5本もPASS。CLI hashはplan/parse/
declaration/type/proof順に
`77f54c338ccd56266afaf4f85e4e3e1546230202d3787712e9826dfb40f7186e` /
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56` /
`71e83ba0b20d4015e07b3bd2c0c4db2837b6151d1251812caed7954530d53c74` /
`4b638a91010f923fc6c62d8fa025e86a7a47958268f18fb7fc32cfc62a154dec` /
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`
のまま。

library inventoryはchecker/runner/resolver/syntax `458/524/144/59`。
resolver productionはfrozen path/content hash付き`15/18871`、trace hashは
`3f510f819f03a3fd2922275b37ab71070e41d4f8e2e0e9c0c94147076552626a`のまま。
active metadataは`425/393`、`232/193`、`101/7/202/1`、type
`257 = 245 covered + 12 deferred`、warnings/errors `23/0`。independent final
read-only qualityは**NO FINDINGS**、全9 hard gate PASS、score capなし、valid
`100/100`（`20/20/15/15/10/10/5/5`）。exact staging/commitとpost-commit
inventoryがpre-implementation gateとして残る。

## Checker Task 263R implementation result

documentation prerequisiteは
`34692ee222d5465750f061da82fe878566a1557c`としてcommitされ、その
post-commit inventoryはcleanでprotected stashも不変だった。fresh inventory後、
frozen correctionをexact `src/symbols.rs`と`src/symbols/tests.rs`だけに実装した。
`CollectedProjection`はprivate optional selector ownerを持ち、declaration-shell
parentをnearest enclosing `StructureDefinition`までwalkして導出する。このownerは
non-overloadable selector conflict keyだけに参加する。owner欠落/missing ancestryは
`None`のままで、全ownerless selectorは引き続きone conservative partitionを共有する。
non-selector、overload/redefinition policy、symbol identity、diagnostic class/range、
recovery、projection orderingは不変で、public API/dependency変更はない。

frozen extractor-backed test 2件を追加した。320-byte cross-owner probeは
Surface/shell/projection/symbol `75/10/8/8`を維持してresolver diagnostic 0となる。
114-byte same-owner collision controlは`30/4/3/3`とexactly one deterministic
`DuplicateDeclaration`を維持し、frozen `47..70` anchor、candidate order、
`DuplicateSpelling` markも一致する。forward/reversed projection inputのconflict
vectorは同一である。resolver library testは`146`、raw/normalized test-list hashは
`954470cb5d545d3f42df1d411341757b10cc6b8023443d4dfb218e0054873fb6` /
`6bb84dffd143e8e3d6ff4716a502626d5fcb6f498d02d16b1828587cff12f3d0`。
resolver productionは15 filesのまま18,896 linesとなり、path/content hashは
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4e3687afdc06f06eb06dd5a8ee9690e502e7341b708ccb00596993c6b2781283`。

test-sufficiency、implementation、source/documentation consistency reviewは
**NO FINDINGS**。focused resolver test（`146 + 11 + 1`）、metadata/lint policy
（`137 + 14 + 2`）、formatting、full warnings-denied workspace Clippy、full
workspace test、Cargo metadata、CLI 5本、exact count/hash、exact probe 2件は
PASS。independent final read-only qualityも**NO FINDINGS**、全9 hard gate PASS、
score capなし、valid `100/100`（`20/20/15/15/10/10/5/5`）。exact staging、
implementation commit `997457dd3189030aa3b137b568ce82fed456fe1e`、post-commit
fresh inventoryは完了し、後続Checker Task 263も完了した。fixture、sidecar、expectation、trace
row/status/count、runner/checker source、metadata、CLI output、Chapter-5
executable corpus creditは変更しない。

## Checker Task 264R frozen property-implementation shell prerequisite

post-Task-263 fresh inventoryはChecker Task 264を選択したが、lower preflightにより
Parser Task 48が表現するtop-level property implementationを
`DeclarationShellCollector`が落としていることが判明した。authorityはChapter 7
§7.4.1、bounded §§7.8.2/7.10、Chapter 5のproperty-value source rule、Chapter 13
§13.1.2のmeans-only `it` restriction、既存parser pass/recovery fixtureである。
Chapter 16のoverlap-coherence ruleはresolver proof/
acceptance behaviorを認可しない。欠けたshellは`source_drift`、contract欠落は
`design_drift`、regression 2件は`test_gap`、blocking `spec_gap`はない。origin
divergenceはreport-only `repo_metadata_conflict`である。

exact implementation scopeは次のとおり。

- `crates/mizar-resolve/src/declarations.rs`にpublic non-exhaustive
  `DeclarationShellKind::PropertyImplementation`を追加し、表現済みnodeを
  parentless/source-ordered/range/recovery-preserving shellとしてcollectする。
- `crates/mizar-resolve/src/symbols.rs`はこのshellをcontext-onlyとし、signature
  projection/semantic identityをextractしない。
- `crates/mizar-resolve/src/declarations/tests.rs`と`src/symbols/tests.rs`に
  exact resolver test 2件、parser-backed shell/recovery preservationとfabricated identity/
  symbol-collector/downstream semantic effectの不在を追加する。

pass oracleは`pass_parser_property_implementations_001.miz`のexact
566 bytes/28 lines/hash
`8ba7fdbfac71d7620cdda1a6075f0662aa1a05b93baf4b66179c4a2997d0600e`、sidecar
hash `bda8ccdd7c24d389b7b0b42719d77ffed42767b44c13a4d1c1ff2699ce6f90d8`。
frontend diagnostic 0とSurface/property implementation/parameter/formula definiens/term
definiens/correctness/modeの`194/4/4/2/2/6/1`を保存し、shellだけ`2 -> 6`、
projection/symbol/definition/resolver diagnosticは`1/1/1/0`のまま。

recovery oracleは`fail_parser_property_implementations_recovery_001.miz`のexact
335 bytes/24 lines/hash
`410d0ad057239db55cfa22f2134a5101ac5fa21668e26e274041289248f82735`、sidecar
hash `7236f57c3a5b2b79e703abb8f5e7cba2c5cc1c117a146001ac161edd4b24acba`。
frontend diagnostic 13、profile `109/3/3/1/2/5/1`を保存し、shellだけ`3 -> 6`、
projection/symbol/definition/resolver diagnosticは`2/2/2/0`、後続theorem shellも保存。
inactive `fail_mode_property_overlap_missing_coherence_001.miz`はbyte-identical/
inactiveのままでexecutable credit外。

recovery oracleはさらにpreimplementation theorem identityのowner `theorem#1`、origin
structural path `[2, 1]`、range `304..334`、`DefinitionId(1)`、source contribution
`0`、existing contribution effectsをfreezeする。property-implementation shellはsemantic
sibling ordinalから除外し、contribution anchorは最初のnon-property-implementation shell、
それがなければ`Point(0)`を選ぶ。shell 6件でもこれらのidentity/profileをexactに保存する。
enum variantは`VisibilityWrapper`の後ろにappend-onlyで、stable code/keyは`28` /
`property-implementation`、existing `0..27` code/keyは不変。

parameter/defining-mode-context interpretation、property lookup/provenance、`means`/`equals`区別、
means-only `it` descendant、
referenced-property return-type lookup、definiens lowering、resolver signature、overload、registration、
dependency、lexical summary、initial obligation、coherence goal、proof/discharge/acceptance/
fact/VC、Typed/Resolved ownership、runner consumer、fixture、sidecar、expectation、trace
row/status/count、Cargo metadataは変更しない。source grammarにad-hoc `assume`
subtreeもimplementation-owned return typeも持たない。Task 264Rは`it`をinterpretせず、
後続Task 264は`means`だけに許可し`equals`には許可しない。shellを`Selector`、`PropertyClause`、
`PropertyRedefinition`、`RegistrationKind::Property`で代用しない。

docs prerequisiteはresolver test 146、raw/normalized hash
`954470cb5d545d3f42df1d411341757b10cc6b8023443d4dfb218e0054873fb6` /
`6bb84dffd143e8e3d6ff4716a502626d5fcb6f498d02d16b1828587cff12f3d0`、production
`15/18896`、path/content hash
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4e3687afdc06f06eb06dd5a8ee9690e502e7341b708ccb00596993c6b2781283`を保存する。
implementationはresolver test `146 -> 148`のみprojectし、corpus/checker/runner/
syntax/metadata/CLI/trace/coverage count/hashは不変。auditにlower owner/deferralのみ
記録し、executable creditは与えない。

exitは全review NO FINDINGS、exact profile 2件、focused resolver test、crate/workspace/
lint/metadata/format/Clippy/CLI/count/hash gate、task-only stage、dedicated implementation commit。
docs prerequisiteを先にcommitし、fresh inventory後Task 264Rをimplement、その後別の
Checker Task 248P binding-context prerequisiteを完了してからTask 264本体をfreezeする。

## Checker Task 264R documentation-prerequisite verification

canonical no-`assume`、referenced-property return type、means-only `it`、sync ledger、
LocalSource anchor、semantic-sibling fingerprint、append-only code/key、collector-effect
wordingの修正後、repeated specification reviewとindependent boundary/consistency reviewは
**NO FINDINGS**。final read-only qualityも**NO FINDINGS**、全9 hard gate PASS、
score capなし、valid `100/100`（`20/20/15/15/10/10/5/5`）。deltaはexact design
document 36件で、source/test/fixture/sidecar/expectation/trace/Cargo diffはゼロ。

resolver library/lintは`146/11`、`mizar-test` lint/metadataは`14/137`でPASS。
`cargo fmt --all --check`、all-target/all-feature warnings-denied Clippy、full workspace
`cargo test --no-fail-fast`、Cargo metadata、exact pass/recovery AST probe、
`git diff --check`はPASS。CLI stdout hash 5件はplan/parse/declaration/type/proofの順に
`5b136ab6861640034de9f04c2ed8ca38697428ac481dec914f48de4308a0324a` /
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56` /
`71e83ba0b20d4015e07b3bd2c0c4db2837b6151d1251812caed7954530d53c74` /
`70e7e2940c13007845ab9cf1770429b8c14b2cf0042155bf3cdf39b10ee9a31b` /
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`のまま。
metadata `426/394`、pass/fail `233/193`、stages `101/7/203/1`、type
`258 = 246 + 12`、warnings/errors `23/0`、trace hash
`cf0ef6d28a132bcbafc8aa1214ded935a715fdffdb3421c37d66c35954f2a06c`も不変。
origin divergenceはreport-only `0/11`、protected stash
`f65cf4a13752ec380710814a9ac6392ccb9d75d4`は不変。exact docs-only staging/
commit/post-commit fresh inventoryはparent-owned gateとして残る。

## Checker Task 264R implementation result

docs prerequisite commit
`b1ed8ea19f8845d8c54f795a7375d4add4af237d`後のclean fresh inventoryから、
frozen resolver 4 filesだけを変更した。context-only shell kind/code/keyをappendし、
source order/range/recovery、semantic sibling fingerprint、`LocalSource` anchorを保存し、
projection/semantic identityを作らない。同じexact 2 testsがParser Task 48 fixtureの
bytes/linesとproperty provenance、following theoremのexact identity/effects、leading-
property/property-only anchor、全禁止index/effect family、`0..28` code/key tableをfreezeする。

resolver library testsは`148`、raw/normalized hashは
`c99d9d179cf14ab9ccd274b11d0404bdc47a64d23a2aa914c69ba674d01a3fee` /
`1c76831124b1e680d708fd30ddfa7a96959aa82d20c840594dbb108dcd063490`。
productionは`15/18906`、path/content hashは
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4953d62ee92da662177cdbaf161afee6a55e422160913cf99fc37bc56cb8810a`。
test-sufficiency/implementation rereviewは**NO FINDINGS**。focused/crate/lint/
metadata/format/warnings-denied Clippy/full workspace/Cargo metadata/5 CLI/
exact parser-resolver probe/count/hashはPASS。corpus/checker/runner/trace/Cargo/
coverage creditは不変で、次のdependency-ready taskはChecker Task 248Pである。
