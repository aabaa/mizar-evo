# Crate Plan: mizar-resolve

> Canonical language: English. Japanese companion: [../ja/00.crate_plan.md](../ja/00.crate_plan.md).

## Responsibility

Owned behavior:

- consume `SurfaceAst` and phase-0 module-index input to produce `ResolvedAst`
  and `SymbolEnv`;
- resolve module paths, imports, exports, namespaces, labels, and stable symbol
  identities;
- collect declaration signatures and dependency-facing module summaries without
  type checking;
- preserve recoverability by representing unresolved and ambiguous references
  explicitly;
- keep output ordering, ids, debug renderings, and diagnostics deterministic.

Out of scope:

- syntax ownership, parser recovery, or AST vocabulary changes;
- type inference, overload winner selection, cluster firing, or coercion
  insertion;
- proof validity, VC generation, ATP interaction, kernel acceptance, and
  artifact emission;
- package manifest parsing, lockfile resolution, source loading, and module
  discovery owned by `mizar-build` and `mizar-session`;
- driver lifecycle, salsa/query integration, and phase registry ownership.

## Readiness

`mizar-resolve` can start with tasks 1-7 from
[todo.md](./todo.md). `mizar-build` wave A is complete, so the resolver-side
module-index seam can be specified against the build-side provider from
[module_index.md](../../mizar-build/en/module_index.md). The parser/syntax
surface is current through `mizar-parser` task 45; task 46 is deferred and does
not block the resolver foundation.

The crate is not ready for full user-facing resolver diagnostics until the
name/import/label diagnostic adoption gap in
[architecture 03](../../architecture/en/03.module_and_symbol_resolution.md),
[spec chapter 22](../../../spec/en/22.error_handling_and_diagnostics.md), and
`mizar-diagnostics` is resolved or explicitly deferred by the relevant task.
The shared diagnostic registry reserves a `Resolution` family, but concrete
resolver-owned semantic names, numeric descriptors, migration behavior, and
consumer projections remain future integration work.

## Specification Items

| Spec ref | Requirement | Status |
|---|---|---|
| `spec.en.11.symbol_management.scope_visibility` | Semantic lookup order covers local bindings, current-module declarations, imports, re-exports, and built-in prelude candidates. | R-014 covers current-module declaration-point filtering, imported public visibility, current-module shadowing, overload placeholders, and builtin fallback over preliminary projections. R-016 covers local term shadowing for dotted chains using lexical `LocalTermScope`; full source-walk integration remains later paired work. |
| `spec.en.11.symbol_management.import_conflicts` | Same-spelling import conflicts are semantic errors unless qualification or aliasing resolves them. | Applicable from import/name tasks; parser/lexical tests cover only syntax and active-lexicon setup. |
| `spec.en.11.symbol_management.signatures` | Signature collection registers declarations, visibility, aliases, overload families, and public/private interface projections without type checking. | R-019 `symbols.md` specifies the declaration-pass contract; R-020 implements opaque collection over explicit projections; R-021 implements parser-backed per-kind extraction and lexical-summary seeds for represented source roles; R-023 adds active declaration-symbol corpus coverage for the initial parser-backed symbol path. |
| `spec.en.20.algorithm_and_verification.signatures` | Public algorithms expose signature and contracts, while bodies remain private to importers. | R-019 includes algorithm signature shells and keeps body lowering, VC generation, and termination decisions out of resolver scope. |
| `spec.en.12.modules.import_prelude` | Imports are file-scoped prelude declarations; aliases, branch imports, relative paths, and cycle rejection are resolver-visible. | Syntax coverage exists; semantic resolution coverage is planned. |
| `spec.en.12.modules.exports_visibility` | Export statements and public/private visibility define the module interface. | Parser coverage exists; semantic legality is planned. |
| `spec.en.15.statements.label_scope` | Statement labels are scoped to the enclosing reasoning block; same-scope duplicates and inner-scope shadowing are forbidden. | R-017 specifies the scope contract and R-018 implements the correct `LabelScopePath` prefix rule over explicit projections. R-032A structural mapping and the R-032B normal-source collector are implemented. Active B5C confinement coverage now privately consumes both unchanged APIs in `mizar-test`. |
| `spec.en.16.theorems_and_proofs.labels` | Theorem/proof labels have dedicated scopes and citation resolution. | R-017 `labels.md` specifies the resolver-owned label contract; R-018 implements theorem/lemma and proof-step label resolution over preliminary projections. R-032A validates structural mapping and R-032B collects the represented normal-source proof-step declarations and simple unqualified citations needed by Checker Task 258B5C. |
| `spec.en.22.error_handling_and_diagnostics` | Diagnostics need stable codes and source spans. | Shared `Resolution` family exists; resolver-specific name/import/label descriptor adoption remains `external_dependency_gap` / deferred. See R-G001 and R-030. |
| `arch.03.phase_4_5` | Resolver owns phases 4-5, the import graph, declaration shells, name resolution, and signature collection. | Canonical design input for all tasks. |
| `arch.03.step_1` | Build/package module index is phase-0 input; resolver consumes it and refines import edges. | Build-side slice complete; resolver-side seam is task 7. |
| `arch.01.resolved_ast_symbol_env` | `ResolvedAst` and `SymbolEnv` boundaries are immutable IR snapshots and checker inputs. | Drives tasks 2-6. |
| `mizar-build.module_index` | Build-side module identities and provider contract must remain alias-independent and source-id free. | Available for task 7; do not duplicate build-owned planning. |

## Relevant Tests

| Test path | Intent | Spec refs |
|---|---|---|
| `tests/lexical/pass/pass_lexical_import_prescan_*.src` | Import pre-scan and active lexical environment seeds. | `spec.en.12.modules.import_prelude` |
| `tests/lexical/fail/fail_lexical_import_prescan_*.src` | Malformed import prelude recovery before semantic validation. | `spec.en.12.modules.import_prelude` |
| `tests/miz/pass/parser/pass_parser_module_skeleton_001.miz` | Parser emits module-level skeletons consumed by resolver. | `spec.en.12.modules.compilation_unit_skeleton` |
| `tests/miz/pass/parser/pass_parser_import_items_001.miz` | Parser emits import item syntax including aliases and branches. | `spec.en.12.modules.import_items.parser` |
| `tests/miz/pass/parser/pass_parser_export_visibility_001.miz` | Parser emits exports and visibility modifiers. | `spec.en.12.modules.export_visibility.parser` |
| `tests/miz/fail/parser/fail_parser_import_*.miz` | Parser rejects syntactically malformed or misplaced imports. | `spec.en.12.modules.import_prelude` |
| `tests/miz/pass/resolve/pass_resolve_declaration_symbol_smoke_001.miz` | Active declaration-symbol runner smoke coverage that parser-backed declarations, visibility-bearing declarations, and theorem/lemma declarations reach symbol collection without internal diagnostics, with exact SymbolEnv-derived symbol and definition fact assertions for represented kind, visibility, and export status. | `spec.en.11.symbol_management.signatures`, `spec.en.11.symbol_management.visibility`, `spec.en.12.modules.visibility.semantic`, `spec.en.16.theorems_and_proofs.labels.declaration_symbols` |
| `tests/miz/fail/resolve/fail_resolve_duplicate_theorem_symbol_001.miz` | Active declaration-symbol fail coverage for same-scope duplicate theorem labels before proof checking. | `spec.en.16.theorems_and_proofs.labels.same_scope_uniqueness` |
| `tests/miz/fail/resolve/fail_resolve_same_signature_return_conflict_001.miz` | Active declaration-symbol fail coverage for the resolver-owned parser-backed functor signature conflict slice: same argument-signature functor declarations with different return signatures conflict during declaration/signature collection. | `spec.en.19.overload.definition_conflict.declaration` |
| `tests/miz/fail/resolve/fail_resolve_same_signature_same_return_conflict_001.miz` | R-031 active declaration-symbol fail coverage for the exact parser-backed ordinary-functor same-signature/same-return definition conflict. | `spec.en.19.overload.definition_conflict.same_return.declaration` |

Planned test-first additions:

- resolver unit tests for deterministic ids, arenas, tables, and provider seams
  during tasks 4-7;
- semantic `.miz` pass/fail cases beyond the R-023 declaration-symbol seed set
  as future runner assertion stages mature; the remaining corpus work is
  tracked by R-G002 and refined by R-G007;
- after the separate R-032A and R-032B lower prerequisites, Checker Task 258B5C active
  inner-to-outer and sibling proof-label confinement cases owned by the
  `mizar-test` `declaration_symbol` consumer;
- traceability entries for broader semantic import/name/label/symbol
  obligations when those future assertion stages exist;
- no existing `.miz` tests or expectations may be rebaselined merely to match
  resolver source behavior.

## Design And Source Inventory

Design files:

- [todo.md](./todo.md)
- [declarations.md](./declarations.md)
- [names.md](./names.md)
- [labels.md](./labels.md)
- [symbols.md](./symbols.md)
- [recovery.md](./recovery.md)
- [source_spec_correspondence.md](./source_spec_correspondence.md)
- [bilingual_documentation_synchronization.md](./bilingual_documentation_synchronization.md)
- [module_boundary_refactor.md](./module_boundary_refactor.md)
- [module_summary_reuse.md](./module_summary_reuse.md)
- [crate_exit_report.md](./crate_exit_report.md)
- [architecture 03](../../architecture/en/03.module_and_symbol_resolution.md)
- [architecture 01](../../architecture/en/01.ir_layers.md)
- [mizar-build module index](../../mizar-build/en/module_index.md)
- [mizar-build planner](../../mizar-build/en/planner.md)
- [mizar-frontend todo](../../mizar-frontend/en/todo.md)
- [mizar-test staged model](../../mizar-test/en/staged_model.md)

Source files:

- `crates/mizar-resolve/` is a workspace crate with manifest, crate-level
  documentation, lint-policy guard, and foundation modules.
- Current resolver source surface: `src/resolved_ast.rs`, `src/env.rs`, the
  task R-007 `src/module_index.rs` input seam over the build-side provider
  contract, and the task R-009/R-010 `src/imports.rs` import graph and
  source-shaped path-resolution seam, plus the task R-011
  `src/declarations.rs` source-shaped declaration-shell collector and the
  task R-013/R-016 `src/names.rs` namespace, symbol-name lookup, and dot-chain
  finalization slices plus private `src/names/diagnostics.rs` internal
  name-diagnostic assembly, plus the task R-018
  `src/labels.rs` theorem/lemma and proof-step label-resolution slice, plus
  the task R-020/R-021 `src/symbols.rs` symbol-collection and parser-backed
  signature-projection slices, plus the task R-022 `src/recovery.rs`
  recovered-syntax helper used by stage-specific call sites.
- R-029 keeps public module roots in `src/*.rs`, moves deterministic snapshot
  helpers to private `src/env/snapshot.rs` and
  `src/resolved_ast/snapshot.rs`, moves resolved-AST validation helpers to
  `src/resolved_ast/validation.rs`, and moves inline unit tests to private
  per-module `tests.rs` files.
- Current upstream source surface: `crates/mizar-syntax`, `crates/mizar-parser`,
  `crates/mizar-frontend`, `crates/mizar-session`, and `crates/mizar-build`.
- Direct `SurfaceAst` declaration-shell collection exists for represented
  declaration-like items and export projections. Executable namespace lookup
  exists for source-shaped namespace path candidates, executable symbol-name
  lookup exists over preliminary `NameSymbolProjection` records, and
  crate-local/internal name diagnostic reports are generated from
  unresolved/ambiguous name and namespace records. Source-shaped dotted chains
  are finalized with lexical local-term shadowing, and task R-018 adds
  executable theorem/lemma plus proof-step label resolution over preliminary
  `LabelProjection` records. R-019 specifies signature collection in
  `symbols.md`, R-020 implements the opaque declaration-symbol collection
  skeleton over explicit projections, and R-021 implements represented
  parser-backed signature extraction plus resolver-local lexical-summary data
  shapes. R-022 adds the cross-stage recovered-syntax policy and suppresses
  dependent name, label, and symbol diagnostics from recovered origins or
  shells while retaining degraded table/env facts. Direct import/export
  semantic validation, artifact-backed summaries,
  grouped shared-prefix lowering, and driver/corpus label and symbol lowering
  remain planned follow-on work beyond R-022. R-023 added declaration-symbol
  corpus collection only; it did not add production `SurfaceAst` proof-label
  declaration/reference projection.

Observed behavior:

- `mizar-build` now provides deterministic `BuildPlan` and `ModuleIndex`
  construction without allocating session-local source ids.
- `mizar-session` owns source identity, normalized paths, and snapshot identity.
- `mizar-frontend` provides lexical environment and parser orchestration seams;
  semantic import validation remains resolver-owned.
- The executable semantic import pass is present up to the R-010
  source-shaped candidate seam: alias binding, relative-prefix interpretation,
  namespace/package binding, unresolved recovery, and canonical graph
  construction exist over unit-test candidates.
- The executable declaration pass is present up to the R-011 source-shaped
  collector slice: represented declaration-like `SurfaceAst` items, visibility
  wrappers, transparent annotation wrappers, recovered-shell state, and export
  projection shells are inventoried without semantic validation.
- `names.md` specifies the R-013 through R-016 name-resolution contract for
  scope, namespace-before-symbol lookup, visibility/shadowing, ambiguity, and
  dot-chain finalization. The executable names pass is present up to the R-013
  namespace lookup slice: import aliases, reserved namespace roots,
  package-name bindings, current-package fallback, recovered namespace paths,
  unresolved import-alias dependencies, ambiguous alias candidate targets,
  provider-error classification, and earliest failing segment ranges are
  represented. The executable names pass is also present up to the R-014
  symbol-name slice: preliminary symbol projections, declaration-point
  filtering, qualified/unqualified visibility, current-module shadowing,
  overload-group placeholder collapse, enabled builtin fallback, and
  deterministic `NameRefTable` outcomes are represented without selector type
  checking or overload winner selection. The executable names pass is also
  present up to the R-015 internal diagnostic slice: deterministic
  `NameDiagnosticRootId` allocation, primary/cascade records, unresolved
  import-alias roots, namespace/name dependent records, stable candidate
  payloads, and internal record ordering are represented without public
  diagnostic codes. The executable names pass is also present up to the R-016
  dot-chain finalization slice: `LocalTermScope`, `LocalTermBinding`,
  `DotChainCandidate`, and `DotChainFinalizer` classify dotted chains as
  checker-deferred selectors or namespace-qualified symbol references without
  selector validation. The R-017 label contract is specified in `labels.md`,
  and R-018 implements `LabelResolver`, `LabelScopePath`, `LabelProjection`,
  `LabelReferenceCandidate`, deterministic `LabelIndex` / `LabelRefTable`
  output, and crate-local/internal label conflict diagnostics. R-019 adds the
  `symbols.md` signature-collection contract, including algorithm signatures,
  exported summaries, and lexical-summary projections. R-020 adds
  `SymbolCollector`, `SymbolDeclarationProjection`, opaque `SignatureShell`
  population, duplicate/conflict diagnostics, overload grouping, registration
  indexing, and contribution tracking. R-021 adds
  `SignatureProjectionExtractor`, parser-owned opaque signature payloads,
  template-role preservation in owning declarations, and
  `ModuleLexicalSummaryIndex` seeds for exported lexer-visible spellings.
  R-022 adds a resolver-local recovered-syntax policy: recovered namespace and
  name/dot-chain references are retained without internal name diagnostic
  roots, recovered label projections do not emit conflict diagnostics, and
  recovered symbol projections retain `RecoveredShell` metadata without
  duplicate or illegal-overload diagnostics. R-025 adds a crate-root
  public-seam determinism regression over import graphs, name diagnostics,
  `ResolvedAst` snapshots, and `SymbolEnv` snapshots, complementing the
  module-local deterministic id/table/order tests. R-026 records the
  public-enum forward-compatibility decision beside every resolver-owned public
  enum family in the module specs and guards future public-enum additions in
  those spec-owned modules with a crate-local lint test. R-027 records the
  source/spec correspondence audit in `source_spec_correspondence.md`, with no
  unclassified blocking/high gaps found. R-028 records the bilingual
  documentation synchronization audit in
  `bilingual_documentation_synchronization.md`, with no remaining English /
  Japanese mismatch found for resolver design files. R-029 records the
  module-boundary refactor gate in `module_boundary_refactor.md`, with private
  helper/test splits only and no public API, behavior, diagnostic, rendering,
  artifact, or boundary change. Crate close-out is recorded in
  `crate_exit_report.md` with quality score 94/100 and full verification
  passing.
  Direct import/export semantic validation, artifact-backed summaries, grouped
  shared-prefix lowering, and corpus-level label/symbol lowering must still be
  driven from spec, tests, and design rather than reverse engineering.

## Known Gaps And Drift

| ID | Class | Evidence | Action |
|---|---|---|---|
| R-G001 | `spec_gap` refined to `external_dependency_gap` / deferred adoption | Architecture 03 requires stable user-facing resolver diagnostics. Spec chapter 22 and `mizar-diagnostics` now reserve a broad `Resolution` family, but concrete resolver name/import/label semantic descriptors, numeric code assignments or aliases, migration behavior, and consumer projections are not adopted. | Do not invent placeholder public resolver descriptors in implementation. R-013 deferred shared diagnostic adoption and R-015 keeps name diagnostics crate-local/internal. R-030 owns a future real adoption gate before any later user-facing resolver diagnostic integration. |
| R-G002 | `test_gap` | Historical: tests covered lexical/parser import and export syntax but not semantic resolver behavior. | Partially closed by R-023, the post-task-20 R-G007 signature-conflict slice, and exact SymbolEnv-derived pass payload assertions with three active declaration-symbol `.miz` fixtures, expectation sidecars, and `spec_trace.toml` requirements. The remaining concrete corpus assertion gap is refined by R-G007; continue growing semantic coverage in future runner assertion expansions without rebaselining existing tests. |
| R-G003 | resolved by R-024 | `ModuleSummary` artifact reuse originally depended on `mizar-artifact` task 5. R-024 now consumes the canonical `mizar-artifact` `ModuleSummary` reader and projects validated public summaries into resolver contribution indexes. | Keep resolver/artifact/build ownership separated; do not add resolver-owned artifact schemas, shims, fallback readers, or public diagnostics. |
| R-G004 | `boundary_violation` risk | `mizar-build` already owns package planning, module discovery, and build-side module-index provider construction. | Task 7 must consume or adapt the build-side provider contract without reimplementing planner/source-loading responsibilities in `mizar-resolve`. |
| R-G005 | design_drift | Historical: top-level immediate-next-work guidance described pre-wave-A `mizar-build` work before R-007. | Resolved through close-out, the 2026-07-02 roadmap sync, and R-024 completion; the top-level design TODO now records the current resolver milestone complete through R-024 and points the immediate queue at `mizar-test` foundation cleanup. |
| R-G006 | external_dependency_gap | `mizar-parser`/`mizar-syntax` exposes direct template parameters as declaration payload roles and nested template loci as parser-owned pattern surface, but no module-level scheme declaration shell is currently present. | R-021 preserves direct template roles in owning signature payloads, keeps nested loci in flattened parser-owned signature surface, and does not fabricate scheme/template module symbols. Clear when parser/syntax exposes a scheme declaration source role. |
| R-G007 | `test_gap` | R-023 activates the declaration-symbol runner with a symbol-collection smoke pass and same-scope duplicate theorem fail seed. Later bounded increments add signature-conflict facts and the active B5C inner-to-outer and sibling proof-label confinement negatives. The runner still does not assert import graph, namespace/name resolution, dot-chain, or other label-reference facts from tasks 9-19 at corpus level. | Keep R-023/R-027 and committed R-032A/R-032B complete. B5C closes only its two spec-derived confinement negatives through private `mizar-test` key `declaration_symbol.label.proof_scope_confinement`; keep public diagnostic codes empty and do not invent behavior beyond `doc/spec/en` or rebaseline existing tests. |

The Checker Task 258B5C inventory found a Medium `source_drift`: although the
resolver core correctly applied the `LabelScopePath` prefix rule to explicit
`LabelProjection` and `LabelReferenceCandidate` inputs, pre-R-032B production
source did not collect proof-step declarations and simple unqualified
citations from normal `SurfaceAst`. Treating R-023 as that full source walk
was `design_drift`; R-023 completed declaration-symbol corpus collection only.
R-032A and committed R-032B source close that bounded lower path. R-G001
remains a Low deferred `spec_gap` for public diagnostic adoption. Active B5C
now closes only the two bounded confinement negatives within R-G007.

No blocking contradiction prevents the completed foundation/name/label/symbol
tasks and hardening audit/policy work through R-029. R-G001 blocks only public
canonical resolver diagnostic-code commitments, R-G003 is resolved by R-024,
and R-G007 remains a non-blocking corpus assertion expansion gap for the
remaining import/name/dot-chain/label runner work.

The crate-wide close-out report passed all hard gates for non-deferred work and
kept R-024 deferred until `mizar-artifact` task 5 existed. R-024 is now
implemented as the resolver-side consumer of that canonical artifact contract.

## Task Decomposition

### Task R-001: Crate Scaffold And Lint Policy

Purpose: create the `mizar-resolve` workspace member and lint guard.

Spec refs:
- architecture 03 phase 4-5 responsibility
- internal crate layout policy

Tests:
- `cargo test -p mizar-resolve`
- lint-policy guard mirroring existing workspace crates

Affected design files:
- `doc/design/mizar-resolve/en/todo.md`
- `doc/design/mizar-resolve/ja/todo.md`

Affected source files:
- `Cargo.toml`
- `crates/mizar-resolve/Cargo.toml`
- `crates/mizar-resolve/src/lib.rs`
- `crates/mizar-resolve/tests/lint_policy.rs`

Completion condition:
- workspace builds with `mizar-session` and `mizar-syntax` dependencies;
- no resolver language behavior is introduced.

Forbidden behavior:
- parser changes;
- semantic resolution stubs that fabricate `SymbolId`s;
- user-facing resolver diagnostic-code decisions.

### Task R-002: `resolved_ast.md` Specification

Purpose: specify `ResolvedAst`, stable ids, reference tables, unresolved and
ambiguous representation, and recovered-shell rules.

Spec refs:
- architecture 01 `ResolvedAst`
- architecture 03 interface definitions

Tests:
- no code; identify planned data-shape tests for R-004.

Affected design files:
- `resolved_ast.md` in English and Japanese
- this crate plan if new gaps are found

Affected source files:
- none

Completion condition:
- English canonical and Japanese companion are synchronized;
- source/spec gaps are classified before implementation.

Forbidden behavior:
- deciding type-directed overload or selector behavior beyond resolver-owned
  name context.

### Task R-003: `env.md` Specification

Purpose: specify `SymbolEnv`, symbol and label indexes, declaration dependency
edges, namespace graph, contribution tracking, and invalidation notes.

Spec refs:
- architecture 01 `SymbolEnv`
- architecture 03 signature collection
- spec chapter 11 scope and visibility

Tests:
- no code; identify planned index and contribution-tracking tests for R-005.

Affected design files:
- `env.md` in English and Japanese
- this crate plan if new gaps are found

Affected source files:
- none

Completion condition:
- the checker-facing boundary is clear and does not include type inference.

Forbidden behavior:
- checker-owned facts, cluster firing, or overload winner selection.

### Task R-004: Implement `resolved_ast` Data Shapes

Purpose: implement deterministic ids, arena/table invariants, and typed accessors
from `resolved_ast.md`.

Spec refs:
- `resolved_ast.md`

Tests:
- id determinism;
- valid child ids and no arena cycles;
- node resolution state preservation and stable node-to-table/import keys;
- `NameRefTable`, `LabelRefTable`, `ResolvedImports` round-trips.

Affected design files:
- `resolved_ast.md` only if implementation reveals design drift.

Affected source files:
- `crates/mizar-resolve/src/resolved_ast.rs`
- `crates/mizar-resolve/src/lib.rs`

Completion condition:
- source matches the spec and narrow tests pass.

Forbidden behavior:
- assigning semantic meaning not specified by `resolved_ast.md`;
- modifying parser/syntax to fit resolver implementation.

### Task R-005: Implement `env` Data Shapes

Purpose: implement `SymbolEnv` indexes and source-contribution tracking from
`env.md`.

Spec refs:
- `env.md`

Tests:
- insert/lookup round-trips;
- deterministic ordering;
- contribution tracking per source unit.

Affected design files:
- `env.md` only if implementation reveals design drift.

Affected source files:
- `crates/mizar-resolve/src/env.rs`
- `crates/mizar-resolve/src/lib.rs`

Completion condition:
- checker-facing indexes exist without checker-owned semantics.

Forbidden behavior:
- type checking, registration activation, or proof obligation generation.

### Task R-006: Deterministic Debug Rendering

Purpose: add stable human-readable renderings for data-shape snapshots.

Spec refs:
- architecture 20 snapshot tests
- `resolved_ast.md`
- `env.md`

Tests:
- byte-identical repeated renderings;
- fixtures covering current node/table kinds.

Affected design files:
- `resolved_ast.md`
- `env.md`

Affected source files:
- `crates/mizar-resolve/src/resolved_ast.rs`
- `crates/mizar-resolve/src/env.rs`

Completion condition:
- debug output is deterministic and not a public artifact schema.

Forbidden behavior:
- embedding absolute paths by default;
- using render order as semantic order unless specified.

### Task R-007: Module-Index Input Contract And Interim Seam

Purpose: define and implement the resolver-side module-index input consumed by
import graph resolution, with a workspace-stub provider for resolver tests until
driver integration exists.

Spec refs:
- architecture 03 Step 1
- `mizar-build` `module_index.md`

Tests:
- stub provider feeds a multi-module fixture;
- canonical module identity is alias-independent;
- provider errors are deterministic.

Affected design files:
- resolver seam documentation or task-7 notes;
- top-level open decision if the seam is closed.

Affected source files:
- `crates/mizar-resolve/src/imports.rs` or a dedicated provider module;
- tests under `crates/mizar-resolve/tests/` or unit tests.

Completion condition:
- resolver-side input contract is clear;
- no `mizar-build` planner/source-loading responsibilities are reimplemented.

Forbidden behavior:
- resolver-owned compatibility shims inside `mizar-build`;
- dependency-summary artifact assumptions before `mizar-artifact` task 5.

### Later Milestone Groups

- Imports R-008 to R-010: semantic import path resolution, import graph, and
  cycle policy.
- Names R-011 to R-016: declaration shells, namespace/name lookup, ambiguity,
  and dot-chain finalization.
- Labels R-017 to R-018: dedicated label scopes, label reference tables, and
  executable theorem/lemma plus proof-step label resolution.
- Symbols R-019 to R-021: signature collection spec, collection skeleton,
  duplicate detection, parser-backed per-kind extraction, and lexical-summary
  seeds are complete for represented source roles.
- Hardening R-022 to R-029: recovered syntax, corpus runner, the R-024
  ModuleSummary external-dependency gate, determinism, enum policy, audits,
  and module-boundary refactor.
- Later bounded prerequisites: R-031 completed the ordinary-functor
  same-return conflict increment; R-032 is the planned Checker Task 258B5C
  normal-source proof-label projection prerequisite.

### Task R-024: ModuleSummary Reuse

Purpose: consume canonical `mizar-artifact` `ModuleSummary` projections for
dependency modules without creating resolver-owned artifact schemas or
re-reading dependency source when a compatible summary is available.

Spec refs:
- architecture 03 "Module Summary"
- architecture 18 dependency fingerprints
- [module_summary_reuse.md](./module_summary_reuse.md)
- [mizar-artifact module_summary.md](../../mizar-artifact/en/module_summary.md)

Tests:
- summary-backed and source-backed exported resolver-facing projections agree
  on a shared fixture;
- incompatible schema or identity/hash mismatch falls back with a deterministic
  crate-local record;
- repeated summary consumption is deterministic.

Affected design files:
- `todo.md`
- `00.crate_plan.md`
- `module_summary_reuse.md`
- `symbols.md` if status wording changes.

Affected source files:
- `crates/mizar-resolve/src/module_summary_reuse.rs`
- `crates/mizar-resolve/src/lib.rs`
- `crates/mizar-resolve/Cargo.toml`

Completion condition:
- validated canonical `ModuleSummary` values can populate resolver summary
  contribution indexes;
- absence or incompatibility falls back to source-backed resolution only when
  the caller provides source-backed input for the same canonical module; pure
  artifact dependencies produce deterministic unavailable/incompatible summary
  records without widening visibility or fabricating identities;
- resolver/artifact/build ownership boundaries remain explicit.

Forbidden behavior:
- defining a resolver-local artifact schema, reader, writer, or hash framing;
- interpreting build `content_hash` as an interface hash without an explicit
  contract;
- assigning public resolver diagnostic codes;
- changing `doc/spec`, existing `.miz` tests, or existing expectations.

## Step 5 Task 31 Addendum

R-031 is the bounded resolver-owned same-signature/same-return declaration
conflict required by canonical Chapter 19, checker Task 37, and the existing
deferred trace row. It extends declaration/signature collection and the active
`declaration_symbol` consumer only after exact unit/corpus and recovery/order
tests pass. It does not perform checker semantic type equivalence, overload
selection, public-diagnostic adoption, or expectation rebaselining. R-031 is
an independent checker-Task-49 prerequisite and is not a dependency of the
Task-180 contradiction vertical slice.

The exact represented family is an ordinary parser-backed functor definition,
or an explicit test projection with the same `SymbolKind::Functor` /
`DefinitionKind::Functor` pair. Equality is resolver-syntactic over namespace,
primary spelling, normalized notation pattern, normalized definition argument
context, and syntactic arity. Return signatures use normalized surface text
only; binder alpha-equivalence and semantic type equivalence are not claimed.
Predicates, modes, redefinitions, registrations, recovered declarations, and
projections with another kind pair do not enter this classifier.

R-031 appends the public non-exhaustive internal-metadata variants
`SymbolDiagnosticClass::SameSignatureDefinitionConflict` and
`DeclarationConflictClass::SameSignatureDefinitionConflict`. The diagnostic
Debug spelling is the Rust variant name `SameSignatureDefinitionConflict`,
while the explicit `SymbolEnv` snapshot spelling for the definition-conflict
variant is exactly `same_signature_definition_conflict`. An all-return-identical group
gets exactly one such diagnostic over every candidate and maps in
`mizar-test` to
`declaration_symbol.signature.same_signature_definition_conflict`. A group
with more than one distinct normalized return signature keeps exactly one
existing `SameSignatureReturnConflict` diagnostic/conflict over every
candidate and the unchanged
`declaration_symbol.signature.same_signature_return_conflict` key. The
different-return class therefore wins for a mixed group; overlapping
diagnostics are forbidden. The diagnostic uses the first candidate in
canonical source order for its shell/range and retains canonical candidate
ordering under input permutations.

The implementation slice is limited to `crates/mizar-resolve/src/symbols.rs`,
`src/env.rs`, `src/env/snapshot.rs`, their unit/snapshot tests, and the existing
private declaration-symbol detail-key projection in
`crates/mizar-test/src/runner/shared.rs`. The existing
same-return `.miz` source remains byte-identical. Its sidecar gains only the
active declaration-symbol tag, exact diagnostic payload, and active wording;
the existing different-return sidecar remains byte-identical. Exactly
`spec.en.19.overload.definition_conflict.same_return.declaration` changes from
deferred to covered. Paired resolver and mizar-test design records and
`doc/design/spec_coverage_audit.md` record this exact new corpus credit.

Exit requires parser-backed and explicit-projection same-return cases;
spelling/pattern, argument-context, arity, namespace/kind, nonordinary, and
recovered near misses; mixed-return priority; source-order/candidate
determinism; exact same-return and unchanged different-return runner keys; and
an exact `same_signature_definition_conflict` SymbolEnv snapshot assertion;
plus focused and workspace verification. R-031 changes no parser/checker behavior,
public numeric diagnostic code, overload viability/specificity/winner logic,
Task-49 member other than this sole same-return seed, Core/CFG/VC/proof path,
or Step 6/7 status.

R-031 is complete within that frozen slice. The appended metadata variants,
explicit snapshot/detail-key projections, exact and negative unit matrix,
mixed-group priority, recovery suppression, active same-return corpus case,
sole covered trace row, and paired coverage records landed together. No
different-return expectation, `.miz` source, semantic type/selection behavior,
or deferred phase status changed.

## Checker Task 258B5C Lower Prerequisites: R-032A / R-032B

R-032 is a resolver-owned umbrella derived from canonical Chapter 15 §15.10
and Chapter 16 §§16.4.2/16.5.1. It does not reopen the completed milestone.
Before the S-026 preflight discovery, the frozen B5C record used the order
documentation prerequisite, R-032A validated structural Surface-to-resolved
arena/map, R-032B proof-label collector, then active Checker Task 258B5C. That
historical four-task record is superseded for execution by the effective
seven-task prerequisite order in the overlays below; every task remains one
commit followed by fresh inventory.

R-032A owns the `SurfaceResolvedArena` API and exact public non-exhaustive error
table frozen in `resolved_ast.md`: complete one-to-one child-first same-index
structure, exact source/module/root/kind/children/range/recovery/origin/path,
validation, checked `usize`-to-`u32` path conversion, and no semantic key.
Ownership is exactly `resolved_ast.rs`, `resolved_ast/tests.rs`,
`tests/lint_policy.rs` for the sole R-026 public-enum decision entry, and
paired docs.

R-032B owns the exact `ProofLabelSourceCollector` and public non-exhaustive
`ProofLabelSourceCollectionError` API frozen in `labels.md`. It validates the
R-032A arena, returns existing `LabelProjection` /
`LabelReferenceCandidate` rows through `projections()` / `references()`, and
has no callback, unmapped side channel, or fabricated id. Implementation
ownership is exactly `labels.rs`, `labels/tests.rs`, and
`tests/lint_policy.rs` for the sole R-026
`ProofLabelSourceCollectionError` / `labels.md` public-enum decision entry,
plus paired docs.

The exact `ProofLabelSourceCollector<'a>` impl stores only `'a` borrows of
`SurfaceAst` and `SurfaceResolvedArena`, owns namespace/contribution, and does
not borrow or store the constructor's `ModuleId`; collection uses the validated
`resolved.module()`. The exact public error declarations, including R-032A
resolution-state/reference-key mismatch variants and R-032B typed overflow
payloads, are canonical in `resolved_ast.md` and `labels.md`.

Top-level theorem proof-owner roots are stable module source-order `[0]`,
`[1]`, ...; nested supported proof scopes append owner-relative child order.
One module-global one-based counter scans supported statement forms in exact
depth-first preorder and is never reset per theorem; unsupported/recovered
subtrees do not consume or descend. `visible_after_ordinal` is the maximum
consumed ordinal in the entire labelled compact subtree including its own
proof. The exact `proof-step-v1` length-framed `LabelOriginPath` grammar, B5C
origin paths, and exhaustive default-deny direct Surface edge table are frozen
in `labels.md`. Its only upper traversal is exact
`Root` -> `CompilationUnit` -> `ItemList` -> direct normal `TheoremItem`;
every unlisted edge skips without row, ordinal, or descent.

Evidence covers the full R-032A wrong/stale/mismatch/overflow matrix;
`[0] -> [0,1]` visibility; inner-to-outer and sibling confinement; an earlier
theorem `[0]` label unresolved from later theorem `[1]` despite eligible
ordinals and without cross-theorem conflict; own-proof pre-completion
unresolved and same-block post-completion resolved; exact ordinals/provenance;
recovery/exclusion; and deterministic formatting/spelling/topology/owner-order
mutations. R-032B additionally requires a positive test per permitted edge,
including the three separate upper edges and wrong/missing/additional/
relocated/wrapped upper alternatives, plus formula/computation/qualified/
grouped/bulk/template/unsupported-owner/recovery negatives,
mixed-reference-list filtering, and a
representative all-other matrix for every table row.

The later consumer is private `mizar-test` `declaration_symbol` with exact key
`declaration_symbol.label.proof_scope_confinement`. Public checker handoff,
parser/frontend production, Cargo/workspace metadata, other resolver owners,
checker/type/proof/Core/CFG/VC semantics, public numeric diagnostic codes,
fixtures, sidecars, trace status/counts, and active runner changes are excluded.
This documentation commit changes none of them. Each implementation commit
requires focused/workspace verification, finding-free reviews, and a clean
task-only commit; the coverage audit must not claim active B5C coverage early.

## Task Index

| Task | Contract |
|---|---|
| STEP5C1-VARIABLE-SEMANTICS | [EN contract](../../task_contracts/en/STEP5C1-VARIABLE-SEMANTICS.md) |
| RESOLVE-TEMPLATE-TYPEPARAM-277R1 | [EN contract](../../task_contracts/en/RESOLVE-TEMPLATE-TYPEPARAM-277R1.md) |
| RESOLVE-FRAENKEL-GENERATOR-VAR-277R2 | [EN contract](../../task_contracts/en/RESOLVE-FRAENKEL-GENERATOR-VAR-277R2.md) |
| RESOLVE-FRAENKEL-NESTED-CAPTURE-257C4C2 | [EN contract](../../task_contracts/en/RESOLVE-FRAENKEL-NESTED-CAPTURE-257C4C2.md) |
| RESOLVE-FRAENKEL-NESTED-MULTI-CAPTURE-257C4C8R | [EN contract](../../task_contracts/en/RESOLVE-FRAENKEL-NESTED-MULTI-CAPTURE-257C4C8R.md) |
| RESOLVE-FRAENKEL-CONTAINING-OWNER-33R | [EN contract](../../task_contracts/en/RESOLVE-FRAENKEL-CONTAINING-OWNER-33R.md) |

## Exit Criteria

Hard gates:

- no blocking/high specification inconsistency remains for the active milestone;
- every public API and promised behavior has design coverage before source
  implementation;
- no source behavior remains absent from `doc/spec`, existing tests, or the
  active design spec;
- `.miz` tests and expectations are not changed merely to match implementation;
- resolver does not own parser, checker, proof, build, driver, artifact, or
  session responsibilities;
- diagnostics are either backed by an explicit spec/code-space decision or
  documented as crate-local/internal for the milestone;
- remaining risks are classified as deferred, out of scope, or human-owned.

Verification commands:

```text
cargo fmt --check
cargo test -p mizar-resolve
cargo clippy -p mizar-resolve --all-targets --all-features -- -D warnings
```

For boundary work, also run the relevant subset:

```text
cargo test -p mizar-build
cargo test -p mizar-session
cargo test -p mizar-syntax
cargo test -p mizar-frontend
cargo test -p mizar-test
```

Review expectations:

- review-only agent for the active specification;
- test sufficiency review before marking implementation tasks complete;
- implementation review with no unresolved findings;
- source/documentation consistency review;
- crate-wide completion requires hard gates plus a read-only quality score of
  at least 90/100.

## R-032A Preflight Dependency Overlay

After B5C documentation commit `1527ca611d3ea33af22675a31cc2d7eaa2bdb07e`,
fresh R-032A preflight found a High `boundary_violation` risk: the syntax
consumer API cannot enumerate a valid disconnected compatibility node with its
`SurfaceNodeId`. The earlier existing-API-sufficiency statement is
`design_drift`. Authority is sufficient for a semantic-free syntax accessor,
so no human semantic decision is required.

The effective order is now S-026 frozen documentation, S-026 syntax
implementation, R-032A lint-policy frozen-documentation correction, R-032A
implementation, R-032B lint-policy frozen-scope documentation correction,
R-032B implementation, then active B5C, with fresh inventory between every
commit. S-026 owns only dense compatibility-node views; resolver ownership and all B5C
language/test intent remain unchanged. The exact S-026 contract is canonical
in `doc/design/mizar-syntax/en/ast.md`; the refined R-032A validation order is
canonical in [resolved_ast.md](./resolved_ast.md).

### R-032A lint-policy documentation-correction verification

Implementation preflight classified the omitted mandatory R-026
enum-decision owner as High `design_drift`, with no blocking `spec_gap`,
test-intent ambiguity, or repository metadata conflict. The first
specification review found and repaired one residual five-task ordering
statement; the repeated specification review, independent test-sufficiency
review, and repeated source/documentation consistency review then reported
**NO FINDINGS**.

The docs-only scope is exactly 31 design files: 16 resolver, eight checker,
six `mizar-test`, and one global ledger. `git diff --check`, focused
`mizar-resolve` tests `111 + 11 + 0`, `cargo fmt --check`, warnings-denied
all-target/all-feature Clippy, and full offline workspace tests pass. The five
CLI counts remain plan `419/387`, pass/fail `228/191`, active
parse/declaration/type/proof `101/5/198/1`, and warnings/errors `23/0`; their
stdout hashes remain
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`,
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`,
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`,
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`,
and `ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.

Resolver library tests remain 111 with raw/normalized list hashes
`ec8fa003e020725a503d900c170ed0a0edb90ece013e6854ef165689948bc3be` /
`81a241fa6a271262527bf0af7c33df5492344538ac6f7b8636c26571e702aa4b`;
resolver production remains `15/17921`, path/content
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`925ba16aaa8208a87fabaad4970666784f8e1c0a8de3ad81f191932786ad5723`.
Checker, runner, and syntax production remain respectively `23/145097`,
`c2eea2db9187c48dd830a010eff37f09b90467f9012a9fe6b3ac669b6d1dac42` /
`c39d43229e85e6136597f0f6cd52c15e1ab1d2057cf7866f6bbbf244307250dc`;
`30/59745`,
`98f3b264a59fed5b08c3e8f20e7ca58ff54efaa154eab16a7572a69ce923f275` /
`75d3e70b1eb6a5871486c1dc6b0ccde06aec4b0d3e23a1b4c5eecf33dfb9039b`;
and `6/4314`,
`27622a38347b8b5f94559b9587d227b944c0f680c2f7729c115e7743253eb733` /
`fec701d87a95506058a6a7ceb4e500786084f4b00646df0bf3a43624a18617b8`.
Their library test counts and raw/normalized list hashes remain checker 430
`5dc6cff8c93d86911dca85f91da81501ddf226c42fd6338f4c4be6105782132e` /
`d7eb7a0d48d2c11b9c3f3b00ca025e1c7a1d5ce9b2b767ca94c2655c5d2dbf27`,
runner 500
`94aa81ba9af645c9de1e927aa06bf8d525e3510509a607074e604eafc00ff995` /
`e0d976ab223f0ac0c1b48bd9926bb3fcf785706bdd4a24ecfd0633c81f66f943`,
and syntax 59
`512775259a51121a0c12ab9fbf0d1083273d3d140362d889cdd9e22184215da6` /
`c11a29c90fee3fe81d839f80f196dcd405cc43c7da86e83f37dc123042066540`.
No specification, `.miz`, expectation,
sidecar, trace, production/test source, Cargo metadata, or coverage state
changes. `spec_coverage_audit.md` remains unchanged because this correction
changes no chapter coverage, traceability, owner crate, or deferred status.
The independent final read-only quality review reports **NO FINDINGS**, all
nine hard gates PASS, no score cap, and valid `100/100`
(`20/20/15/15/10/10/5/5`).

### R-032A implementation status

R-032A implements the frozen structural arena in exactly three Rust files and
synchronizes the exact 17 live design-status records identified by the
source/documentation review. The resolver now owns complete dense same-index
lowering, exact provenance, bounded accessors, fail-closed validation, and the
public forward-compatible error table; it owns no label collection or
semantic result. Eight focused unit tests plus one downstream-style rustdoc
test cover the complete mapping/error/precedence/helper/determinism contract,
and the R-026 lint table contains only the authorized new decision.

Initial reviews found one Medium equivalent-input `test_gap`, one Medium
32-bit test portability `source_drift`, and one High live-status
`design_drift`; all were corrected. Test-sufficiency and implementation
re-reviews report **NO FINDINGS**. At R-032A completion the R-032B stream
became next; its current first logical task is the separate lint-policy
docs correction. `spec_coverage_audit.md` remains unchanged because R-032A changes no
active `.miz` mapping, trace/backlink/status/count, owner crate, deferred
status, or coverage credit.

Final pre-commit verification passes the eight focused R-032A tests, all
`mizar-resolve` tests (`119` unit, `11` lint-policy, and `1` rustdoc),
`cargo fmt --check`,
`cargo clippy --all-targets --all-features -- -D warnings`, and full
workspace `cargo test`. The five plan/parse/declaration/type/proof CLI hashes
remain
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`,
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`,
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`,
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`,
and `ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.
Counts remain plan `419/387`, pass/fail `228/191`, active
parse/declaration/type/proof `101/5/198/1`, and warnings/errors `23/0`.

The resolver library list is now `119` tests with raw/normalized hashes
`429e6a470d00316a7b12d5659c2b7609aca726d368f0637a149b7b109a6ad560` /
`f32a0e2768c986114b5bbaacde3ef5a9c9a8fb7e464d74ebd54f85f619d09c11`.
Its production manifest remains 15 paths with the unchanged path hash
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29`
and becomes 18,261 lines with ordered content hash
`7afdd9951182064b21d1be88a0a5330491c972fb78b1917e6e6ad68f3485e369`.
Checker, runner, and syntax production and their test-list hashes remain the
exact values recorded by the preceding lint-policy correction. Final
`git diff --check`, explicit staging, cached-diff review, independent quality
review, commit, and post-commit/fresh-inventory gates remain pending at this
pre-commit record.

### R-032B lint-policy frozen-scope correction (completed prerequisite record)

Fresh R-032B implementation inventory found that the planned public
`ProofLabelSourceCollectionError` is necessarily scanned by the mandatory
R-026 guard. The earlier two-Rust-file ownership is High `design_drift`, not a
semantic `spec_gap`, `test_gap`, or test-intent change. Later implementation
is frozen to exactly `crates/mizar-resolve/src/labels.rs`,
`crates/mizar-resolve/src/labels/tests.rs`, and
`crates/mizar-resolve/tests/lint_policy.rs`; the last file may receive only
the sole `ProofLabelSourceCollectionError` owning-spec decision with
`spec_name: "labels.md"`.

This completed synchronized docs-only correction has an exact total scope of
31 design files: 16 resolver, eight checker, six `mizar-test`, and one global
ledger. It changes no specification, semantic behavior, test intent, source,
fixture, sidecar, expectation, trace status/count, or Cargo metadata.
`doc/design/spec_coverage_audit.md` is a deliberate no-op because chapter
coverage, traceability, owner crate, deferral, and coverage credit do not
change. The independent specification, test/scope, and source/documentation
consistency reviews report **NO FINDINGS**, and the docs-only verification,
count, and hash gates PASS. The independent final read-only quality review
also reports **NO FINDINGS**; all nine hard gates PASS with no cap at valid
`100/100` (`20/20/15/15/10/10/5/5`). At that pre-commit record, only
task-only staging/cached-diff review, commit, and post-commit
invariant/fresh-inventory gates remained pending. They subsequently completed
in dedicated correction commit
`f1cf0a5d15f2db51176e9e91a4f5a6447a88ad7a` and its fresh inventory.

Focused `lint_policy` passes `11/11`; all `mizar-resolve` tests pass
(`119` unit, `11` lint-policy, and `1` rustdoc), as do `cargo fmt --check`,
`cargo clippy --all-targets --all-features -- -D warnings`, and full
workspace `cargo test`. The CLI counts remain plan `419/387`, pass/fail
`228/191`, active parse/declaration/type/proof `101/5/198/1`, and
warnings/errors `23/0`. The plan/parse/declaration/type/proof hashes remain
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`,
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`,
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`,
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`,
and `ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.

Test-list counts and raw/normalized hashes remain resolver `119`
`429e6a470d00316a7b12d5659c2b7609aca726d368f0637a149b7b109a6ad560` /
`f32a0e2768c986114b5bbaacde3ef5a9c9a8fb7e464d74ebd54f85f619d09c11`,
checker `430`
`5dc6cff8c93d86911dca85f91da81501ddf226c42fd6338f4c4be6105782132e` /
`d7eb7a0d48d2c11b9c3f3b00ca025e1c7a1d5ce9b2b767ca94c2655c5d2dbf27`,
runner `500`
`94aa81ba9af645c9de1e927aa06bf8d525e3510509a607074e604eafc00ff995` /
`e0d976ab223f0ac0c1b48bd9926bb3fcf785706bdd4a24ecfd0633c81f66f943`,
and syntax `59`
`512775259a51121a0c12ab9fbf0d1083273d3d140362d889cdd9e22184215da6` /
`c11a29c90fee3fe81d839f80f196dcd405cc43c7da86e83f37dc123042066540`.

Production path/line counts and path/content hashes remain resolver
`15/18261`,
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`7afdd9951182064b21d1be88a0a5330491c972fb78b1917e6e6ad68f3485e369`,
checker `23/145097`,
`c2eea2db9187c48dd830a010eff37f09b90467f9012a9fe6b3ac669b6d1dac42` /
`c39d43229e85e6136597f0f6cd52c15e1ab1d2057cf7866f6bbbf244307250dc`,
runner `30/59745`,
`98f3b264a59fed5b08c3e8f20e7ca58ff54efaa154eab16a7572a69ce923f275` /
`75d3e70b1eb6a5871486c1dc6b0ccde06aec4b0d3e23a1b4c5eecf33dfb9039b`,
and syntax `6/4314`,
`27622a38347b8b5f94559b9587d227b944c0f680c2f7729c115e7743253eb733` /
`fec701d87a95506058a6a7ceb4e500786084f4b00646df0bf3a43624a18617b8`.
The exact scope remains 31 design files and
`doc/design/spec_coverage_audit.md` remains a deliberate no-op.

### R-032B implementation result

After dedicated correction commit
`f1cf0a5d15f2db51176e9e91a4f5a6447a88ad7a` and fresh inventory, R-032B
added the frozen public `ProofLabelSourceCollector<'a>`,
`ProofLabelSourceCollection`, and non-exhaustive
`ProofLabelSourceCollectionError` in exactly
`crates/mizar-resolve/src/labels.rs`; its focused evidence belongs only to
`crates/mizar-resolve/src/labels/tests.rs`; and
`crates/mizar-resolve/tests/lint_policy.rs` receives only the sole R-026
`ProofLabelSourceCollectionError` / `labels.md` decision.

The collector validates the R-032A arena at construction and on collection,
stores only the AST/arena borrows, owns namespace/contribution, and does not
store the constructor module. Its exact default-deny direct-edge traversal
derives theorem-root and nested proof scopes, one module-global one-based
statement ordinal, whole-subtree completion boundaries, exact simple
citation candidates, richer structural origins, and length-framed
`proof-step-v1` identities. Unsupported, recovered, wrapped, semantic,
qualified, grouped, bulk, and template forms produce no row, ordinal, or
descent. The collector feeds the existing `LabelResolver`; it neither
duplicates resolution nor creates proof/checker semantics.

The preimplementation specification review reports **NO FINDINGS**. The
test-sufficiency stream fixed its initial High/Medium gaps and two fresh
Medium gaps; its final fresh rereview reports **NO FINDINGS**. The
implementation stream fixed the Medium unrelated-third-child dependency and
the two Medium unauthorized `Default` / `From` findings; its final fresh
rereview reports **NO FINDINGS**. The final fresh source/documentation
consistency rereview also reports **NO FINDINGS**.

Final pre-commit verification passes the focused collector matrix `25/25`,
the complete labels matrix `35/35`, all `mizar-resolve` tests (`144` unit,
`11` lint-policy, `1` rustdoc), `cargo fmt --check`, workspace
`cargo clippy --all-targets --all-features -- -D warnings`, full workspace
`cargo test`, and `git diff --check`.

CLI counts remain plan `419/387`, pass/fail `228/191`, active
parse/declaration/type/proof `101/5/198/1`, and warnings/errors `23/0`.
The plan/parse/declaration/type/proof hashes remain
`4cc13ea6bee6c1a6458d4a7d027a7eea685b711eda8410edafad8faa01809d54`,
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56`,
`210055108c257ff65c6f45fb654c82e506653ec4617b68d111893bb3aa1da5a8`,
`f87a743b914d2d51d6b9a8dbcf3c8d93bbc1403b44907fd85123a1865f84edd5`,
and `ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.

Test-list count and raw/normalized hash are resolver `144`
`727e09ff53088f3cb5b7a0194789a109bcfe459789d0757124a32ff4346f9b29` /
`8cf9e92ddf79231302390978f600b23aa7db7428f552eb72e4d8990b29558bf2`,
checker `430`
`5dc6cff8c93d86911dca85f91da81501ddf226c42fd6338f4c4be6105782132e` /
`d7eb7a0d48d2c11b9c3f3b00ca025e1c7a1d5ce9b2b767ca94c2655c5d2dbf27`,
runner `500`
`94aa81ba9af645c9de1e927aa06bf8d525e3510509a607074e604eafc00ff995` /
`e0d976ab223f0ac0c1b48bd9926bb3fcf785706bdd4a24ecfd0633c81f66f943`,
and syntax `59`
`512775259a51121a0c12ab9fbf0d1083273d3d140362d889cdd9e22184215da6` /
`c11a29c90fee3fe81d839f80f196dcd405cc43c7da86e83f37dc123042066540`.

Production path/line count and path/content hash are resolver `15/18871`
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4288b37b1ba0af59496ab1468f1df30aec95fa0527682b511ed471d780adcfac`,
checker `23/145097`
`c2eea2db9187c48dd830a010eff37f09b90467f9012a9fe6b3ac669b6d1dac42` /
`c39d43229e85e6136597f0f6cd52c15e1ab1d2057cf7866f6bbbf244307250dc`,
runner `30/59745`
`98f3b264a59fed5b08c3e8f20e7ca58ff54efaa154eab16a7572a69ce923f275` /
`75d3e70b1eb6a5871486c1dc6b0ccde06aec4b0d3e23a1b4c5eecf33dfb9039b`,
and syntax `6/4314`
`27622a38347b8b5f94559b9587d227b944c0f680c2f7729c115e7743253eb733` /
`fec701d87a95506058a6a7ceb4e500786084f4b00646df0bf3a43624a18617b8`.
The exact task scope is 20 files: three Rust owners plus these 17 live-status
records. Independent final read-only quality reports **NO FINDINGS**; all nine
hard gates PASS with no score cap at valid `100/100`
(`20/20/15/15/10/10/5/5`). Task-only restaging/cached-diff review, dedicated
commit `b3a7e79a6b60db2974e911c69bb56ff5f4609064`, and post-commit
invariant/fresh inventory are complete.

No `.miz` fixture, expectation, sidecar, trace row/status/count, active runner,
checker handoff, public diagnostic code, Cargo metadata, or
checker/type/proof/Core/CFG/VC behavior changed in R-032B. Its current private
production consumer is `mizar-test` B5C under
`declaration_symbol.label.proof_scope_confinement`; public checker
`SourceStatementReferenceHandoff` remains excluded.
`doc/design/spec_coverage_audit.md` remains a deliberate no-op because no
active mapping, traceability, owner crate, deferral, or coverage credit
changes.

### Checker Task 258B5C active implementation status

R-032B is committed at `b3a7e79a6b60db2974e911c69bb56ff5f4609064`.
The historical B5C task privately consumed the unchanged R-032A
`SurfaceResolvedArena` and R-032B `ProofLabelSourceCollector` /
`LabelResolver` APIs in `mizar-test`. It adds exactly two fail `.miz` fixtures,
their two expectation sidecars, and two covered trace rows.
Its exact `mizar-test` source/test consumers are
`crates/mizar-test/src/runner/declaration_symbol.rs`,
`crates/mizar-test/src/runner/tests.rs`,
`crates/mizar-test/src/runner/tests/declaration_symbol.rs`, and
`crates/mizar-test/tests/metadata.rs`; the last updates four frozen
active-count/CLI assertions from declaration stage `5` to `7`. No resolver
production source or public API changes.

The active plan is `421/389`, pass/fail is `228/193`, active
parse/declaration/type/proof stages are `101/7/198/1`, and warnings/errors are
`23/0`. Public diagnostic codes remain empty; the route is authenticated only
by private key `declaration_symbol.label.proof_scope_confinement`. This closes
only the inner-to-outer and sibling confinement negatives in R-G007. R-G007
remains open for import, name, dot-chain, and other label-reference coverage.
B5C is a historical completed checkpoint, committed as
`33ac57e96f048dc40559565f54369cac854409a7`; its post-commit fresh inventory
passed. Test, implementation, source/documentation reviews and all
verification gates are complete. Independent final quality reports
**NO FINDINGS**; all nine hard gates PASS with no score cap at valid
`100/100` (`20/20/15/15/10/10/5/5`).

## Checker Task 263R Frozen Selector-Owner Resolver Prerequisite

Fresh post-Task-262 inventory selected Checker Task 263, then its mandatory
parser/resolver preflight exposed a lower-stage defect. The exact future
Task-263 source is 320 bytes with SHA-256
`078eaee4b17341c9d8ebeb8a1f631ca984873bd07eb4e5d9c1a9486b39ac6671`.
The literal oracle, including its final LF, is:

```mizar
definition
  struct Task263Base where
    field carrier -> set;
    property marker -> set;
  end;

  struct Task263Derived where
    field carrier -> set;
    property marker -> set;
  end;

  inherit Task263Derived extends Task263Base where
    field carrier from carrier;
    property marker from marker;
  end;
end;
```

Frontend parsing has zero diagnostics,
75 stored Surface nodes, ten declaration shells, and eight symbol projections.
Preimplementation collection preserved all eight symbols but incorrectly reported two
`DuplicateDeclaration` diagnostics for `carrier` and `marker`.

Canonical authority is Chapter 5 §§5.2-5.5, especially the per-structure
member declarations, root-declaration/path-view identity, and the multiple-
structure examples that repeat `carrier`. Existing
`pass_parser_structures_001` supplies parser-shape/control evidence, but it
neither repeats selectors across structures nor executes resolver conflict
classification; Chapter 5 §5.4 is the owner-sensitive authority. Chapter 16
does not authorize a new resolver proof obligation here. The disagreement is
preimplementation `source_drift` in `symbols.rs`, `design_drift` in the name-level selector
duplicate rule, and a canonical-derived `test_gap`. There is no blocking
`spec_gap`, test-expectation drift, or authority-free semantic decision.
Origin divergence `0/5` is a report-only `repo_metadata_conflict`; protected
`stash@{0}` remains
`f65cf4a13752ec380710814a9ac6392ccb9d75d4` and is not modified.

The implementation scope is exactly `crates/mizar-resolve/src/symbols.rs` and
`crates/mizar-resolve/src/symbols/tests.rs`. The production owner adds only an
internal nearest-`StructureDefinition` selector-owner partition to the
non-overloadable conflict key. Missing owners fail closed into one conservative
partition. Two extractor-backed resolver tests prove cross-structure
coexistence, same-structure field/property collision, and projection-order
determinism; the existing duplicate-family test preserves non-selector and
missing-owner policy. No public API, module, dependency, diagnostic
class/code, fixture, sidecar, expectation, trace row/status/count, active
runner, checker source, or Cargo metadata changes.

The tests are frozen as
`selectors_in_distinct_structure_owners_do_not_conflict` and
`field_and_property_in_same_structure_keep_duplicate_conflict`. The first
builds one definition-block shell 0 containing the two structure subtrees from
the literal oracle: structure shells 1/4 own field shells 2/5 and property
shells 3/6 respectively. Extraction yields the two structure symbols and four
selector symbols; after reversing that six-projection input, collection must
return six symbols, zero diagnostics, and no selector definition conflict.
The existing ownerless explicit-projection duplicate test remains the
non-selector and missing-owner control. The second new test uses this exact
114-byte/final-LF control, SHA-256
`f020065dfd37f637ac73ad0581efaa990dbf28b3e541ea3e5454a5fd7959477b`:

```mizar
definition
  struct Task263Collision where
    field duplicate -> set;
    property duplicate -> set;
  end;
end;
```

Its measured profile is 30 Surface nodes, four shells, three projections,
three symbols, and one `DuplicateDeclaration`. Structure shell 1 owns field
shell 2 and property shell 3; the diagnostic is anchored to shell 2/range
`47..70`, candidates stay in source order, and both selector definitions carry
`DuplicateSpelling`. Reversing projection input must preserve that result.

This documentation prerequisite preserves resolver tests `144` with raw/
normalized hashes
`727e09ff53088f3cb5b7a0194789a109bcfe459789d0757124a32ff4346f9b29` /
`8cf9e92ddf79231302390978f600b23aa7db7428f552eb72e4d8990b29558bf2`
and production `15/18871` with path/content hashes
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4288b37b1ba0af59496ab1468f1df30aec95fa0527682b511ed471d780adcfac`.
Implementation projects resolver tests `144 -> 146`; all corpus/CLI/checker/
runner/syntax counts and hashes remain unchanged. The coverage audit changes
only to record the Chapter-5 lower owner and deferral; it grants no executable
corpus credit.

Exit requires findings-free specification, test, implementation, and source/
documentation reviews; the exact parser/resolver probe must become zero
diagnostics while a same-owner collision remains one deterministic diagnostic;
all resolver and workspace gates must pass. This docs-only prerequisite must
commit first. Fresh inventory then implements Task 263R in its own commit and
returns to Task 263 without mixing checker production or structure semantics.

## Checker Task 263R Documentation-Prerequisite Verification

Repeated specification review, test-sufficiency review, and source/
documentation boundary review report **NO FINDINGS**. All prior
`design_drift` findings are resolved: the literal probes and exact test schema
are reproducible, Chapter 5 §5.4 is the owner-sensitive authority, historical
R-032B/B5C status is synchronized, and the audit leaves source/test gaps open
for the later implementation. The delta is exactly 28 design files and changes
no executable source, Rust test, corpus artifact, trace metadata, or Cargo
metadata.

Resolver tests pass `144 + 11 + 1`; `mizar-test` metadata/lint/public-enum
policies pass `137 + 14 + 2`. Workspace format, warnings-denied Clippy, full
workspace tests, Cargo metadata, `git diff --check`, and all five CLIs pass.
The CLI hashes remain plan/parse/declaration/type/proof
`77f54c338ccd56266afaf4f85e4e3e1546230202d3787712e9826dfb40f7186e` /
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56` /
`71e83ba0b20d4015e07b3bd2c0c4db2837b6151d1251812caed7954530d53c74` /
`4b638a91010f923fc6c62d8fa025e86a7a47958268f18fb7fc32cfc62a154dec` /
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.

Library inventories remain checker/runner/resolver/syntax `458/524/144/59`.
Resolver production remains `15/18871` with the frozen path/content hashes;
the trace hash remains
`3f510f819f03a3fd2922275b37ab71070e41d4f8e2e0e9c0c94147076552626a`.
Active metadata remains `425/393`, `232/193`, `101/7/202/1`, type
`257 = 245 covered + 12 deferred`, and warnings/errors `23/0`. Independent
final read-only quality reports **NO FINDINGS**; all nine hard gates PASS with
no score cap at valid `100/100` (`20/20/15/15/10/10/5/5`). Exact staging/
commit and post-commit inventory remain the pre-implementation gates.

## Checker Task 263R Implementation Result

The documentation prerequisite committed as
`34692ee222d5465750f061da82fe878566a1557c`; its post-commit inventory was
clean and preserved the protected stash. Fresh inventory then implemented the
frozen correction in exactly `src/symbols.rs` and `src/symbols/tests.rs`.
`CollectedProjection` now carries a private optional selector owner, derived
by walking declaration-shell parents to the nearest enclosing
`StructureDefinition`. The owner participates only in the non-overloadable
selector conflict key. An absent owner or missing ancestry remains `None`, so all
ownerless selectors still share the conservative partition. Non-selectors,
overload/redefinition policy, symbol identity, diagnostic classes and ranges,
recovery, and projection ordering are unchanged; there is no public API or
dependency change.

The two frozen extractor-backed tests are present. The 320-byte cross-owner
probe keeps `75/10/8/8` Surface/shell/projection/symbol counts and now has zero
resolver diagnostics. The 114-byte same-owner collision control keeps
`30/4/3/3` and exactly one deterministic `DuplicateDeclaration`, including
the frozen `47..70` anchor, candidate order, and `DuplicateSpelling` marks.
Forward and reversed projection inputs have identical conflict vectors.
Resolver library tests are now `146`, with raw/normalized test-list hashes
`954470cb5d545d3f42df1d411341757b10cc6b8023443d4dfb218e0054873fb6` /
`6bb84dffd143e8e3d6ff4716a502626d5fcb6f498d02d16b1828587cff12f3d0`.
Resolver production remains 15 files and becomes 18,896 lines; path/content
hashes are
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4e3687afdc06f06eb06dd5a8ee9690e502e7341b708ccb00596993c6b2781283`.

Test-sufficiency, implementation, and source/documentation consistency reviews
report **NO FINDINGS**. Focused resolver tests (`146 + 11 + 1`), metadata/lint
policies (`137 + 14 + 2`), formatting, full warnings-denied workspace Clippy,
full workspace tests, Cargo metadata, all five CLIs, exact count/hash checks,
and both exact probes pass. Independent final read-only quality reports
**NO FINDINGS**; all nine hard gates PASS with no score cap at valid `100/100`
(`20/20/15/15/10/10/5/5`). Exact staging, implementation commit
`997457dd3189030aa3b137b568ce82fed456fe1e`, and post-commit fresh inventory
are complete; Checker Task 263 subsequently completed. No
fixture, sidecar, expectation, trace row/status/count, runner/checker source,
metadata, CLI output, or Chapter-5 executable corpus credit changes.

## Checker Task 264R Frozen Property-Implementation Shell Prerequisite

Fresh post-Task-263 inventory selects Checker Task 264, but lower preflight
finds that Parser Task 48's represented top-level property implementations are
dropped by `DeclarationShellCollector`. Chapter 7 §7.4.1, bounded §§7.8.2 and
7.10, Chapter 5's property-value source rule, Chapter 13 §13.1.2's means-only
`it` restriction, and the existing parser pass and recovery fixtures are
sufficient authority for a separate resolver correction.
Chapter 16's overlap-coherence rule does not authorize resolver proof or
acceptance behavior. The missing shell is `source_drift`, its absent contract
is `design_drift`, and the two regressions are `test_gap`; there is no blocking
`spec_gap`. Origin divergence is report-only `repo_metadata_conflict`.

The exact implementation scope is:

- `crates/mizar-resolve/src/declarations.rs` adds one public non-exhaustive
  `DeclarationShellKind::PropertyImplementation` and collects each represented
  node as a parentless, source-ordered, range/recovery-preserving shell;
- `crates/mizar-resolve/src/symbols.rs` treats that shell as context-only and
  extracts no signature projection or semantic identity;
- `crates/mizar-resolve/src/declarations/tests.rs` and
  `crates/mizar-resolve/src/symbols/tests.rs` add exactly two resolver tests:
  parser-backed shell/recovery preservation and no fabricated identity or
  symbol-collector/downstream semantic effect.

The pass oracle is
`tests/miz/pass/parser/pass_parser_property_implementations_001.miz`, exactly
566 bytes/28 lines with SHA-256
`8ba7fdbfac71d7620cdda1a6075f0662aa1a05b93baf4b66179c4a2997d0600e`;
its sidecar hash is
`bda8ccdd7c24d389b7b0b42719d77ffed42767b44c13a4d1c1ff2699ce6f90d8`.
It retains frontend diagnostics zero and profile
`194/4/4/2/2/6/1` for Surface nodes/property implementations/parameters/
formula definientia/term definientia/correctness conditions/mode definitions.
Only shells change `2 -> 6`; projections/symbols/definitions/diagnostics remain
`1/1/1/0`.

The recovery oracle is
`tests/miz/fail/parser/fail_parser_property_implementations_recovery_001.miz`,
exactly 335 bytes/24 lines with SHA-256
`410d0ad057239db55cfa22f2134a5101ac5fa21668e26e274041289248f82735`;
its sidecar hash is
`7236f57c3a5b2b79e703abb8f5e7cba2c5cc1c117a146001ac161edd4b24acba`.
It retains thirteen expected frontend diagnostics and profile
`109/3/3/1/2/5/1`; only shells change `3 -> 6`, while
projections/symbols/definitions/resolver diagnostics remain `2/2/2/0` and the
following theorem shell survives. The inactive
`fail_mode_property_overlap_missing_coherence_001.miz` remains byte-identical,
inactive, and outside executable credit.

The recovery oracle additionally freezes the preimplementation theorem identity
owner `theorem#1`, origin structural path `[2, 1]`, range `304..334`,
`DefinitionId(1)`, source contribution `0`, and existing contribution effects.
Property-implementation shells are excluded from semantic sibling ordinals,
and contribution anchoring selects the first non-property-implementation shell
or `Point(0)`. The six-shell result preserves those bytes exactly. The enum
variant is append-only after `VisibilityWrapper`; its stable code/key is `28` /
`property-implementation`, and existing `0..27` codes/keys do not move.

No parameter/defining-mode-context interpretation, property lookup/provenance,
`means` / `equals` distinction, the means-only `it` descendant, referenced-
property return-type lookup, or definiens lowering,
resolver signature, overload, registration, dependency, lexical summary,
initial obligation, coherence goal, proof/discharge/acceptance/fact/VC, Typed/
Resolved ownership, runner consumer, fixture, sidecar, expectation, trace row/
status/count, or Cargo metadata changes. The source grammar has no ad-hoc
`assume` subtree or implementation-owned return type. Task 264R does not
interpret `it`; later Task 264 must permit it only in `means`, never `equals`.
In particular the shell must never be
encoded as `Selector`, `PropertyClause`, `PropertyRedefinition`, or
`RegistrationKind::Property`.

The docs prerequisite preserves resolver tests `146`, raw/normalized hashes
`954470cb5d545d3f42df1d411341757b10cc6b8023443d4dfb218e0054873fb6` /
`6bb84dffd143e8e3d6ff4716a502626d5fcb6f498d02d16b1828587cff12f3d0`,
and resolver production `15/18896`, path/content hashes
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4e3687afdc06f06eb06dd5a8ee9690e502e7341b708ccb00596993c6b2781283`.
Implementation projects resolver tests `146 -> 148`; all corpus, checker,
runner, syntax, metadata, CLI, trace, and coverage counts/hashes remain
unchanged. `spec_coverage_audit.md` records only the lower owner and deferral,
not executable credit.

Exit requires findings-free specification, test-sufficiency, implementation,
source/documentation, and final quality reviews; both exact profiles; focused
resolver tests; all crate/workspace/lint/metadata/format/Clippy/CLI/count/hash
gates; exact task-only staging; and a dedicated implementation commit. This
documentation prerequisite commits first. Fresh inventory then implements
Task 264R, followed by the separate Checker Task 248P binding-context
prerequisite, before Checker Task 264 itself is frozen.

## Checker Task 264R Documentation-Prerequisite Verification

Repeated specification review and independent boundary/consistency review end
with **NO FINDINGS** after correcting the canonical no-`assume`, referenced-
property return-type, means-only `it`, synchronization-ledger, LocalSource
anchor, semantic-sibling fingerprint, append-only code/key, and collector-
effect wording. The final read-only quality review reports **NO FINDINGS**;
all nine hard gates PASS with no score cap at valid `100/100`
(`20/20/15/15/10/10/5/5`). The delta is exactly 36 design documents, with
zero source, test, fixture, sidecar, expectation, trace, or Cargo diff.

Resolver library/lint tests pass `146/11`; `mizar-test` lint/metadata pass
`14/137`. `cargo fmt --all --check`, all-target/all-feature warnings-denied
Clippy, full workspace `cargo test --no-fail-fast`, Cargo metadata, exact
pass/recovery AST probes, and `git diff --check` pass. All five CLI stdout
hashes remain plan/parse/declaration/type/proof
`5b136ab6861640034de9f04c2ed8ca38697428ac481dec914f48de4308a0324a` /
`a8a7aa639d2ebc65eddc923c7e9369ea5637d50e935f808600f446da1bfbda56` /
`71e83ba0b20d4015e07b3bd2c0c4db2837b6151d1251812caed7954530d53c74` /
`70e7e2940c13007845ab9cf1770429b8c14b2cf0042155bf3cdf39b10ee9a31b` /
`ccf3d2d4d0a3755e00989d97af369a7c560302f76798d0a185d57ec3891e8450`.
Metadata remains `426/394`, pass/fail `233/193`, stages `101/7/203/1`, type
`258 = 246 + 12`, warnings/errors `23/0`, and trace hash
`cf0ef6d28a132bcbafc8aa1214ded935a715fdffdb3421c37d66c35954f2a06c`.
Origin divergence remains report-only `0/11`; protected stash
`f65cf4a13752ec380710814a9ac6392ccb9d75d4` is unchanged. Exact docs-only
staging, commit, and post-commit fresh inventory remain parent-owned gates.

## Checker Task 264R Implementation Result

Documentation prerequisite commit
`b1ed8ea19f8845d8c54f795a7375d4add4af237d` was followed by clean fresh
inventory. The implementation changes exactly the frozen four resolver files.
It appends the context-only shell kind/code/key, preserves source order, range,
recovery, semantic sibling fingerprints, and `LocalSource` anchoring, and
creates no projection or semantic identity. The same exactly two tests freeze
the Parser Task 48 fixture bytes/lines and property provenance, the exact
following-theorem identity/effects, leading-property and property-only anchors,
all prohibited index/effect families, and the full `0..28` code/key table.

Resolver library tests are `148`; raw/normalized list hashes are
`c99d9d179cf14ab9ccd274b11d0404bdc47a64d23a2aa914c69ba674d01a3fee` /
`1c76831124b1e680d708fd30ddfa7a96959aa82d20c840594dbb108dcd063490`.
Production is `15/18906`; path/content hashes are
`e8c49ab15ef9683aaa0ced8b0772e5fb6769697f75ae94ab029cf9a496d7df29` /
`4953d62ee92da662177cdbaf161afee6a55e422160913cf99fc37bc56cb8810a`.
Test-sufficiency and implementation rereviews report **NO FINDINGS**. Focused,
crate, lint, metadata, format, warnings-denied Clippy, full workspace tests,
Cargo metadata, five CLIs, exact parser/resolver probes, and count/hash gates
pass. Corpus, checker, runner, trace, Cargo, and coverage credit remain
unchanged; Checker Task 248P remains the next dependency-ready task.
