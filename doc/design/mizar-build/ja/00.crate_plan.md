# Crate Plan: mizar-build

> 正本は英語です。英語版:
> [../en/00.crate_plan.md](../en/00.crate_plan.md)。

## 責務

所有する挙動:

- pipeline phase 0 の workspace 計画: package/workspace manifest、lockfile
  検証、依存グラフ解決、決定的な `BuildPlan` 生成。
- `BuildPlan` と source layout または dependency artifact summary からの
  phase-0 module-index 構築。
- 並列検証と増分 invalidation で使う versioned task graph。
- scheduling、決定的な result collation、resource budget、協調的
  cancellation、有界 failure propagation、決定的 artifact commit ordering。
- scheduler-selected phase dispatch callback ownership。task readiness、
  dependency ordering、resource admission、cancellation、cache consumption、
  deterministic collation は `mizar-build` に残り、external dispatcher は owner
  registry boundary を通じて selected task を実行してよい。
- 実行前に `mizar-cache` の判断を消費でき、clean-build equivalence を保つ
  cache-aware scheduling seam。

範囲外:

- build request、CLI/watch/LSP entry point、build session、event stream、
  phase-service registry、`salsa` query/database 境界。これらはすべて
  `mizar-driver` が所有する。
- raw IR storage、output handle、snapshot-handle rehydration。これらは
  `mizar-ir` の仕事である。
- `mizar-cache` の `CacheKey` 構築、dependency fingerprint 構築、
  cache-store compatibility check、proof-reuse validation。
- artifact schema ownership、manifest transaction 内部、proof witness
  publication token、producer-owned artifact projection。
- proof authority、proof search、ATP/backend winner selection、kernel
  acceptance、proof-status promotion。

`mizar-build` は、検証済み cache hit の後で task execution を skip してよい。
ただし hit は optimization only である。semantic acceptance、proof
authority、あるいは task、artifact record、backend result、externally
attested proof をより trusted な status に昇格する理由になってはならない。

`mizar-build` は `mizar-driver` に依存してはならない。依存方向は
driver-to-build である。

## 仕様項目

| Spec ref | Requirement | Status |
|---|---|---|
| [spec 23](../../../spec/ja/23.package_management_and_build_system.md) | Package manifest、workspace、lockfile、build lifecycle、cache/artifact directory、reproducibility。 | planner tasks 2-4 と後続 graph/scheduler/commit tasks が扱う。 |
| [architecture 03](../../architecture/ja/03.module_and_symbol_resolution.md) | build-side package/module index は resolver-owned import/name resolution へ入力される。 | tasks 5-6 で完了済み。 |
| [architecture 14](../../architecture/ja/14.parallel_verification_and_scheduling.md) | Scheduling は semantics から分離される。task graph edge は correctness edge であり、result ordering は決定的である。 | tasks 7-20 と 24-26 の milestone invariant。 |
| [architecture 19](../../architecture/ja/19.failure_semantics.md) | failure category、blocked task state、有界 propagation、決定的 error ordering。 | tasks 15-16 が扱う。 |
| [architecture 22](../../architecture/ja/22.incremental_verification_contract.md) | 増分実行と並列実行は clean-build equivalent でなければならない。cache hit と proof reuse は authority ではない。 | tasks 18, 20, 24-25 が扱う。 |
| [internal 01](../../internal/ja/01.compiler_driver_and_pipeline_scheduler.md) | `mizar-build` は planning/scheduling を所有し、`mizar-driver` は request、registry、session、event を所有する。 | 全 task の boundary invariant。 |
| [internal 02](../../internal/ja/02.artifact_store_cache_key_and_manifest.md) | cache lookup は validation 後にのみ execution を skip できる。artifact commit は manifest-first で決定的である。 | tasks 17-18 が cache/artifact internals を再実装せず消費する。 |
| [internal 07](../../internal/ja/07.crate_module_layout.md) | `mizar-build` module は task graph、scheduler、resource、cancellation、failure state を含む。 | module spec と source task が精緻化する。 |

## 関連テスト

| Test path | Intent | Spec refs |
|---|---|---|
| `crates/mizar-build/tests/lint_policy.rs` | workspace lint opt-in、crate policy、task-21 public enum forward compatibility を守る。 | tasks 1 and 21, internal 07。 |
| `crates/mizar-build/src/planner/tests.rs` unit tests | manifest parsing、lockfile validation、dependency graph resolution、決定的 `BuildPlan` 生成、stable diagnostics を守る。 | tasks 3-4, spec 23, `planner.md`。 |
| `crates/mizar-build/src/module_index/tests.rs` unit tests | source discovery、dependency summary consumption、alias-independent module identity、決定的 diagnostics、provider lookup を守る。 | task 6, `module_index.md`, architecture 03。 |
| `crates/mizar-build/src/task_graph/tests.rs` unit tests | graph expansion、versioned task identity、correctness edge、dependency coverage、`BuildPlan` からの決定的 expansion を守る。 | tasks 7-8, `task_graph.md`。 |
| `crates/mizar-build/src/scheduler/tests.rs` unit tests | deterministic dispatch batching、priority hints、queue routing、dependency coverage、skip/cancel/block behavior、disabled and validated-hit cache seam boundaries、resource-admission integration、cancellation integration、telemetry collation、scheduler-selected dispatch callback behavior、canonical result/event collation を守る。 | tasks 9-10, 12, 14, 18, 27, `scheduler.md`, `resource.md`, `cancel.md`, `cache_seam.md`, `phase_dispatch.md`。 |
| `crates/mizar-build/src/resource.rs` unit tests | hierarchical budget accounting、worker pools、memory units、backend fanout、idempotent duplicate admission、overcommit なしの queueing を守る。 | tasks 11-12, `resource.md`。 |
| `crates/mizar-build/src/cancel.rs` unit tests | versioned cancellation、monotonic generation、token propagation、canonical cancellation-decision ordering、checkpoint decision を守る。 | tasks 13-14, `cancel.md`。 |
| `crates/mizar-build/src/failure_state.rs` と scheduler unit tests | direct failure record、explicit blocked state、nearest-blocker propagation、direct scheduler block reason、決定的 failure ordering を守る。 | tasks 15-16、`failure_state.md`。 |
| `crates/mizar-build/src/artifact_commit.rs` unit tests | deterministic manifest update staging、`mizar-artifact` transaction consumption、freshness rejection、conflict propagation、commit-boundary placeholder absence を守る。 | task 17、`artifact_commit.md`、internal 02。 |
| `crates/mizar-build/src/cache_seam.rs` と scheduler unit tests | caller-supplied cache scheduling decisions、validated-hit execution skip、miss/no-key/error fallback、duplicate and unknown-task-decision diagnostics、deterministic hit payload collation、non-authority boundaries を守る。 | task 18、`cache_seam.md`、internal 02。 |
| `crates/mizar-build/tests/batch_integration.rs` | 利用可能な batch path を plan、module index、task graph、scheduler、artifact manifest commit にまたがって守り、利用できない driver、IR、producer-token integrations を external gaps として記録する。 | task 19、`batch_integration.md`、internal 01。 |
| `crates/mizar-build/tests/determinism_suite.rs` | 実装済み plan、module-index、task-graph、scheduler、cache-seam、artifact-commit projections と、task-24 の実装済み seam clean/incremental/parallel equivalence gate の cross-boundary determinism を守る。完全な実 driver、IR、producer publication-token equivalence は BUILD-G-017 `external_dependency_gap` として残る。 | tasks 20 and 24、`determinism_suite.md`、`incremental_parallel_equivalence.md`、architecture 20/22。 |

Planned test-first additions:

- task 0 は protocol-required crate plan を追加するだけで language behavior を
  変更しないため、`.miz` test は不要である。
- Wave B の source task は、real driver、IR、producer、artifact boundaries が
  利用可能になるまで synthetic phase output を持つ Rust fixture を使う。
  それら利用できない external integrations は placeholder API で置き換えず、
  `external_dependency_gap` として記録する。task 18 は caller-supplied decisions を
  使う build-side cache consumer seam を実装する。real driver、IR、producer
  publication integration は external gaps のままである。task 19 は利用可能な
  public APIs 上の batch integration fixture を追加し、driver、IR、producer
  publication-token integration を external gaps として維持する。

## 設計およびソース inventory

Design files:

- `doc/design/mizar-build/en/todo.md`
- `doc/design/mizar-build/ja/todo.md`
- この crate plan と日本語 companion
- 完了済み module spec: `planner.md`, `module_index.md`, `task_graph.md`,
  `scheduler.md`, `resource.md`, `cancel.md`, `failure_state.md`,
  `artifact_commit.md`, `cache_seam.md`, `phase_dispatch.md`,
  `batch_integration.md`, `determinism_suite.md`
- source/spec correspondence report: `source_spec_correspondence.md`
- bilingual documentation sync report:
  `bilingual_documentation_synchronization.md`
- incremental/parallel equivalence gate note:
  `incremental_parallel_equivalence.md`
- architecture-22 follow-up audit report:
  `architecture_22_follow_up_audit.md`
- module-boundary refactor gate report:
  `module_boundary_refactor_gate.md`
- crate exit report:
  `crate_exit_report.md`

Source files:

- `crates/mizar-build/Cargo.toml`
- `crates/mizar-build/src/lib.rs`
- `crates/mizar-build/src/planner.rs`
- `crates/mizar-build/src/planner/tests.rs`
- `crates/mizar-build/src/module_index.rs`
- `crates/mizar-build/src/module_index/tests.rs`
- `crates/mizar-build/src/task_graph.rs`
- `crates/mizar-build/src/task_graph/tests.rs`
- `crates/mizar-build/src/resource.rs`
- `crates/mizar-build/src/cancel.rs`
- `crates/mizar-build/src/failure_state.rs`
- `crates/mizar-build/src/artifact_commit.rs`
- `crates/mizar-build/src/cache_seam.rs`
- `crates/mizar-build/src/scheduler.rs`
- `crates/mizar-build/src/scheduler/tests.rs`
- `crates/mizar-build/tests/batch_integration.rs`
- `crates/mizar-build/tests/determinism_suite.rs`
- `crates/mizar-build/tests/lint_policy.rs`

Observed behavior:

- Wave A planner/module-index source、wave B task-graph construction source、
  scheduler core source、resource-budget source、cancellation source、failure-state
  source、artifact-commit boundary source、cache-seam source は存在し、focused
  tests で覆われている。
- Wave B resource-budget spec と `src/resource.rs` は、modeled hierarchical admission、
  queueing、release telemetry、non-authority boundaries について一致している。
- Wave B cancellation spec と `src/cancel.rs` は、cooperative versioned
  cancellation、snapshot invalidation、partial-publication 禁止、non-authority
  boundaries について一致している。
- Wave B failure-state spec は、task 16 に向けて direct failure record、
  blocked-work record、有界 propagation、決定的 failure ordering、
  non-authority publication boundary を定義している。
- `src/failure_state.rs` と scheduler integration は、failed / blocked /
  cancelled work が output を publish しないまま、決定的な failure record と
  blocked record を emit する。
- `src/artifact_commit.rs` は `mizar-artifact` manifest transaction を消費し、
  module entries を canonical build-side order で stage し、freshness check を
  forwarding する。producer artifact payload や publication token は作らない。
- `src/cache_seam.rs` は scheduler が消費する caller-supplied validated hit/miss
  decisions だけを model する。cache keys、dependency fingerprints、proof-reuse
  validation は `mizar-cache` が所有し続ける。
- task-19 batch integration suite は、利用可能な planner、module-index、
  graph、scheduler、artifact-commit boundaries を接続し、driver、IR、
  cache-key、dependency-fingerprint、producer-token、proof-authority
  placeholders を作らない。
- task 20 は implemented-seam determinism suite として仕様化される。real
  driver/IR/producer-token clean/incremental equivalence は external
  integration gap のままである。
- `tests/determinism_suite.rs` は implemented seams について、shuffled logical
  inputs、scheduler worker/priority/completion variants、cache hit/miss
  placement、shuffled manifest updates、boundary placeholder absence を覆う。
- task 21 は現在のすべての `mizar-build` public enum を downstream
  forward-compatible な `#[non_exhaustive]` API として分類する。所有する各
  module spec には英語版と日本語版の `Public Enum Policy` table があり、
  exhaustive public enum exception がないことを記録する。
  `tests/lint_policy.rs` が source/spec drift を guard する。
- task 22 は `source_spec_correspondence.md` に、実装済みの全 public API
  family と behavior promise について source/spec/test correspondence を記録する。
  audit は新しい blocking/high drift を見つけておらず、BUILD-G-016 が
  non-blocking public-helper test gap を 1 件記録し、既存の driver、IR、
  producer-token、full real clean/incremental integration gaps は
  `external_dependency_gap` のまま残る。
- task 23 は、すべての英語正本 `mizar-build` design document が同名の
  日本語 companion を持ち、module boundary、public API family、task status、
  gap classification、boundary invariant、handoff wording が同期していることを
  `bilingual_documentation_synchronization.md` に記録する。
- task 24 は implemented-seam determinism suite を拡張し、clean sequential、
  clean parallel、incremental sequential、incremental parallel scheduler runs を
  またぐ architecture-22 equivalence gate を追加する。この gate は externally
  visible manifest、hash、diagnostic、failure、blocked-record projections だけを
  比較する。cache hits は execution skips のままであり、stale または superseded
  results は current manifest updates を publish しない。full real driver / IR /
  producer integration は `external_dependency_gap` のまま残る。
- task 25 は `architecture_22_follow_up_audit.md` で、task-24
  architecture-22 implemented-seam gate に対する source/spec と bilingual audits を
  再実行し、source/spec と bilingual audit baselines を更新する。stale-publication
  または deterministic commit-boundary gap は low severity 超で unresolved のまま
  残っていない。
- task 26 は `module_boundary_refactor_gate.md` に BUILD-G-018 を記録し、
  `planner`、`module_index`、`task_graph`、`scheduler` の大きな inline
  unit-test bodies を private child modules へ移動して解決する。public
  exports、diagnostics、deterministic renderings、schemas、behavior は変更して
  いない。
- closeout は hard-gate status、deferred items、verification results、human
  review surface、next-phase handoff を `crate_exit_report.md` に記録する。
- task 27 は [phase_dispatch.md](./phase_dispatch.md) と scheduler-selected
  callback API を追加する。この checkout では `mizar-driver` が存在し、その API を
  driver-to-build 方向から消費してよいが、`mizar-build` は `mizar-driver` に依存せず、
  driver sessions、registry semantics、phase input identity production、real IR
  outputs、producer publication tokens、cache compatibility、proof authority、
  artifact serialization、LSP conversion を所有しない。
- `doc/design/mizar-build/{en,ja}/todo.md` はすでに ordered task list と
  module table を定義している。
- protocol-required `00.crate_plan.md` は task 0 の前には存在しなかった。
- `crates/mizar-cache` は存在し、cache key、dependency fingerprint、
  proof-reuse validation を所有する。`mizar-build` はそれらの境界だけを
  消費しなければならない。
- この checkout には `crates/mizar-ir` と `crates/mizar-driver` が存在するが、
  `mizar-build` は driver-to-build 方向の external owner seams を通じてだけそれらを
  消費し、`mizar-driver` dependency を追加してはならない。
- real producer artifact publication token と full phase-15 emission
  integration は、`mizar-build` が消費できる状態ではない。

## 既知の gap と drift

`external_dependency_gap` は、利用できない upstream/downstream integration
境界を記録するための、この repository / user request で要求された分類である。
これはこの task では protocol taxonomy を拡張するが、planned `mizar-build`
work ではなく external seam の欠落を記録する場合にだけ使う。

| ID | Class | Evidence | Action |
|---|---|---|---|
| BUILD-G-001 | `design_drift` | autonomous crate protocol は `doc/design/mizar-build/en/00.crate_plan.md` を要求するが、この crate には TODO/module spec しかなかった。 | task 0 でこの英語 plan と日本語 companion を追加する。 |
| BUILD-G-002 | `external_dependency_gap` | `mizar-driver` は存在するが、request/session/registry/event/salsa authority は driver-owned であり `mizar-build` の外側に残る。 | `mizar-build` を entry-point agnostic に保ち、driver-owned API や driver dependency を追加しない。 |
| BUILD-G-003 | `external_dependency_gap` | real build-side IR output storage、sealed handles、snapshot rehydration は `mizar-build` から利用できない。 | scheduler tests では synthetic in-memory task output を使い、placeholder IR API を作らない。 |
| BUILD-G-004 | `external_dependency_gap` | real producer artifact publication token と full phase-15 emission input が公開されていない。 | 可能な場合は commit-order boundary だけを実装し、real producer publication integration は deferred として、token を創作しない。 |
| BUILD-G-005 | `source_drift` / `test_gap` | `mizar-cache` は存在し、`CacheKey`、dependency fingerprint、proof-reuse validation を所有する。task 18 以前の `mizar-build` には validated decision consumer seam がなかった。 | task 18 が consumer seam を追加し、必要な tests では synthetic caller-supplied decisions を使う。cache internals を再実装しない。 |
| BUILD-G-006 | `external_dependency_gap` | full resolver/checker/VC/proof/kernel/driver integration は end-to-end real build に利用できない。 | producer/consumer seam が存在するまで、Wave B tests は synthetic に保つ。 |
| BUILD-G-007 | `source_drift` / `test_gap` | task 15 後に `failure_state.md` は存在したが、task 16 以前には `src/failure_state.rs` と focused failure-state tests が存在しなかった。 | task 16 で source/test slice を追加した。diagnostic-registry、driver、IR、cache、artifact-token、proof-authority placeholder は創作しない。 |
| BUILD-G-008 | `source_drift` / `test_gap` | `artifact_commit.md` は task 17 で追加され、実装 slice 以前は `src/artifact_commit.rs` が存在しなかった。 | task 17 は `mizar-artifact` を消費する deterministic commit-order boundary だけを追加する。producer artifact payload と publication token は external gap のまま残す。 |
| BUILD-G-009 | `external_dependency_gap` | driver-owned cache query adapter、`mizar-ir` output rehydration、producer publication tokens はまだ存在しない。 | task-18 cache tests は synthetic に保ち、placeholder driver、IR、producer-token API を追加しない。 |
| BUILD-G-010 | `source_drift` / `test_gap` | 利用可能な planner -> module-index -> graph -> scheduler -> commit path は task 19 以前に単一 batch integration fixture で覆われていなかった。 | task 19 で `crates/mizar-build/tests/batch_integration.rs` に integration suite を追加する。 |
| BUILD-G-011 | historical `external_dependency_gap`; task-27 resolved source/test gap | earlier closeout は real build requests、sessions、phase registry、event streams、driver-owned salsa boundary を消費できないと記録していた。現在は `mizar-driver` が存在し、task 27 は build-owned scheduler-selected dispatch callback と tests を実装する。 | task 27 は `mizar-driver` dependency を追加せず callback seam を公開する。driver-owned phase input identities、registry execution resources、diagnostics/output publisher handles、event/session publication は driver/owner concerns のままである。 |
| BUILD-G-012 | `external_dependency_gap` | real output handles と snapshot-handle rehydration は build-owned seam 経由では利用できない。 | task 19 では scheduler-owned synthetic output refs だけを使い、placeholder IR handles を作らない。 |
| BUILD-G-013 | `external_dependency_gap` | Real producer artifact publication tokens と full phase-15 emission inputs は利用できない。 | caller-supplied `ModuleArtifactEntry` values を `mizar-artifact` 経由で commit し、tokens や producer authority を作らない。 |
| BUILD-G-014 | `test_gap` | 実装済み seams には focused determinism tests があったが、task 20 以前に plan/index/graph/scheduler/cache/commit projections を単一 deterministic path として比較する cross-boundary suite はなかった。 | task 20 で `crates/mizar-build/tests/determinism_suite.rs` を追加する。 |
| BUILD-G-015 | `external_dependency_gap` | Full clean/incremental equivalence には real `mizar-driver`、`mizar-ir`、producer-token integrations がまだない。 | task 20 は implemented seams に限定し、full external integration は placeholders なしで後続 tasks に残す。 |
| BUILD-G-016 | `test_gap` | task 22 は public helper `sorted_manifest_updates` が `commit_manifest_updates` を通じて間接的に covered だが、standalone canonical-ordering の direct test を持たないことを確認した。 | method-level helper coverage を主張する前に、後続 artifact-commit hardening slice で focused test を追加する。 |
| BUILD-G-017 | `external_dependency_gap` | task 24 は architecture-22 equivalence を実装済み `mizar-build` seams に限って覆うが、real driver sessions、real IR output rehydration、producer-owned artifact projection、producer publication tokens は存在しない。 | equivalence gate は synthetic かつ build-side に保つ。placeholder driver、IR、producer-token、cache-key、dependency-fingerprint、proof-reuse、proof-authority APIs を追加しない。 |
| BUILD-G-019 | `external_dependency_gap` | real phase dispatch は owner-provided phase input identities、parent output handles、diagnostics sinks、output publishers を必要とする。これらは build-owned ではない。 | missing owner inputs は dispatch/owner gaps として記録する。phase inputs、producer outputs、diagnostics records、IR handles、cache decisions、artifact tokens を fabricate しない。 |

## Boundary invariants

| ID | Boundary | Rule |
|---|---|---|
| BUILD-I-001 | cache/proof authority | scheduling や cache reuse を proof authority として扱うと unsound になる。すべての scheduler/cache/commit task は clean-build equivalence と trust の非昇格を保つ。 |
| BUILD-I-002 | driver/build dependency | internal 01 は build request と registry ownership を `mizar-driver` に割り当てる。`mizar-build` は `mizar-driver` に依存してはならない。逆方向依存は boundary violation である。 |

## Task decomposition

### Task 0: Protocol-required crate plan

Purpose: wave B autonomous work を続ける前に、crate responsibility、
authority references、gaps、task decomposition、exit gates を確立する。

Spec refs: autonomous crate development protocol、architecture 14/19/22、
internal 01/02/07、`todo.md`。

Tests: documentation と review のみ。

Affected design files: この plan と Japanese companion。

Affected source files: なし。

Completion condition: paired plan files が存在し、reviews に
blocking/high findings が残らず、diff checks が通り、task が単独で
commit される。

Forbidden behavior: source implementation なし。

### Tasks 1-6: Completed wave A planning and module-index work

Purpose: phase-0 planning と module-index construction を、後続
task-graph expansion の土台として維持する。

Spec refs: spec 23、architecture 03/14、internal 01、`planner.md`、
`module_index.md`。

Tests: 既存 planner/module-index unit tests と lint-policy guard。

Affected design/source files: 既存 `planner.md`、`module_index.md`、
`src/planner.rs`、`src/module_index.rs`、`src/lib.rs`、lint tests。

Completion condition: prior commits で完了済み。今後の作業はこれらの API
や dependency boundary を regress してはならない。

Forbidden behavior: resolver-owned import semantics、driver request handling、
source identity allocation、proof behavior なし。

### Task 7: Spec `task_graph.md`

Purpose: task kind、versioned task identity、correctness dependency edge、
phase grouping、VC granularity、初期 granularity decision を定義する。

Spec refs: architecture 14 "Task Graph" と "VCs Are Fine-Grained Tasks"、
architecture 22、internal 01。

Tests: documentation review のみ。

Affected design files: 両言語 directory の `task_graph.md`。

Affected source files: なし。

Completion condition: 英語版と日本語版 spec が同期され、review 済みである。

Forbidden behavior: source implementation、driver/IR placeholder なし。

### Task 8: Task graph construction

Purpose: `BuildPlan`、`ModuleIndex`、dependency overlay、explicit VC descriptors
を、synthetic phase unit と module-index entry 上の決定的な versioned task graph
へ展開する。

Spec refs: `task_graph.md`。

Tests: graph expansion fixtures、dependency-edge fixtures、missing overlay
coverage fixtures、explicit VC descriptor ordering、該当する場合の
cycle/blocking fixtures、deterministic expansion。

Affected design files: source review で spec drift が見つかった場合のみ。

Affected source files: `src/task_graph.rs`、`src/lib.rs`、tests。

Completion condition: source/tests/spec が一致し、crate verification が通る。

Forbidden behavior: phase semantics、proof status、driver sessions、IR
storage、cache keys なし。

### Task 9: Spec `scheduler.md`

Purpose: work queue、priority policy、batch/watch/LSP scheduling contracts、
build-event shape、deterministic result ordering を定義する。

Spec refs: architecture 14 "Deterministic Result Ordering"、internal 01
"Pipeline Scheduler"。

Tests: documentation review のみ。

Affected design files: 両言語 directory の `scheduler.md`。

Affected source files: なし。

Completion condition: 英語版と日本語版 spec が同期され、review 済みである。

Forbidden behavior: source implementation なし。

### Task 10: Scheduler core

Purpose: task graph 上で synthetic tasks を実行し、任意の completion order
の下で決定的な result/event collation を保つ。

Spec refs: `scheduler.md`, `task_graph.md`。

Tests: shuffled completion、worker-count variation、immutable output
publication、deterministic event/result order。

Affected source files: `src/scheduler.rs`、必要な場合のみ `src/task_graph.rs`、
`src/lib.rs`、tests。

Completion condition: source/tests/spec が一致し、crate verification が通る。

Forbidden behavior: proof acceptance、cache reuse、artifact file I/O、driver
registry、worker-completion ordering を semantic order とすること。

### Task 11: Spec `resource.md`

Purpose: hierarchical resource budget と queue admission rules を定義する。

Spec refs: architecture 14 "Resource Budgets Are Hierarchical"。

Tests: documentation review のみ。

Affected design files: 両言語 directory の `resource.md`。

Affected source files: なし。

Completion condition: 英語版と日本語版 spec が同期され、review 済みである。

Forbidden behavior: source implementation なし。

### Task 12: Resource budgets

Purpose: scheduler 内で budget accounting と enforcement を実装する。

Spec refs: `resource.md`, `scheduler.md`。

Tests: budget exhaustion が overcommit ではなく queueing になること、
workspace/package/module/obligation/backend/commit の hierarchical composition、
deterministic admission ordering、terminal release が exactly once であること、
ATP portfolio/backend-slot separation、backend fanout/global process limits、
impossible-request diagnostics、worker-count collation stability、
shuffled ready/completion admission determinism、telemetry と I/O commit permits が
non-authority を保つこと、`mizar-driver`、`mizar-cache`、ATP OS-process、
artifact publication token、proof-authority placeholder を導入しないこと。

Affected source files: `src/resource.rs`、`src/scheduler.rs`、`src/lib.rs`、
tests。

Affected design files: 両言語 directory の同期済み `scheduler.md`、`resource.md`、
この plan、`todo.md`。

Implementation notes: `worker_count` は synthetic executor dispatch cap のままであり、
`ResourceBudget` は別の build-side admission layer である。per-obligation backend
fanout limit は `ResourceBudget` field であり、`TaskGraph` descriptor field ではない。

Completion condition: source/tests/spec が一致し、crate verification が通る。

Forbidden behavior: modeled budget decision を超える OS process management なし。

### Task 13: Spec `cancel.md`

Purpose: cooperative versioned cancellation、snapshot invalidation、
no-partial-publication rule を定義する。

Spec refs: architecture 14 "Cancellation Is Cooperative and Versioned"、
architecture 22、internal 01。

Tests: documentation review のみ。

Affected design files: 両言語 directory の `cancel.md`。

Affected source files: なし。

Completion condition: 英語版と日本語版 spec が同期され、review 済みである。

Result: versioned cancellation、snapshot invalidation、cooperative checkpoint、
publication freshness guard、resource-release handoff、cache/artifact/proof
non-authority boundaries、driver session、IR output storage、backend process
termination、producer publication token の external dependency gaps を扱う
同期済み `cancel.md` specs を追加した。

Forbidden behavior: source implementation なし。

### Task 14: Cancellation

Purpose: cancellation token、snapshot-version invalidation、publication 前の
obsolete completed work の discard を実装する。

Spec refs: `cancel.md`, `scheduler.md`。

Tests: pending/ready の開始前 cancellation、safe checkpoint での running
cancellation、monotonic generation / token propagation、canonical cancellation
decision ordering、publication 前の stale completed-result discard、cancelled
work から current diagnostics、cache records、artifact commit attempts が出ないこと、
exactly-once resource release、modeled atomic transaction 開始前/後の commit-boundary
挙動、deterministic / idempotent cancellation、driver/cache/IR/process/artifact-token/
proof-authority placeholder がないこと。

Affected source files: `src/cancel.rs`、`src/scheduler.rs`、`src/lib.rs`、
tests。

Completion condition: source/tests/spec が一致し、crate verification が通る。

Result: monotonic cancellation generation、token、canonical decision、snapshot
freshness check、commit-started decision、pre-start / checkpoint /
obsolete-completed cancellation の scheduler integration を備えた
`src/cancel.rs` を実装した。focused tests は no-current-publication、resource
release、deterministic collation、commit-boundary behavior、driver/cache/IR/
process/artifact-token/proof-authority placeholder がないことを覆う。

Forbidden behavior: worker thread の hard kill、obsolete result publication
なし。

### Task 15: Spec `failure_state.md`

Purpose: blocked/failed task states、有界 propagation、stable failure
categories、deterministic failure reporting を定義する。

Spec refs: architecture 14 "Failure Propagation Is Bounded"、architecture 19。

Tests: documentation review のみ。

Affected design files: 両言語 directory の `failure_state.md`。

Affected source files: なし。

Result: direct failure、blocked-work records、有界 propagation、stable phase
categories、deterministic ordering、publication / authority boundaries、
task 16 coverage を扱う同期済み `failure_state.md` specs を追加した。

Completion condition: 英語版と日本語版 spec が同期され、review 済みである。

Forbidden behavior: source implementation なし。

### Task 16: Failure propagation

Purpose: 明示的な blocked/failed state と bounded propagation、
deterministic reporting を実装する。

Spec refs: `failure_state.md`, `scheduler.md`。

Tests: 1 つの failing task が正確にその dependents だけを block すること、
independent failures が可視のままであること、failure order が決定的であること。

Affected source files: `src/failure_state.rs`、`src/scheduler.rs`、必要な場合のみ
`src/task_graph.rs`、`src/lib.rs`、tests。

Result: direct failure record、blocked-task record、stable block reason、
deterministic sorting、synthetic task-category projection を備えた
`src/failure_state.rs` を追加した。scheduler は `failure_records` と
`blocked_records` の emission、nearest-blocker propagation、direct scheduler
block reason、failed / blocked work の no-output publication を統合した。

Completion condition: source/tests/spec が一致し、crate verification が通る。

Forbidden behavior: blocked work の silent omission、deterministic
predecessor failure 後の proof success なし。

### Task 17: Deterministic commit boundary

Purpose: artifact-owned boundary が利用可能な場合に、scheduler-side commit
ordering を artifact manifest transactions と統合する。

Spec refs: internal 01 "Artifact Commit Boundary"、internal 02、
architecture 14、`scheduler.md`、`artifact_commit.md`。

Tests: shuffled completion でも mock または real artifact boundary を通じて
同一 manifest updates を commit すること、interrupted commit が previous state
を visible のまま残すこと、publication order が canonical であること。

Affected source files: `src/artifact_commit.rs`、`src/lib.rs`、
`Cargo.toml`、scheduler docs。

Result: deterministic manifest update ordering、`mizar-artifact`
`ManifestTransaction` consumption、freshness-check forwarding、deterministic
commit records、driver、IR、cache、proof-authority、producer-publication-token
placeholder を避ける boundary tests を備えた `src/artifact_commit.rs` を追加した。

Completion condition: source/tests/spec が一致し、必要に応じて
`mizar-artifact` boundary verification を実行し、external producer-token gaps
を stub ではなく記録する。

Forbidden behavior: artifact schema ownership、manifest transaction
reimplementation、fake publication tokens、worker-completion commit order なし。

### Task 18: Cache-aware scheduling seam

Purpose: task execution 前の consumer-side cache lookup decision seam を追加し、
既定は uncached execution とする。

Spec refs: internal 02 "Cache Lookup Before Task Execution"、architecture 14、
architecture 22、`scheduler.md`、`mizar-cache` public contracts。

Tests: synthetic caller-supplied cache scheduling decisions、hit が externally
visible results を同一に保ったまま execution を skip すること、miss/no-key/error
decisions が execution を強制すること。

Affected source files: `src/scheduler.rs`、`src/cache_seam.rs`、`src/lib.rs`。

Completion condition: source/tests/spec が一致し、可能な範囲で関連
cache/artifact/proof verification を実行し、seam が `mizar-cache` を
再実装せず消費する。task 18 は caller-supplied validated decisions を受け取り、
driver、IR、producer-token integration を external gaps として残すことでこれを満たす。

Forbidden behavior: local `CacheKey`、dependency fingerprint、proof reuse
validation implementation なし。cache hit から semantic acceptance や
trusted-status promotion を行わない。`mizar-driver` dependency なし。

### Task 19: Batch-build integration suite

Purpose: その時点で利用可能な phase services を通じて、小さな workspace 上の
plan -> graph -> schedule -> commit を検証する。

Spec refs: internal 01 "Batch Build"、`task_graph.md`、`scheduler.md`、
commit boundary docs、利用可能な service specs。

Tests: 利用可能な public APIs による end-to-end batch fixture、決定的
diagnostics/publication order、利用できない driver/IR/producer boundaries の
明示的 gap checks。

Affected design files: `batch_integration.md`、TODO status、この crate plan。

Affected source files: `crates/mizar-build/tests/batch_integration.rs`。新しい
driver-owned API は追加しない。

Completion condition: 利用可能な batch path が verification を通り、利用できない
driver、IR、producer-token services が `external_dependency_gap` として分類される。
task 19 は `tests/batch_integration.rs` によりこれを満たす。この test は
frontend-shaped tasks を synthetic outcomes で schedule し、caller-supplied
`ModuleArtifactEntry` values を `mizar-artifact` 経由で commit する。suite は
dependency/source placeholder の明示的 guard と、hit が manifest update や
proof authority にならないことを示す integrated validated-cache-hit case も含む。

Forbidden behavior: fake `mizar-driver`、`mizar-ir`、producer artifact tokens、
proof authority なし。

### Task 20: Determinism suite

Purpose: 同一入力に対する plans、graphs、schedules、events、commits の
worker counts をまたいだ決定性 coverage を広げる。

Spec refs: architecture 20、architecture 14、architecture 22、実装済みの全
module specs。

Tests: worker-count variation、ready-queue priority variation、利用可能な場合の
cache hit/miss timing、shuffled manifest updates、implemented seams の同一
canonical outputs に対する table-driven fixtures。

Affected design files: `determinism_suite.md`、TODO status、この crate plan。

Affected source files: `crates/mizar-build/tests/determinism_suite.rs`。小さな
test hooks は module specs で文書化される場合のみ。

Completion condition: 実装済み seam に対する determinism coverage が通り、
利用できない real driver、IR、producer-token integrations が
`external_dependency_gap` として分類される。task 20 は
`tests/determinism_suite.rs` によりこれを満たす。full real clean/incremental
equivalence は後続 external integration 向けの BUILD-G-015 として残る。

Forbidden behavior: timing、worker id、cache lookup timing、completion order
を canonical semantic/artifact ordering に含めない。

### Task 21: Public-enum forward-compatibility policy

Purpose: workspace の public-enum compatibility policy をすべての
`mizar-build` public enum に適用する。

Spec refs: 全 module specs と既存 public-enum procedure。

Tests: 該当する enum forward-compatibility を守る focused API/lint tests または
compile-time fixtures。task 21 は
`public_build_enums_are_forward_compatible_and_documented` lint-policy test
でこれを満たす。

Affected design files: public API policy の明確化が必要な場合のみ module specs
と TODO status。task 21 は所有するすべての module specs に同期済みの public-enum
policy table を追加する。

Affected source files: public enum declarations、API tests、exports。

Completion condition: すべての public enum が監査され、更新されるか、spec に
より意図的に closed と分類される。task 21 は現在のすべての public enum を監査し、
すべて `#[non_exhaustive]` として記録し、意図的に closed な public enum exception
は 0 件である。

Forbidden behavior: semantic behavior changes や無関係な API expansion なし。

### Task 22: Source/spec correspondence audit

Purpose: module specs の全 public API と約束された挙動を source と tests へ
trace し、gap を follow-up tasks として記録する。

Spec refs: 全 `mizar-build` module specs とこの crate plan。

Tests: audit-only。ただし review が missing required coverage を見つけた場合は、
別の implementation slice で focused tests を追加する。

Affected design files: source/spec correspondence report、TODO status、drift を
修正する場合の module specs。

Affected source files: 監査された source drift の修正が小さく review-approved
である場合のみ。

Completion condition: audit report が source/spec/test correspondence を記録し、
unresolved blocking/high drift が残らない。task 22 は
`source_spec_correspondence.md` によりこれを満たし、新しい blocking/high drift は
見つからなかった。

Forbidden behavior: incorrect source behavior に合わせた docs rebaseline なし。

### Task 23: Bilingual documentation sync audit

Purpose: `doc/design/mizar-build/en/` 配下のすべての English canonical document
を Japanese companion と比較し、内容を同期する。

Spec refs: repository documentation policy とすべての `mizar-build` docs。

Tests: documentation review と diff checks。

Affected design files: English/Japanese companion docs、bilingual sync audit、
TODO status。

Affected source files: なし。

Completion condition: bilingual audit が同期済み、または理由付きで明確に
deferred された companion updates を報告する。

task 23 は `bilingual_documentation_synchronization.md` によりこれを満たす。
deferred された companion update は残っていない。

Forbidden behavior: source implementation なし。

### Task 24: Incremental/parallel equivalence gate

Purpose: 同じ `BuildSnapshot` と verifier policy に対して clean sequential、
clean parallel、incremental sequential、incremental parallel が一致することを
確認する scheduler-level architecture-22 regression gate を追加する。

Spec refs: architecture 22、architecture 14、architecture 20、`scheduler.md`、
`cancel.md`、cache/commit seam specs。

Tests: randomized ready-task scheduling、worker counts、synthetic cache decision
hit/miss timing、cancellation/supersession、deterministic commit boundary behavior。

Affected design files: gate が drift を露出した場合の TODO status、equivalence
notes、module specs。

Affected source files: scheduler/cache/cancel/commit tests と、既存 specs に
文書化された小さな test hooks。

Completion condition: 実装済み seam に対して equivalence gate が通り、利用
できない real driver/IR/producer integrations が明示的に分類される。

task 24 は `incremental_parallel_equivalence.md` と
`crates/mizar-build/tests/determinism_suite.rs` によりこれを満たす。BUILD-G-017 が
残る external integration gap を記録する。

Forbidden behavior: stale publication を current output として扱わない。
cache-hit trust promotion なし。completion-order-dependent proof/artifact
status なし。

### Task 25: Architecture-22 follow-up audit

Purpose: task 24 の後に、scheduler equivalence、cancellation、cache seam
behavior について source/spec と bilingual audits を再実行する。

Spec refs: 全 module specs、architecture 22、この TODO、documentation policy。

Tests: audit-only。ただし missing coverage が見つかり、監査 scope 内で修正
する場合を除く。

Affected design files: follow-up audit report、TODO status、必要な場合の synced
docs。

Affected source files: audit findings が要求する narrow fixes のみ。

Completion condition: stale-publication または deterministic commit-boundary gap
が low severity 超で unresolved のまま残らない。

task 25 は `architecture_22_follow_up_audit.md` によりこれを満たす。
stale-publication または deterministic commit-boundary finding は low severity
超で unresolved のまま残っていない。

Forbidden behavior: architecture-22 follow-up scope の外の新しい behavior なし。

### Task 26: Module-boundary refactor gate

Purpose: oversized files、mixed responsibilities、module table と spec boundaries
に沿って分割すべき helpers がないか source layout を監査する。

Spec refs: この TODO、internal 07、全 module specs。

Tests: 既存 behavior tests と、moved APIs に対する source/spec・bilingual
audits。

Affected design files: module-boundary report、TODO status、file が移動する
場合の module table updates。

Affected source files: private module splits のみ。必要に応じて unchanged
public exports と tests。

Completion condition: 必要な split が behavior-preserving で、review・verified
済みであり、crate hard gates が closeout 可能な状態である。

task 26 は `module_boundary_refactor_gate.md` と `planner`、`module_index`、
`task_graph`、`scheduler` の private unit-test module split によりこれを満たす。
public API、diagnostic、schema、behavior change は行っていない。

Forbidden behavior: module move 内で API exposure、behavior cleanup、
diagnostic change、schema change、unrelated refactor を行わない。

### Task 27: Scheduler-selected phase dispatch callback seam

Purpose: DRIVER-G-011 に必要な `mizar-build` scheduler-selected callback seam を公開し、
scheduler ownership と driver/registry ownership boundaries を保つ。

Spec refs: [phase_dispatch.md](./phase_dispatch.md)、[scheduler.md](./scheduler.md)、
internal 01、architecture 14/22、mizar-driver `driver.md` / `registry.md`。

Tests: focused scheduler dispatch tests、driver registry-consumption contract
tests、既存 boundary/source guards。

Affected design files: この plan、`todo.md`、`scheduler.md`、`phase_dispatch.md`、
source/spec audit docs、日本語 companion。

Affected source files: `crates/mizar-build/src/scheduler.rs`、
`crates/mizar-build/src/scheduler/tests.rs`、選択された `mizar-driver` scheduler
consumption helpers/tests、新しい public enum が導入される場合の lint-policy inventory。

Completion condition: scheduler が `run_scheduler_with_dispatcher` を公開し、driver が
real owner-provided dispatch inputs つきで registry 経由で消費でき、missing owner inputs
は classified gaps のまま残り、review-only agents が blocking/high finding なしを報告し、
verification が pass し、task が 1 commit として記録される。

Forbidden behavior: `mizar-build` に `mizar-driver` dependency を追加しない。
`mizar-driver` に scheduler semantics を置かない。fake phase adapter、stub producer
output、provisional artifact token、cache/proof authority、artifact serialization、
diagnostics identity allocation、LSP protocol conversion を作らない。

## Exit criteria

Hard gates:

- blocking/high specification inconsistency が残っていない。
- `mizar-build` source behavior が module specs または記録済み
  external gap list なしに残っていない。
- source、tests、英語 docs、日本語 companion docs が同期している。
- 利用できない downstream integration は stub ではなく
  `external_dependency_gap` として分類されている。
- crate が `mizar-driver` に依存していない。
- cache-aware scheduling は optimization-only execution-skip seam として
  文書化・実装されている。
- cache hits と artifact records が proof trust や semantic acceptance を
  昇格しない。
- task graph、scheduler、resource budget、cancellation、failure propagation、
  deterministic commit boundary、cache seam behavior が focused tests または
  明示的 deferred reasons で覆われている。
- review-only spec/test/full/source-doc reviews に unresolved blocking/high
  findings が残っていない。
- required verification commands が通るか、未実行 command が明示的に正当化
  されている。

Verification commands:

```text
cargo test -p mizar-build
cargo clippy -p mizar-build --all-targets -- -D warnings
cargo fmt --check
```

scheduler/cache/artifact/commit boundaries に触れた場合は、利用可能な隣接
crate checks も実行する:

```text
cargo test -p mizar-cache
cargo test -p mizar-artifact
cargo test -p mizar-vc
cargo test -p mizar-proof
cargo test -p mizar-driver
```

crate completion では以下を実行する:

```text
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Review expectations:

- 各 task は AGENTS.md の spec/documentation、test sufficiency、full
  implementation、source/documentation consistency の review loop に従う。
- 残る low/medium notes は concrete deferred reason を添えて記録するか、
  closeout 前に修正する。
- crate completion には read-only quality score 90/100 以上が必要である。

Closeout status: `crate_exit_report.md` は hard-gate evidence、deferred items、
crate-completion verification、quality score 96/100、next-phase handoff を記録する。
