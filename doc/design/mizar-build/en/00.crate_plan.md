# Crate Plan: mizar-build

> Canonical language: English. Japanese companion:
> [../ja/00.crate_plan.md](../ja/00.crate_plan.md).

## Responsibility

Owned behavior:

- pipeline phase 0 workspace planning: package/workspace manifests, lockfile
  validation, dependency graph resolution, and deterministic `BuildPlan`
  production;
- phase-0 module-index construction from `BuildPlan` plus source layout or
  dependency artifact summaries;
- the versioned task graph used by parallel verification and incremental
  invalidation;
- scheduling, deterministic result collation, resource budgets, cooperative
  cancellation, bounded failure propagation, and deterministic artifact commit
  ordering;
- a cache-aware scheduling seam that can consume `mizar-cache` decisions before
  execution while preserving clean-build equivalence.

Out of scope:

- build requests, CLI/watch/LSP entry points, build sessions, event streams,
  phase-service registry, and the `salsa` query/database boundary, all of which
  are owned by `mizar-driver`;
- raw IR storage, output handles, and snapshot-handle rehydration, which remain
  `mizar-ir` work;
- `mizar-cache` `CacheKey` construction, dependency fingerprint construction,
  cache-store compatibility checks, or proof-reuse validation;
- artifact schema ownership, manifest transaction internals, proof witness
  publication tokens, and producer-owned artifact projection;
- proof authority, proof search, ATP/backend winner selection, kernel
  acceptance, or proof-status promotion.

`mizar-build` may skip task execution after a validated cache hit, but the hit
is an optimization only. It must never become semantic acceptance, proof
authority, or a reason to upgrade a task, artifact record, backend result, or
externally attested proof to a more trusted status.

`mizar-build` must not depend on `mizar-driver`. The dependency direction is
driver-to-build.

## Specification Items

| Spec ref | Requirement | Status |
|---|---|---|
| [spec 23](../../../spec/en/23.package_management_and_build_system.md) | Package manifests, workspaces, lockfiles, build lifecycle, cache/artifact directories, and reproducibility. | Covered by planner tasks 2-4 and by later graph/scheduler/commit tasks. |
| [architecture 03](../../architecture/en/03.module_and_symbol_resolution.md) | Build-side package and module indexes feed resolver-owned import/name resolution. | Covered by tasks 5-6. |
| [architecture 14](../../architecture/en/14.parallel_verification_and_scheduling.md) | Scheduling is separate from semantics; task graph edges are correctness edges; result ordering is deterministic. | Milestone invariant for tasks 7-20 and 24-26. |
| [architecture 19](../../architecture/en/19.failure_semantics.md) | Failure categories, blocked task states, bounded propagation, and deterministic error ordering. | Covered by tasks 15-16. |
| [architecture 22](../../architecture/en/22.incremental_verification_contract.md) | Incremental and parallel execution must be clean-build equivalent; cache hits and proof reuse are not authority. | Covered by tasks 18, 20, and 24-25. |
| [internal 01](../../internal/en/01.compiler_driver_and_pipeline_scheduler.md) | `mizar-build` owns planning/scheduling while `mizar-driver` owns requests, registry, sessions, and events. | Boundary invariant for every task. |
| [internal 02](../../internal/en/02.artifact_store_cache_key_and_manifest.md) | Cache lookup may skip execution only after validation; artifact commit is manifest-first and deterministic. | Consumed by tasks 17-18 without reimplementing cache or artifact internals. |
| [internal 07](../../internal/en/07.crate_module_layout.md) | `mizar-build` modules include task graph, scheduler, resources, cancellation, and failure state. | Refined by module specs and source tasks. |

## Relevant Tests

| Test path | Intent | Spec refs |
|---|---|---|
| `crates/mizar-build/tests/lint_policy.rs` | Guard workspace lint opt-in and crate policy. | task 1, internal 07. |
| `crates/mizar-build/src/planner.rs` unit tests | Guard manifest parsing, lockfile validation, dependency graph resolution, deterministic `BuildPlan` production, and stable diagnostics. | tasks 3-4, spec 23, `planner.md`. |
| `crates/mizar-build/src/module_index.rs` unit tests | Guard source discovery, dependency summary consumption, alias-independent module identity, deterministic diagnostics, and provider lookups. | task 6, `module_index.md`, architecture 03. |
| `crates/mizar-build/src/task_graph.rs` unit tests | Guard graph expansion, versioned task identity, correctness edges, dependency coverage, and deterministic expansion from a `BuildPlan`. | tasks 7-8, `task_graph.md`. |
| Future `scheduler` tests | Guard deterministic result/event order under shuffled completion and immutable published outputs. | tasks 9-10. |
| Future `resource` tests | Guard hierarchical budget accounting and queueing without overcommit. | tasks 11-12. |
| Future `cancel` tests | Guard versioned cancellation, snapshot supersession, and no partial publication. | tasks 13-14. |
| Future `failure_state` tests | Guard bounded propagation, explicit blocked states, and deterministic failure ordering. | tasks 15-16. |
| Future commit/cache/equivalence tests | Guard deterministic manifest commit ordering, cache-hit execution skip equivalence, and architecture-22 clean/incremental/parallel agreement. | tasks 17-20 and 24. |

Planned test-first additions:

- No `.miz` tests are required for task 0 because it adds the protocol-required
  crate plan and does not change language behavior.
- Wave B source tasks use Rust fixtures with synthetic phase outputs until real
  driver, IR, producer, and artifact seams are available. Those unavailable
  external integrations are recorded as `external_dependency_gap` rather than
  replaced by placeholder APIs. The build-side cache consumer seam is planned
  `mizar-build` work in task 18 and remains `deferred` until implemented.

## Design And Source Inventory

Design files:

- `doc/design/mizar-build/en/todo.md`
- `doc/design/mizar-build/ja/todo.md`
- this crate plan and its Japanese companion
- completed module specs: `planner.md`, `module_index.md`, `task_graph.md`,
  `scheduler.md`
- future module specs: `resource.md`, `cancel.md`, and `failure_state.md`
- future source/spec correspondence, bilingual sync, module-boundary, and
  closeout reports created by hardening tasks.

Source files:

- `crates/mizar-build/Cargo.toml`
- `crates/mizar-build/src/lib.rs`
- `crates/mizar-build/src/planner.rs`
- `crates/mizar-build/src/module_index.rs`
- `crates/mizar-build/src/task_graph.rs`
- `crates/mizar-build/tests/lint_policy.rs`
- future wave B modules: `src/scheduler.rs`, `src/resource.rs`,
  `src/cancel.rs`, and `src/failure_state.rs`

Observed behavior:

- Wave A planner/module-index source and wave B task-graph construction source
  are present and covered by focused tests.
- `doc/design/mizar-build/{en,ja}/todo.md` already defines the ordered task
  list and module table.
- The protocol-required `00.crate_plan.md` was absent before task 0.
- `crates/mizar-cache` exists and owns cache keys, dependency fingerprints, and
  proof-reuse validation. `mizar-build` must consume those boundaries only.
- `crates/mizar-ir` and `crates/mizar-driver` are absent in this checkout.
- Real producer artifact publication tokens and full phase-15 emission
  integration are not ready for `mizar-build` to consume.

## Known Gaps And Drift

`external_dependency_gap` is the repository/user-requested classification for
missing upstream or downstream integration boundaries. It extends the protocol
taxonomy for this task and is used only to record unavailable external seams,
not planned `mizar-build` work.

| ID | Class | Evidence | Action |
|---|---|---|---|
| BUILD-G-001 | `design_drift` | The autonomous crate protocol requires `doc/design/mizar-build/en/00.crate_plan.md`, but the crate only had TODO/module specs. | Task 0 adds this English plan and Japanese companion. |
| BUILD-G-002 | `external_dependency_gap` | `mizar-driver` request/session/registry/salsa tasks are open and the crate is absent. | Keep `mizar-build` entry-point agnostic and do not add driver-owned APIs or a driver dependency. |
| BUILD-G-003 | `external_dependency_gap` | `mizar-ir` is absent; output storage, sealed handles, and snapshot rehydration are unavailable. | Use synthetic in-memory task outputs for scheduler tests and do not create placeholder IR APIs. |
| BUILD-G-004 | `external_dependency_gap` | Real producer artifact publication tokens and full phase-15 emission inputs are not exposed. | Implement only the commit-order boundary when possible; defer real producer publication integration and do not invent tokens. |
| BUILD-G-005 | `deferred` | `mizar-cache` exists and owns `CacheKey`, dependency fingerprints, and proof-reuse validation, but the build-side consumer seam is still task 18. | Implement the `mizar-build` consumer seam in task 18 with mock decisions where needed; do not reimplement cache internals. |
| BUILD-G-006 | `external_dependency_gap` | Full resolver/checker/VC/proof/kernel/driver integration is not available for end-to-end real builds. | Keep wave B tests synthetic until producer/consumer seams exist. |

## Boundary Invariants

| ID | Boundary | Rule |
|---|---|---|
| BUILD-I-001 | cache/proof authority | Scheduling or cache reuse would be unsound if treated as proof authority. Every scheduler/cache/commit task must preserve clean-build equivalence and non-promotion of trust. |
| BUILD-I-002 | driver/build dependency | Internal 01 assigns build requests and registry ownership to `mizar-driver`. `mizar-build` must not depend on `mizar-driver`; any reverse dependency is a boundary violation. |

## Task Decomposition

### Task 0: Protocol-required crate plan

Purpose: establish crate responsibility, authority references, gaps, task
decomposition, and exit gates before wave B autonomous work continues.

Spec refs: autonomous crate development protocol, architecture 14/19/22,
internal 01/02/07, and `todo.md`.

Tests: documentation and review only.

Affected design files: this plan and Japanese companion.

Affected source files: none.

Completion condition: paired plan files exist, reviews report no blocking/high
findings, diff checks pass, and the task is committed separately.

Forbidden behavior: no source implementation.

### Tasks 1-6: Completed wave A planning and module-index work

Purpose: keep phase-0 planning and module-index construction available as the
foundation for later task-graph expansion.

Spec refs: spec 23, architecture 03/14, internal 01, `planner.md`, and
`module_index.md`.

Tests: existing planner and module-index unit tests plus lint-policy guard.

Affected design/source files: existing `planner.md`, `module_index.md`,
`src/planner.rs`, `src/module_index.rs`, `src/lib.rs`, and lint tests.

Completion condition: already completed in prior commits; future work must not
regress these APIs or dependency boundaries.

Forbidden behavior: no resolver-owned import semantics, driver request
handling, source identity allocation, or proof behavior.

### Task 7: Spec `task_graph.md`

Purpose: define task kinds, versioned task identity, correctness dependency
edges, phase grouping, VC granularity, and the initial granularity decision.

Spec refs: architecture 14 "Task Graph" and "VCs Are Fine-Grained Tasks",
architecture 22, and internal 01.

Tests: documentation review only.

Affected design files: `task_graph.md` in both language directories.

Affected source files: none.

Completion condition: English and Japanese specs are synchronized and reviewed.

Forbidden behavior: no source implementation or driver/IR placeholders.

### Task 8: Task graph construction

Purpose: expand a `BuildPlan`, `ModuleIndex`, dependency overlay, and explicit
VC descriptors into a deterministic versioned task graph over synthetic phase
units and module-index entries.

Spec refs: `task_graph.md`.

Tests: graph expansion fixtures, dependency-edge fixtures, missing overlay
coverage fixtures, explicit VC descriptor ordering, cycle/blocking fixtures
where applicable, and deterministic expansion.

Affected design files: only if source review finds spec drift.

Affected source files: `src/task_graph.rs`, `src/lib.rs`, and tests.

Completion condition: source/tests/spec agree and crate verification passes.

Forbidden behavior: no phase semantics, proof status, driver sessions, IR
storage, or cache keys.

### Task 9: Spec `scheduler.md`

Purpose: define work queues, priority policy, batch/watch/LSP scheduling
contracts, build-event shape, and deterministic result ordering.

Spec refs: architecture 14 "Deterministic Result Ordering" and internal 01
"Pipeline Scheduler".

Tests: documentation review only.

Affected design files: `scheduler.md` in both language directories.

Affected source files: none.

Completion condition: English and Japanese specs are synchronized and reviewed.

Forbidden behavior: no source implementation.

### Task 10: Scheduler core

Purpose: execute synthetic tasks over the task graph while preserving
deterministic result and event collation under arbitrary completion order.

Spec refs: `scheduler.md` and `task_graph.md`.

Tests: shuffled completion, worker-count variation, immutable output
publication, and deterministic event/result order.

Affected source files: `src/scheduler.rs`, `src/task_graph.rs` only as needed,
`src/lib.rs`, and tests.

Completion condition: source/tests/spec agree and crate verification passes.

Forbidden behavior: no proof acceptance, cache reuse, artifact file I/O, driver
registry, or worker-completion ordering as semantic order.

### Task 11: Spec `resource.md`

Purpose: define hierarchical resource budgets and queue admission rules.

Spec refs: architecture 14 "Resource Budgets Are Hierarchical".

Tests: documentation review only.

Affected design files: `resource.md` in both language directories.

Affected source files: none.

Completion condition: English and Japanese specs are synchronized and reviewed.

Forbidden behavior: no source implementation.

### Task 12: Resource budgets

Purpose: implement budget accounting and enforcement in the scheduler.

Spec refs: `resource.md` and `scheduler.md`.

Tests: budget exhaustion queues instead of overcommitting, hierarchical budget
composition, and deterministic admission ordering.

Affected source files: `src/resource.rs`, `src/scheduler.rs`, `src/lib.rs`, and
tests.

Completion condition: source/tests/spec agree and crate verification passes.

Forbidden behavior: no OS process management beyond modeled budget decisions.

### Task 13: Spec `cancel.md`

Purpose: define cooperative versioned cancellation, snapshot invalidation, and
the no-partial-publication rule.

Spec refs: architecture 14 "Cancellation Is Cooperative and Versioned",
architecture 22, and internal 01.

Tests: documentation review only.

Affected design files: `cancel.md` in both language directories.

Affected source files: none.

Completion condition: English and Japanese specs are synchronized and reviewed.

Forbidden behavior: no source implementation.

### Task 14: Cancellation

Purpose: implement cancellation tokens, snapshot-version invalidation, and
discarding of obsolete completed work before publication.

Spec refs: `cancel.md` and `scheduler.md`.

Tests: cancel mid-graph leaves no partial published state, stale snapshots
cannot publish, and cancellation is deterministic/idempotent.

Affected source files: `src/cancel.rs`, `src/scheduler.rs`, `src/lib.rs`, and
tests.

Completion condition: source/tests/spec agree and crate verification passes.

Forbidden behavior: no hard worker-thread killing or obsolete result
publication.

### Task 15: Spec `failure_state.md`

Purpose: define blocked/failed task states, bounded propagation, stable failure
categories, and deterministic failure reporting.

Spec refs: architecture 14 "Failure Propagation Is Bounded" and architecture
19.

Tests: documentation review only.

Affected design files: `failure_state.md` in both language directories.

Affected source files: none.

Completion condition: English and Japanese specs are synchronized and reviewed.

Forbidden behavior: no source implementation.

### Task 16: Failure propagation

Purpose: implement explicit blocked/failed states with bounded propagation and
deterministic reporting.

Spec refs: `failure_state.md` and `scheduler.md`.

Tests: one failing task blocks exactly its dependents, independent failures
remain visible, and failure order is deterministic.

Affected source files: `src/failure_state.rs`, `src/scheduler.rs`,
`src/task_graph.rs` only as needed, `src/lib.rs`, and tests.

Completion condition: source/tests/spec agree and crate verification passes.

Forbidden behavior: no silent omission of blocked work or proof success after
deterministic predecessor failure.

### Task 17: Deterministic commit boundary

Purpose: integrate scheduler-side commit ordering with artifact manifest
transactions when the artifact-owned boundary is available.

Spec refs: internal 01 "Artifact Commit Boundary", internal 02, architecture
14, and `scheduler.md`.

Tests: shuffled completion commits identical manifest updates through a mock or
real artifact boundary, interrupted commit leaves previous state visible, and
publication order is canonical.

Affected source files: `src/scheduler.rs` plus a focused commit-boundary module
if introduced.

Completion condition: source/tests/spec agree, `mizar-artifact` boundary
verification runs where applicable, and external producer-token gaps are
recorded rather than stubbed.

Forbidden behavior: no artifact schema ownership, manifest transaction
reimplementation, fake publication tokens, or worker-completion commit order.

### Task 18: Cache-aware scheduling seam

Purpose: add the consumer-side cache lookup decision seam before task
execution, with uncached execution as the default.

Spec refs: internal 02 "Cache Lookup Before Task Execution", architecture 14,
architecture 22, `scheduler.md`, and `mizar-cache` public contracts.

Tests: mock cache hit/miss fixtures, hit skips execution with identical
externally visible results, and failed/unknown cache decisions force execution
or no-key behavior.

Affected source files: `src/scheduler.rs` plus a focused cache seam module if
introduced.

Completion condition: source/tests/spec agree, relevant cache/artifact/proof
verification runs where possible, and the seam consumes rather than
reimplements `mizar-cache`.

Forbidden behavior: no local `CacheKey`, dependency fingerprint, or proof reuse
validation implementation; no semantic acceptance or trusted-status promotion
from cache hits; no `mizar-driver` dependency.

### Task 19: Batch-build integration suite

Purpose: exercise plan -> graph -> schedule -> commit over a small workspace
through the phase services available at the time.

Spec refs: internal 01 "Batch Build", `task_graph.md`, `scheduler.md`, commit
boundary docs, and available service specs.

Tests: end-to-end batch fixture with synthetic or real available services,
deterministic diagnostics/publication order, and explicit skipped checks for
unavailable driver/IR/producer seams.

Affected design files: TODO status and integration notes if gaps are found.

Affected source files: integration tests and existing modules needed to expose
the batch path; no new driver-owned APIs.

Completion condition: available batch path passes verification and unavailable
services are classified as `external_dependency_gap` or `deferred`.

Forbidden behavior: no fake `mizar-driver`, `mizar-ir`, producer artifact
tokens, or proof authority.

### Task 20: Determinism suite

Purpose: broaden deterministic coverage across plans, graphs, schedules,
events, and commits for identical inputs across worker counts.

Spec refs: architecture 20, architecture 14, architecture 22, and all
implemented module specs.

Tests: property or table-driven fixtures for worker-count variation,
ready-queue shuffling, cache hit/miss timing where available, and identical
canonical outputs.

Affected design files: TODO status and determinism notes if gaps are found.

Affected source files: determinism tests and small test hooks only when
documented by the module specs.

Completion condition: determinism coverage passes for implemented seams and
unavailable real integrations are explicitly deferred.

Forbidden behavior: no timing, worker id, cache lookup timing, or completion
order in canonical semantic/artifact ordering.

### Task 21: Public-enum forward-compatibility policy

Purpose: apply the workspace public-enum compatibility policy to every
`mizar-build` public enum.

Spec refs: all module specs and the established public-enum procedure.

Tests: focused API/lint tests or compile-time fixtures that protect enum
forward-compatibility where applicable.

Affected design files: module specs and TODO status only when public API policy
requires clarification.

Affected source files: public enum declarations, API tests, and exports.

Completion condition: every public enum is audited and updated or explicitly
classified as intentionally closed by spec.

Forbidden behavior: no semantic behavior changes or unrelated API expansion.

### Task 22: Source/spec correspondence audit

Purpose: trace every public API and promised behavior in the module specs to
source and tests, recording gaps as follow-up tasks.

Spec refs: all `mizar-build` module specs and this crate plan.

Tests: audit-only unless the review finds missing required coverage, in which
case add focused tests in a separate implementation slice.

Affected design files: source/spec correspondence report, TODO status, and
module specs if drift is fixed.

Affected source files: only when fixing an audited source drift in the same
task is small and review-approved.

Completion condition: audit report records source/spec/test correspondence and
no unresolved blocking/high drift remains.

Forbidden behavior: no rebaseline of docs to match incorrect source behavior.

### Task 23: Bilingual documentation sync audit

Purpose: compare every English canonical document under
`doc/design/mizar-build/en/` with its Japanese companion and synchronize
content.

Spec refs: repository documentation policy and all `mizar-build` docs.

Tests: documentation review and diff checks.

Affected design files: English/Japanese companion docs, bilingual sync audit,
and TODO status.

Affected source files: none.

Completion condition: bilingual audit reports synchronization or clearly marked
deferred companion updates with reasons.

Forbidden behavior: no source implementation.

### Task 24: Incremental/parallel equivalence gate

Purpose: add the scheduler-level architecture-22 regression gate for clean
sequential, clean parallel, incremental sequential, and incremental parallel
agreement on the same `BuildSnapshot` and verifier policy.

Spec refs: architecture 22, architecture 14, architecture 20, `scheduler.md`,
`cancel.md`, and cache/commit seam specs.

Tests: randomized ready-task scheduling, worker counts, mock cache hit/miss
timing, cancellation/supersession, and deterministic commit boundary behavior.

Affected design files: TODO status, equivalence notes, and module specs only if
the gate exposes drift.

Affected source files: scheduler/cache/cancel/commit tests and small test hooks
documented by existing specs.

Completion condition: the equivalence gate passes for implemented seams and
unavailable real driver/IR/producer integrations are explicitly classified.

Forbidden behavior: no stale publication as current output, no cache-hit trust
promotion, and no completion-order-dependent proof/artifact status.

### Task 25: Architecture-22 follow-up audit

Purpose: re-run source/spec and bilingual audits after task 24 for scheduler
equivalence, cancellation, and cache seam behavior.

Spec refs: all module specs, architecture 22, this TODO, and documentation
policy.

Tests: audit-only unless missing coverage is found and fixed in the audited
scope.

Affected design files: follow-up audit report, TODO status, and synced docs if
needed.

Affected source files: only narrow fixes required by audit findings.

Completion condition: no stale-publication or deterministic commit-boundary gap
remains unresolved above low severity.

Forbidden behavior: no new behavior outside architecture-22 follow-up scope.

### Task 26: Module-boundary refactor gate

Purpose: audit the source layout for oversized files, mixed responsibilities,
and helpers that should be split along the module table and spec boundaries.

Spec refs: this TODO, internal 07, and all module specs.

Tests: existing behavior tests plus source/spec and bilingual audits for moved
APIs.

Affected design files: module-boundary report, TODO status, and module table
updates if files move.

Affected source files: private module splits only, plus unchanged public exports
and tests as needed.

Completion condition: any necessary split is behavior-preserving, reviewed, and
verified; crate hard gates are ready for closeout.

Forbidden behavior: no API exposure, behavior cleanup, diagnostic change,
schema change, or unrelated refactor inside the module move.

## Exit Criteria

Hard gates:

- no blocking/high specification inconsistency remains;
- no `mizar-build` source behavior remains absent from its module specs or
  recorded external/deferred gap list;
- source, tests, English docs, and Japanese companion docs are synchronized;
- every unavailable downstream integration is classified as
  `external_dependency_gap` or `deferred` rather than stubbed;
- the crate has no dependency on `mizar-driver`;
- cache-aware scheduling is documented and implemented as an optimization-only
  execution-skip seam;
- cache hits and artifact records never promote proof trust or semantic
  acceptance;
- task graph, scheduler, resource budget, cancellation, failure propagation,
  deterministic commit boundary, and cache seam behavior are covered by focused
  tests or explicit deferred reasons;
- review-only spec/test/full/source-doc reviews report no unresolved
  blocking/high findings;
- required verification commands pass, or any unrun command is explicitly
  justified.

Verification commands:

```text
cargo test -p mizar-build
cargo clippy -p mizar-build --all-targets -- -D warnings
cargo fmt --check
```

When scheduler/cache/artifact/commit boundaries are touched, also run the
available adjacent crate checks:

```text
cargo test -p mizar-cache
cargo test -p mizar-artifact
cargo test -p mizar-vc
cargo test -p mizar-proof
cargo test -p mizar-driver
```

For crate completion, run:

```text
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Review expectations:

- every task follows the AGENTS.md review loop for spec/documentation, test
  sufficiency, full implementation, and source/documentation consistency;
- remaining low/medium notes must be recorded with a concrete deferred reason
  or fixed before closeout;
- crate completion requires a read-only quality score of at least 90/100.
