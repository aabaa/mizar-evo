# crate plan: mizar-checker

> 正本は英語です。英語版: [../en/00.crate_plan.md](../en/00.crate_plan.md)。

状態: 自律 crate 開発 plan。preliminary task 0 が [todo.md](./todo.md) task 1
の前にこの plan を作成し、その後 task 1 が crate scaffold を追加し、task 2 が
`TypedAst` data-shape spec を追加し、task 3 が `typed_ast` data shape を実装し、
task 4 が `binding_env` spec を追加し、task 5 が `binding_env` data layer と
module shell を実装し、task 6 が `type_checker` spec を追加し、task 7 が
type-expression normalization を実装し、task 8 が declaration と local-binding
checking を実装し、task 9 が checker-owned payload 上の term/formula inference を
実装し、task 10 が checker-owned payload 上の coercion candidate と
initial-obligation checking を実装し、task 11 が checker-owned fact table と
local context 上の deterministic type-fact query を実装し、task 12 が
source-to-checker payload extraction gap 用の active `type_elaboration` corpus runner を
追加し、task 13 が `registration_resolution` spec を追加し、task 14 が
registration database data layer を実装し、task 15 が `cluster_trace` spec を追加し、
task 16 が trace recording 付き cluster closure data layer を実装し、task 17 が
cluster loop、bound、explicit contradiction failure handling を実装し、task 18 が
explicit payload 上の reduction trace data layer を実装した。

## 責務

所有する挙動:

- pipeline phase 6-8、すなわち type checking、registration/cluster
  resolution、overload resolution を実装する。
- `mizar_resolve::resolved_ast::ResolvedAst` と
  `mizar_resolve::env::SymbolEnv` を消費し、`TypedAst`、
  `TypeFactTable`、`ResolutionTrace`、`ResolvedTypedAst` を生成する。
- soft type expression を決定的な predicate 風の正規形へ正規化する。
- resolver-owned symbol identity の上に checker-local な階層型 typing
  context を構築する。
- recoverable error state を保持しながら term と formula の型を推論する。
- type fact、coercion candidate、`InitialObligationId` を持つ initial
  obligation を記録し、`VcId` は割り当てない。
- pending / activated registration を索引し、再生可能な cluster closure を
  計算し、来歴付きで reduction registration を適用し、有界飽和失敗を検出する。
- overload candidate を展開・filter し、ordinary root を選択し、受理済み
  same-root refinement を join し、許可された `qua` view を挿入し、失敗 site を
  保持する。
- id、debug rendering、diagnostics、fact query、candidate order、trace order、
  artifact-facing projection を決定的に保つ。

対象外:

- lexical、syntax、parser、frontend、module-index、source-loading、package
  planning の所有。
- `mizar-resolve` が所有する name lookup、import/export validation、label
  resolution、signature collection。
- 任意の一階 reasoning、ATP search、proof certificate acceptance、theorem
  validity checking、kernel replay。
- `VcId` assignment、`ObligationAnchor` construction、VC generation、checker-local
  initial obligation の発行を超える proof policy decision。
- stable published artifact schema、cache storage、LSP rendering、build scheduling。
- 同じ source pass で先に現れたという理由だけで unverified local registration を
  活性化すること。

境界規則:

- checker fact はすべて declared type predicate、checker-owned inference fact、
  または trace provenance を持つ registration-derived fact として説明可能であること。
- source behavior は観測した source convenience ではなく `doc/spec/en/` と既存
  `.miz` test intent に従うこと。
- recovered または erroneous node には partial typed output を与えてよいが、
  invalid fact を verified metadata として export してはならない。
- automatic inference に使う active registration は、accepted dependency artifact
  または accepted local summary から来なければならない。暫定 policy は
  pending / unverified registration と generated obligation を記録してよいが、
  unverified registration を automatic fact に寄与させてはならない。
- proof search、hidden coercion insertion、未記録 cluster fact、return-type-based
  overload selection はどの task でも導入しない。

## authority order and specification references

language behavior には repository の authority order を使う:

1. `doc/spec/en/`
2. `tests/**/*.miz`
3. `tests/coverage/spec_trace.toml`
4. `tests/**/*.expect.toml`
5. `doc/design/`
6. `crates/`

主要 reference:

| ref | 要件 | checker status |
|---|---|---|
| [architecture 01](../../architecture/ja/01.ir_layers.md) | `TypedAst`、`ResolutionTrace`、`ResolvedTypedAst` は immutable source-shaped IR layer。 | task 2、3、15、27、28 で精緻化する。 |
| [architecture 04](../../architecture/ja/04.type_and_registration_resolution.md) | phase 6-7 は type normalization、local type context、type fact、coercion candidate、registration database、cluster closure、reduction provenance、diagnostic 後の partial typing を所有する。 | wave 1 と wave 2 の主要 design input。 |
| [architecture 05](../../architecture/ja/05.overload_resolution.md) | phase 8 は template expansion、viability、specificity graph、root selection、refinement join、inserted view、failed-site preservation、`ResolvedTypedAst` を所有する。 | wave 3 の主要 design input。 |
| [architecture 16](../../architecture/ja/16.substitution_and_binding.md) | binding identity、alpha-equivalence、capture avoidance、definition-time closure は決定的でなければならない。 | task 4 で精緻化し task 5 で消費する。 |
| [architecture 17](../../architecture/ja/17.cluster_trace_format.md) | `ResolutionTrace` は cluster / reduction step を deterministic traversal と loop detection 付きで明示記録する。 | task 15-18 で精緻化する。 |
| [architecture 18](../../architecture/ja/18.dependency_fingerprint.md) | checker output は type、registration、cluster、reduction、overload、inserted-view の dependency slice に寄与する。 | registration / overload task では source ごとの contribution tracking が必要。cache storage は対象外。 |
| [architecture 19](../../architecture/ja/19.failure_semantics.md) | type error、overload ambiguity、cluster loop、blocked work は stable phase ownership と deterministic ordering を必要とする。 | diagnostic を持つすべての task に適用する。 |
| [internal 07](../../internal/ja/07.crate_module_layout.md) | `mizar-checker` は type checking、registration resolution、overload resolution input を所有する。 | `todo.md` の module split を定義する。 |
| [spec 03](../../../spec/ja/03.type_system.md) | soft type、radix type、attribute、subtyping。 | task 6-11 と 20 が消費する。 |
| [spec 08](../../../spec/ja/08.type_inference.md) | inferred type、coercion、`qua`、`reconsider`。 | task 6-11 と 21-28 が消費する。 |
| [spec 13](../../../spec/ja/13.term_expression.md) | term expression、set expression、choice term、term-level typing surface。 | task 6 と 9-11 が消費する。 |
| [spec 17](../../../spec/ja/17.clusters_and_registrations.md) | cluster、existential、functorial、reduction registration。 | task 13-20 が消費する。 |
| [spec 18](../../../spec/ja/18.templates.md) | template definition と template argument behavior。 | task 21 と 23 が消費する。 |
| [spec 19](../../../spec/ja/19.overload_resolution.md) | ordinary overload root selection と active refinement。 | task 21-28 が消費する。 |
| [spec 22](../../../spec/ja/22.error_handling_and_diagnostics.md) | type、registration、overload、diagnostic class。 | user-facing diagnostic 前に stable public code ownership を確認する。 |
| [mizar-test staged model](../../mizar-test/ja/staged_model.md) | `type_elaboration`、`formula_statement`、`advanced_semantics` stage が semantic corpus growth を順序づける。 | task 12 が最初の checker stage runner を作り、task 29 が advanced semantic coverage を拡張する。 |

## 既存テストと不足 coverage

| test path | 現在の意図 | checker との関係 |
|---|---|---|
| `tests/miz/pass/parser/pass_parser_type_expressions_001.miz` と対応 fail case | parser が type-expression syntax を受理・recover する。 | task 7 の normalization fixture の入力は存在するが、parse-only であり checker behavior は検査しない。 |
| `tests/miz/pass/parser/pass_parser_qua_terms_001.miz` と `fail_parser_qua_missing_type_001.miz` | parser が `qua` source shape をカバーする。 | task 10 の coercion candidate と task 26 の view insertion の入力。checker assertion はまだない。 |
| `tests/miz/pass/parser/pass_parser_consider_reconsider_001.miz` と対応 fail case | parser が declaration / binding surface をカバーする。 | task 5、8、10 の入力。checker assertion はまだない。 |
| `tests/miz/pass/parser/pass_parser_registrations_001.miz` と対応 fail case | parser が registration syntax と recovery をカバーする。 | task 13-20 の入力。active registration semantics はまだない。 |
| `tests/miz/pass/parser/pass_parser_template_arguments_001.miz` と `pass_parser_template_references_001.miz` | parser が template argument / reference syntax をカバーする。 | task 21 と 23 の入力。overload-template semantics はまだない。 |
| `tests/miz/pass/resolve/pass_resolve_declaration_symbol_smoke_001.miz` | active declaration-symbol smoke coverage が resolver symbol collection に到達する。 | `SymbolEnv` を消費する checker test の下位 stage prerequisite。 |
| `tests/miz/fail/resolve/fail_resolve_duplicate_theorem_symbol_001.miz` | active resolver duplicate-symbol failure coverage。 | resolver failure の一部が checker stage より前に起きることを確認する。 |
| `tests/coverage/spec_trace.toml` の planned `type_elaboration` / `advanced_semantics` row | active checker execution なしで将来の semantic obligation を記録している。 | task 12 は最初の active `type_elaboration` gap runner entry を追加し、real pass semantic coverage は source-to-checker payload extraction が存在するまで deferred のままにする。task 29 は advanced entry を後で更新する。 |
| `tests/miz/pass/types`, `tests/miz/fail/types`, `tests/miz/pass/attributes`, `tests/miz/pass/clusters`, `tests/miz/fail/clusters`, `tests/miz/fail/overload` | directory placeholder。 | `test_gap`: checker semantic fixture set は未着手。 |

計画中の test-first 追加:

- deterministic id、table、rendering、query API、trace ordering、candidate order、
  diagnostic ordering に対する task-local Rust unit test。
- task 10 は widening、source `qua`、narrowing coercion candidate、
  sethood/non-emptiness/narrowing obligation、invalid `qua`、evidence 欠落、
  non-consumable supporting fact の Rust coverage を追加済みである。
- task 11 は fact query の provenance matching、deterministic canonical order、fact
  duplication なし、local assumption visibility、missing context behavior、
  contradiction diagnostic、non-mutating query の Rust coverage を追加済みである。
- task 12 は source-to-checker type-elaboration payload bridge 欠落に対する
  active `.miz` fail coverage を追加する。real checker pass fixture は、AST 全体の
  extraction が task 7-11 の checker-owned payload API に捏造なしで入力できるまで
  deferred のままにする。
- task 14 は resolver registration identity/origin metadata の pending / activated
  / rejected index、explicit activation seam、diagnostic/debug rendering の
  Rust coverage を追加する。
- task 15 は trace 記録付き cluster closure、loop/bound handling、reduction replay、
  strategy-audit diagnostic、trace determinism の planned Rust coverage を仕様化する。
  実装 coverage は tasks 16-18 から始まる。
- task 16 は replayable cluster closure、deterministic cluster step order、
  multi-antecedent conditional cluster、transitive chain、inactive/non-cluster
  non-firing、mismatched activation payload の Rust coverage を追加する。
- task 17 は direct / indirect cluster loop、depth / generated-fact bound、
  zero-antecedent depth、explicit contradiction diagnostic、incomplete closure status、
  cache-key-visible traversal setting の Rust coverage を追加する。
- task 18 は replayable reduction step、architecture 17 reduction provenance field、
  inactive/rejected/non-reduction non-firing、invalid substitution、missing guard evidence、
  strategy-audit mismatch、`such` guard が applicability-only evidence であることの
  Rust coverage を追加する。
- registration / overload wave 後、task 29 で `formula_statement` と
  `advanced_semantics` の active `.miz` fixture と traceability entry を追加する。
- 既存 `.miz` test や expectation sidecar を current source behavior に合わせる
  目的だけで rebaseline してはならない。

## design/source inventory

design files:

- [todo.md](./todo.md)
- 本 crate plan
- 既存の module spec: [typed_ast.md](./typed_ast.md)、
  [binding_env.md](./binding_env.md)、[type_checker.md](./type_checker.md)、
  [registration_resolution.md](./registration_resolution.md)、
  [cluster_trace.md](./cluster_trace.md)
- 将来の module spec: `overload_resolution.md`、`resolved_typed_ast.md`
- architecture reference 01、04、05、16、17、18、19
- [mizar-resolve crate plan](../../mizar-resolve/ja/00.crate_plan.md)、
  [resolved_ast.md](../../mizar-resolve/ja/resolved_ast.md)、
  [env.md](../../mizar-resolve/ja/env.md)
- [mizar-test staged model](../../mizar-test/ja/staged_model.md)、
  [expectation schema](../../mizar-test/ja/expectation_schema.md)、
  [traceability](../../mizar-test/ja/traceability.md)

source files:

- task 1 は `crates/mizar-checker/Cargo.toml`、
  `crates/mizar-checker/src/lib.rs`、
  `crates/mizar-checker/tests/lint_policy.rs` を追加する。
- task 3 は `crates/mizar-checker/src/typed_ast.rs` を追加し、文書化済み
  `typed_ast` module を `src/lib.rs` から公開する。
- task 4 は `doc/design/mizar-checker/ja/binding_env.md` と英語正本を追加する。
- task 5 は `crates/mizar-checker/src/binding_env.rs` を追加し、文書化済み
  `binding_env` module を `src/lib.rs` から公開する。
- task 6 は `doc/design/mizar-checker/ja/type_checker.md` と英語正本を追加する。
  source implementation は task 7 から始まる。
- task 7 は `crates/mizar-checker/src/type_checker.rs` を追加し、文書化済み
  `type_checker` module を `src/lib.rs` から公開する。
- task 9 は `crates/mizar-checker/src/type_checker.rs` を拡張し、
  `TermFormulaChecker`、checked term/formula record、task-local open
  candidate-set table、expected-type constraint、task-local inference fact
  recording を追加する。
- task 10 は `crates/mizar-checker/src/type_checker.rs` を拡張し、
  `CoercionObligationChecker`、task-local coercion request input、
  sethood/non-emptiness/narrowing obligation input、coercion/obligation debug
  rendering、supporting fact 用 input fact id の保持を追加する。
- task 11 は `crates/mizar-checker/src/type_checker.rs` を拡張し、
  `TypeFactQueryEngine`、`TypeFactQuery`、`TypeFactQueryOutput`、既存
  `TypeFactTable` entry 上の deterministic query status/debug rendering を追加する。
- task 13 は `doc/design/mizar-checker/ja/registration_resolution.md` と英語正本を
  追加する。source implementation は task 14 から始まる。
- task 14 は `crates/mizar-checker/src/registration_resolution.rs` を追加し、
  `registration_resolution` public module を crate root から公開し、新 module を
  lint-policy guard に登録する。
- task 15 は `doc/design/mizar-checker/ja/cluster_trace.md` と英語正本を追加する。
  source implementation は task 16 から始まる。
- task 16 は `crates/mizar-checker/src/cluster_trace.rs` を追加し、文書化済み
  `cluster_trace` module を `src/lib.rs` から公開し、新しい public API surface に合わせて
  lint-policy guard を更新する。
- task 17 は `crates/mizar-checker/src/cluster_trace.rs` を ancestry tracking、
  loop/bound/contradiction diagnostic、closure status、cache-key material、explicit
  checker-owned trace payload 上の task-local test で拡張する。
- task 18 は `crates/mizar-checker/src/cluster_trace.rs` を
  `ReductionTraceBuilder`、explicit reduction input payload、reduction trace step、
  replay validation、diagnostic、task-local test で拡張する。
- scaffold の direct dependency は `crates/mizar-session/` と
  `crates/mizar-resolve/`。後続 dependency は task-scoped spec を必要とする。
- 現在の resolver source surface には `resolved_ast`、`env`、`symbols`、
  `labels`、`names`、recovered-syntax handling がある。checker は name lookup や
  symbol collection をやり直さず、resolver-owned id と opaque signature shell を
  消費しなければならない。
- 現在の corpus infrastructure には active parse-only runner と
  declaration-symbol runner がある。task 12 は frontend parsing と resolver symbol
  collection まで到達し、その後に checker payload-extraction bridge 欠落を
  external dependency gap として report する active `type_elaboration` runner を追加する。

observed behavior:

- task 0 前の `main` は local `origin/main` と同一 (`51bcaed...`) で、
  worktree は clean、ahead/behind commit はない。
- `mizar-resolve` task 1-23 と 25-29 は完了済み。resolver task 24 は
  resolver close-out 時点で `mizar-artifact` の `ModuleSummary` dependency により
  deferred と記録された。
- 現在の `mizar-artifact` は `ModuleSummary` と `RegistrationSummary` の schema、
  writer、reader を提供している。producer / reuse integration は resolver と
  checker crate の後続 task に残る。
- `mizar-resolve` task 5 と task 20 は完了済みであるため、checker task 1 の
  宣言済み resolver prerequisite は利用可能である。
- `mizar-resolve` task 21 は完了扱いなので、後続の checker registration / overload
  task は現在の signature-projection data を調査できる。ただし各 task は実装前に
  checker が必要とする正確な payload が存在するか確認しなければならない。

observed behavior は inventory に限る。`doc/spec/en`、tests、traceability
metadata、design docs、または明示的な人間の承認に支えられない限り normative ではない。

## 既知 gap / drift

`class` column には protocol classification を記録する。欠けている input が
`mizar-checker` の外部にある場合、user が要求した `external_dependency_gap` entry は
直接記録してよい。すでに分類済みの項目が external-dependency / deferred planning
gate でもある場合は、その status を evidence / action text に記録する。

| ID | class | evidence | action |
|---|---|---|---|
| MC-G002 | `test_gap` | task 12 前は checker semantic directory が placeholder で、active `type_elaboration` runner は存在しなかった。 | 実装済み checker seam は task-local Rust test で cover する。task 12 は source-to-checker bridge gap 用の最初の active checker corpus runner と traceability update を追加する。real semantic pass/fail `.miz` coverage は MC-G020 に従う。 |
| MC-G003 | `design_drift` | 以前の checker TODO wording は obligation generation 成功後の activation 可能性を示していたが、architecture 04 は automatic inference が registration を使う前に accepted verifier status を要求する。proof acceptance input が未実装なので deferred `external_dependency_gap` でもある。 | task 0 で修正済み: 暫定 policy は未検証 activation なし。task 19 は pending / unverified state を明示し、generated obligation を accepted proof と扱わない。 |
| MC-G004 | `source_drift` | resolver close-out は task 24 を `mizar-artifact` task 5 により deferred と記録した。現在の `mizar-artifact` は canonical `ModuleSummary` と `RegistrationSummary` schema を提供しているが、resolver / checker producer and reuse integration はまだ配線されていない。これは task 1 ではなく integration task の `external_dependency_gap` planning gate である。 | task 1 では `mizar-artifact` を dependency に追加しない。後続 producer / reuse task は task-scoped spec を通じてのみ canonical artifact schema を消費する。checker-owned artifact schema や reader を創作しない。 |
| MC-G005 | `spec_gap` | 専用 `mizar-diagnostics` crate と public checker diagnostic code-space は workspace crate または canonical spec/design range として存在しない。これは `external_dependency_gap` planning gate である。 | 関連 spec/design owner が public code を割り当てるまでは、task-local diagnostics は crate-internal に保つ。test が必要とする stable class / detail key は保持する。 |
| MC-G006 | `source_drift` | resolver close-out は、module-level scheme/template declaration source role が parser/syntax から公開されていないことを記録している。これは template / overload task の `external_dependency_gap` planning gate である。 | template / overload task は module-level scheme symbol を創作しない。その時点で parser/resolver が実際に公開する role だけを実装する。 |
| MC-G007 | `design_drift` | architecture 04/05 の古い "Affected Modules" list (`types.md`, `overload.md` など) は checker `todo.md` の refined module table と異なる。 | checker `todo.md` と本 plan を crate-level module split として扱う。task 0 では architecture file を rename しない。 |
| MC-G011 | `external_dependency_gap` | task 5 は、現在の resolver API が `LocalTermScope`、`LocalTermBinding`、definition shell id、`NameRefEntry::resolution()` を公開する一方、AST 全体の local binding declaration table、use-site scope/ordinal table、reserve payload、captured-free-variable payload、checker-owned test 用 syntax-free empty `ResolvedAst` fixture は公開していないことを確認した。 | task 5 は module shell に external-gap diagnostic を記録し、利用可能な場合の explicit binding payload を受け取り、direct `mizar-syntax` dependency を追加せず public module-shell signature を type-check する。完全な source extraction と closure replay には、後続 resolver/source-walk integration が不足 payload と fixture を提供する必要がある。 |
| MC-G014 | `external_dependency_gap` | task 7 は、現在の resolver API が symbol kind と opaque signature shell を公開する一方、AST 全体の type-expression site payload、mode radix/attribute expansion payload、attribute argument payload、structure/mode arity payload を公開していないことを確認する。 | task 7 は checker-owned payload と mode-expansion provider seam を実装し、explicit mode-expansion provider payload 欠落を degraded normalization diagnostic として記録し、unsupported checker-owned payload を recoverable degraded entry として扱い、raw syntax を walk したり opaque resolver signature を checker type payload と扱ったりしない。 |
| MC-G016 | `external_dependency_gap` | task 8 は、resolver / binding input が AST 全体の declaration/type-site table、reserve default payload、right-hand-side term payload、definition body payload、attributed / constrained declaration obligation 用 evidence query をまだ公開していないことを確認する。 | task 8 は `BindingEnv` と task-7 normalized type expression 上に checker-owned declaration payload seam を実装する。不足する RHS / body / default-type / evidence input は、source walk、RHS type の捏造、早すぎる `InitialObligation` ではなく deferred diagnostic にする。 |
| MC-G017 | `external_dependency_gap` | task 9 は、resolver / source input が AST 全体の typed term/formula payload table、built-in numeric type payload、functor/predicate candidate signature、selector / structure-field payload、source `qua` coercion evidence、sethood / non-emptiness evidence query をまだ公開していないことを確認する。 | task 9 は checker-owned term/formula payload seam、task-local open candidate-set table、expected-type constraint、partial/error/skipped recovery を実装する。不足 resolver/source payload は degraded diagnostic にし、task 10 が所有する coercion / obligation work は捏造せず deferred として残す。 |
| MC-G018 | `external_dependency_gap` | task 10 は、resolver / dependency input が AST 全体の coercion request table、active dependency-summary fact database、structure inheritance graph、cluster-closure evidence、sethood evidence、non-emptiness evidence、proof-query result をまだ公開していないことを確認する。 | task 10 は checker-owned coercion / obligation payload seam を実装し、input fact id を保持し、不足 support を blocked/rejected entry と diagnostic として記録し、local `InitialObligationId` を発行する。不足 payload は後続 source extraction、fact query、registration、cluster、proof-owned task の external gap として残す。 |
| MC-G019 | `external_dependency_gap` | task 11 は、resolver/checker input が AST 全体の statement/proof assumption table、theorem acceptance payload、phase-7 `ResolutionTrace` fact payload をまだ公開していないことを確認する。 | task 11 は既存 checker fact table と local context 上の deterministic query だけを実装する。不足 statement/proof/trace payload は statement、proof、registration、cluster task の external gap として残し、task 11 は accepted proof fact や registration provenance を捏造しない。 |
| MC-G020 | `external_dependency_gap` / `deferred` | task 12 は、repository が parsed/resolved `.miz` の declaration、type expression、term、formula、coercion site、recorded fact を tasks 7-11 の checker-owned payload へ変換する AST-wide source-to-checker extraction API をまだ持たないことを確認する。 | task 12 は lower-stage parsing と symbol collection が成功した後に `type_elaboration.external_dependency.ast_payload_extraction` を surface する active `type_elaboration` runner と fail fixture を追加する。real type-elaboration pass coverage と task 7-11 semantic assertion は deferred のままにし、syntax string から checker payload を捏造したり stub で pass case を受理したりしない。 |
| MC-G021 | `external_dependency_gap` / `deferred` | task 13 は、resolver registration index が declaration identity、registration kind、opaque target shell、visibility/export metadata、dependency、recovery state、source contribution を公開する一方、checker-ready typed registration pattern、parameter type payload、correctness-condition anchor、accepted verifier status、active dependency-summary consumption、reduction `LHS` / `RHS` term payload、guard-evidence payload を公開していないことを確認する。task 14 は公開済み identity/origin metadata を保存し、explicit activation input seam を追加するが、不足する semantic payload は提供しない。 | task 14 は resolver registration を identity/origin record としてだけ使う。task 16-20 は explicit checker-owned payload seam を使うか、opaque shell の parse、summary の創作、emitted obligation の accepted 扱いをせず behavior を defer する。 |
| MC-G023 | `test_gap` / `external_dependency_gap` / `deferred` | task 15 は checker-local `ResolutionTrace` contract を仕様化する。tasks 16-18 は explicit payload seam 上で checker-owned cluster closure、loop/bound/contradiction handling、reduction trace data layer と Rust coverage を実装するが、active cluster/reduction semantic `.miz` fixture、artifact JSON emission、cache reader、source-derived `TypeFactTable` contradiction integration、real source-to-checker trace extraction は未実装のままである。real source-derived trace step には、MC-G020/MC-G021 の checker-ready source / registration payload と accepted activation input がまだ必要である。 | task 29 は source-to-checker payload seam が存在した後に active `.miz` corpus coverage を追加する。artifact emission/reuse task は checker-local schema fork ではなく architecture 17 を消費しなければならない。 |
| MC-G009 | `repo_metadata_conflict` | task 0 inventory では観測なし。 | 将来 metadata conflict が見つかった場合は報告のみ行い、自動修復しない。 |

## resolved setup history

| ID | class | resolution |
|---|---|---|
| MC-G001 | `source_drift` | task 1 が `mizar-checker` workspace member、最小 crate root、manifest を追加する。 |
| MC-G010 | `test_gap` | task 1 が workspace lint opt-in、dependency-boundary check、documented `allow` exception の checker lint-policy guard を追加する。 |
| MC-G012 | `source_drift` | task 5 が `src/binding_env.rs` を追加し、文書化済み `binding_env` module を公開し、新しい public API surface に合わせて lint-policy guard を更新する。 |
| MC-G013 | `design_drift` | task 6 が architecture 04 の broad `TypeContext` / `CoercionCandidateTable` example name を checker-local `BindingEnv`、immutable `LocalTypeContextTable`、candidate-owning `CoercionTable` split へ解決する。 |
| MC-G015 | `source_drift` | task 7 が `src/type_checker.rs` を追加し、文書化済み `type_checker` module を公開し、新しい public API surface に合わせて lint-policy guard を更新する。 |
| MC-G022 | `source_drift` | task 14 が `src/registration_resolution.rs` を追加し、文書化済み `registration_resolution` module を公開し、新しい public API surface に合わせて lint-policy guard を更新する。 |
| MC-G024 | `source_drift` | task 16 が `src/cluster_trace.rs` を追加し、文書化済み `cluster_trace` module を公開し、新しい public API surface に合わせて lint-policy guard を更新する。 |

task 18 まで checker semantic `source_undocumented_behavior` は観測されていない。
実装済みの `typed_ast`、`binding_env`、type-expression normalization、declaration /
local-binding checking、term/formula inference、coercion / initial-obligation
checking、type-fact query、registration database data layer、cluster closure /
loop / bound trace source、reduction trace source は、英日 design document と
task-local Rust test に支えられている。
task 12 runner と task 13 registration spec は checker semantic source behavior を
追加せず文書化されている。task 15 も documentation-only である。
将来、spec/test に支えられない source behavior は保持前に分類しなければならず、crate completion 時点に残すことは
できない。

task 18 後の継続を妨げる blocking contradiction は見つかっていない。後続 gap は
それぞれの owning task の local gate である。

## ordered task decomposition

この section は [todo.md](./todo.md) と対応する。各 numbered task は、次 task 開始前に
独立 review、verification、commit を完了しなければならない。

| task | purpose | spec refs | tests / affected files | completion and deferral rule |
|---|---|---|---|---|
| 0 | 本 crate plan を英日で作成する。 | autonomous crate protocol; checker TODO。 | `00.crate_plan.md`; docs-only `git diff --check` と cached diff check。 | task 1 前に commit。code なし。 |
| 1 | `mizar-checker` と lint-policy guard を scaffold する。 | architecture 04; internal 07。 | `Cargo.toml`, `crates/mizar-checker/**`; lint-policy test。 | `mizar-session` と `mizar-resolve` に依存する workspace が build できる。checker semantics は導入しない。 |
| 2 | `typed_ast.md` を仕様化する。 | architecture 04 Typed AST; architecture 01。 | 英日 docs; task 3 の planned tests。 | arena、local context snapshot、type/fact/coercion table、`InitialObligationId`、status semantics、partial typing、deferred arena decision をカバーする。 |
| 3 | `typed_ast` data shape を実装する。 | `typed_ast.md`。 | `src/typed_ast.rs`; id/context/table/status/rendering と boundary tests。 | deterministic id、context snapshot、round-trip、status consumption、proof-owned id 不在、final overload/view field 不在、rendering。arena representation decision を解決する。 |
| 4 | `binding_env.md` を仕様化する。 | architecture 04 Step 1; architecture 16。 | 英日 docs; task 5 の planned tests。 | layered context、binder identity、lookup、reserved variable、closure metadata、diagnostic、external dependency gap を type inference なしで仕様化する。 |
| 5 | binding environment と context build を実装する。 | `binding_env.md`; resolver `SymbolEnv` boundary。 | `src/binding_env.rs`; lookup、reserve、scope、iteration、module-shell、diagnostic、boundary tests。 | binding-env data layer、explicit-payload lookup、resolver-resolution fallback、deterministic shell として実装済み。AST 全体の local extraction、use-site scope/ordinal、reserve payload、closure payload、syntax-free `ResolvedAst` fixture の欠落は捏造せず MC-G011 external gap として残す。 |
| 6 | `type_checker.md` を仕様化する。 | architecture 04 Steps 2-4; spec 03; spec 08; spec 13。 | 英日 docs; task 7-11 の planned tests。 | phase-6 boundary、normalized type model、declaration、inference、coercion、initial obligation、fact、partial recovery、deterministic expectation、external/deferred gate を定義する。code なし。 |
| 7 | type-expression normalization を実装する。 | `type_checker.md` normalization。 | `src/type_checker.rs`; canonicalization/idempotence tests。 | task-local `NormalizedTypeTable` を `TypeNormalizationOutput` 内で所有し、checker-owned type-expression payload を正規化し、不足 resolver/signature seam は raw syntax walk せず MC-G014 external gap として degrade する。 |
| 8 | declaration と local-binding checking を実装する。 | `type_checker.md` declarations。 | binding fixture と diagnostic range tests。 | `DeclarationChecker` として checker-owned declaration/context payload、`BindingEnv`、task-7 normalized type expression 上に実装済み。不足 RHS / body / reserve / evidence payload は MC-G016 external gap として残し deferred diagnostic に記録する。proof、VC、task-10 obligation semantics は捏造しない。 |
| 9 | term / formula type inference を実装する。 | `type_checker.md` inference。 | kind ごとの inference test、partial inference test。 | `TermFormulaChecker` として checker-owned term/formula payload、task-local candidate set、expected-type constraint、最小 inference fact 上に実装済み。不足 resolver/source payload は MC-G017 external gap として残し、task-10 coercion/obligation emission と final overload root selection は捏造しない。 |
| 10 | coercion candidate と initial obligation を実装する。 | `type_checker.md` coercions/obligations; spec 08。 | coercion / obligation tests、sethood、non-emptiness、invalid `qua` fail。 | checker-owned payload 上の `CoercionObligationChecker` として実装済み。`InitialObligationId` を発行し、input fact id を保持し、obligation-backed fact を追加し、不足 support は `VcId` 割り当て、obligation discharge、view 捏造なしで blocked/rejected にする。 |
| 11 | type-fact recording と query API を実装する。 | `type_checker.md` type facts。 | provenance、determinism、dedup tests。 | 既存 fact table と optional local context 上の deterministic `TypeFactQueryEngine` として実装済み。query order は canonical、assumed fact は context visibility なしに漏れず、contradiction は diagnostic-only、MC-G019 proof/statement/trace gap は external のまま。 |
| 12 | `type_elaboration` corpus runner を追加する。 | mizar-test staged model。 | `mizar-test` runner、`.miz` fixture、expectation、traceability。 | frontend parsing と resolver symbol collection を実行し、source-to-checker payload extraction が存在するまで stable detail key で MC-G020 を report する active runner として実装する。external gap の fail coverage を追加し、real task 7-11 pass/fail semantic `.miz` assertion は payload を捏造せず deferred にする。 |
| 13 | `registration_resolution.md` を仕様化する。 | architecture 04 registration databases; spec 17。 | 英日 docs; task 14 と 16-20 の planned tests。 | task 13 で完了: spec は pending/activated database、validation obligation、existential gating、cluster closure、reduction provenance、diagnostic、planned test、MC-G021 external/deferred payload gap を code なしで定義する。 |
| 14 | registration index を実装する。 | `registration_resolution.md`; resolver registration increment。 | `src/registration_resolution.rs`; pending/activated/rejected/index/debug tests。 | task 14 で完了: resolver registration は identity/origin record として保存し、opaque shell と recovered origin は発火させず、semantic payload は explicit `ActivationInput` 経由でのみ accepted active record に移す。MC-G021 semantic payload gap は deferred のまま。 |
| 15 | `cluster_trace.md` を仕様化する。 | architecture 17。 | 英日 docs; task 16-18 の planned tests。 | task 15 で完了: `cluster_trace.md` は canonical `ResolutionTrace` schema を fork せず精緻化し、checker-local な cluster/reduction step ownership、antecedent fact、audit key、deterministic traversal、replay bound、failure class、planned test、MC-G023 deferred/external/test gap を code なしで定義する。 |
| 16 | trace recording 付き cluster closure を実装する。 | `cluster_trace.md`; `registration_resolution.md`。 | closure、replay、order tests。 | task 16 で完了: `ClusterTraceBuilder` は explicit cluster rule/fact payload と activated registration を消費し、replayable cluster step と trace provenance 付き derived closure fact を記録し、inactive/non-cluster/mismatched payload を拒否する。reduction、loop/bound failure、contradiction failure、artifact emission、`TypeFactTable` integration は deferred のまま。 |
| 17 | cluster loop detection と bounded saturation を実装する。 | architecture 17。 | loop/limit diagnostic、contradiction、config-visible bound tests。 | task 17 で完了: cluster closure は fact ancestry/depth を追跡し、direct / indirect loop を診断し、profile/cache-key visibility 付きで depth / generated-fact bound を強制し、explicit conflict-fingerprint contradiction を incomplete closure result として報告し、拒否された degraded fact は挿入しない。source-derived `TypeFactTable` contradiction check と artifact/cache integration は deferred のまま。 |
| 18 | reduction application を実装する。 | `registration_resolution.md`; architecture 17。 | redex path、guard evidence、replay tests。 | task 18 で完了: `ReductionTraceBuilder` は explicit payload 上で replayable reduction step を記録し、architecture-17 provenance field を保持し、active reduction registration、rule-view fingerprint、substitution、guard evidence、strategy-audit key を検証し、`such` guard を applicability-only evidence として扱う。raw syntax matching、resolver-shell parsing、artifact/cache integration、source-derived reduction extraction は deferred のまま。 |
| 19 | pending-registration validation と activation gating を実装する。 | `registration_resolution.md`; architecture 04 Step 6。 | invalid-registration と unverified-activation tests。 | 暫定 policy は未検証 registration を inactive に保つ。proof-policy dependency を分類する。 |
| 20 | attribute 付き型使用の existential gating を強制する。 | `registration_resolution.md`; spec 17。 | missing-existential fail fixtures。 | error 後は安全に degrade し、unverifiable inhabitant を export しない。 |
| 21 | `overload_resolution.md` を仕様化する。 | architecture 05; spec 18; spec 19。 | 英日 docs; task 22-26 の planned tests。 | site、template、viability、specificity、view、failed site の named section を持つ。 |
| 22 | candidate site collection を実装する。 | `overload_resolution.md` sites。 | site coverage、provenance、candidate-order tests。 | scope-filter 済み candidate を消費し、global scan しない。 |
| 23 | template expansion を実装する。 | `overload_resolution.md` templates; parser template surface。 | expansion と exclusion-reason tests。 | unsupported scheme/template role は捏造せず defer する。 |
| 24 | viability filtering を実装する。 | `overload_resolution.md` viability。 | viability と rejection-reason tests。 | recorded fact のみを使い、新しい inference や cluster firing はしない。 |
| 25 | specificity graph construction を実装する。 | `overload_resolution.md` specificity。 | ordering、incomparability、deterministic rendering tests。 | graph は site ごとであり global DAG ではない。 |
| 26 | root selection、refinement join、view insertion を実装する。 | `overload_resolution.md` selection/views。 | selection、ambiguity、refinement、failed-site tests。 | return-type selection はせず、inserted `qua` view は widening 限定。 |
| 27 | `resolved_typed_ast.md` を仕様化する。 | architecture 01; architecture 05 Step 6。 | 英日 docs; task 28 の planned tests。 | final semantic AST table と metadata contract を明示する。 |
| 28 | `ResolvedTypedAst` assembly を実装する。 | `resolved_typed_ast.md`。 | assembly、`ExprId` metadata、rendering tests。 | failed overload site は明示的に残し、成功 node を捏造しない。 |
| 29 | `formula_statement` / `advanced_semantics` corpus を拡張する。 | mizar-test staged model; architecture 20。 | registration/overload/soundness `.miz` fixture と traceability。 | prerequisite がある review-audit negative case を含める。proof-owned case は明示 defer。 |
| 30 | determinism suite を追加する。 | architecture 20。 | type、fact、trace、candidate、diagnostic の property/regression tests。 | 同一 input は run をまたいで同一 output を生成する。 |
| 31 | public-enum forward-compatibility policy を適用する。 | all module specs; frontend task-25 procedure。 | lint guard update と spec decision。 | すべての public enum を分類し guard する。 |
| 32 | source/spec correspondence を audit する。 | all module specs and this TODO。 | audit doc と修正または follow-up record。 | すべての public API と promised behavior を implementation/test へ trace するか分類する。 |
| 33 | bilingual documentation sync を audit する。 | repository docs policy。 | 英日 checker design docs。 | content を同期するか、理由付き explicit sync debt を記録する。 |
| 34 | module-boundary refactor gate を実行する。 | internal 07; all module specs。 | source-layout audit と必要なら behavior-preserving split。 | move に behavior/API change を混ぜない。source/spec と bilingual scope を再実行する。 |

task-specific forbidden behavior:

| task | forbidden behavior |
|---|---|
| 0 | code edit、semantic stub 作成、plan commit なしでの task 1 開始。 |
| 1 | scaffold と guard を超える checker semantics、fake typed output、非最小 API の追加。 |
| 2 | architecture 04/01 に支えられない implementation behavior の選択、または `VcId` assignment。 |
| 3 | `typed_ast.md` を超える language semantics の拡張、arena id を proof/artifact identity にすること。 |
| 4 | resolver-owned lookup、proof obligation、type inference behavior の仕様化。 |
| 5 | resolver symbol collection の再実装、欠けている binding payload の捏造。 |
| 6 | `doc/spec` semantics の暗黙変更、spec-only task への implementation 混入。 |
| 7 | source spelling order、hash-map order、現行 implementation の癖を normalization rule にすること。 |
| 8 | invalid binding を verified fact として受理すること、proof-search behavior の生成。 |
| 9 | ordinary overload root selection、新しい registration fact の導出、hard error 後の successful type 捏造。 |
| 10 | `VcId` assignment、obligation discharge、narrowing を proof-free success と扱うこと。 |
| 11 | nondeterministic な fact query order、semantic に同一な fact の重複。 |
| 12 | 既存 expectation を current checker output に合わせて rebaseline すること、traceability なしの active case 化。 |
| 13 | pending registration を active と扱うこと、architecture 04 の registration boundary を fork すること。 |
| 14 | resolver registration payload、artifact schema、activation status の捏造。 |
| 15 | architecture 17 の canonical trace schema の fork、replay field の省略。 |
| 16 | hidden cluster fact の追加、traversal accident への依存。 |
| 17 | saturation の黙った切り捨て、発散、bound を configuration/cache key から見えなくすること。 |
| 18 | parser syntax rewrite、guard evidence の省略、provenance なしの reduction 適用。 |
| 19 | generated obligation を accepted proof と扱うこと、unverified registration の activation、暫定 gating policy の迂回。 |
| 20 | unverified inhabitant の export、existential gap を warning only とすること。 |
| 21 | ordinary root selection に return type を使うこと、template behavior を implementation-only にすること。 |
| 22 | resolver-filtered visibility を超えて global `SymbolEnv` scan で candidate set を広げること。 |
| 23 | unsupported scheme/template source role の捏造、template inference 中の cluster expansion 使用。 |
| 24 | viability check 中の新規 fact 導出、registration firing、新しい type inference。 |
| 25 | global specificity DAG の構築、nondeterministic tie-break への依存。 |
| 26 | return-type tie-breaker、narrowing view insertion、failed site の valid output 化。 |
| 27 | implementation の追加、failed-site / metadata contract の spec からの省略。 |
| 28 | core logic への elaboration、failed overload site の削除、final definition の捏造。 |
| 29 | 下位 stage prerequisite が実行可能でない advanced fixture の追加。ただし明示 deferred は除く。 |
| 30 | platform order、worker timing、transient path に依存する determinism test。 |
| 31 | 新しい public enum を owning spec で未分類・未文書化のままにすること。 |
| 32 | implementation/test evidence または explicit deferral なしで audit gap を close すること。 |
| 33 | documented sync reason なしで片方の language companion だけを更新すること。 |
| 34 | file move に behavior cleanup、API exposure、diagnostic change、schema change を混ぜること。 |

## crate completion and exit criteria

Hard gate:

- [todo.md](./todo.md) の non-deferred task がすべて 1 task = 1 commit で commit されている。
- blocking/high specification inconsistency が残っていない。
- `doc/spec/en` と tests に存在しない implemented source behavior が残っていない。
  まだ仕様化・テスト化できない behavior は milestone scope から除外するか、
  deferred design item として未実装のままにする。
- milestone-owned specification item には `.miz` coverage、task-local Rust test、
  または explicit deferred reason がある。
- 既存 `.miz` test と expectation は implementation behavior に合わせるためだけには変更されていない。
- design docs、英日 companion、source が同期している。
- checker crate boundary が侵害されていない。
- required verification command が通過しているか、未実行の明示理由がある。
- hard gate 通過後、read-only crate quality review が 90/100 以上を与えている。

Verification commands:

```text
cargo fmt --check
cargo clippy -p mizar-checker --all-targets -- -D warnings
cargo test -p mizar-checker
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

resolver boundary、corpus、shared harness に触れる task では [todo.md](./todo.md) に
従って追加 command を実行する。

Crate exit report:

- crate completion に到達したら `doc/design/mizar-checker/en/crate_exit_report.md` と
  日本語 companion を追加する。
- task commit、review result、verification、deferred item、external dependency
  gap、quality score、next-crate handoff を含める。
