# Crate Plan: mizar-checker

> Canonical language: English. Japanese companion:
> [../ja/00.crate_plan.md](../ja/00.crate_plan.md).

Status: autonomous crate-development plan. Preliminary task 0 created this
plan before [todo.md](./todo.md) task 1; task 1 has since added the crate
scaffold; task 2 has since added the `TypedAst` data-shape spec; task 3 has
since implemented the `typed_ast` data shapes; task 4 has since added the
`binding_env` spec; task 5 has since implemented the `binding_env` data layer
and module shell; task 6 has since added the `type_checker` spec; task 7 has
since implemented type-expression normalization; task 8 has since implemented
declaration and local-binding checking; task 9 has since implemented
term/formula inference over checker-owned payloads; task 10 has since
implemented coercion candidate and initial-obligation checking over
checker-owned payloads; task 11 has since implemented deterministic type-fact
queries over checker-owned fact tables and local contexts; task 12 has since
added the active `type_elaboration` corpus runner for the source-to-checker
payload extraction gap; task 13 has since added the
`registration_resolution` spec; task 14 has since implemented the
registration database data layer; task 15 has since added the
`cluster_trace` spec; task 16 has since implemented the cluster closure data
layer with trace recording; task 17 has since implemented cluster loop,
bound, and explicit contradiction failure handling; task 18 has since
implemented the reduction trace data layer over explicit payloads; task 19
has since implemented pending-registration validation and activation gating
over explicit payloads; task 20 has since implemented existential gates over
explicit payloads; task 21 has since added the `overload_resolution` spec;
task 22 has since implemented checker-local overload site/candidate
collection over explicit payloads; task 23 has since implemented
checker-local template expansion over explicit payloads; task 24 has since
implemented checker-local viability filtering over explicit recorded-evidence
payloads; task 25 has since implemented per-site specificity graph
construction over explicit comparison payloads; task 26 has since implemented
root selection, refinement joins, and inserted-view recording; task 27 has
since added the `resolved_typed_ast` spec; task 28 has since implemented final
resolved typed AST assembly; task 29 has since recorded deferred corpus
obligations; task 30 has since added a cross-module determinism suite; task 31
has since guarded public enum forward compatibility; task 32 has since added
the source/spec correspondence audit; task 33 has since added the bilingual
documentation sync audit; task 34 has since recorded the module-boundary
refactor gate audit; task 38 has since resolved the functorial-cluster `for`
applicability-guard decision; task 39 has since resolved property-implementation
coherence for overlapping mode domains; task 48 has since added the
syntax-free reserve source declaration producer seam; task 50 has since added
the same-module attributed reserve evidence-query bridge; task 51 has since
added the same-module local mode reserve expansion-gap bridge; task 52 has
since added the same-module local structure reserve evidence-gap bridge; task
53 has since added the attributed local structure reserve evidence-gap bridge;
task 54 has since added the attributed local mode reserve expansion-gap bridge;
task 55 has since added the bare local mode expansion bridge; task 56 has
since added the one-edge local mode expansion chain bridge; task 57 has since
added the local mode structure-RHS evidence-gap bridge; task 58 has since
added the local mode attributed-builtin RHS evidence-gap bridge; task 59 has
since added the attributed local mode reserve evidence-gap bridge; task 60 has
since added the attributed local mode structure-RHS evidence-gap bridge; task
61 has since added the attributed local mode attributed-builtin-RHS
evidence-gap bridge; task 62 has since added the local mode structure-RHS
chain evidence-gap bridge; task 63 has since added the local mode
attributed-builtin-RHS chain evidence-gap bridge; task 64 has since added the
attributed local mode bare-builtin chain evidence-gap bridge; task 65 has since
added the attributed local mode structure-RHS chain evidence-gap bridge; the
closeout task has since added this crate exit report.

## Responsibility

Owned behavior:

- implement pipeline phases 6-8: type checking, registration/cluster
  resolution, and overload resolution;
- consume `mizar_resolve::resolved_ast::ResolvedAst` plus
  `mizar_resolve::env::SymbolEnv` and produce `TypedAst`,
  `TypeFactTable`, `ResolutionTrace`, and `ResolvedTypedAst`;
- normalize soft type expressions into deterministic predicate-like forms;
- build layered checker-local typing contexts over resolver-owned symbol
  identities;
- infer term and formula types while preserving recoverable error state;
- record type facts, coercion candidates, and `InitialObligationId`-backed
  initial obligations without assigning `VcId`s;
- index pending and activated registrations, compute replayable cluster
  closure, apply reduction registrations with provenance, and detect bounded
  saturation failures;
- expand and filter overload candidates, select ordinary roots, join accepted
  same-root refinements, insert allowed `qua` views, and preserve failed sites;
- keep ids, debug renderings, diagnostics, fact queries, candidate order, trace
  order, and artifact-facing projections deterministic.

Out of scope:

- lexical, syntax, parser, frontend, module-index, source-loading, and package
  planning ownership;
- name lookup, import/export validation, label resolution, and signature
  collection, which remain owned by `mizar-resolve`;
- arbitrary first-order reasoning, ATP search, proof certificate acceptance,
  theorem validity checking, and kernel replay;
- `VcId` assignment, `ObligationAnchor` construction, VC generation, and proof
  policy decisions beyond emitting checker-local initial obligations;
- stable published artifact schemas, cache storage, LSP rendering, and build
  scheduling;
- activating unverified local registrations merely because they occur earlier
  in the same source pass.

Boundary rules:

- every checker fact must be explainable as a declared type predicate,
  checker-owned inference fact, or registration-derived fact with trace
  provenance;
- source behavior must follow `doc/spec/en/` and existing `.miz` test intent,
  not observed source convenience;
- recovered or erroneous nodes may receive partial typed output, but invalid
  facts must not be exported as verified metadata;
- active registrations used for automatic inference must come from accepted
  dependency artifacts or accepted local summaries; an interim policy may
  record pending/unverified registrations and generated obligations, but it
  must not make unverified registrations contribute automatic facts;
- no task may introduce proof search, hidden coercion insertion, unrecorded
  cluster facts, or return-type-based overload selection.

## Authority Order And Specification References

For language behavior, use the repository authority order:

1. `doc/spec/en/`
2. `tests/**/*.miz`
3. `tests/coverage/spec_trace.toml`
4. `tests/**/*.expect.toml`
5. `doc/design/`
6. `crates/`

Primary references:

| Ref | Requirement | Checker status |
|---|---|---|
| [architecture 01](../../architecture/en/01.ir_layers.md) | `TypedAst`, `ResolutionTrace`, and `ResolvedTypedAst` are immutable source-shaped IR layers. | Refined by tasks 2, 3, 15, 27, and 28. |
| [architecture 04](../../architecture/en/04.type_and_registration_resolution.md) | Phases 6-7 own type normalization, local type contexts, type facts, coercion candidates, registration databases, cluster closure, reduction provenance, and partial typing after diagnostics. | Primary design input for wave 1 and wave 2. |
| [architecture 05](../../architecture/en/05.overload_resolution.md) | Phase 8 owns template expansion, viability, specificity graphs, root selection, refinement joins, inserted views, failed-site preservation, and `ResolvedTypedAst`. | Primary design input for wave 3. |
| [architecture 16](../../architecture/en/16.substitution_and_binding.md) | Binding identity, alpha-equivalence, capture avoidance, and definition-time closures must remain deterministic. | Refined by task 4 and consumed by task 5. |
| [architecture 17](../../architecture/en/17.cluster_trace_format.md) | `ResolutionTrace` must explicitly record cluster and reduction steps with deterministic traversal and loop detection. | Refined by tasks 15-18. |
| [architecture 18](../../architecture/en/18.dependency_fingerprint.md) | Checker outputs contribute type, registration, cluster, reduction, overload, and inserted-view dependency slices. | Per-source contribution tracking is required in registration and overload tasks; cache storage is out of scope. |
| [architecture 19](../../architecture/en/19.failure_semantics.md) | Type errors, overload ambiguity, cluster loops, and blocked work need stable phase ownership and deterministic ordering. | Applies to all diagnostic-bearing tasks. |
| [internal 07](../../internal/en/07.crate_module_layout.md) | `mizar-checker` owns type checking, registration resolution, and overload resolution inputs. | Defines the module split used by `todo.md`. |
| [spec 03](../../../spec/en/03.type_system.md) | Soft types, radix types, attributes, and subtyping. | Consumed by tasks 6-11 and 20. |
| [spec 08](../../../spec/en/08.type_inference.md) | Inferred types, coercions, `qua`, and `reconsider`. | Consumed by tasks 6-11 and 21-28. |
| [spec 13](../../../spec/en/13.term_expression.md) | Term expressions, set expressions, choice terms, and term-level typing surfaces. | Consumed by tasks 6 and 9-11. |
| [spec 17](../../../spec/en/17.clusters_and_registrations.md) | Cluster, existential, functorial, and reduction registrations. | Consumed by tasks 13-20. |
| [spec 18](../../../spec/en/18.templates.md) | Template definitions and template argument behavior. | Consumed by tasks 21 and 23. |
| [spec 19](../../../spec/en/19.overload_resolution.md) | Ordinary overload root selection and active refinements. | Consumed by tasks 21-28. |
| [spec 22](../../../spec/en/22.error_handling_and_diagnostics.md) | Type, registration, overload, and diagnostic classes. | Stable public code ownership must be checked before user-facing diagnostics. |
| [mizar-test staged model](../../mizar-test/en/staged_model.md) | `type_elaboration`, `formula_statement`, and `advanced_semantics` stages order semantic corpus growth. | Task 12 creates the first checker stage runner; task 29 records deferred formula/advanced semantic obligations until runners and source payloads exist. |

## Relevant Existing Tests And Missing Coverage

| Test path | Current intent | Checker relevance |
|---|---|---|
| `tests/miz/pass/parser/pass_parser_type_expressions_001.miz` and matching fail cases | Parser accepts and recovers type-expression syntax. | Source syntax exists for task 7 normalization fixtures, but these are parse-only and do not test checker behavior. |
| `tests/miz/pass/parser/pass_parser_qua_terms_001.miz` and `fail_parser_qua_missing_type_001.miz` | Parser covers `qua` source shape. | Inputs for task 10 coercion candidates and task 26 view insertion; no checker assertions yet. |
| `tests/miz/pass/parser/pass_parser_consider_reconsider_001.miz` and matching fail case | Parser covers declaration and binding surfaces. | Inputs for tasks 5, 8, and 10; no checker assertions yet. |
| `tests/miz/pass/parser/pass_parser_registrations_001.miz` and matching fail case | Parser covers registration syntax and recovery. | Inputs for tasks 13-20; no active registration semantics yet. |
| `tests/miz/pass/parser/pass_parser_template_arguments_001.miz` and `pass_parser_template_references_001.miz` | Parser covers template argument/reference syntax. | Inputs for tasks 21 and 23; no overload-template semantics yet. |
| `tests/miz/pass/resolve/pass_resolve_declaration_symbol_smoke_001.miz` | Active declaration-symbol smoke coverage reaches resolver symbol collection. | Lower-stage prerequisite for checker tests that consume `SymbolEnv`. |
| `tests/miz/fail/resolve/fail_resolve_duplicate_theorem_symbol_001.miz` | Active resolver duplicate-symbol failure coverage. | Confirms some resolver failures occur before checker stages. |
| `tests/coverage/spec_trace.toml` planned/deferred `type_elaboration`, `formula_statement`, and `advanced_semantics` rows | Records future semantic obligations without active checker execution. | Task 12 adds the first active `type_elaboration` gap runner entry; task 16 adds active builtin type-expression pass coverage; task 29 records formula/advanced obligations as deferred rows without active fixtures. |
| `tests/miz/pass/types`, `tests/miz/fail/types`, `tests/miz/pass/attributes`, `tests/miz/pass/clusters`, `tests/miz/fail/clusters`, `tests/miz/fail/overload` | Directory placeholders. | `test_gap`: checker semantic fixture sets have not started. |

Planned test-first additions:

- task-local Rust unit tests for deterministic ids, tables, rendering, query
  APIs, trace ordering, candidate order, and diagnostic ordering;
- task 10 now adds Rust coverage for widening, source `qua`, and narrowing
  coercion candidates, sethood/non-emptiness/narrowing obligations, invalid
  `qua`, missing evidence, and non-consumable supporting facts;
- task 11 now adds Rust coverage for fact query provenance matching,
  deterministic canonical ordering, no fact duplication, local assumption
  visibility, missing context behavior, contradiction diagnostics, and
  non-mutating queries;
- task 12 now adds active `.miz` fail coverage for the missing
  source-to-checker type-elaboration payload bridge; task 16 adds active pass
  coverage only for source-derived builtin `set`/`object` type-expression
  normalization; Campaign II task 17 projects that supported slice through
  checker-owned `ResolvedTypedAst`; task 48 moves reserve binding/declaration
  handoff production into a checker-owned syntax-free seam; task 50 extracts
  same-module attributed builtin reserve payloads to the checker evidence-query
  diagnostic; task 51 extracts same-module local mode reserve heads to the
  checker missing mode-expansion diagnostic; task 52 extracts same-module
  local structure reserve heads to the checker missing evidence-query
  diagnostic; task 53 extracts attributed same-module local structure reserve
  heads to the checker missing evidence-query diagnostic; task 54 extracts
  attributed same-module local mode reserve heads to the checker missing
  mode-expansion diagnostic; task 55 extracts real AST-derived bare-builtin RHS
  expansions for same-module no-argument local mode reserve heads; task 56
  extends that pass slice by extracting one-edge same-module local mode chains
  whose dependency has an accepted bare builtin RHS expansion; task 57 extracts
  real same-module local-mode expansions whose RHS is a same-module local
  structure head to the checker evidence-query diagnostic for missing
  base-shape/constructor-witness evidence; task 58 extracts real same-module
  local-mode expansions whose RHS is an attributed builtin head to the checker
  evidence-query diagnostic for missing attributed-type existential evidence;
  task 59 extracts attributed same-module local mode reserve heads with real
  direct bare-builtin RHS expansions to the checker evidence-query diagnostic
  when the same mode is not mixed with a bare reserve use; task 60 extracts
  attributed same-module local mode reserve heads with real direct local
  structure RHS expansions to the checker evidence-query diagnostic when the
  same mode is not mixed with a bare reserve use; task 61 extracts attributed
  same-module local mode reserve heads with real direct attributed-builtin RHS
  expansions to the checker evidence-query diagnostic when the same mode is
  not mixed with a bare reserve use; task 62 extracts one-edge bare local-mode
  chains ending in a same-module local structure RHS to the checker
  evidence-query diagnostic for missing base-shape/constructor-witness
  evidence; task 63 extracts one-edge bare local-mode chains ending in an
  attributed builtin RHS to the checker evidence-query diagnostic for missing
  attributed-type existential evidence; task 64 extracts attributed same-module
  local mode reserve heads whose one-edge dependency chain ends in a bare
  builtin RHS to the same missing attributed-type evidence query. Imported,
  argument-bearing, parameterized,
  contextual, ambiguous, cyclic, or forward-reference mode expansion,
  structure base-shape/full
  attributed-type existential evidence, and AST-wide task 7-11 payload
  extraction remain deferred without fabrication;
- task 14 now adds Rust coverage for pending registration non-firing,
  deterministic activation movement, per-source contribution round-tripping,
  invalid activation rejection, and stable checker-owned ordering/rendering;
- task 15 now specifies planned Rust coverage for trace-recorded cluster
  closure, loop/bound handling, reduction replay, strategy-audit diagnostics,
  and trace determinism; implementation coverage starts in tasks 16-18;
- task 16 now adds Rust coverage for replayable cluster closure, deterministic
  cluster step ordering, multi-antecedent conditional clusters, transitive
  chains, inactive/non-cluster non-firing, and mismatched activation payloads;
- task 17 now adds Rust coverage for direct and indirect cluster loops,
  depth and generated-fact bounds, zero-antecedent depth, explicit
  contradiction diagnostics, incomplete closure status, and cache-key-visible
  traversal settings;
- task 18 now adds Rust coverage for replayable reduction steps, architecture
  17 reduction provenance fields, inactive/rejected/non-reduction non-firing,
  invalid substitutions, missing guard evidence, strategy-audit mismatches, and
  `such` guards as applicability-only evidence;
- task 22 now adds Rust coverage for overload site coverage across supported
  roles, site and candidate provenance retention, source-written `qua`
  metadata retention, already-filtered candidate preservation, deterministic
  candidate order, duplicate site keys, missing candidate-site links, and
  deferred unsupported roles;
- task 23 now adds Rust coverage for explicit template expansion, omitted
  inference payloads, missing omitted inference, accepted/missing/deferred
  constraint evidence, source-`qua` widening/narrowing status, non-template
  priority, deferred unsupported candidates, exclusion diagnostics, and
  deterministic expansion rendering;
- task 24 now adds Rust coverage for exact matches, consumable fact widening,
  accepted source-`qua` coercions, pending/degraded/rejected/out-of-scope fact
  evidence, missing evidence, invalid narrowing, ambiguous inheritance,
  blocked/deferred external evidence, missing viability payloads, stable
  rejection/block diagnostics, and deterministic viability rendering;
- task 25 now adds Rust coverage for comparable, equivalent, and incomparable
  candidate pairs, accepted edge rendering, per-site graph construction,
  return-type-independent ordering, missing/duplicate/unknown/cross-site
  comparison payload diagnostics, and deterministic graph rendering;
- task 29 records deferred traceability entries for `formula_statement` and
  `advanced_semantics`; active `.miz` fixtures remain future work after those
  runners and the required source-to-checker payload seams land;
- task 38 adds inactive advanced-semantics coverage for the functorial-cluster
  `for` guard, pinning that consequents are unavailable outside the guarded
  result type until MC-G020/MC-G021/MC-G023/MC-G030 runner and payload gaps
  close;
- task 39 adds inactive advanced-semantics coverage for overlapping property
  implementations without a required coherence proof, deferred until
  MC-G020/MC-G030 runner and property-payload extraction gaps close;
- task 30 adds a checker-owned Rust determinism suite covering exact reruns
  and canonicalized equivalent-input permutations for type normalization,
  fact queries, cluster traces, overload candidate ordering/diagnostics, and
  final `ResolvedTypedAst` projection;
- task 31 records public enum forward-compatibility decisions in every checker
  module spec and adds a lint-policy guard that every plain `pub enum` remains
  `#[non_exhaustive]` and listed in the owning English/Japanese policy table;
- task 32 records a source/spec correspondence audit that inventories every
  current checker `pub mod` export, top-level public item, and public
  `dense_id!` / `string_key!` newtype, then traces promised behavior to source,
  Rust tests, or explicit gap/deferred rows; the lint-policy guard now checks
  that the audit covers the current public surface and MC-G rows;
- task 33 records a bilingual documentation sync audit covering every current
  English/Japanese checker design document pair, with `none` sync debt and a
  lint-policy guard over pair inventory and file pairing;
- task 34 records a module-boundary refactor gate audit over the current Rust
  source/test-support file layout, with no required behavior-neutral split and
  a lint-policy guard over the source layout inventory;
- the closeout task records `crate_exit_report.md` in English/Japanese,
  including task commits, hard gates, deferred items, verification, quality
  score, and next-crate handoff;
- no existing `.miz` tests or expectation sidecars may be rebaselined merely
  to match current source behavior.

## Design And Source Inventory

Design files:

- [todo.md](./todo.md)
- this crate plan
- [source_spec_audit.md](./source_spec_audit.md)
- [bilingual_sync_audit.md](./bilingual_sync_audit.md)
- [module_boundary_audit.md](./module_boundary_audit.md)
- [crate_exit_report.md](./crate_exit_report.md)
- existing module specs: [typed_ast.md](./typed_ast.md),
  [binding_env.md](./binding_env.md), [type_checker.md](./type_checker.md),
  [registration_resolution.md](./registration_resolution.md), and
  [cluster_trace.md](./cluster_trace.md), and
  [overload_resolution.md](./overload_resolution.md), and
  [resolved_typed_ast.md](./resolved_typed_ast.md)
- future module specs: none currently identified
- architecture references 01, 04, 05, 16, 17, 18, and 19
- [mizar-resolve crate plan](../../mizar-resolve/en/00.crate_plan.md),
  [resolved_ast.md](../../mizar-resolve/en/resolved_ast.md), and
  [env.md](../../mizar-resolve/en/env.md)
- [mizar-test staged model](../../mizar-test/en/staged_model.md),
  [expectation schema](../../mizar-test/en/expectation_schema.md), and
  [traceability](../../mizar-test/en/traceability.md)

Source files:

- Task 1 adds `crates/mizar-checker/Cargo.toml`,
  `crates/mizar-checker/src/lib.rs`, and
  `crates/mizar-checker/tests/lint_policy.rs`.
- Task 3 adds `crates/mizar-checker/src/typed_ast.rs` and exposes the
  documented `typed_ast` module from `src/lib.rs`.
- Task 4 adds `doc/design/mizar-checker/en/binding_env.md` and the Japanese
  companion.
- Task 5 adds `crates/mizar-checker/src/binding_env.rs` and exposes the
  documented `binding_env` module from `src/lib.rs`.
- Task 6 adds `doc/design/mizar-checker/en/type_checker.md` and the Japanese
  companion; source implementation starts in task 7.
- Task 7 adds `crates/mizar-checker/src/type_checker.rs` and exposes the
  documented `type_checker` module from `src/lib.rs`.
- Task 9 extends `crates/mizar-checker/src/type_checker.rs` with
  `TermFormulaChecker`, checked term/formula records, a task-local open
  candidate-set table, expected-type constraints, and task-local inference
  fact recording.
- Task 10 extends `crates/mizar-checker/src/type_checker.rs` with
  `CoercionObligationChecker`, task-local coercion request inputs, sethood/
  non-emptiness/narrowing obligation inputs, coercion/obligation debug
  rendering, and preservation of input fact ids for supporting facts.
- Task 11 extends `crates/mizar-checker/src/type_checker.rs` with
  `TypeFactQueryEngine`, `TypeFactQuery`, `TypeFactQueryOutput`, and
  deterministic query status/debug rendering over existing `TypeFactTable`
  entries.
- Task 13 adds `doc/design/mizar-checker/en/registration_resolution.md` and
  the Japanese companion; source implementation starts in task 14.
- Task 14 adds `crates/mizar-checker/src/registration_resolution.rs`, exposes
  the documented `registration_resolution` module from `src/lib.rs`, and
  updates the lint-policy guard for the new public API surface.
- Task 15 adds `doc/design/mizar-checker/en/cluster_trace.md` and the
  Japanese companion; source implementation starts in task 16.
- Task 16 adds `crates/mizar-checker/src/cluster_trace.rs`, exposes the
  documented `cluster_trace` module from `src/lib.rs`, and updates the
  lint-policy guard for the new public API surface.
- Task 17 extends `crates/mizar-checker/src/cluster_trace.rs` with ancestry
  tracking, loop/bound/contradiction diagnostics, closure status, cache-key
  material, and task-local tests over explicit checker-owned trace payloads.
- Task 18 extends `crates/mizar-checker/src/cluster_trace.rs` with
  `ReductionTraceBuilder`, explicit reduction input payloads, reduction trace
  steps, replay validation, diagnostics, and task-local tests.
- Task 22 adds `crates/mizar-checker/src/overload_resolution.rs`, exposes the
  documented `overload_resolution` module from `src/lib.rs`, and updates the
  lint-policy guard for the new public API surface.
- Task 23 extends `crates/mizar-checker/src/overload_resolution.rs` with
  `TemplateExpansionOutput`, `TemplateExpansionTable`, explicit template
  parameter/inference/constraint/source-qua payload validation, and task-local
  tests over explicit payloads.
- Task 24 extends `crates/mizar-checker/src/overload_resolution.rs` with
  `CandidateViabilityOutput`, `CandidateViabilityTable`, explicit
  recorded-evidence viability payload validation, viable candidate filtering,
  view-plan recording, rejection/block diagnostics, and task-local tests.
- Task 25 extends `crates/mizar-checker/src/overload_resolution.rs` with
  `SpecificityGraphOutput`, `SpecificityGraphTable`, explicit pairwise
  comparison payload validation, per-site graph/node/comparison/edge records,
  specificity diagnostics, and task-local tests.
- Task 26 extends `crates/mizar-checker/src/overload_resolution.rs` with
  `OverloadSelectionOutput`, root-selection, refinement-join, inserted-view,
  failed-site, and deterministic selection tests.
- Task 27 adds `doc/design/mizar-checker/en/resolved_typed_ast.md` and the
  Japanese companion; source implementation starts in task 28.
- Task 28 adds `crates/mizar-checker/src/resolved_typed_ast.rs`, exposes the
  documented `resolved_typed_ast` module from `src/lib.rs`, and updates the
  lint-policy guard for the new public API surface.
- Task 30 adds `crates/mizar-checker/src/determinism_suite.rs` as a
  checker-owned test module.
- Task 32 adds `doc/design/mizar-checker/en/source_spec_audit.md` and the
  Japanese companion without changing source behavior.
- Task 33 adds `doc/design/mizar-checker/en/bilingual_sync_audit.md` and the
  Japanese companion without changing source behavior.
- Task 34 adds `doc/design/mizar-checker/en/module_boundary_audit.md` and the
  Japanese companion, and extends `crates/mizar-checker/tests/lint_policy.rs`
  with a source-layout audit guard, without changing checker source behavior.
- The closeout task adds `doc/design/mizar-checker/en/crate_exit_report.md` and
  the Japanese companion without changing source behavior.
- Direct dependencies for the scaffold are `crates/mizar-session/` and
  `crates/mizar-resolve/`; later dependencies require task-scoped specs.
- Current resolver source surface includes `resolved_ast`, `env`, `symbols`,
  `labels`, `names`, and recovered-syntax handling. The checker must consume
  resolver-owned ids and opaque signature shells instead of redoing name
  lookup or symbol collection.
- Current corpus infrastructure has active parse-only and declaration-symbol
  runners. Task 12 adds an active `type_elaboration` runner that reaches
  frontend parsing and resolver symbol collection, then reports the missing
  checker payload-extraction bridge as an external dependency gap.

Observed behavior:

- `main` is exactly at local `origin/main` before task 0 (`51bcaed...`) with a
  clean worktree and no ahead/behind commits.
- `mizar-resolve` tasks 1-23 and 25-29 are complete; resolver task 24 was
  deferred at resolver close-out on the `mizar-artifact` `ModuleSummary`
  dependency.
- `mizar-artifact` now provides `ModuleSummary` and `RegistrationSummary`
  schemas, writers, and readers. Producer/reuse integration remains a later
  task for resolver and checker crates.
- `mizar-resolve` task 5 and task 20 are complete, so checker task 1's declared
  resolver prerequisites are available.
- `mizar-resolve` task 21 is marked complete, so later checker registration and
  overload tasks may inspect current signature-projection data, but each task
  must still verify whether the exact checker-needed payload exists before
  implementing behavior.

Observed behavior is inventory only. It is not normative unless supported by
`doc/spec/en`, tests, traceability metadata, design documents, or explicit
human approval.

## Known Gaps And Drift

The `Class` column records the protocol classification. User-requested
`external_dependency_gap` entries may appear directly when the missing input is
external to `mizar-checker`; when an already-classified item is also an
external-dependency or deferred planning gate, that status is recorded in the
evidence/action text.

| ID | Class | Evidence | Action |
|---|---|---|---|
| MC-G002 | `test_gap` | Checker semantic directories were placeholders and no active `type_elaboration` runner existed before task 12. | Task-local Rust tests cover implemented checker seams. Task 12 adds the first active checker corpus runner and traceability update for the source-to-checker bridge gap; real semantic pass/fail `.miz` coverage remains governed by MC-G020. |
| MC-G003 | `design_drift` | The previous checker TODO wording suggested possible activation after successful obligation generation, but architecture 04 requires accepted verifier status before automatic inference can use a registration. This is also a deferred `external_dependency_gap` because proof acceptance inputs are not implemented. | Repaired in task 0 and enforced in task 19: validated local registrations remain pending until explicit accepted verifier/artifact status is supplied. |
| MC-G004 | `source_drift` | Resolver close-out recorded task 24 as deferred on `mizar-artifact` task 5. `mizar-artifact` now provides the canonical `ModuleSummary` and `RegistrationSummary` schemas, but resolver/checker producer and reuse integration is still not wired. This remains an `external_dependency_gap` planning gate for integration tasks, not for task 1. | Task 1 must not add `mizar-artifact` as a dependency. Later producer/reuse tasks may consume canonical artifact schemas only through task-scoped specs. Do not invent checker-owned artifact schemas or readers. |
| MC-G005 | `spec_gap` / `external_dependency_gap` | `mizar-diagnostics` now exists, but public checker diagnostic code-space is still not allocated as a canonical spec/design range. | Keep task-local diagnostics crate-internal unless the relevant spec/design owner allocates public codes. Preserve stable classes and detail keys where tests need them. |
| MC-G006 | `source_drift` | Resolver close-out records that module-level scheme/template declaration source roles are not exposed by parser/syntax. This is an `external_dependency_gap` planning gate for template/overload tasks. | Template and overload tasks must not fabricate module-level scheme symbols. Implement only parser/resolver roles actually exposed at that time. |
| MC-G007 | `design_drift` | Architecture 04/05 older "Affected Modules" lists (`types.md`, `overload.md`, etc.) differ from the refined module table in checker `todo.md`. | Treat checker `todo.md` plus this plan as the crate-level module split; do not rename architecture files in task 0. |
| MC-G011 | `external_dependency_gap` | Task 5 confirmed that current resolver APIs expose `LocalTermScope`, `LocalTermBinding`, definition shell ids, and `NameRefEntry::resolution()`, but not a complete AST-wide local binding declaration table, use-site scope/ordinal table, reserve payload, captured-free-variable payload, or syntax-free empty `ResolvedAst` fixture for checker-owned tests. | Task 5 records external-gap diagnostics in the module shell, accepts explicit binding payloads when available, and type-checks the public module-shell signature without adding a direct `mizar-syntax` dependency. Later resolver/source-walk integration must provide the missing payloads and fixture before full source extraction and closure replay can be implemented. |
| MC-G014 | `external_dependency_gap` | Task 7 confirms that current resolver APIs expose symbol kinds and opaque signature shells, but not AST-wide type-expression site payloads, mode radix/attribute expansion payloads, attribute argument payloads, or structure/mode arity payloads. | Task 7 implements a checker-owned payload and mode-expansion provider seam, records missing explicit mode-expansion provider payloads as degraded normalization diagnostics, treats unsupported checker-owned payloads as recoverable degraded entries, and does not walk raw syntax or treat opaque resolver signatures as checker type payloads. |
| MC-G016 | `external_dependency_gap` | Task 8 confirms that resolver/binding inputs still do not expose an AST-wide declaration/type-site table, reserve default payload, right-hand-side term payload, definition body payload, or evidence queries for attributed/constrained declaration obligations. | Task 8 implements a checker-owned declaration payload seam over `BindingEnv` and task-7 normalized type expressions. Missing RHS/body/default-type/evidence inputs produce deferred diagnostics rather than fabricated source walks, inferred RHS types, or premature `InitialObligation`s. |
| MC-G017 | `external_dependency_gap` | Task 9 confirms that resolver/source inputs still do not expose an AST-wide typed term/formula payload table, built-in numeric type payloads, functor/predicate candidate signatures, selector and structure-field payloads, source `qua` coercion evidence, or sethood/non-emptiness evidence queries. | Task 9 implements a checker-owned term/formula payload seam, task-local open candidate-set table, expected-type constraints, and partial/error/skipped recovery. Missing resolver/source payloads produce degraded diagnostics; task-10-owned coercion and obligation work remains deferred rather than fabricated. |
| MC-G018 | `external_dependency_gap` | Task 10 confirms that resolver/dependency inputs still do not expose an AST-wide coercion request table, active dependency-summary fact database, structure inheritance graph, cluster-closure evidence, sethood evidence, non-emptiness evidence, or proof-query results. Task 47 adds an explicit-payload omitted-`reconsider` gate over supplied proof-free evidence markers and consumable fact ids, but source-derived omitted-`reconsider` extraction is still absent. | Task 10 implements checker-owned coercion and obligation payload seams, preserves input fact ids, records blocked/rejected entries with diagnostics for missing support, and emits local `InitialObligationId`s. Task 47 rejects omitted narrowing without consumable proof-free evidence as `type.narrowing_requires_proof` without creating an implicit obligation. Missing source payloads remain external gaps for later source extraction, fact query, registration, cluster, and proof-owned tasks. |
| MC-G019 | `external_dependency_gap` | Task 11 confirms that resolver/checker inputs still do not expose an AST-wide statement/proof assumption table, theorem acceptance payload, or phase-7 `ResolutionTrace` fact payloads. | Task 11 implements deterministic queries over existing checker fact tables and local contexts only. Missing statement/proof/trace payloads remain external gaps for statement, proof, registration, and cluster tasks; task 11 must not fabricate accepted proof facts or registration provenance. |
| MC-G020 | `partial source_drift` / `external_dependency_gap` / `deferred` | Task 16 adds a narrow reserve-only AST walk in `mizar-test` that converts unrecovered builtin `set`/`object` source `TypeExpression` nodes into checker-owned `TypeExpressionInput` payloads, runs `TypeNormalizer`, and assembles a minimal `TypedAst` shell without adding a direct `mizar-syntax` dependency to `mizar-checker`. Campaign II task 17 projects that supported shell through checker-owned `ResolvedTypedAst` using real expression metadata, source-preserved node hints, and empty cluster/overload predecessor outputs. The reserve declaration continuation extracts the same supported source family, task 48 moves checker-owned `BindingEnv`, `DeclarationInput`, and `DeclarationChecker` handoff production into a syntax-free `type_checker` seam consumed by the active runner before `TypedAst` and `ResolvedTypedAst` assembly, task 50 extracts same-module attributed builtin reserve payloads to the checker evidence-query diagnostic, task 51 extracts same-module local mode reserve heads to the checker missing mode-expansion diagnostic, task 52 extracts same-module local structure reserve heads to the checker missing evidence-query diagnostic, task 53 extracts attributed same-module local structure reserve heads to the same evidence-query diagnostic, task 54 extracts attributed same-module local mode reserve heads to the missing mode-expansion diagnostic when no supported real expansion is available or the same mode is mixed with a bare reserve use, task 55 extracts real AST-derived bare-builtin RHS expansions for same-module no-argument local mode reserve heads, task 56 extracts real AST-derived one-edge same-module local mode expansion chains when the dependency mode already has an accepted task-55 builtin RHS expansion, task 57 extracts a real same-module local mode expansion whose RHS is a same-module local structure head far enough to reach the checker evidence-query diagnostic for missing base-shape/constructor-witness evidence, task 58 extracts a real same-module local mode expansion whose RHS is an attributed builtin head far enough to reach the checker evidence-query diagnostic for missing attributed-type existential evidence, task 59 extracts an attributed same-module local mode reserve head with a real direct bare-builtin RHS expansion far enough to reach that same attributed-type evidence-query diagnostic when the same mode is not mixed with a bare reserve use, task 60 extracts an attributed same-module local mode reserve head with a real direct local-structure RHS expansion far enough to reach the missing full attributed structure-type evidence query when the same mode is not mixed with a bare reserve use, task 61 extracts an attributed same-module local mode reserve head with a real direct attributed-builtin RHS expansion far enough to reach the missing attributed-type evidence query when the same mode is not mixed with a bare reserve use, task 62 extracts a one-edge bare local-mode chain ending in a same-module local structure RHS far enough to reach the missing base-shape/constructor-witness evidence query, and task 63 extracts a one-edge bare local-mode chain ending in an attributed builtin RHS far enough to reach the missing attributed-type evidence query. The repository still lacks AST-wide source-to-checker extraction for non-builtin declarations, imported attributes/modes/structures, attribute arguments, mode/structure arguments, structure base-shape and full attributed-type existential evidence, broader/imported/attributed/argument-bearing/parameterized/contextual/ambiguous/cyclic/forward-reference mode expansion, terms, formulas, coercion sites, overload payloads, recorded facts, CoreIr, ControlFlowIr, VC payloads, and proof evidence. | Keep active pass coverage limited to the reserve-only builtin declaration/type-expression/`ResolvedTypedAst` slice plus task-55 bare local-mode expansions with builtin RHS and task-56 one-edge same-module local-mode chains whose dependency has that accepted builtin RHS expansion. Treat task-57 structure-RHS, task-58 attributed-builtin-RHS, task-59 attributed local-mode reserve, task-60 attributed local-mode structure-RHS, task-61 attributed local-mode attributed-builtin-RHS, task-62 local-mode structure-RHS chain, and task-63 local-mode attributed-builtin-RHS chain expansions as active diagnostic coverage only until real structure or attributed-type existential evidence exists. Continue to surface `type_elaboration.external_dependency.ast_payload_extraction` or checker-owned fail-closed diagnostics for unsupported payload families, mixed attributed/bare local-mode uses, and imported/ambiguous symbols, and do not fabricate checker payloads from syntax strings, base-shape/existential payloads, or broader pass cases through a stub. |
| MC-G021 | `external_dependency_gap` / `deferred` | Task 13 confirms that the resolver registration index exposes declaration identity, registration kind, opaque target shell, visibility/export metadata, dependencies, recovery state, and source contribution, but not checker-ready typed registration patterns, parameter type payloads, correctness-condition anchors, accepted verifier status, active dependency-summary consumption, reduction `LHS`/`RHS` term payloads, or guard-evidence payloads. Task 14 preserves resolver registration origins and exposes explicit activation payload seams. Task 19 consumes explicit validation payloads and validates them, but still does not source-extract those payloads or create accepted status. | Registration tasks use explicit checker-owned payload seams or defer behavior rather than parsing opaque shells, inventing summaries, or treating emitted obligations as accepted. |
| MC-G023 | `test_gap` / `external_dependency_gap` / `deferred` | Task 15 specifies the checker-local `ResolutionTrace` contract. Tasks 16-18 implement checker-owned cluster closure, loop/bound/contradiction handling, and reduction trace data layers with Rust coverage over explicit payload seams. Task 46 strengthens that explicit-payload coverage for fatal contradiction diagnostics and reduction trace identity over discharged side-condition evidence. Active `.miz` cluster/reduction semantic fixtures, artifact JSON emission, cache readers, source-derived `TypeFactTable` contradiction integration, source-derived normalization-result dependence, and real source-to-checker trace extraction remain unimplemented. Real source-derived trace steps still require MC-G020/MC-G021 checker-ready source and registration payloads plus accepted activation inputs. | Task 29 records deferred traceability rows instead of adding fake active `.miz` fixtures, because source-to-checker payload seams and an active advanced-semantics runner still do not exist. Artifact emission/reuse tasks must consume architecture 17 rather than inventing a checker-local schema fork. |
| MC-G025 | `external_dependency_gap` / `deferred` | Task 19 emits checker-local registration-correctness `InitialObligationId`s and gates activation on accepted verifier/artifact status, but the proof/artifact phase that creates or imports that accepted status is not wired to `mizar-checker`. | Keep valid local registrations pending until explicit accepted status input is supplied. Do not promote generated obligations to activated registrations. |
| MC-G026 | `test_gap` / `external_dependency_gap` / `deferred` | Task 20 implements existential gates over explicit payloads, and task 47 extends that seam to explicit base-shape inhabitation evidence for built-in `object`/`set`, accepted mode applications, structure constructor witnesses, and schema type parameters. Source-to-checker extraction of attributed/base gate sites, accepted mode tuples, complete structure-field coverage, schema-context assumptions, accepted-status production/import beyond task-19 activation input, artifact emission/reuse, and active `.miz` existential gate fixtures are not wired. | Keep task-local Rust coverage over explicit gate and base-evidence payloads. Defer real source-derived gate cases and artifact reuse until the owning extraction/proof/artifact tasks provide inputs. |
| MC-G027 | `test_gap` / `external_dependency_gap` / `deferred` | Task 21 specifies overload resolution; task 22 implements checker-local site/candidate collection over explicit payloads; task 23 implements checker-local template expansion over explicit payloads; task 24 implements checker-local viability filtering over explicit recorded-evidence payloads; task 25 implements per-site specificity graph construction over explicit comparison payloads; task 26 implements checker-local root selection, refinement-join validation, inserted-view recording, and failed-site preservation over explicit selection payloads; task 27 specifies the `ResolvedTypedAst` projection contract without code; task 28 implements checker-local `ResolvedTypedAst` assembly over explicit predecessor outputs; task 45 adds explicit-payload regressions for equivalent-root ambiguity, encoded non-template/template priority, and the omitted-redefinition-target producer boundary. AST-wide source-to-checker extraction for overload sites, candidate groups, template payloads, source `qua` paths, viability evidence, specificity comparison evidence, selection/refinement payloads, inserted-view evidence, source expression metadata, omitted `coherence with` target inference diagnostics, scheme/theorem roles, public diagnostic-code allocation, artifact emission/reuse, and active `.miz` overload semantic fixtures are still not wired. | Task 28 assembles only from explicit checker outputs and preserves failed sites rather than rewriting them into success. Task 29 records deferred overload traceability rows instead of adding fake active `.miz` fixtures, because source-to-checker payload seams and an active advanced-semantics runner still do not exist. Task 22 keeps unsupported site/candidate roles deferred with stable diagnostics; task 23 keeps unsupported template/scheme roles and missing external template payloads deferred or rejected with stable expansion diagnostics; task 24 keeps missing or externally deferred viability evidence blocked rather than inferring facts or inserting views; task 25 keeps missing/deferred comparison evidence blocked rather than inventing ordering edges or selecting roots; task 26 keeps missing selection/view payloads, non-selected refinements, blocked specificity graphs, and invalid inserted views as failed records rather than deriving facts or fabricating success; task 45 keeps omitted `coherence with` target inference as an upstream producer obligation, requires accepted redefinition candidates to arrive with one already-bound `ordinary_root` and `CandidateOrigin::Redefinition`, and keeps artifact emission, public diagnostic codes, source extraction, and active `.miz` semantic fixtures deferred. |
| MC-G030 | `test_gap` / `external_dependency_gap` / `deferred` | Task 29 confirms that active `formula_statement` and `advanced_semantics` `.miz` corpus growth cannot be soundly added yet: `mizar-test` has no active runner/tag gate for those stages, MC-G019/MC-G020 statement/formula/proof source extraction is missing, MC-G021 checker-ready registration payloads and accepted status are missing, MC-G023 cluster/reduction trace extraction is missing, and MC-G027 overload source extraction is missing. | Task 29 records deferred `spec_trace.toml` rows for formula/statement, cluster/reduction, overload/refinement, and review-audit negative obligations, with empty test lists and concrete deferral reasons. Do not add active pass/fail fixtures until the runners and source-to-checker payload seams exist. |
| MC-G009 | `repo_metadata_conflict` | None observed during task 0 inventory. | Report only if future metadata conflicts are found; do not auto-repair repository metadata conflicts. |

## Resolved Setup History

| ID | Class | Resolution |
|---|---|---|
| MC-G001 | `source_drift` | Task 1 adds the `mizar-checker` workspace member, minimal crate root, and manifest. |
| MC-G010 | `test_gap` | Task 1 adds the checker lint-policy guard for workspace lint opt-in, dependency-boundary checking, and documented `allow` exceptions. |
| MC-G012 | `source_drift` | Task 5 adds `src/binding_env.rs`, exposes the documented `binding_env` module, and updates the lint-policy guard for the new public API surface. |
| MC-G013 | `design_drift` | Task 6 resolves architecture 04's broad `TypeContext` / `CoercionCandidateTable` example names into the checker-local `BindingEnv`, immutable `LocalTypeContextTable`, and candidate-owning `CoercionTable` split. |
| MC-G015 | `source_drift` | Task 7 adds `src/type_checker.rs`, exposes the documented `type_checker` module, and updates the lint-policy guard for the new public API surface. |
| MC-G022 | `source_drift` | Task 14 adds `src/registration_resolution.rs`, exposes the documented `registration_resolution` module, and updates the lint-policy guard for the new public API surface. |
| MC-G024 | `source_drift` | Task 16 adds `src/cluster_trace.rs`, exposes the documented `cluster_trace` module, and updates the lint-policy guard for the new public API surface. |
| MC-G028 | `source_drift` | Task 22 adds `src/overload_resolution.rs`, exposes the documented `overload_resolution` module, and updates the lint-policy guard for the new public API surface. |
| MC-G029 | `source_drift` | Task 28 adds `src/resolved_typed_ast.rs`, exposes the documented `resolved_typed_ast` module, and updates the lint-policy guard for the new public API surface. |

## Resolved Step 2 Spec Decisions

| ID | Class | Resolution |
|---|---|---|
| SSA-001 | `spec_gap` | Task 35 resolves constructor property arguments vs fields-only extensionality by adopting the fields-only constructor rule. Spec 05 and 07 now state that default constructors accept fields only, generate projection axioms only for fields, and leave property values to Chapter 7 property implementations. The inactive `advanced_semantics` seed `fail_structure_constructor_property_arg_001` and traceability rows `spec.en.05.structures.constructor_fields_only.semantic` / `spec.en.07.modes.property_implementation.not_constructor_source.semantic` pin the rejected constructor-property form until MC-G020/MC-G030 runner and payload gaps close. No checker/core source semantics changed. |
| SSA-002, SSA-011, SSA-012 | `spec_gap` | Task 36 resolves structure member identity, implicit upcast path uniqueness, and inheritance acyclicity. Spec 05 now tracks inherited member identity as root declaration plus inheritance path/view, requires exact parent-member coverage and per-parent type-inclusion obligations discharged by the existing `coherence` block, preserves renamed same-root paths as distinct views, and names `structures.inherit.cycle` for cyclic inheritance. Spec 19 now states that implicit upcast uniqueness is syntactic over resolved `inherit` declaration paths. The inactive `advanced_semantics` seed `fail_structure_inherit_duplicate_member_coverage_001` pins duplicate parent-member coverage; existing structure/overload seeds remain valid, and the template view-leak seed guards cross-view evidence. No renamed-view reject seed was added because renamed-view exposure remains positive behavior. No checker/core source semantics changed. |
| SSA-003, SSA-010, SSA-016, SSA-019 | `spec_gap` / editorial | Task 37 resolves overload tie-breaks, equivalent-root ambiguity, specificity preorder wording, and the duplicated §19.6.1 text. Spec 19 now compares instantiated concrete parameter vectors under the normal `⊑` preorder after template expansion; template declared constraint strictness is not a tie-breaker, non-template priority applies only to mutually equivalent concrete vectors, return type remains excluded, and ambiguity is a nonempty maximal-root set with more than one distinct ordinary root. Ordinary definitions with identical argument signatures are declaration conflicts regardless of return type. Architecture 05 and the checker overload design now match this rule. Inactive seeds `fail_overload_equivalent_roots_ambiguity_001`, `fail_overload_template_equivalent_roots_ambiguity_001`, and `fail_resolve_same_signature_same_return_conflict_001` pin ordinary/template-derived equivalent-root ambiguity and the same-return ordinary declaration-conflict rule; the last remains inactive until resolver declaration-symbol support grows beyond the current different-return diagnostic. Mizar-core task 26 / template-audit F7 records the separate Phase A omitted-template inference determinism rule. No checker/core/resolver source semantics changed. |
| SSA-004 | `spec_gap` | Task 38 resolves functorial-cluster `for` semantics. Spec 17 now treats the trailing `for` type expression as an applicability guard over the full known normalized result type, including parameters and attributes, and the coherence/FOL encoding includes that guard as a premise. Spec 16's proof-obligation summary and spec 23's registration-node discussion delegate the guarded functorial encoding to Chapter 17. The inactive seed `fail_cluster_functorial_for_guard_001` and traceability row `spec.en.17.clusters.functorial_for_guard.semantic` pin that a valid registration does not provide its consequent at same-radix use sites that lack the guarded attribute. No checker/core source semantics changed. |
| SSA-005 | `spec_gap` | Task 39 resolves property-implementation coherence. Spec 07 now permits but conditionally requires a `coherence` block on property `means`/`equals` implementations when the implementation's normalized mode domain overlaps an earlier visible implementation of the same struct property; the obligation proves both implementations assign the same property value over the overlap. Spec 16 and Appendix A were synchronized. The inactive seed `fail_mode_property_overlap_missing_coherence_001` and traceability row `spec.en.07.modes.property_implementation.coherence.semantic` pin rejection of a narrower overlapping implementation without coherence, while deferred parser row `spec.en.07.modes.property_implementation.parser` records the property-implementation syntax support gap. No checker/core source semantics changed. |
| SSA-006 | `design_drift` | Task 40 resolves registration activation timing. Spec 17 keeps item-ordered activation as the language contract and states that correctness acceptance may be asynchronous: implementation may keep modules/use sites pending while proof/artifact status is unresolved, but a completed verification pass must not finally reject later uses that become accepted after an earlier registration is accepted. Architecture 04 and `registration_resolution.md` now label task 19's no-accepted-input behavior as an interim conservative approximation to be lifted when `mizar-vc`/`mizar-proof`/artifact integration supplies accepted status. The existing inactive seed `fail_mode_existential_after_declaration_001` and traceability row `spec.en.17.clusters.registration_activation_timing.semantic` pin the negative non-retroactive slice; positive accepted-local activation remains deferred on MC-G020/MC-G021/MC-G025/MC-G026. No checker/core source semantics changed. |
| SSA-007, SSA-008, SSA-020 | `spec_gap` | Task 41 resolves closure termination, contradiction detection, and `attr(args)`. Spec 17 now makes the restricted no-argument cluster `adjective` grammar a load-bearing termination premise, spec 19 points its closure fixpoint argument back to that premise, and architecture 04 treats saturation bounds as defensive failure diagnostics rather than successful truncated semantics. Spec 17.7.3 now maps contradictory derived attributes to closure-time fatal `cluster` diagnostics, including the static contradictory-consequent seed `fail_cluster_contradictory_consequent_001`. Spec 03/06/Appendix A define `attribute_name(args)` as a use-site positional application of a declared parameterized attribute while preserving the restricted cluster adjective grammar; active parser recovery for argument-bearing registration adjectives is traced by `spec.en.17.clusters.restricted_adjective_grammar.parser`. No checker/core source semantics changed. |
| SSA-009 | `spec_gap` | Task 42 resolves reduction determinism. Spec 17 now defines normalization as a deterministic function of the term, in-scope activated reduction rules, and the discharged side-condition set available at the rewrite site. `such` side conditions are applicability-only guards backed by stable local-fact, trace, or citation evidence and are excluded from specificity. Rule selection is pattern-first, guard-second, with position-wise §19.2.3 guard comparison only after pattern comparison does not select a unique rule; remaining equal, mixed, or incomparable cases fall to the lexicographically smallest rule FQN. `registration_resolution.md` mirrors this rule. No checker/core source semantics changed. |
| SSA-013, SSA-014 | `spec_gap` | Task 43 resolves parameterized sethood and built-in/base-shape inhabitation. Spec 07 now gives guarded mode-existence and sethood obligations over valid parameter tuples, makes exported sethood a module-interface semantic fact without exporting the concrete witness, and points mode RHS inhabitation to the 17.3.4 table. Spec 13 checks comprehension sethood at the resolved mode and normalized argument tuple. Spec 17 distinguishes attributed type expressions, which still require active existential registrations, from unattributed `object`, `set`, accepted mode applications, bare structure radixes, and bare schema type parameters inside template bodies. The bare-structure row is a constructor-witness rule over inhabited fields, excluding properties; the schema-parameter row is backed by the §18.10.2 schema-context assumption. Spec 18 template type actuals now use the same table. Existing inactive sethood/existential/template seeds keep their rejection intent; positive source-derived coverage remains deferred on runner and payload gaps. No checker/core source semantics changed. |
| SSA-015, SSA-017 | `spec_gap` / `test_expectation_drift` / `source_drift` | Task 44 resolves omitted `reconsider` justification and ambiguous omitted `coherence with` target inference. Spec 04/08/15/Appendix A now permit omitted `reconsider` simple-justification syntax, while spec 08/22 accept it only when proof-free widening, inheritance, cluster closure, or already recorded local type facts discharge every target-type obligation; otherwise `type.narrowing_requires_proof` requests an explicit proof. Spec 19/22 now reject omitted `coherence with` when several visible earlier roots of the same symbol kind and arity are strictly sharpened by the redefinition signature, using `resolve.ambiguous_redefinition_target` rather than declaration/import order or return type. Inactive seeds `fail_types_reconsider_omitted_justification_001` and `fail_overload_redefine_ambiguous_target_001` pin the negative cases; the existing parser omitted-justification rejection is recorded as deferred parser task 47. No checker/core source semantics changed. |

No checker semantic `source_undocumented_behavior` is observed through task 47:
the implemented `typed_ast`, `binding_env`, type-expression normalization,
declaration/local-binding checking, term/formula inference, coercion/
initial-obligation checking, type-fact query, and registration database data
layer, pending registration validation/activation gating, existential gates, cluster
closure/loop/bound trace sources, reduction trace source, and overload
site/candidate collection, template expansion, viability filtering, and
specificity graph construction, root selection, refinement join validation,
inserted-view recording, failed-site preservation, task-45 overload
tie-break/redefinition-target boundary regressions, task-46 fatal
contradiction and reduction side-condition trace identity regressions,
task-47 base-shape inhabitation evidence and omitted-`reconsider` gate
regressions, and
resolved typed AST assembly are covered by their English/Japanese design
documents, the task-32
source/spec audit, bilingual sync audit, module-boundary audit, crate exit
report, and task-local Rust tests. The task 12 runner and tasks 13, 15, 21, 27,
32, 33, 34, 45, 46, 47, and closeout documentation are documented without adding checker
semantic source behavior. Future source behavior without spec/test support must
be classified before it is kept, and it cannot remain at crate completion.

No blocking contradiction prevents crate closeout. The crate exit report records
the remaining deferred items and quality review outcome.

## Ordered Task Decomposition

This section mirrors [todo.md](./todo.md). Each numbered task must be reviewed,
verified, and committed independently before the next task starts.

| Task | Purpose | Spec refs | Tests / affected files | Completion and deferral rule |
|---|---|---|---|---|
| 0 | Create this crate plan in English and Japanese. | autonomous crate protocol; checker TODO. | `00.crate_plan.md`; docs-only `git diff --check` and cached diff check. | Commit before task 1; no code. |
| 1 | Scaffold `mizar-checker` and lint-policy guard. | architecture 04; internal 07. | `Cargo.toml`, `crates/mizar-checker/**`; lint-policy test. | Workspace builds with `mizar-session` and `mizar-resolve`; no checker semantics. |
| 2 | Specify `typed_ast.md`. | architecture 04 Typed AST; architecture 01. | English/Japanese docs; planned tests for task 3. | Data-shape contract covers arenas, local context snapshots, type/fact/coercion tables, `InitialObligationId`, status semantics, partial typing, and the deferred arena decision. |
| 3 | Implement `typed_ast` data shapes. | `typed_ast.md`. | `src/typed_ast.rs`; id/context/table/status/rendering and boundary tests. | Deterministic ids, context snapshots, round-trips, status consumption, no proof-owned ids, no final overload/view fields, and rendering; resolve arena representation decision. |
| 4 | Specify `binding_env.md`. | architecture 04 Step 1; architecture 16. | English/Japanese docs; planned tests for task 5. | Layered contexts, binder identity, lookup, reserved variables, closure metadata, diagnostics, and external dependency gaps are specified without type inference. |
| 5 | Implement binding environment and context build. | `binding_env.md`; resolver `SymbolEnv` boundary. | `src/binding_env.rs`; lookup, reserve, scope, iteration, module-shell, diagnostics, and boundary tests. | Implemented as a binding-env data layer, explicit-payload lookup, resolver-resolution fallback, and deterministic shell. Missing AST-wide local extraction, use-site scope/ordinal, reserve payload, closure payload, and syntax-free `ResolvedAst` fixture remain MC-G011 external gaps rather than fabricated behavior. |
| 6 | Specify `type_checker.md`. | architecture 04 Steps 2-4; spec 03; spec 08; spec 13. | English/Japanese docs; planned tests for tasks 7-11. | Defines phase-6 boundary, normalized type model, declarations, inference, coercions, initial obligations, facts, partial recovery, deterministic expectations, and external/deferred gates; no code. |
| 7 | Implement type-expression normalization. | `type_checker.md` normalization. | `src/type_checker.rs`; canonicalization/idempotence tests. | Own a task-local `NormalizedTypeTable` in `TypeNormalizationOutput`, normalize checker-owned type-expression payloads, and degrade missing resolver/signature seams as MC-G014 external gaps instead of walking raw syntax. |
| 8 | Implement declaration and local-binding checking. | `type_checker.md` declarations. | Binding fixtures and diagnostic range tests. | Implemented as `DeclarationChecker` over checker-owned declaration/context payloads, `BindingEnv`, and task-7 normalized type expressions. Missing RHS/body/reserve/evidence payloads remain MC-G016 external gaps and are recorded as deferred diagnostics; no proof, VC, or task-10 obligation semantics are fabricated. |
| 9 | Implement term and formula type inference. | `type_checker.md` inference. | Per-kind inference tests; partial inference tests. | Implemented as `TermFormulaChecker` over checker-owned term/formula payloads, task-local candidate sets, expected-type constraints, and minimal inference facts. Missing resolver/source payloads remain MC-G017 external gaps; task-10 coercion/obligation emission and final overload root selection are not fabricated. |
| 10 | Implement coercion candidates and initial obligations. | `type_checker.md` coercions/obligations; spec 08. | Coercion and obligation tests, including sethood, non-emptiness, and invalid `qua` failures. | Implemented as `CoercionObligationChecker` over checker-owned payloads. Emits `InitialObligationId`s, preserves input fact ids, appends obligation-backed facts, and blocks/rejects missing support without assigning `VcId`s, discharging obligations, or fabricating views. |
| 11 | Implement type-fact recording and query APIs. | `type_checker.md` type facts. | Provenance, determinism, and dedup tests. | Implemented as deterministic `TypeFactQueryEngine` over existing fact tables and optional local contexts. Query order is canonical, assumed facts do not leak without context visibility, contradictions are diagnostic-only, and MC-G019 proof/statement/trace gaps remain external. |
| 12 | Add corpus runner at `type_elaboration`. | mizar-test staged model. | `mizar-test` runner, `.miz` fixtures, expectations, traceability. | Implemented as an active runner that executes frontend parsing and resolver symbol collection, then reports MC-G020 with a stable detail key for unsupported source-to-checker payload families. Adds fail coverage for the external gap; task 16 adds only builtin type-expression pass coverage and keeps broader task 7-11 pass/fail semantic `.miz` assertions deferred rather than fabricating payloads. |
| 13 | Specify `registration_resolution.md`. | architecture 04 registration databases; spec 17. | English/Japanese docs; planned tests for tasks 14 and 16-20. | Completed by task 13: the spec defines pending/activated databases, validation obligations, existential gating, cluster closure, reduction provenance, diagnostics, planned tests, and MC-G021 external/deferred payload gaps without code. |
| 14 | Implement registration index. | `registration_resolution.md`; resolver registration increment. | Pending/activated/index tests. | Completed by task 14: `RegistrationDatabase` preserves resolver registration origins, records external-gap pending entries, rejects malformed/invalid activation inputs, accepts only full caller-supplied activation payloads, sorts by checker-owned keys, renders deterministically, and does not parse opaque shells or fabricate semantic payloads. |
| 15 | Specify `cluster_trace.md`. | architecture 17. | English/Japanese docs; planned tests for tasks 16-18. | Completed by task 15: `cluster_trace.md` refines the canonical `ResolutionTrace` schema without forking it, defines checker-local cluster/reduction step ownership, antecedent facts, audit keys, deterministic traversal, replay bounds, failure classes, planned tests, and MC-G023 deferred/external/test gaps without code. |
| 16 | Implement cluster closure with trace recording. | `cluster_trace.md`; `registration_resolution.md`. | Closure, replay, and order tests. | Completed by task 16: `ClusterTraceBuilder` consumes explicit cluster rule/fact payloads plus activated registrations, records replayable cluster steps and derived closure facts with trace provenance, rejects inactive/non-cluster/mismatched payloads, and keeps reductions, loop/bound failure, contradiction failure, artifact emission, and `TypeFactTable` integration deferred. |
| 17 | Implement cluster loop detection and bounded saturation. | architecture 17. | Loop/limit diagnostics, contradiction, and config-visible bound tests. | Completed by task 17: cluster closure tracks fact ancestry/depth, diagnoses direct and indirect loops, enforces depth and generated-fact bounds with profile/cache-key visibility, reports explicit conflict-fingerprint contradictions as incomplete closure results, and avoids inserting rejected degraded facts. Source-derived `TypeFactTable` contradiction checks and artifact/cache integration remain deferred. |
| 18 | Implement reduction applications. | `registration_resolution.md`; architecture 17. | Redex path, guard evidence, replay tests. | Completed by task 18: `ReductionTraceBuilder` records replayable reduction steps over explicit payloads, preserves architecture-17 provenance fields, validates active reduction registrations, rule-view fingerprints, substitutions, guard evidence, and strategy-audit keys, treats `such` guards as applicability-only evidence, and keeps raw syntax matching, resolver-shell parsing, artifact/cache integration, and source-derived reduction extraction deferred. |
| 19 | Implement pending-registration validation and activation gating. | `registration_resolution.md`; architecture 04 Step 6. | Invalid-registration and unverified-activation tests. | Completed by task 19: `RegistrationValidationInput` validates explicit checker-ready pending payloads, emits checker-local `InitialObligationId`s, keeps validated records pending with `inference=false`, rejects recovered origins and malformed kind-specific payloads, enforces fixed spec-17.6.4 reduction size/variable rules, and rejects activation inputs whose verifier/artifact status is missing or rejected. Source extraction, accepted-status production/import, artifact reuse, and active `.miz` semantic fixtures remain deferred. |
| 20 | Enforce existential gating of attributed type use. | `registration_resolution.md`; spec 17. | Missing-existential fail fixtures. | Completed by task 20: `ExistentialGateOutput` evaluates explicit checker-owned gate payloads against activated existential registrations, binds candidates to accepted validation kind plus pattern/correctness/evidence/fingerprint records, requires visible consumable guard fact evidence, matches the full accepted attributed-type pattern, applies deterministic result precedence, and ensures only satisfied normal gates may seed verified facts. Source extraction, artifact reuse, accepted-status production, and active `.miz` gate fixtures remain deferred. |
| 21 | Specify `overload_resolution.md`. | architecture 05; spec 18; spec 19. | English/Japanese docs; planned tests for tasks 22-26. | Completed by task 21: `overload_resolution.md` defines checker-local phase-8 boundaries, site/candidate provenance, template expansion, viability over recorded facts, per-site specificity graphs, root selection, refinement joins, widening-only inserted `qua` views, failed-site preservation, diagnostics, determinism, planned task coverage, and MC-G027 test/deferred/external gaps without code. |
| 22 | Implement candidate site collection. | `overload_resolution.md` sites. | Site coverage, provenance, candidate-order tests. | Completed by task 22: `OverloadCollectionOutput::collect` consumes explicit checker-owned site/candidate payloads, assigns deterministic local ids, preserves provenance/source-`qua`/template/coherence metadata, diagnoses duplicate site keys and missing candidate links while retaining rejected input provenance, defers unsupported roles, and preserves already scope-filtered candidates without global scans. |
| 23 | Implement template expansion. | `overload_resolution.md` templates; parser template surface. | Expansion, constrained-template evidence, and exclusion-reason tests. | Completed by task 23: `TemplateExpansionOutput::expand` validates explicit template payload metadata, copies non-template candidates, instantiates successful templates into concrete `TemplateDerived` candidates, records substitutions and expansion rows, rejects/defer skipped templates with stable diagnostics, and covers explicit/omitted/source-`qua`/constraint/deferred/deterministic cases without fresh inference or cluster expansion. |
| 24 | Implement viability filtering. | `overload_resolution.md` viability. | Viability, consumable-evidence, and rejection-reason tests. | Completed by task 24: `CandidateViabilityOutput::filter` consumes explicit recorded-evidence payloads, emits only viable candidates, records all candidate decisions, preserves view plans, rejects non-consumable/missing/narrowing evidence, blocks ambiguous or externally deferred evidence, and does not derive facts, fire registrations, run inference, select roots, or insert views. |
| 25 | Implement specificity graph construction. | `overload_resolution.md` specificity. | Ordering, incomparability, deterministic rendering tests. | Completed by task 25: `SpecificityGraphOutput::build` consumes explicit comparison payloads, emits per-site graph/node/comparison/edge records, preserves incomparable rows without edges, diagnoses missing/duplicate/unknown/cross-site payloads, and does not derive facts, inspect return types for ordering, apply selection tie-breakers, join refinements, or insert views. |
| 26 | Implement root selection, refinement joins, and view insertion. | `overload_resolution.md` selection/views. | Selection, ambiguity, refinement-join, inserted-view, and failed-site tests. | Completed by task 26: `OverloadSelectionOutput::resolve` consumes explicit checker-owned selection payloads, selects unique maximal non-redefinition ordinary root candidates from specificity graphs, records no-match, ambiguity, missing/ambiguous ordinary-root candidate, incompatible refinement joins, and blocked sites as failed results, validates same-root redefinition payloads with accepted coherence, records only accepted widening/source-`qua` views, and does not derive facts, apply additional root-selection tie-breakers, use return types for ordering, or fabricate successful overload results. |
| 27 | Specify `resolved_typed_ast.md`. | architecture 01; architecture 05 Step 6. | English/Japanese docs; planned tests for task 28. | Completed by task 27: `resolved_typed_ast.md` defines the final source-shaped AST boundary, expression metadata, overload resolution projection, coercion insertion table, cluster fact references/provenance preservation, failure preservation, deterministic rendering, task-28 planned tests, and deferred source-extraction/artifact gaps without code. |
| 28 | Implement `ResolvedTypedAst` assembly. | `resolved_typed_ast.md`. | Assembly, `ExprId` metadata, rendering tests. | Completed by task 28: `ResolvedTypedAst::assemble` consumes explicit typed AST, cluster fact, overload collection/template/viability/specificity, and selection outputs; emits source-shaped resolved nodes, expression metadata, collection/expanded/viable candidate summaries, template expansion summaries, viability decisions, specificity graph summaries, overload records, inserted coercions, diagnostics, and deterministic debug rendering; preserves failed overload sites and cluster fact provenance; and does not elaborate to core logic, fabricate final definitions, inspect raw syntax, emit artifacts, allocate public diagnostic codes, or add active `.miz` fixtures. |
| 29 | Record deferred corpus obligations at `formula_statement` and `advanced_semantics`. | mizar-test staged model; architecture 20. | Deferred traceability rows for registration/overload/soundness obligations. | Completed by task 29 as a deferred corpus-record task: `spec_trace.toml` now records deferred formula/statement, cluster/reduction, overload/refinement, and review-audit negative obligations with concrete MC-G019/MC-G020/MC-G021/MC-G023/MC-G027 and runner blockers. No active `.miz` fixtures were added because doing so would require fabricating source-to-checker payloads or an unsupported runner. |
| 30 | Add determinism suite. | architecture 20. | Property/regression tests for types, facts, traces, candidates, diagnostics. | Completed by task 30: `determinism_suite` reruns identical checker-owned fixtures and equivalent order permutations for normalized type output, type-fact contradiction queries, cluster trace closure, overload collection/template/viability/specificity/selection output, and final `ResolvedTypedAst::assemble` projection. Active `.miz` corpus expansion and source-to-checker extraction remain deferred under the existing MC-G019/MC-G020/MC-G021/MC-G023/MC-G027/MC-G030 gaps. |
| 31 | Apply public-enum forward-compatibility policy. | all module specs; frontend task-25 procedure. | Lint guard updates and spec decisions. | Completed by task 31: all current checker-owned public enums are classified as downstream forward-compatible and guarded as `#[non_exhaustive]`; each owning EN/JA module spec contains a `Public Enum Policy` table plus no-exhaustive-exceptions statement, and `tests/lint_policy.rs` fails future public enum source/spec drift. |
| 32 | Audit source/spec correspondence. | all module specs and this TODO. | [source_spec_audit.md](./source_spec_audit.md), Japanese companion, and lint-policy audit guard. | Completed by task 32: every current checker `pub mod` export, top-level public item, and public `dense_id!` / `string_key!` newtype is inventoried; promised behavior is traced to source, Rust tests, or explicit MC-G gap/deferred rows; `tests/lint_policy.rs` guards audit drift for public surface and MC-G reconciliation; no source/API behavior is changed. |
| 33 | Audit bilingual documentation sync. | repository docs policy. | [bilingual_sync_audit.md](./bilingual_sync_audit.md), Japanese companion, and lint-policy sync guard. | Completed by task 33: every current English/Japanese checker design document filename pair is inventoried, companion links and comparison basis are recorded, sync debt is `none`, and `tests/lint_policy.rs` guards future pair inventory drift. |
| 34 | Run module-boundary refactor gate. | internal 07; all module specs. | [module_boundary_audit.md](./module_boundary_audit.md), Japanese companion, and lint-policy source-layout guard. | Completed by task 34: every current checker Rust source/test-support file is inventoried with line count, boundary label, owning specification, split decision, and hard-gate status; no behavior-neutral split is required, no hard-gate finding is hidden by a file move, and `tests/lint_policy.rs` guards future source-layout audit drift. |
| 45 | Align overload tie-break and redefinition-target implementation. | spec 19; architecture 05; tasks 37 and 44. | `src/overload_resolution.rs`; `overload_resolution.md`; source/spec and coverage audits. | Completed by task 45: explicit-payload Rust regressions cover equivalent template-derived root ambiguity, encoded non-template/template priority, strictly-more-specific template wins, unencoded ordinary/template-derived equivalence ties, and same-root redefinition metadata not breaking distinct-root ties. `overload_resolution.md` records that omitted `coherence with` target diagnostics are declaration-checking/source-extraction producer behavior; this data layer receives only already-bound redefinition payloads and preserves missing/deferred/rejected producer records. Active `.miz` overload/redefinition corpus remains deferred under MC-G027/MC-G030. |
| 46 | Align closure contradiction and reduction termination/determinism implementation. | spec 17; architecture 04/17; tasks 41 and 42. | `src/cluster_trace.rs`; `src/determinism_suite.rs`; `cluster_trace.md`; `registration_resolution.md`; source/spec and coverage audits. | Completed by task 46: explicit-payload Rust regressions assert closure-time contradiction diagnostics are fatal error results with incomplete closure status and no degraded fact export, and that reduction trace identity includes the discharged side-condition evidence set while the strategy-audit key excludes `such` applicability evidence. `cluster_trace.md` and `registration_resolution.md` cite the grammar-based termination premise and the corrected reduction determinism signature. Source-derived contradiction extraction, normalization-result dependence, artifact/cache replay, and active `.miz` cluster/reduction corpus remain deferred under MC-G020/MC-G021/MC-G023/MC-G030. |
| 47 | Align existential gate, activation contract, and omitted-reconsider implementation. | spec 08; spec 17; spec 18; architecture 04; tasks 40, 43, and 44. | `src/registration_resolution.rs`; `src/type_checker.rs`; `registration_resolution.md`; `type_checker.md`; source/spec and coverage audits. | Completed by task 47: explicit-payload Rust regressions cover built-in `object`/`set` base evidence, accepted mode tuple matching, attributed gates rejecting base evidence, zero-field and guarded structure constructor witnesses, schema type-parameter evidence, blocked guard visibility/consumability, and omitted `reconsider` narrowing over only consumable proof-free evidence markers. `registration_resolution.md` records the task-40 asynchronous activation contract as the target of the interim accepted-input policy, while source-derived base-shape extraction, positive accepted-local activation, source-derived omitted-`reconsider` parsing/extraction, artifacts, and active `.miz` fixtures remain deferred under MC-G018/MC-G020/MC-G021/MC-G025/MC-G026/MC-G030. |
| 48 | Add reserve source declaration producer seam. | Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; source/spec and module-boundary audits; mizar-test runner docs. | Completed by task 48: `type_checker.rs` exposes `SourceReserveDeclarationBridge`, `SourceReserveBindingInput`, and `SourceReserveDeclarationHandoff` so the active runner can pass syntax-free source-derived bare builtin reserve payloads into checker-owned `BindingEnv` and `DeclarationCheckingOutput` production. The runner still owns `.miz` AST extraction and keeps unsupported families on `type_elaboration.external_dependency.ast_payload_extraction`; no `.miz` expectations, CoreIr/ControlFlowIr/VC/proof payloads, or broader semantic pass coverage are promoted. Spec coverage audit impact: no chapter coverage status or follow-up owner changes beyond this crate-plan/TODO owner task, so `doc/design/spec_coverage_audit.md` is unchanged. |
| 50 | Add source-derived attributed reserve evidence-gap bridge. | spec 03; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 50: the syntax-free reserve seam accepts same-module source-derived attribute payloads on builtin heads, marks attributed declarations with `MissingEvidenceQuery`, and adds an active fail fixture that reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Imported attributes, successful attributed declarations, existential evidence, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to note the active attributed-reserve evidence-gap diagnostic while positive existential execution remains deferred. |
| 51 | Add source-derived local mode reserve expansion-gap bridge. | spec 03; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 51: the syntax-free reserve seam accepts same-module source-derived local mode heads with no attributes or type arguments, validates exact `SymbolKind::Mode` plus local `LocalSource` provenance, and adds an active fail fixture that reaches `type_elaboration.checker.checker.type.external.mode_expansion_payload`. Imported modes, mode arguments, attributed mode heads outside the later task-54 diagnostic slice, successful local-mode reserve declarations, mode-expansion payload extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to note the active local-mode expansion-gap diagnostic while positive accepted-mode execution remains deferred. |
| 52 | Add source-derived local structure reserve evidence-gap bridge. | spec 03; spec 05; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 52: the syntax-free reserve seam accepts same-module source-derived local structure heads with no attributes or type arguments, validates exact `SymbolKind::Structure` plus local `LocalSource` provenance, marks those declarations with `MissingEvidenceQuery`, and adds an active fail fixture that reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Imported structures, structure arguments, attributed structure heads outside the later task-53 diagnostic slice, successful local-structure reserve declarations, structure base-shape/constructor-witness extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to note the active local-structure evidence-gap diagnostic while positive structure inhabitation execution remains deferred. |
| 53 | Add source-derived attributed local structure reserve evidence-gap bridge. | spec 03; spec 05; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 53: the syntax-free reserve seam accepts same-module source-derived no-argument attribute payloads on same-module local structure heads, validates exact `SymbolKind::Structure` plus local `LocalSource` provenance, marks those declarations with `MissingEvidenceQuery`, and adds an active fail fixture that reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Imported attributes/structures, attribute arguments, attributed local mode heads outside the later task-54 diagnostic slice, structure arguments, successful attributed-structure reserve declarations, full attributed-type existential evidence, structure base-shape/constructor-witness extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to note the active attributed local structure evidence-gap diagnostic while positive attributed-type execution remains deferred. |
| 54 | Add source-derived attributed local mode reserve expansion-gap bridge. | spec 03; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 54: the syntax-free reserve seam accepts same-module source-derived no-argument attribute payloads on same-module local mode heads, validates exact `SymbolKind::Mode` plus local `LocalSource` provenance, preserves the attributes, and adds an active fail fixture that reaches `type_elaboration.checker.checker.type.external.mode_expansion_payload` without adding an evidence-query diagnostic. Imported attributes/modes, attribute arguments, mode arguments, successful attributed-mode reserve declarations, real mode expansion, full attributed-type existential evidence, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to note the active attributed local mode expansion-gap diagnostic while positive accepted-mode/existential execution remains deferred. |
| 55 | Add source-derived bare local mode expansion bridge. | spec 03; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 55: the active runner extracts a real AST-derived `ModeExpansion` only for same-module no-argument local mode definitions with a preceding unrecovered source definition, no definition-local context, and a bare builtin `set` / `object` RHS, then passes the expansion through the checker-owned reserve seam so a bare local-mode reserve declaration reaches the existing `TypedAst` / `ResolvedTypedAst` / summary / binder-only context readiness checks as an active pass case. Attributed or mixed attributed/bare local-mode uses, attributed RHSs, imported/argument-bearing/parameterized/contextual modes, existential evidence, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to narrow the real mode-expansion gap to the task-55 bare builtin RHS slice. |
| 56 | Add source-derived local mode expansion chain bridge. | spec 03; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 56: the active runner may extract a real AST-derived one-edge local-mode expansion chain when a same-module no-argument local mode reserve head expands to a preceding same-module no-argument local mode whose own preceding source definition has an accepted task-55 bare builtin `set` / `object` RHS expansion. It inserts both expansion payloads before the checker-owned reserve seam, adds active pass fixtures for `B -> A -> set` and `B -> A -> object`, and adds an active fail fixture proving that an attributed dependency withholds the whole chain and reaches the missing mode-expansion diagnostic. Forward references, ambiguous/local-imported dependencies, cycles, partial `B -> A` payloads without accepted `A -> set/object`, attributed uses, attributed RHSs, arguments, parameterized/contextual definitions, existential evidence, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-56 one-edge chain slice while keeping broader mode expansion deferred. |
| 57 | Add source-derived local mode structure-RHS evidence-gap bridge. | spec 03; spec 05; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 57: the active runner may extract a real AST-derived same-module no-argument local-mode expansion whose RHS is a preceding same-module no-argument local structure head, and the checker-owned reserve seam consumes that real expansion rather than reporting `checker.type.external.mode_expansion_payload`. Because source-derived structure base-shape/constructor-witness evidence is still absent, the expanded reserve declaration is marked with `MissingEvidenceQuery` and the active fail fixture reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Imported structures, structure arguments, attributed structure RHSs, positive local-structure acceptance, structure base-shape/constructor-witness extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-57 structure-RHS diagnostic slice while keeping positive structure evidence deferred. |
| 58 | Add source-derived local mode attributed-builtin RHS evidence-gap bridge. | spec 03; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 58: the active runner may extract a real AST-derived same-module no-argument local-mode expansion whose RHS is a preceding attributed builtin `set` / `object` head, and the checker-owned reserve seam consumes that real expansion rather than reporting `checker.type.external.mode_expansion_payload`. Because source-derived attributed-type existential evidence is still absent, the expanded reserve declaration is marked with `MissingEvidenceQuery` and the active fail fixture reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Attributed reserve heads, mixed attributed/bare reserve uses, imported attributes or modes, attribute arguments, mode arguments, attributed-RHS chains, positive attributed-type acceptance, existential evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to move the attributed-RHS fixture from missing-expansion diagnostic coverage to the attributed-type evidence diagnostic slice. |
| 59 | Add source-derived attributed local mode reserve evidence-gap bridge. | spec 03; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 59: the active runner may extract a real AST-derived direct bare-builtin RHS local-mode expansion for a same-module attributed local-mode reserve head when the same mode is not mixed with a bare reserve use, and the checker-owned reserve seam consumes that real expansion rather than reporting `checker.type.external.mode_expansion_payload`. Because source-derived attributed-type existential evidence is still absent, the expanded attributed declaration is marked with `MissingEvidenceQuery` and the active fail fixture reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Mixed attributed/bare reserve uses, imported attributes or modes, attribute arguments, mode arguments, attributed dependencies or chains, structure RHSs, attributed RHSs, positive attributed-type acceptance, existential evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to move the single attributed local-mode reserve fixture from missing-expansion diagnostic coverage to the attributed-type evidence diagnostic slice while keeping mixed-use coverage on the missing-expansion requirement. |
| 60 | Add source-derived attributed local mode structure-RHS evidence-gap bridge. | spec 03; spec 05; spec 06; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 60: the active runner may extract a real AST-derived direct local-structure RHS local-mode expansion for a same-module attributed local-mode reserve head when the mode definition is unique, unrecovered, no-argument, preceding, and free of definition-local context, the structure definition is unique, unrecovered, same-module, and precedes the mode definition, and the same mode is not mixed with a bare reserve use. The checker-owned reserve seam consumes that real expansion rather than reporting `checker.type.external.mode_expansion_payload`; because source-derived base-shape/constructor-witness and full attributed-type existential evidence are still absent, the expanded attributed declaration reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Mixed attributed/bare reserve uses, dependencies/chains, imported attributes/modes/structures, attribute arguments, mode/structure arguments, attributed structure RHSs, attributed-builtin RHSs, positive attributed/structure acceptance, evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-60 attributed local-mode structure-RHS diagnostic slice while keeping positive structure/attributed evidence deferred. |
| 61 | Add source-derived attributed local mode attributed-builtin RHS evidence-gap bridge. | spec 03; spec 06; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `src/type_checker.rs`; `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 61: the active runner may extract a real AST-derived direct attributed-builtin RHS local-mode expansion for a same-module attributed local-mode reserve head when the mode definition is unique, unrecovered, no-argument, preceding, and free of definition-local context, and the same mode is not mixed with a bare reserve use. The checker-owned reserve seam consumes that real expansion rather than reporting `checker.type.external.mode_expansion_payload`; because source-derived full attributed-type existential evidence is still absent, the expanded attributed declaration reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Mixed attributed/bare reserve uses, dependencies/chains ending in attributed RHSs, imported attributes/modes, attribute arguments, mode arguments, structure RHSs, attributed structure RHSs, positive attributed acceptance, evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-61 attributed local-mode attributed-builtin-RHS diagnostic slice while keeping positive attributed evidence deferred. |
| 62 | Add source-derived local mode structure-RHS chain evidence-gap bridge. | spec 03; spec 05; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 62: the active runner may extract a real AST-derived one-edge bare local-mode chain ending in a same-module local structure RHS when the structure definition, terminal mode definition, chain mode definition, and reserve use are unique, unrecovered, same-module, no-argument where applicable, definition-local-context-free for the mode definitions, and ordered `structure -> terminal mode -> chain mode -> reserve`. The checker-owned reserve seam consumes both real expansions rather than reporting `checker.type.external.mode_expansion_payload`; because source-derived structure base-shape/constructor-witness evidence is still absent, the expanded reserve declaration reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Attributed roots, mixed attributed/bare reserve uses, attributed or deeper chains, imported/ambiguous symbols, arguments, contextual/parameterized definitions, positive structure acceptance, evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-62 local-mode structure-RHS chain diagnostic slice while keeping positive structure evidence deferred. |
| 63 | Add source-derived local mode attributed-builtin-RHS chain evidence-gap bridge. | spec 03; spec 06; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 63: the active runner may extract a real AST-derived one-edge bare local-mode chain ending in an attributed builtin RHS when the terminal mode definition, chain mode definition, and reserve use are unique, unrecovered, same-module, no-argument where applicable, definition-local-context-free for the mode definitions, and ordered `terminal mode -> chain mode -> reserve`; RHS attributes must resolve to argument-free same-module attribute symbols. The checker-owned reserve seam consumes both real expansions rather than reporting `checker.type.external.mode_expansion_payload`; because source-derived attributed-type existential evidence is still absent, the expanded reserve declaration reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Attributed roots, mixed attributed/bare reserve uses, attributed or deeper chains, imported/ambiguous symbols, attribute or mode arguments, contextual/parameterized definitions, positive attributed-type acceptance, evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-63 local-mode attributed-builtin-RHS chain diagnostic slice while keeping positive attributed evidence deferred. |
| 64 | Add source-derived attributed local mode bare-builtin chain evidence-gap bridge. | spec 03; spec 06; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 64: the active runner may extract a real AST-derived one-edge chain for an attributed same-module local-mode reserve head when `A` is not mixed with a bare reserve use, `B` is not used as an attributed reserve head, both mode definitions are unique, unrecovered, same-module, no-argument, definition-local-context-free, and ordered `B -> A -> reserve`, and `B` has a direct bare builtin RHS. The checker-owned reserve seam consumes both real expansions and reserve-head attributes rather than reporting `checker.type.external.mode_expansion_payload`; because source-derived attributed-type existential evidence is still absent, the expanded declaration reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Structure-RHS or attributed-builtin-RHS terminal dependencies, deeper chains, mixed uses, imported/ambiguous symbols, arguments, contextual/parameterized definitions, positive attributed-type acceptance, evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-64 attributed local-mode bare-builtin chain diagnostic slice while keeping positive attributed evidence deferred. |
| 65 | Add source-derived attributed local mode structure-RHS chain evidence-gap bridge. | spec 03; spec 05; spec 06; spec 07; spec 17; Step 5 source-derived semantic bridge; `type_checker.md`; mizar-test task 10. | `crates/mizar-test/src/runner.rs`; `type_checker.md`; mizar-test runner docs; traceability metadata. | Completed by task 65: the active runner may extract a real AST-derived one-edge chain for an attributed same-module local-mode reserve head when `A` is not mixed with a bare reserve use, `B` is not used as an attributed reserve head, `B is LocalStruct` and `A is B` are unique, unrecovered, same-module, no-argument, definition-local-context-free, the structure definition is unique, unrecovered, same-module, and ordered `LocalStruct -> B -> A -> reserve`. The checker-owned reserve seam consumes both real expansions and reserve-head attributes rather than reporting `checker.type.external.mode_expansion_payload`; because source-derived structure base-shape/constructor-witness and full attributed-type existential evidence are still absent, the expanded declaration reaches `type_elaboration.checker.checker.declaration.deferred.evidence_query`. Attributed-builtin terminal dependencies, deeper chains, mixed uses, attributed dependencies, imported/ambiguous symbols, arguments, contextual/parameterized definitions, positive structure/attributed-type acceptance, evidence extraction, CoreIr/ControlFlowIr/VC/proof payloads, and broader semantic pass coverage remain deferred. Spec coverage audit impact: update `doc/design/spec_coverage_audit.md` to add the task-65 attributed local-mode structure-RHS chain diagnostic slice while keeping positive structure/attributed evidence deferred. |

Task-specific forbidden behavior:

| Task | Forbidden behavior |
|---|---|
| 0 | Editing code, creating semantic stubs, or starting task 1 before the plan commit exists. |
| 1 | Adding checker semantics, fake typed outputs, or non-minimal APIs beyond the scaffold and guard. |
| 2 | Choosing implementation behavior not supported by architecture 04/01, or assigning `VcId`s. |
| 3 | Extending language semantics beyond `typed_ast.md`, or making arena ids proof/artifact identities. |
| 4 | Specifying resolver-owned lookup, proof obligations, or type inference behavior. |
| 5 | Re-implementing resolver symbol collection or inventing missing binding payloads. |
| 6 | Changing `doc/spec` semantics implicitly, or mixing implementation into a spec-only task. |
| 7 | Treating source spelling order, hash-map order, or current implementation quirks as normalization rules. |
| 8 | Accepting invalid bindings as verified facts or generating proof-search behavior. |
| 9 | Selecting ordinary overload roots, deriving new registration facts, or fabricating successful types after hard errors. |
| 10 | Assigning `VcId`s, discharging obligations, or treating narrowing as proof-free success. |
| 11 | Returning nondeterministic fact query order or duplicating semantically identical facts. |
| 12 | Rebaselining existing expectations to current checker output or activating cases without traceability. |
| 13 | Treating pending registrations as active or forking the architecture 04 registration boundary. |
| 14 | Inventing resolver registration payloads, artifact schemas, or activation status. |
| 15 | Forking the canonical architecture 17 trace schema or omitting replay fields. |
| 16 | Adding hidden cluster facts or relying on traversal accidents. |
| 17 | Silently truncating saturation, diverging, or making bounds invisible to configuration/cache keys. |
| 18 | Rewriting parser syntax, omitting guard evidence, or applying reductions without provenance. |
| 19 | Treating generated obligations as accepted proofs, activating unverified registrations, or bypassing the interim gating policy. |
| 20 | Exporting unverified inhabitants or using existential gaps as warnings only. |
| 21 | Using return types for ordinary root selection or making template behavior implementation-only. |
| 22 | Widening candidate sets by global `SymbolEnv` scans beyond resolver-filtered visibility. |
| 23 | Fabricating unsupported scheme/template source roles or using cluster expansion during template inference. |
| 24 | Deriving new facts, firing registrations, or performing new type inference during viability checks. |
| 25 | Building a global specificity DAG or relying on nondeterministic tie-breaking. |
| 26 | Using return-type tie-breakers, inserting narrowing views, or turning failed sites into valid output. |
| 27 | Adding implementation or omitting failed-site and metadata contracts from the spec. |
| 28 | Elaborating to core logic, dropping failed overload sites, or fabricating final definitions. |
| 29 | Adding advanced fixtures whose lower-stage prerequisites are not executable, unless explicitly deferred. |
| 30 | Making determinism tests depend on platform order, worker timing, or transient paths. |
| 31 | Leaving new public enums unclassified or undocumented in their owning specs. |
| 32 | Closing audit gaps without implementation/test evidence or explicit deferral. |
| 33 | Updating only one language companion without a documented sync reason. |
| 34 | Mixing behavior cleanup, API exposure, diagnostic changes, or schema changes into file moves. |
| 45 | Applying unencoded root-selection tie-breakers, using result types or redefinition presence to select roots, representing rejected omitted-target declarations as active candidates, or activating source-derived overload fixtures without MC-G027/MC-G030 support. |
| 46 | Claiming source-derived cluster/reduction execution from explicit trace payload tests, treating saturation bounds as successful truncated semantics, putting `such` guards into reduction specificity/strategy-audit keys, or activating cluster/reduction `.miz` fixtures without MC-G020/MC-G021/MC-G023/MC-G030 support. |
| 47 | Inferring base-shape inhabitation from type spelling or resolver opaque shells, using base evidence for attributed gates, accepting omitted `reconsider` by proof search or implicit `by`, creating obligations for omitted proof-free failure sites, or activating source-derived existential/reconsider fixtures without MC-G018/MC-G020/MC-G021/MC-G025/MC-G026/MC-G030 support. |
| 48 | Importing `mizar-syntax` into `mizar-checker`, scanning raw syntax inside checker, accepting non-builtin reserve declarations through the reserve seam, fabricating type/term/formula/proof payloads, changing existing `.miz` expectations, or treating summary/context readiness as CoreIr/ControlFlowIr/VC/proof execution. |
| 50 | Fabricating imported attribute symbols, accepting attributed reserve declarations without real existential/evidence-query inputs, treating the checker evidence-query diagnostic as successful type elaboration, or promoting CoreIr/ControlFlowIr/VC/proof payloads from reserve-only declarations. |
| 51 | Fabricating imported mode symbols or mode-expansion payloads, accepting structures or argument-bearing mode heads through the local-mode bridge, accepting attributed mode heads outside the later task-54 diagnostic slice, treating the missing mode-expansion diagnostic as successful type elaboration, or promoting CoreIr/ControlFlowIr/VC/proof payloads from reserve-only local-mode declarations. |
| 52 | Fabricating imported structure symbols or base-shape/constructor-witness evidence, accepting argument-bearing structure heads or attributed structure heads outside the task-53 diagnostic slice through the local-structure bridge, inferring structure inhabitation from the symbol alone, treating the missing evidence-query diagnostic as successful type elaboration, or promoting CoreIr/ControlFlowIr/VC/proof payloads from reserve-only local-structure declarations. |
| 53 | Fabricating imported attribute/structure symbols, accepting attributed local mode heads outside the later task-54 diagnostic slice, treating bare structure base-shape evidence as sufficient for full attributed-type acceptance, accepting argument-bearing attributed structure heads, treating the missing evidence-query diagnostic as successful type elaboration, or promoting CoreIr/ControlFlowIr/VC/proof payloads from reserve-only attributed local-structure declarations. |
| 54 | Fabricating imported attribute/mode symbols, fabricating mode-expansion or existential evidence, attaching an evidence-query diagnostic before a real mode expansion exists, accepting argument-bearing attributed mode heads, treating the missing mode-expansion diagnostic as successful type elaboration, or promoting CoreIr/ControlFlowIr/VC/proof payloads from reserve-only attributed local-mode declarations. |
| 55 | Fabricating mode-expansion payloads instead of extracting them from the same `SurfaceAst`, expanding attributed/mixed/imported/argument-bearing/parameterized/contextual mode uses, accepting attributed mode RHSs without existential evidence, accepting forward-reference or definition-local-context mode definitions, treating task-55 readiness as CoreIr/ControlFlowIr/VC/proof execution, or weakening task-54 attributed local-mode fail-closed behavior. |
| 56 | Fabricating chained mode-expansion payloads, inserting partial chain payloads when any dependency lacks an accepted source-derived expansion, accepting attributed dependency uses or attributed RHSs, accepting forward-reference/ambiguous/imported/cyclic/argument-bearing/parameterized/contextual chain definitions, treating the chain readiness checks as CoreIr/ControlFlowIr/VC/proof execution, or weakening task-54/task-55 fail-closed behavior. |
| 57 | Treating a structure RHS expansion as positive structure acceptance without real base-shape/constructor-witness evidence, reporting the old missing mode-expansion diagnostic after a real structure-RHS expansion was extracted, accepting imported/argument-bearing/attributed structure RHSs, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 58 | Treating an attributed-builtin RHS expansion as positive attributed-type acceptance without real existential evidence, reporting the old missing mode-expansion diagnostic after a real attributed RHS expansion was extracted, accepting attributed reserve heads or attributed-RHS chains through this slice, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 59 | Treating an attributed local-mode reserve expansion as positive attributed-type acceptance without real existential evidence, accepting mixed bare/attributed local-mode uses or attributed dependencies/chains through this slice, reporting the old missing mode-expansion diagnostic after a real direct bare-builtin expansion was extracted for the attributed reserve head, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 60 | Treating an attributed local-mode structure-RHS expansion as positive structure or attributed-type acceptance without real base-shape/constructor-witness and existential evidence, accepting mixed bare/attributed uses, dependencies/chains, imported/argument-bearing symbols, or attributed structure RHSs through this slice, reporting the old missing mode-expansion diagnostic after a real direct structure-RHS expansion was extracted for the attributed reserve head, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 61 | Treating an attributed local-mode attributed-builtin RHS expansion as positive attributed-type acceptance without real existential evidence, accepting mixed bare/attributed uses, dependencies/chains, imported/argument-bearing symbols, structure RHSs, or attributed structure RHSs through this slice, reporting the old missing mode-expansion diagnostic after a real direct attributed-builtin RHS expansion was extracted for the attributed reserve head, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 62 | Fabricating any link in the `A -> B -> LocalStruct` expansion chain instead of extracting both mode definitions from the same `SurfaceAst`, accepting attributed roots, attributed/deeper chains, imported/ambiguous/argument-bearing/contextual/parameterized definitions, treating the structure-RHS chain as positive structure acceptance without real base-shape/constructor-witness evidence, reporting the old missing mode-expansion diagnostic after both real expansions were extracted, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 63 | Fabricating any link in the `A -> B -> marked set` expansion chain instead of extracting both mode definitions and same-module argument-free RHS attributes from the same `SurfaceAst`, accepting attributed roots, attributed/deeper chains, imported/ambiguous/argument-bearing/contextual/parameterized definitions, treating the attributed-builtin-RHS chain as positive attributed-type acceptance without real existential evidence, reporting the old missing mode-expansion diagnostic after both real expansions were extracted, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 64 | Fabricating any link in the `marked A -> A -> B -> set/object` expansion chain instead of extracting both mode definitions and the reserve-head attribute from the same `SurfaceAst`, treating the attributed-root chain as positive attributed-type acceptance without real existential evidence, accepting structure-RHS or attributed-builtin-RHS terminal dependencies, deeper chains, mixed bare/attributed roots, attributed dependencies, imported/ambiguous/argument-bearing/contextual/parameterized definitions, reporting the old missing mode-expansion diagnostic after both real expansions were extracted, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |
| 65 | Fabricating any link in the `marked A -> A -> B -> LocalStruct` expansion chain instead of extracting both mode definitions, the structure definition, and the reserve-head attribute from the same `SurfaceAst`, treating the attributed-root structure chain as positive structure or attributed-type acceptance without real base-shape/constructor-witness and existential evidence, accepting attributed-builtin terminal dependencies, deeper chains, mixed bare/attributed roots, attributed dependencies, imported/ambiguous/argument-bearing/contextual/parameterized definitions, reporting the old missing mode-expansion diagnostic after both real expansions were extracted, or promoting this diagnostic-only bridge to CoreIr/ControlFlowIr/VC/proof payloads. |

## Crate Completion And Exit Criteria

Hard gates:

- every non-deferred task in [todo.md](./todo.md) is committed independently,
  with one task per commit;
- no blocking/high specification inconsistency remains;
- no implemented source behavior remains that is absent from `doc/spec/en` and
  tests; behavior that cannot be specified/tested yet must be excluded from the
  milestone scope or left unimplemented as a deferred design item;
- milestone-owned specification items have `.miz` coverage, task-local Rust
  tests, or explicit deferred reasons;
- existing `.miz` tests and expectations were not changed merely to match
  implementation behavior;
- design docs, English/Japanese companions, and source remain synchronized;
- checker crate boundaries are not violated;
- required verification commands pass or have explicit unrun justification;
- read-only crate quality review assigns at least 90/100 after hard gates pass.

Verification commands:

```text
cargo fmt --check
cargo clippy -p mizar-checker --all-targets -- -D warnings
cargo test -p mizar-checker
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

Additional commands are required for resolver boundary, corpus, or shared
harness tasks as noted in [todo.md](./todo.md).

Crate exit report:

- added [crate_exit_report.md](./crate_exit_report.md) and the Japanese
  companion at crate closeout;
- includes task commits, review results, verification, deferred items, external
  dependency gaps, quality score, and next-crate handoff.
