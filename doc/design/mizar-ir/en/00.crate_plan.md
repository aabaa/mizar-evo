# Crate Plan: mizar-ir

> Canonical language: English. Japanese companion:
> [../ja/00.crate_plan.md](../ja/00.crate_plan.md).

## Responsibility

Owned behavior:

- compiler-internal immutable IR output storage and typed
  `PhaseOutputRef<T>` handles;
- snapshot-scoped IR identity tables and parent/derived phase-output lineage;
- phase-output publication that seals complete outputs and hides unsealed,
  partial, or obsolete outputs from current publication;
- content-addressed internal blobs and lifetime collection for retained
  outputs;
- an IR cache adapter boundary that consumes validated `mizar-cache` records
  and rehydrates handles only after fail-closed validation;
- artifact projection from sealed internal outputs into
  `mizar-artifact` stable external schemas.

Out of scope:

- proof acceptance, trusted status, verifier-policy selection, deterministic
  proof winner selection, kernel acceptance, or `used_axioms` trust;
- `mizar-cache` `CacheKey`, dependency fingerprint, dependency slice, or
  proof-reuse validation ownership;
- raw `SurfaceAst`, `TypedAst`, `CoreIr`, `ControlFlowIr`, `VcIr`,
  `AtpProblem`, kernel-internal state, or storage references in published
  artifacts;
- `mizar-driver` build sessions, `mizar-diagnostics` integration, real
  producer projection tokens, and downstream artifact publication tokens until
  those owning crates expose real seams.

## Specification Items

| Spec ref | Requirement | Status |
|---|---|---|
| [internal 06](../../internal/en/06.ir_storage_and_snapshot_handles.md) | IR storage allocates immutable slots, typed handles, blobs, retention, publisher, cache adapter, and projection boundary. | Covered by tasks 1-13 and audited by tasks 16-19. The older API sketch is narrowed by IR-G-009 where it conflicts with current crate ownership. |
| [internal 07](../../internal/en/07.crate_module_layout.md) | `mizar-ir` owns internal IR storage, runtime kind tags, identity tables, and sealed output blobs. | Covered by task 1 scaffold and module-boundary gate task 19. |
| [architecture 01](../../architecture/en/01.ir_layers.md) | IRs are immutable snapshots; `VerifiedArtifact` is a stable projection, not a raw IR dump. | Covered by storage, publisher, and projection tasks. |
| [architecture 18](../../architecture/en/18.dependency_fingerprint.md) | Missing dependency data is not "no dependency"; cache reuse requires complete dependency information. | Consumed by task 9/10 cache adapter without reimplementing fingerprint ownership. |
| [architecture 20](../../architecture/en/20.test_strategy.md) | Deterministic ids/hashes and lifetime properties require regression/property-style coverage. | Covered by task 14. |
| [architecture 22](../../architecture/en/22.incremental_verification_contract.md) | Cache misses are performance-only; incomplete/unknown/uncacheable/incompatible records fail closed before handle reconstruction; obsolete snapshot results cannot publish as current. | Covered by tasks 8, 10, 13, and 18. |
| [mizar-cache exit report](../../mizar-cache/en/crate_exit_report.md) | `mizar-cache` owns cache keys, dependency fingerprints, cache records, and proof-reuse validation. | Consumed as boundary input; no ownership moved into `mizar-ir`. |
| [mizar-artifact crate plan](../../mizar-artifact/en/00.crate_plan.md) | `mizar-artifact` owns published schemas and manifest publication. | Consumed by projection task 12; no manifest token is fabricated. |

## Relevant Tests

| Test path | Intent | Spec refs |
|---|---|---|
| `crates/mizar-ir/tests/lint_policy.rs` | Guard workspace membership, lint inheritance, dependency boundary, public enum policy, and raw/proof-authority leakage markers. | task 1, internal 07. |
| `crates/mizar-ir/src/identity.rs` unit tests | Deterministic snapshot-scoped ids, incompatible-snapshot reuse rejection, and lineage round trips. | `identity.md`, internal 06. |
| `crates/mizar-ir/src/storage.rs` unit tests | Slot allocation, sealing, typed handles, blob spill, retain/release, collect, and stale handle lifetime. | `storage.md`, internal 06. |
| `crates/mizar-ir/src/publisher.rs` unit tests | Snapshot/work-unit validation, obsolete publication rejection, content hash determinism, and side-table access. | `publisher.md`, architecture 22. |
| `crates/mizar-ir/src/cache_adapter.rs` unit tests | Cache record conversion, fail-closed miss behavior, and validation-before-rehydration. | `cache_adapter.md`, architecture 18/22. |
| `crates/mizar-ir/src/projection.rs` unit tests | Stable `VerifiedArtifact` draft construction and raw internal IR leakage rejection. | `projection.md`, architecture 01. |
| `crates/mizar-ir/tests/determinism_lifetime.rs` | Property-style deterministic identity/hash and retain/collect lifetime scenarios. | architecture 20. |

Planned test-first additions:

- No `.miz` tests are required for this crate milestone because the requested
  work creates internal storage/adapters and does not change language behavior.
  Rust unit/integration tests cover the crate-owned contracts.

## Design And Source Inventory

Design files:

- `doc/design/mizar-ir/en/todo.md`
- `doc/design/mizar-ir/ja/todo.md`
- this crate plan and its Japanese companion
- task-scoped module specs and audits under `doc/design/mizar-ir/{en,ja}/`,
  including `source_spec_correspondence.md` and
  `bilingual_documentation_sync.md`, and
  `architecture_22_follow_up_audit.md`, and
  `module_boundary_refactor_gate.md`

Source files:

- task 1 adds `crates/mizar-ir/Cargo.toml`
- task 1 adds `crates/mizar-ir/src/lib.rs`
- task 1 adds `crates/mizar-ir/tests/lint_policy.rs`
- tasks 3/5/6/8/10/12/13/14 add or extend `identity.rs`,
  `storage.rs`, `publisher.rs`, `cache_adapter.rs`, `projection.rs`, and
  crate-level tests

Observed behavior:

- Before task 1, `mizar-ir` is not a workspace member and no source module
  exists.
- `mizar-session` already owns `BuildSnapshotId`, source ids, source hashes,
  snapshot registry, and retention reasons used by IR output references.
- `mizar-cache` already owns `CacheKey`, dependency footprints, cache-store
  fail-closed lookup, and proof-reuse validation metadata.
- `mizar-artifact` already owns stable external artifact schemas, including
  `VerifiedArtifact`.
- `mizar-driver` and `mizar-diagnostics` are absent from this checkout.

## Known Gaps And Drift

| ID | Class | Evidence | Action |
|---|---|---|---|
| IR-G-001 | `source_drift` | `doc/design/mizar-ir/en/todo.md` requires a workspace crate, but `crates/mizar-ir` is absent. | Add the crate in task 1. |
| IR-G-002 | `design_drift` | Protocol requires a crate plan before implementation; no `00.crate_plan.md` exists. | Add this plan and the Japanese companion before implementation. |
| IR-G-003 | `test_gap` | No mizar-ir tests exist because the crate is absent. | Add task-local Rust tests with each implementation slice. |
| IR-G-004 | `design_drift` | Real `mizar-driver` build sessions, phase registry, and driver-owned cache lookup are absent although TODO/prerequisite text mentions downstream pipeline wiring. | Record as risk tag `external_dependency_gap`; do not add placeholder driver APIs or make `mizar-ir` depend on `mizar-driver`. |
| IR-G-005 | `design_drift` | `mizar-diagnostics` is absent, so real diagnostic registry/renderer side tables cannot be integrated although internal 06 describes diagnostic side-table attachment. | Record as risk tag `external_dependency_gap`; store stable diagnostic side-table references as crate-owned values and defer real integration. |
| IR-G-006 | `design_drift` | Real resolver/checker/core/VC/ATP/kernel/proof producer projection payloads and publication tokens are not exposed for phase 15. | Record as risk tag `external_dependency_gap`; implement a stable projection boundary over validated projection input and do not fabricate producer tokens or proof authority. |
| IR-G-007 | `test_gap` | Full clean/incremental/parallel driver equivalence requires downstream orchestration crates. | Record as risk tag `external_dependency_gap`; cover crate-local deterministic and lifetime properties and defer system equivalence. |
| IR-G-008 | `boundary_violation` | Reimplementing cache keys, dependency fingerprints, proof-reuse validation, or proof trusted status in `mizar-ir` would violate `mizar-cache`/proof/kernel ownership. | Consume `mizar-cache` outcomes and stable artifact schemas only; enforce this in specs, lint guard, and tests. |
| IR-G-009 | `design_drift` | The internal 06 API sketch predates the completed `mizar-session` and `mizar-cache` ownership reports: it lists snapshot identity assignment details and an `IrCacheAdapter::cache_key` method that can be read as owning `BuildSnapshotId`, `SourceId`, or `CacheKey` construction. Current ownership keeps `BuildSnapshotId`/source ids in `mizar-session` and `CacheKey`/dependency fingerprints/proof-reuse validation in `mizar-cache`. | Resolve in `mizar-ir` module specs and source by consuming `mizar-session` ids and `mizar-cache` validated lookup/record data only. Do not implement cache-key or dependency-fingerprint builders in `mizar-ir`. |

No blocking `spec_gap` currently prevents implementation. IR-G-009 records the
known design drift in an implementation-facing API sketch; the task stream must
repair the derived `mizar-ir` specs/source toward the current crate ownership
reports and architecture boundary. The milestone is internal crate behavior;
language specification and `.miz` expectation files must not be changed.

## Task Decomposition

### Task 1: Crate scaffold and lint-policy guard

Purpose: add the workspace member, manifest, crate boundary statement, and lint
guard.

Spec refs: internal 06 and internal 07.

Tests: `crates/mizar-ir/tests/lint_policy.rs`, `cargo test -p mizar-ir`.

Affected design files: TODO files only for status update if verification
passes.

Affected source files: workspace `Cargo.toml`, `crates/mizar-ir/Cargo.toml`,
`src/lib.rs`, and `tests/lint_policy.rs`.

Completion condition: workspace builds with the new crate; lint guard passes;
task-1 dependency sections contain only `mizar-session`; no `mizar-cache`,
`mizar-artifact`, `mizar-build`, `mizar-driver`, or `mizar-diagnostics`
dependency exists yet.

Forbidden behavior: storage implementation, cache ownership, proof authority,
driver stubs, diagnostics stubs, or artifact publication tokens.

### Task 2: Spec `identity.md`

Purpose: specify snapshot-scoped IR identities and lineage.

Spec refs: internal 06 and architecture 01.

Tests: documentation review only.

Affected design files: `identity.md` in both language directories.

Affected source files: none.

Completion condition: EN/JA specs are synchronized and review has no
blocking/high findings.

Forbidden behavior: source implementation.

### Task 3: Snapshot handle registry

Purpose: implement deterministic IR-local id assignment and lineage tracking
without reowning `BuildSnapshotId`.

Spec refs: `identity.md`.

Tests: determinism, incompatible reuse rejection, lineage round trip.

Affected source files: `src/identity.rs`, exports, and tests.

Completion condition: source/tests/spec agree and crate verification passes.

Forbidden behavior: cross-snapshot reuse without validation, proof-reuse
identity ownership, or cache-key construction.

### Task 4: Spec `storage.md`

Purpose: specify immutable slots, typed handles, sealing, blob placement, and
retention/collection.

Spec refs: internal 06.

Tests: documentation review only.

Affected design files: `storage.md` in both language directories.

Affected source files: none.

Completion condition: EN/JA specs are synchronized.

Forbidden behavior: source implementation.

### Task 5: Storage slots and sealing

Purpose: allocate slots, seal complete outputs, return typed handles, and hide
unsealed outputs.

Spec refs: `storage.md`.

Tests: double seal rejection, pre-seal access failure, typed round trip.

Affected source files: `src/storage.rs`, exports, and tests.

Completion condition: sealed outputs are immutable and unsealed outputs are
invisible.

Forbidden behavior: exposing raw mutable IR or accepting type mismatches.

### Task 6: Content-addressed blobs and collection

Purpose: spill large outputs to content-addressed internal blobs and collect
unreferenced outputs while retained handles survive.

Spec refs: `storage.md`.

Tests: blob round trip, precise collection, retained output survival.

Affected source files: `src/storage.rs`.

Completion condition: retain/release/collect behavior matches spec.

Forbidden behavior: publishing blobs as artifacts or changing source semantics
when storage is collected.

### Task 7: Spec `publisher.md`

Purpose: specify narrow phase-output publication.

Spec refs: internal 06 and architecture 22.

Tests: documentation review only.

Affected design files: `publisher.md` in both language directories.

Affected source files: none.

Completion condition: EN/JA specs are synchronized.

Forbidden behavior: source implementation.

### Task 8: Phase output publisher

Purpose: implement snapshot/work-unit validation, deterministic hashes, side
tables, and obsolete/current publication checks.

Spec refs: `publisher.md`.

Tests: wrong/obsolete snapshot rejection, stable hashes, side-table retrieval.

Affected source files: `src/publisher.rs`, `src/storage.rs`, exports, and
tests.

Completion condition: dependent tasks can receive sealed handles only after
publisher validation.

Forbidden behavior: open-buffer artifact publication, partial IR exposure, or
proof authority.

### Task 9: Spec `cache_adapter.md`

Purpose: specify cacheability, record encoding, validation-before-rehydration,
and no-proof-authority rules.

Spec refs: internal 06, internal 02, architecture 18, architecture 22.

Tests: documentation review only.

Affected design files: `cache_adapter.md` in both language directories.

Affected source files: none.

Completion condition: EN/JA specs are synchronized and fail-closed cases are
explicit.

Forbidden behavior: reimplementing `CacheKey`, dependency fingerprints,
proof-reuse validation, policy selection, or trusted proof status.

### Task 10: Cache adapter

Purpose: encode sealed outputs and rehydrate handles only from validated cache
records.

Spec refs: `cache_adapter.md`.

Tests: mock/in-memory cache round trip, invalid/incomplete/uncacheable miss,
validated rehydrated handle equality.

Affected source files: `src/cache_adapter.rs`, `src/storage.rs`, exports, and
tests.

Completion condition: no handle is created before a validated hit; all unknown
or incompatible states miss.

Forbidden behavior: cache hit as proof authority or evidence-class upgrade.

### Task 11: Spec `projection.md`

Purpose: specify artifact projection boundary and raw-IR leakage prohibition.

Spec refs: internal 06 and architecture 01.

Tests: documentation review only.

Affected design files: `projection.md` in both language directories.

Affected source files: none.

Completion condition: EN/JA specs are synchronized.

Forbidden behavior: publishing raw IR or kernel-internal state.

### Task 12: Artifact projection service

Purpose: build `mizar-artifact` `VerifiedArtifact` values from validated sealed
projection inputs.

Spec refs: `projection.md`.

Tests: projection fixtures and raw-IR leakage rejection.

Affected source files: `src/projection.rs`, exports, and tests.

Completion condition: only stable external schema values are returned.

Forbidden behavior: producer stubs, publication tokens, manifest commits, or
proof acceptance.

### Task 13: Watch/LSP snapshot replacement

Purpose: supersede current snapshots while retained old outputs remain readable
and cannot publish as current.

Spec refs: internal 06 and architecture 22.

Tests: replacement fixtures, stale handle lifetime, superseded publication
rejection, cache-validation eligibility.

Affected source files: `src/identity.rs`, `src/storage.rs`,
`src/publisher.rs`.

Completion condition: stale outputs are explicit retained/cache inputs, not
current results.

Forbidden behavior: obsolete snapshot publication as current.

### Task 14: Determinism and lifetime property suite

Purpose: broaden deterministic and lifetime coverage.

Spec refs: architecture 20.

Tests: `tests/determinism_lifetime.rs`.

Affected source files: tests only unless a defect is found.

Completion condition: property-style tests pass.

Forbidden behavior: nondeterministic ids/hashes or use-after-collect.

### Task 15: Public-enum forward-compatibility policy

Purpose: enforce `#[non_exhaustive]` on public enums unless explicitly
documented otherwise.

Spec refs: all module specs.

Tests: lint-policy guard.

Affected source files: public enum declarations and `tests/lint_policy.rs`.

Completion condition: every public enum policy is guarded.

Forbidden behavior: undocumented exhaustive public enums.

### Task 16: Source/spec correspondence audit

Purpose: trace public APIs, private correctness-critical behavior, and promised
behavior to implementation/tests.

Spec refs: all module specs and TODO.

Tests: documentation review and diff checks.

Affected design files: `source_spec_correspondence.md` in both language
directories and TODO status.

Affected source files: none unless audit finds drift.

Completion condition: no unresolved blocking/high source/spec drift, including
private behavior that affects canonical hashes, fail-closed cache transitions,
projection filtering, or collection semantics.

Forbidden behavior: changing language specs or `.miz` expectations to match
source.

### Task 17: Bilingual documentation sync audit

Purpose: synchronize every English mizar-ir document with its Japanese
companion.

Spec refs: repository documentation policy.

Tests: documentation review and diff checks.

Affected design files: `bilingual_documentation_sync.md` in both language
directories and any synchronized docs.

Affected source files: none.

Completion condition: no unresolved blocking/high bilingual drift.

Forbidden behavior: leaving changed English docs without Japanese companion.

### Task 18: Architecture-22 follow-up audit

Purpose: re-audit publisher/cache/snapshot replacement for architecture-22
freshness and fail-closed reuse.

Spec refs: architecture 22 and all module specs.

Tests: documentation review plus relevant crate tests.

Affected design files: `architecture_22_follow_up_audit.md` in both language
directories and TODO status.

Affected source files: none unless audit finds drift.

Completion condition: obsolete/open/incomplete/cache-miss rules are traced.

Forbidden behavior: publishing obsolete outputs as current or rehydrating
handles before validation.

### Task 19: Module-boundary refactor gate

Purpose: audit source layout for oversized/mixed-responsibility files and split
only when needed without changing public behavior.

Spec refs: internal 07, TODO, all module specs.

Tests: source/spec and bilingual audit scopes for moved APIs.

Affected design files: `module_boundary_refactor_gate.md` in both language
directories and TODO status.

Affected source files: private module moves only if needed.

Completion condition: source layout matches module ownership and no
review-bottleneck file remains.

Forbidden behavior: behavior cleanup or API exposure mixed into a move.

## Exit Criteria

Hard gates:

- no blocking/high specification inconsistency remains;
- no source behavior remains absent from module specs/tests, including private
  canonicalization, retention, fail-closed cache, and projection-filtering
  behavior;
- no language specification, `.miz` test, traceability metadata, or expectation
  sidecar is changed to match implementation behavior;
- docs and source agree for the `mizar-ir` milestone;
- EN/JA mizar-ir documentation is synchronized;
- `mizar-ir` has no `mizar-driver` dependency and does not create driver,
  diagnostics, producer-token, or publication-token placeholders;
- cache rehydration is fail-closed and cannot create a handle before
  validation;
- published projection values contain stable external schema only;
- cache hits and rehydrated handles never upgrade proof/trusted status;
- required verification commands pass or unrun commands are explicitly
  justified;
- all remaining risks are classified as deferred, out of scope, human-owned,
  or have an allowed protocol class plus risk tag `external_dependency_gap`;
- read-only quality review assigns a valid score of at least 90/100.

Verification commands:

```text
cargo test -p mizar-ir
cargo clippy -p mizar-ir --all-targets -- -D warnings
cargo fmt --check
cargo test -p mizar-cache
cargo test -p mizar-artifact
cargo test -p mizar-build
cargo clippy --all-targets --all-features -- -D warnings
cargo test
git diff --check
git diff --cached --check
```

`cargo test -p mizar-driver` is expected to remain unrunnable in this checkout
until the real driver crate exists; that condition is risk-tagged
`external_dependency_gap`, not patched with a placeholder crate.

Review expectations:

- review-only Agent reviews for spec/docs, test sufficiency, implementation,
  source/documentation consistency, and final quality;
- medium findings are fixed or explicitly deferred with a reason;
- blocking/high findings are fixed before the relevant task commit.
