# Crate Plan: mizar-kernel

> Canonical language: English. Japanese companion:
> [../ja/00.crate_plan.md](../ja/00.crate_plan.md).

Status: complete. Preliminary task 0 created this plan before autonomous
`mizar-kernel` crate development started; the completed milestone and current
residual integration gaps are recorded by the task ledger and closeout. Task-by-task commits, review outcomes,
verification results, and deferred reasons are recorded in
[task_ledger.md](./task_ledger.md). A task is not complete until its commit
exists in history. Because a commit cannot contain its own final hash, each
task's self-hash is verified from `git log` before the next task starts and is
backfilled by a later committed bookkeeping point or by closeout.

Initial repository state for task 0:

- worktree clean before edits;
- `HEAD` = `origin/main` = `0996ad28c57298bd68024eb1f9a6638ef7e37108`;
- local history ends at `docs(vc-closeout): add crate exit report`;
- the `mizar-vc` closeout report commit exists in history:
  `0996ad28c57298bd68024eb1f9a6638ef7e37108`;
- no `crates/mizar-kernel/` source tree exists yet;
- existing `mizar-kernel` design files are limited to [todo.md](./todo.md) and
  its Japanese companion;
- path-specific history shows no completed `mizar-kernel`, `mizar-atp`,
  `mizar-proof`, or `mizar-cache` crate work at `HEAD`; older
  `mizar-artifact` commits are unrelated to this task.

Past backup branches and stash entries are intentionally not consulted.

## Responsibility

Task-22 legacy milestone owned behavior:

- implement the task-22 legacy phase-14 surface: normalized-certificate replay
  for migration/audit inventory;
- parse and structurally validate normalized certificates without granting
  semantic trust during parsing;
- represent SAT clauses canonically, remove duplicate literals, reject malformed
  or unsupported shapes, and produce deterministic renderings and hashes;
- replay MiniSAT-compatible resolution traces deterministically from explicit
  parent clauses and pivots;
- verify substitution, alpha-conversion, freshness, and free-variable side
  conditions independently against the `mizar-core` binder contract;
- check imported facts by stable id and content fingerprint against immutable
  kernel context inputs;
- replay explicit cluster/reduction traces only when their evidence is present
  and upstream cluster-search output is already serialized;
- return policy-independent kernel acceptance or stable rejection records with
  deterministic ordering.

Post-closeout correction owned behavior:

- parse and structurally validate formula/substitution evidence without
  granting semantic trust during parsing;
- validate provenance, imported fact identity/fingerprint/proof status, target
  VC binding, kernel profile binding, explicit check kind / goal polarity, and
  non-imported source context identity;
- validate explicit substitutions against the kernel binder contract and derive
  instantiated formulas inside the kernel;
- derive deterministic SAT problems from checked formulas and substitutions;
- accept only when the audited in-process Rust SAT checker reports
  UNSAT/empty-clause on the kernel-derived SAT problem;
- treat legacy certificates, MiniSAT-compatible resolution traces, backend
  proof methods, SMT proof objects, and backend logs as unsupported under
  normal proof policy and migration/audit-only outside acceptance.

Out of scope:

- ATP backend execution, proof search, premise selection,
  heuristic search, overload resolution, cluster search, registration
  activation, implicit coercion insertion, fallback inference, and global
  mutable compiler state;
- SAT solving outside the audited in-process SAT check over a kernel-derived
  problem from validated evidence;
- source loading, parsing, name resolution, type checking, elaboration, VC
  generation, ATP encoding, portfolio winner selection, proof-policy
  projection, artifact publication, cache lookup, or cache reuse validation;
- changing `doc/spec`, existing `.miz` tests, or expectation sidecars merely to
  match current implementation behavior;
- placeholder integration with unfinished `mizar-atp`, `mizar-proof`,
  `mizar-cache`, or `mizar-artifact` seams.

Dependency policy before the post-closeout correction:

- production dependencies are limited to `mizar-session` and `mizar-core`;
- `mizar-atp`, `mizar-proof`, `mizar-cache`, and `mizar-artifact` must not be
  production dependencies of `mizar-kernel`;
- any missing consumer or producer integration is classified as
  `external_dependency_gap` or `deferred`, not mocked inside the kernel.

Post-closeout correction:

- architecture 15 now supersedes legacy resolution-trace certificate
  acceptance with formula/substitution evidence plus trusted in-process Rust SAT
  checking over a kernel-derived SAT problem;
- task 24 revises the dependency policy to allow exactly one audited direct
  pure-Rust SAT checker dependency:
  `batsat = { version = "=0.6.0", default-features = false }`;
- task 27 must encode that exact dependency allow-list in the manifest and
  lint-policy guard before source integration;
- SAT checking over the kernel-derived problem is trusted evidence checking,
  not ATP/proof search;
- legacy certificate/resolution trace acceptance is `design_drift` /
  `source_drift` until tasks 25-29 replace, gate, or retire it.

Kernel prohibitions:

- no proof search;
- no heuristic premise selection;
- no overload resolution;
- no cluster search;
- no ATP search or backend invocation;
- no implicit coercion insertion;
- no fallback inference;
- no acceptance from backend-reported success alone;
- no hidden reads of mutable compiler-global state, wall-clock time, random
  state, filesystem caches, or unordered iteration.

Trusted-baseline lint policy planned for task 1:

- the crate root forbids unsafe code and opts into workspace warnings/clippy
  denial;
- a crate-local `tests/lint_policy.rs` guard checks exact production
  dependencies with no dev/build/target dependency escape hatches, workspace
  membership, `[lints] workspace = true`, crate-root trust statement, no public
  semantic surface before paired specs exist, and the absence of known
  forbidden imports/tokens for ATP/proof/cache/artifact coupling;
- public enums are planned to be `#[non_exhaustive]` unless a later
  compatibility audit records a justified exception;
- module specs must include a trusted-kernel statement and restate the kernel
  prohibitions before their corresponding source modules are exposed.

## Authority Order And Specification References

For language behavior, use the repository authority order:

1. `doc/spec/en/`
2. `tests/**/*.miz`
3. `tests/coverage/spec_trace.toml`
4. `tests/**/*.expect.toml`
5. `doc/design/`
6. `crates/`

Primary references:

| Ref | Requirement | Kernel status |
|---|---|---|
| [architecture 08](../../architecture/en/08.reasoning_boundary.md) | ATP and pre-ATP discharge results are evidence; the kernel owns trusted acceptance and must distinguish policy statuses from proof status. | Refined by checker and closeout tasks. |
| [architecture 15](../../architecture/en/15.kernel_certificate_format.md) | Defines the legacy canonical certificate surface for tasks 2-16 and the post-closeout formula/substitution evidence, deterministic SAT derivation, SAT-checker acceptance, and rejection reasons for tasks 23-29. | Primary input for tasks 2-16 and 23-29. |
| [architecture 16](../../architecture/en/16.substitution_and_binding.md) | Binder normalization, alpha-equivalence, capture avoidance, deterministic freshness, and substitution replay are stable and kernel-checkable. | Refined by tasks 10-12. |
| [architecture 17](../../architecture/en/17.cluster_trace_format.md) | Cluster/reduction search is outside the kernel; only explicit traces are replayed. | Refined by tasks 13 and 15. |
| [architecture 19](../../architecture/en/19.failure_semantics.md) | Kernel rejection categories and structured rejection reasons are stable failure details. | Refined by tasks 6-7 and audits. |
| [architecture 20](../../architecture/en/20.test_strategy.md) | Kernel-adjacent tests emphasize malformed/fail/soundness cases and deterministic snapshots. | Refined by tasks 17-18. |
| [internal 04](../../internal/en/04.atp_portfolio_and_kernel_check_integration.md) | Kernel check service consumes evidence and immutable contexts, validates kernel-owned acceptance material, and returns policy-independent results. | Refined by tasks 13-16 and 23-29. |
| [internal 07](../../internal/en/07.crate_module_layout.md) | `mizar-kernel` owns legacy certificate parsing, formula/substitution evidence checking, SAT refutation checking, substitution checking, and resolution replay. | Defines module ownership used by [todo.md](./todo.md). |
| [mizar-vc closeout](../../mizar-vc/en/crate_exit_report.md) | Upstream VC evidence, discharge evidence, proof reuse candidates, and downstream kernel/proof/cache validation remain external to VC. | Consumed as upstream context; no direct kernel integration is created in task 0. |

No task 0 finding justifies editing `doc/spec`, `.miz` tests, expectation
sidecars, or traceability metadata.

## Relevant Existing Tests And Missing Coverage

| Test path | Current intent | Kernel relevance |
|---|---|---|
| `crates/mizar-core` Rust tests | Core terms, formulas, binder-normalized logic, control-flow data, and upstream determinism. | Future substitution and binder replay tests may use stable core fixtures, but the kernel must independently re-check evidence. |
| `crates/mizar-vc` Rust tests | VC generation, deterministic discharge evidence, dependency slices, and reuse candidates. | Inputs are untrusted evidence or context candidates for later kernel/proof integration. |
| `doc/design/mizar-test/en/fail_soundness.md` | Defines required soundness failure domains and rejection contracts. | Tasks 17-18 must emphasize malformed certificate, substitution, SAT proof, unresolved symbol, timeout, and resource exhaustion cases. |
| `tests/miz/**` and expectation sidecars | Source-level language fixtures for earlier stages. | No active source-to-kernel-evidence runner exists at task 23; source-derived formula/substitution evidence corpus coverage is deferred. |

Task-0 missing coverage and current roadmap overlay:

The first three rows below are the task-0 inventory retained for audit history;
the crate/source/spec/test scaffold gaps were resolved by the completed
`mizar-kernel` milestone. The remaining current gaps are integration or producer
ownership gaps.

- `source_drift` / `test_gap`: no `mizar-kernel` crate, source files, or
  crate-local tests exist before task 1;
- `spec_gap`: module specs `clause.md`, `certificate_parser.md`,
  `rejection.md`, `resolution_trace.md`, `substitution_checker.md`, and
  `checker.md` do not exist yet;
- `test_gap`: no Rust fixtures cover canonical clauses, certificate parsing,
  rejection records, resolution trace replay, substitution/alpha/FV checking,
  imported facts, cluster trace replay, batch determinism, fail/soundness
  mutations, or public enum compatibility;
- `external_dependency_gap`: at task 0, `mizar-atp`, `mizar-proof`, and
  `mizar-cache` were not available as completed local consumer crates, and
  `mizar-artifact` had no active proof-witness consumer contract for this
  crate. The roadmap sync records that those crates now exist; current
  remaining work is owner-scoped producer, proof-policy, cache-validation, and
  artifact-publication integration.
- `external_dependency_gap`: no active source-derived formula/substitution
  evidence corpus runner exists in `mizar-test`; legacy certificate-runner work
  is migration-only;
- `deferred`: real formula/substitution evidence producers, proof-policy
  projection, proof-witness artifact publication, and cache reuse validation
  remain downstream or producer-owned. MiniSAT-compatible backend traces and
  normalized certificate producers are legacy migration material, not trusted
  targets.

Planned test-first additions:

- task-local Rust unit and integration tests for every data shape, parser
  validation rule, replay step, rejection category, deterministic rendering,
  and fail-closed branch;
- integration tests for deterministic batch ordering and stable rejection
  output;
- mutation-oriented fail/soundness tests after the checker API exists;
- explicit deferred rows instead of fabricated `.miz` formula/substitution
  evidence coverage until source-to-evidence corpus runner support exists;
- no existing `.miz` tests or expectation sidecars may be rebaselined merely to
  match implementation behavior.

## Design And Source Inventory

Design files:

- existing [todo.md](./todo.md);
- this crate plan and [task_ledger.md](./task_ledger.md);
- module specs to be created by TODO tasks:
  [clause.md](./clause.md),
  [certificate_parser.md](./certificate_parser.md),
  [rejection.md](./rejection.md),
  [resolution_trace.md](./resolution_trace.md),
  [substitution_checker.md](./substitution_checker.md), and
  [checker.md](./checker.md);
- audit and closeout documents to be created:
  `soundness_corpus_gap.md`, `determinism_replay_audit.md`,
  `public_enum_policy.md`, `source_spec_audit.md`,
  `bilingual_sync_audit.md`, `module_boundary_audit.md`, and
  `crate_exit_report.md`;
- post-closeout correction specs/audits:
  [formula_evidence.md](./formula_evidence.md),
  [sat_encoding.md](./sat_encoding.md),
  [sat_checker.md](./sat_checker.md), and
  [sat_dependency_audit.md](./sat_dependency_audit.md).

Source files:

- no `crates/mizar-kernel/` directory exists before task 1;
- task 1 will add `crates/mizar-kernel/Cargo.toml`,
  `crates/mizar-kernel/src/lib.rs`, `crates/mizar-kernel/tests/lint_policy.rs`,
  and the workspace member;
- tasks 3, 5, 7, 9, 11, 12, and 14-16 will add or extend the module sources
  named in [todo.md](./todo.md);
- tasks 17-22 may extend tests, lint guards, docs, and private module layout
  without changing public behavior outside their task scopes.
- tasks 25-31 add `formula_evidence`, `sat_encoding`, `sat_checker`, and
  SAT-backed `checker` integration only after task 24's dependency audit
  exists in history, then close goal-polarity and non-imported
  context-identity soundness gaps.

Observed behavior:

- no kernel source behavior exists yet;
- `mizar-core` exists and is the only upstream production logic dependency
  planned beyond `mizar-session`;
- `mizar-vc` is complete and classifies downstream kernel/proof/cache/ATP
  validation as external/deferred;
- observed source behavior remains inventory only. It is not normative.

## Known Gaps And Drift

| ID | Class | Evidence | Action |
|---|---|---|---|
| KERNEL0-G001 | resolved `source_drift` / `test_gap` | Before task 1, `crates/mizar-kernel/` did not exist, so no source or crate-local tests existed. | Resolved by task 1 scaffold, trust statement, dependency guard, lint-policy tests, and subsequent milestone implementation. |
| KERNEL0-G002 | resolved `spec_gap` | At task 0, required module specs listed in `todo.md` were absent. | Resolved by tasks 2, 4, 6, 8, 10, and 13, which created paired English/Japanese specs before implementation tasks. |
| KERNEL0-G003 | resolved `test_gap` | At task 0, no fixtures covered canonical clauses, parsing, rejection, trace replay, substitution, imported facts, cluster replay, determinism, or fail/soundness mutations. | Resolved by implementation and hardening tasks with focused Rust coverage; source-derived corpus remains deferred until runner support exists. |
| KERNEL0-G004 | `external_dependency_gap` | `mizar-atp` is not an active workspace producer of formula/substitution evidence candidates. Legacy normalized certificates or MiniSAT-compatible traces are no longer the trusted target. | Kernel tasks define and check normalized formula/substitution evidence only; do not implement ATP translation or backend proof extraction. |
| KERNEL0-G005 | resolved `external_dependency_gap` with residual downstream scope | At task 0, the proof crate had no `KernelCheckResult` policy-consumer surface; tasks 30-31 now provide limited consumers for accepted proof-obligation status, trusted used-axiom, and witness-boundary fixtures. | Kernel still returns policy-independent accepted/rejected results; richer proof-policy projection and externally authenticated evidence policy remain downstream. |
| KERNEL0-G006 | `external_dependency_gap` | `mizar-cache` is not an active proof-cache validator, and `mizar-artifact` has no current proof-witness consumer contract for this crate. | Do not add cache/artifact coupling; record witness/cache validation as downstream. |
| KERNEL0-G007 | `external_dependency_gap` | `mizar-test` lacks a source-to-evidence proof-verification corpus runner for formula/substitution evidence. Legacy certificate corpus work is migration-only. | Use Rust fixtures and record deferred formula/substitution evidence corpus activation instead of fabricating `.miz` evidence tests. |
| KERNEL0-G008 | `deferred` | Pre-ATP discharge replay versus policy-level built-in evidence is explicitly open in `todo.md` and owned by `mizar-proof` task 6 unless replay is later chosen. | Keep kernel built-in/discharge replay narrow and explicit; do not infer acceptance from VC evidence. |
| KERNEL0-G009 | `design_drift` | Internal 04 names additional future docs (`context.md`, `used_axioms.md`) while `todo.md` uses the minimum six-module split. | Treat context and used-axiom behavior as sections of `checker.md` for this milestone; add separate docs only if module-boundary audit requires it. |
| KERNEL0-G010 | `source_undocumented_behavior` risk | Future implementation could expose public APIs before module specs exist. | Lint-policy guard and task reviews must require paired module specs before source exposure. |
| KERNEL0-G011 | `repo_metadata_conflict` | None observed at task 0. | Report only if later metadata conflicts appear; do not auto-repair unrelated metadata. |
| KERNEL24-G001 | `source_drift` / `deferred` | Task 24 selects `batsat`, but the manifest, lockfile, `sat_checker` source, and dependency lint guard remain at the task-1 baseline until source integration. | Task 27 adds the exact dependency, verifies `Cargo.lock`, updates `tests/lint_policy.rs`, and rejects unsupported solver-step budgets unless callback limits are proven deterministic. |

No blocking contradictory specification is known at task 0. The crate is
allowed to start because missing module specs are scheduled before
implementation, and unfinished external integrations are classified rather than
mocked.

## Task Decomposition

Task execution is strictly linear even when a later task has only a narrower
semantic dependency. Each task is complete only after specification check,
classification, required documentation sync, review-only agent reviews,
implementation or audit, tests, verification, explicit path staging,
staged-content checks, commit creation, ledger update, and a next-task handoff
prompt. The immediately previous task's commit hash must exist in `git log`
before the next task starts; semantic dependency notes never authorize skipping
an earlier task.

| Task | Purpose and refs | Tests | Affected files | Completion and forbidden behavior |
|---|---|---|---|---|
| 0. Crate plan | Create paired crate plan and ledger from kernel TODO, architecture 08/15/16/17/19/20, internal 04/07, and mizar-vc closeout. | `git diff --check`; explicit path staging; `git diff --cached --check`. | `doc/design/mizar-kernel/{en,ja}/00.crate_plan.md`, `task_ledger.md`. | Docs-only commit. Do not create crate source or start task 1. |
| 1. Crate scaffold and trusted-baseline lint policy | Add workspace member, crate manifest, crate-root trust statement, and lint-policy guard; refs internal 07 and this plan. | `cargo fmt --check`; `cargo test -p mizar-kernel`; clippy; dependency/lint guard. | `Cargo.toml`, `crates/mizar-kernel/**`, paired ledger/todo status updates. | Scaffold only. Production dependencies are exactly `mizar-core` and `mizar-session`; no normal/dev/build/target dependency escape hatches; no public semantic APIs beyond module-free crate shell. |
| 2. Spec: `clause.md` | Specify clause/literal representation, canonical ordering, structural well-formedness, tautology policy, hashes, deterministic rendering, and trust statement. | diff checks. | English/Japanese `clause.md`, ledger/todo. | Spec-only. Do not implement clauses. |
| 3. Implement clause representation | Implement canonical literals/clauses with structural validation and deterministic rendering. | well-formed/malformed fixtures, duplicate removal, ordering, rendering/hash stability. | `src/clause.rs`, `src/lib.rs`, tests, ledger. | No resolution replay or certificate parsing beyond clause-owned validation. |
| 4. Spec: `certificate_parser.md` | Specify normalized certificate schema, format tags, backend metadata, locations, structural validation, and schema ownership. | diff checks. | English/Japanese `certificate_parser.md`, ledger/todo. | Spec-only. Kernel owns normalized schema; no backend-specific parser contract is invented. |
| 5. Implement certificate parsing and structural validation | Parse normalized certificates into schema types with resource/position-aware structural validation only. | round-trips, malformed rejection with locations, unknown tags, forward refs, resource bounds. | `src/certificate_parser.rs`, tests, ledger. | Parsing never grants trust or runs semantic replay. |
| 6. Spec: `rejection.md` | Specify stable rejection categories and structured reasons per architecture 15, architecture 19, and internal 04 (`unsupported_certificate_format`, `context_mismatch`, missing provenance, malformed witness data, timeout, and resource exhaustion). | diff checks. | English/Japanese `rejection.md`, ledger/todo. | Spec-only. Do not implement diagnostics policy projection. |
| 7. Implement rejection records | Implement stable rejection categories, reasons, locations, ordering, and deterministic rendering, including the internal-04 context/provenance/unsupported-format/malformed-witness reason set. | category stability, location propagation, ordering, public enum policy baseline, internal-04 reason coverage. | `src/rejection.rs`, tests, ledger. | Rejection remains proof failure even after backend success. |
| 8. Spec: `resolution_trace.md` | Specify MiniSAT-compatible resolution trace replay, parent visibility, pivots, canonical resolvents, and linear replay bounds. | diff checks. | English/Japanese `resolution_trace.md`, ledger/todo. | Spec-only. No SAT solver or proof search. |
| 9. Implement resolution trace checker | Replay resolution steps and reject invalid parent/pivot/resolvent/cost cases. | valid traces, single-step mutations, forward refs, missing pivots, tautology policy, linear cost bound. | `src/resolution_trace.rs`, tests, ledger. | No clause learning, search, or SAT solving. |
| 10. Spec: `substitution_checker.md` | Specify substitution, alpha-equivalence, freshness, and free-variable checks independently of core implementation logic. | diff checks. | English/Japanese `substitution_checker.md`, ledger/todo. | Spec-only. No hidden binder repair or inference. |
| 11. Implement substitution checking | Validate substitution applications against explicit claimed results. | valid substitutions, capture violations, binder collisions, mismatched results, malformed side conditions. | `src/substitution_checker.rs`, tests, ledger. | Do not reuse `mizar-core` checker logic as proof authority; only consume data shapes/contracts. |
| 12. Implement alpha-conversion and FV checks | Check alpha-equivalence claims and free-variable side conditions. | alpha fixtures, FV violations, freshness witnesses, deterministic normalized renderings. | `src/substitution_checker.rs`, tests, ledger. | No heuristic renaming beyond recorded deterministic freshness rules. |
| 13. Spec: `checker.md` | Specify `KernelCheckInput`/`KernelCheckResult`, imported facts, cluster replay, sub-checker orchestration, acceptance, used axioms, and batch ordering. | diff checks. | English/Japanese `checker.md`, ledger/todo. | Spec-only. Restate kernel prohibitions and consumer boundaries. |
| 14. Implement imported-fact checking | Validate declared imported facts by stable id/content hash/policy requirement against immutable context inputs. | undeclared use, hash mismatch, unavailable status, duplicate facts, deterministic used-axiom extraction. | `src/checker.rs`, tests, ledger. | Do not query global artifact/cache state. |
| 15. Implement cluster trace replay | Replay explicit cluster/reduction traces without search, only after confirming the external `mizar-checker` cluster trace payload contract is ready or recording it as `external_dependency_gap` / `deferred`. | valid traces, mutated antecedents/facts, hidden transitive expansion rejection, invalid reduction substitution, cost bound; `cargo test -p mizar-checker` when replay touches the checker trace boundary. | `src/checker.rs` or private modules, tests, ledger. | No cluster search, implicit insertion, registration activation, or fabricated cluster payloads. |
| 16. Kernel check service and deterministic batch ordering | Orchestrate certificate, clause, substitution, trace, imported-fact, cluster checks, and internal-04 context/provenance rejection projection; provide deterministic batch order. | service round-trips, rejected certificates, context mismatch, missing provenance, unsupported format, malformed witness data, batch order shuffle, policy-independent results. | `src/checker.rs`, tests, ledger. | No proof-policy projection, cache acceptance, artifact publication, or ATP fallback. |
| 17. Soundness fail-test corpus | Add mutation-heavy fail/soundness suite over every checker. | malformed certificate, invalid substitution, invalid SAT proof, unresolved symbol, timeout, resource exhaustion, false theorem boundary where available. | tests, optional `soundness_corpus_gap.md`, ledger/todo. | Do not fabricate source-derived `.miz` runner support. |
| 18. Determinism and replay-cost suite | Add deterministic result/rejection and replay-cost property coverage. | repeated identical inputs, shuffled batches, stable renderings, cost counters. | tests, optional `determinism_replay_audit.md`, ledger/todo. | Determinism hardening only unless spec-backed bugs are fixed. |
| 19. Public-enum forward-compatibility policy | Classify public enums and guard `#[non_exhaustive]` compatibility. | lint-policy guard. | `public_enum_policy.md`, module specs referenced by its inventory, `tests/lint_policy.rs`, ledger/todo. | No undocumented exhaustive public enums. |
| 20. Source/spec correspondence and prohibition audit | Trace public APIs and promised behavior to specs, source, tests, or deferred rows; verify every module spec restates prohibitions/trust. | audit guard if useful; crate verification. | `source_spec_audit.md`, docs/tests as needed, ledger/todo. | Audit-only unless fixing documented drift. |
| 21. Bilingual documentation sync audit | Compare English canonical docs and Japanese companions and synchronize. | deterministic file-pair/link checks, `cargo test -p mizar-kernel --test lint_policy`, diff checks. | `bilingual_sync_audit.md`, docs, ledger/todo. | Docs-only unless sync drift requires paired edits. |
| 22. Module-boundary refactor gate | Audit for oversized or mixed-responsibility files and perform move-only private splits only if required. | crate tests/clippy/fmt if source moves; otherwise diff checks. | `module_boundary_audit.md`, optional private modules, ledger/todo. | No behavior cleanup or public API changes in move-only splits. |
| Closeout. Crate exit report and quality review | Record hard gates, task commits, reviews, verification, remaining gaps, score, and handoff. | broad workspace verification plus diff checks. | English/Japanese `crate_exit_report.md`, ledgers, docs. | Complete only with hard gates passing and read-only quality score >= 90. |
| 23. Spec: kernel evidence format correction | Supersede legacy resolution-trace certificate acceptance with formula/substitution evidence, kernel-derived SAT encoding, and SAT-backed acceptance. | docs diff checks plus focused lint guard where needed. | `formula_evidence.md`, `sat_encoding.md`, `sat_checker.md`, architecture/internal/spec sync, ledger/todo. | Spec/docs only. Legacy certificate/trace inputs become normal-policy unsupported and migration/audit-only. |
| 24. Spec and audit: trusted SAT checker dependency | Select and audit the trusted in-process Rust SAT checker dependency and lint-policy revision. | docs diff checks plus dependency metadata/source audit. | `sat_dependency_audit.md`, `sat_checker.md`, crate plan, source/spec audit, ledger/todo. | Docs-only. Selects exact `batsat` dependency but does not edit manifest/source. |
| 25. Formula/substitution evidence schema and parser | Implement kernel-owned formula/substitution evidence schema, provenance binding, target binding, and stable hashes. | parser round-trips, malformed evidence rejection, provenance failure, deterministic hash/rendering. | `src/formula_evidence.rs`, `src/lib.rs`, tests, docs/ledger. | Legacy certificate parsing may remain only outside the new trusted acceptance path. |
| 26. Formula instantiation and deterministic SAT encoding | Derive instantiated formulas and deterministic SAT problems from checked evidence. | stable encoding, capture/provenance mutation rejection, caller-order determinism. | `src/sat_encoding.rs`, substitution integration tests, docs/ledger. | Instantiated formulas and SAT clauses are kernel-derived artifacts, never caller-supplied trusted payload. |
| 27. Trusted SAT checker wrapper | Integrate audited `batsat` behind a small deterministic wrapper and update dependency lint guards. | satisfiable rejection, UNSAT acceptance, limit/error rejection, dependency guard, no process/network behavior. | `Cargo.toml`, `Cargo.lock`, `src/sat_checker.rs`, `tests/lint_policy.rs`, docs/ledger. | No external solver process, no proof search configuration, no model/proof output as trusted material. |
| 28. SAT-backed kernel check service | Replace trusted acceptance with validated formula/substitution evidence plus kernel-derived SAT UNSAT checking. | end-to-end evidence fixtures, mutations, context mismatches, deterministic batch ordering. | `src/checker.rs`, tests, docs/ledger. | Backend proof method, resolution trace, SMT proof object, and backend log remain non-trusted. |
| 29. Migration audit and quality re-review | Gate/retire legacy public surface for normal policy and rerun source/spec, bilingual, dependency, and quality audits. | full kernel verification, workspace verification when practical, docs checks, quality score >= 90. | audits, crate exit report/update docs, ledger/todo. | No new closeout claim unless hard gates pass and remaining gaps are classified. |
| 30. Goal-polarity binding in the check service | Bind `final_goal.polarity` to an explicit caller `KernelEvidenceCheckKind`; reject mismatches before context/SAT work and carry the check kind on `KernelCheckResult`. | proof/consistency polarity matrix, F1-shaped mismatch before context/SAT, proof-policy non-trust for accepted consistency checks. | `src/checker.rs`, `crates/mizar-proof/src/{policy,selection,status}.rs`, paired docs/ledger. | Consistency checks may be accepted by the kernel but are diagnostic-only for proof policy; no proof-obligation status projection from consistency polarity. |
| 31. Context-identity verification for non-imported source bindings | Verify local-hypothesis, cited-premise, and generated-VC-fact formula sources against immutable context rows before SAT encoding. | context identity acceptance/rejection, hash grammar, source-class matrix, proof consumer fixtures. | `src/checker.rs`, formula-evidence/context types, proof fixtures, docs/ledger. | Treat the `mizar-vc` canonical handoff hash as opaque caller context; no checker/core/VC semantic bridge or source-derived payload fabrication. |
| 32. Solver step-budget revisit trigger | Keep the trusted SAT wrapper's exact-step-budget gap parked until a dependency exposes a stable deterministic API. | none until trigger; dependency audit on trigger. | TODO/ledger/audits. | Deferred by design; do not emulate solver budgets with unstable callbacks or wall-clock time. |
| 33. Imported-statement projection validation | Require canonical architecture-18 imported-statement projection payloads before imported formula evidence reaches SAT encoding. | parser projection mutations, caller-context projection mismatch, ATP/VC/proof fixture alignment. | formula evidence/checker source, VC helper, affected fixtures, paired docs/ledger. | Keep statement fingerprints distinct from kernel formula-tree fingerprints; no source-derived payload fabrication or expectation rebaseline. |
| 34. Legacy tautology-marker semantics | Pin legacy tautology markers to audit-only replay data and final-goal rejection. | resolution trace marker replay and service final-goal rejection. | resolution/checker tests, paired docs/ledger. | Legacy audit replay remains rejected diagnostic data and cannot populate trusted `final_goal` or `used_axioms`. |
| 35. Reduct-view soundness re-audit | Recheck the kernel soundness argument after core task 27 reduct-view lowering. | `cargo test -p mizar-kernel`, `cargo test -p mizar-test`, workspace/diff checks. | `soundness_argument.md`, core template audit, TODO/ledger/coverage/handoff docs. | Docs-only Step 4 closure. View choices are normalized atom subject bytes; the kernel does not infer/collapse `qua` paths, and no corpus sidecar rewrite is required. |

## Exit Criteria

Hard gates:

- no unresolved blocking/high specification inconsistency remains;
- no source behavior remains absent from module specs, tests, traceability
  metadata, explicit human approval, or explicit deferred rows;
- milestone-owned certificate, clause, substitution, resolution, imported-fact,
  cluster-replay, and checker-service behavior has Rust coverage or a concrete
  deferred reason;
- existing `.miz` fixtures and expectation sidecars are not changed merely to
  match implementation behavior;
- English canonical docs and Japanese companions are synchronized;
- crate dependency and module exposure policy guards pass;
- the kernel contains no solver except the audited in-process SAT checker over
  kernel-derived SAT problems, and no ATP, proof-policy, cache, artifact,
  overload, cluster-search, coercion-insertion, fallback-inference, or
  global-state coupling;
- required verification commands pass, or any unrun command is explicitly
  justified;
- remaining risks are classified as `external_dependency_gap`, `deferred`, out
  of scope, or human-owned.

Per Rust-source task verification:

```text
cargo fmt --check
cargo test -p mizar-kernel
cargo clippy -p mizar-kernel --all-targets --all-features -- -D warnings
git diff --check
git diff --cached --check
```

Additional required checks when binder contracts or checker/trace boundaries
are touched, unless a task records a concrete reason they cannot be run:

```text
cargo test -p mizar-core
cargo test -p mizar-checker
```

Docs-only tasks run at least:

```text
git diff --check
git diff --cached --check
```

Crate closeout runs:

```text
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
git diff --check
git diff --cached --check
```

Review expectations:

- every task uses review-only agents for implementation specification /
  documentation review, test sufficiency review, full implementation review,
  and source/documentation consistency review;
- blocking/high findings must be fixed and re-reviewed before commit;
- medium findings must be fixed or explicitly deferred with a reason;
- crate completion requires hard gates to pass and a read-only quality score of
  at least 90/100.
