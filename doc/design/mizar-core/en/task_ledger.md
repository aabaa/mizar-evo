# Task Ledger: mizar-core

> Canonical language: English. Japanese companion:
> [../ja/task_ledger.md](../ja/task_ledger.md).

This ledger is the restart point for autonomous `mizar-core` crate work. Before
starting any task, check `git status`, `git log`, and this table. A task is
complete only when it has a commit hash, final review outcomes, verification
results, and deferred reasons.

| Task | Status | Commit | Reviews | Verification | Deferred / notes |
|---|---|---|---|---|---|
| 0. Crate plan | complete | `57fb027c13c0218fa214bdee763be8d0041a0b12` | Spec/doc review: no blocking/high/medium findings. Test sufficiency review: medium staged-check gap fixed, re-review no findings. Full implementation review: no blocking/high/medium findings. Source/doc consistency review: no blocking/high/medium findings. | `git diff --check` passed before staging; `git diff --cached --check` passed after explicit path staging. | Docs-only. Deferred external gaps classified in `00.crate_plan.md`; no task-local deferred work. |
| 1. Crate scaffold and lint-policy guard | complete | `c3dccec37e8fcd18e522a1cfd4af9502a1ebabda` | Spec/doc review: medium lint-boundary findings fixed, re-review no findings. Test sufficiency review: medium alias/API-surface findings fixed, re-review no findings. Full implementation review: no blocking/high/medium findings. Source/doc consistency review: no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Scaffold-only. Adds workspace member, lockfile entry, minimal crate, and lint guard. No deferred task-local work. |
| 2. Spec: `core_ir.md` | complete | `40d9c3b202cb953851a1fccee67030e4e8cafa5a` | Spec/doc review: medium seed/statement/diagnostic/choice/comprehension/source-map findings fixed, re-review no findings. Test sufficiency review: no blocking/high/medium findings. Full implementation review: medium findings fixed via spec/doc re-review. Source/doc consistency review: no blocking/high/medium findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Rust tests deferred to task 3; source-derived `.miz` core fixtures deferred until checker payload extraction and mizar-test stage support exist. |
| 3. Implement `core_ir` data shapes | complete | `b58be7c00a3fcadf96f54ebbe434bd04e42f1718` | Spec/doc review: medium stable-choice/proof-status/source-validation findings fixed, low owner-diagnostic/doc-status findings fixed, final re-review no blocking/high/medium findings. Test sufficiency review: medium invalid-reference/seed/debug/error coverage findings fixed; medium diagnostic/provenance tie-break coverage and low source-map/owner/error mismatch gaps fixed, final re-review no findings. Full implementation review: medium algorithm-owner/generated-origin/provenance findings fixed, low crate-doc/source-sort findings fixed. Source/doc consistency review: medium proof-node/provenance/label/status doc findings fixed, final re-review no findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements `core_ir` data shapes, validation, deterministic debug rendering, and lint guard expansion. Source-derived `.miz` core fixtures remain deferred until checker payload extraction and mizar-test stage support exist; no task-local external dependency is fabricated. |
| 4. Spec: `binder_normalization.md` | complete | `6e7c458dd8f7005011fddf4d13577646af35582d` | Spec/doc review: generated-origin, freshness witness, open/depth, closure guard side-condition, and sync findings fixed; final re-review no findings. Test sufficiency review: composition/malformed coverage findings fixed; nonzero-depth operation, metadata rejection, witness-field, and generated-origin cases added; final re-review no findings. Full design review: closure guard, de Bruijn depth, guard scope, variable metadata, and generated-origin findings fixed; final re-review no findings. Source/doc consistency review: metadata and closure-guard findings fixed; final re-review no findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Rust implementation and tests deferred to tasks 5 and 6. Source-derived `.miz` fixtures remain deferred until checker payload extraction and mizar-test stage support exist. Kernel certificate schema, proof acceptance, `VcId`s, and proof reuse anchors remain external/deferred; no external dependency is fabricated. |
| 5. Binder representation and substitution | complete | `300f814eaf83a54f5c8e170d443c18aebd087b95` | Spec/doc review: `NormalizedBinder` scope, task-5-vs-task-6 test split, and public enum policy findings fixed; final re-review no findings. Test sufficiency review: nested-binder substitution, rejected composition, closure mismatch, use-site shadowing, metadata coverage, and formula helper findings fixed; final re-review no findings. Full implementation review: closure capture, invalid bound indices, metadata validation, replayable freshness, formula helpers, checked depth conversion, and nested freshening collision findings fixed; final re-review no findings. Source/doc consistency review: untracked implementation staging warning handled by explicit path staging; content re-review no findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements `binder_normalization` representation, substitution, freshness witnesses, closure expansion, and task-5 tests. Task 6 alpha-equivalence, canonical forms, and full normalization utilities remain deferred. Source-derived `.miz` fixtures remain deferred until checker payload extraction and mizar-test stage support exist. Kernel certificate schema, proof acceptance, `VcId`s, and proof reuse anchors remain external/deferred; no external dependency is fabricated. |
| 6. Alpha-equivalence and normalization utilities | complete | `841ba6e41d743ac0512d7f2518924fa080cfd2e0` | Spec/doc review: task status, public API shape, metadata-default, module-rustdoc, and ambient-context-frame findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: guard/free-variable, malformed raw evidence, generated-origin matrix, alpha/canonical, idempotence, and de Bruijn coverage findings fixed; final re-review no findings. Full implementation review: ambient context depth, free-variable cache validation, generated-origin guard scan, freshened binder revalidation, platform-stable fresh-id, substitution traversal depth, closure formal depth, and free-original-var scope findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: generated-origin guard scan and TODO/ledger status findings fixed; final follow-up no semantic source-doc drift remains. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; `git diff --cached --check` after explicit path staging. | Implements alpha-equivalence, canonical keys, raw `CoreIr` normalization utilities, validation, guard-aware binders, generated-origin semantic records, and task-6 tests. Source-derived `.miz` fixtures remain deferred until checker payload extraction and mizar-test stage support exist. Kernel certificate schema, proof acceptance, `VcId`s, proof reuse anchors, VC/proof/kernel handoff remain external/deferred; no external dependency is fabricated. |
| 7. Spec: `elaborator.md` | complete | `02905416a6c245a8ac1c0ba697a21328e8a7d298` | Spec/doc review: source-map scope, module-table status, visibility/export metadata, stable-choice ownership/reuse, and algorithm statement-owner findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: stable-choice, Fraenkel sethood evidence, exported-definition reuse, theorem/lemma ownership, executable `the`, and ghost-only `Pick` obligations fixed; final re-review no findings. Full implementation review: module implementation status finding fixed; final re-review no findings. Source/doc consistency review: TODO/ledger status and deferred/external wording findings fixed; final re-review no findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Adds English/Japanese elaborator module spec for tasks 8-13. Source implementation, Rust tests, and source-derived `.miz` fixtures are deferred to tasks 8-13 and mizar-test/source-extraction support. External dependencies for artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 8. Core context preparation | complete | `a0d6d3f90d729c765c403648b6edb817669d8f98` | Spec/doc review: binder/variable context and checker/resolver provenance findings fixed; TODO/ledger consistency finding fixed; final re-review no blocking/high/medium findings. Test sufficiency review: recovered-site, canonical reference identity, binder class/sort, and definition-boundary coverage findings fixed; final re-review no blocking/high/medium findings. Full implementation review: module ownership, checker diagnostic preservation, generated-origin evidence provenance, and undeclared binder-source findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: only self-referential pending wording was noted as low/admin and fixed; no blocking/high/medium source-doc drift. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 1 context preparation only: checker/resolver-owned context seeds, canonical item registry, definition-boundary registry, generated-origin registry, binder metadata/type-fact context, source-map/diagnostic builders, and deterministic worklist. Type/fact lowering, term/formula lowering, definition/proof/algorithm lowering, source-derived `.miz` fixtures, artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 9. Type and fact lowering | complete | `860736aadcb72fdc67ef3e57b036a3dd218ac54a` | Spec/doc review: cluster fact, subject-table, obligation-seed invariant, and CoreContext composition gaps fixed in the implementation spec; re-review no blocking/high/medium findings. Test sufficiency review: missing-evidence matrix, TypePred subject, view provenance, cluster fact, reconsidering, and standalone carried-obligation coverage findings fixed; final re-review no blocking/high/medium findings. Full implementation review: reconsidering subject, term-sort subject, and accepted cluster fact boundary findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: Step 2 `qua` wording and stale gap wording fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 2 type/fact lowering only as a delta over `CoreContext`: declared binder guards/assumptions, type assertions, attribute conjunctions, mode expansion predicates, accepted cluster fact predicates without registration activation, source/inserted `qua` view provenance, reconsider/narrowing binders, carried checker initial obligations, and missing-evidence diagnostics/deferred seeds. General term/formula lowering, definition/proof/algorithm lowering, source-derived `.miz` fixtures, artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 10. Term and formula lowering | complete | `14e76d67d155b173ee61d45e2227a9cbcb0991df` | Spec/doc review: generated-origin reuse, Fraenkel obligation preservation, and True/False formula gaps fixed in the task spec; final implementation kept docs synchronized. Test sufficiency review: generated-origin delta/reuse, Fraenkel reuse/obligation invariants, stable-choice Apply functor/args/key, inserted `qua`, `Exists`, guard bypass, and formula child wiring findings fixed; final re-review no blocking/high/medium findings. Full implementation review: guard free-variable derivation, generated-origin param/functor validation, generated delta shape, Fraenkel membership and missing-sethood obligation invariants fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: generated-origin functor/delta/reuse docs, Fraenkel invariants, guard derivation, and English/Japanese sync checked; no blocking/high/medium drift. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 3 term/formula lowering only as a seed-based delta over `CoreContext`: variables, constants, applications, selectors, tuples, set enumerations, predicate/equality/type/connective/quantifier formulas, source/inserted `qua` erasure with view records, stable-choice generated Apply lowering with existing/current-delta origin reuse, Fraenkel generated Apply lowering with sethood evidence, active membership-axiom handoff or explicit already-carried marker, missing-sethood error/deferred seed preservation, and failed-site error nodes. Definition/proof/algorithm lowering, source-derived `.miz` fixtures, artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 11. Definition lowering | complete | `9523a0e33b03ea91c1caf44336d984b9b3445c08` | Spec/doc review: algorithm-backed body boundary, existing-correctness backrefs, todo unfolding wording, and closure-use deferred wording findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: invalid boundary, deferred/existing correctness, spurious generated dependency, stable-choice reuse/no-delta, and closure-use wording findings fixed; final re-review no blocking/high/medium findings. Full implementation review: existing-correctness backrefs and owner-mismatch validation findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: algorithm boundary, existing-correctness backrefs, generated-dependency boundary, and stale metadata findings fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 4 definition lowering only as a delta over `CoreContext` and Step 3 output: definition seeds to `CoreDefinitionTable`, expansion policies, formal binders/guards, guarded/otherwise bodies, generated dependencies reachable through term/formula graphs, correctness obligation seeds and cloned existing seeds with definition/body backrefs, item status/diagnostic deltas for skipped/error bodies, and validation for owner/boundary/symbol/generated/correctness invariants. Source-to-checker extraction of closure-use payloads, explicit unfolding execution, proof acceptance, VC generation, kernel checking, algorithm body lowering, source-derived `.miz` fixtures, artifact schemas, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 12. Proof-skeleton lowering | complete | `e4afecbf874a6448ffc68b3b1b2bd98a4b7b81d6` | Spec/doc review: label-citation scope, step-vs-terminal obligation semantics, proof-status boundary, completion marker, symbol-kind wording, todo checklist, current-goal wording, and explicit terminal-seed boundary findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: active path formula, terminal external citation, explicit error-root, current-goal, sequence-label, and durable terminal-citation coverage findings fixed; final re-review no blocking/high/medium findings. Full implementation review: sequence label propagation, branch isolation, thesis stack, durable terminal citations, and malformed diagnostic ownership findings checked; final re-review no blocking/high/medium findings. Source/doc consistency review: raw Core IR citation-kind validation drift fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 5 proof-skeleton lowering only as a delta over `CoreContext`, Step 3 output, and Step 4 output: theorem/lemma proof seeds to `CoreProofTable`, proof-node trees, current-goal thesis transitions, sequence label propagation, introduced binders, assumptions, labeled steps, local label-scope validation, proof-like symbol/generated/label citations, branch preservation with sibling-scope isolation, terminal `TheoremProof` obligation seeds with proof/node/item/formula/citation backrefs, durable terminal citation storage, proof status records, malformed/missing skeleton diagnostics, and validation for owner/symbol/status/citation/label invariants. Proof acceptance, discharge, VC generation, proof search, kernel checking, artifact schemas, `VcId`s, source-derived `.miz` fixtures, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 13. Algorithm-shell lowering | complete | `23d420b3e12f4c0069d4e60292f892d98efe72c6` | Spec/doc review: parser-coverage dependency and `CoreAlgorithm` diagnostics wording findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: malformed statement diagnostics, exact source/provenance checks, boundary status, and control-flow-specific invalid references fixed; final re-review no findings. Full implementation review: initial review no findings; diagnostic aggregation re-review no blocking/high/medium findings. Source/doc consistency review: generated-origin pick wording, Task 13 dependency, and reserved `AlgorithmPick` wording fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 6 algorithm-shell lowering only as a delta over `CoreContext`, Step 3 output, and Step 5 proof output: algorithm seeds to `CoreAlgorithmTable`, statement-shell trees for let/assign/assert/if/while/match/return/break/continue/pick/error, params/results/contracts/ghost effects, local `Pick` binders without generated-origin emission, source/provenance/source-map preservation, algorithm-shell diagnostics for missing/malformed payloads, parent diagnostic aggregation, and validation for owner/symbol/boundary/binder/term/formula/target invariants. Source-to-checker extraction for full algorithm payloads, parser task 32-34 source-derived coverage, CFG construction, flow diagnostics, VC generation, proof acceptance, kernel checking, artifact schemas, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 14. Spec: `control_flow.md` | complete | `93a66e3afa77ebb3794ca9285b2c9fa48e720b11` | Spec/doc review: call-site metadata, `for` metadata, statement-placement source maps, unsupported-call diagnostics, and `ForRange` step/exit metadata findings fixed; final re-review no findings. Test sufficiency review: local table, exact source/provenance, and unsupported external-payload coverage findings fixed; final re-review no findings. Full design review: partial-call `ensures`, context table, local declaration metadata, snapshot/claim boundary, and context fact/effect storage findings fixed; final re-review no findings. Source/doc consistency review: architecture-07 drift, task dependency, and snapshot/claim external-gap findings fixed; final re-review no blocking/high/medium findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Adds English/Japanese `control_flow.md` defining `ControlFlowIr`, basic blocks, locals, contexts, contracts/calls, ghost effects, termination, source maps/statement placements, core-to-CFG construction, diagnostics, determinism, and future test obligations. Rust implementation and tests are deferred to tasks 15-17; obligation-seed handoff to task 18. Source-to-checker extraction, parser task 32-34 source-derived coverage, snapshots/claims until explicit checker shells, VC generation, proof acceptance, kernel checking, artifact schemas, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 15. `ControlFlowIr` construction | complete | `73a5786581c745a9d87ada892c0f6fa8329b987f` | Spec/doc review: hidden locals, unsupported local declarations, illegal break/continue, minimal contexts, deterministic rendering, diagnostic-id shape, structural diagnostic tests, and crate-plan scope findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: context, CFG edge/block shape, local table/source-map fidelity, and structural diagnostic source/ref coverage findings fixed; final re-review no findings. Full implementation review: fallthrough closure, break context join, loop-carried context propagation, and unreachable-join filtering findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: final review found no blocking/high/medium findings; EN/JA docs, task plan, todo, ledger, and public module export policy are synchronized. | `cargo fmt`; `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements phase-10 `ControlFlowIr` construction only: public `control_flow` module, deterministic flow table/map per `CoreAlgorithm`, blocks/terminators for let/assign/assert/if/while/match/return/break/continue/pick/error, local table for parameters/results/let/pick with unsupported-role metadata, minimal `ProgramContext` propagation, source maps and statement placements, structural diagnostics for unsupported local declarations and illegal break/continue, phase-9 error carry-through, byte-stable debug rendering, lint-policy export guard, and regression coverage for fallthrough, loop break/continue/carry, unreachable joins, source fidelity, and deterministic order. Contract/ghost/termination semantic attachment, flow diagnostics such as use-before-assignment/unreachable reporting, obligation-seed handoff, source-derived `for`/snapshot/claim payloads, source-to-checker extraction, VC generation, proof acceptance, kernel checking, artifact schemas, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 16. Contracts, ghost effects, termination measures | complete | `07d802f52b244d92a4f332d3fe606b327499f956` | Spec/doc review: call-site deferred shape, ghost-table overstatement, assertion timing, and status-document findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: loop-invariant context facts, exact invariant/decreasing placement, requirement/assertion fact exactness, and initialized ghost-let assignment split findings fixed; final re-review no findings. Full implementation review: aggregate contract assertions/invariants and assertion-timing findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: final pass found no blocking/high/medium findings; EN/JA docs, todo, ledger, and Task 16 source shape are synchronized. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Task 16 contract/ghost/termination attachment over phase-10 CFGs: entry `requires` facts/sites, aggregate `CoreContractSet.assertions` and `invariants` as `AlgorithmContract` metadata, statement assertion successor facts/sites, `ensures` on explicit and implicit returns, loop invariant header/backedge/break/continue sites plus context facts, algorithm and loop decreasing sites, partial algorithm/loop termination sites when decreasing payloads are absent, and deterministic runtime/ghost assignment-effect and pick-local splits including initialized ghost `let`. Checker-owned call contract payloads, flow diagnostics, obligation-seed handoff, source-derived `for`/snapshot/claim payloads, source-to-checker extraction, VC generation, proof acceptance, kernel checking, artifact schemas, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 17. Flow diagnostics | complete | `004c837170b1088330b052e4718bea42bb2b3ecb` | Spec/doc review: narrowed diagnostic scope, site/context matrix, diagnostic shape/source ordering, and quantifier shadowing findings fixed; final re-review no findings. Test sufficiency review: statement-owned site coverage, quantifier left-to-right coverage, stable source ordering, deferred non-fabrication, and unreachable block-payload findings fixed; final re-review no findings. Full implementation review: diagnostic-ordering finding fixed with source/block/class sorting and id remapping; final re-review no blocking/high/medium findings. Source/doc consistency review: final review no blocking/high/medium findings; EN/JA docs, todo, ledger, and Task 17 source shape are synchronized. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements concrete Task 17 flow diagnostics only: `UseBeforeAssignment` over reachable statement-owned terms/formulas, `UnreachableStatement` over unreachable statements, stable diagnostic sorting/remapping, source-map anchors, and regression coverage for all in-scope sites and quantifier binder scope. Immutable-assignment, ghost-leakage, call/contract instantiation, pattern/snapshot/claim payload, alias/lvalue-precision, obligation-seed, source-to-checker extraction, VC/proof/kernel/artifact schema, `VcId`, and proof/cache-reuse diagnostics remain external/deferred; no external dependency is fabricated. |
| 18. Obligation-seed handoff contract | complete | `64ce704102663d2f47c3454a22f9c566e7d034ea` | Spec/doc review: handoff shape, `ObligationSeed.diagnostics`, and crate-plan scope medium findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: loop/partial/continue coverage, exact source/provenance/site/order, `core_refs`, generated/deferred/error existing seeds, break-exit invariant, semantic-origin, and empty-context findings fixed; final review no findings. Full implementation review: no blocking/high/medium findings. Source/doc consistency review: stale TODO wording fixed; final follow-up no findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements the Task 18 seed handoff view: existing core seeds are cloned with original backrefs, labels, paths, source/provenance, diagnostics, status, and refs; explicit flow metadata emits deferred algorithm contract, termination, and ghost-erasure seeds with CFG-local site refs, exact source maps, deterministic ordering, and no fabricated VC context. Deferred/external: concrete `VcId`, `ObligationAnchor`, VC/anchor fingerprints, call/result substitution, concrete assertion/invariant VC context, termination well-foundedness schemas, ghost-erasure proof shape, source-derived call/pattern/snapshot/claim payloads, source-to-checker extraction, VC/proof/kernel/artifact schemas, proof acceptance, and proof/cache reuse remain external/deferred; no external dependency is fabricated. |
| 19. Snapshot dumps and corpus contributions | complete | `45a8762baaea1d58945abc2fde291302f5c10e36` | Spec/doc review: no blocking/high/medium findings; reviewer confirmed docs/metadata-only deferred traceability is appropriate because mizar-test exposes only parse-only SurfaceAst snapshots, type_elaboration stops at the payload extraction gap, and proof_verification has no runner. Test sufficiency review: no findings; metadata validation is sufficient for deferred traceability rows and Rust tests are not required because no Rust source, runner behavior, or active fixtures changed. Full implementation review: no blocking/high/medium findings; traceability rows are deferred snapshot coverage with empty tests and precise external-dependency reasons, and docs do not overclaim active runner support. Source/doc consistency review: no findings; EN/JA crate plan, todo, ledger, and traceability metadata are synchronized and do not overclaim active runner support. | `git diff --check`; `cargo run -p mizar-test -- plan --validation-mode metadata` (0 errors, 1 pre-existing warning for `spec.en.algorithm.vc.assignment_loop_exits`); `git diff --cached --check` after explicit path staging. | Metadata-only. Adds explicit deferred snapshot traceability for `CoreIr` type_elaboration baselines and `ControlFlowIr` proof_verification baselines. External/deferred: mizar-test type_elaboration/proof_verification snapshot comparison support, source-to-checker/source-to-core payload extraction, proof/algorithm payload flow, VC/proof/kernel downstream seams. No runner support or source-derived baseline is fabricated. |
| 20. Determinism suite | complete | `6bfe55b5f1a8cf4b0bbd8ee7f9a8184508254e54` | Spec/doc review: public-API-vs-`ResolvedTypedAst` overclaim finding fixed; final re-review no blocking/high/medium findings. Test sufficiency review: fresh-run structural equality and binder-guard coverage findings fixed; final re-review no findings. Full implementation review: no blocking/high/medium findings after follow-up; structural fixture equality and guarded quantifier paths are valid. Source/doc consistency review: no findings; EN/JA crate plan, todo, ledger, and the new determinism suite agree on public-API scope and deferred source-derived determinism. | `cargo fmt --check`; `cargo test -p mizar-core --test determinism_suite`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; `git diff --cached --check` after explicit path staging. | Adds a public-API determinism integration suite for fresh `CoreIr` fixture rebuilds, structural `CoreIr`/`ControlFlowOutput`/`ObligationSeedHandoff` equality, byte-stable debug renderings, deterministic flow/handoff ordering, and binder canonicalization across renamed guarded quantifiers. Full source-derived `ResolvedTypedAst` determinism, source-to-checker extraction, corpus runner support, VC/proof/kernel seams, and downstream artifact schemas remain external/deferred; no source-derived payload is fabricated. |
| 21. Public-enum forward-compatibility policy | complete | `184c0f113eded9badff51957ef92ad60592a852d` | Spec/doc review: no blocking/high/medium findings; low guard precision, internal-exhaustive-match notes, and TODO/ledger timing finding incorporated; final re-review no findings. Test sufficiency review: exact source/doc set equality and nested/re-export public-surface guard findings fixed; final re-review no findings. Full implementation review: stale-doc medium finding fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: no enum-list drift; ledger pending-status finding fixed in final update. | `cargo fmt --check`; `cargo test -p mizar-core public_core_enums_are_forward_compatible_and_documented`; `cargo test -p mizar-core --test lint_policy`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; `git diff --cached --check` after explicit path staging. | Classifies every current public `mizar-core` enum as downstream forward-compatible and guarded by `#[non_exhaustive]`, records per-module EN/JA policy tables, and adds a lint-policy drift guard that scans source attributes, rejects unguarded nested/re-export public enum surfaces, and checks exact source/policy-table equality in owning EN/JA sections. No exhaustive public enum exceptions are owned by `mizar-core`; future public enum additions must update the owning specs and guard shape. |
| 22. Source/spec correspondence audit | complete | `6b62bc8fbf028039fae525b9b54abca5ac6e4704` | Spec/doc review: medium `source_undocumented_behavior` and follow-up-register shape findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: public item inventory guard, final verification, and follow-up-detail findings fixed; final re-review no blocking/high/medium findings. Full implementation review: no blocking/high/medium findings. Source/doc consistency review: no blocking/high/medium findings; low staging-scope note handled by explicit Task 22 path staging. | `cargo fmt --check`; `cargo test -p mizar-core source_spec_audit_covers_public_modules_and_deferred_gaps`; `cargo test -p mizar-core --test lint_policy`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; `git diff --cached --check` after explicit path staging. | Audit-only plus lint guard. Adds English/Japanese source/spec correspondence audit with item-level public API inventory, source/spec/test/deferred tracing, `source_undocumented_behavior` pass, and CORE-AUDIT follow-up register. External/deferred: source-to-checker extraction, active type_elaboration/proof_verification snapshots, artifact schema, proof acceptance, VC generation, kernel checking, concrete `VcId`/`ObligationAnchor`, proof/cache reuse, source-derived call/result substitution, pattern, snapshot, claim, and public diagnostic code-space remain external/deferred; no behavior or external dependency is fabricated. |
| 23. Bilingual documentation sync audit | complete | `260e41ed8a1a6c8c8d3da4bf4b53b44a0b451d39` | Spec/doc review: no blocking/high/medium findings after low paired-file inventory and crate-plan result notes were fixed. Test sufficiency review: no blocking/high/medium findings; docs-only diff checks are sufficient and staged diff check is required after explicit staging. Full implementation review: no blocking/high/medium findings. Source/doc consistency review: no blocking/high/medium findings; low ledger-placeholder note fixed before staging. | `git diff --check`; `git diff --cached --check` after explicit path staging. | Docs-only. Adds English/Japanese bilingual sync audit for all current mizar-core doc pairs, records no blocking bilingual documentation drift, and defers future `module_boundary_audit.md` and `crate_exit_report.md` companion checks to their owning tasks. No Rust source, `.miz` fixtures, expectations, or traceability metadata changed. |
| 24. Module-boundary refactor gate | complete | `6c7268d083a71e261c521bad9e90e315173c9bee` | Spec/doc review: no blocking/high/medium findings after medium wording and bilingual-audit coverage findings were fixed; low ledger/todo polish was fixed. Test sufficiency review: no blocking/high/medium findings; docs-only diff checks are sufficient and staged diff check is required after explicit staging. Full implementation review: no blocking/high/medium findings after the stale active `CORE-BILINGUAL-G001` classification was moved to resolved pair updates; low ledger-placeholder note was fixed before staging. Source/doc consistency review: no blocking/high/medium findings; low pending-review ledger note was fixed before staging. | `git diff --check`; `git diff --cached --check` after explicit path staging. | Audit-only. Adds English/Japanese module-boundary audit, records large but cohesive source files that match public module specs, defers optional private helper extraction to future move-only follow-ups, and performs no Rust source/API/behavior changes. External/downstream seams remain external/deferred; no placeholder modules are fabricated. |
| Closeout. Crate exit report and quality review | ready to commit | pending; closeout commit hash is recorded in the final handoff because self-hash cannot be embedded in this commit | Spec/doc review: no blocking/high/medium findings after the corrected closeout specification made completion conditional on hard gates, required ledger commit-hash backfill, and matched the crate-exit template. Test sufficiency review: no blocking/high/medium findings; broad workspace verification plus diff checks are sufficient for the docs-only closeout scope. Full implementation review: no blocking/high/medium findings after Japanese Task 3 ledger verification drift and stale closeout wording were fixed. Source/doc consistency review: no blocking/high/medium findings; task hashes, bilingual closeout sync, deferred seam classifications, and docs-only scope are consistent. Read-only quality review: no hard gate failures, no blocking/high/medium findings, no score caps; valid score 94/100. | `cargo fmt --check` passed; `cargo clippy --all-targets --all-features -- -D warnings` passed; `cargo test` passed; `git diff --check` passed; `git diff --cached --check` passed after explicit closeout path staging. | Docs-only closeout. Adds English/Japanese exit report, backfills task commit hashes in the ledgers, resolves the closeout bilingual audit row, records final hard-gate/quality evidence, and changes no Rust source, `.miz` fixtures, expectations, `doc/spec`, or traceability metadata. Remaining source-extraction, VC/proof/kernel/artifact, diagnostic-code, and optional move-only split seams stay external/deferred. |
