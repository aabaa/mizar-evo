# Task Ledger: mizar-core

> Canonical language: English. Japanese companion:
> [../ja/task_ledger.md](../ja/task_ledger.md).

This ledger is the restart point for autonomous `mizar-core` crate work. Before
starting any task, check `git status`, `git log`, and this table. A task is
complete only when it has a commit hash, final review outcomes, verification
results, and deferred reasons.

| Task | Status | Commit | Reviews | Verification | Deferred / notes |
|---|---|---|---|---|---|
| 0. Crate plan | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that creates this row | Spec/doc review: no blocking/high/medium findings. Test sufficiency review: medium staged-check gap fixed, re-review no findings. Full implementation review: no blocking/high/medium findings. Source/doc consistency review: no blocking/high/medium findings. | `git diff --check` passed before staging; `git diff --cached --check` passed after explicit path staging. | Docs-only. Deferred external gaps classified in `00.crate_plan.md`; no task-local deferred work. |
| 1. Crate scaffold and lint-policy guard | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: medium lint-boundary findings fixed, re-review no findings. Test sufficiency review: medium alias/API-surface findings fixed, re-review no findings. Full implementation review: no blocking/high/medium findings. Source/doc consistency review: no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Scaffold-only. Adds workspace member, lockfile entry, minimal crate, and lint guard. No deferred task-local work. |
| 2. Spec: `core_ir.md` | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: medium seed/statement/diagnostic/choice/comprehension/source-map findings fixed, re-review no findings. Test sufficiency review: no blocking/high/medium findings. Full implementation review: medium findings fixed via spec/doc re-review. Source/doc consistency review: no blocking/high/medium findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Rust tests deferred to task 3; source-derived `.miz` core fixtures deferred until checker payload extraction and mizar-test stage support exist. |
| 3. Implement `core_ir` data shapes | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: medium stable-choice/proof-status/source-validation findings fixed, low owner-diagnostic/doc-status findings fixed, final re-review no blocking/high/medium findings. Test sufficiency review: medium invalid-reference/seed/debug/error coverage findings fixed; medium diagnostic/provenance tie-break coverage and low source-map/owner/error mismatch gaps fixed, final re-review no findings. Full implementation review: medium algorithm-owner/generated-origin/provenance findings fixed, low crate-doc/source-sort findings fixed. Source/doc consistency review: medium proof-node/provenance/label/status doc findings fixed, final re-review no findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage checks pending. | Implements `core_ir` data shapes, validation, deterministic debug rendering, and lint guard expansion. Source-derived `.miz` core fixtures remain deferred until checker payload extraction and mizar-test stage support exist; no task-local external dependency is fabricated. |
| 4. Spec: `binder_normalization.md` | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: generated-origin, freshness witness, open/depth, closure guard side-condition, and sync findings fixed; final re-review no findings. Test sufficiency review: composition/malformed coverage findings fixed; nonzero-depth operation, metadata rejection, witness-field, and generated-origin cases added; final re-review no findings. Full design review: closure guard, de Bruijn depth, guard scope, variable metadata, and generated-origin findings fixed; final re-review no findings. Source/doc consistency review: metadata and closure-guard findings fixed; final re-review no findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Rust implementation and tests deferred to tasks 5 and 6. Source-derived `.miz` fixtures remain deferred until checker payload extraction and mizar-test stage support exist. Kernel certificate schema, proof acceptance, `VcId`s, and proof reuse anchors remain external/deferred; no external dependency is fabricated. |
| 5. Binder representation and substitution | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: `NormalizedBinder` scope, task-5-vs-task-6 test split, and public enum policy findings fixed; final re-review no findings. Test sufficiency review: nested-binder substitution, rejected composition, closure mismatch, use-site shadowing, metadata coverage, and formula helper findings fixed; final re-review no findings. Full implementation review: closure capture, invalid bound indices, metadata validation, replayable freshness, formula helpers, checked depth conversion, and nested freshening collision findings fixed; final re-review no findings. Source/doc consistency review: untracked implementation staging warning handled by explicit path staging; content re-review no findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements `binder_normalization` representation, substitution, freshness witnesses, closure expansion, and task-5 tests. Task 6 alpha-equivalence, canonical forms, and full normalization utilities remain deferred. Source-derived `.miz` fixtures remain deferred until checker payload extraction and mizar-test stage support exist. Kernel certificate schema, proof acceptance, `VcId`s, and proof reuse anchors remain external/deferred; no external dependency is fabricated. |
| 6. Alpha-equivalence and normalization utilities | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: task status, public API shape, metadata-default, module-rustdoc, and ambient-context-frame findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: guard/free-variable, malformed raw evidence, generated-origin matrix, alpha/canonical, idempotence, and de Bruijn coverage findings fixed; final re-review no findings. Full implementation review: ambient context depth, free-variable cache validation, generated-origin guard scan, freshened binder revalidation, platform-stable fresh-id, substitution traversal depth, closure formal depth, and free-original-var scope findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: generated-origin guard scan and TODO/ledger status findings fixed; final follow-up no semantic source-doc drift remains. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; `git diff --cached --check` after explicit path staging. | Implements alpha-equivalence, canonical keys, raw `CoreIr` normalization utilities, validation, guard-aware binders, generated-origin semantic records, and task-6 tests. Source-derived `.miz` fixtures remain deferred until checker payload extraction and mizar-test stage support exist. Kernel certificate schema, proof acceptance, `VcId`s, proof reuse anchors, VC/proof/kernel handoff remain external/deferred; no external dependency is fabricated. |
| 7. Spec: `elaborator.md` | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: source-map scope, module-table status, visibility/export metadata, stable-choice ownership/reuse, and algorithm statement-owner findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: stable-choice, Fraenkel sethood evidence, exported-definition reuse, theorem/lemma ownership, executable `the`, and ghost-only `Pick` obligations fixed; final re-review no findings. Full implementation review: module implementation status finding fixed; final re-review no findings. Source/doc consistency review: TODO/ledger status and deferred/external wording findings fixed; final re-review no findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Adds English/Japanese elaborator module spec for tasks 8-13. Source implementation, Rust tests, and source-derived `.miz` fixtures are deferred to tasks 8-13 and mizar-test/source-extraction support. External dependencies for artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 8. Core context preparation | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: binder/variable context and checker/resolver provenance findings fixed; TODO/ledger consistency finding fixed; final re-review no blocking/high/medium findings. Test sufficiency review: recovered-site, canonical reference identity, binder class/sort, and definition-boundary coverage findings fixed; final re-review no blocking/high/medium findings. Full implementation review: module ownership, checker diagnostic preservation, generated-origin evidence provenance, and undeclared binder-source findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: only self-referential pending wording was noted as low/admin and fixed; no blocking/high/medium source-doc drift. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 1 context preparation only: checker/resolver-owned context seeds, canonical item registry, definition-boundary registry, generated-origin registry, binder metadata/type-fact context, source-map/diagnostic builders, and deterministic worklist. Type/fact lowering, term/formula lowering, definition/proof/algorithm lowering, source-derived `.miz` fixtures, artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 9. Type and fact lowering | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: cluster fact, subject-table, obligation-seed invariant, and CoreContext composition gaps fixed in the implementation spec; re-review no blocking/high/medium findings. Test sufficiency review: missing-evidence matrix, TypePred subject, view provenance, cluster fact, reconsidering, and standalone carried-obligation coverage findings fixed; final re-review no blocking/high/medium findings. Full implementation review: reconsidering subject, term-sort subject, and accepted cluster fact boundary findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: Step 2 `qua` wording and stale gap wording fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 2 type/fact lowering only as a delta over `CoreContext`: declared binder guards/assumptions, type assertions, attribute conjunctions, mode expansion predicates, accepted cluster fact predicates without registration activation, source/inserted `qua` view provenance, reconsider/narrowing binders, carried checker initial obligations, and missing-evidence diagnostics/deferred seeds. General term/formula lowering, definition/proof/algorithm lowering, source-derived `.miz` fixtures, artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 10. Term and formula lowering | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: generated-origin reuse, Fraenkel obligation preservation, and True/False formula gaps fixed in the task spec; final implementation kept docs synchronized. Test sufficiency review: generated-origin delta/reuse, Fraenkel reuse/obligation invariants, stable-choice Apply functor/args/key, inserted `qua`, `Exists`, guard bypass, and formula child wiring findings fixed; final re-review no blocking/high/medium findings. Full implementation review: guard free-variable derivation, generated-origin param/functor validation, generated delta shape, Fraenkel membership and missing-sethood obligation invariants fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: generated-origin functor/delta/reuse docs, Fraenkel invariants, guard derivation, and English/Japanese sync checked; no blocking/high/medium drift. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 3 term/formula lowering only as a seed-based delta over `CoreContext`: variables, constants, applications, selectors, tuples, set enumerations, predicate/equality/type/connective/quantifier formulas, source/inserted `qua` erasure with view records, stable-choice generated Apply lowering with existing/current-delta origin reuse, Fraenkel generated Apply lowering with sethood evidence, active membership-axiom handoff or explicit already-carried marker, missing-sethood error/deferred seed preservation, and failed-site error nodes. Definition/proof/algorithm lowering, source-derived `.miz` fixtures, artifact schemas, proof acceptance, VC generation, kernel checking, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 11. Definition lowering | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: algorithm-backed body boundary, existing-correctness backrefs, todo unfolding wording, and closure-use deferred wording findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: invalid boundary, deferred/existing correctness, spurious generated dependency, stable-choice reuse/no-delta, and closure-use wording findings fixed; final re-review no blocking/high/medium findings. Full implementation review: existing-correctness backrefs and owner-mismatch validation findings fixed; final re-review no blocking/high/medium findings. Source/doc consistency review: algorithm boundary, existing-correctness backrefs, generated-dependency boundary, and stale metadata findings fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 4 definition lowering only as a delta over `CoreContext` and Step 3 output: definition seeds to `CoreDefinitionTable`, expansion policies, formal binders/guards, guarded/otherwise bodies, generated dependencies reachable through term/formula graphs, correctness obligation seeds and cloned existing seeds with definition/body backrefs, item status/diagnostic deltas for skipped/error bodies, and validation for owner/boundary/symbol/generated/correctness invariants. Source-to-checker extraction of closure-use payloads, explicit unfolding execution, proof acceptance, VC generation, kernel checking, algorithm body lowering, source-derived `.miz` fixtures, artifact schemas, `VcId`s, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 12. Proof-skeleton lowering | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: label-citation scope, step-vs-terminal obligation semantics, proof-status boundary, completion marker, symbol-kind wording, todo checklist, current-goal wording, and explicit terminal-seed boundary findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: active path formula, terminal external citation, explicit error-root, current-goal, sequence-label, and durable terminal-citation coverage findings fixed; final re-review no blocking/high/medium findings. Full implementation review: sequence label propagation, branch isolation, thesis stack, durable terminal citations, and malformed diagnostic ownership findings checked; final re-review no blocking/high/medium findings. Source/doc consistency review: raw Core IR citation-kind validation drift fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 5 proof-skeleton lowering only as a delta over `CoreContext`, Step 3 output, and Step 4 output: theorem/lemma proof seeds to `CoreProofTable`, proof-node trees, current-goal thesis transitions, sequence label propagation, introduced binders, assumptions, labeled steps, local label-scope validation, proof-like symbol/generated/label citations, branch preservation with sibling-scope isolation, terminal `TheoremProof` obligation seeds with proof/node/item/formula/citation backrefs, durable terminal citation storage, proof status records, malformed/missing skeleton diagnostics, and validation for owner/symbol/status/citation/label invariants. Proof acceptance, discharge, VC generation, proof search, kernel checking, artifact schemas, `VcId`s, source-derived `.miz` fixtures, and proof/cache reuse anchors remain deferred/external; no external dependency is fabricated. |
| 13. Algorithm-shell lowering | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: parser-coverage dependency and `CoreAlgorithm` diagnostics wording findings fixed; final re-review no blocking/high/medium findings. Test sufficiency review: malformed statement diagnostics, exact source/provenance checks, boundary status, and control-flow-specific invalid references fixed; final re-review no findings. Full implementation review: initial review no findings; diagnostic aggregation re-review no blocking/high/medium findings. Source/doc consistency review: generated-origin pick wording, Task 13 dependency, and reserved `AlgorithmPick` wording fixed; final re-review no blocking/high/medium findings. | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Implements Step 6 algorithm-shell lowering only as a delta over `CoreContext`, Step 3 output, and Step 5 proof output: algorithm seeds to `CoreAlgorithmTable`, statement-shell trees for let/assign/assert/if/while/match/return/break/continue/pick/error, params/results/contracts/ghost effects, local `Pick` binders without generated-origin emission, source/provenance/source-map preservation, algorithm-shell diagnostics for missing/malformed payloads, parent diagnostic aggregation, and validation for owner/symbol/boundary/binder/term/formula/target invariants. Source-to-checker extraction for full algorithm payloads, parser task 32-34 source-derived coverage, CFG construction, flow diagnostics, VC generation, proof acceptance, kernel checking, artifact schemas, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 14. Spec: `control_flow.md` | ready to commit | recorded after commit in task handoff; self-hash cannot be embedded in the commit that updates this row | Spec/doc review: call-site metadata, `for` metadata, statement-placement source maps, unsupported-call diagnostics, and `ForRange` step/exit metadata findings fixed; final re-review no findings. Test sufficiency review: local table, exact source/provenance, and unsupported external-payload coverage findings fixed; final re-review no findings. Full design review: partial-call `ensures`, context table, local declaration metadata, snapshot/claim boundary, and context fact/effect storage findings fixed; final re-review no findings. Source/doc consistency review: architecture-07 drift, task dependency, and snapshot/claim external-gap findings fixed; final re-review no blocking/high/medium findings. | `git diff --check` before staging; `git diff --cached --check` after explicit path staging. | Spec-only. Adds English/Japanese `control_flow.md` defining `ControlFlowIr`, basic blocks, locals, contexts, contracts/calls, ghost effects, termination, source maps/statement placements, core-to-CFG construction, diagnostics, determinism, and future test obligations. Rust implementation and tests are deferred to tasks 15-17; obligation-seed handoff to task 18. Source-to-checker extraction, parser task 32-34 source-derived coverage, snapshots/claims until explicit checker shells, VC generation, proof acceptance, kernel checking, artifact schemas, `VcId`s, and proof/cache reuse anchors remain external/deferred; no external dependency is fabricated. |
| 15. `ControlFlowIr` construction | not started | pending | pending | pending |  |
| 16. Contracts, ghost effects, termination measures | not started | pending | pending | pending |  |
| 17. Flow diagnostics | not started | pending | pending | pending |  |
| 18. Obligation-seed handoff contract | not started | pending | pending | pending |  |
| 19. Snapshot dumps and corpus contributions | not started | pending | pending | pending |  |
| 20. Determinism suite | not started | pending | pending | pending |  |
| 21. Public-enum forward-compatibility policy | not started | pending | pending | pending |  |
| 22. Source/spec correspondence audit | not started | pending | pending | pending |  |
| 23. Bilingual documentation sync audit | not started | pending | pending | pending |  |
| 24. Module-boundary refactor gate | not started | pending | pending | pending |  |
| Closeout. Crate exit report and quality review | not started | pending | pending | pending | Requires all tasks complete and quality review score >= 90. |
