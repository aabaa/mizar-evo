# task ledger: mizar-core

> 正本は英語です。英語版:
> [../en/task_ledger.md](../en/task_ledger.md)。

この ledger は `mizar-core` 自律 crate 作業の再開点である。task を始める前に
`git status`、`git log`、本表を確認する。task は commit hash、最終 review 結果、
verification 結果、deferred 理由が揃って初めて完了とする。

| task | status | commit | reviews | verification | deferred / notes |
|---|---|---|---|---|---|
| 0. Crate plan | complete | `57fb027c13c0218fa214bdee763be8d0041a0b12` | Spec/doc review: blocking/high/medium finding なし。Test sufficiency review: staged-check gap の medium を修正し、再 review で finding なし。Full implementation review: blocking/high/medium finding なし。Source/doc consistency review: blocking/high/medium finding なし。 | stage 前の `git diff --check` 通過。明示 path stage 後の `git diff --cached --check` 通過。 | docs-only。external gap は `00.crate_plan.md` で分類済み。task-local deferred なし。 |
| 1. Crate scaffold and lint-policy guard | complete | `c3dccec37e8fcd18e522a1cfd4af9502a1ebabda` | Spec/doc review: lint boundary の medium を修正し、再 review で finding なし。Test sufficiency review: alias/API surface の medium を修正し、再 review で finding なし。Full implementation review: blocking/high/medium finding なし。Source/doc consistency review: blocking/high/medium finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | scaffold-only。workspace member、lockfile entry、最小 crate、lint guard を追加。task-local deferred なし。 |
| 2. Spec: `core_ir.md` | complete | `40d9c3b202cb953851a1fccee67030e4e8cafa5a` | Spec/doc review: seed/statement/diagnostic/choice/comprehension/source-map の medium を修正し、再 review で finding なし。Test sufficiency review: blocking/high/medium finding なし。Full implementation review: medium を spec/doc 再 review で解消。Source/doc consistency review: blocking/high/medium finding なし。 | stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | spec-only。Rust test は task 3 に deferred。source-derived `.miz` core fixture は checker payload extraction と mizar-test stage support まで deferred。 |
| 3. Implement `core_ir` data shapes | complete | `b58be7c00a3fcadf96f54ebbe434bd04e42f1718` | Spec/doc review: stable-choice/proof-status/source validation の medium を修正。owner diagnostic / task status doc の low も修正。最終再 review で blocking/high/medium finding なし。Test sufficiency review: invalid reference / seed / debug / error coverage の medium を修正。diagnostic/provenance tie-break coverage の medium と source-map / owner / error mismatch coverage の low を修正し、最終再 review で finding なし。Full implementation review: algorithm owner / generated origin / provenance の medium を修正し、crate doc / source sort の low も修正。Source/doc consistency review: proof-node/provenance/label/status doc の medium を修正し、最終再 review で finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | `core_ir` data shape、validation、deterministic debug rendering、lint guard 拡張を実装。source-derived `.miz` core fixture は checker payload extraction と mizar-test stage support まで deferred。task-local external dependency の仮実装なし。 |
| 4. Spec: `binder_normalization.md` | complete | `6e7c458dd8f7005011fddf4d13577646af35582d` | Spec/doc review: generated-origin、freshness witness、open/depth、closure guard side-condition、sync の finding を修正し、最終再 review で finding なし。Test sufficiency review: composition / malformed coverage finding を修正。nonzero-depth operation、metadata rejection、witness field、generated-origin case を追加し、最終再 review で finding なし。Full design review: closure guard、de Bruijn depth、guard scope、variable metadata、generated-origin finding を修正し、最終再 review で finding なし。Source/doc consistency review: metadata と closure-guard finding を修正し、最終再 review で finding なし。 | stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | spec-only。Rust 実装と test は task 5 と 6 に deferred。source-derived `.miz` fixture は checker payload extraction と mizar-test stage support まで deferred。kernel certificate schema、proof acceptance、`VcId`、proof reuse anchor は external/deferred のまま。external dependency の仮実装なし。 |
| 5. Binder representation and substitution | complete | `300f814eaf83a54f5c8e170d443c18aebd087b95` | Spec/doc review: `NormalizedBinder` scope、task 5 と task 6 の test split、public enum policy finding を修正し、最終再 review で finding なし。Test sufficiency review: nested-binder substitution、rejected composition、closure mismatch、use-site shadowing、metadata coverage、formula helper finding を修正し、最終再 review で finding なし。Full implementation review: closure capture、invalid bound index、metadata validation、replayable freshness、formula helper、checked depth conversion、nested freshening collision finding を修正し、最終再 review で finding なし。Source/doc consistency review: untracked implementation staging warning は明示 path staging で処理。content 再 review は finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | `binder_normalization` representation、substitution、freshness witness、closure expansion、task-5 test を実装。task 6 の alpha-equivalence、canonical form、full normalization utility は deferred。source-derived `.miz` fixture は checker payload extraction と mizar-test stage support まで deferred。kernel certificate schema、proof acceptance、`VcId`、proof reuse anchor は external/deferred のまま。external dependency の仮実装なし。 |
| 6. Alpha-equivalence and normalization utilities | complete | `841ba6e41d743ac0512d7f2518924fa080cfd2e0` | Spec/doc review: task status、public API shape、metadata default、module rustdoc、ambient context frame finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: guard/free-variable、malformed raw evidence、generated-origin matrix、alpha/canonical、idempotence、de Bruijn coverage finding を修正し、最終再 review で finding なし。Full implementation review: ambient context depth、free-variable cache validation、generated-origin guard scan、freshened binder revalidation、platform-stable fresh id、substitution traversal depth、closure formal depth、free-original-var scope finding を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: generated-origin guard scan と TODO/ledger status finding を修正し、最終 follow-up で semantic source-doc drift なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | alpha-equivalence、canonical key、raw `CoreIr` normalization utility、validation、guard-aware binder、generated-origin semantic record、task-6 test を実装。source-derived `.miz` fixture は checker payload extraction と mizar-test stage support まで deferred。kernel certificate schema、proof acceptance、`VcId`、proof reuse anchor、VC/proof/kernel handoff は external/deferred のまま。external dependency の仮実装なし。 |
| 7. Spec: `elaborator.md` | complete | `02905416a6c245a8ac1c0ba697a21328e8a7d298` | Spec/doc review: source-map scope、module table status、visibility/export metadata、stable-choice ownership/reuse、algorithm statement owner finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: stable-choice、Fraenkel sethood evidence、exported-definition reuse、theorem/lemma ownership、executable `the`、ghost-only `Pick` obligation を修正し、最終再 review で finding なし。Full implementation review: module implementation status finding を修正し、最終再 review で finding なし。Source/doc consistency review: TODO/ledger status と deferred/external wording finding を修正し、最終再 review で finding なし。 | stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | spec-only。task 8-13 用に英日 elaborator module spec を追加。source 実装、Rust test、source-derived `.miz` fixture は task 8-13 と mizar-test/source-extraction support まで deferred。artifact schema、proof acceptance、VC generation、kernel checking、`VcId`、proof/cache reuse anchor は external/deferred のまま。external dependency の仮実装なし。 |
| 8. Core context preparation | complete | `a0d6d3f90d729c765c403648b6edb817669d8f98` | Spec/doc review: binder/variable context と checker/resolver provenance の finding を修正。TODO/ledger consistency finding も修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: recovered-site、canonical reference identity、binder class/sort、definition-boundary coverage の finding を修正し、最終再 review で blocking/high/medium finding なし。Full implementation review: module ownership、checker diagnostic preservation、generated-origin evidence provenance、undeclared binder-source finding を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: self-referential pending wording の low/admin のみを修正。blocking/high/medium の source-doc drift なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | Step 1 context preparation のみを実装: checker/resolver-owned context seed、正準 item registry、definition-boundary registry、generated-origin registry、binder metadata/type-fact context、source-map/diagnostic builder、deterministic worklist。type/fact lowering、term/formula lowering、definition/proof/algorithm lowering、source-derived `.miz` fixture、artifact schema、proof acceptance、VC generation、kernel checking、`VcId`、proof/cache reuse anchor は deferred/external のまま。external dependency の仮実装なし。 |
| 9. Type and fact lowering | complete | `860736aadcb72fdc67ef3e57b036a3dd218ac54a` | Spec/doc review: cluster fact、subject table、obligation-seed invariant、CoreContext composition gap を implementation spec で修正し、再 review で blocking/high/medium finding なし。Test sufficiency review: missing-evidence matrix、TypePred subject、view provenance、cluster fact、reconsidering、standalone carried-obligation coverage finding を修正し、最終再 review で blocking/high/medium finding なし。Full implementation review: reconsidering subject、term-sort subject、accepted cluster fact boundary finding を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: Step 2 `qua` wording と stale gap wording を修正し、最終再 review で blocking/high/medium finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | Step 2 type/fact lowering のみを `CoreContext` 上の delta として実装: declared binder guard/assumption、type assertion、attribute conjunction、mode expansion predicate、registration activation なしの accepted cluster fact predicate、source/inserted `qua` view provenance、reconsider/narrowing binder、checker initial obligation carry、missing-evidence diagnostic/deferred seed。general term/formula lowering、definition/proof/algorithm lowering、source-derived `.miz` fixture、artifact schema、proof acceptance、VC generation、kernel checking、`VcId`、proof/cache reuse anchor は deferred/external のまま。external dependency の仮実装なし。 |
| 10. Term and formula lowering | complete | `14e76d67d155b173ee61d45e2227a9cbcb0991df` | Spec/doc review: generated-origin reuse、Fraenkel obligation preservation、True/False formula gap を task spec で修正し、最終実装では docs を同期。Test sufficiency review: generated-origin delta/reuse、Fraenkel reuse/obligation invariant、stable-choice Apply functor/args/key、inserted `qua`、`Exists`、guard bypass、formula child wiring finding を修正し、最終再 review で blocking/high/medium finding なし。Full implementation review: guard free-variable derivation、generated-origin param/functor validation、generated delta shape、Fraenkel membership と missing-sethood obligation invariant を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: generated-origin functor/delta/reuse docs、Fraenkel invariant、guard derivation、英日同期を確認し、blocking/high/medium drift なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | Step 3 term/formula lowering のみを `CoreContext` 上の seed-based delta として実装: variable、constant、application、selector、tuple、set enumeration、predicate/equality/type/connective/quantifier formula、source/inserted `qua` erasure と view record、existing/current-delta origin reuse を伴う stable-choice generated Apply lowering、sethood evidence と active membership-axiom handoff または明示 already-carried marker を伴う Fraenkel generated Apply lowering、missing-sethood error/deferred seed preservation、failed-site error node。definition/proof/algorithm lowering、source-derived `.miz` fixture、artifact schema、proof acceptance、VC generation、kernel checking、`VcId`、proof/cache reuse anchor は deferred/external のまま。external dependency の仮実装なし。 |
| 11. Definition lowering | complete | `9523a0e33b03ea91c1caf44336d984b9b3445c08` | Spec/doc review: algorithm-backed body boundary、existing-correctness backref、todo unfolding wording、closure-use deferred wording の finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: invalid boundary、deferred/existing correctness、spurious generated dependency、stable-choice reuse/no-delta、closure-use wording の finding を修正し、最終再 review で blocking/high/medium finding なし。Full implementation review: existing-correctness backref と owner-mismatch validation の finding を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: algorithm boundary、existing-correctness backref、generated-dependency boundary、stale metadata finding を修正し、最終再 review で blocking/high/medium finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | Step 4 definition lowering のみを `CoreContext` と Step 3 output 上の delta として実装: definition seed から `CoreDefinitionTable`、expansion policy、formal binder/guard、guarded/otherwise body、term/formula graph 経由で到達する generated dependency、definition/body backref 付き correctness obligation seed と cloned existing seed、skipped/error body 用 item status/diagnostic delta、owner/boundary/symbol/generated/correctness invariant の validation。closure-use payload の source-to-checker extraction、明示 unfolding execution、proof acceptance、VC generation、kernel checking、algorithm body lowering、source-derived `.miz` fixture、artifact schema、`VcId`、proof/cache reuse anchor は deferred/external のまま。external dependency の仮実装なし。 |
| 12. Proof-skeleton lowering | complete | `e4afecbf874a6448ffc68b3b1b2bd98a4b7b81d6` | Spec/doc review: label-citation scope、step-vs-terminal obligation semantics、proof-status boundary、completion marker、symbol-kind wording、todo checklist、current-goal wording、explicit terminal-seed boundary の finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: active path formula、terminal external citation、explicit error-root、current-goal、sequence-label、durable terminal-citation coverage finding を修正し、最終再 review で blocking/high/medium finding なし。Full implementation review: sequence label propagation、branch isolation、thesis stack、durable terminal citation、malformed diagnostic ownership を確認し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: raw Core IR citation-kind validation drift を修正し、最終再 review で blocking/high/medium finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | Step 5 proof-skeleton lowering のみを `CoreContext`、Step 3 output、Step 4 output 上の delta として実装: theorem/lemma proof seed から `CoreProofTable`、proof-node tree、current-goal thesis transition、sequence label propagation、introduced binder、assumption、labeled step、local label-scope validation、proof-like symbol/generated/label citation、sibling scope を分離する branch preservation、proof/node/item/formula/citation backref 付き terminal `TheoremProof` obligation seed、durable terminal citation storage、proof status record、malformed/missing skeleton diagnostic、owner/symbol/status/citation/label invariant の validation。proof acceptance、discharge、VC generation、proof search、kernel checking、artifact schema、`VcId`、source-derived `.miz` fixture、proof/cache reuse anchor は deferred/external のまま。external dependency の仮実装なし。 |
| 13. Algorithm-shell lowering | complete | `23d420b3e12f4c0069d4e60292f892d98efe72c6` | Spec/doc review: parser coverage dependency と `CoreAlgorithm` diagnostics 文言の finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: malformed statement diagnostic、exact source/provenance check、boundary status、control-flow-specific invalid reference の finding を修正し、最終再 review で finding なし。Full implementation review: 初回 review は finding なし。diagnostic aggregation 追加後の再 review も blocking/high/medium finding なし。Source/doc consistency review: generated-origin pick 文言、Task 13 dependency、予約済み `AlgorithmPick` 文言を修正し、最終再 review で blocking/high/medium finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | Step 6 algorithm-shell lowering のみを `CoreContext`、Step 3 output、Step 5 proof output 上の delta として実装: algorithm seed から `CoreAlgorithmTable`、let/assign/assert/if/while/match/return/break/continue/pick/error の statement-shell tree、parameter/result/contract/ghost effect、generated origin を emit しない local `Pick` binder、source/provenance/source-map preservation、missing/malformed payload 用 algorithm-shell diagnostic、parent diagnostic aggregation、owner/symbol/boundary/binder/term/formula/target invariant の validation。full algorithm payload の source-to-checker extraction、parser task 32-34 の source-derived coverage、CFG construction、flow diagnostic、VC generation、proof acceptance、kernel checking、artifact schema、`VcId`、proof/cache reuse anchor は external/deferred のまま。external dependency の仮実装なし。 |
| 14. Spec: `control_flow.md` | complete | `93a66e3afa77ebb3794ca9285b2c9fa48e720b11` | Spec/doc review: call-site metadata、`for` metadata、statement-placement source map、unsupported-call diagnostic、`ForRange` step/exit metadata の finding を修正し、最終再 review で finding なし。Test sufficiency review: local table、exact source/provenance、unsupported external-payload coverage の finding を修正し、最終再 review で finding なし。Full design review: partial-call `ensures`、context table、local declaration metadata、snapshot/claim boundary、context fact/effect storage の finding を修正し、最終再 review で finding なし。Source/doc consistency review: architecture-07 drift、task dependency、snapshot/claim external-gap の finding を修正し、最終再 review で blocking/high/medium finding なし。 | stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | spec-only。英日 `control_flow.md` を追加し、`ControlFlowIr`、basic block、local、context、contract/call、ghost effect、termination、source map / statement placement、core-to-CFG construction、diagnostic、determinism、将来 test obligation を定義する。Rust 実装と test は task 15-17、obligation-seed handoff は task 18 に deferred。source-to-checker extraction、parser task 32-34 source-derived coverage、explicit checker shell ができるまでの snapshot/claim、VC generation、proof acceptance、kernel checking、artifact schema、`VcId`、proof/cache reuse anchor は external/deferred のまま。external dependency の仮実装なし。 |
| 15. `ControlFlowIr` construction | complete | `73a5786581c745a9d87ada892c0f6fa8329b987f` | Spec/doc review: hidden local、unsupported local declaration、illegal break/continue、最小 context、deterministic rendering、diagnostic-id shape、structural diagnostic test、crate-plan scope の finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: context、CFG edge/block shape、local table/source-map fidelity、structural diagnostic source/ref coverage の finding を修正し、最終再 review で finding なし。Full implementation review: fallthrough closure、break context join、loop-carried context propagation、unreachable-join filtering の finding を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: 最終 review で blocking/high/medium finding なし。英日 docs、task plan、todo、ledger、public module export policy は同期済み。 | `cargo fmt`; `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | phase 10 `ControlFlowIr` construction のみを実装: public `control_flow` module、`CoreAlgorithm` ごとの deterministic flow table/map、let/assign/assert/if/while/match/return/break/continue/pick/error 用 block/terminator、parameter/result/let/pick と unsupported-role metadata を持つ local table、最小 `ProgramContext` propagation、source map と statement placement、unsupported local declaration と illegal break/continue の structural diagnostic、phase 9 error carry-through、byte-stable debug rendering、lint-policy export guard、fallthrough、loop break/continue/carry、unreachable join、source fidelity、deterministic order の regression coverage。contract/ghost/termination semantic attachment、use-before-assignment/unreachable reporting などの flow diagnostic、obligation-seed handoff、source-derived `for`/snapshot/claim payload、source-to-checker extraction、VC generation、proof acceptance、kernel checking、artifact schema、`VcId`、proof/cache reuse anchor は external/deferred のまま。external dependency の仮実装なし。 |
| 16. Contracts, ghost effects, termination measures | complete | `07d802f52b244d92a4f332d3fe606b327499f956` | Spec/doc review: call-site deferred shape、ghost-table overstatement、assertion timing、status document の finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: loop-invariant context fact、exact invariant/decreasing placement、requirement/assertion fact exactness、initialized ghost-let assignment split の finding を修正し、最終再 review で finding なし。Full implementation review: aggregate contract assertions/invariants と assertion-timing の finding を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: 最終 pass で blocking/high/medium finding なし。英日 docs、todo、ledger、Task 16 source shape は同期済み。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | phase-10 CFG 上の Task 16 contract/ghost/termination attachment を実装: entry `requires` fact/site、aggregate `CoreContractSet.assertions` と `invariants` の `AlgorithmContract` metadata、statement assertion successor fact/site、explicit/implicit return の `ensures`、loop invariant header/backedge/break/continue site と context fact、algorithm/loop decreasing site、decreasing payload がない場合の partial algorithm/loop termination site、initialized ghost `let` を含む runtime/ghost assignment-effect と pick-local split。checker-owned call contract payload、flow diagnostic、obligation-seed handoff、source-derived `for`/snapshot/claim payload、source-to-checker extraction、VC generation、proof acceptance、kernel checking、artifact schema、`VcId`、proof/cache reuse anchor は external/deferred のまま。external dependency の仮実装なし。 |
| 17. Flow diagnostics | complete | `004c837170b1088330b052e4718bea42bb2b3ecb` | Spec/doc review: 診断範囲の絞り込み、site/context matrix、diagnostic shape/source ordering、quantifier shadowing の finding を修正し、最終再 review で finding なし。Test sufficiency review: statement-owned site coverage、quantifier left-to-right coverage、stable source ordering、deferred non-fabrication、unreachable block-payload finding を修正し、最終再 review で finding なし。Full implementation review: diagnostic ordering finding を source/block/class sorting と id remapping で修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: 最終 review で blocking/high/medium finding なし。英日 docs、todo、ledger、Task 17 source shape は同期済み。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | 具体的な Task 17 flow diagnostic のみを実装: reachable な statement-owned term/formula 上の `UseBeforeAssignment`、unreachable statement 上の `UnreachableStatement`、安定した diagnostic sorting/remapping、source-map anchor、in-scope site と quantifier binder scope の regression coverage。immutable assignment、ghost leakage、call/contract instantiation、pattern/snapshot/claim payload、alias/lvalue precision、obligation seed、source-to-checker extraction、VC/proof/kernel/artifact schema、`VcId`、proof/cache reuse diagnostic は external/deferred のまま。external dependency の仮実装なし。 |
| 18. Obligation-seed handoff contract | complete | `64ce704102663d2f47c3454a22f9c566e7d034ea` | Spec/doc review: handoff shape、`ObligationSeed.diagnostics`、crate-plan scope の medium finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: loop/partial/continue coverage、exact source/provenance/site/order、`core_refs`、generated/deferred/error existing seed、break-exit invariant、semantic origin、empty context の finding を修正し、最終 review で finding なし。Full implementation review: blocking/high/medium finding なし。Source/doc consistency review: stale TODO wording を修正し、最終 follow-up で finding なし。 | `cargo fmt --check`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; stage 前の `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | Task 18 seed handoff view を実装: 既存 core seed は元 backref、label、path、source/provenance、diagnostic、status、ref を保って clone する。明示 flow metadata から deferred な algorithm contract、termination、ghost-erasure seed を生成し、CFG-local site ref、正確な source map、決定的順序を持たせ、VC context は仮造しない。Deferred/external: concrete `VcId`、`ObligationAnchor`、VC/anchor fingerprint、call/result substitution、具体的な assertion/invariant VC context、termination well-foundedness schema、ghost-erasure proof shape、source-derived call/pattern/snapshot/claim payload、source-to-checker extraction、VC/proof/kernel/artifact schema、proof acceptance、proof/cache reuse は external/deferred のまま。external dependency の仮実装なし。 |
| 19. Snapshot dumps and corpus contributions | complete | `45a8762baaea1d58945abc2fde291302f5c10e36` | Spec/doc review: blocking/high/medium finding なし。reviewer は、mizar-test が parse-only SurfaceAst snapshot のみを公開し、type_elaboration は payload extraction gap で止まり、proof_verification runner がないため、docs/metadata-only の deferred traceability が適切であると確認。Test sufficiency review: finding なし。deferred traceability row には metadata validation で十分であり、Rust source、runner behavior、active fixture を変更していないため Rust test は不要と確認。Full implementation review: blocking/high/medium finding なし。traceability row は empty tests と具体的 external-dependency reason を持つ deferred snapshot coverage であり、docs は active runner support を過大主張していない。Source/doc consistency review: finding なし。英日 crate plan、todo、ledger、traceability metadata は同期しており、active runner support を過大主張していない。 | `git diff --check`; `cargo run -p mizar-test -- plan --validation-mode metadata` (0 errors、`spec.en.algorithm.vc.assignment_loop_exits` の既存 warning 1 件); 明示 path stage 後の `git diff --cached --check`。 | metadata-only。`CoreIr` type_elaboration baseline と `ControlFlowIr` proof_verification baseline の明示 deferred snapshot traceability を追加する。External/deferred: mizar-test の type_elaboration / proof_verification snapshot comparison support、source-to-checker/source-to-core payload extraction、proof/algorithm payload flow、VC/proof/kernel downstream seam。runner support や source-derived baseline は仮造しない。 |
| 20. Determinism suite | complete | `6bfe55b5f1a8cf4b0bbd8ee7f9a8184508254e54` | Spec/doc review: public-API と `ResolvedTypedAst` の過大主張 finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: fresh-run structural equality と binder-guard coverage の finding を修正し、最終再 review で finding なし。Full implementation review: follow-up 後も blocking/high/medium finding なし。structural fixture equality と guarded quantifier path は妥当。Source/doc consistency review: finding なし。英日 crate plan、todo、ledger、新規 determinism suite は public-API scope と deferred source-derived determinism で一致している。 | `cargo fmt --check`; `cargo test -p mizar-core --test determinism_suite`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | fresh `CoreIr` fixture rebuild、structural な `CoreIr` / `ControlFlowOutput` / `ObligationSeedHandoff` equality、byte-stable debug rendering、deterministic flow/handoff ordering、renamed guarded quantifier の binder canonicalization を cover する public-API determinism integration suite を追加する。完全な source-derived `ResolvedTypedAst` determinism、source-to-checker extraction、corpus runner support、VC/proof/kernel seam、downstream artifact schema は external/deferred のまま。source-derived payload の仮造なし。 |
| 21. Public-enum forward-compatibility policy | complete | `184c0f113eded9badff51957ef92ad60592a852d` | Spec/doc review: blocking/high/medium finding なし。low の guard precision、internal exhaustive match note、TODO/ledger timing finding を反映し、最終再 review で finding なし。Test sufficiency review: exact source/doc set equality と nested/re-export public-surface guard の finding を修正し、最終再 review で finding なし。Full implementation review: stale-doc medium finding を修正し、最終再 review で blocking/high/medium finding なし。Source/doc consistency review: enum-list drift なし。ledger pending-status finding は最終更新で修正済み。 | `cargo fmt --check`; `cargo test -p mizar-core public_core_enums_are_forward_compatible_and_documented`; `cargo test -p mizar-core --test lint_policy`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | 現在の public `mizar-core` enum をすべて downstream forward-compatible として分類し、`#[non_exhaustive]` で guard する。module ごとの英日 policy table を記録し、source attribute、unguarded nested/re-export public enum surface、owning EN/JA section の exact source/policy-table equality を検査する lint-policy drift guard を追加する。`mizar-core` が所有する exhaustive public enum exception はない。将来の public enum 追加時は owning spec と guard shape の更新が必要。 |
| 22. Source/spec correspondence audit | complete | `6b62bc8fbf028039fae525b9b54abca5ac6e4704` | Spec/doc review: `source_undocumented_behavior` と follow-up-register shape の medium finding を修正し、最終再 review で blocking/high/medium finding なし。Test sufficiency review: public item inventory guard、final verification、follow-up detail の finding を修正し、最終再 review で blocking/high/medium finding なし。Full implementation review: blocking/high/medium finding なし。Source/doc consistency review: blocking/high/medium finding なし。low の staging-scope note は Task 22 path の明示 staging で対応する。 | `cargo fmt --check`; `cargo test -p mizar-core source_spec_audit_covers_public_modules_and_deferred_gaps`; `cargo test -p mizar-core --test lint_policy`; `cargo test -p mizar-core`; `cargo clippy -p mizar-core --all-targets -- -D warnings`; `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | audit-only plus lint guard。item-level public API inventory、source/spec/test/deferred trace、`source_undocumented_behavior` pass、CORE-AUDIT follow-up register を持つ英日 source/spec correspondence audit を追加。External/deferred: source-to-checker extraction、active type_elaboration/proof_verification snapshot、artifact schema、proof acceptance、VC generation、kernel checking、concrete `VcId` / `ObligationAnchor`、proof/cache reuse、source-derived call/result substitution、pattern、snapshot、claim、public diagnostic code-space は external/deferred のまま。behavior や external dependency の仮実装なし。 |
| 23. Bilingual documentation sync audit | complete | `260e41ed8a1a6c8c8d3da4bf4b53b44a0b451d39` | Spec/doc review: low の paired-file inventory と crate-plan result note を修正後、blocking/high/medium finding なし。Test sufficiency review: blocking/high/medium finding なし。docs-only diff check で十分で、明示 staging 後の staged diff check が必要。Full implementation review: blocking/high/medium finding なし。Source/doc consistency review: blocking/high/medium finding なし。low の ledger-placeholder note は staging 前に修正済み。 | `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | docs-only。現在の mizar-core doc pair 全体を対象にした英日 bilingual sync audit を追加し、blocking bilingual documentation drift がないことを記録する。future `module_boundary_audit.md` と `crate_exit_report.md` companion check は owning task に deferred。Rust source、`.miz` fixture、expectation、traceability metadata は変更しない。 |
| 24. Module-boundary refactor gate | complete | `6c7268d083a71e261c521bad9e90e315173c9bee` | Spec/doc review: medium の wording と bilingual-audit coverage finding を修正後、blocking/high/medium finding なし。low の ledger/todo polish は修正済み。Test sufficiency review: blocking/high/medium finding なし。docs-only diff check で十分で、明示 staging 後の staged diff check が必要。Full implementation review: stale active `CORE-BILINGUAL-G001` classification を resolved pair updates へ移した後、blocking/high/medium finding なし。low の ledger-placeholder note は staging 前に修正済み。Source/doc consistency review: blocking/high/medium finding なし。low の pending-review ledger note は staging 前に修正済み。 | `git diff --check`; 明示 path stage 後の `git diff --cached --check`。 | audit-only。英日 module-boundary audit を追加し、大きいが public module spec と一致する cohesive な source file を記録する。optional private helper extraction は future move-only follow-up に deferred。Rust source/API/behavior は変更しない。external/downstream seam は external/deferred のままで、placeholder module は仮造しない。 |
| Closeout. Crate exit report and quality review | ready to commit | pending; closeout commit hash は self-hash をこの commit に埋め込めないため final handoff で記録する | Spec/doc review: corrected closeout specification が completion を hard gate 条件付きにし、ledger commit-hash backfill を要求し、crate-exit template に一致した後、blocking/high/medium finding なし。Test sufficiency review: blocking/high/medium finding なし。docs-only closeout scope には broad workspace verification と diff check で十分。Full implementation review: Japanese Task 3 ledger verification drift と stale closeout wording を修正後、blocking/high/medium finding なし。Source/doc consistency review: blocking/high/medium finding なし。task hash、bilingual closeout sync、deferred seam classification、docs-only scope は一致。Read-only quality review: hard gate failure、blocking/high/medium finding、score cap なし。valid score 94/100。 | `cargo fmt --check` passed; `cargo clippy --all-targets --all-features -- -D warnings` passed; `cargo test` passed; `git diff --check` passed; 明示 closeout path staging 後の `git diff --cached --check` passed。 | docs-only closeout。英日 exit report を追加し、ledger に task commit hash を backfill し、closeout bilingual audit row を解消し、final hard-gate / quality evidence を記録する。Rust source、`.miz` fixture、expectation、`doc/spec`、traceability metadata は変更しない。source-extraction、VC/proof/kernel/artifact、diagnostic-code、optional move-only split seam は external/deferred のまま。 |
