schema_version = 1
id = "fail_types_qua_unrelated_struct_001"
kind = "fail"
stage = "advanced_semantics"
domain = "types.conversion"
source = "fail_types_qua_unrelated_struct_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "qua_target_not_reachable"
stable_detail_key = "conversion.qua.target_not_reachable_by_widening"
diagnostic_codes = []
spec_refs = [
  "spec.en.13.qua.widening_only.semantic",
]
notes = "Inactive advanced_semantics seed: OneStr does not inherit from OtherStr, so `(a qua OtherStr)` has no inherit-chain or cluster widening justification; nominal, not structural, compatibility governs qua views (spec 13.6.1, 13.6.4)."
