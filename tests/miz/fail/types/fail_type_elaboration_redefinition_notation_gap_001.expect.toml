schema_version = 1
id = "fail_type_elaboration_redefinition_notation_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_redefinition_notation_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "redefinition_notation_payload_extraction_gap"
stable_detail_key = "type_elaboration.external_dependency.ast_payload_extraction"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.external_dependency.ast_payload_extraction",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.11.symbol_management.redefinition_notation.type_elaboration_payload_gap",
  "spec.en.19.redefinition.type_elaboration_payload_gap",
  "spec.en.checker.type_elaboration.payload_extraction",
  "spec.en.checker.type_elaboration.payload_extraction.redefinition_notation_gap",
]
notes = "Active type_elaboration runner boundary coverage for checker task 96: top-level and definition-local synonym/antonym aliases plus attr/pred/func redefinition surfaces are parser/resolver executable using documented parser.type_fixtures symbols, but the checker-owned source bridge lacks real redefinition payload extraction, notation alias relation payloads, redefinition target inference, coherence proof-obligation payloads, overload candidate payloads, recorded facts, CoreIr/ControlFlowIr/VC/proof payloads, and formula_statement or advanced_semantics runner support, so it must stay on the source-to-checker payload extraction gap. This sidecar deliberately does not credit Chapter 11 alias semantic resolution or Chapter 19 redefinition semantics."
