schema_version = 1
id = "fail_type_elaboration_attributed_reserve_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_attributed_reserve_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "imported_empty_attribute_evidence_payload_gap"
stable_detail_key = "type_elaboration.checker.checker.declaration.deferred.evidence_query"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.checker.declaration.deferred.evidence_query",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.03.type_expressions.imported_empty_attribute_provenance_bridge",
  "spec.en.06.attributes.imported_empty_attribute_evidence_payload_gap",
  "spec.en.11.symbol_management.imported_empty_attribute_provenance_bridge",
  "spec.en.12.modules.imported_empty_attribute_provenance_bridge",
  "spec.en.checker.type_elaboration.imported_empty_attribute_provenance_bridge",
  "spec.en.checker.type_elaboration.source_attribute_payload",
]
notes = "Active type_elaboration runner boundary coverage for checker tasks 85 and 250: source containing imported attribute symbol `empty` from the documented parser.type_fixtures import summary and `reserve x for non empty set;` co-installs the authenticated negative raw Task-249/250 source handoffs with the existing imported AttributeInput declaration route, preserving the exact written `non` occurrence. The existing checker evidence-query rejection remains authoritative because attributed-type existential/evidence semantics, acceptance, attribute truth, imported module AST extraction, and CoreIr/ControlFlowIr/VC/proof payloads remain unavailable."
