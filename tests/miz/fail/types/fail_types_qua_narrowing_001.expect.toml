schema_version = 1
id = "fail_types_qua_narrowing_001"
kind = "fail"
stage = "advanced_semantics"
domain = "types.conversion"
source = "fail_types_qua_narrowing_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "qua_narrowing_rejected"
stable_detail_key = "conversion.qua.narrowing_rejected"
diagnostic_codes = []
spec_refs = [
  "spec.en.13.qua.widening_only.semantic",
]
notes = "Inactive advanced_semantics seed: `s qua keen set` claims an attribute not derivable from cl(set); qua is proof-free widening only, narrowing needs reconsider (spec 13.6.4, 8.2.2). The attribute being semantically trivial does not make the static view valid."
