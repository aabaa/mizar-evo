schema_version = 1
id = "fail_type_elaboration_source_type_application_payload_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_source_type_application_payload_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "source_evidence_dependency_input_missing"
stable_detail_key = "type_elaboration.checker.source_evidence.dependency_input_missing"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.source_evidence.dependency_input_missing",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.checker.type_elaboration.source_evidence_request_payload",
  "spec.en.checker.type_elaboration.source_type_application_payload",
]
notes = "Tasks 249 and 251 bounded source-type/evidence-request handoff coverage. The exact broad source publishes syntax-free checker-owned outer application, recursive expression/head, ordered TermSite/TypeSite/QuaSite rows, and eight missing source-evidence requests (five mode-expansion and three structure-inhabitation) through TypedAst and ResolvedTypedAst. The runner-owned missing-dependency detail grants no supplied or accepted evidence, normalization, term or qua binding selection, facts, declaration acceptance, proof semantics, or downstream IR credit."
