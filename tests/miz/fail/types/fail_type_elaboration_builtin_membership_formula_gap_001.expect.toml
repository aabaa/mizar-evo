schema_version = 1
id = "fail_type_elaboration_builtin_membership_formula_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_builtin_membership_formula_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "numeric_type_payload_extraction_gap"
stable_detail_key = "type_elaboration.checker.checker.term.external.numeric_type_payload"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.checker.formula.term.partial",
  "type_elaboration.checker.checker.term.external.numeric_type_payload",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.13.term_expressions.type_elaboration_payload_gap",
  "spec.en.14.formulas.term_formula_payload_gap",
  "spec.en.16.theorems_and_proofs.term_formula_payload_gap",
  "spec.en.checker.type_elaboration.payload_extraction",
  "spec.en.checker.type_elaboration.payload_extraction.builtin_membership_formula_gap",
]
notes = "Active type_elaboration runner bridge coverage for checker task 108 superseding the task 100 generic extraction-gap boundary: the exact builtin membership theorem formula with numeral term operands is parser/resolver executable and now produces real checker TermInput/FormulaInput payloads from the source AST, but numeral terms still lack real numeric type payloads and the membership formula remains partial. Membership operand expected-type construction/checking, recorded facts, theorem acceptance, CoreIr/ControlFlowIr/VC/proof payloads, and the dedicated formula_statement runner remain deferred."
