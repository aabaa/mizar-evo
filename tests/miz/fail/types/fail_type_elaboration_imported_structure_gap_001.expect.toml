schema_version = 1
id = "fail_type_elaboration_imported_structure_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_imported_structure_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "imported_structure_type_head_payload_gap"
stable_detail_key = "type_elaboration.external_dependency.ast_payload_extraction"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.external_dependency.ast_payload_extraction",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.03.type_expressions.imported_structure_type_head_payload_gap",
  "spec.en.05.structures.imported_structure_type_head_payload_gap",
  "spec.en.11.symbol_management.imported_structure_type_head_payload_gap",
  "spec.en.12.modules.imported_structure_type_head_payload_gap",
  "spec.en.checker.type_elaboration.payload_extraction",
  "spec.en.checker.type_elaboration.payload_extraction.imported_structure_gap",
]
notes = "Active type_elaboration runner boundary coverage for checker task 78: source containing an imported structure symbol from the documented parser.type_fixtures import summary and a reserve use such as `R` is parser/resolver executable, but the checker-owned source bridge lacks real imported structure provenance, structure type-head payload extraction, and base-shape/constructor-witness evidence, so it must stay on the source-to-checker payload extraction gap instead of accepting the imported structure type or promoting CoreIr/ControlFlowIr/VC/proof payloads."
