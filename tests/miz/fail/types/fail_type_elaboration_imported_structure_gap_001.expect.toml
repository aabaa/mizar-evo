schema_version = 1
id = "fail_type_elaboration_imported_structure_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_imported_structure_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "imported_structure_evidence_payload_gap"
stable_detail_key = "type_elaboration.checker.checker.declaration.deferred.evidence_query"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.checker.declaration.deferred.evidence_query",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.03.type_expressions.imported_structure_provenance_bridge",
  "spec.en.05.structures.imported_structure_evidence_payload_gap",
  "spec.en.11.symbol_management.imported_structure_provenance_bridge",
  "spec.en.12.modules.imported_structure_provenance_bridge",
  "spec.en.checker.type_elaboration.imported_structure_provenance_bridge",
]
notes = "Active type_elaboration runner boundary coverage for checker task 83: source containing imported structure symbol `R` from the documented parser.type_fixtures import summary and a reserve use such as `R` is parser/resolver executable, and the checker-owned source bridge carries the real imported structure provenance/type-head payload to declaration checking. It still lacks imported module AST extraction and base-shape/constructor-witness evidence, so it must fail closed on the checker evidence-query diagnostic instead of accepting the imported structure type or promoting CoreIr/ControlFlowIr/VC/proof payloads."
