schema_version = 1
id = "fail_types_reconsider_omitted_justification_001"
kind = "fail"
stage = "advanced_semantics"
domain = "types.conversion"
source = "fail_types_reconsider_omitted_justification_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "reconsider_omitted_justification_requires_proof"
stable_detail_key = "conversion.reconsider.narrowing_requires_proof"
diagnostic_codes = []
spec_refs = [
  "spec.en.08.reconsider.omitted_justification.semantic",
]
notes = "Inactive advanced_semantics seed: omitted `reconsider` justification is accepted only when current widening, inheritance/view, cluster-closure, or recorded local type facts discharge the target type. `s` has type `set`, while `keen set` requires an attribute not already derivable, so the statement must report `type.narrowing_requires_proof` rather than silently invoking proof search."
