schema_version = 1
id = "fail_type_elaboration_registration_block_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_registration_block_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "registration_block_payload_extraction_gap"
stable_detail_key = "type_elaboration.external_dependency.ast_payload_extraction"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.external_dependency.ast_payload_extraction",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.17.clusters_and_registrations.type_elaboration_payload_gap",
  "spec.en.checker.type_elaboration.payload_extraction",
  "spec.en.checker.type_elaboration.payload_extraction.registration_block_gap",
]
notes = "Active type_elaboration runner boundary coverage for checker task 95: a top-level registration block containing existential and conditional clusters is parser/resolver executable, but the checker-owned source bridge lacks real registration-item payload extraction, correctness-condition/proof-obligation payloads, accepted activation/evidence status, cluster/reduction semantics, recorded facts, CoreIr/ControlFlowIr/VC/proof payloads, and formula_statement or advanced_semantics runner support, so it must stay on the source-to-checker payload extraction gap. This sidecar deliberately does not credit Chapter 17 semantic cluster/reduction rows."
