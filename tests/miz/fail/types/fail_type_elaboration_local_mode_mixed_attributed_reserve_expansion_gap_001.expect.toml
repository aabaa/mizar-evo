schema_version = 1
id = "fail_type_elaboration_local_mode_mixed_attributed_reserve_expansion_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_local_mode_mixed_attributed_reserve_expansion_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "missing_mode_expansion_payload"
stable_detail_key = "type_elaboration.checker.checker.type.external.mode_expansion_payload"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.checker.type.external.mode_expansion_payload",
  "type_elaboration.checker.checker.type.recovery",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.checker.type_elaboration.payload_extraction.attributed_local_mode_expansion_gap",
  "spec.en.checker.type_elaboration.payload_extraction.local_mode_expansion_gap",
]
notes = "Active type_elaboration runner coverage for checker tasks 54 and 55: a same-module attributed local mode reserve use is deliberately paired with a bare use of the same local mode head, so the runner withholds the otherwise available bare RHS expansion for that symbol and checker normalization fails closed on the missing mode-expansion payload."
