schema_version = 1
id = "fail_type_elaboration_imported_mode_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_imported_mode_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "imported_mode_expansion_payload_gap"
stable_detail_key = "type_elaboration.checker.checker.type.external.mode_expansion_payload"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.checker.type.external.mode_expansion_payload",
  "type_elaboration.checker.checker.type.recovery",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.03.type_expressions.imported_mode_provenance_bridge",
  "spec.en.07.modes.imported_mode_expansion_payload_gap",
  "spec.en.11.symbol_management.imported_mode_provenance_bridge",
  "spec.en.12.modules.imported_mode_provenance_bridge",
  "spec.en.checker.type_elaboration.imported_mode_provenance_bridge",
]
notes = "Active type_elaboration runner boundary coverage for checker task 82: source containing an imported mode symbol from the documented parser.type_fixtures import summary and a reserve use such as `TypeCaseMode` is parser/resolver executable, and the checker-owned source bridge carries the real imported mode provenance/type-head payload to type normalization. It still lacks real imported mode-definition/module-summary expansion payload extraction, so it must stay on the checker missing mode-expansion diagnostic instead of accepting the imported mode type or promoting CoreIr/ControlFlowIr/VC/proof payloads."
