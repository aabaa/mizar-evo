schema_version = 1
id = "fail_type_elaboration_formula_connective_quantifier_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_formula_connective_quantifier_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "formula_connective_quantifier_payload_extraction_gap"
stable_detail_key = "type_elaboration.external_dependency.ast_payload_extraction"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.external_dependency.ast_payload_extraction",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.14.formulas.type_elaboration_payload_gap",
  "spec.en.16.theorems_and_proofs.type_elaboration_payload_gap",
  "spec.en.checker.type_elaboration.payload_extraction",
  "spec.en.checker.type_elaboration.payload_extraction.formula_connective_quantifier_gap",
]
notes = "Active type_elaboration runner boundary coverage for checker task 99: a theorem formula using Chapter 14 implication, universal quantification, and formula negation is parser/resolver executable, but the checker-owned source bridge lacks real formula payload extraction, quantifier binder/context payloads, formula checking, recorded facts, theorem acceptance, CoreIr/ControlFlowIr/VC/proof payloads, and a dedicated formula_statement runner, so it must stay on the source-to-checker payload extraction gap."
