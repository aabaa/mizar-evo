schema_version = 1
id = "fail_type_elaboration_term_formula_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_term_formula_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "numeric_type_payload_extraction_gap"
stable_detail_key = "type_elaboration.checker.checker.term.external.numeric_type_payload"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.checker.formula.term.partial",
  "type_elaboration.checker.checker.term.external.numeric_type_payload",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.13.term_expressions.type_elaboration_payload_gap",
  "spec.en.14.formulas.term_formula_payload_gap",
  "spec.en.16.theorems_and_proofs.term_formula_payload_gap",
  "spec.en.checker.type_elaboration.payload_extraction",
  "spec.en.checker.type_elaboration.payload_extraction.term_formula_gap",
  "spec.en.checker.type_elaboration.source_primary_term_payload",
]
notes = "Active type_elaboration runner bridge coverage for checker task 106 superseding the task 87 generic extraction-gap boundary: a theorem formula with numeral term operands and builtin equality is parser/resolver executable and now produces real checker TermInput/FormulaInput payloads from source AST, but numeral terms still lack real numeric type payloads and the equality formula remains partial. Task 252 additionally transports the two root Numeral source rows and their two unresolved numeric-type requests through immutable TypedAst-to-ResolvedTypedAst ownership; this source-only handoff does not provide a numeric type, semantic result, fact, or acceptance and does not change the existing failure detail. Recorded facts, theorem acceptance, CoreIr/ControlFlowIr/VC/proof payloads, and the dedicated formula_statement runner remain deferred."
