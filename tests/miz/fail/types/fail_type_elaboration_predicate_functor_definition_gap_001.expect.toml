schema_version = 1
id = "fail_type_elaboration_predicate_functor_definition_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_predicate_functor_definition_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "definition_declaration_payload_extraction_gap"
stable_detail_key = "type_elaboration.external_dependency.ast_payload_extraction"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.external_dependency.ast_payload_extraction",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.09.predicate_definitions.type_elaboration_payload_gap",
  "spec.en.10.functor_definitions.type_elaboration_payload_gap",
  "spec.en.checker.type_elaboration.payload_extraction",
  "spec.en.checker.type_elaboration.payload_extraction.predicate_functor_definition_gap",
]
notes = "Active type_elaboration runner boundary coverage for checker task 90: predicate and functor definitions are parser/resolver executable, but the checker-owned source bridge lacks real predicate/functor definition declaration payload extraction, definition-local context, definiens formula/term payloads, overload payloads, facts, CoreIr/ControlFlowIr/VC/proof payloads, and a dedicated formula_statement runner, so it must stay on the source-to-checker payload extraction gap."
