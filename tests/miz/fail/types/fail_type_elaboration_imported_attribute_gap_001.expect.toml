schema_version = 1
id = "fail_type_elaboration_imported_attribute_gap_001"
kind = "fail"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "fail_type_elaboration_imported_attribute_gap_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "external_dependency_gap"
rejection_reason = "imported_attribute_evidence_payload_gap"
stable_detail_key = "type_elaboration.checker.checker.declaration.deferred.evidence_query"
diagnostic_codes = []
diagnostic_payloads = [
  "type_elaboration.checker.checker.declaration.deferred.evidence_query",
]
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.03.type_expressions.imported_attribute_provenance_bridge",
  "spec.en.06.attributes.imported_attribute_evidence_payload_gap",
  "spec.en.11.symbol_management.imported_attribute_provenance_bridge",
  "spec.en.12.modules.imported_attribute_provenance_bridge",
  "spec.en.checker.type_elaboration.imported_attribute_provenance_bridge",
]
notes = "Active type_elaboration runner boundary coverage for checker task 84: source containing imported attribute symbol `TypeCaseAttr` from the documented parser.type_fixtures import summary and a reserve type such as `TypeCaseAttr set` is parser/resolver executable, and the checker-owned source bridge carries the real imported attribute provenance/AttributeInput payload to declaration checking. It still lacks imported module AST extraction and attributed-type existential/evidence payloads, so it must fail closed on the checker evidence-query diagnostic instead of accepting the attributed type or promoting CoreIr/ControlFlowIr/VC/proof payloads."
