schema_version = 1
id = "fail_proof_verification_mode_sethood_unprovable_001"
kind = "fail"
stage = "proof_verification"
domain = "modes.sethood"
source = "fail_proof_verification_mode_sethood_unprovable_001.miz"
expected_outcome = "fail"
expected_phase = "verification"
failure_category = "proof_failure"
stable_detail_key = "modes.sethood.unprovable"
diagnostic_codes = []
spec_refs = [
  "spec.en.07.modes.sethood.property",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. The instances of a mode whose radix is bare set form a proper class, so its sethood obligation is unprovable and must fail verification (spec 7.8; the trivial thus-thesis attempt cannot discharge it)."
