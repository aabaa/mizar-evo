schema_version = 1
id = "fail_mode_property_overlap_missing_coherence_001"
kind = "fail"
stage = "advanced_semantics"
domain = "modes.property_implementation"
source = "fail_mode_property_overlap_missing_coherence_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "property_overlap_missing_coherence"
stable_detail_key = "modes.property_implementation.overlap_missing_coherence"
diagnostic_codes = []
spec_refs = [
  "spec.en.07.modes.property_implementation.coherence.semantic",
]
notes = "Inactive advanced_semantics seed: `Group` refines `UnitalMagma`, so the second `unit` property implementation overlaps the first and must carry an accepted `coherence` proof. The seed remains inactive until property-implementation parser support, the advanced_semantics runner, and property-implementation payload extraction exist (spec 7.4.1, 7.8.2)."
