schema_version = 1
id = "fail_type_elaboration_mode_dependent_arity_mismatch_001"
kind = "fail"
stage = "type_elaboration"
domain = "modes.dependent"
source = "fail_type_elaboration_mode_dependent_arity_mismatch_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
tags = ["active_type_elaboration"]
failure_category = "type_error"
stable_detail_key = "modes.dependent.argument_arity_mismatch"
diagnostic_codes = []
spec_refs = [
  "spec.en.07.modes.dependent.of_parameters",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. A dependent mode applied with the wrong number of arguments is rejected (spec 7.7)."
