schema_version = 1
id = "fail_mode_missing_existential_001"
kind = "fail"
stage = "advanced_semantics"
domain = "modes.existential_gating"
source = "fail_mode_missing_existential_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "missing_existential_registration"
stable_detail_key = "types.mode.missing_existential_registration"
diagnostic_codes = []
spec_refs = [
  "spec.en.07.modes.existential_gating.semantic",
]
notes = "Inactive advanced_semantics seed: `let s be tinted set` uses an attributed type with no existential cluster in scope; attributed type use is gated on inhabitation evidence (spec 17.3.4, 7.8). Task 43 leaves unattributed built-ins, accepted modes, bare structure radixes, and bare schema type parameters inside template bodies to the 17.3.4 inhabitation table, but this attributed type still requires an existential registration."
