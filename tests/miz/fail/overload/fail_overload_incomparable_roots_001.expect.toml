schema_version = 1
id = "fail_overload_incomparable_roots_001"
kind = "fail"
stage = "advanced_semantics"
domain = "overload.resolution"
source = "fail_overload_incomparable_roots_001.miz"
expected_outcome = "fail"
expected_phase = "overload_resolution"
failure_category = "overload_ambiguity"
rejection_reason = "incomparable_ordinary_roots"
stable_detail_key = "overload.roots.incomparable_ambiguity"
diagnostic_codes = []
spec_refs = [
  "spec.en.19.overload.ambiguity.semantic",
]
notes = "Inactive advanced_semantics seed: cl(giant set) contains both wide and tall, so measure(wide set) and measure(tall set) are viable incomparable roots; the site must report ambiguous overload, never pick one silently (spec 19.2.3, 19.4.4)."
