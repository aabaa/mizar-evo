schema_version = 1
id = "fail_overload_equivalent_roots_ambiguity_001"
kind = "fail"
stage = "advanced_semantics"
domain = "overload.resolution"
source = "fail_overload_equivalent_roots_ambiguity_001.miz"
expected_outcome = "fail"
expected_phase = "overload_resolution"
failure_category = "overload_ambiguity"
rejection_reason = "equivalent_ordinary_roots"
stable_detail_key = "overload.roots.equivalent_ambiguity"
diagnostic_codes = []
spec_refs = [
  "spec.en.19.overload.ambiguity.semantic",
]
notes = "Inactive advanced_semantics seed: closure makes `wide set` and `broad set` mutually at least as specific, so both `probe` roots are viable and equivalent; the site must report ambiguous overload because no allowed tie-breaker chooses between distinct equivalent ordinary roots (spec 19.2.3, 19.4.4)."
