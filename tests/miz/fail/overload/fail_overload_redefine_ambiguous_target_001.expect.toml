schema_version = 1
id = "fail_overload_redefine_ambiguous_target_001"
kind = "fail"
stage = "advanced_semantics"
domain = "overload.redefinition"
source = "fail_overload_redefine_ambiguous_target_001.miz"
expected_outcome = "fail"
expected_phase = "overload_resolution"
failure_category = "overload_ambiguity"
rejection_reason = "ambiguous_redefinition_target"
stable_detail_key = "overload.redefinition.ambiguous_target"
diagnostic_codes = []
spec_refs = [
  "spec.en.19.redefinition.target.semantic",
]
notes = "Inactive advanced_semantics seed: omitted `coherence with` target inference for a `redefine` succeeds only with one visible earlier root. `giant set` sharpens both `wide set` and `tall set` roots for `tune`, so declaration checking must report `resolve.ambiguous_redefinition_target` and must not select by declaration/import order or return type."
