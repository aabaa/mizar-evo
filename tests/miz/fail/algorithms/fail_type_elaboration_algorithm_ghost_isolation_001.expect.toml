schema_version = 1
id = "fail_type_elaboration_algorithm_ghost_isolation_001"
kind = "fail"
stage = "type_elaboration"
domain = "algorithms.ghost"
source = "fail_type_elaboration_algorithm_ghost_isolation_001.miz"
expected_outcome = "fail"
expected_phase = "elaboration"
failure_category = "type_error"
stable_detail_key = "algorithms.ghost.isolation_violation"
diagnostic_codes = []
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.20.algorithms.ghost.isolation_static",
]
notes = "Source algorithm checking and Core/CFG reject the runtime initializer dependency on a ghost local (spec 20.1.3 and 20.13.5), without obligations or VCs."
