schema_version = 1
id = "fail_type_elaboration_set_forward_reference_001"
kind = "fail"
stage = "type_elaboration"
domain = "variables.local_constant"
source = "fail_type_elaboration_set_forward_reference_001.miz"
expected_outcome = "fail"
expected_phase = "resolve"
failure_category = "resolve_error"
stable_detail_key = "variables.local_constant.forward_reference"
diagnostic_codes = []
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.04.variables.local_constant_set.semantic",
]
notes = "Step 5C.1 active semantic oracle; outcome, phase, key, and backlinks remain frozen. A local constant body may not reference a local constant introduced later in the same reasoning sequence (spec 4.4, 4.6)."
