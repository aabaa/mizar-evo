schema_version = 1
id = "fail_type_elaboration_template_arity_mismatch_001"
kind = "fail"
stage = "type_elaboration"
domain = "templates.instantiation"
source = "fail_type_elaboration_template_arity_mismatch_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
stable_detail_key = "templates.argument.arity_mismatch"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.instantiation.arity",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. A template instantiation must supply exactly the declared template loci (spec 18.x)."
