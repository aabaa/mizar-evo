schema_version = 1
id = "fail_type_elaboration_template_bound_violation_001"
kind = "fail"
stage = "type_elaboration"
domain = "templates.bounded_parameter"
source = "fail_type_elaboration_template_bound_violation_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
stable_detail_key = "templates.argument.bound_violation"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.type_parameter.extends_bound",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. A template argument must satisfy the declared structure bound; object supplies no BoundBox structure view (spec 18.2.2, 18.10.2)."
