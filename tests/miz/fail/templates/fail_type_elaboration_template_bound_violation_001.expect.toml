schema_version = 1
id = "fail_type_elaboration_template_bound_violation_001"
kind = "fail"
stage = "type_elaboration"
domain = "templates.bounded_parameter"
source = "fail_type_elaboration_template_bound_violation_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
stable_detail_key = "templates.argument.bound_violation"
diagnostic_codes = []
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.18.templates.type_parameter.extends_bound",
]
notes = "Source checking rejects both authenticated builtin-object actuals against the actual structure bound; unsupported or malformed calls cannot supply this diagnostic."
