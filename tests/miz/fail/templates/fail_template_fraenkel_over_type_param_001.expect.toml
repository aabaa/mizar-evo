schema_version = 1
id = "fail_template_fraenkel_over_type_param_001"
kind = "fail"
stage = "advanced_semantics"
domain = "templates.sethood_gating"
source = "fail_template_fraenkel_over_type_param_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "missing_sethood_evidence"
stable_detail_key = "types.template.fraenkel_type_param_sethood"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.encoding_soundness.semantic",
]
notes = "Inactive advanced_semantics seed: a Fraenkel comprehension whose generator ranges over a bare template type parameter must be rejected during symbolic verification — no sethood evidence exists for T, and admitting it would let `para[set]` build the Russell class (spec 18.10.2, 13.4.2, 7.8.1). Template-encoding audit finding F5."
