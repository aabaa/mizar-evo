schema_version = 1
id = "fail_template_type_actual_missing_existential_001"
kind = "fail"
stage = "advanced_semantics"
domain = "templates.type_actual_gating"
source = "fail_template_type_actual_missing_existential_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "missing_existential_registration"
stable_detail_key = "types.template.type_actual_missing_existential"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.encoding_soundness.semantic",
]
notes = "Inactive advanced_semantics seed: `Inhab[hollow set]` passes an attributed type expression with no existential cluster as a template type actual; type actuals are gated on inhabitation evidence because the schema context assumes each type parameter inhabited (spec 18.10.2, 17.3.4). Template-encoding audit finding F2."
