schema_version = 1
id = "fail_template_func_actual_result_widening_001"
kind = "fail"
stage = "advanced_semantics"
domain = "templates.scheme_actual_signature"
source = "fail_template_func_actual_result_widening_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "incompatible_scheme_actual_signature"
stable_detail_key = "types.template.func_actual_result_widening"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.encoding_soundness.semantic",
]
notes = "Inactive advanced_semantics seed: `StaysSmall[wrap]` instantiates a func(set) -> small set parameter with a deffunc whose declared result type `set` does not widen to `small set`; the covariant-codomain check must reject the actual so the typing guard is never asserted unproved (spec 18.10.4, 18.9). Template-encoding audit finding F4."
