schema_version = 1
id = "fail_template_inference_declared_type_ambiguity_001"
kind = "fail"
stage = "advanced_semantics"
domain = "templates.inference"
source = "fail_template_inference_declared_type_ambiguity_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "ambiguous_template_inference"
stable_detail_key = "template.inference.ambiguous_declared_type"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.inference_determinism.semantic",
]
notes = "Inactive advanced_semantics seed: omitted `[T]` for `pair_as(w, b)` leaves declared-type candidates `wide set` and `broad set` for the same type parameter. Even though registrations make their closures equivalent, §18.2.7 inference uses mode-unfolded declared types only, without cluster expansion, widening search, or inferred qua views; explicit `[T]` is required. Template-encoding audit finding F7."
