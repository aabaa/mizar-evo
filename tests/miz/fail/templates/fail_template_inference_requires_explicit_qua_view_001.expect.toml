schema_version = 1
id = "fail_template_inference_requires_explicit_qua_view_001"
kind = "fail"
stage = "advanced_semantics"
domain = "templates.inference"
source = "fail_template_inference_requires_explicit_qua_view_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "ambiguous_template_inference"
stable_detail_key = "template.inference.explicit_qua_view_required"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.inference_determinism.semantic",
]
notes = "Inactive advanced_semantics seed: omitted `[T]` for `point_witness(D)` must not infer a `qua` view of `D`. `BiPointStr` has distinct left and right paths to `PointedStr`; §18.2.7 requires explicit `[D qua ...]` rather than silently choosing an inherited view during template inference. Template-encoding audit finding F7."
