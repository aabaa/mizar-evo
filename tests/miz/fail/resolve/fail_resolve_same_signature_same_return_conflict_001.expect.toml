schema_version = 1
id = "fail_resolve_same_signature_same_return_conflict_001"
kind = "fail"
stage = "declaration_symbol"
domain = "resolve.signature_conflict"
source = "fail_resolve_same_signature_same_return_conflict_001.miz"
expected_outcome = "fail"
expected_phase = "resolve"
failure_category = "resolve_error"
rejection_reason = "same_signature_definition_conflict"
stable_detail_key = "declaration_symbol.signature.same_signature_definition_conflict"
diagnostic_codes = []
spec_refs = [
  "spec.en.19.overload.definition_conflict.same_return.declaration",
]
notes = "Inactive declaration_symbol seed: two gauge definitions share the same resolver-owned argument signature key and the same return signature. Task 37 generalizes the spec 19 declaration conflict to identical argument signatures regardless of return type, but active resolver support is deferred because the current active declaration_symbol runner only diagnoses different-return signature conflicts."
