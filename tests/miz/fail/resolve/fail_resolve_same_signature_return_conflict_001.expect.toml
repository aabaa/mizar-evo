schema_version = 1
id = "fail_resolve_same_signature_return_conflict_001"
kind = "fail"
stage = "declaration_symbol"
domain = "resolve.signature_conflict"
source = "fail_resolve_same_signature_return_conflict_001.miz"
expected_outcome = "fail"
expected_phase = "resolve"
failure_category = "resolve_error"
rejection_reason = "same_signature_return_conflict"
stable_detail_key = "declaration_symbol.signature.same_signature_return_conflict"
diagnostic_codes = []
diagnostic_payloads = [
  "declaration_symbol.signature.same_signature_return_conflict",
]
tags = ["active_declaration_symbol"]
spec_refs = [
  "spec.en.19.overload.definition_conflict.declaration",
]
notes = "Active declaration_symbol fail coverage for a parser-backed functor signature conflict: two gauge definitions share the same resolver-owned argument signature key but advertise different return signatures. This is a declaration/signature collection conflict, not overload winner selection."
