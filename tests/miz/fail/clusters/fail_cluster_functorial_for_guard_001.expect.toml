schema_version = 1
id = "fail_cluster_functorial_for_guard_001"
kind = "fail"
stage = "advanced_semantics"
domain = "clusters.functorial_for_guard"
source = "fail_cluster_functorial_for_guard_001.miz"
expected_outcome = "fail"
expected_phase = "cluster_resolution"
failure_category = "type_error"
rejection_reason = "functorial_for_guard_unavailable"
stable_detail_key = "registration.cluster.functorial_for_guard_unavailable"
diagnostic_codes = []
spec_refs = [
  "spec.en.17.clusters.functorial_for_guard.semantic",
]
notes = "Inactive advanced_semantics seed: `payload(G)` is declared with result type plain `set`, so the `for warm set` functorial cluster is not applicable at `UsePayloadDef` even though the radix matches; the consequent `bright` must be unavailable unless the result is already known as the full guarded type expression or a subtype (spec 17.5, 17.9.3)."
