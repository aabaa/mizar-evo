schema_version = 1
id = "fail_structure_inherit_cycle_001"
kind = "fail"
stage = "advanced_semantics"
domain = "structures.inheritance"
source = "fail_structure_inherit_cycle_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "inheritance_cycle"
stable_detail_key = "structures.inherit.cycle"
diagnostic_codes = []
spec_refs = [
  "spec.en.05.structures.inheritance.semantic",
]
notes = "Inactive advanced_semantics seed: mutually recursive inherit declarations create a cyclic ancestry; the inherit closure must stay well-founded (spec 5.3; qua-encoding cycle-freedom axioms, spec 13.8.7)."
