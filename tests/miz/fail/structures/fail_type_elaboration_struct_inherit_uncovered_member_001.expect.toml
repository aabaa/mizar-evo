schema_version = 1
id = "fail_type_elaboration_struct_inherit_uncovered_member_001"
kind = "fail"
stage = "type_elaboration"
domain = "structures.inheritance"
source = "fail_type_elaboration_struct_inherit_uncovered_member_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
stable_detail_key = "structures.inherit.uncovered_base_member"
diagnostic_codes = []
spec_refs = [
  "spec.en.05.structures.inherit.member_coverage",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. Every base member must be inherited by name match or rename; an uncovered base field is rejected (spec 5.3; sample sorted.miz comment)."
