schema_version = 1
id = "fail_type_elaboration_struct_diamond_inconsistent_001"
kind = "fail"
stage = "type_elaboration"
domain = "structures.inheritance"
source = "fail_type_elaboration_struct_diamond_inconsistent_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
stable_detail_key = "structures.inherit.diamond_inconsistency"
diagnostic_codes = []
spec_refs = [
  "spec.en.05.structures.inherit.diamond_consistency",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. Diamond inheritance is rejected when two distinct top members collapse onto one base member through different paths (spec 5.4)."
