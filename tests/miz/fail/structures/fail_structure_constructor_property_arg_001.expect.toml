schema_version = 1
id = "fail_structure_constructor_property_arg_001"
kind = "fail"
stage = "advanced_semantics"
domain = "structures.constructor"
source = "fail_structure_constructor_property_arg_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "property_constructor_argument"
stable_detail_key = "structures.constructor.property_argument_rejected"
diagnostic_codes = []
spec_refs = [
  "spec.en.05.structures.constructor_fields_only.semantic",
  "spec.en.07.modes.property_implementation.not_constructor_source.semantic",
]
notes = "Inactive advanced_semantics seed: `unit` is declared as a property, so `UnitalMagmaStr(..., unit: e)` must be rejected before any constructor or kernel axiom can treat `e` as a stored component. Property values come only from property implementations (spec 5.5.1, 5.8.4, 7.4.1)."
