schema_version = 1
id = "fail_structure_inherit_uncovered_member_001"
kind = "fail"
stage = "advanced_semantics"
domain = "structures.inheritance"
source = "fail_structure_inherit_uncovered_member_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "uncovered_base_member"
stable_detail_key = "structures.inherit.uncovered_member"
diagnostic_codes = []
spec_refs = [
  "spec.en.05.structures.inheritance.semantic",
]
notes = "Inactive advanced_semantics seed: the explicit inherit block leaves base field `payload` unmapped; all base fields and properties must be covered (spec 5.3.1)."
