schema_version = 1
id = "fail_structure_inherit_duplicate_member_coverage_001"
kind = "fail"
stage = "advanced_semantics"
domain = "structures.inheritance"
source = "fail_structure_inherit_duplicate_member_coverage_001.miz"
expected_outcome = "fail"
expected_phase = "type_check"
failure_category = "type_error"
rejection_reason = "duplicate_base_member_coverage"
stable_detail_key = "structures.inherit.duplicate_member_coverage"
diagnostic_codes = []
spec_refs = [
  "spec.en.05.structures.inheritance.semantic",
]
notes = "Inactive advanced_semantics seed: the explicit inherit block maps parent field `payload` twice, through `payload` and `payload_alias`; every base field/property must be covered exactly once by an inherit declaration (spec 5.3.1)."
