schema_version = 1
id = "pass_parser_type_expressions_001"
kind = "pass"
stage = "parse_only"
domain = "parser.type_expressions"
source = "pass_parser_type_expressions_001.miz"
expected_outcome = "pass"
expected_phase = "parse"
diagnostic_codes = []
tags = ["active_parse_only"]
spec_refs = [
  "spec.en.03.type_expressions.parser",
]
notes = "Active parse-only runner coverage for parser task 8 reserve-hosted type expressions, attribute chains, of/over arguments, bracket type arguments, and qua_arg placeholders. Local parameter-prefix preservation is covered by parser unit tests because the current frontend lexer does not expose standalone hyphen-split tokens."
