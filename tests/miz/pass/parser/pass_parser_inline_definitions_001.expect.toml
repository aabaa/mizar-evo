schema_version = 1
id = "pass_parser_inline_definitions_001"
kind = "pass"
stage = "parse_only"
domain = "parser.statements"
source = "pass_parser_inline_definitions_001.miz"
expected_outcome = "pass"
expected_phase = "parse"
diagnostic_codes = []
tags = ["active_parse_only"]
spec_refs = [
  "spec.en.15.statements.syntax",
]
notes = "Active parse-only runner coverage for parser task 21 local inline definitions: typed and zero-argument deffunc/defpred statements plus definitions inside a reasoning body."
