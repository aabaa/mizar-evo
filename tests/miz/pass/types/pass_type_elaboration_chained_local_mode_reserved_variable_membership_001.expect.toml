schema_version = 1
id = "pass_type_elaboration_chained_local_mode_reserved_variable_membership_001"
kind = "pass"
stage = "type_elaboration"
domain = "checker.type_elaboration"
source = "pass_type_elaboration_chained_local_mode_reserved_variable_membership_001.miz"
expected_outcome = "pass"
expected_phase = "type_check"
diagnostic_codes = []
diagnostic_payloads = []
tags = ["active_type_elaboration"]
spec_refs = [
  "spec.en.04.variables_and_constants.reserved_theorem_variable_type_elaboration",
  "spec.en.13.term_expressions.type_elaboration_payload_gap",
  "spec.en.14.formulas.term_formula_payload_gap",
  "spec.en.16.theorems_and_proofs.term_formula_payload_gap",
  "spec.en.checker.type_elaboration.local_mode_expansion_chain_source_bridge",
  "spec.en.checker.type_elaboration.chained_local_mode_reserved_variable_membership_source_bridge",
]
notes = "Spec-derived Task 141 intent for the exact one-edge set-terminal local-mode-chain left reserved-variable membership with an independently reserved builtin-set right operand. The left result retains its written ChainModeMembership symbol and range, while the right result and sole expected-set input retain the explicit set reserve range. Two real AST-derived expansions normalize the left outer-mode result through BaseModeMembership to builtin set; the right builtin-set roles normalize directly, and all three roles intern to one identity canonically anchored at the terminal set RHS. The checker resolves x/y to BindingId(0/1), records two Inferred variables and one fact-free Checked Membership, and projects no expected type onto the left. This does not credit mode declaration checking/acceptance/inhabitation, membership truth/facts, implicit closure/order, theorem acceptance, formula_statement, proof, CoreIr, ControlFlowIr, or VC."
