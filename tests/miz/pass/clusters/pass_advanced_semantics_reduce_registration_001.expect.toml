schema_version = 1
id = "pass_advanced_semantics_reduce_registration_001"
kind = "pass"
stage = "advanced_semantics"
domain = "clusters.reduction"
source = "pass_advanced_semantics_reduce_registration_001.miz"
expected_outcome = "pass"
expected_phase = "cluster_resolution"
diagnostic_codes = []
spec_refs = [
  "spec.en.17.clusters.reduce.registration",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. A reduction registration rewrites a definitionally transparent functor application to its simpler operand (spec 17.6)."
