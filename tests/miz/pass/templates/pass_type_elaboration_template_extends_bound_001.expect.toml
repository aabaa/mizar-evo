schema_version = 1
id = "pass_type_elaboration_template_extends_bound_001"
kind = "pass"
stage = "type_elaboration"
domain = "templates.bounded_parameter"
source = "pass_type_elaboration_template_extends_bound_001.miz"
expected_outcome = "pass"
expected_phase = "type_check"
diagnostic_codes = []
spec_refs = [
  "spec.en.18.templates.type_parameter.extends_bound",
]
notes = "Audit-1 semantic-bridge inactive oracle seed (September 2026 audit wave): binding expectation for the Step-5 semantic bridge; no active runner tag, earns no current credit. A template type parameter with an extends bound admits widening its instances to the bound (spec 18.x; A.18 bound_type)."
