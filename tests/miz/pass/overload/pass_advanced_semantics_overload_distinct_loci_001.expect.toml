schema_version = 1
id = "pass_advanced_semantics_overload_distinct_loci_001"
kind = "pass"
stage = "advanced_semantics"
domain = "overload.resolution"
source = "pass_advanced_semantics_overload_distinct_loci_001.miz"
expected_outcome = "pass"
expected_phase = "overload_resolution"
diagnostic_codes = []
tags = ["active_advanced_semantics"]
spec_refs = [
  "spec.en.19.overload.resolution.distinct_loci",
]
notes = "Step-5 source overload execution: both ordinary functor roots remain candidates; actual set arguments select the set root at both applications (spec 19.4). No proof or registration acceptance is claimed."
