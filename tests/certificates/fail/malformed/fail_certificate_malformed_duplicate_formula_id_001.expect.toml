schema_version = 1
id = "fail_certificate_malformed_duplicate_formula_id_001"
kind = "fail"
stage = "advanced_semantics"
domain = "certificate.envelope"
source = "fail_certificate_malformed_duplicate_formula_id_001.cert.json"
expected_outcome = "fail"
expected_phase = "certificate_check"
failure_category = "certificate_rejection"
rejection_reason = "malformed_certificate"
stable_detail_key = "certificate.malformed.duplicate_formula_id"
diagnostic_codes = []
spec_refs = [
  "spec.en.architecture_15.kernel_evidence.envelope_rejection",
]
notes = "Two formula evidence entries reuse formula_id 1 with different formulas. Duplicate ids must reject: two readers must never disagree about which formula an id denotes. Refs: formula_evidence.md 'Canonical V1 Envelope' (duplicate ids reject); soundness_argument.md invariant E3."
